---
title: Channel-wise Retrieval for Multivariate Time Series Forecasting
title_zh: 多变量时间序列预测的逐通道检索
authors: "Junhyeok Kang, Jun Seo, Soyeon Park, Sangjun Han, Seohui Bae, Hyeokjun Choe, Soonyoung Lee"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05543v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 多变量时间序列预测的通道检索
tldr: 针对多元时间序列预测中长程依赖捕捉难及变量间异质性被忽视的问题，本文提出CRAFT框架。该框架采用通道独立检索策略，通过时域稀疏关系图初步筛选和频域谱相似度精确排序的两阶段流程，有效提取各变量特有的历史参考信息。实验证明CRAFT在多个基准数据集上超越了现有最先进模型，兼具高精度与推理效率。
selection_source: fresh_fetch
motivation: 现有的检索增强预测方法采用通道无关策略，忽视了多元时间序列中不同变量间显著的异质性和不同的周期特性。
method: 提出CRAFT框架，利用时域稀疏图进行候选剪枝，并结合频域谱相似度对各通道独立进行历史片段检索与排序。
result: 在七个公开基准数据集上的实验结果显示，CRAFT的预测准确度优于当前最先进的基线模型。
conclusion: 通道级检索能更精准地捕捉变量特有的长程依赖，是提升多元时间序列预测性能的有效途径。
---

## 摘要
多变量时间序列预测由于固定的回顾窗口，往往难以捕捉长程依赖关系。检索增强预测通过从存储中检索历史片段来解决这一问题，但现有方法依赖于通道无关策略，将相同的参考片段应用于所有变量。这忽略了变量间的异质性，即不同通道表现出截然不同的周期性和频谱特征。我们提出了 CRAFT（逐通道检索增强预测），这是一个为每个通道独立执行检索的新型框架。为了确保效率，CRAFT 采用了两阶段流水线：在时域构建稀疏关系图以剪枝无关候选样本，并在频域利用频谱相似度对参考片段进行排序，在抑制噪声的同时强调主要的周期性成分。在七个公开基准数据集上的实验表明，CRAFT 优于最先进的预测基准模型，在具备实用推理效率的同时实现了卓越的准确性。

## Abstract
Multivariate time series forecasting often struggles to capture long-range dependencies due to fixed lookback windows. Retrieval-augmented forecasting addresses this by retrieving historical segments from memory, but existing approaches rely on a channel-agnostic strategy that applies the same references to all variables. This neglects inter-variable heterogeneity, where different channels exhibit distinct periodicities and spectral profiles. We propose CRAFT (Channel-wise retrieval-augmented forecasting), a novel framework that performs retrieval independently for each channel. To ensure efficiency, CRAFT adopts a two-stage pipeline: a sparse relation graph constructed in the time domain prunes irrelevant candidates, and spectral similarity in the frequency domain ranks references, emphasizing dominant periodic components while suppressing noise. Experiments on seven public benchmarks demonstrate that CRAFT outperforms state-of-the-art forecasting baselines, achieving superior accuracy with practical inference efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《Channel-wise Retrieval for Multivariate Time Series Forecasting》（多变量时间序列预测的逐通道检索）的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统多变量时间序列（MTS）预测模型受限于固定的“回顾窗口”（Lookback Window），难以捕捉长程依赖。虽然现有的检索增强（RAG）方法能引入历史片段，但大多采用**通道无关（Channel-agnostic）**策略，即所有变量共享同一个检索到的多变量片段。
*   **研究动机**：MTS 中的不同变量（通道）通常具有显著的**异质性**（如不同的周期、季节性和频谱特征）。强制所有通道使用相同的参考时间线会忽略变量间的差异，导致检索内容与特定通道不匹配，从而限制预测精度。

### 2. 方法论：CRAFT 框架
CRAFT（Channel-wise Retrieval-Augmented ForecasTing）提出了逐通道检索的思路，核心包含以下关键技术：
*   **两阶段检索流水线**：
    1.  **时域稀疏关系图（Sparse Relation Graph）**：为了避免全量搜索的计算开销，预先计算变量间的余弦相似度，为每个变量构建一个仅包含前 $M$ 个最相关邻居的稀疏图。检索时，仅在这些候选邻居中搜索。
    2.  **频域谱相似度（Spectral Similarity）**：在候选集中，利用快速傅里叶变换（FFT）提取低频成分（保留前 $F$ 个分量），通过归一化复数内积计算查询序列与候选片段的相似度。这能有效抑制高频噪声，聚焦于主要的周期性模式。
*   **预测集成**：
    *   **直接预测**：使用基础预测器（如两层 MLP 结合 NLinear 归一化）生成初步预测。
    *   **检索修正**：将检索到的历史未来片段进行归一化和线性投影，通过加法修正（权重系数 $\alpha$）集成到直接预测结果中。该设计具有**模型无关性**，可无缝集成到现有预测架构中。

### 3. 实验设计
*   **数据集**：使用了 7 个公开基准数据集，涵盖电力（ETTh1, ETTh2, ETTm1, ETTm2, ECL）、交通（Traffic）和天气（Weather）。
*   **预测任务**：长程预测，预测长度 $H \in \{96, 192, 336, 720\}$。
*   **对比基准（Baselines）**：对比了 8 种最先进模型，包括检索增强模型 **RAFT**，以及 **TimeMixer, PatchTST, TimesNet, MICN, DLinear, Stationary, Autoformer**。
*   **评价指标**：均方误差（MSE）和平均绝对误差（MAE）。

### 4. 资源与算力
*   **硬件设备**：实验在一台 **NVIDIA RTX 3090 GPU** 上运行。
*   **软件环境**：PyTorch 2.0.1。
*   **训练细节**：使用 Adam 优化器，学习率为 0.001，Batch Size 为 32。
*   **推理效率**：文中提到推理速度约为 0.03 秒/批次，每秒可处理超过 1000 个时间序列实例，证明了其在实时服务中的可行性。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集、4 种预测长度下进行了全面测试，并计算了平均性能。
*   **消融与分析**：
    *   进行了**候选邻居数量 $M$ 的敏感性分析**，证明即使 $M$ 很小（如 $M=3$），模型也能保持高精度。
    *   提供了**定性可视化分析**，展示了检索到的片段与真实未来值的高度一致性。
*   **充分性评价**：实验设计较为客观、公平，对比了近年来主流的 Transformer 类和线性类模型，覆盖了不同领域的数据特征。

### 6. 主要结论与发现
*   **性能领先**：CRAFT 在所有基准数据集上的平均 MSE 和 MAE 均优于现有 SOTA 模型（包括之前的检索增强模型 RAFT）。
*   **异质性处理的重要性**：实验证明，允许每个通道独立检索其特有的历史参考信息，能显著提升预测质量。
*   **效率与精度的平衡**：通过稀疏图和频域截断，CRAFT 在实现逐通道检索的同时，将复杂度控制在与变量数 $C$ 呈线性关系的水平，解决了计算瓶颈。

### 7. 优点
*   **创新性**：首次在 MTS 预测中引入逐通道检索范式，解决了变量异质性问题。
*   **高效性**：两阶段设计（稀疏图剪枝 + 频域排序）极大地降低了检索开销。
*   **通用性**：作为一种加法修正模块，它可以轻松集成到任何现有的预测模型中，无需大幅改动原模型架构。

### 8. 不足与局限
*   **超参数敏感性**：权重系数 $\alpha$ 被设定为固定值（0.001），在不同数据集或动态环境下，可能需要更复杂的自适应权重机制。
*   **基础模型较简单**：实验中使用的直接预测器仅为简单的两层 MLP，虽然证明了检索的增益，但未深入探讨与更复杂的 Backbone（如 PatchTST）结合后的表现。
*   **存储开销**：虽然推理高效，但构建和存储通道级知识库（Knowledge Base）在处理超大规模变量或极长历史数据时，可能会带来额外的内存/显存压力。

（完）
