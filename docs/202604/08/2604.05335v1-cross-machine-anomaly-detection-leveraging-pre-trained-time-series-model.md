---
title: Cross-Machine Anomaly Detection Leveraging Pre-trained Time-series Model
title_zh: 利用预训练时间序列模型的跨机器异常检测
authors: "Yangmeng Li, Kei Sano, Toshihiro Kitao, Ryoji Anzaki, Yukiya Saitoh, Hironori Moki, Dragan Djurdjanovic"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05335v1"
tags: ["query:ts-fm"]
score: 10.0
evidence: 利用预训练时序基础模型MOMENT进行异常检测
tldr: 本研究针对制造业中同型号机器间行为差异导致的异常检测难题，提出了一种跨机器时间序列异常检测框架。该框架利用预训练大模型MOMENT提取特征，并通过随机森林分类器将嵌入向量解耦为机器相关和工况相关特征，从而提取出跨机器的领域不变表示。实验证明，该方法在工业数据集上的表现优于原始信号和基础模型嵌入，显著提升了异常检测在未见目标机器上的泛化能力。
selection_source: fresh_fetch
motivation: 解决同型号机器在执行相同工序时因个体差异导致的异常检测模型泛化性差的问题。
method: 利用预训练模型MOMENT提取特征，并结合随机森林分类器解耦出与具体机器无关的领域不变特征。
result: 在包含三台不同机器的工业数据集上，该方法在跨机器异常检测任务中优于基于原始信号和基础MOMENT嵌入的基准模型。
conclusion: 通过解耦特征提取领域不变表示，能有效增强时间序列异常检测模型在不同机器间的迁移与泛化能力。
---

## 摘要
实现具有韧性和高质量的制造需要可靠的数据驱动异常检测方法，这些方法能够处理名义上相同且执行相同过程的不同个体机器之间的行为差异。为了解决利用从执行相同程序的多个不同个体机器收集的传感器数据来检测某台机器异常的问题，本文提出了一种跨机器时间序列异常检测框架，该框架集成了一个领域不变特征提取器和一个无监督异常检测模块。利用预训练基础模型 MOMENT，该提取器采用随机森林分类器将嵌入向量解耦为机器相关特征和工况相关特征，后者作为对个体机器差异具有不变性的表示。这些精炼的特征使下游异常检测器能够有效地泛化到未见的候选机器。在从执行名义上相同操作的三台不同机器收集的工业数据集上进行的实验表明，所提出的方法优于基于原始信号和基于 MOMENT 嵌入特征的基准模型，证实了其在增强跨机器泛化方面的有效性。

## Abstract
Achieving resilient and high-quality manufacturing requires reliable data-driven anomaly detection methods that are capable of addressing differences in behaviors among different individual machines which are nominally the same and are executing the same processes. To address the problem of detecting anomalies in a machine using sensory data gathered from different individual machines executing the same procedure, this paper proposes a cross-machine time-series anomaly detection framework that integrates a domain-invariant feature extractor with an unsupervised anomaly detection module. Leveraging the pre-trained foundation model MOMENT, the extractor employs Random Forest Classifiers to disentangle embeddings into machine-related and condition-related features, with the latter serving as representations which are invariant to differences between individual machines. These refined features enable the downstream anomaly detectors to generalize effectively to unseen target machines. Experiments on an industrial dataset collected from three different machines performing nominally the same operation demonstrate that the proposed approach outperforms both the raw-signal-based and MOMENT-embedding feature baselines, confirming its effectiveness in enhancing cross-machine generalization.

---

## 论文详细总结（自动生成）

这篇论文针对工业制造中“跨机器”异常检测的泛化性难题，提出了一种结合预训练时序基础模型与特征解耦技术的创新框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在工业场景中，即使是型号完全相同的机器，由于环境、磨损、设置等微小差异，其传感器数据分布也会存在显著不同（即“领域偏移”或“分布外”问题）。
*   **研究动机**：传统的异常检测模型在某台机器上训练后，直接应用于另一台未见过的机器时效果往往大幅下降。由于获取目标机器的标注数据成本极高，研究如何提取“领域不变（Domain-Invariant）”特征以实现跨机器的泛化具有重要的现实意义。

### 2. 提出的方法论
该框架的核心思想是利用预训练大模型的表征能力，并通过特征选择剔除与特定机器相关的干扰信息。
*   **特征提取（MOMENT）**：利用预训练的时序基础模型 MOMENT 将多变量传感器信号转化为 1024 维的嵌入向量（Embedding）。
*   **特征解耦（核心创新）**：
    *   训练两个随机森林分类器（RFC）：**RF-Condition**（区分正常/异常工况）和 **RF-Machine**（区分数据来自哪台机器）。
    *   通过基尼不纯度衡量特征重要性，识别出对“工况分类”贡献大、但对“机器身份识别”贡献小的特征。
    *   **领域不变掩码**：若两组重要特征集的重叠度低于阈值（如 10%），则选取工况相关的特征作为“领域不变特征”。
*   **无监督异常检测（UAD）**：将精炼后的特征输入下游模型（如 Deep SVDD、Isolation Forest、Autoencoder、GANomaly）。
*   **数据增强**：采用时间平移（Time-shifting）和混合增强（Mix-up）来进一步提升模型的鲁棒性。

### 3. 实验设计
*   **数据集**：来自工业传送带系统的电机转矩和负载角速度信号。包含三台机器（M1, M2, M3），其中 M2 和 M3 作为源域（训练），M1 作为目标域（测试）。
*   **基准对比（Benchmarks）**：
    *   **输入特征对比**：原始信号（Raw signals）、原始 MOMENT 嵌入、提出的领域不变特征（DI features）。
    *   **检测算法对比**：Deep SVDD、孤立森林（Isolation Forest）、自动编码器（AE）、GANomaly。
*   **评价指标**：AUC-ROC、AUPRC、F1-score。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：提到了使用随机森林进行特征选择，以及对下游无监督模型进行了超参数网格搜索。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每种模型组合进行了 **10 次独立运行**（不同随机种子），并报告了平均值和标准差。
    *   使用了 47,000 个正常样本进行训练，测试集包含 900 个正常和 100 个异常样本。
*   **充分性评价**：实验设计较为客观，通过对比不同特征输入和多种主流异常检测算法，验证了方法的普适性。引入了 EAG（Expected Anomaly Gap）指标在无标签情况下进行超参数调优，增加了实验的严谨性。但实验仅基于单一类型的工业设备数据集，跨行业/跨任务的验证略显不足。

### 6. 主要结论与发现
*   **领域不变特征的优越性**：在所有测试的异常检测器中，使用解耦后的领域不变特征的效果均显著优于原始信号和原始 MOMENT 嵌入。
*   **算法表现差异**：**孤立森林（Isolation Forest）**在领域不变特征上表现提升最明显，AUC-ROC 和 AUPRC 达到最高。
*   **降维作用**：特征解耦不仅提升了泛化性，还通过降维使 GANomaly 等复杂模型在训练时更加稳定。
*   **原始信号失效**：直接使用原始信号进行跨机器检测几乎完全失效（F1 分数仅约 0.18）。

### 7. 优点与亮点
*   **解耦思路巧妙**：利用随机森林简单高效地实现了特征解耦，无需复杂的对抗训练或生成模型。
*   **结合基础模型**：成功将时序大模型（MOMENT）应用于特定的工业下游任务，证明了基础模型在工业领域的潜力。
*   **实用性强**：采用无监督学习框架，符合工业界缺乏异常标签的实际情况。

### 8. 不足与局限
*   **假设依赖**：该方法假设“工况特征”与“机器特征”在嵌入空间中是可分离的，且重叠度低。如果两者高度耦合，该方法可能失效。
*   **数据集单一**：实验仅在一个特定的工业数据集上完成，尚未在更多样化的 OOD 场景（如不同采样频率、不同负载）下验证。
*   **基础模型局限**：MOMENT 作为通用模型，其生成的嵌入向量可能包含大量与特定工业任务无关的噪声，完全依赖预训练模型可能存在性能瓶颈。

（完）
