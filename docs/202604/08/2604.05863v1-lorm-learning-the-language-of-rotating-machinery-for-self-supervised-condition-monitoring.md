---
title: "LoRM: Learning the Language of Rotating Machinery for Self-Supervised Condition Monitoring"
title_zh: LoRM：学习旋转机械语言以实现自监督状态监测
authors: "Xiao Qin, Xingyi Song, Tong Liu, Hatim Laalej, Zepeng Liu, Yunpeng Zhu, Ligang He"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05863v1"
tags: ["query:ts-fm"]
score: 8.0
evidence: 用于机械信号标记化和预测的自监督框架
tldr: LoRM是一个自监督框架，将旋转机械信号视为“机器语言”，通过将多模态传感器数据转化为离散Token序列并预测未来演变来实现状态监测。该方法利用预训练语言模型进行微调，避免了手工特征提取，通过跟踪Token预测误差作为健康指标。实验证明其在实时监控和跨工具泛化方面表现优异，为工业信号分析提供了新思路。
selection_source: fresh_fetch
motivation: 传统信号处理依赖手工特征且难以处理复杂多模态数据，需要一种更通用、自监督的旋转机械状态监测方法。
method: 将多模态传感器信号离散化为Token序列，并利用微调后的预训练语言模型根据历史上下文预测未来Token，以预测误差作为健康指标。
result: 在原位刀具状态监测实验中，LoRM实现了稳定的实时跟踪和强大的跨工具泛化能力。
conclusion: LoRM成功将语言建模技术应用于工业信号分析，为旋转机械的自监督状态监测提供了一个实用且高效的框架。
---

## 摘要
我们提出了 LoRM（旋转机械语言），这是一个用于多模态旋转机械信号理解和实时状态监测的自监督框架。LoRM 基于这样一种理念：旋转机械信号可以被视为一种机器语言，即局部信号可以被标记化（tokenised）为离散的符号单元，并且可以根据观察到的多传感器上下文预测其未来的演变。与依赖手工变换和特征的传统信号处理方法不同，LoRM 将多模态传感器数据重新表述为基于标记（token）的序列预测问题。对于每个数据窗口，观察到的上下文片段以连续形式保留，而每个传感通道的未来目标片段则被量化为离散标记。然后，通过在工业信号上对通用预训练语言模型进行部分微调，实现高效的知识迁移，从而避免了从头开始训练大模型的需要。最后，通过跟踪标记预测误差作为健康指标来进行状态监测，其中误差增加表示性能退化。原位刀具状态监测（TCM）实验证明了稳定的实时跟踪和强大的跨刀具泛化能力，表明 LoRM 在语言建模和工业信号分析之间架起了一座实用的桥梁。源代码可在 https://github.com/Q159753258/LormPHM 公开获取。

## Abstract
We present LoRM (Language of Rotating Machinery), a self-supervised framework for multi-modal rotating-machinery signal understanding and real-time condition monitoring. LoRM is built on the idea that rotating-machinery signals can be viewed as a machine language: local signals can be tokenised into discrete symbolic units, and their future evolution can be predicted from observed multi-sensor context. Unlike conventional signal-processing methods that rely on hand-crafted transforms and features, LoRM reformulates multi-modal sensor data as a token-based sequence-prediction problem. For each data window, the observed context segment is retained in continuous form, while the future target segment of each sensing channel is quantised into a discrete token. Then, efficient knowledge transfer is achieved by partially fine-tuning a general-purpose pre-trained language model on industrial signals, avoiding the need to train a large model from scratch. Finally, condition monitoring is performed by tracking token-prediction errors as a health indicator, where increasing errors indicate degradation. In-situ tool condition monitoring (TCM) experiments demonstrate stable real-time tracking and strong cross-tool generalisation, showing that LoRM provides a practical bridge between language modelling and industrial signal analysis. The source code is publicly available at https://github.com/Q159753258/LormPHM.

---

## 论文详细总结（自动生成）

这是一份关于论文《LoRM: Learning the Language of Rotating Machinery for Self-Supervised Condition Monitoring》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：旋转机械（如电机、涡轮、机床）在工业中至关重要，其故障会导致巨大经济损失。传统的监测方法高度依赖专家知识和手工提取的特征，且难以有效融合来自振动、声学、力等不同传感器的异构多模态数据。
*   **核心问题**：如何构建一个通用的、自监督的框架，能够自动学习多模态工业信号的内在规律，并在无需大量故障标签的情况下实现精准的实时状态监测？
*   **整体含义**：论文提出了 **LoRM（旋转机械语言）** 概念，将机械信号类比为语言。它认为局部信号模式可以被标记化（Tokenized）为离散符号，而机器状态的演变则遵循类似于语言语法的上下文逻辑。

### 2. 方法论
*   **核心思想**：将多模态传感器数据重新表述为一个基于标记的序列预测问题。
*   **关键技术细节**：
    *   **上下文-目标划分**：将每个信号窗口分为“上下文片段”（连续波形，作为输入）和“目标片段”（未来的信号，作为预测目标）。
    *   **信号标记化（Tokenization）**：对每个传感通道独立使用 **K-means 聚类** 构建码本（Codebook），将连续的目标片段量化为离散的 Token。
    *   **多传感器融合（代码切换启发）**：将不同通道的信号切片（Patches）按预定义顺序平铺成一个统一序列，模拟多语言交流中的“代码切换”，使模型能同时建模时间和跨通道依赖。
    *   **模型架构**：采用预训练语言模型（如 **GPT-2**）作为骨干网络。通过线性嵌入层将信号 Patch 映射到隐藏空间，利用自注意力机制提取特征。
    *   **自监督学习**：通过预测未来 Token 的交叉熵损失进行训练。
    *   **健康指标（HI）**：在监测阶段，跟踪 Token 的预测误差。当机器退化时，信号偏离正常模式，预测误差随之增大，从而触发报警。

### 3. 实验设计
*   **实验场景**：英国谢菲尔德大学 AMRC 的原位刀具状态监测（TCM）。使用 5 轴 CNC 铣床进行全寿命周期磨损实验。
*   **数据集**：包含 11 个通道（3 轴主轴振动、3 轴工作台振动、3 轴切削力、功率、声学信号）。涉及 3 把刀具（T1, T2, T3）的磨损过程。
*   **Benchmark 与对比方法**：
    *   **传统统计方法**：基于马氏距离的统计特征方法。
    *   **预训练模型变体**：BERT、T5、Whisper（语音预训练）。
    *   **消融实验**：从头训练的 GPT-2（无预训练权重）。
    *   **时间序列模型**：GPT4TS（基于连续回归的方法）。

### 4. 资源与算力
*   **硬件环境**：Ubuntu 系统，24 GB 内存，**NVIDIA GeForce RTX 4090 GPU**。
*   **训练策略**：采用了**部分微调（Partial Fine-tuning）**策略，冻结了 GPT-2 的大部分注意力层和前馈层，仅训练嵌入层、位置编码和预测头，显著降低了算力需求并提高了训练效率。
*   **时长**：文中未明确给出具体的训练总时长，但提到通过早停法（Early Stopping）控制训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了跨刀具验证（例如：在 T1 上训练，在 T2/T3 上测试），这在工业场景中极具挑战性。
    *   对比了 4 种不同架构的预训练模型（GPT-2, BERT, T5, Whisper）。
    *   进行了离散 Token 预测与连续波形回归的对比实验。
*   **充分性评价**：实验设计较为充分，涵盖了从模型架构选择、预训练作用到预测目标形式（离散 vs 连续）的深度分析。跨刀具的泛化性测试证明了该方法的客观性和实用价值。

### 6. 主要结论与发现
*   **离散化优势**：将连续信号转化为离散 Token 预测比直接进行连续波形回归（如 GPT4TS）更稳定，对噪声和振幅波动的敏感度更低。
*   **预训练价值**：使用通用领域（文本/语音）预训练的模型比从头训练的模型具有更强的跨工具泛化能力，说明大规模序列建模能力可以迁移到工业信号中。
*   **架构敏感性**：自回归模型（如 GPT-2）在处理此类预测任务时优于掩码模型（如 BERT）。
*   **实时监测能力**：LoRM 能够生成平滑的健康指标曲线，准确识别刀具磨损超过 ISO 标准（300μm）的时刻。

### 7. 优点
*   **创新视角**：将工业信号处理转化为“机器语言”建模，为多模态融合提供了统一且简洁的框架。
*   **自监督学习**：无需人工标注故障标签，仅需正常运行数据即可训练，极具工程落地潜力。
*   **泛化性强**：通过部分微调预训练模型，实现了优秀的跨设备/跨工具监测效果。

### 8. 不足与局限
*   **码本依赖**：K-means 聚类生成的码本质量直接影响 Token 的代表性，对于极度复杂的非平稳信号，固定码本可能不足。
*   **超参数敏感性**：窗口长度、Patch 大小和聚类数 $K$ 的选择仍带有一定的经验性。
*   **应用限制**：目前主要在铣削加工场景验证，对于转速剧烈波动或极低速旋转机械的适用性尚待进一步探索。

（完）
