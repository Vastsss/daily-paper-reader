---
title: Physics-Aware LLM-Based Probabilistic Wind Power Scenario Generation under Extreme Icing Conditions
authors: "Lei Wang, Ying Zhang, Di Shi, Fei Ding"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23524v1"
tags: ["query:llm-wd"]
score: 10.0
evidence: 物理感知的LLM用于风力发电场景生成和预测
tldr: 针对极端结冰条件下风电出力不确定性难以刻画的问题，本文提出一种基于物理感知的LLM框架。该框架结合SCADA物理建模、多模态分词与因果Transformer架构，通过自回归方式生成概率场景。引入物理感知解码方案以约束额定功率和爬坡率，确保生成轨迹的物理一致性。实验证明该方法能高保真还原结冰导致的功率衰减，显著提升了电力系统的韧性评估与恢复规划能力。
selection_source: fresh_fetch
motivation: 准确刻画极端结冰及灾后条件下的风电出力不确定性，是实现电力系统韧性运行的关键挑战。
method: 提出一种集成SCADA物理建模、多模态分词与因果Transformer的物理感知大语言模型框架，并采用物理约束解码方案。
result: 实验结果显示，该方法能有效重现极端天气下结冰引起的功率下降和时间变异性，生成具有高保真度的物理一致场景。
conclusion: 该研究通过提供高质量的风电出力场景，为高比例新能源电力系统的韧性评估和灾后恢复提供了重要工具。
---

## Abstract
Accurately characterizing wind power uncertainty under icing and post-disaster conditions remains a critical challenge for resilient power system operation. To address this issue, this paper proposes a physics-aware large language model (LLM) framework for probabilistic wind power scenario generation under extreme icing conditions. The proposed framework integrates supervisory control and data acquisition (SCADA)-based physical modeling, multimodal tokenization, and a causal Transformer architecture trained in an autoregressive manner. A physics-aware decoding scheme effectively enforces rated power limits and ramping constraints on the generated trajectories while preserving stochastic diversity. Case studies using real wind turbine data show that the proposed method reproduces icing-induced power degradation and temporal variability observed during extreme weather. The resulting scenarios are physically consistent and high-fidelity, thereby significantly enhancing resilience assessment and recovery planning in renewable-integrated power systems.

---

## 论文详细总结（自动生成）

这篇论文提出了一种基于物理感知大语言模型（LLM）的框架，专门用于在极端结冰条件下生成风力发电的概率场景。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在极端结冰天气下，风力发电机叶片结冰会导致空气动力学性能下降和严重的功率损失。现有的场景生成方法（如VAE、GAN或扩散模型）通常针对正常运行条件设计，难以捕捉结冰引起的非平稳、非线性功率衰减动态，且生成的场景往往违反物理约束（如额定功率限制和爬坡率限制）。
*   **研究背景**：准确刻画灾后风电出力不确定性对于电力系统的韧性评估、恢复优化和安全运行至关重要。

### 2. 方法论
该框架集成了物理建模、多模态分词和因果Transformer架构，核心步骤包括：
*   **多模态分词（Multimodal Tokenization）**：将连续的SCADA数据（功率、风速、温度、桨距角等）离散化为Token。功率采用 $\mu$-law 算法量化为256个Bin以保留低出力细节；气象和运行变量根据物理范围量化为16-64个Bin。
*   **因果Transformer架构**：采用仅解码器（Decoder-only）的架构，通过自回归方式学习Token序列的概率分布。
*   **物理感知训练与解码**：
    *   **损失函数**：在交叉熵损失（$L_{CE}$）基础上，引入了功率上限损失（$L_{cap}$）、爬坡损失（$L_{ramp}$）和时间平滑度惩罚（$L_{tv}$，基于Huber损失）。
    *   **约束采样**：在推理阶段，利用核采样（Nucleus Sampling）生成候选Token，并根据物理约束（额定功率、物理基准线、爬坡率）进行剪枝，确保生成的轨迹符合物理规律。

### 3. 实验设计
*   **数据集**：使用来自中国北方两台风力发电机的真实SCADA数据，分辨率为1分钟。数据分为非结冰期（用于拟合物理基准线）和结冰期（用于训练和评估）。
*   **实验划分**：按时间顺序分为训练集（70%）、验证集（15%）和测试集（15%）。
*   **Benchmark（基准方法）**：对比了三种代表性的深度生成模型：
    1.  **CGAN**（条件生成对抗网络）
    2.  **VAE**（变分自编码器）
    3.  **Diffusion**（扩散模型）
*   **评估指标**：连续分级概率评分（CRPS）、KL散度（KLD）以及物理约束违反率（VR）。

### 4. 资源与算力
*   **硬件环境**：使用了一台配备 **Intel Xeon E5-2680 v4 CPU (2.4 GHz)** 和 **NVIDIA GeForce RTX 3080 Ti GPU** 的工作站。
*   **软件环境**：Python 3.8.2, PyTorch 1.8.0。
*   **训练时长**：文中未明确说明具体的训练总时长，但指出了模型是在GPU后端运行的。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了50组生成的风电轨迹对比，并针对高波动结冰窗口进行了敏感性分析。
*   **充分性评价**：实验涵盖了概率准确性、统计相似性和物理一致性三个维度。通过对比不同生成模型和不同约束强度（无约束、默认约束、严格约束），验证了物理感知解码的有效性。虽然数据集仅来自两个风机，但在极端天气这一特定场景下，实验设计较为客观且具有针对性。

### 6. 主要结论与发现
*   **性能优越**：LLM框架在CRPS（0.121）和KLD（0.038）指标上均优于CGAN、VAE和扩散模型，说明其概率保真度更高。
*   **物理一致性强**：引入物理约束后，违反率（VR）降至2.85%，显著低于基准模型的6.58%-7.72%。
*   **平衡多样性与合规性**：物理感知解码能在抑制非物理尖峰（如异常跳变）的同时，保留生成场景的随机多样性。

### 7. 优点（亮点）
*   **物理与数据驱动结合**：将LLM的强大序列建模能力与风机物理特性（功率曲线、爬坡限制）有机结合，解决了纯数据驱动模型“不合物理逻辑”的问题。
*   **多模态处理**：通过分词技术有效整合了气象、控制和功率等异构数据。
*   **自回归优势**：相比扩散模型，自回归LLM在处理长序列和非平稳动态时更具解释性，且便于在每一步解码时施加即时约束。

### 8. 不足与局限
*   **泛化性验证有限**：实验仅基于特定地区的两台风机，尚未验证在其他地理环境或不同类型极端天气（如暴风雨、沙尘暴）下的表现。
*   **计算开销**：LLM的自回归推理速度通常慢于VAE或GAN，对于超大规模风电场的实时场景生成可能存在计算压力。
*   **Token化偏差**：离散化过程（Bin的数量选择）可能引入量化误差，影响极低或极高功率区域的精度。

（完）
