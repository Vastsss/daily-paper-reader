---
title: "AdaMamba: Adaptive Frequency-Gated Mamba for Long-Term Time Series Forecasting"
authors: "Xudong Jiang, Mingshan Loo, Hanchen Yang, Wengen Li, Mingrui Zhang, Yichao Zhang, Jihong Guan, Shuigeng Zhou"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23239v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 长期时间序列预测框架
tldr: 针对长期时间序列预测中复杂的长程依赖和跨域异构性挑战，本文提出AdaMamba框架。该方法将自适应频率分析融入Mamba状态空间模型，通过交互式补丁编码捕获变量间动态，并设计自适应频率门控状态空间模块，实现统一的时频遗忘门机制。AdaMamba能根据输入动态调整状态转换，在保持计算效率的同时，显著提升了捕捉复杂周期模式和长程依赖的能力。
selection_source: fresh_fetch
motivation: 现有频率域预测方法往往假设跨域同质性，难以处理现实世界中变量在时域同步但在频域差异巨大的异构性问题。
method: 提出一种自适应频率门控Mamba模型，结合交互式补丁编码和统一的时频遗忘门，实现上下文感知的频率分析与状态更新。
result: 在七个公共基准测试和两个特定领域数据集上的实验表明，AdaMamba在预测精度上持续优于现有最先进方法。
conclusion: AdaMamba通过内生化自适应频率分析，有效增强了Mamba模型在处理复杂长序列预测任务中的表现和鲁棒性。
---

## Abstract
Accurate long-term time series forecasting (LTSF) requires the capture of complex long-range dependencies and dynamic periodic patterns. Recent advances in frequency-domain analysis offer a global perspective for uncovering temporal characteristics. However, real-world time series often exhibit pronounced cross-domain heterogeneity where variables that appear synchronized in the time domain can differ substantially in the frequency domain. Existing frequency-based LTSF methods often rely on implicit assumptions of cross-domain homogeneity, which limits their ability to adapt to such intricate variability. To effectively integrate frequency-domain analysis with temporal dependency learning, we propose AdaMamba, a novel framework that endogenizes adaptive and context-aware frequency analysis within the Mamba state-space update process. Specifically, AdaMamba introduces an interactive patch encoding module to capture inter-variable interaction dynamics. Then, we develop an adaptive frequency-gated state-space module that generates input-dependent frequency bases, and generalizes the conventional temporal forgetting gate into a unified time-frequency forgetting gate. This allows dynamic calibration of state transitions based on learned frequency-domain importance, while preserving Mamba's capability in modeling long-range dependencies. Extensive experiments on seven public LTSF benchmarks and two domain-specific datasets demonstrate that AdaMamba consistently outperforms state-of-the-art methods in forecasting accu racy while maintaining competitive computational efficiency. The code of AdaMamba is available at https://github.com/XDjiang25/AdaMamba.

---

## 论文详细总结（自动生成）

这是一份关于论文《AdaMamba: Adaptive Frequency-Gated Mamba for Long-Term Time Series Forecasting》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在长期时间序列预测（LTSF）中同时捕获复杂的长程依赖关系和动态的周期性模式。
*   **研究背景**：
    *   **跨域异构性（Cross-domain Heterogeneity）**：作者发现，许多变量在时域上看起来高度同步，但在频域（如功率谱密度）上表现出显著差异。
    *   **现有方法局限**：现有的频域预测方法通常假设“跨域同质性”（即时域模式等同于频域表示），且多采用静态频率假设或硬编码的频率选择（如Top-K），难以适应现实数据的复杂多变。
    *   **集成挑战**：现有的Mamba与频谱分析的结合往往是机械的堆叠，缺乏统一的理论框架来联合建模时频特征。

### 2. 方法论：AdaMamba 框架
AdaMamba 的核心思想是将自适应、上下文感知的频率分析内生化到 Mamba 的状态空间更新过程中。
*   **交互式补丁编码模块（Interactive Patch Encoding）**：
    *   利用一维卷积（Conv1D）捕获变量间的交互相关性。
    *   采用多尺度补丁（Patching）策略，将序列划分为不同长度的块，以捕获不同分辨率的局部和全局特征。
*   **自适应频率门控状态空间模块（AFSSM）**：
    *   **动态频率适配**：不使用固定的傅里叶基底，而是通过一个轻量级适配器（Adapter）根据输入特征生成动态频率偏移量（$\Delta \omega$），实现上下文感知的频率基准。
    *   **时频统一遗忘门**：将 Mamba 原有的时域选择性机制扩展为“时频统一遗忘门”。它通过外积操作结合时域特征和频域重要性，动态校准状态转移。
    *   **复数状态分解**：在状态更新中分别处理实部和虚部，并利用**振幅（Amplitude）**信息进行输出门控，过滤掉不稳定的相位（Phase）干扰。

### 3. 实验设计
*   **数据集**：
    *   **7个公共基准**：ETT（h1, h2, m1, m2）、Weather、ILI（流感）、Exchange Rate。
    *   **2个领域特定数据集**：KnowAir-v2（空气质量预测）、NOAA SST（海表温度预测）。
*   **对比基准（Baselines）**：
    *   **Mamba系**：Affirm, S_Mamba, STDMamba, TimeMachine。
    *   **Transformer/CNN/MLP系**：iTransformer, PatchTST, ModernTCN, FreTS。
*   **评估指标**：均方误差（MSE）和平均绝对误差（MAE）。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA RTX 4090 GPU** 上完成。
*   **训练细节**：Batch size 设置为 24。
*   **效率表现**：论文强调了 AdaMamba 保持了 Mamba 的**线性计算复杂度 $O(M)$**，在 Weather 数据集上的训练速度和参数量对比中，AdaMamba 在保持高精度的同时，计算开销与 Affirm 等强基准模型相当。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 9 个数据集，每个数据集包含多个预测长度（如 96, 192, 336, 720 步）。
*   **消融实验**：设计了四种变体（Case I-IV），分别验证了交互编码模块、自适应频率模块以及 Mamba 基础块的贡献。
*   **敏感性分析**：对层数、隐藏维度、学习率、多尺度补丁配置进行了详尽的参数搜索。
*   **充分性评价**：实验设计非常充分，不仅有定量指标，还包含预测曲线可视化、频率分析可视化以及计算复杂度证明，逻辑闭环完整。

### 6. 主要结论与发现
*   **性能领先**：AdaMamba 在几乎所有数据集和预测步长上均优于 SOTA 模型。特别是在 ILI 数据集上，MSE 降低了 11.47%。
*   **频率适配的有效性**：动态调整频率基底比固定频率基底更有效，能更好地应对跨域异构性。
*   **振幅 vs 相位**：研究发现，仅使用振幅信息比结合相位信息效果更好。相位对微小的时间偏移极其敏感，容易导致梯度不稳定和过拟合。

### 7. 优点与亮点
*   **理论创新**：将频率分析从“外部预处理”转变为“内生门控机制”，实现了时频特征的深度融合。
*   **解决异构性**：通过动态频率偏移解决了时域同步但频域差异大的痛点。
*   **高效性**：在显著提升精度的前提下，保留了状态空间模型（SSM）的线性推理优势，适合处理长序列。

### 8. 不足与局限
*   **相位信息的舍弃**：虽然论文解释了相位会导致不稳定，但相位在某些极度依赖精确时间对齐的场景中可能仍有潜在价值，目前模型完全舍弃了这一维度。
*   **超参数敏感性**：实验显示隐藏维度（Hidden Dimension）对性能影响较大，可能需要针对不同领域数据进行精细调优。
*   **应用范围**：目前主要集中在 LTSF 任务，尚未在多模态时空预测或大规模预训练任务中进行验证。

（完）
