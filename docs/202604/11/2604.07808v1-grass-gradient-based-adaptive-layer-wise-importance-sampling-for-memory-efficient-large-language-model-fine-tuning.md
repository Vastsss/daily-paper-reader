---
title: "GRASS: Gradient-based Adaptive Layer-wise Importance Sampling for Memory-efficient Large Language Model Fine-tuning"
title_zh: GRASS：基于梯度的自适应逐层重要性采样，用于显存高效的大语言模型微调
authors: "Kaiyuan Tian, Yu Tang, Gongqingjian Jiang, Baihui Liu, Yifu Gao, Xialin Su, Linbo Qiao, Dongsheng Li"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07808v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 大语言模型的高效内存微调
tldr: 针对大语言模型全参数微调显存消耗巨大的挑战，本文提出GRASS框架。该框架利用梯度范数作为层重要性的动态指标，实现自适应的层采样微调策略，并结合层级优化器状态卸载技术以优化显存与计算效率。实验证明，GRASS在显著降低显存占用的同时，在多个基准任务上取得了优于现有方法的性能表现，为高效微调提供了新方案。
selection_source: fresh_fetch
motivation: 现有的层级微调方法多采用静态采样，无法适应不同任务和训练阶段中各层重要性的动态变化，导致性能受限。
method: 提出一种基于梯度范数的自适应层采样框架，并引入计算与通信重叠的层级优化器状态卸载机制以降低显存。
result: "实验显示GRASS在多个模型上比现有SOTA方法平均准确率提升达4.38%，且显存占用最高减少了19.97%。"
conclusion: GRASS通过动态感知层重要性并优化显存管理，实现了兼顾显存效率与模型性能的大语言模型微调。
---

## 摘要
大语言模型的全参数微调受限于巨大的 GPU 显存需求。低秩自适应方法通过仅更新参数子集来缓解这一挑战。然而，这些方法通常会限制模型的表达能力，且性能低于全参数微调。逐层微调方法作为一种替代方案出现，通过静态层重要性采样策略实现了显存高效的训练。然而，这些方法忽略了不同任务和训练阶段中层重要性的变化，导致在下游任务上的性能欠佳。为了解决这些局限性，我们提出了 GRASS，一个基于梯度的自适应逐层重要性采样框架。GRASS 利用平均梯度范数作为一种任务感知和训练阶段感知的指标，用于估计层重要性。此外，GRASS 通过自适应训练策略动态调整层采样概率。我们还引入了一种逐层优化器状态卸载机制，通过重叠计算与通信进一步减少显存占用，同时保持相当的训练吞吐量。在多个模型和基准测试上的广泛实验表明，GRASS 一致优于现有最先进的方法，平均准确率提升高达 4.38 个百分点，显存占用减少高达 19.97%。

## Abstract
Full-parameter fine-tuning of large language models is constrained by substantial GPU memory requirements. Low-rank adaptation methods mitigate this challenge by updating only a subset of parameters. However, these approaches often limit model expressiveness and yield lower performance than full-parameter fine-tuning. Layer-wise fine-tuning methods have emerged as an alternative, enabling memory-efficient training through static layer importance sampling strategies. However, these methods overlook variations in layer importance across tasks and training stages, resulting in suboptimal performance on downstream tasks. To address these limitations, we propose GRASS, a gradient-based adaptive layer-wise importance sampling framework. GRASS utilizes mean gradient norms as a task-aware and training-stage-aware metric for estimating layer importance. Furthermore, GRASS adaptively adjusts layer sampling probabilities through an adaptive training strategy. We also introduce a layer-wise optimizer state offloading mechanism that overlaps computation and communication to further reduce memory usage while maintaining comparable training throughput. Extensive experiments across multiple models and benchmarks demonstrate that GRASS consistently outperforms state-of-the-art methods, achieving an average accuracy improvement of up to 4.38 points and reducing memory usage by up to 19.97\%.

---

## 论文详细总结（自动生成）

以下是对论文《GRASS: Gradient-based Adaptive Layer-wise Importance Sampling for Memory-efficient Large Language Model Fine-tuning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）的全参数微调（FFT）面临巨大的显存瓶颈。现有的参数高效微调（PEFT，如 LoRA）虽然节省显存，但由于低秩限制，其表达能力和性能往往不如 FFT。
*   **研究背景**：新兴的“逐层微调”方法（如 LISA）通过每轮只激活部分层来降低显存，但它们多采用**静态或任务无关**的采样策略，忽略了不同任务和训练阶段中各层重要性的动态变化，导致性能在特定任务上出现退化。
*   **研究目的**：提出一种能够感知任务和训练阶段、动态选择重要层进行更新的显存高效微调框架。

### 2. 方法论
GRASS 框架的核心思想是利用**梯度信息**作为层重要性的动态指标，其关键技术包括：
*   **平均梯度范数（MGN）作为重要性度量**：基于一阶泰勒展开，梯度的量级反映了参数更新对损失函数的影响。GRASS 通过计算各层参数梯度的平均范数（归一化处理以消除层大小差异）来量化层的重要性。
*   **自适应层采样策略**：
    1.  **探测阶段（Probing Phase）**：在训练初期进行少量步数（如150步）的完整前向和反向传播（不更新参数），收集初始 MGN。
    2.  **动态概率更新**：利用 Softmax 函数将 MGN 转换为采样概率。在训练过程中，定期（每隔 $T_s$ 步）重新采样 $\gamma$ 个层进行更新，并使用指数移动平均（EMA）更新 MGN 统计量。
*   **层级优化器状态卸载（Offloading）与重叠（Overlap）**：
    *   为了解决逐层微调中所有层的优化器状态仍需占用空间的问题，GRASS 将优化器状态存储在 CPU 中。
    *   **关键优化**：在更新当前层时，异步预取下一层的优化器状态，实现计算与通信的重叠，从而在不降低吞吐量的前提下极大减少 GPU 显存占用。

### 3. 实验设计
*   **模型规模**：涵盖了 TinyLlama (1B)、Gemma-2B 和 LLaMA2-7B 三种规模。
*   **数据集与任务**：
    *   **算术推理**：在 Math 数据集上微调，并在 MultiArith、AddSub、GSM8K、AQuA、SingleEq、SVAMP 六个基准上评估。
    *   **常识推理**：在包含 BoolQ、PIQA、SIQA、HellaSwag 等八个任务的集合上进行评估。
*   **对比方法**：包括全参数微调（FFT）、LoRA、DoRA、LISA 以及最新的层级采样方法 IST 和 OWS。

### 4. 资源与算力
*   **硬件环境**：所有实验均在 **2 台 NVIDIA H100 80G GPU** 上完成。
*   **训练细节**：论文详细列出了不同任务的超参数（如学习率、Batch Size、采样周期 $T_s$ 等）。例如，探测阶段固定为 150 步。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的对比实验，覆盖了 3 个模型和 14 个主流基准测试。
*   **消融实验**：
    *   对比了静态采样（GRASS*）与自适应采样的效果。
    *   分析了采样周期 $T_s$、激活层数 $\gamma$、探测步数 $T_p$ 对性能的影响。
    *   测试了不同随机种子下的稳定性。
*   **充分性评价**：实验设计非常充分且客观。通过跨模型、跨任务的对比，以及对显存和吞吐量的定量分析，有力地证明了该方法的有效性。

### 6. 主要结论与发现
*   **性能优越**：GRASS 在多个基准上一致优于 LoRA、DoRA 和 LISA。在某些任务上，其平均准确率比 LoRA 提升了 4.38 个百分点，甚至在部分模型上超过了 FFT（可能具有正则化效应）。
*   **显存高效**：相比 FFT，显存占用大幅降低；相比 LoRA 和 DoRA，在长文本序列下表现出更好的显存扩展性（最高减少 19.97% 显存）。
*   **动态性验证**：实验证实了层重要性确实随任务和训练阶段动态变化，GRASS 能够准确捕捉并聚焦于这些关键层。

### 7. 优点
*   **任务感知与动态性**：突破了以往静态采样的局限，使微调过程更加智能化。
*   **显存与速度兼顾**：通过层级优化器卸载和重叠技术，在极低显存消耗下保持了与 LoRA 相当的训练吞吐量。
*   **通用性强**：在不同规模的模型和不同类型的推理任务上均表现稳健。

### 8. 不足与局限
*   **计算开销**：虽然比例很小（约 2%-6%），但探测阶段和 MGN 的计算确实引入了额外的运行时间。
*   **架构限制**：目前主要在 Decoder-only 架构的语言模型上验证，尚未在 Encoder-Decoder 架构或其他模态（如视觉）模型上进行测试。
*   **超参数敏感性**：虽然论文给出了推荐值，但采样周期 $T_s$ 和激活层数 $\gamma$ 仍可能需要针对特定新任务进行微调。

（完）
