---
title: Structure-Aware Commitment Reduction for Network-Constrained Unit Commitment with Solver-Preserving Guarantees
title_zh: 具有求解器保留保证的网络约束机组组合结构感知削减框架
authors: "Guangwen Wang, Jiaqi Wu, Yang Weng, Baosen Zhang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02788v1"
tags: ["query:llm"]
score: 9.0
evidence: 大语言模型预测电力系统中的机组组合计划
tldr: 本文针对电力系统机组组合（UC）中二进制变量导致的计算复杂度难题，提出了一种结构感知的降维框架。该方法利用大语言模型（LLM）识别并预先固定一部分结构稳定的机组状态变量，而将其余变量和所有约束交由原MILP求解器处理。这种“求解器兼容”的策略在缩小搜索空间的同时，保留了物理约束的严谨性和求解器的最优性保证。实验表明，该方法在多个IEEE标准及大规模算例中实现了数量级的加速，且保持了近乎最优的经济性。
selection_source: fresh_fetch
motivation: 随着发电机组和安全约束增加，机组组合问题的分支定界搜索空间呈爆炸式增长，导致传统求解器耗时过长。
method: 提出一种降维框架，利用LLM识别并固定部分稳定的二进制决策变量，其余部分仍由MILP求解器在完整约束下进行优化。
result: 在IEEE 57/73/118节点及大规模系统测试中，该方法显著减少了搜索节点数，实现了数量级的求解加速且目标值接近最优。
conclusion: 将LLM的启发式预测与传统优化求解器相结合，能在保证物理可行性的前提下有效提升复杂电力系统调度问题的求解效率。
---

## 摘要
随着独立发电机组、混合资源和安全约束数量的增加，网络约束机组组合（UC）的计算负担显著增加，其中大部分求解时间消耗在针对“机组-小时”二进制变量的分支定界树搜索上。为了减轻这种组合负担，最近的方法探索了基于学习的引导来辅助机组组合决策。然而，直接使用大语言模型（LLM）等工具预测完整的机组计划是不可靠的，因为不可行或不一致的二进制决策可能会违反跨时段约束并降低经济最优性。本文提出了一种适用于 UC 的求解器兼容降维框架，该框架利用了机组组合决策中的结构规律。该框架并非生成完整的计划，而是在优化前识别并固定一小部分结构稳定的机组二进制变量。其中一种实现方式是使用 LLM 来选择这些变量。LLM 并不取代优化过程，而是提供部分变量限制，而所有约束和剩余决策仍由原始混合整数线性规划（MILP）求解器处理，该求解器继续执行网络、爬坡、备用和安全约束。我们从形式上证明了掩码后的问题定义了原始 UC 模型的一个缩减可行域，从而保留了可行性，并能在受限空间内实现求解器认证的最优性。在 IEEE 57 节点、RTS 73 节点、IEEE 118 节点以及扩展的大规模案例（包括安全约束变体）上的实验表明，分支定界节点数和求解时间一致减少，在复杂实例上实现了数量级的加速，同时保持了近乎最优的目标函数值。

## Abstract
The growing number of individual generating units, hybrid resources, and security constraints has significantly increased the computational burden of network-constrained unit commitment (UC), where most solution time is spent exploring branch-and-bound trees over unit-hour binary variables. To reduce this combinatorial burden, recent approaches have explored learning-based guidance to assist commitment decisions. However, directly using tools such as large language models (LLMs) to predict full commitment schedules is unreliable, as infeasible or inconsistent binary decisions can violate inter-temporal constraints and degrade economic optimality. This paper proposes a solver-compatible dimensionality reduction framework for UC that exploits structural regularities in commitment decisions. Instead of generating complete schedules, the framework identifies a sparse subset of structurally stable commitment binaries to fix prior to optimization. One implementation uses an LLM to select these variables. The LLM does not replace the optimization process but provides partial variable restriction, while all constraints and remaining decisions are handled by the original MILP solver, which continues to enforce network, ramping, reserve, and security constraints. We formally show that the masked problem defines a reduced feasible region of the original UC model, thereby preserving feasibility and enabling solver-certified optimality within the restricted space. Experiments on IEEE 57-bus, RTS 73-bus, IEEE 118-bus, and augmented large-scale cases, including security-constrained variants, demonstrate consistent reductions in branch-and-bound nodes and solution time, achieving order-of-magnitude speedups on high-complexity instances while maintaining near-optimal objective values.

---

## 论文详细总结（自动生成）

### 论文总结：具有求解器保留保证的网络约束机组组合结构感知削减框架

#### 1. 核心问题与整体含义（研究动机和背景）
机组组合（Unit Commitment, UC）是电力系统运行的核心问题，旨在确定发电机组的启停状态和出力分配。随着电网规模扩大及新能源接入，UC 问题中的二进制变量（机组-小时状态）呈爆炸式增长，导致传统混合整数线性规划（MILP）求解器在分支定界（Branch-and-Bound）搜索上耗费大量时间。
**研究动机：** 现有的机器学习辅助方法往往试图直接预测完整计划，但这容易违反复杂的物理约束（如爬坡、网络安全约束）。本文旨在结合大语言模型（LLM）的模式识别能力与传统求解器的严谨性，通过“预先固定部分稳定变量”来缩小搜索空间，同时确保物理可行性和最优性保证。

#### 2. 方法论
核心思想是**“结构感知降维”**：利用 LLM 识别在相似负荷条件下保持稳定的机组状态，并将其预先固定，剩余的复杂决策和物理约束仍由 MILP 求解器处理。
*   **核心流程：**
    1.  **上下文构建：** 将发电机参数（容量、最小启停时间等）和 24 小时负荷曲线转化为结构化 Prompt。
    2.  **LLM 掩码生成：** LLM 并不生成完整计划，而是输出一个稀疏的元组列表 $(t, g, u)$，表示在 $t$ 时刻固定机组 $g$ 的状态为 $u$。
    3.  **可行性预筛：** 在调用求解器前，进行简单的逻辑检查（如总容量是否满足负荷），防止因过度限制导致不可行。
    4.  **受限 MILP 求解：** 将固定变量作为等式约束加入原模型，由 Gurobi 等求解器在缩小的可行域 $X_F$ 内寻找最优解。
*   **理论保证：** 论文通过数学证明显示，受限问题的可行域是原问题的子集（$X_F \subseteq X$），因此求解器输出的任何解在物理上都是绝对可行的。

#### 3. 实验设计
*   **测试系统：** IEEE 57-bus、RTS 73-bus、IEEE 118-bus，以及通过增加机组数量构建的扩展系统（118-bus+20, 118-bus+30）以测试可扩展性。
*   **对比基准（Benchmark）：**
    *   **Original MILP：** 无限制的原始求解过程。
    *   **Random Freezing：** 随机固定相同比例的变量（证明结构感知的必要性）。
    *   **K-means / KNN Masking：** 基于历史数据的传统聚类和近邻匹配方法。
*   **场景设置：** 包含负荷预测不确定性实验（10%-40% 噪声）、无历史数据场景（冷启动）以及长达 365 天的连续运行模拟。

#### 4. 资源与算力
*   **LLM 模型：** 使用了 GPT-4o。
*   **优化工具：** 使用 AMPL 建模，Gurobi 作为 MILP 求解器。
*   **算力说明：** 文中未明确给出具体的硬件配置（如 CPU 型号或 GPU 数量），但强调了求解时间是基于 Gurobi 返回的内部求解时间，具有可比性。由于 LLM 通过 API 调用，其推理时间未详细计入总耗时，但论文指出 LLM 生成掩码的速度通常很快。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 57 节点到 118 节点扩展版的 5 种不同规模系统。
*   **充分性：** 实验设计较为全面。不仅对比了求解速度，还详细分析了分支定界节点数、单纯形迭代次数和目标函数偏差。
*   **客观性：** 通过 365 天的长期模拟和不同噪声水平的鲁棒性测试，证明了方法在不同工况下的稳定性。对比随机固定变量的实验有力地证明了 LLM 提取的是“结构性特征”而非盲目降维。

#### 6. 主要结论与发现
*   **显著加速：** 在复杂系统（如 RTS 73-bus）上实现了高达 **6.7 倍** 的加速，分支定界节点数显著减少。
*   **高精度：** 尽管缩小了搜索空间，但经济成本偏差（Objective Deviation）通常低于 **0.3%**，在极端噪声下也能保持在 3% 左右。
*   **冷启动能力：** 即使在没有任何历史机组计划数据的情况下，仅凭发电机参数和负荷曲线，LLM 依然能提供有效的变量固定建议。
*   **稳定性：** 在 365 天的连续模拟中，该方法消除了原始 MILP 求解中偶尔出现的“计算耗时尖峰”，使求解时间更加平稳可控。

#### 7. 优点
*   **安全性（Solver-Preserving）：** 最大的亮点是不改变原有的物理约束方程，完全保留了求解器对可行性的验证能力，避免了纯 AI 模型输出不可行解的风险。
*   **结构感知：** 成功利用了电力系统运行中的“结构稳定性”（如基荷机组通常长期开启），将启发式搜索与严谨优化完美结合。
*   **低数据依赖：** 相比需要大量标注数据的监督学习方法，该方法对历史数据的依赖较低。

#### 8. 不足与局限
*   **LLM 成本与延迟：** 依赖闭源模型 API（如 GPT-4o）可能存在网络延迟和调用成本问题，且在实时调度（如 5 分钟调度）中可能面临挑战。
*   **超参数敏感性：** 固定变量的数量上限 $K$ 需要人工设定。如果 $K$ 过大，可能导致可行域过度缩小甚至不可行；如果 $K$ 过小，加速效果不明显。
*   **网络约束覆盖：** 虽然 LLM 考虑了负荷和机组，但对复杂网络拓扑（如特定线路阻塞）的感知能力在文中未深入探讨，主要依赖求解器在后期处理这些约束。

（完）
