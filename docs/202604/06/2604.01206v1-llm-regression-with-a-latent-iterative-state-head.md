---
title: LLM REgression with a Latent Iterative State Head
title_zh: RELISH：基于潜迭代状态头的大语言模型回归
authors: "Yiheng Su, Matthew Lease"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01206v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 直接从冻结的大模型表示中预测标量值以进行回归
tldr: "针对大语言模型在文本回归任务中的局限性，本文提出了一种轻量级架构 RELISH。该方法无需将数值解码为文本，而是通过在冻结的 LLM 表示上利用交叉注意力机制迭代优化学习到的潜状态，并最终映射为标量预测值。RELISH 在多个数据集和模型上表现优于现有基准，且参数量极小（仅占 0.01-0.04%），为高效、高精度的 LLM 回归提供了新方案。"
selection_source: fresh_fetch
motivation: 现有的 LLM 回归方法在处理数值预测时，往往面临精度不足或参数开销过大的挑战。
method: 利用交叉注意力机制在冻结的 LLM 词元表示上迭代优化潜状态，并将其映射为最终的标量估计值。
result: RELISH 在多个数据集和模型上均优于自回归解码及 LoRA 等基准，且参数量仅为后者的极小部分。
conclusion: RELISH 证明了通过极少量的额外参数即可实现高性能的 LLM 文本回归，具有极高的实用价值。
---

## 摘要
我们提出了 RELISH（基于潜迭代状态头的回归），这是一种专为大语言模型文本回归设计的新型轻量级架构。RELISH 并非将数值目标解码为文本或聚合多个生成输出，而是通过对标记级表示进行交叉注意力，迭代优化学习到的潜状态，从而直接从冻结的 LLM 表示中预测标量值，最后通过线性回归器将最终状态映射为点估计。在五个数据集、四个 LLM 主干网络和两种 LLM 训练方案中，RELISH 的表现始终优于来自三大主要 LLM 回归家族的先前基准，包括自回归解码、回归感知推理以及现有的预测头方法。尽管取得了这些提升，RELISH 仍保持了极高的参数效率，在冻结的 LLM 主干上仅需 3.4-3.7M 个可训练参数（仅增加 0.01-0.04% 的额外开销），远低于随模型规模增长的基于 LoRA 的替代方案（0.26-0.42%）。

## Abstract
We present RELISH (REgression with a Latent Iterative State Head), a novel, lightweight architecture designed for text regression with large language models. Rather than decoding numeric targets as text or aggregating multiple generated outputs, RELISH predicts scalar values directly from frozen LLM representations by iteratively refining a learned latent state through cross-attention over token-level representations, and then mapping the final state to a point estimate with a linear regressor. Across five datasets, four LLM backbones, and two LLM training regimes, RELISH consistently outperforms prior baselines from all three major LLM regression families, including autoregressive decoding, regression-aware inference, and existing predictive head methods. Despite these gains, RELISH remains highly parameter-efficient, requiring only 3.4-3.7M trainable parameters across frozen LLM backbones (only 0.01-0.04% additional overhead), far less than LoRA-based alternatives that grow with model size (0.26-0.42%).

---

## 论文详细总结（自动生成）

### 论文总结：LLM REgression with a Latent Iterative State Head (RELISH)

#### 1. 核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）在生成任务上表现卓越，但在**回归任务**（预测连续数值，如语义相似度评分、翻译质量评估）中面临挑战。
*   **背景痛点**：
    *   **自回归解码**：将数字视为文本生成，忽略了数值间的连续性和邻近性。
    *   **回归感知推理（如 RAIL/RAFT）**：虽然引入了数值优化，但推理成本高（需多次采样或候选枚举）。
    *   **传统预测头**：通常采用简单的池化（如均值池化）提取特征，难以捕捉复杂的局部或分布信息。
*   **核心目标**：开发一种轻量级、高精度且推理高效（单次通过）的 LLM 回归架构。

#### 2. 方法论
*   **核心思想**：RELISH 引入了一个**潜迭代状态头**。它不改变 LLM 的主干参数（冻结状态），而是通过交叉注意力机制，让一个可学习的“潜状态”多次迭代地从 LLM 的词元级（Token-level）表示中提取关键回归信息。
*   **关键技术细节**：
    1.  **特征提取**：输入文本经过冻结的 LLM，获取倒数第二层的隐藏状态 $H(x)$。
    2.  **维度投影**：将高维的 LLM 状态投影到较小的头部维度 $d_h$（如 256），以降低计算开销。
    3.  **迭代细化**：初始化一个可学习的潜状态 $r^{(0)}$。在 $L$ 个迭代步骤中，利用 Transformer 结构的残差块，以潜状态为 Query，LLM 词元表示为 Key 和 Value 进行交叉注意力计算，不断更新潜状态。
    4.  **标量映射**：将最终的潜状态通过一个线性层映射为单个连续数值。
    5.  **损失函数**：采用 **Huber Loss** 进行训练，该损失函数结合了均方误差（MSE）和平均绝对误差（MAE）的优点，对异常值更具鲁棒性。

#### 3. 实验设计
*   **数据集**：
    *   **语义文本相似度 (STS)**：STS-Benchmark, SICKR-STS。
    *   **机器翻译质量评估 (WMT)**：WMT20 任务中的英中 (EN-ZH)、俄英 (RU-EN)、僧伽罗英 (SI-EN) 子集。
*   **LLM 主干网络**：Llama 3.1 8B Instruct, Qwen3 8B, Qwen3 32B, Gemma 3 27B Instruct。
*   **对比基准 (Baselines)**：
    *   **自回归类**：Zero-shot, Many-shot (128-shot)。
    *   **回归感知类**：RAIL, RAFT。
    *   **预测头类**：线性回归 (Linear), 多层感知机 (MLP)。
    *   **微调方案**：对比了冻结主干和基于 LoRA 微调的不同组合。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到使用 AdamW 优化器，采用 bfloat16 精度，学习率为 1e-4，Batch Size 为 32。RELISH 的可训练参数量极小（约 3.4M-3.7M），通常在 10 个 Epoch 内收敛。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种不同规模和家族的 LLM，5 个跨领域的回归数据集，每组实验均使用 3 个随机种子取平均值。
*   **消融实验**：
    *   对比了 Huber Loss 与 MSE Loss 的效果。
    *   研究了迭代深度 $L$（从 1 到 5）对性能的影响，验证了迭代机制的必要性。
*   **公平性**：对比实验中，MLP 基准的参数量被调整至与 RELISH 相同，确保了性能提升源于架构设计而非参数容量。整体实验设计详尽、客观。

#### 6. 主要结论与发现
*   **性能卓越**：RELISH 在所有数据集和模型上均显著优于现有的回归方法。相比次优的 RAFT，其皮尔逊相关系数提升了 6.7%，均方根误差（NRMSE）降低了 19.9%。
*   **参数效率极高**：RELISH 仅需约 3.5M 个可训练参数（占总参数量的 0.01%-0.04%）。
*   **扩展性强**：与 LoRA 不同，RELISH 的参数量不随 LLM 主干规模线性增长，而是主要取决于隐藏层维度，这使其在超大规模模型上更具优势。
*   **推理高效**：作为预测头方案，它支持单次推理（Single-pass），无需像自回归方法那样逐词元生成。

#### 7. 优点
*   **架构创新**：将潜变量迭代细化机制引入 LLM 回归，解决了简单池化导致的信息丢失问题。
*   **极简开销**：在保持极低训练成本的同时，实现了超越全量微调或 LoRA 微调的回归精度。
*   **鲁棒性**：在不同语言对（包括低资源语言）和不同任务类型上表现出高度的一致性。

#### 8. 不足与局限
*   **任务覆盖**：目前仅验证了语义相似度和翻译质量评估，尚未在情感分析、价格预测等更广泛的回归场景中测试。
*   **长文本限制**：交叉注意力机制的计算复杂度随输入序列长度线性增长，在处理超长文本时可能存在性能瓶颈。
*   **依赖主干**：由于主干被冻结，RELISH 的上限仍受限于预训练模型本身提取特征的能力，对于需要深度领域知识迁移的任务，可能仍需配合微调。

（完）
