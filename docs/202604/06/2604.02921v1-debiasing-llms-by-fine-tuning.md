---
title: Debiasing LLMs by Fine-tuning
title_zh: 通过微调消除大语言模型的偏差
authors: "Zhenyu Gao, Wenxi Jiang, Yutong Yan"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02921v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 使用 LoRA 微调大语言模型以减轻预测任务中的外推偏差
tldr: 针对大语言模型在预测任务中普遍存在的系统性外推偏差，本文提出了一种基于监督微调（SFT）的去偏方法。研究利用LoRA技术在理性基准预测指令集上对模型进行训练，从参数层面改变其信息映射方式。实验证明，该方法在受控预测实验和股票收益预测中均能有效消除外推偏差，为提升LLM预测准确性提供了一种低成本且通用的解决方案。
selection_source: fresh_fetch
motivation: 现有大语言模型在处理实验和现实数据预测时存在严重的外推偏差，且仅靠提示词工程难以有效缓解。
method: 采用低秩自适应（LoRA）技术，在基于理性基准预测构建的指令数据集上对预训练模型进行监督微调。
result: 在受控预测实验和跨行业股票收益预测两个场景中，微调后的模型均在样本外表现出显著的去偏效果。
conclusion: 通过参数层面的干预，监督微调是一种低成本且可推广的消除大语言模型系统性偏差的有效手段。
---

## 摘要
先前的研究表明，大语言模型（LLMs）在根据实验和现实世界数据进行预测时，表现出系统性的外推偏差，且基于提示（prompt-based）的方法在缓解这种偏差方面效果有限。我们提出了一种监督微调（SFT）方法，利用低秩自适应（LoRA）在基于理性基准预测构建的指令数据集上训练现成的大语言模型。通过在参数层面进行干预，SFT 改变了大语言模型将观测信息映射为预测的方式，从而减轻了外推偏差。我们在两种场景下评估了微调后的模型：受控预测实验和截面股票收益预测。在这两种场景中，微调都纠正了样本外的外推偏差，从而建立了一种低成本且可推广的大语言模型去偏方法。

## Abstract
Prior research shows that large language models (LLMs) exhibit systematic extrapolation bias when forming predictions from both experimental and real-world data, and that prompt-based approaches appear limited in alleviating this bias. We propose a supervised fine-tuning (SFT) approach that uses Low-Rank Adaptation (LoRA) to train off-the-shelf LLMs on instruction datasets constructed from rational benchmark forecasts. By intervening at the parameter level, SFT changes how LLMs map observed information into forecasts and thereby mitigates extrapolation bias. We evaluate the fine-tuned model in two settings: controlled forecasting experiments and cross-sectional stock return prediction. In both settings, fine-tuning corrects the extrapolative bias out-of-sample, establishing a low-cost and generalizable method for debiasing LLMs.

---

## 论文详细总结（自动生成）

以下是对论文《Debiasing LLMs by Fine-tuning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLMs）在预测任务中表现出与人类类似的“外推偏差”（Extrapolation Bias），即过度依赖近期趋势来预测未来。
*   **研究背景**：
    *   LLM 在预训练阶段吸收了大量包含人类偏见的金融文本（如分析师报告、新闻），在对齐阶段又受到可能带有偏见的人类反馈影响。
    *   现有的“提示词工程”（Prompt Engineering）方法（如要求模型理性思考）在纠正这种偏差上效果微乎其微，表明偏差已深深植根于模型的参数表示中，而非仅是输入框架的问题。
*   **整体含义**：本文旨在证明通过参数层面的干预（监督微调），可以有效且低成本地消除 LLM 的系统性行为偏差，使其预测更符合理性基准。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：引入一个额外的监督微调（SFT）步骤，介于标准对齐和部署之间。通过向模型展示“理性预测”的示例，直接重塑模型从观测数据到预测结果的映射逻辑。
*   **关键技术细节**：
    *   **低秩自适应（LoRA）**：为了避免全参数微调带来的高昂计算成本和“灾难性遗忘”（即丧失通用语言能力），研究者冻结了预训练权重 $W_0$，仅训练新增的低秩矩阵 $A$ 和 $B$。输出计算公式为 $h = W_0x + BAx$。
    *   **指令数据集构建**：
        *   **输入（Prompt）**：一段历史数值序列（如过去 40 期的 AR(1) 过程或 12 个月的股票收益率）。
        *   **目标（Target）**：理性的基准预测。在受控实验中是条件期望值；在股票预测中是实际的未来收益率。
    *   **训练策略**：采用验证集性能触发的“早停法”（Early Stopping），确保模型学习到的是通用的预测逻辑而非对特定序列的过拟合。

### 3. 实验设计
研究设计了两个主要场景来验证去偏效果：
*   **场景 A：受控 AR(1) 预测实验**
    *   **设置**：模拟 6 种不同持久度（$\rho \in \{0, 0.2, \dots, 1.0\}$）的随机过程。
    *   **Benchmark**：理性预期模型（Rational Expectations）。
    *   **评估指标**：通过回归分析预测误差与预测修正的关系，负系数 $b$ 代表过度反应（Overreaction）。
*   **场景 B：截面股票收益预测**
    *   **设置**：预测标普 500 指数成份股的月度收益率。
    *   **Benchmark**：实际实现的未来收益率。
    *   **对比方法**：将微调后的模型与原始（Base）Qwen3-32B 模型进行对比。实验严格划分了训练期（2001-2011）、验证期（2012-2015）和测试期（2016-2024）。

### 4. 资源与算力
*   **模型选择**：使用了拥有 320 亿参数的开源模型 **Qwen3-32B**。
*   **算力消耗**：文中明确提到，整个训练过程在商业云集群上的成本仅为 **“几百美元”**。
*   **具体细节**：虽然未列出具体的 GPU 型号和确切时长，但强调该成本相对于预训练（数百万美元）是微不足道的，且 LoRA 技术使得在有限显存下微调 32B 模型成为可能。

### 5. 实验数量与充分性
*   **实验规模**：
    *   AR(1) 实验包含约 30,000 个观测点。
    *   股票预测实验涵盖了标普 500 成份股多年间的 51,724 个样本点。
*   **充分性与公平性**：
    *   **样本外测试**：所有评估均在模型从未见过的测试集上进行，排除了“记忆”历史数据的可能性。
    *   **匿名化处理**：在股票预测中去除了公司名称和日期，仅保留数值，有效防止了模型利用预训练中可能存在的“未来信息”（Lookahead Bias）。
    *   **统计显著性**：回归分析提供了 t 统计量和 R²，结果在 1% 的水平上显著，实验设计较为严谨客观。

### 6. 主要结论与发现
*   **原生模型存在严重偏差**：原始 LLM 在 AR(1) 实验中表现出与人类高度一致的过度反应模式，且在股票预测中表现出显著的近期趋势外推（系数为正且显著）。
*   **微调能有效去偏**：
    *   在 AR(1) 任务中，微调后过度反应系数变得统计学上不显著（接近 0）。
    *   在股票预测中，模型从“追涨杀跌”转变为识别“短期反转”模式（滞后收益率系数转为负值），更贴近真实市场的均值回归特性。
*   **通用能力保留**：LoRA 微调在修正预测逻辑的同时，保留了模型的语言理解和指令遵循能力。

### 7. 优点
*   **干预深度**：突破了提示词工程的表层限制，从参数层面解决了根深蒂固的认知偏差。
*   **低成本高效率**：证明了无需重新预训练，仅需少量针对性数据和 LoRA 技术即可完成复杂的行为对齐。
*   **方法通用性**：该框架可推广至信用风险评估、宏观经济预测等其他存在系统性偏差的金融领域。

### 8. 不足与局限
*   **偏差覆盖有限**：研究主要集中在“外推偏差”，尚未探讨微调是否会引入其他类型的偏见（如保守主义偏差）。
*   **基准依赖性**：该方法高度依赖于“理性基准”的质量。在某些缺乏明确理性定义的复杂经济场景下，构建高质量的指令集可能存在困难。
*   **应用限制**：目前仅在开源模型上验证，对于闭源模型（如 GPT-4），用户无法进行参数级微调，该方法无法直接应用。

（完）
