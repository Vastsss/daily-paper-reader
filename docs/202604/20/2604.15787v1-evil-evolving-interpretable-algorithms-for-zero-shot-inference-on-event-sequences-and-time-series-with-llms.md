---
title: "EVIL: Evolving Interpretable Algorithms for Zero-Shot Inference on Event Sequences and Time Series with LLMs"
title_zh: EVIL：利用大语言模型演化用于事件序列和时间序列零样本推理的可解释算法
authors: David Berghaus
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15787v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 利用LLM引导的进化搜索进行时间序列的零样本推理
tldr: EVIL是一种利用大语言模型（LLM）引导进化搜索来发现可解释算法的新方法。它不依赖于在大规模数据集上训练神经网络，而是进化出纯Python/NumPy程序，用于处理事件序列和时间序列的零样本推理任务。在事件预测、马尔可夫跳跃过程估计和时间序列插补等任务中，EVIL生成的单一算法展现了极强的泛化能力，性能媲美甚至超越了最先进的深度学习模型，同时具备极高的运行效率和完全的可解释性。
selection_source: fresh_fetch
motivation: 旨在克服传统深度学习模型在动力系统推理中缺乏可解释性、计算开销大且难以跨数据集泛化的局限。
method: 采用LLM引导的进化搜索，自动生成并优化执行零样本、上下文内推理的简洁Python/NumPy程序。
result: 在三个不同领域的任务中，发现的算法性能优于或等同于SOTA深度学习模型，且运行速度快几个数量级。
conclusion: 证明了LLM引导的程序进化能够发现通用的、紧凑且可解释的推理函数，为动力系统建模提供了高效的新范式。
---

## 摘要
我们介绍了 EVIL（Evolving Interpretable algorithms with LLMs），这是一种利用大语言模型引导的演化搜索来发现用于动力系统推理的简单、可解释算法的方法。EVIL 并非在大型数据集上训练神经网络，而是演化出纯 Python/NumPy 程序，这些程序可以跨数据集执行零样本、上下文内推理。我们将 EVIL 应用于三个不同的任务：时间点过程中的下一事件预测、马尔可夫跳跃过程的速率矩阵估计以及时间序列插补。在每种情况下，单个演化出的算法都能泛化到所有评估数据集，而无需针对每个数据集进行训练（类似于摊销推理模型）。据我们所知，这是首项证明大语言模型引导的程序演化能够为这些动力系统问题发现单一紧凑推理函数的工作。在这三个领域中，所发现的算法通常与最先进的深度学习模型具有竞争力，甚至优于后者，同时速度快几个数量级，并保持完全的可解释性。

## Abstract
We introduce EVIL (\textbf{EV}olving \textbf{I}nterpretable algorithms with \textbf{L}LMs), an approach that uses LLM-guided evolutionary search to discover simple, interpretable algorithms for dynamical systems inference. Rather than training neural networks on large datasets, EVIL evolves pure Python/NumPy programs that perform zero-shot, in-context inference across datasets. We apply EVIL to three distinct tasks: next-event prediction in temporal point processes, rate matrix estimation for Markov jump processes, and time series imputation. In each case, a single evolved algorithm generalizes across all evaluation datasets without per-dataset training (analogous to an amortized inference model). To the best of our knowledge, this is the first work to show that LLM-guided program evolution can discover a single compact inference function for these dynamical-systems problems. Across the three domains, the discovered algorithms are often competitive with, and even outperform, state-of-the-art deep learning models while being orders of magnitudes faster, and remaining fully interpretable.

---

## 论文详细总结（自动生成）

这是一份关于论文《EVIL: Evolving Interpretable Algorithms for Zero-Shot Inference on Event Sequences and Time Series with LLMs》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的动力系统建模（如时间点过程、马尔可夫跳跃过程等）主要由深度学习模型主导。然而，这些模型存在三大痛点：
    1.  **黑盒性**：预测过程不透明，难以解释。
    2.  **高成本**：需要大规模数据集训练，计算资源消耗大。
    3.  **泛化差**：难以应对分布偏移或未见过的输出维度。
*   **核心问题**：能否利用大语言模型（LLM）的先验知识和编程能力，通过进化搜索自动发现简洁、纯代码形式、且具备强泛化能力的“摊销推理函数”？
*   **整体含义**：EVIL 提出了一种新范式，即不再学习神经网络的参数，而是搜索可解释的 Python/NumPy 算法。这些算法一旦发现，即可在不同数据集上实现零样本（Zero-shot）推理。

### 2. 论文提出的方法论
*   **核心思想**：利用 LLM 引导的程序演化（基于 AlphaEvolve 框架）来迭代优化 Python 函数。
*   **关键技术细节**：
    *   **演化流程**：从一个平凡的初始程序（如预测中位数）开始，LLM 以 `diff` 格式提出代码修改建议。
    *   **种群管理**：使用 MAP-Elites 算法和基于岛屿的种群策略，平衡搜索的探索（Exploration）与利用（Exploitation）。
    *   **约束条件**：系统提示词严格限制算法只能使用 Python 标准库和 NumPy，禁止使用任何深度学习框架（如 PyTorch）。
    *   **评分机制**：根据算法在验证子集上的预测质量（如准确率、RMSE、MAE 等）进行评分。
    *   **推理模式**：生成的算法是“上下文内（In-context）”的，即它直接接收上下文观测数据并输出预测，无需针对新数据集进行参数微调。

### 3. 实验设计
*   **应用场景与数据集**：
    1.  **时间点过程 (TPP)**：使用 Taxi, Taobao, Stack Overflow, Amazon, Retweet 等数据集，以及 MIMIC-II（用于测试超大规模标记类别的泛化）。
    2.  **马尔可夫跳跃过程 (MJP)**：使用合成的 DFR（闪烁棘轮）系统，以及真实的离子通道记录 (IonCh)、分子动力学 (ADP) 和蛋白质折叠 (PFold) 数据。
    3.  **时间序列插补**：涵盖空气质量 (Beijing, Italy)、交通 (Guangzhou, PeMS)、电力 (Electricity) 等 10 个数据集。
*   **Benchmark 与对比方法**：
    *   **TPP**：对比 A-NHP, NHP, CDiff, 以及基础推理模型 FIM-PP。
    *   **MJP**：对比 NeuralMJP 和 FIM-MJP。
    *   **插补**：对比 BRITS, SAITS, CSDI, 以及预训练模型 FIM-ℓ。

### 4. 资源与算力
*   **搜索成本**：演化过程极其高效。单次任务的演化仅需 **15 到 40 分钟** 的墙面时间，API 调用成本 **低于 1 美元**。
*   **推理效率**：
    *   在 TPP 任务中，EVIL 在 **单核 CPU** 上仅需 **5 秒** 即可完成评估。
    *   相比之下，深度学习基准模型 FIM-PP 在 **A100 GPU** 上需要 **964 秒**（约 16 分钟）。
*   **硬件需求**：演化出的算法仅依赖基础数组运算，可在普通商用硬件（如笔记本 CPU）上实时运行。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个完全不同的动力系统领域进行了广泛测试，涉及超过 20 个真实和合成数据集。
*   **消融实验**：
    *   测试了仅在合成数据上演化的版本（EVIL synthetic prior）的泛化性。
    *   进行了不同随机种子的重复实验（5 次独立运行）以验证稳定性。
    *   进行了长达 800 次迭代的长程演化实验，探讨性能上限。
*   **客观性与公平性**：EVIL 在演化过程中接触的数据量远少于基准模型（仅使用训练集的极小部分作为验证），且在完全未见过的 MIMIC-II 数据集上展现了卓越的零样本性能，实验设计较为严谨。

### 6. 论文的主要结论与发现
*   **性能卓越**：演化出的简单算法在多个任务上达到了 SOTA 深度学习模型的水平，甚至在 MIMIC-II 等复杂任务上实现了超越。
*   **强泛化性**：单个算法可以处理任意数量的标记类别（Marks）和不同的分布，无需重新训练。
*   **发现新策略**：在插补任务中，EVIL 自动发现了“模式检索（Motif Retrieval）”策略，这与人类设计的先进启发式方法不谋而合。
*   **效率优势**：推理速度比神经网络快 200 倍以上，且无需昂贵的 GPU 资源。

### 7. 优点
*   **完全可解释**：输出的是人类可读的 Python 代码，逻辑透明，便于领域专家审查和修改。
*   **极低门槛**：无需深度学习专业知识，通过简单的系统提示词即可将领域知识融入演化过程。
*   **鲁棒性**：相比神经网络，简单的程序逻辑在面对分布偏移时表现得更加稳健。

### 8. 不足与局限
*   **表征能力限制**：在需要极强非线性表征的任务（如长窗口时间序列插补）中，EVIL 的表现仍略逊于经过大规模预训练的深度学习模型（如 FIM-ℓ）。
*   **确定性限制**：目前演化出的算法主要是确定性的，尚未深入探索如何生成能够量化不确定性的随机算法。
*   **新颖性界定**：虽然算法是自动发现的，但其核心逻辑往往是已知统计方法的优化组合，难以评估其在科学发现意义上的绝对原创性。

（完）
