---
title: Scattered Hypothesis Generation for Open-Ended Event Forecasting
title_zh: 针对开放式事件预测的分散式假设生成
authors: "He Chang, Zhulin Tao, Lifang Yang, Xianglin Huang, Yunshan Ma"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15788v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 基于 LLM 的开放式事件预测
tldr: 针对开放式事件预测中现有LLM方法仅关注高概率结果而忽略不确定性的问题，本文提出了“散射预测”范式，旨在生成涵盖多种可能未来的假设集。为此，作者开发了SCATTER强化学习框架，通过结合有效性、组内多样性和组间多样性的混合奖励机制，在确保预测合理性的同时提升了结果的覆盖面和多样性。实验证明该方法在OpenForecast和OpenEP数据集上显著优于现有基准。
selection_source: fresh_fetch
motivation: 现有的LLM事件预测方法往往只关注最可能的结果，无法捕捉现实世界事件中固有的不确定性和多样性。
method: 提出SCATTER强化学习框架，利用包含有效性、组内多样性和组间多样性的混合奖励函数来优化假设生成的全面性。
result: 在OpenForecast和OpenEP两个真实世界基准数据集上的实验结果显示，SCATTER在预测的包容性和多样性方面均显著优于强基准模型。
conclusion: 通过将预测目标从单一结果转向散射假设生成，并结合有效性约束的多样性优化，可以更有效地应对开放式事件预测中的不确定性。
---

## 摘要
尽管开放式事件预测对风险管理至关重要，但目前基于大语言模型（LLM）的方法主要仅针对最可能的结果，忽略了现实世界事件固有的不确定性。为了弥补这一差距，我们通过引入假设生成这一代理任务，将开放式事件预测从精准预测推进到分散预测。该范式旨在生成一组具有包容性和多样性的假设，以广泛覆盖合理未来事件的空间。为此，我们提出了 SCATTER，这是一个联合优化假设包容性与多样性的强化学习框架。具体而言，我们设计了一种新型混合奖励，由三个部分组成：1）衡量与观测事件语义一致性的有效性奖励；2）鼓励采样响应内部变化的组内多样性奖励；以及 3）促进跨不同模式探索的组间多样性奖励。通过将有效性门控分数集成到总体目标中，我们将对高度多样化结果的探索限制在上下文合理的未来范围内，从而防止模式崩溃问题。在两个真实世界基准数据集（即 OpenForecast 和 OpenEP）上的实验表明，SCATTER 显著优于强基准模型。我们的代码可在 https://github.com/Sambac1/SCATTER 获取。

## Abstract
Despite the importance of open-ended event forecasting for risk management, current LLM-based methods predominantly target only the most probable outcomes, neglecting the intrinsic uncertainty of real-world events. To bridge this gap, we advance open-ended event forecasting from pinpoint forecasting to scatter forecasting by introducing the proxy task of hypothesis generation. This paradigm aims to generate an inclusive and diverse set of hypotheses that broadly cover the space of plausible future events. To this end, we propose SCATTER, a reinforcement learning framework that jointly optimizes inclusiveness and diversity of the hypothesis. Specifically, we design a novel hybrid reward that consists of three components: 1) a validity reward that measures semantic alignment with observed events, 2) an intra-group diversity reward to encourage variation within sampled responses, and 3) an inter-group diversity reward to promote exploration across distinct modes. By integrating the validity-gated score into the overall objective, we confine the exploration of wildly diversified outcomes to contextually plausible futures, preventing the mode collapse issue. Experiments on two real-world benchmark datasets, i.e., OpenForecast and OpenEP, demonstrate that SCATTER significantly outperforms strong baselines. Our code is available at https://github.com/Sambac1/SCATTER.

---

## 论文详细总结（自动生成）

这是一份关于论文《Scattered Hypothesis Generation for Open-Ended Event Forecasting》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的基于大语言模型（LLM）的事件预测方法大多遵循“精准预测”（Pinpoint Forecasting）范式，即只生成一个最可能的未来结果。然而，现实世界的事件具有内在的不确定性和多模态性，单一预测无法捕捉多种合理的未来走向。
*   **研究动机**：为了应对这种不确定性，论文提出了“分散预测”（Scatter Forecasting）的新范式。其目标是生成一组既具有**包容性**（涵盖真实结果）又具有**多样性**（覆盖不同可能路径）的假设集。

### 2. 方法论
论文提出了 **SCATTER** 框架，基于组相对策略优化（GRPO）算法，通过设计一种新型的**混合奖励机制**来联合优化假设的有效性与多样性：
*   **核心思想**：利用有效性信号作为“门控”，确保模型在探索多样化结果时，始终锚定在上下文合理的未来范围内，防止产生荒谬的预测（即防止奖励作弊和模式崩溃）。
*   **关键技术细节（混合奖励函数）**：
    1.  **有效性奖励（Validity Reward）**：利用预训练文本嵌入模型计算生成假设与真实结果之间的最大余弦相似度，衡量语义对齐程度。
    2.  **组内多样性奖励（Intra-group Diversity）**：计算单次响应内不同假设之间的成对不相似度，并由有效性分数加权，鼓励在合理范围内的变异。
    3.  **组间多样性奖励（Inter-group Diversity）**：利用加权非对称倒角距离（Chamfer Distance）衡量不同采样组之间的覆盖差异，促进全局探索。
*   **优化流程**：采用两阶段训练，先进行监督微调（SFT）以对齐格式，再通过 GRPO 强化学习优化上述混合奖励。

### 3. 实验设计
*   **数据集**：
    *   **OpenForecast**：大规模开放式事件预测数据集（包含 1950-2024 年的时间跨度）。
    *   **OpenEP**：针对新闻文章的开放式未来事件预测数据集。
    *   **Hard Subsets**：专门筛选出 GPT-4o-mini 预测失败的样本组成“困难子集”。
*   **基准模型（Baselines）**：
    *   闭源模型：GPT-4o-mini。
    *   开源基座：Qwen2.5-3B/7B-Instruct, Llama3.2-3B-Instruct。
    *   训练变体：标准 SFT、标准 GRPO（仅含有效性奖励）。
*   **评估指标**：SoftPass@K（命中率）、SoftRecall@K（覆盖率）、ValidRatio@K（有效多样性比例）以及基于 GPT-5o-mini 的逻辑验证。

### 4. 资源与算力
*   **模型规模**：主要实验集中在 3B 参数模型，补充实验涵盖了 7B 模型。
*   **训练技术**：使用了参数高效微调技术 **LoRA**（Rank=64, Alpha=32）。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到了训练批次大小（Batch Size）为 480，强化学习训练样本量为 10k。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括主实验（两个数据集）、困难子集测试、跨领域泛化测试（在 OpenForecast 训练，在 OpenEP 测试）。
*   **消融实验**：针对奖励组件（组内/组间多样性）和有效性门控策略（Vanilla/Mean/Min/SCATTER）进行了详细对比。
*   **充分性与公平性**：
    *   实验涵盖了不同架构的基座模型（Qwen 和 Llama）。
    *   通过改变采样轮数 $K$ 和每轮假设数 $M$ 验证了扩展性。
    *   引入了跨嵌入模型（Cross-embedding）评估，证明结果不依赖于特定的向量模型，体现了实验的客观性和严谨性。

### 6. 主要结论与发现
*   **性能领先**：SCATTER 在所有指标上显著优于 SFT 和标准 GRPO，尤其是在捕捉长尾事件和维持假设多样性方面。
*   **克服模式崩溃**：标准 GRPO 虽然在单一指标上表现尚可，但极易陷入模式崩溃（ValidRatio 接近 0），而 SCATTER 通过有效性门控成功维持了高水平的有效多样性。
*   **泛化能力**：在未见过的领域（OpenEP）上，SCATTER 依然保持了强大的预测能力，证明其学习到的是预测逻辑而非简单的模式记忆。

### 7. 优点
*   **范式创新**：将事件预测从“单点预测”转向“散射假设生成”，更符合现实世界的随机性。
*   **奖励设计巧妙**：通过有效性门控多样性，解决了强化学习中“多样性”与“合理性”往往冲突的难题。
*   **评估严谨**：结合了嵌入相似度和 LLM 逻辑验证，并进行了详尽的鲁棒性分析。

### 8. 不足与局限
*   **模型规模限制**：受限于算力，主要实验在 3B 模型上完成，未在 70B 或更大规模模型上验证。
*   **采样成本**：为了达到最佳效果，需要进行多轮采样（如 $K=16$），这在实时预测场景下可能带来较高的推理延迟和计算成本。
*   **标签依赖**：目前的奖励机制仍依赖于已有的真实结果（Ground Truth），对于完全未知的未来事件，如何构建无监督或弱监督的奖励信号仍是挑战。

（完）
