---
title: Convolutionally Low-Rank Models with Modified Quantile Regression for Interval Time Series Forecasting
title_zh: 结合改进分位数回归的卷积低秩模型用于区间时间序列预测
authors: "Miaoxuan Zhu, Yi Yu, Yuyang Li, Wei Li, Guangcan Liu"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15791v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 区间时间序列预测与不确定性
tldr: 针对时间序列预测中不确定性量化的问题，本文在卷积低秩模型（LbCNNM）的基础上，结合改进的分位数回归（MQR）和区间校准技术，提出了LbCNNM-MQR模型。该方法不仅继承了LbCNNM在多步预测中的卷积低秩特性，还实现了高质量的区间预测。在超过10万条真实时间序列上的实验证明，该模型在预测精度和区间可靠性方面均表现优异。
selection_source: fresh_fetch
motivation: 现有的卷积低秩点预测模型（LbCNNM）虽然在多步预测中表现出色，但缺乏衡量预测不确定性的能力。
method: 通过将改进的分位数回归（MQR）集成到LbCNNM框架中，并结合区间校准技术，构建了全新的区间预测模型。
result: 在超过10万条真实世界时间序列的大规模实验中，LbCNNM-MQR在预测区间的准确性和可靠性上均优于现有方法。
conclusion: LbCNNM-MQR成功为卷积低秩模型引入了不确定性估计能力，为复杂决策场景提供了更具参考价值的区间预测方案。
---

## 摘要
预测模型中的不确定性量化对于可靠的决策至关重要，但仍是一个重大挑战。区间时间序列预测通过提供预测区间（PIs）为这一问题提供了原则性的解决方案，预测区间指示了真实值落在预测范围内的概率。我们考虑了一种近期建立的点预测（PFs）方法，称为基于学习的卷积核范数最小化（LbCNNM），该方法利用从训练数据中导出的卷积低秩特性，直接生成多步提前预测。尽管 LbCNNM 在理论上是完备的且在实证中是有效的，但它缺乏固有的不确定性估计能力，这是许多先进预测方法共有的局限性。为了解决这一问题，我们改进了著名的分位数回归（QR）并将其集成到 LbCNNM 中，从而提出了一种名为“结合改进分位数回归的 LbCNNM”（LbCNNM-MQR）的新型区间预测方法。此外，我们设计了区间校准技术，以进一步提高预测区间的准确性。在超过 100,000 条真实世界时间序列上的广泛实验证明了 LbCNNM-MQR 的卓越性能。

## Abstract
The quantification of uncertainty in prediction models is crucial for reliable decision-making, yet remains a significant challenge. Interval time series forecasting offers a principled solution to this problem by providing prediction intervals (PIs), which indicates the probability that the true value falls within the predicted range. We consider a recently established point forecasts (PFs) method termed Learning-Based Convolution Nuclear Norm Minimization (LbCNNM), which directly generates multi-step ahead forecasts by leveraging the convolutional low-rankness property derived from training data. While theoretically complete and empirically effective, LbCNNM lacks inherent uncertainty estimation capabilities, a limitation shared by many advanced forecasting methods. To resolve the issue, we modify the well-known Quantile Regression (QR) and integrate it into LbCNNM, resulting in a novel interval forecasting method termed LbCNNM with Modified Quantile Regression (LbCNNM-MQR). In addition, we devise interval calibration techniques to further improve the accuracy of PIs. Extensive experiments on over 100,000 real-world time series demonstrate the superior performance of LbCNNM-MQR.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LbCNNM-MQR** 的新型区间时间序列预测方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
在时间序列预测中，单纯的点预测（Point Forecasts）无法衡量结果的不确定性，这在金融、能源和交通等高风险决策场景中是致命的。**区间预测（Interval Forecasting）**通过提供预测区间（PIs）来量化不确定性。
本文的研究动机在于：现有的先进点预测模型（如基于卷积低秩特性的 LbCNNM）虽然在处理少样本和多步预测方面表现优异，但缺乏生成预测区间的能力。作者旨在将分位数回归（QR）引入该框架，并解决标准分位数回归在优化过程中的不稳定性问题。

### 2. 方法论
LbCNNM-MQR 的核心由三个关键部分组成：
*   **卷积低秩骨干（LbCNNM）：** 将预测问题视为向量补全问题。通过学习一个正交变换矩阵 $A$，使变换后的序列在卷积意义下具有低秩性，从而利用核范数最小化进行多步预测。
*   **改进的分位数回归（Modified QR）：** 
    *   **核心思想：** 标准分位数回归在 ADMM 优化算法中的子问题涉及 `median`（中位数）函数，该函数不连续且对输入敏感，导致优化不稳定。
    *   **技术细节：** 作者提出一种启发式方法，将 `median` 替换为 `mean`（均值）来平滑优化过程。公式上，将迭代步骤中的中值计算改为均值计算，从而在保持分位数特性的同时增强了数值稳定性。
*   **区间校准（Interval Calibration）：** 借鉴符合性预测（Conformal Prediction）的思想。将观测数据分为训练集和校准集，在校准集上计算残差得分，并据此调整初步预测区间的边界，以确保在统计上满足预设的覆盖概率（如 95%）。

### 3. 实验设计
*   **数据集：** 
    *   **M4 数据集：** 包含 100,000 条真实世界序列，涵盖年、季、月、周、日、小时等不同频率。
    *   **辅助数据集：** Electricity（321 条）和 Traffic（862 条）多变量数据集（作为单变量任务处理）。
*   **对比方法（Benchmarks）：** 
    *   传统统计模型：AutoARIMA, AutoETS, AutoTheta。
    *   深度学习模型：DeepAR, DeepTCN。
    *   变体对比：LbCNNM-CP（仅结合符合性预测）、LbCNNM-QR（使用标准分位数回归）。
*   **评价指标：** MSIS（平均比例区间评分，衡量区间宽度与惩罚）和 ACD（绝对覆盖差异，衡量实际覆盖率与目标值的偏差）。

### 4. 资源与算力
*   **硬件平台：** 实验主要在 Intel Xeon Platinum 8358P CPU 上运行。
*   **GPU 使用：** 仅对比方法中的深度学习模型（DeepAR 和 DeepTCN）使用了单张 NVIDIA A800 80GB GPU。
*   **运行效率：** LbCNNM-MQR 在 M4 全数据集上的平均运行时间约为 0.952 秒/序列，虽然慢于简单的 AutoETS，但远快于深度学习模型，且在 CPU 上即可高效运行。

### 5. 实验数量与充分性
*   **实验规模：** 论文在超过 10 万条序列上进行了测试，样本量极大，具有很强的统计说服力。
*   **消融与分析：** 
    *   对比了 LbCNNM-QR 与 LbCNNM-MQR，验证了“均值替代中值”改进的有效性。
    *   进行了超参数 $\lambda$ 的敏感性分析，证明了模型在不同参数下的鲁棒性。
    *   测试了不同校准得分集（$s_{Cal}$）对结果的影响。
*   **客观性：** 实验覆盖了多种频率和领域的数据，对比方法涵盖了从经典统计到现代深度学习的主流模型，评价过程较为客观公平。

### 6. 主要结论与发现
*   **性能领先：** LbCNNM-MQR 在 M4 全数据集上的 MSIS 指标优于所有对比方法，比次优的 AutoETS 提升了 10.4%。
*   **覆盖率精准：** 其 ACD 仅为 0.18%，意味着生成的 95% 预测区间非常接近真实的 95% 覆盖要求，可靠性极高。
*   **改进有效：** 实验证明，将分位数回归中的中值函数改为均值函数（MQR）能显著提升区间预测的准确性，避免了标准 QR 在低秩框架下的过拟合或优化失效问题。

### 7. 优点
*   **无需调参：** 继承了 LbCNNM 的特性，能够自动适应不同特征的序列，无需针对每条序列手动调整参数。
*   **理论与实证结合：** 既有基于压缩感知和低秩矩阵恢复的理论支撑，又有大规模真实数据的实证支持。
*   **高效性：** 相比深度学习方法，该方法不需要复杂的训练过程，且在 CPU 环境下表现优异，易于在大规模工业场景部署。

### 8. 不足与局限
*   **单变量局限：** 目前主要针对单变量时间序列，虽然在多变量数据集上做了测试，但并未充分利用变量间的空间相关性。
*   **周期性处理：** 在具有极强周期性和海量历史数据的场景（如 Electricity）中，其表现略逊于专门优化的深度学习模型（如 DeepAR）。
*   **启发式改进：** 将 `median` 替换为 `mean` 的做法虽然在实验中效果显著，但其深层的理论数学证明仍有待进一步完善。

（完）
