---
title: The CTLNet for Shanghai Composite Index Prediction
title_zh: 用于上证指数预测的 CTLNet
authors: Haibin Jiao
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16835v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 基于Transformer的股票指数多变量时间序列预测
tldr: 本研究针对上证指数预测这一热点问题，提出了一种名为CTLNet的深度学习混合模型。该模型融合了CNN提取局部特征、Transformer处理长序列依赖与多变量相关性以及LSTM捕捉时序特征的优势。通过在上证指数数据集上的对比实验，证明了CTLNet在预测精度上显著优于现有的主流基准模型，为金融时间序列预测提供了有效的技术方案。
selection_source: fresh_fetch
motivation: 旨在解决上证指数预测中长序列依赖和多变量相关性难以有效捕捉的问题。
method: 提出了一种结合CNN、Transformer和LSTM优势的混合神经网络架构CTLNet。
result: 对比实验显示，所提出的CTLNet模型在预测性能上超越了目前最先进的基准模型。
conclusion: CTLNet通过整合多种深度学习组件，证明了其在处理复杂金融时间序列预测任务中的优越性。
---

## 摘要
上证指数预测已成为许多投资者和学术研究人员关注的热点问题。深度学习模型被广泛应用于多元时间序列预测，包括循环神经网络 (RNN)、卷积神经网络 (CNN) 和 Transformer。具体而言，Transformer 编码器凭借其独特的注意力机制和并行处理能力，已成为时间序列预测中的重要工具，并在处理长序列依赖和多元数据相关性方面具有优势。借鉴各种模型的优点，我们提出了 CNN-Transformer-LSTM 网络 (CTLNet)。本文探讨了 CTLNet 在上证指数预测中的应用，对比实验表明，所提模型优于目前最先进的基准模型。

## Abstract
Shanghai Composite Index prediction has become a hot issue for many investors and academic researchers. Deep learning models are widely applied in multivariate time series forecasting, including recurrent neural networks (RNN), convolutional neural networks (CNN), and transformers. Specifically, the Transformer encoder, with its unique attention mechanism and parallel processing capabilities, has become an important tool in time series prediction, and has an advantage in dealing with long sequence dependencies and multivariate data correlations. Drawing on the strengths of various models, we propose the CNN-Transformer-LSTM Networks (CTLNet). This paper explores the application of CTLNet for Shanghai Composite Index prediction and the comparative experiments show that the proposed model outperforms state-of-the-art baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《The CTLNet for Shanghai Composite Index Prediction》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：上证指数（SSE Composite Index）作为中国证券市场的核心指标，其预测对投资者和研究者具有重要意义。
*   **核心问题**：金融时间序列具有高波动性、复杂性和信息稀疏性。现有的单一模型（如CNN、LSTM或Transformer）在处理此类数据时存在局限：CNN擅长局部特征但忽略长程依赖；LSTM擅长时序依赖但难以处理并行交互；Transformer虽擅长全局建模，但在原始时间点级别上存在信息稀疏和噪声干扰。
*   **研究动机**：通过整合多种架构的优势，提出一种能够同时捕捉局部模式、全局交互和长时序依赖的综合模型。

### 2. 论文提出的方法论：CTLNet
CTLNet 采用了一种三阶段的混合架构，核心流程如下：
*   **CNN 模块（局部特征提取）**：使用一维卷积（CNN1D）作为前端。它通过滑动窗口将相邻的时间点聚合为“时间节点（Time Nodes）”。这一步的作用是增强局部表示，减少原始数据的噪声，并初步提取时间模式。
*   **Transformer 编码器模块（全局特征提取）**：将 CNN 输出的节点序列视为 Token。通过多头自注意力机制（MSA）和位置编码，促进不同时间节点之间的全面交互。这一步旨在解决信息稀疏问题，从全局视角丰富每个节点的特征表达。
*   **LSTM 模块（时序依赖捕捉）**：将经过 Transformer 增强的节点序列输入双向 LSTM（Bi-LSTM）。利用其门控机制（遗忘门、输入门、输出门）来捕捉金融数据中的长期演变趋势和时序依赖关系。
*   **预测输出**：最后通过全连接层输出预测值，并使用均方误差（MSE）作为损失函数进行监督学习。

### 3. 实验设计
*   **数据集**：使用了两个真实世界的金融数据集：**上证综合指数（SSE Composite Index）**和**上证50指数（SSE 50 Index）**。数据频率为5分钟，包含开盘价、收盘价、最高价、最低价和成交量。
*   **实验场景**：采用滑动窗口方法，利用前5个时间点的数据预测第6个时间点的数值（回归任务）。
*   **基准模型（Benchmarks）**：
    *   **LSTM**：基础循环神经网络。
    *   **CNN-LSTM**：结合局部提取与时序建模的常用模型。
    *   **TCLNet**：将 Transformer 置于 CNN 之前的变体模型（用于对比模块顺序的影响）。
    *   **PatchTST**：目前先进的基于补丁的时间序列 Transformer 模型。
*   **评估指标**：平均绝对误差（MAE）和决定系数（$R^2$）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件配置（如 GPU 型号或数量）。
*   **训练细节**：提到了优化器为 SGD，学习率为 0.01，动量为 0.9。
*   **训练时长**：在 Table 2 中记录了训练时间，CTLNet 在上证指数数据集上的训练时间约为 **97.58秒**，相比之下 LSTM 为 38.23秒，CNN-LSTM 为 37.17秒。这表明模型虽然更复杂，但在小规模数据集上的计算开销仍在可接受范围内。

### 5. 实验数量与充分性
*   **实验规模**：论文在2个代表性指数数据集上进行了测试，并对比了4种基准模型。
*   **消融实验**：通过对比 CTLNet 与 TCLNet（改变模块顺序），验证了“先 CNN 提取局部特征、再 Transformer 全局交互”的顺序优于“先 Transformer 后 CNN”。
*   **充分性评价**：实验设计较为基础。虽然涵盖了核心对比和顺序消融，但缺乏针对不同预测步长（Long-term vs Short-term）的深入探讨，且数据集仅限于中国市场，跨市场的泛化性验证略显不足。

### 6. 论文的主要结论与发现
*   **性能优越性**：CTLNet 在 MAE 和 $R^2$ 指标上均优于所有基准模型，证明了混合架构的有效性。
*   **模块顺序至关重要**：实验发现，直接在原始时间点上应用 Transformer（如 TCLNet）效果最差，因为原始数据噪声大且信息稀疏；而先通过 CNN 聚合后再处理效果显著提升。
*   **特征互补**：CNN 的压缩能力提高了计算效率，Transformer 增强了节点间的关联，LSTM 稳定了时序趋势的捕捉。

### 7. 优点
*   **架构融合创新**：有机结合了三种主流架构，逻辑清晰，能够从局部、全局、时序三个维度解析金融数据。
*   **解决稀疏性**：通过 CNN 节点化处理，有效缓解了 Transformer 处理长序列时的计算压力和信息稀疏问题。
*   **可解释性**：模型各层的功能定义明确（局部提取 -> 全局交互 -> 时序建模），易于理解。

### 8. 不足与局限
*   **实验覆盖面有限**：仅使用了5分钟频率的数据，未测试日线或更长时间尺度的数据，也未涉及其他国家的股票指数。
*   **超参数敏感性**：论文提到 CNN 的滑动窗口大小对结果影响显著，但未给出自动选择或优化该参数的系统方法。
*   **硬件信息缺失**：缺乏算力环境的描述，不利于其他研究者评估其在工业环境下的部署成本。
*   **复杂度风险**：相比单一模型，CTLNet 参数量更多（约10.2万），在极大规模数据上可能面临过拟合或训练成本激增的风险。

（完）
