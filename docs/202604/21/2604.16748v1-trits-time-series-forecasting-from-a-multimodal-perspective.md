---
title: "TriTS: Time Series Forecasting from a Multimodal Perspective"
title_zh: TriTS：从多模态视角进行时间序列预测
authors: Xiang Ao
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16748v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 时间序列预测的多模态视角
tldr: 针对长程时间序列预测中1D信号动态复杂、难以解耦的问题，本文提出TriTS框架。该框架从多模态视角出发，将1D序列投影至时间、频率和2D视觉三个正交空间。通过引入Visual Mamba和周期感知重塑技术，在保持线性复杂度的同时捕捉全局视觉纹理，并结合小波混合模块实现精细的时频定位。实验证明TriTS在多个基准数据集上达到SOTA，且显著降低了参数量和推理延迟。
selection_source: fresh_fetch
motivation: 传统的1D视角难以充分捕捉时间序列中高度纠缠的复杂动态，导致长程预测存在表示瓶颈。
method: 提出一种跨模态解耦框架，利用Visual Mamba高效建模2D视觉纹理，并结合多分辨率小波混合与时域线性分支进行多维度特征融合。
result: 在多个基准数据集上取得了SOTA性能，并大幅减少了模型参数量和推理延迟。
conclusion: TriTS通过多模态表征的动态融合，为高效且精准的长程时间序列预测提供了一种全新的解耦视角。
---

## 摘要
时间序列预测在金融、能源、交通和气象等关键领域发挥着至关重要的作用。然而，长期时间序列预测（LTSF）仍然是一个重大挑战，因为现实世界的信号包含高度纠缠的时间动态，很难从纯一维（1D）视角完全捕捉。为了突破这一表示瓶颈，我们提出了 TriTS，这是一种新颖的跨模态解耦框架，它将一维时间序列投影到正交的时间、频率和二维（2D）视觉空间中。为了在不产生 Vision Transformer (ViT) 高昂的 $O(N^2)$ 计算开销的情况下，无缝衔接一维到二维的模态鸿沟，我们引入了周期感知重塑（Period-Aware Reshaping）策略，并结合了 Visual Mamba (Vim)。该方法在保持线性计算复杂度的同时，将跨周期依赖关系高效地建模为全局视觉纹理。作为补充，我们为频率模态设计了一个多分辨率小波混合（MR-WM）模块，该模块将非平稳信号显式解耦为趋势和噪声分量，以实现细粒度的时频定位。最后，在时域中保留了一个流式线性分支，以确保数值稳定性。通过动态融合这三种互补的表示，TriTS 能够有效地适应多样化的数据背景。在多个基准数据集上的广泛实验表明，TriTS 达到了最先进（SOTA）的性能，通过大幅减少参数量和推理延迟，从根本上超越了现有的基于视觉的预测模型。

## Abstract
Time series forecasting plays a pivotal role in critical sectors such as finance, energy, transportation, and meteorology. However, Long-term Time Series Forecasting (LTSF) remains a significant challenge because real-world signals contain highly entangled temporal dynamics that are difficult to fully capture from a purely 1D perspective. To break this representation bottleneck, we propose TriTS, a novel cross-modal disentanglement framework that projects 1D time series into orthogonal time, frequency, and 2D-vision spaces.To seamlessly bridge the 1D-to-2D modality gap without the prohibitive $O(N^2)$ computational overhead of Vision Transformers (ViTs), we introduce a Period-Aware Reshaping strategy and incorporate Visual Mamba (Vim). This approach efficiently models cross-period dependencies as global visual textures while maintaining linear computational complexity. Complementing this, we design a Multi-Resolution Wavelet Mixing (MR-WM) module for the frequency modality, which explicitly decouples non-stationary signals into trend and noise components to achieve fine-grained time-frequency localization. Finally, a streaming linear branch is retained in the time domain to anchor numerical stability. By dynamically fusing these three complementary representations, TriTS effectively adapts to diverse data contexts. Extensive experiments across multiple benchmark datasets demonstrate that TriTS achieves state-of-the-art (SOTA) performance, fundamentally outperforming existing vision-based forecasters by drastically reducing both parameter count and inference latency.

---

## 论文详细总结（自动生成）

这篇论文提出了 **TriTS**，一个从多模态视角出发的长程时间序列预测（LTSF）框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现实世界的时间序列信号包含高度纠缠的动态（如趋势、季节性、非平稳噪声），仅从一维（1D）时域视角难以完全捕捉这些复杂特征。
*   **研究背景**：
    *   现有的跨模态方法（如 VisionTS）虽然利用了视觉模型的结构感知能力，但其依赖的 Vision Transformer (ViT) 具有 $O(N^2)$ 的计算复杂度，处理超长序列时内存和延迟开销巨大。
    *   频域方法（如基于 FFT 的模型）使用全局基函数，难以定位局部突变和非平稳偏移。
*   **整体含义**：TriTS 旨在通过将 1D 信号投影到**时间、频率、2D 视觉**三个正交空间，在打破表示瓶颈的同时，利用线性复杂度的架构提升计算效率。

### 2. 方法论：核心思想与关键技术细节
TriTS 的核心思想是**跨模态解耦与动态融合**，具体包含三个并行分支：
*   **视觉分支 (Vision Domain)**：
    *   **周期感知重塑**：利用自相关函数确定主周期 $P$，将 1D 序列折叠为 2D “时间图像”，将周期内变化映射为行纹理，跨周期演化映射为列结构。
    *   **Visual Mamba (Vim)**：引入双向选择性状态空间模型（Bi-SSM），以 $O(N)$ 的线性复杂度捕捉全局视觉纹理，替代了高开销的 ViT。
*   **频率分支 (Frequency Domain)**：
    *   **多分辨率小波混合 (MR-WM)**：采用离散小波变换（DWT）将信号分解为近似分量（趋势）和细节分量（噪声）。
    *   **独立分辨率分支**：为不同频率分量构建独立的参数空间，避免高频噪声污染低频趋势，实现精细的时频定位。
*   **时间分支 (Time Domain)**：
    *   **流式线性锚点**：使用指数移动平均（EMA）提取趋势，配合线性层提供数值稳定性，作为整个框架的“数值锚点”。
*   **自适应尺度感知融合**：设计了一个轻量级的门控网络，根据输入特征动态生成三个模态的置信度权重，进行加权求和。

### 3. 实验设计
*   **数据集**：涵盖 7 个真实世界基准数据集，包括 ETT (ETTh1, ETTh2, ETTm1, ETTm2)、Weather、Electricity (ECL) 和 Traffic。
*   **对比方法 (Benchmarks)**：对比了 8 种 SOTA 模型，涵盖不同类别：
    *   **视觉类**：VisionTS, VisionTS++。
    *   **频率类**：WPMixer, FDNet。
    *   **Transformer 类**：iTransformer, PatchTST。
    *   **线性/混合类**：TimeMixer, DLinear。
*   **预测设置**：回顾窗口固定为 96，预测长度 $T \in \{96, 192, 336, 720\}$。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce A800 (80G) GPU**。
*   **软件**：PyTorch 框架。
*   **效率表现**：论文明确对比了训练时间和内存占用。TriTS 使用了轻量级的 Vim-Tiny 骨干网络，相比基于 MAE-Base 的 VisionTS，显著降低了显存消耗和推理延迟。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集、4 种预测长度上进行了详尽的对比测试。
*   **消融实验**：分别验证了移除时域分支、频域分支、视觉分支以及门控融合模块对性能的影响，证明了各组件的必要性。
*   **分析深度**：提供了模态权重的可视化（解释了不同数据集对模态的偏好）以及计算效率对比图。
*   **客观性**：遵循社区标准基准，使用 MSE/MAE 指标，并采用了早停（Early Stopping）策略确保公平比较。

### 6. 主要结论与发现
*   **性能领先**：TriTS 在绝大多数数据集和预测长度上达到了 SOTA 性能，尤其在长序列预测中表现优异。
*   **模态互补**：时域分支保证数值稳定性，频域分支解析周期与噪声，视觉分支捕捉长程结构纹理。
*   **效率突破**：通过引入 Visual Mamba，TriTS 成功打破了视觉预测模型 $O(N^2)$ 的复杂度限制，实现了精度与效率的平衡。
*   **数据敏感性**：发现 Weather 数据集更依赖频域特征（局部周期强），而 ETT 数据集更依赖视觉/时域特征（全局依赖强）。

### 7. 优点
*   **多模态视角**：提供了一个统一的框架来整合不同域的优势，解决了单模态表示不足的问题。
*   **高效率**：利用 Mamba 架构实现了线性复杂度，使其在处理超长历史窗口时比传统 Transformer 更有优势。
*   **物理可解释性**：小波变换的引入比 FFT 更能处理非平稳信号，具有更好的物理先验。
*   **鲁棒性**：EMA 线性分支的设计有效缓解了深度模型在数值预测上的不稳定性。

### 8. 不足与局限
*   **周期依赖**：视觉分支的重塑依赖于准确的周期检测，对于缺乏明显周期性或周期高度变化的序列，效果可能受限。
*   **超参数敏感性**：多分支结构引入了较多超参数（如小波分解层数、Patch 大小等），可能需要针对不同场景进行精细调优。
*   **应用范围**：目前主要针对常规采样的时间序列，尚未探讨在不规则采样或极度稀疏数据上的表现。

（完）
