---
title: Agentic Forecasting using Sequential Bayesian Updating of Linguistic Beliefs
title_zh: 基于语言信念序列贝叶斯更新的智能体预测
authors: Kevin Murphy
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18576v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 使用LLM和语言信念进行二元预测的智能体系统
tldr: 本研究提出BLF（贝叶斯语言预测器），一种用于二元预测的智能体系统。该系统核心在于贝叶斯语言信念状态，通过迭代更新半结构化的证据摘要和概率估计，而非简单堆叠上下文。结合分层多试验聚合与分层校准技术，BLF在ForecastBench基准测试中显著超越了GPT-5、Grok 4.20等顶尖模型，并提供了低泄漏率的严谨回测框架。
selection_source: fresh_fetch
motivation: 针对现有预测系统在处理长上下文时效率低下以及极端预测校准不准的问题，寻求更高效的证据整合与聚合方法。
method: 利用LLM迭代更新包含数值概率与语言证据的信念状态，并采用带数据依赖先验的分层收缩聚合与Platt缩放校准。
result: 在ForecastBench的400个回测问题中，BLF性能位居榜首，消融实验证明结构化信念状态对性能提升的作用与联网搜索相当。
conclusion: 通过贝叶斯框架整合语言信念与分层统计校准，可以显著提升智能体在复杂预测任务中的准确性和鲁棒性。
---

## 摘要
我们提出了 BLF（贝叶斯语言预测器），这是一个用于二元预测的智能体系统，在 ForecastBench 基准测试中达到了最先进的性能。该系统基于三个核心理念：(1) 贝叶斯语言信念状态：一种结合了数值概率估计与自然语言证据摘要的半结构化表示，由大语言模型（LLM）在迭代工具使用循环的每一步进行更新。这与将所有检索到的证据附加到不断增长的上下文中的常用方法形成对比。(2) 分层多试验聚合：运行 $K$ 次独立试验，并使用带有数据依赖先验的 logit 空间收缩方法将其结合。(3) 分层校准：采用分层先验的 Platt 缩放，避免了对具有偏斜基准率的数据源的极端预测进行过度收缩。在 ForecastBench 排行榜的 400 个回测问题上，BLF 的表现优于所有顶尖的公开方法，包括 Cassi、GPT-5、Grok 4.20 和 Foresight-32B。消融研究表明，结构化信念状态的影响力与网络搜索权限相当，且收缩聚合和分层校准各自带来了显著的额外增益。此外，我们开发了一个泄漏率低于 1.5% 的稳健回测框架，并使用严谨的统计方法在控制各种噪声源的同时对不同方法进行比较。

## Abstract
We present BLF (Bayesian Linguistic Forecaster), an agentic system for binary forecasting that achieves state-of-the-art performance on the ForecastBench benchmark. The system is built on three ideas. (1) A Bayesian linguistic belief state: a semi-structured representation combining numerical probability estimates with natural-language evidence summaries, updated by the LLM at each step of an iterative tool-use loop. This contrasts with the common approach of appending all retrieved evidence to an ever-growing context. (2) Hierarchical multi-trial aggregation: running $K$ independent trials and combining them using logit-space shrinkage with a data-dependent prior. (3) Hierarchical calibration: Platt scaling with a hierarchical prior, which avoids over-shrinking extreme predictions for sources with skewed base rates.   On 400 backtesting questions from the ForecastBench leaderboard, BLF outperforms all the top public methods, including Cassi, GPT-5, Grok~4.20, and Foresight-32B. Ablation studies show that the structured belief state is as impactful as web search access, and that shrinkage aggregation and hierarchical calibration each provide significant additional gains.   In addition, we develop a robust back-testing framework with a leakage rate below 1.5\%, and use rigorous statistical methodology to compare different methods while controlling for various sources of noise.

---

## 论文详细总结（自动生成）

这是一份关于论文《Agentic Forecasting using Sequential Bayesian Updating of Linguistic Beliefs》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**如何利用大语言模型（LLM）构建更准确、更稳健的二元事件预测系统**。
*   **研究背景**：预测未来事件（如地缘政治、金融、公共卫生）对决策至关重要。虽然现有研究表明具备联网搜索能力的 LLM 预测水平已接近人类专家，但仍面临两大挑战：一是如何有效整合海量检索信息（传统方法只是简单堆叠上下文，导致信息过载）；二是如何在多次预测试验中进行有效的统计聚合与校准。
*   **研究动机**：作者旨在通过引入“贝叶斯语言信念状态”和分层统计技术，提升智能体在复杂预测任务中的逻辑一致性和准确性。

### 2. 方法论：核心思想与关键技术
作者提出了 **BLF (Bayesian Linguistic Forecaster)** 系统，其核心由三个支柱组成：

*   **贝叶斯语言信念状态 (Bayesian Linguistic Belief State)**：
    *   **核心思想**：智能体不只是积累原始文本，而是维护一个半结构化的 JSON 对象（信念状态），包含：数值概率估计、置信度、支持/反对的关键证据摘要、待解决的问题。
    *   **迭代更新**：在每一步工具调用循环中，LLM 根据新证据更新该信念状态。这模拟了序列贝叶斯推理过程，将历史信息压缩为结构化信念，避免了上下文窗口溢出。
*   **智能体循环与工具箱**：
    *   系统运行一个多步循环（最多 10 步），工具包括：网页搜索（Brave 引擎）、结果摘要、特定数据源提取（如 Yahoo Finance、FRED 经济数据、Wikipedia 历史版本、DBnomics 气温数据）。
*   **分层多试验聚合 (Hierarchical Multi-trial Aggregation)**：
    *   对每个问题运行 $K=5$ 次独立试验。除了简单的算术平均，还探索了**Logit 空间收缩 (Shrinkage)** 方法：当不同试验间差异较大时，通过经验贝叶斯先验将预测值向 0.5（中立）拉拢，以降低过拟合风险。
*   **分层校准 (Hierarchical Calibration)**：
    *   使用 **Platt 缩放 (Platt Scaling)** 进行后处理校准。创新点在于引入了**分层先验**，针对不同数据源（如 Polymarket 预测市场 vs. Wikipedia 事实）学习不同的截距偏移，防止全局校准对某些特定领域的极端预测过度收缩。

### 3. 实验设计
*   **数据集**：
    *   **ForecastBench**：主要基准，包含 400 个回测问题（涉及 791 个分辨率日期），涵盖市场类（Polymarket, Metaculus 等）和数据集类（经济指标、气温、股票等）。
    *   **AIBQ2**：Metaculus AI 预测锦标赛的 113 个二元问题。
*   **对比方法 (Baselines)**：
    *   ForecastBench 排行榜顶尖方法：Cassi, GPT-5 (Zero-shot), Grok 4.20, Foresight-32B。
    *   基准线：人群预测（Crowd estimate）、经验先验（Empirical prior）、零样本 LLM。
*   **评估指标**：Brier Index (BI, 官方主要指标), Adjusted Brier Index (ABI), Metaculus Baseline Score (MBS)。

### 4. 资源与算力
*   **模型使用**：主要使用 **Gemini-3.1-Pro** 作为核心推理引擎，Gemini-3-Flash 用于辅助任务（如泄露检测、摘要）。
*   **算力与成本**：
    *   论文未提及具体的 GPU 训练算力，因为该系统基于现有的 API 构建。
    *   **推理成本**：单次完整评估（400 题 × 5 试验）消耗约 50M-100M token，在 Gemini-3.1-Pro 上的成本约为 **230 美元**。
    *   **耗时**：单个问题耗时 1-8 分钟；使用 50 个并行 worker，完整评估需 1-3 小时。

### 5. 实验数量与充分性
*   **实验规模**：在 400 个真实世界预测问题上进行了测试，每个问题运行 5 次试验，总计约 2000 次智能体运行。
*   **消融实验**：进行了 11 组详细的消融实验，分别验证了信念状态、搜索工具、特定数据工具、模型能力、聚合方式和校准方法对性能的贡献。
*   **客观性与公平性**：
    *   **防泄露机制**：实施了四层防御（搜索日期过滤、LLM 泄露分类器、工具日期钳位、URL 黑名单），事后审计显示泄露率低于 1.5%，确保了回测的有效性。
    *   **统计严谨性**：使用 ANOVA 方差分析和自助法（Bootstrap）置信区间来控制问题难度差异带来的干扰。

### 6. 主要结论与发现
*   **性能领先**：BLF 在所有指标上均显著优于现有 SOTA 方法（p < 0.001）。其 ABI 评分（71.0）已接近人类超级预测者的中位数水平（70.9）。
*   **信念状态的关键性**：消融实验显示，**结构化信念状态对性能的提升（+5.1 BI）甚至超过了网页搜索本身（+3.4 BI）**。
*   **市场预测突破**：BLF 是唯一一个在市场类问题上显著击败“人群预测（Crowd）”基准的系统，其他顶尖 AI 方法在这些问题上通常只能与市场价格持平。
*   **校准的作用**：分层校准比全局校准更有效，尤其是在处理具有极端基准率（Base rates）的数据源时。

### 7. 优点与亮点
*   **架构创新**：将复杂的预测任务转化为“序列贝叶斯更新”的语言过程，解决了长上下文中的信息稀释问题。
*   **回测严谨**：针对 LLM 预测中最受诟病的“数据污染/时间泄露”问题，设计了极为详尽的防御和审计流程。
*   **统计深度**：不仅关注准确率，还深入探讨了评分规则（凸性 vs 线性）与聚合方法之间的数学关系。

### 8. 不足与局限
*   **任务局限**：目前仅专注于**二元（Yes/No）预测**，尚未扩展到多分类或连续数值预测。
*   **模型依赖**：系统性能高度依赖于底层 LLM（如 Gemini-3.1-Pro），在较弱模型（如 Flash）上性能下降明显。
*   **实时性验证**：虽然回测设计严谨，但由于 ForecastBench 的延迟，缺乏大规模的实时在线预测验证。
*   **成本问题**：作为智能体系统，其推理成本和 token 消耗远高于简单的零样本预测，可能限制其在大规模实时监控场景下的应用。

（完）
