---
title: "RePrompT: Recurrent Prompt Tuning for Integrating Structured EHR Encoders with Large Language Models"
title_zh: RePrompT：用于将结构化 EHR 编码器与大语言模型集成的循环提示微调
authors: "Arya Hadizadeh Moghaddam, Drew Ross, Mohsen Nayebi Kerdabadi, Dongjie Wang, Zijun Yao"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17725v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 使LLM适应纵向临床序列中的时序数据挖掘
tldr: 针对LLM在处理结构化电子健康记录（EHR）时存在的时序结构模糊和缺乏群体模式的问题，本文提出RePrompT框架。该框架通过提示微调集成结构化EHR编码器，利用循环机制保留纵向临床信息，并引入群体训练的编码器注入任务相关的群体知识。在MIMIC-III/IV上的实验证明，该方法在多项临床预测任务中显著优于现有基线。
selection_source: fresh_fetch
motivation: LLM在处理结构化EHR时难以捕捉复杂的时序规律，且在独立推理时缺乏群体层面的表征学习能力。
method: 提出RePrompT框架，通过循环机制整合历史就诊状态，并利用可训练的提示词注入来自群体训练编码器的任务对齐信息。
result: 在MIMIC-III和MIMIC-IV数据集上的多项临床预测任务中，RePrompT的表现均优于传统的EHR模型和纯LLM基线。
conclusion: RePrompT通过提示微调有效集成了结构化编码器与LLM，在保留时序信息的同时提升了临床预测的准确性。
---

## 摘要
大语言模型 (LLMs) 在挖掘电子健康记录 (EHR) 方面展现出巨大潜力，能够通过对纵向临床信息进行推理来捕捉上下文丰富的患者轨迹。然而，利用 LLMs 处理结构化 EHR（例如标准化的诊断和药物代码）面临两个关键挑战。首先，将带有时间戳的 EHR 序列转换为纯文本可能会模糊时间结构和代码标识，从而削弱捕捉代码共现和纵向规律的能力。其次，与在队列上训练、学习跨患者共享且任务对齐的表示空间的预测模型不同，LLMs 通常应用于病例隔离的推理场景，即独立处理每个患者，而未能利用群体层面的模式。为了应对这些挑战，我们提出了 RePrompT，这是一个时间感知型 LLM 框架，它通过提示微调集成结构化 EHR 编码器，且无需修改底层架构。具体而言，RePrompT 循环地整合来自先前就诊的隐藏状态以保留纵向信息，并通过源自队列训练、任务对齐的 EHR 编码器的可训练提示标记注入群体层面的信息。在 MIMIC-III 和 MIMIC-IV 上的实验表明，RePrompT 在多个临床预测任务中始终优于基于 EHR 和基于 LLM 的基准模型。

## Abstract
Large Language Models (LLMs) have shown strong promise for mining Electronic Health Records (EHRs) by reasoning over longitudinal clinical information to capture context-rich patient trajectories. However, leveraging LLMs for structured EHRs (e.g., standardized diagnosis and medication codes) presents two key challenges. First, translating time-stamped EHR sequences into plain text can obscure both temporal structure and code identities, weakening the ability to capture code co-occurrence and longitudinal regularities. Second, unlike cohort-trained predictive models that learn a shared, task-aligned representation space across patients, LLMs are often applied in a case-isolated inference setting where each patient is processed independently without leveraging population-level patterns. To address these challenges, we introduce RePrompT, a time-aware LLM framework that integrates structured EHR encoders through prompt tuning, without modifying underlying architectures. Specifically, RePrompT recurrently incorporates latent states from prior visits to preserve longitudinal information, and injects population-level information through trainable prompt tokens derived from a cohort-trained, task-aligned EHR encoder. Experiments on MIMIC-III and MIMIC-IV demonstrate that RePrompT consistently outperforms both EHR-based and LLM-based baselines across multiple clinical prediction tasks.

---

## 论文详细总结（自动生成）

### RePrompT：用于将结构化 EHR 编码器与大语言模型集成的循环提示微调 —— 论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
大语言模型（LLMs）在处理电子健康记录（EHR）时展现了强大的推理能力，但直接应用面临两大挑战：
*   **时序结构模糊**：将带有时间戳的 EHR 序列（诊断、药物代码等）线性化为纯文本会破坏其固有的时间结构和代码间的共现规律，导致模型难以捕捉患者病情的纵向演变。
*   **缺乏群体知识**：传统 EHR 模型通过在大型队列上训练，能学习到跨患者的、任务对齐的特征空间（如疾病共现模式）。而 LLM 通常在推理时将每个患者视为孤立案例，无法显式利用群体层面的规律。
**研究动机**：开发一种既能保留 LLM 推理能力，又能整合结构化时序信息和群体知识的框架。

#### 2. 论文提出的方法论
论文提出了 **RePrompT** 框架，其核心思想是通过**提示微调（Prompt Tuning）**将结构化编码器的输出和历史状态注入 LLM，而无需修改 LLM 的底层架构。关键技术包括：
*   **临床记录合成（Clinical Record Synthesis）**：利用强大的通用模型（如 DeepSeek-V3）将冗长、嘈杂的就诊记录和结构化代码去噪并总结为简洁的文本摘要。
*   **状态循环提示微调（State-Recurrent Prompt Tuning）**：为了捕捉纵向依赖，模型每次只处理一次就诊。将上一次就诊的 LLM 隐藏状态通过线性变换转化为“软提示（Soft Prompt）”，传递给当前步骤，实现时序信息的循环传递。
*   **结构化编码提示微调（Struct-Encoded Prompt Tuning）**：使用传统的结构化 EHR 编码器（如 RETAIN）处理医疗代码序列，提取群体层面的表征，并将其作为另一组可训练的软提示注入 LLM。
*   **预测与优化**：在 LLM（如 Llama 3.1 1B）的顶层添加分类头，利用最后一次就诊的隐藏状态进行二分类（死亡/再入院）或多标签分类（药物推荐）任务。

#### 3. 实验设计
*   **数据集**：使用了两个大规模真实世界数据集：**MIMIC-III** 和 **MIMIC-IV**。
*   **预测任务**：死亡率预测（Mortality）、再入院预测（Readmission）、药物推荐（Medication Recommendation）。
*   **Benchmark 与对比方法**：
    *   **传统 EHR 模型**：Deepr, RETAIN, GRAM, GRASP, AdaCare, StageNet, Adore, ARCI。
    *   **LLM 基线**：Zero-Shot（GPT-5 级别模型）、标准 Prompt-Tuning、COCONUT（连续思维链方法）。

#### 4. 资源与算力
*   **硬件环境**：使用了高性能服务器，配备 **3 块 NVIDIA A6000 GPU**，256 GB RAM 和 48 核 CPU。
*   **模型配置**：合成阶段使用 DeepSeek-V3，预测阶段使用 Llama 3.1 1B（通过 LLM2Vec 增强）。
*   **训练细节**：LLM 参数保持冻结，仅训练 EHR 编码器、线性变换层和分类头。论文附录中提供了推理时间分析，处理 8 个患者的批次约需 1.44 秒。

#### 5. 实验数量与充分性
*   **实验规模**：在两个数据集上针对三个不同性质的任务进行了广泛测试。
*   **消融实验**：
    *   验证了“状态循环”和“结构化编码”两个模块的各自贡献。
    *   验证了 DeepSeek 总结步骤对性能的提升作用。
    *   测试了不同 EHR 编码器（LSTM, Transformer, RETAIN）作为骨干网的效果。
*   **客观性**：采用了 AUROC、PRAUC、F1-score 和 Jaccard 相似度等多种指标，并进行了多次运行取平均值，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **性能领先**：RePrompT 在所有任务和数据集上均显著优于传统的 EHR 深度学习模型和现有的 LLM 提示方法。
*   **协同效应**：结构化编码器提供的群体知识与 LLM 的文本推理能力具有互补性，结合两者能更精准地建模患者轨迹。
*   **时序建模的重要性**：消融实验显示，移除“状态循环”模块会导致性能大幅下降，证明了显式处理就诊间时序依赖的必要性。
*   **小模型胜大模型**：经过特定任务微调的小型 LLM（1B 参数）在临床预测上的表现优于零样本的大型通用模型。

#### 7. 优点（亮点）
*   **非侵入性集成**：通过软提示技术将结构化模型与 LLM 结合，无需对庞大的 LLM 进行全参数微调，降低了计算成本。
*   **多模态融合**：有效地整合了非结构化的临床笔记和结构化的医疗代码。
*   **时间感知**：引入循环机制解决了 LLM 处理长序列 EHR 时容易丢失时间敏感信息的问题。

#### 8. 不足与局限
*   **推理延迟**：虽然比全参数微调快，但由于引入了循环机制和 LLM 推理，其延迟（1.44s/batch）仍显著高于传统轻量级模型（如 RETAIN 仅需 0.05s）。
*   **数据依赖性**：模型性能高度依赖于 EHR 数据的质量和完整性，对于数据缺失严重的场景可能表现受限。
*   **泛化风险**：实验仅限于 MIMIC 系列数据集，跨机构、跨系统的泛化能力（Domain Shift）尚待进一步验证。

（完）
