---
title: "Bi-level Heterogeneous Learning for Time Series Foundation Models: A Federated Learning Approach"
title_zh: 时间序列基础模型的双层异构学习：一种联邦学习方法
authors: "Shengchao Chen, Guodong Long, Dikai Liu, Jing Jiang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06727v1"
tags: ["query:ts-fm"]
score: 10.0
evidence: 时序基础模型的双层学习方法
tldr: 针对时间序列基础模型（TSFM）训练中存在的跨域异构性和梯度冲突问题，本文提出了一种双层异构联邦学习方法。该方法通过局部正则化减少域内冲突，并利用域感知聚合增强跨域协作，从而在异构环境下从头训练出更稳健的TSFM。实验证明，该方法在点预测、概率预测及零样本任务中均优于现有基准。
selection_source: fresh_fetch
motivation: 现有的时间序列基础模型训练常采用混合批次策略，导致严重的梯度冲突和表示质量下降。
method: 提出一种双层联邦学习框架，结合局部正则化以实现域内语义一致性，并采用域感知聚合来处理域间差异。
result: 在多个基准测试中，该方法在点预测、概率预测和大规模零样本性能上均显著优于集中式和联邦式基准模型。
conclusion: 该研究为在异构数据环境下从零开始训练高性能时间序列基础模型提供了一种灵活且有效的联邦学习路径。
---

## 摘要
时间序列数据中的异构性比视觉或语言数据更为显著，因为时间动态在不同领域和任务之间存在巨大差异。现有的从头开始训练时间序列基础模型（TSFM）的工作通常采用混合批次策略，合并大规模数据集，这可能导致梯度冲突并降低表示质量。为解决这一问题，我们提出了一种细粒度学习方法，在减少跨域干扰的同时，从异构序列中提取不变知识。我们从两个层面刻画异构性：域间和域内。为了应对这种双层异构性，我们设计了一种联邦学习方法，通过局部正则化强制实现域不变且语义一致的表示，从而缓解域内冲突；并通过域感知聚合增强跨域协作，从而解决域间差异。在不同基准测试上的实验表明，使用我们方法训练的 TSFM 在点预测和概率预测方面始终优于集中式和联邦式 TSFM 基准模型，同时在大规模场景下也实现了具有竞争力的零样本性能，为在异构环境中从头开始训练 TSFM 提供了一条灵活的路径。

## Abstract
Heterogeneity in time series data is more pronounced than in vision or language, as temporal dynamics vary substantially across domains and tasks. Existing efforts on training time series foundation models (TSFMs) from scratch are often trained with mixed-batch strategies that merge large-scale datasets, which can cause gradient conflicts and degrade representation quality. To address this, we propose a fine-grained learning method that distills invariant knowledge from heterogeneous series while reducing cross-domain interference. We characterize heterogeneity at two levels: inter-domain and intra-domain. To tackle this bi-level heterogeneity, we design a federated learning method that mitigates intra-domain conflicts by enforcing domain-invariant and semantically consistent representations through local regularization, and addresses inter-domain discrepancies by enhancing cross-domain collaboration via domain-aware aggregation. Experiments across diverse benchmarks show that TSFMs trained with our method consistently outperform both centralized and federated TSFM baselines in point and probabilistic forecasting, while also achieving competitive zero-shot performance at scale, offering a flexible pathway for training TSFMs from scratch in heterogeneous environments.

---

## 论文详细总结（自动生成）

这篇论文《Bi-level Heterogeneous Learning for Time Series Foundation Models: A Federated Learning Approach》提出了一种名为 **FedTRL** 的联邦学习框架，旨在解决时间序列基础模型（TSFM）在异构数据环境下的预训练难题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：时间序列数据具有极强的**异构性**（跨领域动态差异大）。现有的 TSFM 训练通常采用“集中式混合批次”策略，将不同来源的数据简单合并，这会导致严重的**梯度冲突**，掩盖领域特定的结构，降低表示质量。
*   **研究动机**：
    *   **双层异构性**：论文指出异构性存在于两个层面：**域间（Inter-domain）**（不同来源数据的协变量偏移）和**域内（Intra-domain）**（单一数据集内部的潜在子域漂移或概念漂移）。
    *   **数据碎片化**：时序数据往往分散在不同机构，难以集中，联邦学习（FL）是天然的解决方案，但现有 FL 方法通常假设局部数据是同质的，无法处理双层异构性。

### 2. 方法论（FedTRL）
核心思想是通过联邦学习框架，在保护隐私的同时，通过细粒度的局部优化和全局聚合来提取域不变知识。
*   **局部更新（客户端）**：
    *   **扩散重构（Diffusion Reconstruction）**：采用基于扩散模型的无监督学习，捕捉点、补丁（Patch）和序列级的时序语义。
    *   **子域对抗正则化（GRL）**：引入梯度反转层（GRL）和子域分类器，强制编码器忽略子域特定的干扰信号，提取域内一致的表示。
    *   **原型对齐（Prototype Alignment）**：将局部原型与全局原型对齐，确保跨客户端的语义一致性。
    *   **双头预测架构**：支持确定性点预测（MSE 损失）和概率预测（Student-t 分布，NLL 损失），并采用热身策略稳定训练。
*   **全局聚合（服务端）**：
    *   **域感知聚合（DaG）**：不再简单平均，而是根据“域不变性得分”（通过训练全局域分类器获得）和“语义对齐偏差”动态调整每个客户端的权重，防止主导域偏置全局模型。

### 3. 实验设计
*   **数据集**：
    *   **TSLib**：包含 ETT、Electricity、Traffic、Weather、Exchange 等 8 个标准基准。
    *   **RW-Bench**：论文新引入的真实世界天气基准，包含 15 个地理分布多样的气象站数据（2022-2025年），保留了噪声和缺失值。
    *   **GIFT-eval & FEV Leaderboard**：用于评估零样本概率预测性能的大规模基准（涵盖数十个数据集）。
*   **对比方法**：
    *   **集中式基础模型**：Time-MoE, Moirai, TimesFM, Chronos, Moment 等。
    *   **联邦学习基准**：FedAvg, FFTS, 以及针对异构性的 FedProx, FedRep 等。
    *   **自监督学习方法**：SimMTM, PatchTST, TimeMAE, CoST 等。

### 4. 资源与算力
论文明确说明了实验环境：
*   **域内预测实验**：使用 4 张 Nvidia RTX A5500-24GB GPU。
*   **大规模基础模型预训练**：使用 **16 张 Nvidia A100-80G GPU**。
*   **训练参数**：全局回合数最高达 10,000 轮，局部 Epoch 为 100，Batch Size 为 2048。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。涵盖了域内预测、全量微调、零样本点预测、零样本概率预测等多个维度。
*   **消融实验**：针对 GRL、原型对齐、DaG 聚合等核心组件进行了详细的消融分析。
*   **扩展性分析**：测试了模型参数量从 38M 到 302M 的扩展表现，以及预训练数据量从 90B 到 540B 的性能趋势。
*   **客观性**：通过引入 RW-Bench 这一非清洗的真实数据集，增强了实验的现实说服力，且对比了当前最先进的商业级基础模型。

### 6. 主要结论与发现
*   **性能超越**：FedTRL 训练的模型在零样本点预测和概率预测上不仅超过了现有的联邦学习方法，甚至在多个指标上**超越了集中式训练**的基础模型（如 Time-MoE）。
*   **解决冲突**：证明了通过双层异构学习可以有效缓解梯度冲突，提升表示的通用性。
*   **可扩展性**：FedTRL 展现了良好的 Scaling Law，模型性能随参数量和数据量的增加而稳定提升。

### 7. 优点与亮点
*   **理论与实践结合**：对双层异构性给出了明确的数学定义和证明（如 Wasserstein 重心、H-散度）。
*   **架构灵活性**：提出的 DaG 聚合机制和局部对抗学习可以无缝集成到现有的基于重构的时序模型中。
*   **真实场景考量**：RW-Bench 的引入填补了学术数据集与真实工业数据（含噪声、缺失值）之间的鸿沟。

### 8. 不足与局限
*   **计算开销**：局部采用扩散模型进行重构，相比于简单的掩码自动编码器（MAE），计算成本和训练时间可能更高。
*   **超参数敏感性**：虽然论文做了敏感性分析，但框架涉及多个平衡系数（$\alpha, \beta, \lambda$ 等），在不同领域数据下可能需要精细调优。
*   **通信负担**：尽管只上传编码器和原型，但在大规模模型（302M+）和频繁聚合的情况下，联邦学习的通信开销仍是一个潜在瓶颈。

（完）
