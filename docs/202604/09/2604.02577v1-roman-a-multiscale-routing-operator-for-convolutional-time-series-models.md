---
title: "ROMAN: A Multiscale Routing Operator for Convolutional Time Series Models"
title_zh: ROMAN：一种用于卷积时间序列模型的多尺度路由算子
authors: Gonzalo Uribarri
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02577v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 时间序列模型的多尺度路由算子
tldr: ROMAN是一种针对时间序列的确定性算子，通过构建抗混叠多尺度金字塔并将不同尺度的窗口堆叠为伪通道，将时间尺度和粗略位置信息映射到通道结构中。该方法在缩短序列长度、提高计算效率的同时，增强了卷积模型对长程相关性和多尺度交互的感知能力。实验表明，ROMAN在处理依赖时间结构的分类任务时表现优异，是长序列时间序列分析的有效预处理工具。
selection_source: fresh_fetch
motivation: 旨在解决标准卷积模型在处理长序列时难以捕捉多尺度交互和粗略位置信息，且计算开销较大的问题。
method: 通过构建多尺度金字塔并提取固定长度窗口堆叠为伪通道，将时间维度信息转化为通道维度表示。
result: 在合成任务中验证了其对位置感知和多尺度交互的有效性，并在UCR/UEA基准测试中展现了良好的效率和任务相关的准确性提升。
conclusion: ROMAN为卷积时间序列模型提供了一种高效的归纳偏置控制机制，特别适用于需要多尺度特征和位置信息的长序列任务。
---

## 摘要
我们引入了 ROMAN（路由多尺度表示），这是一种用于时间序列的确定性算子，它在减少序列长度的同时，将时间尺度和粗粒度时间位置映射到显式的通道结构中。ROMAN 构建了一个抗混叠的多尺度金字塔，从每个尺度提取固定长度的窗口，并将它们堆叠为伪通道，从而产生一种标准卷积分类器可以处理的紧凑表示。通过这种方式，ROMAN 提供了一种简单的机制来控制下游模型的归纳偏置：它可以减少时间不变性，使时间池化隐式地具备粗粒度位置感知能力，并通过通道混合揭示多尺度交互，同时通常通过缩短处理的时间轴来提高计算效率。我们对 ROMAN 算子进行了形式化分析，然后通过衡量其作为四种代表性卷积分类器（MiniRocket、MultiRocket、标准基于 CNN 的分类器和全卷积网络 (FCN) 分类器）预处理步骤的影响，从两个互补的方面对其进行了评估。首先，我们设计了合成时间序列分类任务，这些任务隔离了粗粒度位置感知、长程相关性、多尺度交互和完全位置不变性，结果表明 ROMAN 的表现与其预期机制一致，并且在类别信息依赖于标准池化卷积往往会抑制的时间结构时最为有用。其次，我们在 UCR 和 UEA 存档的长序列子集上对使用和不使用 ROMAN 的相同模型进行了基准测试，结果表明 ROMAN 提供了一种具有实际用途的替代表示，其对准确性的影响取决于具体任务，但对效率的影响通常是有利的。代码可在 https://github.com/gon-uri/ROMAN 获取。

## Abstract
We introduce ROMAN (ROuting Multiscale representAtioN), a deterministic operator for time series that maps temporal scale and coarse temporal position into an explicit channel structure while reducing sequence length. ROMAN builds an anti-aliased multiscale pyramid, extracts fixed-length windows from each scale, and stacks them as pseudochannels, yielding a compact representation on which standard convolutional classifiers can operate. In this way, ROMAN provides a simple mechanism to control the inductive bias of downstream models: it can reduce temporal invariance, make temporal pooling implicitly coarse-position-aware, and expose multiscale interactions through channel mixing, while often improving computational efficiency by shortening the processed time axis. We formally analyze the ROMAN operator and then evaluate it in two complementary ways by measuring its impact as a preprocessing step for four representative convolutional classifiers: MiniRocket, MultiRocket, a standard CNN-based classifier, and a fully convolutional network (FCN) classifier. First, we design synthetic time series classification tasks that isolate coarse position awareness, long-range correlation, multiscale interaction, and full positional invariance, showing that ROMAN behaves consistently with its intended mechanism and is most useful when class information depends on temporal structure that standard pooled convolution tends to suppress. Second, we benchmark the same models with and without ROMAN on long-sequence subsets of the UCR and UEA archives, showing that ROMAN provides a practically useful alternative representation whose effect on accuracy is task-dependent, but whose effect on efficiency is often favorable. Code is available at https://github.com/gon-uri/ROMAN

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ROMAN**（ROuting Multiscale representAtioN）的确定性算子，旨在通过重新路由时间序列的结构来增强卷积模型的性能和效率。以下是对该论文的详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的卷积时间序列分类（TSC）模型（如 ROCKET 系列或 FCN）在捕捉“局部模式”方面非常出色，但由于普遍采用全局时间池化（Global Pooling），它们往往会丢失**粗粒度位置信息**和**多尺度交互信息**。
*   **研究动机**：简单地移除池化层会导致模型变得庞大且难以优化。作者希望找到一种方法，既能保留卷积模型的简洁性和鲁棒性，又能显式地将时间位置和尺度信息暴露给模型，同时降低处理长序列的计算开销。

### 2. 方法论：核心思想与技术细节
ROMAN 是一个**确定性的预处理算子**，它将输入的时间序列转换为一种“多尺度路由”表示。
*   **核心思想**：将时间轴上的“尺度”和“粗略位置”映射到“通道”维度中，从而缩短序列长度。
*   **关键流程**：
    1.  **构建抗混叠金字塔**：使用低通滤波器（二项式滤波器）和下采样（Decimation）构建一个 $S$ 层的双向金字塔。
    2.  **窗口提取**：在每个尺度 $s$ 上，提取长度为 $L_{base}$（最底层序列长度）的固定长度窗口。
    3.  **伪通道堆叠**：将所有尺度、所有位置提取出的窗口在通道维度（Channel axis）上进行拼接。
*   **超参数**：$S$（尺度深度）控制时间轴缩短的程度；$\alpha$（重叠率）控制窗口采样的密度。
*   **结果**：输入形状从 $(C, L)$ 变为 $(C', L_{base})$，其中 $C' \gg C$ 且 $L_{base} \ll L$。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：设计了四个针对性任务：粗略位置感知、长程相关性、多尺度交互、完全位置不变性。
    *   **真实数据集**：选取了 UCR 和 UEA 存档中序列长度 $L \ge 256$ 的长序列子集（共 83 个 UCR 数据集和 15 个 UEA 数据集）。
*   **对比方法（Backbones）**：
    *   **MiniRocket / MultiRocket**：代表池化驱动的随机卷积模型。
    *   **CNNClassifier / FCNClassifier**：代表学习型卷积网络（前者保留位置索引，后者使用全局平均池化）。
*   **Benchmark**：以 $S=1$（即原始输入表示）作为基准线，对比 $S \in \{2, 3, 4\}$ 时的表现。

### 4. 资源与算力
*   **硬件设备**：实验在一台 **MacBook Pro (M1 Pro 芯片, 10核 CPU, 32GB 统一内存)** 上运行。
*   **算力说明**：论文未提及使用 GPU，这反映了 ROMAN 及其配套的 ROCKET 类模型对算力要求较低，主要依赖 CPU 计算。
*   **时长与效率**：论文详细记录了训练和推理的时间比率，显示 ROMAN 在 $S=4$ 时能将推理时间缩短至基准的 30%-40%（针对 ROCKET 类模型）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了近百个公开数据集，每个实验运行了 5 到 10 个随机种子（Seeds/Realizations）。
*   **充分性**：实验设计非常全面。不仅有大规模的存档基准测试，还通过精心设计的合成实验验证了算子的物理机制。此外，还包含了一个简单的集成学习（Ensemble）案例研究，证明了不同 $S$ 值带来的表示多样性。
*   **公平性**：所有对比均在相同的 Backbone 和超参数设置下进行，仅改变输入表示，确保了评估的客观性。

### 6. 主要结论与发现
*   **机制验证**：ROMAN 确实能恢复被全局池化抑制的位置信息，并增强模型处理长程相关性和跨尺度交互的能力。
*   **任务相关性**：ROMAN 的准确率提升具有任务依赖性。在需要位置信息的任务中表现优异；但在完全平移不变（即模式出现在哪都不影响分类）的任务中，ROMAN 可能会因为削弱了这种不变性而导致性能下降。
*   **效率提升**：通过缩短时间轴，ROMAN 显著降低了下游模型的计算负担，尤其是在处理极长序列时。

### 7. 优点
*   **模型无关性**：作为一个预处理算子，它可以插入任何现有的卷积架构之前，无需修改模型内部结构。
*   **确定性与简单性**：不涉及参数学习，计算开销极低，且易于实现。
*   **归纳偏置可控**：通过调整 $S$，研究者可以灵活地在“平移不变性”和“位置感知”之间寻找平衡。

### 8. 不足与局限
*   **通道爆炸风险**：随着 $S$ 的增加，伪通道数量 $C'$ 会迅速增长，对于多变量序列（$C$ 较大时），可能导致内存压力或表示过于碎片化。
*   **适用范围限制**：主要针对长序列任务。对于短序列，金字塔下采样会导致信息过快丢失。
*   **超参数敏感性**：虽然 $S=1$ 到 $4$ 提供了多样性，但如何为特定任务自动选择最优的 $S$ 仍是一个未解决的问题。

（完）
