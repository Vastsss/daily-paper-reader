---
title: End-to-End Learning of Correlated Operating Reserve Requirements in Security-Constrained Economic Dispatch
title_zh: 安全约束经济调度中相关运行储备需求的端到端学习
authors: "Owen Shen, Hung-po Chao, Haihao Lu, Patrick Jaillet"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05167v1"
tags: ["query:llm"]
score: 8.0
evidence: 经济调度与可再生能源预测误差
tldr: "本文针对安全约束经济调度（SCED）中备用容量需求依赖可再生能源预测误差相关性的问题，提出了一种端到端学习框架。该方法将相关备用集设计建模为可训练的鲁棒优化问题，通过最小化调度成本并结合共形预测确保覆盖率。利用KKT信息实现梯度传递，优化椭球不确定性集的形状。在IEEE 118节点系统上的测试显示，该方法在保证可靠性的同时，比传统基准降低了4.8%的调度成本。"
selection_source: fresh_fetch
motivation: 现有的电力调度备用需求通常基于预设的相关性结构，未能针对具体的调度成本和任务目标进行端到端优化。
method: 将椭球不确定性集的设计转化为单阶段可微目标函数，利用KKT对偶信息获取任务梯度，并引入共形校准保证覆盖率。
result: "在IEEE 118节点系统实验中，学习到的静态椭球体相比样本协方差基准降低了4.8%的调度成本，且满足目标覆盖水平。"
conclusion: 通过将不确定性建模与下游调度任务深度耦合，可以有效提升电力系统运行的经济效益并确保运行安全。
---

## 摘要
安全约束经济调度（SCED）中的运行储备需求在很大程度上取决于可再生能源预测误差的假设相关结构，然而，这种结构通常是外生指定的，而不是针对调度任务本身进行学习的。本文将相关储备集设计建模为一个端到端可训练的鲁棒优化问题：在满足目标覆盖率要求的前提下，选择椭球不确定性集的形状以最小化鲁棒调度成本。通过将覆盖率约束刻画为与形状相关的半径，原始的双层问题转变为单阶段可微目标，并且来自 SCED 求解的 KKT/对偶信息提供了任务梯度，而无需通过求解器进行微分。对于未知分布，采用四路训练/调优/校准/测试划分，结合用于训练的平滑分位数敏感度估计器和用于部署的分裂共形校准，在可交换性条件下产生有限样本边际覆盖，并为平滑目标提供一致的梯度估计器。同样的任务梯度也可以传递给上游的上下文相关编码器，我们将其作为次要扩展进行报告。该框架在 IEEE 118 节点系统上进行了评估，采用包含区域间传输约束的耦合 SCED 公式。与样本协方差基准相比，学习到的静态椭球在保持经验覆盖率高于目标水平的同时，将调度成本降低了约 4.8%。

## Abstract
Operating reserve requirements in security-constrained economic dispatch (SCED) depend strongly on the assumed correlation structure of renewable forecast errors, yet that structure is usually specified exogenously rather than learned for the dispatch task itself. This paper formulates correlated reserve-set design as an end-to-end trainable robust optimization problem: choose the ellipsoidal uncertainty-set shape to minimize robust dispatch cost subject to a target coverage requirement. By profiling the coverage constraint into a shape-dependent radius, the original bilevel problem becomes a single-stage differentiable objective, and KKT/dual information from the SCED solve provides task gradients without differentiating through the solver. For unknown distributions, a four-way train/tune/calibrate/test split combines a smoothed quantile-sensitivity estimator for training with split conformal calibration for deployment, yielding finite-sample marginal coverage under exchangeability and a consistent gradient estimator for the smoothed objective. The same task gradient can also be passed upstream to context-dependent encoders, which we report as a secondary extension. The framework is evaluated on the IEEE~118-bus system with a coupled SCED formulation that includes inter-zone transfer constraints. The learned static ellipsoid reduces dispatch cost by about 4.8\% relative to the Sample Covariance baseline while maintaining empirical coverage above the target level.

---

## 论文详细总结（自动生成）

这篇论文提出了一种在电力系统安全约束经济调度（SCED）中，通过端到端学习来优化运行储备需求（Operating Reserve Requirements）的新框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在可再生能源高渗透的电力系统中，如何确定既能保证系统可靠性（覆盖预测误差），又能最小化调度成本的“储备不确定性集（Uncertainty Set）”。
*   **研究动机**：传统的储备需求通常基于经验或简单的统计方法（如样本协方差），这些方法是“任务无关”的。由于预测误差在空间和时间上具有强相关性，且不同区域的储备成本差异巨大，因此需要一种能够根据下游调度任务的经济性来自动调整不确定性集形状（相关性结构）的方法。

### 2. 方法论
*   **核心思想**：将储备集设计建模为一个端到端可训练的鲁棒优化问题。通过优化椭球不确定性集的形状参数（Cholesky 因子 $L$），在满足目标覆盖率 $\tau$ 的前提下，最小化 SCED 的期望成本。
*   **关键技术细节**：
    *   **半径剖面化（Radius Profiling）**：将覆盖率约束转化为一个依赖于形状的半径函数 $\rho(\theta)$，从而将双层优化问题简化为单阶段目标。
    *   **包络梯度（Envelope Gradient）**：利用 SCED 问题的 KKT 对偶信息（影子价格）直接计算调度成本对不确定性集形状的梯度，**无需通过优化求解器进行隐式微分**，极大地降低了计算开销。
    *   **四路数据划分**：采用训练（训练形状）、调优（估计分位数敏感度）、校准（共形预测确定最终半径）、测试（评估）的划分方式。
    *   **分裂共形校准（Split Conformal Calibration）**：在部署前利用独立数据校准半径，确保在分布无关的情况下满足边际覆盖率保证。
*   **算法流程**：迭代执行“求解 SCED 提取对偶变量 -> 计算形状梯度 -> 更新椭球参数 -> 投影回可行域”，最后通过共形预测锁定半径。

### 3. 实验设计
*   **实验场景**：基于 **IEEE 118-bus 系统**，将其划分为 10 个调度区和 5 个地理区域。考虑了 15 维不确定性来源（各区域的负荷、太阳能、风能预测误差）。
*   **对比基准（Benchmarks）**：
    1.  **Sample Covariance**：基于样本协方差矩阵的传统相关性模型。
    2.  **Independent**：忽略相关性，仅考虑边际标准差的对角阵模型。
*   **对比方法**：
    *   **Learned (Static)**：本文提出的静态学习椭球。
    *   **Learned (Contextual)**：扩展版本，使用神经网络编码器根据实时上下文（如预测功率、时间等）动态生成椭球形状。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。
*   **软件环境**：使用了 Python 的 `pandapower` 库进行电力系统建模，求解器使用了开源的 `HiGHS`（线性规划求解器）。由于该方法不需要通过求解器求导，其计算效率主要取决于单次 SCED 求解的速度。

### 5. 实验数量与充分性
*   **数据规模**：使用了 4 年（35,040 小时）的合成数据，这些数据校准自真实的德国 OPSD 和美国 EIA 负荷数据。
*   **实验覆盖**：
    *   **解耦 SCED**：基础区域储备模型。
    *   **耦合 SCED**：包含复杂的区域间传输约束（Transfer Constraints），验证了方法在受限环境下的鲁棒性。
    *   **目标水平扫描**：测试了不同覆盖率目标（90% 到 99%）下的表现。
*   **充分性评价**：实验设计较为充分，通过对比独立模型和协方差模型，证明了“学习到的相关性”优于“统计相关性”。但上下文学习（Contextual）部分仅报告了单次随机种子的结果，统计严谨性略逊于静态部分。

### 6. 主要结论与发现
*   **成本降低**：在 IEEE 118 节点系统中，学习到的静态椭球比样本协方差基准**降低了约 4.8% 的调度成本**。
*   **储备效率**：在保持相同覆盖率（95%）的情况下，学习方法显著减少了总储备采购量（从 1,572 MW 降至 977 MW），说明它能将储备精准分配到“最经济”的区域。
*   **可靠性**：通过共形校准，所有方法在测试集上的实际覆盖率均达到了或超过了预设目标。

### 7. 优点
*   **端到端闭环**：直接针对经济目标优化不确定性集，而非单纯的统计拟合。
*   **计算高效**：巧妙利用对偶信息避开了高成本的隐式微分（Implicit Differentiation）。
*   **统计保证**：引入共形预测框架，解决了机器学习模型在电力系统安全约束下难以提供可靠性保证的痛点。

### 8. 不足与局限
*   **时间相关性假设**：共形预测依赖于“可交换性（Exchangeability）”假设，而电力系统数据具有显著的时间序列相关性，虽然论文使用了块自助法（Block Bootstrap）评估，但在极端非平稳环境下的有效性仍存疑。
*   **几何限制**：目前仅限于椭球不确定性集，虽然易于计算，但可能无法捕捉非对称或多峰的复杂误差分布。
*   **上下文模型稳定性**：上下文编码器（Contextual）在某些实验中表现不如静态模型稳定，可能存在过拟合或梯度估计方差大的问题。

（完）
