---
title: Interpretable models for forecasting high-dimensional functional time series
title_zh: 高维函数型时间序列预测的可解释模型
authors: "Han Lin Shang, Cristian F. Jiménez-Varón"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28344v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 高维函数型时间序列的建模与预测
tldr: 本研究针对具有时间依赖性和截面相关性的高维函数型时间序列，提出了一种可解释的预测模型。通过函数型方差分析（FANOVA）将数据分解为由特定因素（如地区、性别）决定的确定性均值结构和随时间变化的残差过程，并利用函数因子模型捕捉残差中的随机趋势。以1975-2023年日本分地区死亡率为案例，证明了该方法在提高预测准确性的同时，增强了模型的可解释性，为政策制定提供了透明的科学依据。
selection_source: fresh_fetch
motivation: 旨在解决高维函数型时间序列预测中模型复杂且缺乏可解释性，以及难以有效处理跨部门相关性的挑战。
method: 结合函数型方差分析（FANOVA）进行结构化分解，并利用函数因子模型对残差过程中的随机趋势进行建模与预测。
result: 通过对日本分地区、分性别的死亡率数据进行实证分析，结果显示该模型在不同预测周期内的点预测和区间预测准确性均有显著提升。
conclusion: 该研究证明了利用可解释组件进行建模不仅能清晰揭示数据背后的驱动因素，还能提供更精准且透明的预测结果，辅助循证决策。
---

## 摘要
我们研究了高维函数型时间序列（high-dimensional functional time series）的建模与预测，这些序列可能具有时间依赖性和横截面相关性。我们采用函数型方差分析（FANOVA）将高维函数型时间序列（例如多年观察到的地方性分年龄和性别的死亡率）分解为两个不同的组成部分：确定性均值结构和随时间变化的残差过程。与纯统计降维技术不同，FANOVA 分解通过将序列划分为可归因于特定数据因素（如地区和性别差异）的效应以及总函数均值，提供了一个直接且具有可解释性的框架。针对残差过程，我们应用函数型因子模型（functional factor model）来捕捉剩余的随机趋势。通过将残差分量的预测与估计的确定性结构相结合，我们获得了高维函数型时间序列的预测曲线。以 1975 年至 2023 年日本地方性分年龄死亡率为例，我们评估并比较了不同预测期限下点预测和区间预测的准确性。结果表明，利用这些可解释的组件不仅阐明了数据的潜在驱动因素，还提高了预测准确性，为循证政策决策提供了更透明的见解。

## Abstract
We study the modeling and forecasting of high-dimensional functional time series, which can be temporally dependent and cross-sectionally correlated. We implement a functional analysis of variance (FANOVA) to decompose high-dimensional functional time series, such as subnational age- and sex-specific mortality observed over years, into two distinct components: a deterministic mean structure and a residual process varying over time. Unlike purely statistical dimensionality-reduction techniques, the FANOVA decomposition provides a direct and interpretable framework by partitioning the series into effects attributable to data-specific factors, such as regional and sex-level variations, and a grand functional mean. From the residual process, we implement a functional factor model to capture the remaining stochastic trends. By combining the forecasts of the residual component with the estimated deterministic structure, we obtain the forecasted curves for high-dimensional functional time series. Illustrated by the age-specific Japanese subnational mortality rates from 1975 to 2023, we evaluate and compare the accuracy of the point and interval forecasts across various forecast horizons. The results demonstrate that leveraging these interpretable components not only clarifies the underlying drivers of the data but also improves forecast accuracy, providing more transparent insights for evidence-based policy decisions.

---

## 论文详细总结（自动生成）

这篇论文由 Han Lin Shang 和 Cristian F. Jiménez-Varón 撰写，探讨了高维函数型时间序列（HDFTS）的建模与预测问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证预测准确性的同时，提高对高维函数型时间序列（如不同地区、性别的死亡率曲线）预测模型的可解释性。
*   **研究动机**：
    *   **数据复杂性**：现代数据常以函数形式呈现（如随年龄变化的死亡率），且具有高维性（多个地区、多个群体）和复杂的时空相关性。
    *   **现有模型局限**：传统的降维技术（如因子模型）虽然高效，但往往是“黑箱”操作，提取的潜在因子缺乏明确的物理或社会学含义，难以辅助政府进行循证决策。
    *   **应用需求**：在人口学中，理解地区差异和性别差异对死亡率的影响对于资源分配和政策制定至关重要。

### 2. 方法论
论文提出了一种结合结构化分解与随机建模的三阶段框架：
*   **核心思想**：将序列分解为“确定性均值结构”和“随机残差过程”。
*   **关键技术步骤**：
    1.  **双向函数型方差分析 (Two-way FANOVA)**：将原始平滑后的数据分解为总函数均值（Grand Mean）、地区效应（Row Effect）、性别效应（Column Effect）以及交互项与残差。
    2.  **单向函数型方差分析 (One-way FANOVA)**：针对交互项进一步分解，提取特定性别的地区均值差异，增强对跨组异质性的捕捉。
    3.  **函数因子模型 (Functional Factor Model, FFM)**：对上述步骤后的随机残差进行建模。利用特征值分解提取随时间变化的因子和函数因子载荷，捕捉剩余的随机趋势。
    4.  **预测与重构**：对因子进行时间序列预测（如使用 ETS 或 ARIMA），然后结合 FANOVA 的确定性组件重构出未来的完整函数曲线。
    5.  **区间预测**：采用**符合性预测 (Conformal Prediction)** 方法（包括 Split 和 Sequential 两种模式），在不假设误差分布的情况下构建预测区间。

### 3. 实验设计
*   **数据集**：1975 年至 2023 年日本 47 个都道府县的分年龄（0-100+岁）、分性别的对数死亡率数据。
*   **场景**：1 至 10 步的前瞻性预测（h=1 到 10）。
*   **Benchmark 与对比方法**：
    *   **TWA + MFTS**：Jiménez-Varón 等人 (2024) 提出的双向 FANOVA 结合多元函数时间序列方法。
    *   **TWA + FFM**：不包含单向 FANOVA 交互项分解的简化版本。
    *   **TWA + OWA + FFM**：本文提出的完整模型。
*   **评估指标**：
    *   点预测：均方根预测误差 (RMSFE) 和平均绝对预测误差 (MAFE)。
    *   区间预测：经验覆盖概率 (ECP)、覆盖概率偏差 (CPD) 和区间评分 (Interval Score)。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或 CPU 核心数）。
*   **软件实现**：提到使用了 R 语言及其相关包（如 `demography` 用于数据平滑），代码已在 GitHub 开源。考虑到函数型数据分析的计算特性，该方法主要涉及矩阵运算和特征值分解，通常在标准工作站或服务器上即可运行。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了日本全部 47 个行政区，跨度 49 年，区分男女两性，总计处理了数千条函数曲线。
*   **充分性**：
    *   采用了**扩展窗口（Expanding-window）**方案进行回测，确保了评估的稳健性。
    *   对比了不同的基础预测模型（ETS 和 ARIMA）。
    *   进行了点预测和区间预测的双重评估。
*   **客观性**：实验设计合理，对比了该领域最新的基准模型，且使用了真实且具有挑战性的社会科学数据集。

### 6. 主要结论与发现
*   **准确性提升**：提出的 TWA + OWA + FFM 模型在点预测（RMSFE 和 MAFE）上优于现有的 TWA + MFTS 方法。
*   **交互项的重要性**：引入单向 FANOVA 来处理性别与地区的交互效应，能显著改善预测性能，尤其是在捕捉特定性别的地区趋势方面。
*   **区间预测优势**：**序列符合性预测 (Sequential Conformal Prediction)** 在处理有限样本不确定性时表现更稳健，能够随着新数据的到达自动更新预测分位数。
*   **可解释性**：模型清晰地展示了日本死亡率的总体趋势、地区差异（如冲绳的长寿效应）以及性别差异，这些组件具有直观的生物学和地理学意义。

### 7. 优点
*   **平衡了性能与解释性**：在不牺牲预测精度的前提下，解决了因子模型“黑箱化”的问题。
*   **结构化分解**：通过 FANOVA 框架，使研究者能够量化不同因素对整体变异的贡献。
*   **分布无关的区间预测**：符合性预测的应用使得模型对模型误设和非正态误差具有更强的鲁棒性。

### 8. 不足与局限
*   **计算复杂度**：虽然比全参数模型简单，但在处理极高维度的横截面数据时，FANOVA 的多级分解可能带来计算开销。
*   **平滑依赖**：模型效果高度依赖于预处理阶段的 P-spline 平滑质量，若原始数据噪声过大或存在系统性偏差，可能影响分解结果。
*   **应用局限**：目前主要在死亡率数据上验证，对于具有更强非平稳性或突发结构变化的函数序列（如高频金融数据），其表现尚待验证。

（完）
