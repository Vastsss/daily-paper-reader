---
title: Calibrating Scientific Foundation Models with Inference-Time Stochastic Attention
title_zh: 利用推理时随机注意力校准科学基础模型
authors: "Akash Yadav, Taiwo A. Adebiyi, Ruda Zhang"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19530v1"
tags: ["query:ts-fm"]
score: 10.0
evidence: 校准天气和时间序列的科学基础模型
tldr: 针对科学基础模型缺乏不确定性量化的问题，本文提出了一种轻量级的“随机注意力”机制。该方法在推理阶段通过多项分布采样替换Softmax权重，无需重新训练即可生成预测集成。通过简单的后验校准，该方法在保持高覆盖率的同时，显著提升了校准精度和预测区间的锐度，且调优效率极高，仅需数分钟即可完成。
selection_source: fresh_fetch
motivation: 现有的科学基础模型多为确定性输出，难以在气象预测等高风险场景中提供可靠的预测不确定性评估。
method: 提出在推理时将注意力权重替换为受浓度参数控制的归一化多项式样本，并通过单变量后验调优实现快速校准。
result: 在天气和时间序列预测任务中，该方法比现有基准具有更强的校准性和更窄的预测区间，且调优时间从数天缩短至数分钟。
conclusion: 随机注意力机制为科学基础模型提供了一种高效、无需重训且表现优异的不确定性量化方案。
---

## 摘要
基于 Transformer 的科学基础模型正越来越多地部署在关键决策场景中，但目前的架构通常给出确定性输出，对校准预测不确定性的支持有限。我们提出了随机注意力（Stochastic Attention），这是一种轻量级的推理时修改方法，通过将 softmax 权重替换为受单一浓度参数控制的归一化多项式样本来实现注意力随机化，从而在无需重新训练的情况下生成预测集成。为了设定该参数，我们引入了一个校准目标，使随机注意力输出与目标相匹配，将其转化为一个高效的单变量事后调优问题。我们在两个用于天气和时间序列预测的科学基础模型以及一个额外的回归任务上评估了该机制。在与不确定性感知基准模型的对比测试中，我们发现随机注意力在同等覆盖率下实现了最强的原生校准和最窄的预测区间，且仅需几分钟的事后调优，而相比之下，竞争基准模型则需要数天的重新训练。

## Abstract
Transformer-based scientific foundation models are increasingly deployed in high-stakes settings, but current architectures give deterministic outputs and provide limited support for calibrated predictive uncertainty. We propose Stochastic Attention, a lightweight inference-time modification that randomizes attention by replacing softmax weights with normalized multinomial samples controlled by a single concentration parameter, and produces predictive ensembles without retraining. To set this parameter, we introduce a calibration objective that matches the stochastic attention output with the target, yielding an efficient univariate post-hoc tuning problem. We evaluate this mechanism on two scientific foundation models for weather and timeseries forecasting along with an additional regression task. Across benchmarks against uncertainty-aware baselines, we find that Stochastic Attention achieves the strongest native calibration and the sharpest prediction intervals at comparable coverage, while requiring only minutes of post-hoc tuning versus days of retraining for competitive baselines.

---

## 论文详细总结（自动生成）

### 论文总结：利用推理时随机注意力校准科学基础模型

#### 1. 核心问题与整体含义
本文针对科学基础模型（Scientific Foundation Models, SFMs）在气象预测、时间序列分析等高风险科学领域应用中的**不确定性量化（Uncertainty Quantification, UQ）**缺失问题展开研究。虽然现有的 Transformer 模型在预测精度上已能媲美传统物理模型，但其输出通常是确定性的，缺乏可靠的概率校准。现有的 UQ 方法（如深度集成、SWAG 等）往往需要昂贵的重新训练成本或复杂的后验近似。论文旨在提出一种既能保持预训练模型精度，又具备高效校准能力且计算成本极低的轻量级机制。

#### 2. 方法论
论文提出了**随机注意力（Stochastic Attention, SA）**机制，其核心思想是将注意力机制视为一种概率采样过程：
*   **核心思想**：传统的注意力输出是基于 Softmax 权重的加权平均（即期望值）。SA 将其替换为从该多项分布中抽取的 $ \nu $ 个样本的归一化平均值。
*   **关键技术细节**：
    *   **浓度参数 $ \nu $**：引入单一参数 $ \nu $ 控制随机性。当 $ \nu \to \infty $ 时，模型退化为确定性注意力；较小的 $ \nu $ 则增加预测的离散度。
    *   **推理时修改**：该机制仅在推理阶段激活，无需修改模型架构或重新训练权重，直接作用于冻结的预训练模型。
    *   **校准目标**：通过最小化“随机偏差幅度”与“验证集残差幅度”之间的平方差异来自动寻找最优的 $ \nu $。
*   **算法流程**：使用贝叶斯优化（BO）在验证集上进行单变量搜索，找到使模型输出分布与实际观测误差最匹配的 $ \nu $ 值。

#### 3. 实验设计
实验涵盖了三个具有代表性的科学建模场景：
*   **数据集/场景**：
    1.  **气象预测**：使用 **ClimaX** 模型在 ERA5 全球天气数据集上进行 72 小时预报。
    2.  **时间序列预测**：使用 **TimesFM** 模型在 ETT（电力变压器温度）数据集上进行测试。
    3.  **通用回归**：使用 **FT-Transformer** 在 8 个 UCI 标准回归数据集上进行评估。
*   **对比方法（Baselines）**：
    *   **权重空间方法**：SWAG、MultiSWAG（集成版 SWAG）、IVON（变分推理）。
    *   **随机前向传递方法**：MC Dropout、Contextual Dropout。
    *   **架构随机化方法**：HSA（层次随机注意力）。

#### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA V100-SXM2-32GB** GPU。
*   **耗时对比**：
    *   **随机注意力（SA）**：事后校准（BO 调优）仅需 **1-3 分钟**。
    *   **基准方法**：SWAG 需要约 29 小时的轨迹收集；10 个种子的 MultiSWAG 在相同任务上累计需要约 **12 天** 的计算量。
    *   SA 的推理延迟增加极低（在最优参数下仅比确定性模型增加约 11%）。

#### 5. 实验数量与充分性
实验设计非常充分且具有多维性：
*   **覆盖面广**：从高维气象数据到一维时间序列，再到表格回归数据，验证了方法的通用性。
*   **多指标评估**：不仅关注精度（RMSE, ACC），还深入分析了校准度（PIT 直方图、W1 距离）、锐度（预测区间宽度）和成本。
*   **消融与对比**：针对 $ \nu $ 参数的敏感性、不同校准目标（如 CRPS 优化 vs. 残差匹配）进行了详细讨论。实验结果显示 SA 在原生校准性上显著优于所有基准。

#### 6. 主要结论与发现
*   **原生校准性最强**：SA 在无需后处理的情况下，其 PIT 直方图最接近均匀分布，而其他方法（如 SWAG, Dropout）普遍表现出严重的过度自信（U型分布）。
*   **预测区间最锐利**：在达到相同覆盖率的前提下，SA 提供的预测区间比其他方法更窄（更具信息量）。
*   **成本效益极高**：SA 实现了“分钟级”的调优，效果却能媲美甚至超过需要“天级”训练的集成方法。
*   **双重优化路径**：研究发现，通过校准目标选定的 $ \nu $ 侧重于统计一致性，而通过 CRPS 评分选定的 $ \nu $ 则能进一步提升预测精度。

#### 7. 优点
*   **非侵入性**：无需重新训练，适用于任何基于 Transformer 的冻结基础模型。
*   **理论优雅**：将注意力权重直接视为多项分布采样，比添加外源噪声更符合 Transformer 的内部计算逻辑。
*   **多目标平衡**：在精度、校准度、锐度和成本这四个维度上找到了极佳的平衡点。

#### 8. 不足与局限
*   **单一参数限制**：全模型共享一个 $ \nu $ 参数虽然简单，但在处理极其复杂或噪声分布不均的数据时，表达能力可能受限（未来可能需要层级或头级别的 $ \nu $）。
*   **依赖强骨干网络**：SA 的效果高度依赖于预训练模型本身的质量。如果骨干模型预测偏差过大，仅靠调整注意力随机性无法弥补根本性的准确度缺失。
*   **应用范围**：目前主要验证了连续值回归任务，在分类任务或离散生成任务（如大语言模型文本生成）中的适用性尚待进一步研究。

（完）
