---
title: Agentic Forecasting using Sequential Bayesian Updating of Linguistic Beliefs
title_zh: 基于语言信念序列贝叶斯更新的代理式预测
authors: Kevin Murphy
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18576v2"
tags: ["query:llm-ts"]
score: 9.0
evidence: 使用LLM和语言信念的代理预测系统
tldr: 本文提出BLF（贝叶斯语言预测器），一种用于二元预测的智能体系统。该系统通过迭代更新包含数值概率与自然语言证据的“语言信念状态”，克服了传统长上下文堆叠的弊端。此外，它采用分层多试验聚合与分层校准技术优化预测结果。在ForecastBench基准测试中，BLF表现优于GPT-5等顶尖模型，并建立了一个低泄漏率的稳健回测框架，为复杂预测任务提供了高效且统计严谨的解决方案。
selection_source: fresh_fetch
motivation: 旨在解决传统预测模型在处理海量检索证据时上下文冗余以及极端预测校准不足的问题。
method: 核心方法是利用LLM迭代更新半结构化的语言信念状态，并结合对数空间收缩聚合与分层Platt缩放校准。
result: BLF在ForecastBench的400个回测问题上超越了所有公开顶尖方法，且结构化信念状态对性能提升显著。
conclusion: 证明了将结构化信念表示与分层统计推断相结合是提升智能体预测性能的关键路径。
---

## 摘要
我们提出了 BLF（贝叶斯语言预测器），这是一个用于二元预测的代理式系统，在 ForecastBench 基准测试中达到了 SOTA 性能。该系统基于三个核心理念：(1) 语言信念状态：一种结合了数值概率估计与自然语言证据摘要的半结构化表示，由 LLM 在迭代工具使用循环的每一步进行更新。这与将所有检索到的证据附加到不断增长的上下文中的常见方法形成对比。(2) 分层多试验聚合：运行 K 个独立试验，并使用带有数据

## Abstract
We present BLF (Bayesian Linguistic Forecaster), an agentic system for binary forecasting that achieves state-of-the-art performance on the ForecastBench benchmark. The system is built on three ideas. (1) A linguistic belief state: a semi-structured representation combining numerical probability estimates with natural-language evidence summaries, updated by the LLM at each step of an iterative tool-use loop. This contrasts with the common approach of appending all retrieved evidence to an ever-growing context. (2) Hierarchical multi-trial aggregation: running $K$ independent trials and combining them using logit-space shrinkage with a data-dependent prior. (3) Hierarchical calibration: Platt scaling with a hierarchical prior, which avoids over-shrinking extreme predictions for sources with skewed base rates. On 400 backtesting questions from the ForecastBench leaderboard, BLF outperforms all the top public methods, including Cassi, GPT-5, Grok~4.20, and Foresight-32B. Ablation studies show that the structured belief state is almost as impactful as web search access, and that shrinkage aggregation and hierarchical calibration each provide significant additional gains. In addition, we develop a robust back-testing framework with a leakage rate below 1.5\%, and use rigorous statistical methodology to compare different methods while controlling for various sources of noise.

---

## 论文详细总结（自动生成）

这篇论文由 Kevin Murphy 撰写，介绍了一个名为 **BLF (Bayesian Linguistic Forecaster，贝叶斯语言预测器)** 的智能体系统，旨在提升大语言模型（LLM）在二元事件预测任务中的表现。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
预测未来事件的概率是地缘政治、金融和公共卫生等领域的核心挑战。虽然现有研究表明具备联网能力的 LLM 预测水平已接近人类，但仍面临三大痛点：
*   **上下文冗余：** 传统方法将所有搜索结果堆叠在上下文中，导致信息过载和推理效率低下。
*   **概率不一致：** LLM 在处理不确定性时存在高方差和过度自信的问题。
*   **回测泄漏：** 在历史数据上测试时，模型容易通过搜索或参数知识接触到“未来信息”。
**研究动机：** 开发一种能够像人类专家一样通过迭代更新“信念”并进行严谨统计校准的预测智能体。

### 2. 方法论：核心思想与关键技术
BLF 系统由三个核心支柱组成：
*   **语言信念状态 (Linguistic Belief State)：** 核心创新点。智能体维护一个半结构化的 JSON 对象，包含数值概率估计、置信度、正反证据摘要及待解决问题。在迭代循环的每一步，LLM 都会根据新证据更新此信念，而非仅仅堆叠文本。这被视为一种近似的序列贝叶斯推理。
*   **分层多试验聚合 (Multi-trial Aggregation)：** 运行 $K=5$ 个独立试验，在对数几率（logit）空间进行平均。引入了基于留一交叉验证（LOO）调优的收缩（Shrinkage）机制，以减少试验间分歧导致的过度自信。
*   **分层校准 (Hierarchical Calibration)：** 使用 Platt 缩放法将原始预测映射为校准概率。针对不同来源（如金融、政治）的基准率差异，采用带有分层先验的截距偏移，防止全局校准对极端预测的过度平滑。
*   **工具使用：** 包含网页搜索（Brave Search）、结果摘要、特定 URL 查找以及针对金融（yfinance）和经济（FRED）的时间序列处理工具。

### 3. 实验设计
*   **数据集：** 
    *   **ForecastBench (FB)：** 包含 400 个回测问题（791 个分辨率日期），涵盖市场类（Polymarket, Metaculus 等）和数据集类（yfinance, FRED, Wikipedia 等）。
    *   **AIBQ2：** 113 个来自 Metaculus AI 锦标赛的二元问题。
*   **Benchmark 与对比方法：** 对比了 ForecastBench 排行榜前五名：Cassi, GPT-5 (zero-shot), Grok 4.20, Foresight-32B。
*   **评估指标：** Brier Index (BI)、Adjusted Brier Index (ABI) 和 Metaculus Baseline Score (MBS)。

### 4. 资源与算力
*   **模型使用：** 主要使用 **Gemini-3.1-Pro** 作为核心推理引擎，**Gemini-3-Flash** 用于搜索结果过滤和摘要。
*   **算力与成本：** 论文未提及具体的 GPU 训练资源（因为是基于 API 的智能体系统）。
*   **成本估算：** 单次完整评估（400 题 × 5 试验）约消耗 50M-100M token，成本约为 **230 美元**。单题预测耗时 1-8 分钟。

### 5. 实验数量与充分性
*   **实验规模：** 进行了 11 种不同配置的消融实验，每种配置运行 5 次试验以控制随机性。
*   **统计严谨性：** 使用了 **ANOVA（方差分析）** 来分解问题难度、方法效应和试验噪声。通过 **配对自助法 (Paired Bootstrap)** 计算 95% 置信区间，确保在控制问题难度这一干扰变量（占方差 62%）的前提下进行公平比较。
*   **泄漏控制：** 实施了四层防御机制（搜索日期过滤、LLM 泄漏分类器、工具日期钳位、URL 屏蔽），事后审计显示泄漏率低于 1.5%，实验设计非常客观。

### 6. 主要结论与发现
*   **SOTA 性能：** BLF 在所有指标上均显著优于 GPT-5 和 Grok 4.20 等顶尖模型。其 ABI (71.0) 达到了人类超级预测者的中位数水平。
*   **信念状态的价值：** 消融实验显示，结构化信念状态对性能的提升（-3.0 BI）几乎与网页搜索（-4.6 BI）同样重要。
*   **市场预测突破：** BLF 是唯一一个在统计学上显著超越“群体预测（市场价格）”基准的 AI 方法。
*   **模型能力：** 较强的基础模型（Gemini-3.1-Pro）对预测至关重要，使用较弱模型（Flash 或 Kimi）会导致性能大幅下降。

### 7. 优点与亮点
*   **架构优雅：** 将贝叶斯思想与自然语言处理结合，解决了长上下文窗口的噪声问题。
*   **统计严谨：** 不仅仅是跑分，还提供了深度的方差分解和校准分析，为 AI 预测研究树立了方法论标杆。
*   **鲁棒的回测框架：** 针对 LLM 预测中极难处理的“数据泄漏”问题提供了切实可行的多层防御方案。

### 8. 不足与局限
*   **应用范围：** 目前仅专注于二元（Yes/No）预测，尚未扩展到多分类或连续数值预测。
*   **成本与延迟：** 迭代循环和多试验聚合虽然准确，但 token 消耗大且实时性较差。
*   **模型依赖：** 性能高度依赖于特定顶级模型（如 Gemini-3.1-Pro），在开源或较小模型上的迁移效果有待验证。
*   **时间序列局限：** 虽然引入了统计工具，但 LLM 在处理原始数值数据方面仍显吃力，部分任务（如 DBnomics）需完全绕过 LLM 使用传统模型。

（完）
