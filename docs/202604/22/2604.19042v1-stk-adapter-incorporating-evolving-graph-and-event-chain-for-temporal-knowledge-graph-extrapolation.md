---
title: "STK-Adapter: Incorporating Evolving Graph and Event Chain for Temporal Knowledge Graph Extrapolation"
title_zh: STK-Adapter：结合演化图与事件链的时序知识图谱外推
authors: "Shuyuan Zhao, Wei Chen, Weijie Zhang, Xinrui Hou, Junfeng Shen, Boyan Shi, Shengnan Guo, Youfang Lin, Huaiyu Wan"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19042v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 使LLM适应时序知识图谱外推
tldr: 针对时序知识图谱（TKG）外推任务中图结构与大语言模型（LLM）对齐浅层及特征稀释的问题，本文提出STK-Adapter。该方法通过空间-时间混合专家（MoE）、事件感知MoE及跨模态对齐MoE，深度融合演化图编码器与LLM，有效捕捉时空模式与语义依赖。实验证明其在多个基准数据集上显著优于现有方法，并具有极强的跨数据集泛化能力。
selection_source: fresh_fetch
motivation: 现有方法在将TKG演化结构与LLM结合时，存在时空信息丢失和微调过程中结构特征稀释的挑战。
method: 提出STK-Adapter，利用空间-时间MoE、事件感知MoE和跨模态对齐MoE来深度整合演化图特征与事件链语义。
result: 在多个基准数据集上的实验结果显示，STK-Adapter显著超越了当前最先进的方法，并展现出优异的泛化性能。
conclusion: STK-Adapter通过多维度MoE机制实现了图结构与文本语义的深度对齐，为时序知识图谱推理提供了更有效的解决方案。
---

## 摘要
时序知识图谱（TKG）外推旨在基于历史事实预测未来事件。最近的研究尝试通过将 TKG 的演化结构表示和文本事件链集成到大语言模型（LLM）中来增强 TKG 外推。然而，两个主要挑战限制了这些方法：(1) 由于 TKG 的图演化结构表示与 LLM 的语义空间之间的浅层对齐，导致关键时空信息的丢失；(2) 在 LLM 微调过程中，TKG 的演化结构特征会逐渐稀释。为了应对这些挑战，我们提出了时空知识适配器（STK-Adapter），它灵活地集成了演化图编码器和 LLM 以促进 TKG 推理。在 STK-Adapter 中，设计了一个时空混合专家模型（Spatial-Temporal MoE）来捕捉 TKG 固有的空间结构和时间模式。采用事件感知混合专家模型（Event-Aware MoE）来建模事件链中复杂的时序语义依赖关系。此外，提出了一种跨模态对齐混合专家模型（Cross-Modality Alignment MoE），通过 TKG 引导的注意力专家促进深层跨模态对齐。在基准数据集上的广泛实验表明，STK-Adapter 显著优于现有最先进的方法，并在跨数据集任务中表现出强大的泛化能力。代码可在 https://github.com/Zhaoshuyuan0246/STK-Adapter 获取。

## Abstract
Temporal Knowledge Graph (TKG) extrapolation aims to predict future events based on historical facts. Recent studies have attempted to enhance TKG extrapolation by integrating TKG's evolving structural representations and textual event chains into Large Language Models (LLMs). Yet, two main challenges limit these approaches: (1) The loss of essential spatial-temporal information due to shallow alignment between TKG's graph evolving structural representation and the LLM's semantic space, and (2) the progressive dilution of the TKG's evolving structural features during LLM fine-tuning. To address these challenges, we propose the Spatial-Temporal Knowledge Adapter (STK-Adapter), which flexibly integrates the evolving graph encoder and the LLM to facilitate TKG reasoning. In STK-Adapter, a Spatial-Temporal MoE is designed to capture spatial structures and temporal patterns inherent in TKGs. An Event-Aware MoE is employed to model intricate temporal semantics dependencies within event chains. In addition, a Cross-Modality Alignment MoE is proposed to facilitate deep cross-modality alignment by TKG-guided attention experts. Extensive experiments on benchmark datasets demonstrate that STK-Adapter significantly outperforms state-of-the-art methods and exhibits strong generalization capabilities in cross-dataset task. The code is available at https://github.com/Zhaoshuyuan0246/STK-Adapter.

---

## 论文详细总结（自动生成）

这是一份关于论文《STK-Adapter: Incorporating Evolving Graph and Event Chain for Temporal Knowledge Graph Extrapolation》的结构化总结：

### 1. 核心问题与研究动机
时序知识图谱（TKG）外推旨在根据历史事实预测未来事件。目前结合大语言模型（LLM）的方法面临两大挑战：
*   **浅层对齐导致信息丢失**：现有方法多采用简单的投影层（如 MLP）将图表示映射到 LLM 语义空间，这种“一刀切”的浅层对齐难以保留复杂的时空结构信息。
*   **特征稀释问题**：LLM 本质上是为文本预测优化的，在深层微调过程中，注入的图结构特征会随着层数增加被文本语义逐渐稀释，导致模型失去对时空动态的建模能力。

### 2. 方法论
论文提出了 **STK-Adapter**，通过在 LLM 的每一层中嵌入三个专门的混合专家（MoE）模块来实现深度融合：
*   **空间-时间混合专家（ST-MoE）**：利用预训练的演化图编码器（如 LogCL）提取实体和关系表示，通过稀疏激活的专家捕捉 TKG 中多样的空间结构和时间模式，缓解深层传播中的特征稀释。
*   **事件感知混合专家（EA-MoE）**：针对线性化的事件链，设计了“事件感知路由”，为同一事件的所有 Token 分配共享路由信号，引导专家学习复杂的时序语义依赖和重复模式。
*   **跨模态对齐混合专家（CMA-MoE）**：采用 TKG 引导的注意力专家，将文本表示作为 Query，图表示作为 Key 和 Value，在每一层进行深度的跨模态特征注入与对齐。
*   **自适应融合与混合评分**：通过门控网络融合各模块输出，并在推理阶段结合 LLM 的解码得分与图模型的拓扑得分进行最终排名。

### 3. 实验设计
*   **数据集**：使用了四个主流 TKG 基准数据集：**ICE14、ICE18、ICE15**（政治事件）和 **WIKI**（维基百科历史事实）。
*   **评估指标**：Hit@1, Hit@3, Hit@10。
*   **对比方法**：共 9 种基线模型，涵盖了：
    *   **嵌入式方法**：REGCN, TiRGN, LogCL, CognTKE。
    *   **纯 LLM 方法**：GPT-NeoX, GenTKG, CoH。
    *   **混合方法**：LLM-DA, MESH。

### 4. 资源与算力
*   **硬件环境**：使用了服务器集群，配备 **8 张 NVIDIA A40 GPU**（每张 48GB 显存）。
*   **模型配置**：以 **Llama3-8B** 为主要骨干网络（也测试了 Qwen2.5-7B 和 Mistral-7B），图编码器维度为 200。
*   **训练细节**：仅微调适配器部分，训练 **2 个 Epoch**，使用 AdamW 优化器，推理时 Beam Search 宽度设为 20。

### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **性能对比**：在 4 个数据集上全面超越所有基线。
*   **兼容性测试**：验证了模型在不同图编码器（4 种）和不同 LLM 骨干（3 种）下的稳定性。
*   **消融实验**：逐一验证了三个 MoE 模块及混合评分机制的有效性。
*   **泛化性实验**：进行了跨数据集的零样本（Zero-shot）迁移测试。
*   **效率分析**：对比了参数量、推理时间和显存占用。
*   **可视化分析**：展示了 MoE 专家的路由分布和案例研究。

### 6. 主要结论与发现
*   **深度对齐至关重要**：逐层注入图特征比单次投影输入效果更好，能有效防止时空信息的丢失。
*   **MoE 的优越性**：相比单一适配器（LoRA），MoE 结构能更好地处理图与文本这两种异构模态的复杂交互。
*   **强大的泛化能力**：STK-Adapter 在未见过的地理/时间分布数据集上表现出比同类混合方法（如 LLM-DA）更强的鲁棒性。
*   **效率平衡**：尽管增加了 MoE 模块，但由于采用了稀疏激活和瓶颈结构，推理速度仍保持在竞争水平。

### 7. 优点
*   **架构创新**：将 MoE 引入 TKG 领域，并针对事件和时空特征设计了专门的路由机制。
*   **即插即用**：不依赖特定的图编码器或 LLM，具有极强的通用性。
*   **解决稀释问题**：通过层级注入机制，确保了长文本推理中结构化知识的持久性。

### 8. 不足与局限
*   **计算开销**：虽然 MoE 激活是稀疏的，但层级注入和 Beam Search 仍然比传统的嵌入式模型（如 REGCN）慢得多，且显存占用较高。
*   **依赖预训练编码器**：模型性能在一定程度上受限于底层图编码器的质量（尽管实验证明其具有鲁棒性）。
*   **推理延迟**：在实时性要求极高的场景下，LLM 的自回归生成过程可能成为瓶颈。

（完）
