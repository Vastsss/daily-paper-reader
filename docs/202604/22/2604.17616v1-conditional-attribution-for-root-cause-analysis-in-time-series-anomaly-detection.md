---
title: Conditional Attribution for Root Cause Analysis in Time-Series Anomaly Detection
title_zh: 时间序列异常检测中根因分析的条件归因
authors: "Shashank Mishra, Karan Patil, Cedric Schockaert, Didier Stricker, Jason Rambach"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17616v1"
tags: ["query:ts-fm"]
score: 8.0
evidence: 时间序列异常检测中的根因分析
tldr: 针对时间序列异常检测中的根因分析（RCA），现有方法因忽略特征依赖和使用不切实际的基准而导致归因不可靠。本文提出一种条件归因框架，通过在VAE和UMAP构建的低维流形空间中检索与异常上下文相似的正常实例作为基准，从而保留特征间依赖并避免分布偏移。实验证明该方法在SWaT和MSDS数据集上显著提升了根因识别的准确性、时间定位能力和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的解释方法往往依赖不切实际的特征扰动且忽略了时间与特征间的依赖关系，导致根因分析结果不可靠。
method: 提出一种条件归因框架，在VAE和UMAP构建的低维流形空间中检索上下文相关的正常状态作为基准，以实现保序且具有操作意义的解释。
result: 在SWaT和MSDS基准测试中，该方法在根因识别准确率、时间定位和对多种检测模型的鲁棒性方面均表现出显著提升。
conclusion: 条件归因通过结合系统学习到的流形信息，为复杂时间序列系统的异常诊断提供了一种更可靠、高效且具有实际应用价值的解释方案。
---

## 摘要
时间序列异常检测中的根因分析（RCA）对于复杂现实系统的可靠运行至关重要。现有的解释方法通常依赖于不切实际的特征扰动，并忽略了时间及跨特征的依赖关系，导致归因结果不可靠。我们提出了一种条件归因框架，通过相对于上下文相似的正常系统状态来解释异常。我们的方法不使用边缘分布或随机采样的基准，而是根据异常观测值检索具有代表性的正常实例，从而实现保留依赖关系且具有操作意义的解释。为了支持高维时间序列数据，上下文检索是在学习到的低维表示中进行的，同时利用了变分自编码器（VAE）潜空间和 UMAP 流形嵌入。通过将检索过程建立在系统学习到的流形之上，该策略避免了分布外伪影，并在保持计算效率的同时确保了归因的保真度。我们进一步引入了置信度感知和时间评估指标，用于评估解释的可靠性和响应性。在 SWaT 和 MSDS 基准测试上的实验表明，所提方法在多个异常检测模型中一致地提高了根因识别准确率、时间定位能力和鲁棒性。这些结果突显了条件归因在复杂时间序列系统可解释异常诊断中的实际应用价值。代码和模型将公开发布。

## Abstract
Root cause analysis (RCA) for time-series anomaly detection is critical for the reliable operation of complex real-world systems. Existing explanation methods often rely on unrealistic feature perturbations and ignore temporal and cross-feature dependencies, leading to unreliable attributions. We propose a conditional attribution framework that explains anomalies relative to contextually similar normal system states. Instead of using marginal or randomly sampled baselines, our method retrieves representative normal instances conditioned on the anomalous observation, enabling dependency-preserving and operationally meaningful explanations. To support high-dimensional time-series data, contextual retrieval is performed in learned low-dimensional representations using both variational autoencoder latent spaces and UMAP manifold embeddings. By grounding the retrieval process in the system's learned manifold, this strategy avoids out-of-distribution artifacts and ensures attribution fidelity while maintaining computational efficiency. We further introduce confidence-aware and temporal evaluation metrics for assessing explanation reliability and responsiveness. Experiments on the SWaT and MSDS benchmarks demonstrate that the proposed approach consistently improves root-cause identification accuracy, temporal localization, and robustness across multiple anomaly detection models. These results highlight the practical utility of conditional attribution for explainable anomaly diagnosis in complex time-series systems. Code and models will be publicly released.

---

## 论文详细总结（自动生成）

这是一份关于论文《Conditional Attribution for Root Cause Analysis in Time-Series Anomaly Detection》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在多变量时间序列异常检测中，如何准确识别导致异常的“根因”（Root Cause）。
*   **研究背景**：虽然深度学习提升了异常检测的准确率，但工业界更需要知道“为什么”发生异常。
*   **动机**：现有的根因分析（RCA）方法（如 SHAP 或集成梯度）通常假设特征相互独立，或者使用随机采样的基准（Baselines）。在复杂的时间序列系统中，传感器之间存在强时间相关性和跨特征依赖，独立的特征扰动会产生“分布外”（OOD）的无效样本，导致归因结果不可靠（例如将症状误判为原因）。

### 2. 方法论：条件归因框架
*   **核心思想**：通过相对于“在上下文中相似的正常系统状态”来解释异常，而非使用全局平均值或边缘分布。
*   **关键技术细节**：
    *   **条件归因（Conditional Attribution）**：定义归因为异常窗口与条件分布 $p(W_j | W_{-j})$ 下正常窗口之间的差异。这确保了替换特征时遵循物理系统的内在约束。
    *   **流形引导的上下文检索**：为了在高维空间中高效找到相似的正常状态，论文将原始数据映射到低维流形空间。
        *   **VAE 潜空间**：利用变分自编码器捕捉非线性依赖。
        *   **UMAP 嵌入**：利用流形学习保持局部邻域拓扑结构。
    *   **算法流程**：
        1.  **构建邻域**：在 VAE 或 UMAP 的低维空间中，检索与当前异常窗口（排除目标传感器后）最接近的 $K$ 个正常窗口。
        2.  **反事实替换**：用检索到的正常值替换异常窗口中的目标传感器数据。
        3.  **计算贡献**：观察异常检测模型得分的变化，聚合得到传感器级和时间级的归因热图。
    *   **新指标**：引入 **CW-RCS**（置信度加权根因得分）来衡量归因的集中度，以及 **TemporalHM** 来衡量定位的及时性和稳定性。

### 3. 实验设计
*   **数据集**：
    *   **SWaT**：安全水处理工业基准数据集（51个传感器）。
    *   **MSDS**：多源分布式系统数据集（高维 IT 监控数据）。
    *   **Paul Wurth 案例**：真实的工业高炉监控数据（超过100个异构传感器）。
*   **对比方法（Baselines）**：
    *   **因果发现类**：$\epsilon$-Diagnosis, RCD, CIRCA, AERCA。
    *   **特征归因类**：KernelSHAP, ShaTS。
*   **检测模型（Backbones）**：为了验证模型无关性，测试了 AE、VAE、LSTM、TCN 和 Transformer 等多种架构。

### 4. 资源与算力
*   **硬件**：论文明确提到实验是在 **NVIDIA RTX A6000 GPU** 上进行的。
*   **软件**：基于 PyTorch 实现。
*   **训练/推理时长**：论文在附录中对比了推理延迟，例如在 SWaT 数据集上，解释单个窗口的时间约为 22 秒（与 ShaTS 相当），虽然比 KernelSHAP 慢，但准确率大幅提升。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个公开基准和一例真实工业场景上进行了测试。
    *   针对 5 种不同的异常检测模型验证了归因的一致性。
    *   **消融实验**：研究了窗口大小、采样数量、输入空间 vs. 表示空间检索、有条件 vs. 无条件检索对结果的影响。
*   **充分性评价**：实验设计非常充分且客观。通过交叉模型验证证明了方法的通用性，并通过合成异常注入（Spike, Shift, Drift 等）进行了压力测试，确保了在已知根因情况下的准确性。

### 6. 主要结论与发现
*   **准确性提升**：在 SWaT 数据集上，Top-3 Recall 相比最强基准提升了约 **36.6%**。
*   **置信度更高**：CW-RCS 指标显示，该方法能更有效地将归因质量集中在真正的根因上，减少了背景噪声。
*   **时间定位更准**：TemporalHM 指标证明该方法能更早地识别异常起始点，并在异常持续期间保持稳定的解释。
*   **模型无关性**：无论底层检测器是简单的 AE 还是复杂的 Transformer，该框架均能提供高质量解释。

### 7. 优点
*   **物理一致性**：通过条件检索避免了 OOD 扰动，使解释更符合物理逻辑。
*   **鲁棒性**：在低维流形空间检索，有效缓解了高维噪声和“维度灾难”问题。
*   **实用性**：引入了更符合工业运维需求的评估指标（关注响应速度和解释稳定性）。
*   **真实落地**：通过高炉案例证明了在极端复杂工业环境下的有效性。

### 8. 不足与局限
*   **计算开销**：尽管在低维空间检索，但对于超大规模传感器网络（如数万个传感器），每一步的 KNN 检索和模型多次前向推理仍可能存在实时性挑战。
*   **依赖正常数据质量**：该方法依赖于学习一个高质量的“正常流形”，如果训练数据中正常模式覆盖不全，检索到的邻域可能不准确。
*   **静态邻域**：目前主要针对离线或离散窗口分析，对于系统动力学随时间剧烈演化的在线增量学习场景，尚需进一步扩展。

（完）
