---
title: Learning a Non-linear Surrogate Model for Multistage Stochastic Transmission Planning
title_zh: 学习用于多阶段随机输电规划的非线性代理模型
authors: "Victor Schmitt, Farzaneh Pourahmadi, Angela Flores-Quiroz, Pablo Apablaza, Pierluigi Mancarella"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17026v1"
tags: ["query:llm"]
score: 8.0
evidence: 促进电力系统中可再生能源的整合
tldr: 针对多阶段随机输电规划（TEP）中因不确定性和规模扩大导致的计算难题，本文提出一种混合机器学习与优化的框架。该方法利用神经网络构建非线性代理模型来近似预期运行成本，并将其转化为混合整数线性约束嵌入优化模型中。实验表明，该方法在保持近乎最优投资决策的同时，将计算速度提升了约13倍，显著增强了大规模场景下的计算可行性。
selection_source: fresh_fetch
motivation: 传统的随机输电规划模型在处理大规模不确定性场景时面临严峻的计算效率挑战。
method: 提出一种混合框架，通过训练神经网络代理模型近似运行成本，并将其重构为线性约束集成到优化模型中。
result: 在IEEE测试系统上的应用显示，该方法在保证决策接近最优的同时，计算时间缩短了高达13倍。
conclusion: 该代理模型框架有效平衡了规划精度与计算效率，为大规模电力系统的不确定性分析提供了可行方案。
---

## 摘要
输电网扩展规划 (TEP) 在确保电力系统可靠性和促进可再生能源整合方面发挥着至关重要的作用。然而，这一过程要求规划者不断应对显著的不确定性。虽然多阶段随机 TEP 模型为识别不确定性下的投资计划提供了一个稳健的框架，但问题规模的快速增长阻碍了其计算的可处理性。为了应对这一挑战，本文开发了一种用于随机 TEP 的混合机器学习-优化框架。所提出的方法利用投资决策和不确定性场景作为输入特征来训练代理神经网络，随后将其重新表述为混合整数线性约束并嵌入到优化模型中。代理模型近似于预期运行成本，以指导 TEP 决策，从而减轻了大型运行问题带来的负担。在 IEEE 测试系统上的案例研究应用表明，经过训练后，与单一的全优化随机公式相比，所提出的方法在实现近乎最优的投资成本的同时，将总计算时间缩短了约 13 倍。这使得执行广泛的多场景分析和压力测试成为可能，而这些在规模较大时通常在计算上是难以实现的。

## Abstract
Transmission expansion planning (TEP) plays a critical role in ensuring power system reliability and facilitating the integration of renewable energy resources. However, this process requires planners to constantly deal with significant uncertainty. While multistage stochastic TEP models provide a robust framework for identifying investment plans under uncertainty, the rapid growth in problem size hinders their computational tractability. To address this challenge, this paper develops a hybrid machine learning-optimisation framework for stochastic TEP. The proposed approach uses investment decisions and uncertainty scenarios as input features to train surrogate neural networks, which are then reformulated as mixed-integer linear constraints and embedded within an optimisation model. The surrogate model approximates expected operational costs to inform TEP decisions, reducing the burden arising from large operational problems. Case study applications on IEEE test systems demonstrate that, after training, the proposed approach achieves near-optimal investment costs while reducing total computational time by up to a factor of around 13 compared to a single full-optimisation stochastic formulation. This enables performing extensive multi-scenario analysis and stress testing that would otherwise be computationally prohibitive at scale.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning a Non-linear Surrogate Model for Multistage Stochastic Transmission Planning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
输电网扩展规划（TEP）是电力系统整合可再生能源的关键。为了应对负荷增长、燃料价格和可再生能源波动等不确定性，**多阶段随机输电规划（STEP）**被广泛采用。然而，随着场景树的加深和运行细节要求的提高，STEP 模型的计算规模呈指数级增长，导致传统优化方法（如直接求解或 Benders 分解）在处理大规模系统时面临严重的“维度灾难”，计算效率极低。本文旨在通过机器学习构建代理模型来替代复杂的运行模拟，从而在保证决策质量的同时大幅提升计算速度。

### 2. 方法论
论文提出了一种**混合机器学习-优化框架**，核心步骤如下：
*   **代理模型构建**：使用前馈神经网络（NN）近似每个场景节点下的年度运行决策（总发电成本和总弃负荷量）。
*   **输入与输出**：输入特征包括投资决策（候选线路是否建设）和场景参数（风、光、负荷剖面）；输出为预测的运行成本和弃负荷。
*   **关键技术细节（ReLU 重构）**：采用带有 ReLU 激活函数的神经网络。由于 ReLU 是分段线性的，利用 **Big-M 法**将其重构为一组混合整数线性约束。
*   **嵌入优化**：将重构后的线性约束直接嵌入到多阶段 TEP 的 MILP（混合整数线性规划）模型中。这样，优化器在做投资决策时，无需调用复杂的每小时运行约束，而是通过神经网络代理模型直接“读取”运行成本。
*   **数据增强**：通过对基准场景添加随机缩放因子（$\kappa$）进行确定性采样，生成覆盖多种运行工况的训练集。

### 3. 实验设计
*   **测试系统**：IEEE 33 节点系统和 IEEE RTS 24 节点系统。
*   **场景设置**：考虑 15 年的规划周期，基于澳大利亚 2024 年综合系统计划（ISP）构建场景树。
*   **Benchmark（基准方法）**：
    1.  **全优化随机公式（Exact Formulation）**：直接求解包含所有运行约束的原始模型。
    2.  **列生成算法（Column Generation, CG）**：一种经典的分解优化技术。
*   **对比指标**：投资决策的一致性、预测准确度（$R^2$、MAE）、求解时间、总运行时间。

### 4. 资源与算力
*   **硬件设施**：使用墨尔本大学的工作站，配备 **AMD EPYC 9474F 处理器**（3.6 GHz，48 核心/96 线程），128 GB RAM。
*   **软件环境**：Python 环境，使用 **Gurobi v12.0.1** 作为优化求解器，**TensorFlow** 用于神经网络训练。
*   **耗时情况**：
    *   数据生成：约 9 小时。
    *   模型训练：每个模型约 1 小时 40 分钟。
    *   超参数优化（HPO）：每个模型约 10 小时。
    *   推理/求解：一旦训练完成，求解速度极快（秒级）。

### 5. 实验数量与充分性
*   **实验规模**：生成了 500 组训练样本。在测试阶段，由于基准方法（全优化）计算极其耗时，仅选取了 5 组实例进行深度对比。
*   **充分性评价**：
    *   **优点**：论文进行了超参数优化（HPO）对比实验，并利用 **SHAP 值**进行了模型可解释性分析，验证了特征影响是否符合物理直觉。此外，还进行了针对噪声范围（$\kappa$）和可靠性标准（$\gamma$）的灵敏度分析。
    *   **局限**：测试实例数量较少（5个），虽然趋势明显，但在统计严谨性上略显薄弱。

### 6. 主要结论与发现
*   **计算加速**：与全优化方法相比，该框架将总计算时间缩短了约 **13.4 倍**，优化求解器时间缩短了约 **24.7 倍**。
*   **决策质量**：代理模型在投资决策上与精确模型高度一致，预测发电成本和弃负荷的 $R^2$ 达到了 1.000（近乎完美拟合）。
*   **模型规模**：通过代理模型替代，变量数量从约 830 万个减少到约 3.5 万个，约束数量也实现了同等规模的削减。
*   **鲁棒性**：当输入噪声在训练分布范围内时，模型表现极佳；但当噪声超出训练范围时，决策准确性会有所下降。

### 7. 亮点
*   **结构化嵌入**：不同于仅作为黑盒预测的 ML，本文将 NN 转化为 MILP 约束，实现了“学习”与“优化”的闭环集成。
*   **可解释性**：引入 SHAP 分析，证明了模型学习到了“增加可再生能源降低成本”和“增加线路容量减少弃负荷”等物理规律，增强了决策的可信度。
*   **实用性**：该方法允许规划者在标准个人电脑上处理原本需要高性能计算集群才能解决的大规模随机规划问题。

### 8. 不足与局限
*   **泛化风险**：模型高度依赖训练数据的分布，对于极端或未见过的场景（Out-of-distribution）可能失效。
*   **数据生成成本**：虽然推理很快，但前期生成 500 组高质量训练数据仍需耗费数小时的计算资源。
*   **激活函数限制**：为了适配 MILP 重构，目前仅限于 ReLU 激活函数，限制了神经网络架构的多样性。
*   **规模扩展性**：在更大规模的实际电网（如数千节点）中，候选线路组合的爆炸性增长可能会增加数据采样的难度。

（完）
