---
title: "DySCo: Dynamic Semantic Compression for Effective Long-term Time Series Forecasting"
title_zh: DySCo：用于有效长程时间序列预测的动态语义压缩
authors: "Xiang Ao, Yinyu Tan, Mengru Chen"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01261v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 能源与气象领域的长期时间序列预测
tldr: 针对长程时间序列预测中历史窗口过长导致的噪声和计算冗余问题，本文提出DySCo框架。该框架通过熵引导动态采样保留关键信息，结合层级频率增强分解保护细节，并利用跨尺度交互混合器融合全局与局部特征。作为即插即用模块，DySCo在降低计算开销的同时显著提升了主流模型捕捉长程相关性的能力。
selection_source: fresh_fetch
motivation: 传统的长窗口预测方法容易引入无关噪声和计算冗余，限制了模型对复杂长程依赖的捕捉。
method: 提出包含熵引导动态采样、层级频率增强分解和跨尺度交互混合器的动态语义压缩框架。
result: 实验证明DySCo能作为通用模块显著增强主流模型的预测性能，并有效降低计算成本。
conclusion: DySCo通过动态压缩冗余信息并保留关键语义特征，为高效长程时间序列预测提供了新方案。
---

## 摘要
时间序列预测 (TSF) 在金融、气象和能源等领域至关重要。虽然从理论上讲，延长回顾窗口可以提供更丰富的历史上下文，但在实践中，这往往会引入无关噪声和计算冗余，阻碍模型有效捕捉复杂的长程依赖关系。为了应对这些挑战，我们提出了一个动态语义压缩 (DySCo) 框架。与依赖固定启发式方法的传统方法不同，DySCo 引入了一种熵引导动态采样 (EGDS) 机制，能够自主识别并保留高熵片段，同时压缩冗余趋势。此外，我们结合了层级频率增强分解 (HFED) 策略，将高频异常与低频模式分离，确保在稀疏采样过程中保留关键细节。最后，设计了一个跨尺度交互混合器 (CSIM)，用于动态融合全局上下文与局部表示，取代了简单的线性聚合。实验结果表明，DySCo 作为一个通用的即插即用模块，在降低计算成本的同时，显著增强了主流模型捕捉长程相关性的能力。

## Abstract
Time series forecasting (TSF) is critical across domains such as finance, meteorology, and energy. While extending the lookback window theoretically provides richer historical context, in practice, it often introduces irrelevant noise and computational redundancy, preventing models from effectively capturing complex long-term dependencies. To address these challenges, we propose a Dynamic Semantic Compression (DySCo) framework. Unlike traditional methods that rely on fixed heuristics, DySCo introduces an Entropy-Guided Dynamic Sampling (EGDS) mechanism to autonomously identify and retain high-entropy segments while compressing redundant trends. Furthermore, we incorporate a Hierarchical Frequency-Enhanced Decomposition (HFED) strategy to separate high-frequency anomalies from low-frequency patterns, ensuring that critical details are preserved during sparse sampling. Finally, a Cross-Scale Interaction Mixer(CSIM) is designed to dynamically fuse global contexts with local representations, replacing simple linear aggregation. Experimental results demonstrate that DySCo serves as a universal plug-and-play module, significantly enhancing the ability of mainstream models to capture long-term correlations with reduced computational cost.

---

## 论文详细总结（自动生成）

### 论文总结：DySCo - 用于有效长程时间序列预测的动态语义压缩

#### 1. 核心问题与研究动机
*   **核心问题**：在长程时间序列预测（TSF）中，延长历史回顾窗口（Lookback Window）理论上能提供更丰富的上下文，但实际操作中往往会导致**信息冗余、噪声累积**以及**计算开销激增**，甚至引发预测精度下降（即“回顾窗口悖论”）。
*   **研究动机**：现有的采样或压缩技术多依赖于固定的启发式规则（如均匀采样），缺乏根据数据语义动态保留高价值信息的能力。论文旨在开发一种能自动识别并保留关键历史信号，同时剔除冗余噪声的动态压缩框架。

#### 2. 方法论
DySCo 是一个通用的“即插即用”框架，包含三个核心组件：
*   **层级频率增强分解 (HFED)**：
    *   构建多粒度金字塔表示。通过不同带宽的低pass滤波器（移动平均滤波）将序列分解。
    *   长窗口应用小带宽以提取全局趋势并滤除高频噪声；短窗口保留大带宽以捕捉局部剧烈波动。
*   **熵引导动态采样 (EGDS)**：
    *   **核心思想**：将序列划分为段，利用可学习的“重要性评分器”（MLP + Sigmoid）评估每段的语义价值（类比熵）。
    *   **动态核计算**：采样核的大小由“时间距离衰减”和“语义调制”共同决定。如果远古片段包含关键异常（高熵），语义调制会抵消距离惩罚，强制进行密集采样以保留细节。
*   **跨尺度交互混合器 (CSIM)**：
    *   取代简单的线性加权，采用基于上下文的门控机制（Gating Network）。
    *   动态计算各尺度预测结果的权重，实现全局趋势稳定性与局部细节敏感性的自适应融合。

#### 3. 实验设计
*   **数据集**：涵盖 7 个主流公开数据集，涉及能源（ETTm1/m2, ETTh1/h2）、电力（Electricity）、交通（Traffic）和气象（Weather）三大领域。
*   **基准模型 (Benchmarks)**：对比了四种代表性架构：
    *   Transformer 类：**PatchTST**, **iTransformer**。
    *   线性/混合类：**TimeMixer**, **DLinear**。
*   **对比协议**：为了公平性，DySCo 固定在长窗口（L=2440），而基准模型则在多个窗口长度（96 到 2440）中搜索其最优性能进行对比。

#### 4. 资源与算力
*   **硬件环境**：所有实验均在单张 **NVIDIA 2080Ti GPU** 上完成。
*   **效率表现**：
    *   **参数量**：在长窗口（L=2440）下，相比基础线性模型，DySCo 可减少约 **58.7%** 的参数。
    *   **计算开销**：对于 Transformer 架构，DySCo 将自注意力机制的复杂度降低了约 **94.3%**，显著缓解了内存瓶颈。
    *   **训练速度**：实验记录显示其训练时间和显存占用均大幅低于处理原始长序列的基础模型。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的对比实验（4 种基座模型 × 7 个数据集）、消融实验（验证 HFED, EGDS, CSIM 的各自贡献）、采样策略对比（与均匀、随机、Top-k 频率采样对比）以及超参数敏感性分析。
*   **充分性评价**：实验设计较为全面，通过在不同架构上集成 DySCo 证明了其通用性。引入“最优窗口基准”对比协议，确保了实验结果的客观性与说服力。

#### 6. 主要结论与发现
*   **性能提升**：DySCo 显著增强了现有模型捕捉长程相关性的能力，在所有测试数据集上均降低了 MSE 和 MAE。
*   **打破悖论**：证明了通过“语义压缩”而非“盲目增加输入长度”，可以有效利用长达 5000+ 的回顾窗口而不引起性能衰退。
*   **高效性**：DySCo 证明了多分支架构在配合激进下采样时，其总计算成本反而低于单分支的长序列处理。

#### 7. 优点与亮点
*   **即插即用**：作为模块化设计，可以轻松集成到现有的各种 TSF 模型中。
*   **语义感知采样**：突破了传统基于距离或固定频率的采样限制，能够根据内容重要性动态分配计算资源。
*   **理论支撑**：结合了频谱分析（高低频分解）与信息论（熵引导），具有较强的解释性。

#### 8. 不足与局限
*   **模型复杂度**：虽然减少了参数和显存，但多分支结构增加了实现和调参的复杂度（如需设置尺度数量 N、段数 n 等）。
*   **熵的近似**：所谓的“熵引导”实际上是基于 MLP 的重要性学习，其是否能完全等同于数学意义上的信息熵仍有待商榷。
*   **应用范围**：主要针对具有明显周期性或趋势性的工业/自然科学数据，在极度随机或缺乏长期规律的数据集上的表现尚未充分讨论。

（完）
