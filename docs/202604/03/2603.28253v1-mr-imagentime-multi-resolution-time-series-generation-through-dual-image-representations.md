---
title: "MR-ImagenTime: Multi-Resolution Time Series Generation through Dual Image Representations"
title_zh: MR-ImagenTime：通过双重图像表示实现的多分辨率时间序列生成
authors: "Xianyong Xu, Yuanjun Zuo, Zhihong Huang, Yihan Qin, Haoxian Xu, Leilei Du, Haotian Wang"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28253v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 多分辨率时间序列生成与预测
tldr: "针对现有时间序列预测模型在处理变长输入和多尺度建模方面的局限性，本文提出MR-CDM框架。该框架结合了分层多分辨率趋势分解、变长输入自适应嵌入机制以及多尺度条件扩散过程。通过在四个真实数据集上的评估，MR-CDM在MAE和RMSE指标上显著优于CSDI和Informer等主流模型，提升幅度约6-10%，为复杂时间序列生成提供了高效且灵活的新方案。"
selection_source: fresh_fetch
motivation: 现有时间序列模型在处理变长输入时存在困难，且缺乏对多尺度特征的有效建模。
method: 提出MR-CDM框架，利用分层多分辨率趋势分解、自适应嵌入和多尺度条件扩散过程进行建模。
result: "在四个真实数据集上，该方法相比CSDI和Informer等SOTA模型，将MAE和RMSE降低了约6-10%。"
conclusion: MR-CDM通过创新的多分辨率表示和扩散机制，显著增强了模型处理变长及多尺度时间序列的能力。
---

## 摘要
时间序列预测在许多领域都至关重要，然而现有模型在处理固定长度输入和多尺度建模不足方面仍存在困难。我们提出了 MR-CDM，这是一个结合了分层多分辨率趋势分解、针对变长输入的自适应嵌入机制以及多尺度条件扩散过程的框架。在四个真实世界数据集上的评估表明，MR-CDM 显著优于最先进的基准模型（例如 CSDI、Informer），在一定程度上将 MAE 和 RMSE 降低了约 6-10。

## Abstract
Time series forecasting is vital across many domains, yet existing models struggle with fixed-length inputs and inadequate multi-scale modeling. We propose MR-CDM, a framework combining hierarchical multi-resolution trend decomposition, an adaptive embedding mechanism for variable-length inputs, and a multi-scale conditional diffusion process. Evaluations on four real-world datasets demonstrate that MR-CDM significantly outperforms state-of-the-art baselines (e.g., CSDI, Informer), reducing MAE and RMSE by approximately 6-10 to a certain degree.

---

## 论文详细总结（自动生成）

这是一份关于论文《MR-ImagenTime: Multi-Resolution Time Series Generation through Dual Image Representations》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统时间序列预测模型（如 RNN、Transformer）和新兴的扩散模型在处理**变长输入**和**多尺度模式**（如趋势、季节性、噪声的交织）时存在局限。
*   **研究动机**：
    *   现实世界的数据（如电力需求）往往具有异构采样率和复杂的多尺度动态。
    *   现有方法若强制将不同分辨率的数据对齐，会损失细节或扭曲全局结构。
    *   将序列映射为 2D 图像虽能增加灵活性，但简单的映射容易破坏时间连续性和长程依赖。

### 2. 方法论：MR-CDM 框架
该模型采用五阶段流水线处理多尺度特征：
*   **多尺度趋势分解**：使用不同窗口大小（5, 25, 51）的移动平均滤波器，将序列分解为短期波动、中期循环、长期趋势和高频残差。
*   **双重图像表示（关键创新）**：
    *   **延迟嵌入（Delay Embedding）**：将短期、中期信号和残差映射为 32×32 的图像，以保留精细的时间依赖。
    *   **短时傅里叶变换（STFT）**：将长期趋势映射到时频域，突出周期性特征。
*   **图像融合**：将上述不同分支的图像拼接成一个 35 通道的融合张量。
*   **条件扩散过程**：利用条件扩散模型（Conditional Diffusion）进行生成，以历史上下文作为先验，确保生成序列的时间一致性。
*   **增强型分层重构器**：通过交叉尺度注意力机制和自适应权重，并行整合各尺度特征，恢复高保真度的预测序列。

### 3. 实验设计
*   **数据集**：
    *   **ETTh1**：电力变压器温度数据集（小时采样）。
    *   **合成数据集**：模拟 ETTh1 的统计特性，包含日/周周期、趋势和高斯噪声。
    *   **跨领域验证**：Weather（天气预测，21 个变量）和 Traffic（交通流预测，862 个传感器）。
*   **基准模型（Benchmarks）**：
    *   传统模型：ARIMA。
    *   深度学习模型：LSTM。
    *   先进扩散模型：CSDI（条件得分扩散模型）。
*   **评估指标**：MSE、MAE、RMSE。

### 4. 资源与算力
*   **硬件配置**：工作站配备 Intel Xeon Silver 4110 CPU，256 GB RAM。
*   **GPU**：**两块 NVIDIA TITAN RTX (每块 24 GB 显存)**。
*   **软件环境**：Ubuntu 20.04, Python 3.11, PyTorch 2.0.1, CUDA 11.8。
*   **训练细节**：文中提到消融实验运行了 50 个 epoch，主实验对比中 LSTM 训练了 200 个 epoch。未明确说明 MR-CDM 完整训练的总时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主对比实验**：在 4 个数据集上进行了性能测试。
    *   **多步预测分析**：测试了 24、48、96、192 步的不同预测长度。
    *   **消融实验**：包括组件消融（无分解、无条件、无图像融合）、趋势分支消融（去掉不同尺度的趋势）。
    *   **敏感性分析**：测试了不同输入长度（48, 96, 192）对结果的影响。
    *   **重复性验证**：每个实验重复 3 次（随机种子 42, 43, 44）并报告均值。
*   **充分性评价**：实验设计非常全面，涵盖了从性能对比到鲁棒性验证的各个维度，通过合成数据和跨领域数据证明了模型的泛化能力，实验结果客观且具有统计学意义。

### 6. 主要结论与发现
*   **性能卓越**：MR-CDM 在所有测试数据集上均显著优于 ARIMA、LSTM 和 CSDI。在 ETTh1 上，相比 CSDI，其 MSE 和 RMSE 进一步降低。
*   **多尺度分解的必要性**：消融实验显示，移除趋势分解模块会导致性能下降约 89.6%，证明了显式建模多尺度特征的价值。
*   **条件引导的重要性**：无条件扩散模型的性能下降超过 1200%，说明历史上下文对于稳定生成至关重要。
*   **长程预测稳定性**：随着预测步数增加，MR-CDM 的误差增长率远低于基准模型，有效抑制了长程预测中的误差累积。

### 7. 优点（亮点）
*   **创新的表示方法**：结合延迟嵌入和 STFT，既利用了计算机视觉的空域归纳偏置，又保留了频域的周期性信息。
*   **处理变长输入**：通过图像化映射，摆脱了传统模型对固定输入窗口的依赖。
*   **分层重构**：多路径重构器比单路径解码器能更有效地捕捉尺度间的依赖关系。

### 8. 不足与局限
*   **计算复杂度**：引入多尺度分解、STFT 变换和 35 通道的图像扩散过程，其推理延迟和内存开销可能高于轻量级模型（文中未详细对比推理速度）。
*   **超参数敏感性**：移动平均窗口（5, 25, 51）和图像分辨率（32x32）等参数可能需要针对不同特征的数据集进行精细调优。
*   **基准对比范围**：虽然对比了 CSDI，但未与最新的基于大语言模型（LLM-based）的时间序列预测方法进行直接对比。

（完）
