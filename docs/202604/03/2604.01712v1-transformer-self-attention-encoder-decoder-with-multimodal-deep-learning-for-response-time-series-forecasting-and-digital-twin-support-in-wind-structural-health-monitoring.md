---
title: Transformer self-attention encoder-decoder with multimodal deep learning for response time series forecasting and digital twin support in wind structural health monitoring
title_zh: 基于多模态深度学习的 Transformer 自注意力编码器-解码器，用于风致结构健康监测中的响应时间序列预测与数字孪生支持
authors: "Feiyu Zhou, Marios Impraimakis"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01712v1"
tags: ["query:llm-wd"]
score: 8.0
evidence: 用于风致响应时间序列预测的Transformer
tldr: 本研究提出基于Transformer自注意力机制的多模态深度学习模型，用于桥梁风致响应预测与数字孪生监测。该模型通过捕捉时间特征实现高精度预测，并利用预测偏差作为结构变化的预警指标。在Hardanger大桥实测中，该方法无需风平稳性假设，能有效应对环境与交通变化，为基础设施的韧性管理和自适应监测提供了新一代智能化工具。
selection_source: fresh_fetch
motivation: 传统方法在处理非平稳风场及多变环境下的结构振动预测时存在局限，难以准确定义正常振动行为。
method: 采用Transformer自注意力机制构建编解码器模型，结合多模态深度学习捕捉结构响应的时间序列特征。
result: 在Hardanger大桥的真实监测数据上实现了高精度的结构行为捕捉，并能有效识别系统激励变化导致的异常。
conclusion: 基于Transformer的数字孪生组件能够实现基础设施的持续学习与自适应监测，显著提升结构健康监测的智能化水平。
---

## 摘要
本文研究了一种新型 Transformer 方法在风致结构响应预测方面的能力。该模型还为桥梁结构健康监测提供了一个数字孪生组件。首先，该方法利用系统的时域特征来训练预测模型。其次，将振动预测值与测量值进行比较，以检测较大的偏差。最后，识别出的案例被用作结构变化的预警指标。这种基于人工智能的模型在响应预测方面优于现有方法，因为它不需要对风的平稳性或结构正常振动行为进行任何假设。具体而言，风激动力行为面临不确定性，当环境或交通条件发生变化时，往往会导致预测效果不佳，从而难以界定什么是正常的振动行为。为此，本文在挪威科技大学监测的哈当厄尔大桥（Hardanger Bridge）的真实测量数据上对该框架进行了严格验证。该方法能够在现实条件下以及系统激励发生变化时，准确捕捉结构行为。重要的是，研究结果突显了基于 Transformer 的数字孪生组件作为下一代工具的潜力，可用于弹性基础设施管理、持续学习以及针对系统生命周期内时域特征的自适应监测。

## Abstract
The wind-induced structural response forecasting capabilities of a novel transformer methodology are examined here. The model also provides a digital twin component for bridge structural health monitoring. Firstly, the approach uses the temporal characteristics of the system to train a forecasting model. Secondly, the vibration predictions are compared to the measured ones to detect large deviations. Finally, the identified cases are used as an early-warning indicator of structural change. The artificial intelligence-based model outperforms approaches for response forecasting as no assumption on wind stationarity or on structural normal vibration behavior is needed. Specifically, wind-excited dynamic behavior suffers from uncertainty related to obtaining poor predictions when the environmental or traffic conditions change. This results in a hard distinction of what constitutes normal vibration behavior. To this end, a framework is rigorously examined on real-world measurements from the Hardanger Bridge monitored by the Norwegian University of Science and Technology. The approach captures accurate structural behavior in realistic conditions, and with respect to the changes in the system excitation. The results, importantly, highlight the potential of transformer-based digital twin components to serve as next-generation tools for resilient infrastructure management, continuous learning, and adaptive monitoring over the system's lifecycle with respect to temporal characteristics.

---

## 论文详细总结（自动生成）

这是一份关于论文《Transformer self-attention encoder–decoder with multimodal deep learning for response time series forecasting and digital twin support in wind structural health monitoring》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**大跨度桥梁在风致激励下的动力响应预测**。传统结构健康监测（SHM）方法通常依赖于线性假设、平稳风场假设或繁琐的手动特征工程，难以应对环境（如温度、风速）和交通载荷变化带来的非平稳性和非线性。
*   **研究动机**：在复杂的现实环境下，难以准确界定什么是“正常振动行为”，导致损伤识别容易产生误报。
*   **核心目标**：开发一种基于人工智能的数字孪生（Digital Twin）组件，通过学习风与结构的交互规律，实现高精度的响应预测和异常检测，为基础设施的韧性管理提供支持。

### 2. 方法论：核心思想与技术细节
论文提出了一种**多模态 Transformer 编码器-解码器网络**，旨在同时学习结构的时间特征及其与风激励的交互。
*   **核心思想**：利用 Transformer 的自注意力（Self-attention）机制捕捉长程时间依赖，并通过交叉注意力（Cross-attention）融合风速特征（输入）与振动信号（输出）。
*   **关键技术细节**：
    *   **多模态输入**：模型接收两个序列，一是风力特征向量 $\mathbf{w}_t$（风速、方向、强度等），二是结构加速度向量 $\mathbf{a}_t$。
    *   **编码器（Encoder）**：处理风力模态，将其映射为内存张量 $M$，用于后续的跨模态调节。
    *   **解码器（Decoder）**：采用掩码自注意力处理历史加速度，并通过编码器-解码器注意力机制（Encoder-Decoder Attention）从 $M$ 中提取相关的风力信息。
    *   **自回归预测**：模型以迭代方式生成未来 $L_{pred}$ 步的加速度预测值。
    *   **损失函数**：使用均方误差（MSE）优化模型参数 $\theta$。
    *   **公式体现**：核心在于注意力计算 $Attention(Q, K, V) = \text{softmax}(\frac{QK^T}{\sqrt{d_k}})V$，通过 $Q$（解码器状态）与 $K, V$（编码器内存）的交互实现物理因果关系的建模。

### 3. 实验设计
*   **数据集**：使用了挪威**哈当厄尔大桥（Hardanger Bridge）**的真实长期监测数据。该桥主跨 1308 米，配备了 20 个三轴加速度计和 9 个超声波风速仪。
*   **实验场景**：选取了桥梁中跨位置的同步风速与加速度记录，涵盖了不同的风速条件。
*   **Benchmark（基准）**：
    1.  **仅加速度模型（Acc-only）**：旁路编码器，仅依赖历史振动数据预测。
    2.  **1D CNN 编解码器**：传统的卷积神经网络架构，用于对比 Transformer 的序列建模能力。
*   **对比指标**：峰值误差（$\Delta Peak$）、均方根比（RMSR）、频带能量保持（BER）、模态峰值误差（MPE）、RMSE、MAE 以及胜率（Win Rate）。

### 4. 资源与算力
*   **硬件设备**：论文明确提到使用了 University of Bath 的双 GPU 工作站，配置为 **2 × NVIDIA RTX 6000 Ada (每张 48 GB 显存)**，支持 CUDA 12.4。
*   **训练细节**：使用了 RayTune 进行超参数自动优化（如学习率、Dropout、权重衰减等）。虽然未详细列出总训练时长，但给出了最大 Epoch 数（100）和早停机制（Early Stopping）的设置。

### 5. 实验数量与充分性
*   **实验规模**：研究针对三个轴向（纵向 x、横向 y、竖向 z）分别进行了测试，并设置了不同的预测步长（1步、8步、18步，对应 0.01s 到 0.18s 的未来预测）。
*   **充分性**：
    *   **多维度验证**：不仅在时域对比了波形，还在频域对比了功率谱密度（PSD）和模态能量保持。
    *   **鲁棒性测试**：专门对比了高风速（强非线性）与低风速（线性主导）下的模型表现。
    *   **公平性**：所有模型（Transformer 与 CNN）均在相同的预处理、数据分割和归一化流程下运行，并使用了自动调参确保对比的客观性。

### 6. 主要结论与发现
*   **多模态优势**：引入风力特征的模型显著优于仅依赖加速度的模型，尤其在竖向（z轴）预测中，18步预测的峰值误差从 -41.6% 降低到了 -4.4%。
*   **能量保持**：Transformer 模型能更好地保留结构的模态能量（RMSR 接近 1），而传统模型在长步长预测时往往会出现严重的振幅衰减。
*   **风险降低**：多模态模型显著减少了预测残差的“尾部风险”（即大误差出现的概率），有助于减少结构健康监测中的误报。
*   **物理一致性**：模型能够准确捕捉结构的低频基本模态（约 1Hz 以下），同时也忠实还原了传感器产生的高频非物理成分。

### 7. 优点（亮点）
*   **无需物理假设**：不需要对风的平稳性或结构刚度做预设，完全由数据驱动，适应性强。
*   **跨模态注意力**：通过注意力权重图可以解释风速变化如何影响特定时刻的振动，具有一定的可解释性潜力。
*   **局部化应用**：模型可以针对桥梁的特定部位进行训练，无需建立全桥复杂的有限元模型，降低了计算门槛。

### 8. 不足与局限
*   **预测时界较短**：实验仅展示了最多 18 步（约 0.18 秒）的短期预测，对于更长期的结构演化预测能力尚待验证。
*   **极端事件挑战**：在强风/极端天气下，由于系统非线性增强，模型的预测误差明显增大，可靠性有所下降。
*   **数据依赖性**：模型高度依赖高质量、同步的风-振传感器流，若传感器出现故障或数据缺失，跨模态学习的效果会大打折扣。
*   **泛化性问题**：目前仅在单一桥梁上验证，跨结构（从一座桥迁移到另一座桥）的泛化能力（Transfer Learning）仍需进一步研究。

（完）
