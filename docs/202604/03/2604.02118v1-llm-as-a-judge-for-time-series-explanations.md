---
title: LLM-as-a-Judge for Time Series Explanations
title_zh: LLM 作为时间序列解释的评判者
authors: "Preetham Sivalingam, Murari Mandal, Saurabh Deshpande, Dhruv Kumar"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02118v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 大语言模型对基于时间序列的自然语言解释进行评估
tldr: 本研究探讨了利用大语言模型（LLM）作为评判者来评估时间序列数据解释的准确性。针对现有评估方法依赖参考答案或无法处理文本推理的局限，作者提出了一种无需参考的评估框架，并构建了包含350个案例的合成基准测试集。研究发现，LLM在评估时间序列解释方面的表现比生成解释更稳定，揭示了生成与评估之间的不对称性，证明了LLM作为时间序列领域可靠评估工具的潜力。
selection_source: fresh_fetch
motivation: 现有的时间序列解释评估方法要么依赖参考答案，要么无法直接验证自由文本与底层数值数据的一致性。
method: 提出一种无需参考的LLM-as-a-Judge框架，通过模式识别、数值准确性和回答忠实度三个维度对解释进行评分，并构建了包含七种查询类型的合成基准。
result: 实验显示生成能力受模式影响波动巨大（准确率0.00至0.96），而评估能力则相对稳定，模型能准确对解释进行排序和评分。
conclusion: LLM在时间序列领域表现出显著的生成与评估不对称性，证明了其作为数据驱动推理可靠评判者的可行性。
---

## 摘要
评估基于时间序列数据的 LLM 生成的自然语言解释的事实准确性仍然是一个开放性挑战。尽管现代模型能够生成数值信号的文本解释，但现有的评估方法存在局限性：基于参考的相似度指标和一致性检查模型需要标准答案（ground truth）解释，而传统的时间序列方法纯粹基于数值运行，无法评估自由形式的文本推理。因此，目前尚不存在一种通用方法，能够在没有预定义参考或特定任务规则的情况下，直接验证解释是否忠实于底层时间序列数据。我们研究了大语言模型在无参考设置下同时作为时间序列解释的生成器和评估器的情况。在这种设置中，给定时间序列、问题和候选解释，评估器根据模式识别、数值准确性和回答忠实度分配三元正确性标签，从而实现原则性的评分和比较。为了支持这一研究，我们构建了一个包含 350 个时间序列案例的合成基准，涵盖七种查询类型，每种类型都配有正确、部分正确和错误的解释。我们在四个任务上评估了模型：解释生成、相对排序、独立评分和多异常检测。结果显示出明显的不对称性：生成过程高度依赖于模式，并在某些查询类型上表现出系统性失败（例如“季节性下降”和“波动性偏移”的准确率仅为 0.00 到 0.12，而“结构性断裂”的准确率为 0.94 到 0.96），而评估过程则更为稳定，即使模型自身的输出不正确，它们也能正确地对解释进行排序和评分。这些发现证明了基于数据驱动的 LLM 评估时间序列解释的可行性，并突显了它们作为时间序列领域数据驱动推理的可靠评估器的潜力。

## Abstract
Evaluating factual correctness of LLM generated natural language explanations grounded in time series data remains an open challenge. Although modern models generate textual interpretations of numerical signals, existing evaluation methods are limited: reference based similarity metrics and consistency checking models require ground truth explanations, while traditional time series methods operate purely on numerical values and cannot assess free form textual reasoning. Thus, no general purpose method exists to directly verify whether an explanation is faithful to underlying time series data without predefined references or task specific rules. We study large language models as both generators and evaluators of time series explanations in a reference free setting, where given a time series, question, and candidate explanation, the evaluator assigns a ternary correctness label based on pattern identification, numeric accuracy, and answer faithfulness, enabling principled scoring and comparison. To support this, we construct a synthetic benchmark of 350 time series cases across seven query types, each paired with correct, partially correct, and incorrect explanations. We evaluate models across four tasks: explanation generation, relative ranking, independent scoring, and multi anomaly detection. Results show a clear asymmetry: generation is highly pattern dependent and exhibits systematic failures on certain query types, with accuracies ranging from 0.00 to 0.12 for Seasonal Drop and Volatility Shift, to 0.94 to 0.96 for Structural Break, while evaluation is more stable, with models correctly ranking and scoring explanations even when their own outputs are incorrect. These findings demonstrate feasibility of data grounded LLM based evaluation for time series explanations and highlight their potential as reliable evaluators of data grounded reasoning in the time series domain.

---

## 论文详细总结（自动生成）

### 论文总结：LLM-as-a-Judge for Time Series Explanations

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）在金融、医疗和工业监控等领域用于解释时间序列数据，如何验证这些自然语言解释的**事实准确性**成为关键挑战。
*   **现有局限**：传统的文本评估指标（如 BLEU、ROUGE）依赖参考答案（Ground Truth），且仅关注词汇相似度，无法验证文本与底层数值数据的一致性；而传统时间序列分析工具仅处理数值，无法理解自由文本推理。
*   **研究目标**：探索 LLM 是否可以在没有参考答案的情况下，直接根据原始数值数据来评估时间序列解释的质量，并研究模型在“生成解释”与“评估解释”能力上的差异。

#### 2. 方法论：核心思想与技术细节
论文提出了一种**基于准则引导（Rubric-guided）、无需参考（Reference-free）**的 LLM 评估框架。
*   **核心思想**：将评估建模为一个三元分类任务：$y = f_\theta(T, q, e)$，其中 $T$ 是时间序列，$q$ 是问题，$e$ 是候选解释。标签 $y \in \{0, 1, 2\}$ 分别代表错误、部分正确（含数值误差）和完全正确。
*   **评估准则**：模型被要求从五个维度进行推理：
    1.  **数据忠实度**：是否正确识别了模式、趋势或异常。
    2.  **数值准确性**：量级、索引、百分比变化等计算是否正确。
    3.  **问题相关性**：是否直接回答了提问。
    4.  **逻辑连贯性**：推理过程是否自洽。
    5.  **无支持声明**：是否避免了幻觉（即未在数据中体现的声明）。
*   **流程**：通过结构化提示词（Structured Prompting）引导模型先观察原始数据，再对照准则对解释进行验证，最后给出评分或排序。

#### 3. 实验设计
*   **数据集（TSQueryBench）**：作者构建了一个合成基准，包含 350 个时间序列实例，涵盖 7 种查询类型（线性尖峰、季节性下降、结构性断裂、多指标一致性、相对极值、均值偏移、波动性偏移）。
*   **任务设置**：
    1.  **解释生成**：直接根据数据生成解释。
    2.  **相对排序**：从正确、部分正确、错误三个选项中选出最佳解释。
    3.  **独立评分**：对单个解释进行三元分类评分。
    4.  **多异常检测**：直接识别序列中所有的异常点及其变化。
*   **对比模型**：选择了三个主流开源模型：Qwen-3 8B、LLaMA-3.1 8B、Gemma-2 9B IT。

#### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   由于实验主要在 **Zero-shot（零样本）** 推理设置下进行，不涉及大规模模型微调，因此算力消耗主要集中在对 1050 个解释实例（350 案例 × 3 候选）的推理生成上。

#### 5. 实验数量与充分性
*   **实验规模**：共 350 个原始案例，扩展为 1050 个评估实例，涵盖了 100 到 500 不等的序列长度。
*   **充分性**：实验设计较为全面，通过四个互补的任务（生成 vs. 评估）深入探讨了模型能力的边界。
*   **客观性**：使用了合成数据以确保 Ground Truth 的绝对精确，并引入了人类专家对生成任务进行标注，评估过程具有较高的客观性和公平性。

#### 6. 主要结论与发现
*   **生成与评估的不对称性**：这是核心发现。模型在生成解释时表现不稳定（如在“波动性偏移”上准确率为 0），但在评估相同类型的解释时，准确率显著提高。这表明 **LLM 识别错误的能力强于其创造正确内容的能力**。
*   **模式依赖性**：模型对“结构性断裂”等显性模式处理较好（准确率 > 0.9），但对“波动性”等高阶统计属性的理解非常薄弱。
*   **评估的稳定性**：在独立评分和排序任务中，Qwen-3 8B 等模型表现出极强的鲁棒性，证明了 LLM 作为“数据驱动评判者”的可行性。
*   **异常检测倾向**：在多异常检测中，模型倾向于过度敏感（高召回率，低精确率），容易将微小波动误判为异常。

#### 7. 优点（亮点）
*   **无需参考答案**：解决了时间序列解释评估中缺乏标准文本答案的痛点。
*   **细粒度准则**：通过区分“数值错误”和“逻辑错误”，提供了比二元分类更深层的评估视角。
*   **揭示能力鸿沟**：系统性地证明了 LLM 在数值推理任务中，评估能力优于生成能力，为未来构建“生成-验证”双系统提供了理论依据。

#### 8. 不足与局限
*   **数据集局限性**：实验完全基于合成数据，虽然受控性强，但可能无法完全模拟真实世界数据（如金融、医疗）中的复杂噪声和非线性特征。
*   **模型规模限制**：仅测试了 8B/9B 级别的模型，未探讨更大规模模型（如 70B 或 GPT-4 级别）是否能克服在波动性检测等任务上的失败。
*   **长序列挑战**：随着时间步长增加（如 500 步以上），部分模型（如 Gemma）因 Token 限制无法处理，且所有模型的数值定位精度均有所下降。

（完）
