---
title: Context-Aware Hospitalization Forecasting Evaluations for Decision Support using LLMs
title_zh: 基于大语言模型（LLMs）的决策支持上下文感知住院预测评估
authors: "Rhea Makkuni, Ananya Joshi"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.23949v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 利用LLM结合时序和非时序背景进行住院预测
tldr: 本研究探讨了在大规模医疗中断期间，如何利用大语言模型（LLM）结合人口、地理等非时间性背景信息进行住院预测。通过对比直接LLM预测、传统时间序列模型及混合模型HybridARX，研究发现将LLM提取的背景信号嵌入结构化模型能显著提高预测的稳定性和校准度，从而为医疗资源分配决策提供更可靠的支持。
selection_source: fresh_fetch
motivation: 传统预测模型主要依赖历史数值，难以整合复杂的非时间性背景信息，导致在现实医疗资源决策场景中可靠性不足。
method: 对比评估了直接LLM预测、经典时间序列模型以及将LLM衍生信号融入结构化模型的HybridARX混合流水线。
result: HybridARX在60个美国县的评估中表现优于传统ARX模型，特别是在处理噪声背景信号时，提供了更稳定且校准更好的预测。
conclusion: 在非平稳的医疗资源预测任务中，将LLM嵌入结构化混合模型是发挥其上下文理解能力并提升决策支持效果的最佳方式。
---

## 摘要
医学和公共卫生专家必须在大规模医疗中断（如运营故障或大流行病）期间，根据预测的住院趋势做出实时资源决策，例如扩大医院床位容量。预测模型可以通过分析设施级别的大量资源相关数据来辅助这一任务，但它们必须在现实世界的数据条件下对决策具有可靠性。最近的研究表明，大语言模型（LLMs）可以将更丰富的上下文形式融入数值预测中。传统模型主要依赖时间上下文（即过去的观测值），而 LLMs 还可以利用非时间性的公共卫生上下文，如人口统计、地理和人口层面的特征。然而，在现实世界的医疗场景中，如何使用这些模型来产生稳定或与决策相关的预测仍不清楚。为了评估 LLMs 在这种场景下如何被有效使用，我们在美国 60 个具有低、中、高住院强度的县评估了三种方法：直接基于 LLM 的预测、经典时间序列模型，以及一种将 LLM 衍生信号融入结构化模型的上下文增强混合流水线（HybridARX）。由于目标是运营决策而非仅仅是误差最小化，除了标准的预测指标外，我们还通过偏差和超前-滞后对齐来评估性能。我们的结果表明，HybridARX 通过产生更稳定和校准更好的预测，优于经典的 ARX 模型，特别是在将噪声上下文信号融入结构化时间序列模型时。这些发现表明，在非平稳的医疗资源预测中，当 LLMs 嵌入到结构化混合模型中时最为有用。

## Abstract
Medical and public health experts must make real-time resource decisions, such as expanding hospital bed capacity, based on projected hospitalization trends during large-scale healthcare disruptions (e.g., operational failures or pandemics). Forecasting models can assist in this task by analyzing large volumes of resource-related data at the facility level, but they must be reliable for decision-making under real-world data conditions. Recent work shows that large language models (LLMs) can incorporate richer forms of context into numerical forecasting. Whereas traditional models rely primarily on temporal context (i.e., past observations), LLMs can also leverage non-temporal public health context such as demographic, geographic, and population-level features. However, it remains unclear how these models should be used to produce stable or decision-relevant predictions in real-world healthcare settings. To evaluate how LLMs can be effectively used in this setting, we evaluate three approaches across 60 counties with low-,mid-, and high-hospitalization intensities in the United States: direct LLM-based forecasting, classical time-series models, and a context-augmented hybrid pipeline (HybridARX) that incorporates LLM-derived signals into structured models. Because the goal is operational decision-making rather than error minimization alone, we evaluate performance with bias and lead-lag alignment in addition to standard forecasting metrics. Our results show that HybridARX improves over classical ARX by yielding more stable and better-calibrated forecasts, particularly when incorporating noisy contextual signals into structured time-series models. These findings suggest that, in non-stationary healthcare resource forecasting, LLMs are most useful when embedded within structured hybrid models.

---

## 论文详细总结（自动生成）

这是一份关于论文《Context-Aware Hospitalization Forecasting Evaluations for Decision Support using LLMs》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是：**如何利用大语言模型（LLM）提升医疗资源（如住院人数）预测的可靠性，以支持实时决策。**
*   **背景**：在流行病或医疗系统中断期间，准确预测住院人数对分配床位、人员和设备至关重要。
*   **动机**：传统时间序列模型（如 AR、指数平滑）主要依赖历史数值（时间上下文），难以整合人口统计、地理和公共卫生政策等非时间性背景信息。虽然 LLM 具备处理复杂背景的能力，但其直接预测往往存在幻觉和不稳定性。
*   **目标**：探索 LLM 在医疗预测中的最佳应用模式，即作为独立预测器还是作为结构化模型中的信号增强组件。

### 2. 论文提出的方法论
论文对比了三种主要方法，核心思想是将 LLM 的上下文推理能力与经典统计模型的稳定性相结合：
*   **直接 LLM 预测（Prompt-Only）**：将过去 8 周的住院数据以结构化文本形式输入 LLM，直接要求其预测下一周的数值。
*   **HybridARX（上下文增强混合模型）**：这是论文提出的核心创新方案，分为两个阶段：
    1.  **第一阶段（LLM 预测指标）**：利用 LLM 预测下一周的“领先指标”（如 ICU 床位占用率、呼吸机使用率、嗅觉/味觉丧失的搜索热度）。
    2.  **第二阶段（结构化预测）**：将 LLM 生成的这些指标作为外生变量（Exogenous inputs），输入到经典的自回归模型（ARX）或线性回归模型中，生成最终的住院预测。
*   **经典基准模型**：包括 Lag-1（滞后一阶）、AR(1)、指数平滑（Exponential Smoothing）和传统的 ARX 模型。

### 3. 实验设计
*   **数据集**：使用了美国宾夕法尼亚州 60 个县的 COVID-19 住院数据（2020-2022年）。
*   **数据源**：宾州卫生部（住院数）、Delphi COVIDcast API（症状调查、搜索趋势）、Google 社区流动性报告、2020 年人口普查数据。
*   **场景划分**：根据平均住院强度将 60 个县分为低、中、高三个层级（Tertiles），以评估模型在不同规模数据下的鲁棒性。
*   **评估指标**：
    *   **MAPE（平均绝对百分比误差）**：衡量准确性。
    *   **MPE（平均百分比误差）**：衡量系统性偏差（过高或过低预测）。
    *   **超前-滞后分析（Lead-Lag Analysis）**：衡量预测趋势与实际趋势在时间上的对齐程度。

### 4. 资源与算力
*   **模型版本**：使用了 `gpt-5-2025-08-07`（注：文中提及的版本号，反映了论文设定的未来时间背景）。
*   **算力说明**：论文**未明确说明**具体的本地 GPU 型号或训练时长。由于主要通过 API 调用 LLM 且第二阶段使用的是轻量级统计模型（ARX、线性回归），该方法对本地算力的需求极低，主要成本在于 API 调用。

### 5. 实验数量与充分性
*   **实验规模**：覆盖了 60 个县的长期滚动窗口预测，每个县、每周、每个模型都进行了多次运行。
*   **重复性**：针对 LLM 的随机性，每个实验独立运行了 3 次以评估变异性。
*   **充分性评价**：实验设计较为充分。通过分层抽样（高/中/低强度县）确保了结论在不同人口规模下的普适性；引入了偏差和时间对齐指标，而非单一的准确率，这在医疗决策场景下非常客观且具有实际意义。

### 6. 主要结论与发现
*   **HybridARX 表现最优**：相比于传统的 ARX 模型，HybridARX 显著降低了大误差，预测结果更稳定且校准度更高。
*   **LLM 的角色定位**：LLM 作为“上下文信号编码器”（将杂乱的背景信息转化为结构化预测指标）比作为直接预测器更有效。
*   **直接预测的局限**：直接 Prompt LLM 虽然能捕捉大致趋势，但在准确性上并不总是优于简单的统计基准（如指数平滑）。
*   **简单基准的顽强性**：Lag-1 模型在 MAPE 指标上依然非常有竞争力，但它无法提供决策所需的预见性。

### 7. 优点
*   **决策导向**：评估不仅看误差大小，还看预测是否超前或滞后，这直接关系到医院能否提前准备资源。
*   **混合架构**：巧妙地解决了 LLM 的不稳定性问题，通过结构化模型（ARX）为 LLM 的输出提供了“护栏”。
*   **处理噪声能力**：证明了 LLM 可以有效地平滑和处理不完整或有噪声的公共卫生领先指标。

### 8. 不足与局限
*   **时间滞后问题**：所有评估的模型（包括 HybridARX）在捕捉趋势转变时仍普遍存在约一周的滞后，未能实现真正的“早期预警”。
*   **地理局限性**：实验仅限于宾夕法尼亚州，不同地区的医疗报告标准和数据质量可能影响模型迁移效果。
*   **缺乏概率估计**：模型主要提供点预测，虽然通过多次运行捕捉了部分不确定性，但缺乏正式的概率区间（如 95% 置信区间）输出。
*   **黑盒依赖**：HybridARX 的第一阶段仍依赖于闭源 LLM，其内部推理逻辑对医疗决策者而言不够透明。

（完）
