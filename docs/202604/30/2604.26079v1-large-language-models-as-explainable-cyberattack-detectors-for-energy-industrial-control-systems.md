---
title: Large Language Models as Explainable Cyberattack Detectors for Energy Industrial Control Systems
title_zh: 大语言模型作为能源工业控制系统的可解释网络攻击检测器
authors: "Weiyi Kong, Ahmad Mohammad Saber, Amr Youssef, Deepa Kundur"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26079v1"
tags: ["query:llm"]
score: 9.0
evidence: 大语言模型在能源工业控制系统中的应用
tldr: 本研究探讨了利用大语言模型（LLM）作为能源工业控制系统（ICS）中Modbus流量的可解释入侵检测器。通过将Modbus通信转换为离散化的令牌字符串，并利用提示词配置LLM进行二分类决策，该方法在无需特定任务权重更新的情况下，实现了与强监督基线相当的检测性能。此外，LLM还能生成基于令牌的事故记录，为操作员提供可审计的决策依据，增强了系统的透明度。
selection_source: fresh_fetch
motivation: 现代能源工业控制系统需要不仅准确且具备可审计性的入侵检测手段，以辅助操作员进行决策。
method: 将Modbus通信字段转换为紧凑的令牌字符串，并利用提示词引导大语言模型生成分类警报及基于令牌的事故记录。
result: 在两个公开数据集上，该LLM检测流水线在无需微调的情况下达到了与强监督基线相当的高预测性能。
conclusion: 大语言模型可作为工业控制系统中有效的、可解释的入侵检测补充层，通过提供可审计信号提升安全分析效率。
---

## 摘要
在现代能源系统中，工业控制系统（ICS）和电力系统 SCADA 需要不仅准确而且可供操作员审计的入侵检测。目前，ICS 入侵检测领域主要由成熟的有监督检测器占据。在本文中，我们研究了现成的大语言模型（LLM）是否可以作为 Modbus 流量的补充性、人机回环层。我们在两个公开的 ICS Modbus 数据集上将其建模为网络侧的二元“正常/关键”决策任务，将攻击时段和其他安全关键行为合并为一个“关键”类别。每个 Modbus 通信实例都被转换为源自离散化协议字段的紧凑标记字符串，随后由提示配置的 LLM 生成正常/关键警报，并附带一份简洁的、基于标记的事件记录供分析师审查。在匹配的事件信息和共享的评估划分下，由此产生的基于 LLM 的分流流水线在两个基准测试中均实现了较高的预测性能，且与强大的有监督基准模型大致相当，同时无需进行特定任务的权重更新。为了评估审计记录，我们应用了基于干预的诊断，包括充分性和必要性测试，这些测试提供的证据表明，引用的标记通常与模型自身的预测决策相关。这些记录旨在作为审计信号，而非完全基于人类理解的解释。

## Abstract
In modern energy systems, industrial control systems (ICS) and power-system SCADA require intrusion detection that is not only accurate but also auditable by operators. The ICS intrusion-detection landscape is currently dominated by established supervised detectors. In this paper, we study whether an off-the-shelf large language model (LLM) can serve as a complementary, human-in-the-loop layer for Modbus traffic. We cast this as a binary network-side normal/critical decision task on two public ICS Modbus datasets, collapsing attack periods and other safety-critical behaviors into a single critical class. Each Modbus communication instance is converted into a compact token string derived from discretized protocol fields, and a prompt-configured LLM produces a normal/critical alert together with a concise, token-grounded incident record for analyst review. Under matched event information and shared evaluation splits, the resulting LLM-based triage pipeline achieves high predictive performance on both benchmarks and is broadly comparable to strong supervised baselines, while requiring no task-specific weight updates. To assess the audit record, we apply intervention-based diagnostics, including sufficiency- and necessity-style tests, which provide evidence that the cited tokens are often decision-relevant to the model's own prediction. These records are intended as audit signals rather than full human-grounded explanations.

---

## 论文详细总结（自动生成）

以下是对论文《Large Language Models as Explainable Cyberattack Detectors for Energy Industrial Control Systems》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
现代能源工业控制系统（ICS）和电力系统 SCADA 依赖 Modbus 等协议进行通信，极易受到网络攻击（如虚假数据注入 FDIA）。现有的入侵检测系统（IDS）多采用有监督学习模型，虽然准确率高，但存在以下痛点：
*   **缺乏可审计性：** 许多模型是“黑盒”，操作员难以理解警报原因，不利于事故响应。
*   **部署成本高：** 往往需要特定站点的标记数据进行重新训练或校准。
*   **解释性不足：** 现有的 XAI 方法（如 SHAP/LIME）生成的解释形式不便于直接作为操作员的事故记录。

本研究探讨了**现成的大语言模型（LLM）**是否能作为一种无需训练、具备可解释性且可实现“人机回环”的补充检测层，通过将协议流量转化为文本令牌，实现对攻击行为的识别与审计。

### 2. 论文提出的方法论
论文提出了一种基于提示词（Prompt）配置的 LLM 检测与审计流水线，核心步骤如下：
*   **输入表示（令牌化）：** 将原始 Modbus/TCP 报文解析并离散化为紧凑的字符串。包含字段：方向（DIR）、功能码（FC）、单元标识（UNIT）、载荷长度桶（LEN）、到达间隔时间桶（IAT）和异常标志（EX）。
*   **两阶段流水线：**
    1.  **分类阶段：** 使用 GPT-4o 作为分类器，通过包含 Modbus 语义和安全原则的提示词（Few-shot），输出“正常/关键”标签、置信度和简短理由。
    2.  **审计阶段：** 在标签确定后，由 LLM 生成结构化记录，包括：**证据片段**（从输入中摘录的原始令牌）、**风险标签**（如功能码异常、时间异常）和**反事实编辑建议**（修改哪个令牌能改变决策）。
*   **决策逻辑：** 采用“安全优先”策略，任何偏离常规协议行为或符合攻击特征的模式都会被标记为“关键（Critical）”。

### 3. 实验设计
*   **数据集：** 使用了两个公开的 ICS Modbus 基准数据集：**LeMay CSET’16** 和 **CIC Modbus 2023**。
*   **任务设置：** 二分类任务。将所有攻击行为和安全关键行为合并为“关键”类，其余为“正常”类。
*   **对比基准（Baselines）：**
    *   传统机器学习：逻辑回归（Logistic Regression）、梯度提升树（LightGBM）。
    *   先进模型：Kolmogorov-Arnold Networks (KAN)、DistilBERT（处理相同的令牌字符串）。
*   **评估指标：** 准确率（Accuracy）、Macro-F1、关键类召回率（Recall）和 F1 分数。

### 4. 资源与算力
*   **模型：** 使用了 OpenAI 的 **GPT-4o** API（文本模式）。
*   **算力说明：** 论文未提及具体的本地 GPU 训练资源，因为 LLM 是通过 API 调用的现成模型，**无需特定任务的权重更新（零微调）**。
*   **性能开销：** 记录了推理延迟，中位延迟约为 **0.91 秒/样本**，p99 延迟为 1.51 秒。成本估算为每千次推理约 0.42 至 1.42 美元。

### 5. 实验数量与充分性
*   **实验规模：** 在两个主流数据集上进行了完整的测试集评估，并与四种不同类型的基准模型进行了对比。
*   **解释性验证：** 引入了基于干预的诊断测试，包括：
    *   **充分性测试（Sufficiency）：** 仅保留证据令牌，观察预测是否保持一致（通过率 95.64%）。
    *   **必要性测试（Necessity）：** 移除证据令牌，观察预测是否反转（反转率 73.15%）。
*   **公平性：** 所有模型使用相同的训练/验证/测试划分，且离散化边界在训练集上拟合后固定，确保了对比的客观性。

### 6. 主要结论与发现
*   **性能优异：** 在无需任何权重微调的情况下，LLM 在两个数据集上均达到了约 **0.98 的准确率**，且在关键类别的召回率上表现出色（CIC 数据集上达到 0.96，优于所有基准模型）。
*   **可审计性强：** LLM 生成的证据令牌与模型决策高度相关，能够为操作员提供具有决策相关性的审计信号。
*   **互补性：** LLM 倾向于给出保守的（高召回率）预测，适合作为安全分流层，与高吞吐量的传统检测器配合使用。

### 7. 优点
*   **即插即用：** 无需针对特定站点进行昂贵的模型训练或特征工程。
*   **原生可解释性：** 能够直接生成基于协议令牌的事故记录，比数值型的特征归因（如 SHAP）更直观。
*   **鲁棒的推理：** 提示词中编码的“安全第一原则”使模型在处理未知或变体攻击时具有较好的泛化能力。

### 8. 不足与局限
*   **任务简化：** 仅进行了二分类（正常/关键）实验，未涉及多分类攻击归因，实际场景中可能需要更细粒度的分类。
*   **延迟与成本：** 相比于毫秒级的传统 ML 模型，LLM API 的延迟（约 1 秒）和按 Token 计费的成本限制了其在高吞吐量实时监测中的应用。
*   **反事实生成能力有限：** 实验显示 LLM 生成的单步反事实编辑导致标签反转的成功率仅为 23.43%，说明其对决策边界的精确理解仍有提升空间。
*   **隐私风险：** 使用 API 模式可能涉及工业数据外流的隐私合规问题（尽管本文使用的是公开数据集）。

（完）
