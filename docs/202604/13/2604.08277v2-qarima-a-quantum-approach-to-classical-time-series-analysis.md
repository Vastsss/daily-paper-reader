---
title: "QARIMA: A Quantum Approach To Classical Time Series Analysis"
title_zh: QARIMA：一种用于经典时间序列分析的量子方法
authors: "Nishikanta Mohanty, Bikash K. Behera, Badshah Mukherjee, Pravat Dash"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08277v2"
tags: ["query:llm-ts"]
score: 9.0
evidence: 用于经典时间序列分析的量子启发方法
tldr: 本研究提出QARIMA，一种将量子辅助滞后发现与固定配置变分量子电路（VQC）相结合的时间序列分析方法。该方法利用量子自相关和偏自相关技术确定模型阶数，并通过VQC进行参数估计和弱滞后项细化。在环境与工业数据集上的实验证明，QARIMA在预测精度上优于传统ARIMA，并明确了量子效应在阶数发现和参数估计中的具体贡献。
selection_source: fresh_fetch
motivation: 旨在利用量子计算的并行性与特征提取能力，解决经典ARIMA模型在滞后项识别和参数估计中的局限性。
method: 结合基于Swap-test的量子自相关/偏自相关分析进行阶数筛选，并利用变分量子电路（VQC）执行AR/MA参数估计与滞后项细化。
result: 在多项数据集的滚动预测评估中，QARIMA在MSE和MAPE等指标上均显著优于自动化经典ARIMA模型。
conclusion: QARIMA通过七项核心量子技术贡献，实现了量子增强的时间序列建模，有效降低了优化开销并提升了预测性能。
---

## 摘要
我们提出了一种受量子启发的 ARIMA 方法，该方法将量子辅助的滞后项发现与固定配置的变分量子电路（VQC）相结合，用于参数估计和弱滞后项细化。差分和候选滞后项通过基于交换测试（swap-test）驱动的量子自相关（QACF）和量子偏自相关（QPACF）进行识别，并采用延迟矩阵构建将量子投影与时域回归量对齐，随后遵循标准的信息准则简约性原则。在给定筛选出的阶数 (p,d,q) 后，我们保留固定的 VQC 拟设（ansatz）、优化器和训练预算，以防止超参数泄漏，并将电路部署在两个估计角色中：用于自回归系数的 VQC-AR 和用于移动平均系数的 VQC-MA。在筛选和估计之间，轻量级的 VQC 弱滞后项细化在不改变 (p,d,q) 的情况下，对筛选出的 AR 滞后项进行重新加权或剪枝。在环境和工业数据集上，我们针对自动化的经典 ARIMA 进行了滚动起点评估，报告了样本外均方误差（MSE）、平均绝对百分比误差（MAPE）以及针对 MSE 和 MAE 的 Diebold-Mariano 检验。实验表明，七项量子贡献——(1) 差分选择、(2) QACF、(3) QPACF、(4) 带有延迟矩阵构建的交换测试原语、(5) VQC-AR、(6) VQC 弱滞后项细化以及 (7) VQC-MA——共同减少了元优化开销，并明确了量子效应在阶数发现、滞后项细化以及 AR/MA 参数估计中的切入点。

## Abstract
We present a quantum-inspired ARIMA methodology that integrates quantum-assisted lag discovery with fixed-configuration variational quantum circuits (VQCs) for parameter estimation and weak-lag refinement. Differencing and candidate lags are identified via swap-test-driven quantum autocorrelation (QACF) and quantum partial autocorrelation (QPACF), with a delayed-matrix construction that aligns quantum projections to time-domain regressors, followed by standard information-criterion parsimony. Given the screened orders (p,d,q), we retain a fixed VQC ansatz, optimizer, and training budget, preventing hyperparameter leakage, and deploy the circuit in two estimation roles: VQC-AR for autoregressive coefficients and VQC-MA for moving-average coefficients. Between screening and estimation, a lightweight VQC weak-lag refinement re-weights or prunes screened AR lags without altering (p,d,q). Across environmental and industrial datasets, we perform rolling-origin evaluations against automated classical ARIMA, reporting out-of-sample mean squared error (MSE), mean absolute percentage error (MAPE), and Diebold-Mariano tests on MSE and MAE. Empirically, the seven quantum contributions (1) differencing selection, (2) QACF, (3) QPACF, (4) swap-test primitives with delayed-matrix construction, (5) VQC-AR, (6) VQC weak-lag refinement, and (7) VQC-MA collectively reduce meta-optimization overhead and make explicit where quantum effects enter order discovery, lag refinement, and AR/MA parameter estimation.

---

## 论文详细总结（自动生成）

以下是对论文《QARIMA: A Quantum Approach To Classical Time Series Analysis》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的自回归差分移动平均模型（ARIMA）在处理具有长程依赖、非线性动力学或高噪声的现实世界数据时，往往表现不佳。此外，ARIMA 依赖于启发式方法（如 ACF/PACF 图、AIC/BIC 准则）进行阶数识别，这些方法在复杂环境下较为脆弱。
*   **整体含义**：本文提出了一种名为 **QARIMA** 的量子启发式框架。其核心动机是利用量子计算的特性（如量子重叠、交换测试和变分电路）来增强经典 ARIMA 的三个关键环节：平稳性处理（差分）、模型阶数识别（滞后项发现）和参数估计。该方法旨在保留 ARIMA 可解释性的同时，引入量子非线性映射能力。

### 2. 方法论：核心思想与关键技术
QARIMA 包含七个协调的量子组件，构成了一个完整的混合量子-经典流水线：
*   **核心思想**：将经典的时间序列向量编码为量子态，利用量子投影（交换测试）代替经典的内积运算，并使用变分量子电路（VQC）作为非线性参数估计器。
*   **关键技术细节**：
    1.  **量子启发式差分选择**：利用量子投影最小化预测误差，通过观察参数 $\gamma$ 的稳定性来确定最优差分阶数 $d$。
    2.  **量子自相关（QACF）与偏自相关（QPACF）**：使用**紧凑交换测试（Compact Swap Test）**计算滞后向量间的余弦相似度，作为识别 AR 阶数 $p$ 和 MA 阶数 $q$ 的量子诊断工具。
    3.  **变分量子电路（VQC）估计器**：
        *   **VQC-AR**：将自回归系数映射为量子旋转门参数，通过最小化包含预测误差、余弦对齐惩罚和量子熵惩罚的复合损失函数来优化系数。
        *   **VQC-MA**：对残差进行类似的量子建模。
    4.  **弱滞后项细化（Weak-Lag Refinement）**：在不改变模型阶数的前提下，利用 VQC 对筛选出的滞后项进行重新加权或剪枝，以提高模型简约性。
    5.  **相位校正**：引入相位权重 $\omega$ 来修正经典点积与量子交换测试输出之间的角度偏差。

### 3. 实验设计
*   **数据集**：涵盖了环境、工业和天文等多个领域：
    *   **Sunspots**（太阳黑子年均值）：具有准周期性和非线性。
    *   **Mauna Loa CO2**（二氧化碳浓度）：具有强趋势和季节性。
    *   **Australian Woollen Yarn**（羊毛产量）：工业生产数据。
    *   **Australian Beer Production**（啤酒产量）：季度性数据。
    *   **Sydney 2024 Weather**（悉尼气温）：短期高频数据。
*   **Benchmark（基准方法）**：经典的自动化 ARIMA 模型（使用 Python 的 `pmdarima` 库）。
*   **对比指标**：样本外（OOS）的均方误差（MSE）、平均绝对百分比误差（MAPE）以及 **Diebold-Mariano (DM) 检验**（用于验证预测差异的统计显著性）。

### 4. 资源与算力
*   **算力说明**：论文明确提到使用了 **IBM Quantum Experience** 平台及其 **Qiskit** 框架。
*   **具体细节**：实验主要在量子模拟器（QASM Simulator）上运行。
*   **缺失信息**：文中未详细列出具体的硬件型号（如 CPU/GPU）、具体的训练时长或模拟所需的总机时。

### 5. 实验数量与充分性
*   **实验数量**：针对 5 个不同特征的数据集进行了完整的滚动起点（Rolling-origin）评估，每个数据集都测试了多个候选的 $(p, d, q)$ 组合。
*   **充分性与客观性**：
    *   **充分性**：实验涵盖了从长序列（太阳黑子）到短序列（羊毛产量）的不同场景，并引入了统计显著性检验（DM Test），这在量子机器学习论文中较为难得。
    *   **公平性**：QARIMA 和经典 ARIMA 使用了相同的训练/测试划分，且 QARIMA 采用了固定的 VQC 配置（固定拟设、优化器和预算），有效防止了超参数泄漏。

### 6. 主要结论与发现
*   **性能提升**：在大多数数据集上，QARIMA 的 MSE 和 MAPE 均优于或持平于经典 ARIMA。
*   **量子优势切入点**：量子效应在处理具有复杂动力学（如太阳黑子）和长记忆性（如 CO2 趋势）的序列时表现尤为突出。
*   **模型简约性**：通过量子弱滞后项细化，模型能够在保持较低阶数的同时捕捉到更细微的信号。
*   **统计显著性**：DM 检验确认了 QARIMA 在多个案例中的改进在统计上是显著的，而非随机波动。

### 7. 优点
*   **模块化集成**：没有完全抛弃 ARIMA，而是将其组件“量子化”，保留了统计模型的可解释性。
*   **硬件友好**：采用了浅层电路（Shallow VQC）和紧凑的交换测试，适合在近期的噪声中等规模量子（NISQ）设备上运行。
*   **系统性强**：从差分到阶数识别再到参数优化，提供了一套完整的量子化流程，而非单一环节的改进。

### 8. 不足与局限
*   **模拟局限**：主要结果基于量子模拟器，尚未在真实的量子硬件上进行大规模验证（尽管文中提到兼容 NISQ）。
*   **计算开销**：虽然量子算法在理论上有并行优势，但在经典计算机上模拟量子交换测试和 VQC 优化的时间成本远高于直接运行经典 ARIMA。
*   **单变量限制**：目前仅限于单变量时间序列分析，尚未扩展到多元（VARIMA）或时空模型。
*   **数据编码瓶颈**：振幅编码（Amplitude Encoding）虽然高效，但在真实硬件上准备这些量子态仍是一个挑战。

（完）
