---
title: "PRAGMA: Revolut Foundation Model"
title_zh: PRAGMA：Revolut 基础模型
authors: "Maxim Ostroukhov, Ruslan Mikhailov, Vladimir Iashin, Artem Sokolov, Andrei Akshonov, Vitaly Protasov, Dmitrii Beloborodov, Vince Mullin, Roman Yokunda Enzmann, Georgios Kolovos, Jason Renders, Pavel Nesterov, Anton Repushko"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08649v1"
tags: ["query:ts-fm"]
score: 8.0
evidence: 银行事件序列的基础模型
tldr: 本研究针对现代金融系统中海量的交易和事件数据，提出了PRAGMA系列银行事件序列基础模型。该模型采用Transformer架构，在异构银行事件语料库上通过自监督掩码建模进行预训练。PRAGMA能够从原始事件序列中提取通用表示，在信用评分、欺诈检测和生命周期价值预测等多种下游任务中表现优异，仅需简单线性模型或轻量化微调即可实现卓越性能，为金融应用提供了通用的表示层。
selection_source: fresh_fetch
motivation: 现代金融系统产生的大规模异构交易数据蕴含丰富经济信号，亟需一种通用的基础模型来有效提取和利用这些信息。
method: 采用基于Transformer的架构，在异构银行事件序列上利用针对离散变长记录设计的自监督掩码建模目标进行预训练。
result: 在信用评分、欺诈检测和生命周期价值预测等多个领域，PRAGMA通过提取嵌入或轻量微调均展现出超越现有方法的性能。
conclusion: PRAGMA证明了基础模型在处理原始金融事件序列方面的强大能力，为金融领域提供了一个高效、通用的表示学习框架。
---

## 摘要
现代金融系统产生海量的交易和事件级数据，这些数据编码了丰富的经济信号。本文提出了 PRAGMA，这是一个针对多源银行事件序列的基础模型系列。我们的方法利用针对金融记录离散、变长特性定制的自监督目标，在海量异构银行事件语料库上对基于 Transformer 的架构进行掩码建模预训练。所得模型支持信用评分、欺诈检测和终身价值预测等广泛的下游任务：仅需在提取的嵌入表示之上训练简单的线性模型即可获得强劲性能，通过轻量级微调可进一步提升效果。通过对下游任务的广泛评估，我们证明了 PRAGMA 直接从原始事件序列中学习，在多个领域均取得了优异性能，为金融应用提供了一个通用的表示层。

## Abstract
Modern financial systems generate vast quantities of transactional and event-level data that encode rich economic signals. This paper presents PRAGMA, a family of foundation models for multi-source banking event sequences. Our approach pre-trains a Transformer-based architecture with masked modelling on a large-scale, heterogeneous banking event corpus using a self-supervised objective tailored to the discrete, variable-length nature of financial records. The resulting model supports a wide range of downstream tasks such as credit scoring, fraud detection, and lifetime value prediction: strong performance can be achieved by training a simple linear model on top of the extracted embeddings and can be further improved with lightweight fine-tuning. Through extensive evaluation on downstream tasks, we demonstrate that PRAGMA achieves superior performance across multiple domains directly from raw event sequences, providing a general-purpose representation layer for financial applications.

---

## 论文详细总结（自动生成）

这是一份关于论文《PRAGMA: Revolut Foundation Model》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代银行和金融科技公司积累了海量的异构数据（交易、APP导航、客服沟通等），但这些数据具有离散、变长、时间不规律且包含数值/文本混合的特点。
*   **研究动机**：传统的自然语言处理（NLP）模型在处理此类结构化金融记录时效率低下（文本序列化导致长度爆炸，且丢失数值的大小和顺序信息）。目前缺乏一个能够整合多源数据、包含静态画像状态，并能跨多种金融预测任务（如信用评分、反欺诈）通用的编码器骨干模型。
*   **整体含义**：PRAGMA 是 Revolut 开发的一系列针对银行事件序列的基础模型（10M 到 1B 参数），旨在通过大规模自监督预训练，为金融领域提供一个通用的表示层。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：采用“键-值-时间”（Key-Value-Time）分词方案，结合三阶段 Transformer 编码器架构，通过掩码建模（MLM）学习用户历史的深度表示。
*   **关键技术细节**：
    *   **分词方案**：将每个事件分解为语义类型（Key）、值（Value）和时间坐标。数值型通过百分位数分桶，类别型映射为单 token，长文本使用 BPE 分词。
    *   **时间编码**：采用双重编码，一是到上一个事件的对数秒数（通过 RoPE 注入），二是周期性特征（小时、周几、月几）。
    *   **架构设计**：
        1.  **画像状态编码器**：处理静态属性（如计划类型、地区）。
        2.  **事件编码器**：独立处理每个事件的内部 token。
        3.  **历史编码器**：融合画像和事件序列，生成最终的用户级表示。
    *   **预训练目标**：混合掩码策略，包括 token 级掩码、整个事件级掩码和语义类型（Key）级掩码。
    *   **下游适配**：支持冻结骨干的“线性探测”（Embedding Probe）和轻量级的 LoRA 微调。

### 3. 实验设计
*   **数据集**：来自 2600 万匿名用户记录，涵盖 111 个国家，包含 240 亿个事件（共 2070 亿个 token）。
*   **下游任务（Benchmark）**：
    1.  **信用评分**：预测 12 个月内的违约概率。
    2.  **欺诈检测**：识别外部欺诈行为。
    3.  **生命周期价值（LTV）**：预测用户产生利润的概率。
    4.  **产品推荐**：预测用户对特定金融产品的采用倾向。
    5.  **循环交易检测**：识别订阅类重复扣款。
    6.  **沟通参与度**：预测用户是否会重新开启中断的贷款申请。
*   **对比方法**：主要对比各业务线现有的**特定任务生产模型**（Task-specific models）以及从头开始训练（Train from scratch）的模型。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **PRAGMA-S (10M)** 和 **PRAGMA-M (100M)**：使用 16 张 NVIDIA H100 GPU。
    *   **PRAGMA-L (1B)**：使用 32 张 NVIDIA H100 GPU。
*   **训练时长**：
    *   10M 版本约 2 天收敛。
    *   100M 和 1B 版本各需约 2 周时间。
*   **优化技术**：使用了混合精度（bf16）、Muon 优化器结合 AdamW，以及针对变长序列的序列打包（Sequence Packing）和动态批处理技术，使吞吐量提升了 2-5 倍。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个完全不同的金融领域任务上进行了验证，涵盖了分类、多标签预测和增量（Uplift）建模。
*   **消融实验**：
    *   **模型规模**：对比了 10M、100M、1B 的性能差异。
    *   **适配方式**：对比了线性探测、LoRA 微调与从头训练的效果。
    *   **组件贡献**：验证了画像状态编码器（Profile State）和预训练文本编码器（如 Nemotron）的加入对结果的影响。
*   **客观性**：实验覆盖了长尾数据和分布偏移问题，但由于商业敏感性，论文仅报告了**相对改进百分比**，未公开绝对指标。

### 6. 主要结论与发现
*   **通用性强**：单一预训练骨干模型在几乎所有任务上都优于专门定制的特定任务模型。
*   **微调优势**：LoRA 微调（仅更新 2-4% 参数）的效果一致优于从头开始的全参数训练。
*   **规模效应**：模型性能随参数量增加而提升，尤其在信用评分等复杂任务上增益显著；但在 LTV 等任务上，小模型已足够具有竞争力。
*   **画像状态的重要性**：对于信用评分等静态特征敏感的任务，专门的画像编码器能带来显著提升（PR-AUC 提升 31.8%）。

### 7. 优点
*   **高效的异构处理**：通过 Key-Value-Time 方案优雅地解决了金融数据中数值、类别和文本并存的问题。
*   **工程优化出色**：引入序列打包和动态批处理，极大地解决了金融长序列数据在 Transformer 中的计算浪费问题。
*   **多任务验证**：证明了金融领域也存在类似 NLP 的“基础模型”范式，减少了各业务线重复造轮子的成本。

### 8. 不足与局限
*   **关系建模缺失**：在反洗钱（AML）任务中表现不佳（下降 47.1%），因为 PRAGMA 仅处理单个用户的历史，无法捕捉洗钱所需的**跨用户/网络关系**信号。
*   **商业敏感性限制**：未公开绝对性能指标和具体特征名称，外部研究者难以完全复现其在真实业务中的绝对强度。
*   **文本编码器开销**：引入预训练文本编码器（如 Nemotron）虽然在文本密集型任务上有提升，但会增加 18% 的训练延迟，并非在所有场景下都划算。

（完）
