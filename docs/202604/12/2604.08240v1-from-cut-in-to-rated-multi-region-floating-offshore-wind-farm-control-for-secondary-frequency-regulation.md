---
title: "From Cut-In to Rated: Multi-Region Floating Offshore Wind Farm Control for Secondary Frequency Regulation"
title_zh: 从切入到额定风速：用于二次调频的多区域漂浮式海上风电场控制
authors: "Stephen Ampleman, Dennice F. Gayme"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08240v1"
tags: ["query:llm-wd"]
score: 8.0
evidence: 海上风电场控制与功率跟踪
tldr: 本研究针对漂浮式海上风电场，提出了一种跨越切入到额定风速的多区域控制框架，旨在参与电力市场的二次调频。研究设计了涵盖转矩控制、桨距角控制及平滑过渡的底层策略，并对比了非线性模型预测控制（NL-MPC）与线性参数变化时延模型预测控制（LPVTD-MPC）在场群功率跟踪中的表现。该框架有效解决了波浪引起的平台俯仰导致的风速大幅波动问题，在满足PJM调频标准的同时平衡了计算效率与控制精度。
selection_source: fresh_fetch
motivation: 针对漂浮式风机因平台俯仰导致的风速剧烈波动，开发能跨越多个运行区域并满足电力市场调频需求的风电场协同控制策略。
method: 结合转矩与桨距角控制实现多区域运行，并对比了基于动态尾流模型的NL-MPC和基于线性参数变化及时间延迟模型的LPVTD-MPC两种场群功率跟踪方法。
result: "两种方法在PJM调频认证测试中均获得89.9%以上的高分，远超75%的合格线，其中LPVTD-MPC计算速度比NL-MPC快一倍以上。"
conclusion: 该多区域控制框架成功实现了漂浮式风电场在复杂环境下的高精度功率跟踪，证明了简化模型在实时调频控制中的可行性与优势。
---

## 摘要
本文描述了一种用于漂浮式海上风电场的多区域控制框架。具体而言，我们提出了一种新型发电机转矩控制器，用于调节区域 2（对应于切入风速与额定风速之间的风速）的转子转速。在区域 3（风速等于或高于额定风速但低于切出风速）中，我们采用 PI-LQR 进行统一桨距角控制。跨越过渡风速（区域 2.5）的控制融合采用了应用于控制变量的 S 型（sigmoid）权重函数。针对具有转子转速正则化的电场级功率跟踪，本文提出了两种建模范式：一种是带有动态尾流模型的非线性模型预测控制器（NL-MPC），另一种是基于线性参数变化风机模型且具有尾流平流时滞表示的降阶模型预测控制器（LPVTD-MPC）。这些方法在三种入流风况下，根据参与二次调频市场的 PJM 辅助服务认证标准进行了评估。结果表明，在三种不同的测试场景下，两种方法的得分均至少达到 89.9%，远高于 75% 的合格阈值。然而，与 NL-MPC 相比，LPVTD-MPC 方法解决问题的时间不到一半，但电场级功率输出的波动略大，突显了性能与计算可行性之间的权衡。该控制框架是首批针对漂浮式海上风电场同时处理多区域风电机组动力学与市场驱动功率跟踪目标的框架之一。在漂浮式风机场景中，由于波浪引起的平台俯仰经常导致剧烈的（跨区域）风速变化，这种多区域控制变得越来越必要。

## Abstract
This paper describes a multi-region control framework for floating offshore wind farms. Specifically, we propose a novel generator torque controller that regulates rotor speed in Region 2, corresponding to wind speeds between the cut-in and rated values. In Region 3 (wind speeds at or above rated but below cut-out speed) we employ a PI-LQR for collective blade pitch. Control blending across the transitional wind speeds (Region 2.5) employs a sigmoid weighting function applied to the control variables. Two modeling paradigms are proposed for farm-level power tracking with rotor speed regularization: a nonlinear model predictive controller (NL-MPC) with a dynamic wake model, and a reduced order model predictive controller based on linear parameter varying turbine models with a time delay representation of wake advection (LPVTD-MPC). These approaches are evaluated over three wind inlet conditions using the PJM ancillary service certification criteria for participation in a secondary frequency regulation market. Results show that both approaches achieve scores of at least 89.9\% for the three different testing scenarios, which are well above the qualification threshold of 75\%. However, the LPVTD-MPC approach solves the problem in under half the time versus NL-MPC but with slightly larger fluctuations in farm-level power output, highlighting the trade-off between performance and computational tractability. The control framework is among the first to address multi-region wind turbine dynamics together with market driven power tracking objectives for floating offshore wind farms. Such multi-region control becomes increasingly necessary in the floating turbine setting where large (region spanning) wind speed variations are common due to wave induced platform pitching.

---

## 论文详细总结（自动生成）

这篇论文针对漂浮式海上风电场在复杂环境下的功率控制问题，提出了一种跨越多个运行区域的协同控制框架。以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：漂浮式海上风机（FOWT）由于波浪引起的平台俯仰运动，会导致风机感受到的有效风速剧烈波动，经常在切入风速（Region 2）和额定风速（Region 3）之间频繁切换。现有的风电场功率跟踪研究多集中在单一区域，缺乏能处理这种跨区域动力学并满足电力市场二次调频（Secondary Frequency Regulation）需求的统一控制方案。
*   **研究背景**：为了参与如 PJM 市场的调频服务，风电场必须能够精确跟踪电网调度指令。这要求控制器不仅要考虑单机动力学，还要协调场内风机间的尾流（Wake）相互作用。

### 2. 论文提出的方法论
论文构建了一个双层控制架构（底层风机控制 + 高层电场控制）：

*   **底层多区域风机控制**：
    *   **区域 2（低风速）**：提出一种新型**发电机转矩控制器**。不同于传统的最大功率跟踪（MPPT），该控制器基于反馈线性化设计，直接调节转子转速以配合电场级的功率分配指令。
    *   **区域 3（高风速）**：采用 **PI-LQR 控制器**调节桨距角，在维持额定功率的同时抑制平台俯仰带来的负阻尼效应。
    *   **区域 2.5（过渡区）**：使用 **S 型（Sigmoid）权重函数**对转矩和桨距角控制变量进行平滑融合，确保控制指令的连续性。
*   **高层电场功率跟踪控制（两种 MPC 范式）**：
    *   **NL-MPC（非线性模型预测控制）**：结合非线性风机模型和基于偏微分方程的动态尾流平流扩散模型，精度高但计算量大。
    *   **LPVTD-MPC（线性参数变化时延 MPC）**：采用降阶的 LPV 风机模型，并利用时间延迟（Time Delay）简化尾流传播过程，旨在提高计算效率。

### 3. 实验设计
*   **场景设置**：模拟了一个由 8 台风机（4x2 排布）组成的漂浮式风电场。
*   **数据集/入流风况**：使用了来自大涡模拟（LES）的三个不同风速剖面（场景 A、B、C），模拟真实的湍流环境。
*   **Benchmark（基准标准）**：采用美国 **PJM 调频市场 RegD 认证标准**。
*   **对比指标**：
    *   **精度得分（Precision）**：跟踪误差的准确度。
    *   **延迟得分（Delay）**：响应调度信号的时效性。
    *   **相关性得分（Correlation）**：输出功率曲线与指令曲线的拟合度。
    *   **综合得分**：合格阈值为 75%。

### 4. 资源与算力
*   **硬件设施**：在一台标准的 **Dell XPS 15 笔记本电脑**上运行。
*   **计算配置**：利用 Matlab 的 `fmincon` 函数求解优化问题，并开启并行处理（使用 **14 个核心**）进行梯度计算。
*   **耗时情况**：
    *   **NL-MPC**：平均求解时间为 945 秒。
    *   **LPVTD-MPC**：平均求解时间为 465 秒（比前者快约 51%）。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 种不同的风速场景和 3 组 40 分钟长的 PJM 实际调频信号进行了仿真测试。
*   **充分性评价**：实验设计较为充分。作者不仅对比了两种不同复杂度的模型，还采用了工业界公认的 PJM 认证算法进行评分，结果具有较强的客观性和说服力。不过，实验主要集中在仿真层面，尚未涉及物理实验验证。

### 6. 论文的主要结论与发现
*   **性能达标**：两种 MPC 方法在所有场景下的综合得分均超过 **89.9%**，远高于 PJM 市场 75% 的准入要求。
*   **效率与精度的权衡**：LPVTD-MPC 虽然在相关性得分上略低于 NL-MPC（功率波动稍大），但其计算速度提升了一倍以上，更接近实时控制的要求。
*   **多区域控制的必要性**：证明了在漂浮式场景下，通过转子转速正则化和多区域平滑切换，可以有效实现高精度的功率跟踪。

### 7. 优点（亮点）
*   **协同设计**：将底层风机控制律与高层电场优化算法深度耦合，特别是针对功率跟踪设计的区域 2 转矩控制具有创新性。
*   **平滑过渡**：引入 Sigmoid 函数解决了风机在不同运行工况切换时的抖动问题。
*   **工业相关性**：直接使用电力市场准入标准作为评价指标，具有很强的工程应用参考价值。

### 8. 不足与局限
*   **理想化假设**：假设传感器反馈是完美的（无噪声、无延迟），且风速预测信息完全准确，这在实际海况中难以实现。
*   **实时性挑战**：尽管 LPVTD-MPC 显著提速，但 465 秒的求解时间对于秒级的调频响应来说仍然偏慢，需进一步优化算法（如采用伴随法梯度计算）。
*   **疲劳载荷未评估**：频繁的转矩和桨距角调节可能会增加风机结构的疲劳载荷，论文未对此进行量化分析。

（完）
