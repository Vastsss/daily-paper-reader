---
title: Prediction of Magnetic Flux Evolution During Solar Active Region Emergence using Long Short-Term Memory Networks
title_zh: 利用长短期记忆网络预测太阳活动区浮现过程中的磁通量演化
authors: "Eren Dogan, Spiridon Kasapis, Sarang Patil, Jonas Tirona, John Stefan, Irina Kitiashvili, Mengjia Xu, Alexander Kosovichev"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03507v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 使用LSTM和编码器-解码器模型进行一维时间序列预测
tldr: 太阳活动区是空间天气的核心驱动力，本研究利用53个活动区的连续谱强度和太阳振荡功率图数据，开发了MagFluxLSTM和MagFluxEnc-Dec模型来预测磁通量演化。实验表明，结构较简单的MagFluxLSTM在泛化能力和稳定性上显著优于复杂的编码器-解码器模型，能在12小时预测窗口内提前3-10小时预报磁通量浮现，为空间天气预警提供了有效工具。
selection_source: fresh_fetch
motivation: 为了提高空间天气预报的准确性，需要开发能够提前预测太阳活动区磁通量浮现演化的机器学习模型。
method: 基于连续谱强度和太阳振荡功率图的1D时间序列，对比研究了标准LSTM网络与LSTM编码器-解码器架构的预测效果。
result: "MagFluxLSTM在超参数优选配置中占比达98%，其泛化性能和训练稳定性均优于复杂的MagFluxEnc-Dec模型。"
conclusion: MagFluxLSTM模型能够提前3-10小时预测磁通量的浮现，证明了利用日震学数据进行太阳活动区早期预报的可行性。
---

## 摘要
太阳活动区（ARs）是空间天气事件的主要驱动源，因此对其进行早期预测对于业务化预报系统至关重要。我们开发了能够预测活动区浮现过程中磁通量演化的机器学习模型，该模型利用了53个活动区及其周围宁静区的连续谱强度和太阳振荡功率图的1D时间序列。每种观测物理量均在固定的30.66°x30.66°视场内进行采样。这些观测数据捕捉了每个活动区的时间演化过程，并作为训练和验证 MagFluxLSTM 及 MagFluxEnc-Dec 模型的输入。MagFluxLSTM 架构实现了一个单阶段的标准长短期记忆（LSTM）网络。MagFluxEnc-Dec 则代表了一个带有教师强制（teacher forcing）的 LSTM 编码器-解码器。为了测试和评估模型性能，我们使用连续谱强度和振荡功率图（由多普勒速度计算出的多个频段）作为输入来预测磁通量。在按验证集导数均方根误差（RMSE）排名的前100个超参数配置中，98% 对应于 MagFluxLSTM，而 MagFluxEnc-Dec 仅占 2%。因此，尽管 MagFluxEnc-Dec 架构具有更高的模型复杂度，但与更简单的 MagFluxLSTM 相比，它对训练集之外活动区的泛化能力较差，且训练稳定性较低。MagFluxLSTM 在实验和业务化设置下，针对5个测试活动区，能够在12小时的预测窗口内提前3-10小时预测磁通量的浮现。

## Abstract
Solar active regions (ARs) are the primary drivers of space weather events, making their early prediction crucial for operational forecasting systems. We develop machine learning models capable of predicting the evolution of magnetic flux during AR emergence using 1D time series of the continuum intensity and solar oscillation power maps for 53 active regions and their surrounding quiet-Sun areas. Each observable is sampled over a fixed 30.66°x30.66° field of view. These observations capture the temporal evolution of each active region and serve as inputs for training and validation of our MagFluxLSTM and MagFluxEnc-Dec models. The MagFluxLSTM architecture implements a single-stage standard Long-Short Term Memory (LSTM) network. MagFluxEnc-Dec represents an LSTM encoder-decoder with teacher forcing. To test and evaluate the models' performance, we use the continuum intensity and oscillation power maps (calculated for several frequency bands from Doppler velocity) as input to predict the magnetic flux. Among the top 100 hyperparameter configurations ranked by validation derivative RMSE, 98% correspond to MagFluxLSTM, compared to only 2% for MagFluxEnc-Dec. Thus, although the MagFluxEnc-Dec architecture has higher model complexity, it leads to poorer generalization to ARs outside the training set and less stable training than the simpler MagFluxLSTM, which can predict magnetic flux emergence 3-10 hours in advance within a 12-hour prediction window in both experimental and operational-type settings for the 5 testing active regions.

---

## 论文详细总结（自动生成）

这篇论文探讨了利用深度学习技术提前预测太阳活动区（Active Regions, ARs）磁通量浮现演化的方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：太阳活动区是耀斑、日冕物质抛射等空间天气事件的源头。传统的观测手段往往在磁场已经浮现到光球层后才能发现，而日震学研究表明，地表下的扰动（如声学功率变化）在磁场可见前就已存在。
*   **核心问题**：如何利用连续谱强度和日震声学功率图（Acoustic Power Maps）的时间序列数据，构建机器学习模型来提前预测磁通量的浮现时间及演化过程，从而为空间天气预报提供更长的预警提前量。

### 2. 方法论
*   **核心思想**：将太阳表面的观测数据转化为1D时间序列，利用长短期记忆网络（LSTM）捕捉时间依赖性，预测未来12小时的磁通量变化。
*   **关键技术细节**：
    *   **输入特征**：包括连续谱强度（$I_c$）和四个频率段（2-6 mHz）的声学功率（$P_a$）。
    *   **模型架构**：对比了两种架构：
        1.  **MagFluxLSTM**：多层堆叠的标准LSTM，采用单次前向传播输出未来12小时的预测值。
        2.  **MagFluxEnc-Dec**：编码器-解码器架构，使用“教师强制”（Teacher Forcing）技术进行自回归预测。
    *   **混合损失函数（Hybrid Loss）**：这是本文的关键创新。公式为 $L_{hybrid} = \alpha L_{mse} + (1 - \alpha) L_{derivative}$。除了惩罚数值误差（MSE），还专门惩罚时间导数（梯度）的差异，以更准确地捕捉磁通量开始快速增长（即浮现）的瞬间。
    *   **浮现准则**：定义为磁通量时间导数超过0.01且持续4小时以上。

### 3. 实验设计
*   **数据集**：使用 SolARED 数据集，包含2010年至2023年间 SDO/HMI 观测到的53个活动区。
*   **数据划分**：41个用于训练（77%），7个用于验证（13%），5个用于独立测试（10%）。
*   **Benchmark与对比**：
    *   对比了 MagFluxLSTM 与 MagFluxEnc-Dec 两种架构。
    *   对比了使用混合损失函数与仅使用 MSE 损失的效果。
    *   通过 Ray Tune 和 Hyperopt 进行了大规模超参数搜索（ASHA 调度器）。

### 4. 资源与算力
*   **硬件环境**：实验在配备 NVIDIA GPU 的服务器上运行，利用 CUDA 加速。
*   **软件框架**：PyTorch 2.7.0，Ray Tune 用于分布式超参数优化。
*   **具体算力**：论文附录代码显示使用了 32 个 CPU 核心和 1 个 GPU 进行超参数搜索。具体的训练时长未明确说明，但提到了每个试验（Trial）最多运行 100 个 Epoch，并设有早停机制。

### 5. 实验数量与充分性
*   **实验规模**：进行了广泛的超参数优化，对前100个最佳配置进行了统计分析。
*   **充分性评价**：
    *   **优点**：超参数搜索非常系统，涵盖了学习率、隐藏层大小、层数、Dropout、损失函数权重等。
    *   **局限**：样本量（53个活动区）对于深度学习而言相对较小，这限制了模型复杂度的发挥。测试集仅包含5个活动区，虽然在日震学研究中属于标准做法，但从统计学角度看，泛化性的验证仍有提升空间。

### 6. 主要结论与发现
*   **模型选择**：简单的 **MagFluxLSTM** 显著优于复杂的编码器-解码器模型。在前100个最优配置中，98%属于 MagFluxLSTM。
*   **预测能力**：MagFluxLSTM 配合混合损失函数，能在磁通量实际浮现前 **3-10 小时**发出预警。
*   **损失函数作用**：引入导数损失（Derivative Loss）能显著提高模型对浮现起始时间的敏感度。
*   **泛化性**：复杂模型（Enc-Dec）在小样本太阳数据上容易过拟合，且训练稳定性较差。

### 7. 优点（亮点）
*   **物理启发式损失**：通过混合损失函数将“浮现即变化”的物理直觉引入模型优化。
*   **端到端流程**：从日震学原始数据到业务化预报的完整流水线，考虑了实际业务中的数据处理延迟（4小时延迟模拟）。
*   **系统性优化**：使用了先进的超参数自动调优工具，保证了模型对比的公平性。

### 8. 不足与局限
*   **空间信息缺失**：模型将 9x9 的网格切分为独立的 1D 序列，忽略了活动区内部不同区域之间的空间相关性。
*   **数据量限制**：53个活动区的数据量可能不足以支撑更深层的架构（如 Transformer 或更复杂的 RNN）。
*   **误报风险**：在部分测试案例中，模型在没有实际浮现的区域产生了“错误局部活动预测”（ILAP），即存在一定的误报率。
*   **输入单一**：目前仅使用了连续谱和声学功率，未整合矢量磁场等其他可能增强预测能力的物理量。

（完）
