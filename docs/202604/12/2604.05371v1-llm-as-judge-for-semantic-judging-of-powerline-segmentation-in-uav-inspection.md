---
title: LLM-as-Judge for Semantic Judging of Powerline Segmentation in UAV Inspection
title_zh: 无人机巡检中输电线分割语义评判的 LLM-as-Judge 研究
authors: "Akram Hossain, Rabab Abdelfattah, Xiaofeng Wang, Kareem Abdelfatah"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05371v1"
tags: ["query:llm"]
score: 9.0
evidence: 大语言模型用于电力系统中的输电线路巡检
tldr: 本研究探讨了利用大语言模型（LLM）作为语义评判员，评估无人机电力线巡检中分割模型可靠性的可行性。针对轻量化分割模型在复杂环境下性能下降的问题，研究设计了评估LLM重复性和感知敏感性的协议。实验表明，LLM在相同条件下表现出高度的一致性，并能敏锐捕捉视觉质量下降及分割错误，证明了其作为安全关键任务监控工具的潜力。
selection_source: fresh_fetch
motivation: 解决无人机载轻量化分割模型在真实复杂环境下性能不可预测且难以实时监控其可靠性的安全挑战。
method: 提出一种“看门狗”机制，通过设计重复性实验和引入视觉损伤（如雨雪、雾等）的敏感性实验，评估LLM对分割结果的评判能力。
result: LLM在重复查询中表现出高度的一致性，并能随图像质量下降合理降低置信度，准确识别电力线缺失或误判。
conclusion: 经过合理约束的LLM可以作为可靠的语义评判员，用于监控安全关键型航空巡检任务中的分割质量。
---

## 摘要
在无人机上部署轻量级分割模型进行自主输电线巡检面临一个关键挑战：在与训练数据不同的真实世界条件下保持可靠的性能。尽管 U-Net 等紧凑型架构能够实现实时机载推理，但其分割输出在恶劣环境下可能会发生不可预测的退化，从而引发安全担忧。在这项工作中，我们研究了使用大语言模型（LLM）作为语义评判者来评估无人机载模型生成的输电线分割结果可靠性的可行性。我们并非引入一套新的巡检系统，而是形式化了一个“看门狗”（watchdog）场景，其中机外 LLM 评估分割覆盖图，并检查该评判者在行为一致性和感知连贯性方面是否值得信赖。为此，我们设计了两种评估协议来分析评判者的重复性和敏感性。首先，我们通过使用相同的输入和固定的提示词重复查询 LLM 来评估重复性，测量其质量评分和置信度估计的稳定性。其次，我们通过引入受控的视觉损坏（雾、雨、雪、阴影和太阳耀斑）并分析评判者的输出如何响应分割质量的逐步退化，来评估感知敏感性。我们的结果表明，LLM 在相同条件下产生高度一致的类别判断，同时随着视觉可靠性的下降表现出适当的置信度降低。此外，即使在具有挑战性的条件下，该评判者对诸如输电线缺失或误识别等感知线索仍保持响应。这些发现表明，在经过仔细约束的情况下，LLM 可以作为可靠的语义评判者，用于监控安全关键型航空巡检任务中的分割质量。

## Abstract
The deployment of lightweight segmentation models on drones for autonomous power line inspection presents a critical challenge: maintaining reliable performance under real-world conditions that differ from training data. Although compact architectures such as U-Net enable real-time onboard inference, their segmentation outputs can degrade unpredictably in adverse environments, raising safety concerns. In this work, we study the feasibility of using a large language model (LLM) as a semantic judge to assess the reliability of power line segmentation results produced by drone-mounted models. Rather than introducing a new inspection system, we formalize a watchdog scenario in which an offboard LLM evaluates segmentation overlays and examine whether such a judge can be trusted to behave consistently and perceptually coherently. To this end, we design two evaluation protocols that analyze the judge's repeatability and sensitivity. First, we assess repeatability by repeatedly querying the LLM with identical inputs and fixed prompts, measuring the stability of its quality scores and confidence estimates. Second, we evaluate perceptual sensitivity by introducing controlled visual corruptions (fog, rain, snow, shadow, and sunflare) and analyzing how the judge's outputs respond to progressive degradation in segmentation quality. Our results show that the LLM produces highly consistent categorical judgments under identical conditions while exhibiting appropriate declines in confidence as visual reliability deteriorates. Moreover, the judge remains responsive to perceptual cues such as missing or misidentified power lines, even under challenging conditions. These findings suggest that, when carefully constrained, an LLM can serve as a reliable semantic judge for monitoring segmentation quality in safety-critical aerial inspection tasks.

---

## 论文详细总结（自动生成）

### 论文总结：无人机巡检中输电线分割语义评判的 LLM-as-Judge 研究

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是无人机（UAV）自主巡检电力线时的**安全性与可靠性**问题。虽然轻量化深度学习模型（如 U-Net）在基准测试中表现良好，但在实际部署中，面对恶劣天气（雨、雪、雾）、光照变化或传感器噪声时，分割质量会发生“静默退化”。由于现场缺乏真值（Ground Truth），系统无法实时感知分割是否失效。
**核心动机**：引入多模态大语言模型（MLLM）作为“语义评判员”（LLM-as-Judge），在无真值的情况下，通过评估分割掩码与原始图像的叠加效果，充当“看门狗”角色，监控视觉感知的可靠性。

#### 2. 方法论
论文提出了一种**语义安全监控框架**，将感知与评判分离：
*   **核心思想**：无人机机载运行轻量化分割模型，将生成的分割掩码与图像叠加后传回地面站，由高性能 MLLM 进行语义推理，判断分割结果的逻辑一致性和结构完整性。
*   **关键技术细节**：
    *   **评判函数**：定义为 $f_\theta(x_i, p) = (s_i, c_i, e_i)$，其中 $x_i$ 是叠加图，$p$ 是固定提示词。输出包括：离散质量评分 $s_i$（1-5分）、连续置信度 $c_i$（0-1）和文本解释 $e_i$。
    *   **重复性评估协议**：通过多次查询相同输入，利用评分一致性（$A_s$）、置信度一致性（$A_c$）和组内相关系数（ICC）来衡量 LLM 输出的稳定性。
    *   **敏感性评估协议**：通过引入受控的视觉损伤，观察评判结果是否随图像质量下降而产生相干的变化（如评分下降、置信度降低）。

#### 3. 实验设计
*   **数据集**：使用 **TTPLA 数据集**（电力线巡检公开基准），包含约 1100 张高空电力线图像。
*   **场景构建**：利用 Albumentations 库生成**挑战集**，模拟 5 种视觉损伤：雾（Fog）、雨（Rain）、雪（Snow）、阴影（Shadow）和太阳耀斑（Sunflare），每种损伤分为 3 个严重等级。
*   **对比与基准**：
    *   **分割模型**：在 TTPLA 训练集上训练 25 个 epoch 的 U-Net。
    *   **评判模型**：使用 **GPT-4o** 作为 LLM-as-Judge。
    *   **基准线**：以未受损的“干净”图像分割结果作为性能基准。

#### 4. 资源与算力
*   **模型训练**：文中提到 U-Net 训练了 25 个 epoch，但**未明确说明具体的 GPU 型号和数量**。
*   **LLM 推理**：通过 OpenAI API 访问 GPT-4o，属于机外（Offboard）计算，不占用无人机机载算力。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **重复性实验**：对每个样本进行 5 次独立重复运行，以评估随机解码的影响。
    *   **敏感性实验**：覆盖了 5 种天气/光照损伤 x 3 个严重等级，并进行了配对显著性检验。
*   **充分性评价**：实验设计较为充分且客观。研究不仅关注了平均分的变化，还引入了**效应量（Effect Size, $d_z$）**和 **Spearman 秩相关系数**来验证响应的单调性和统计学意义，确保了结论的科学性。

#### 6. 主要结论与发现
*   **高稳定性**：在相同输入下，LLM 的离散评分一致性高达 78%-91%，ICC 系数处于高位（0.85-0.91），证明其作为评判员具有极高的可靠性。
*   **感知敏感**：LLM 能敏锐捕捉到分割失效。在“雾”环境下，评分和置信度出现剧烈且稳定的下降；在“雨”和“雪”环境下，评分随严重程度呈单调下降趋势。
*   **保守的置信度**：当视觉线索模糊时，LLM 会主动降低置信度，表现出一种“谨慎”的评估行为，这对于安全关键型任务至关重要。

#### 7. 优点
*   **创新视角**：首次将 LLM-as-Judge 应用于细长物体（电力线）分割的在线可靠性监控，解决了无真值环境下的评估难题。
*   **严谨的评估框架**：通过“重复性”和“敏感性”两个维度量化 LLM 的行为，而非仅仅依赖主观观察。
*   **架构合理**：机载感知与地面评判的分离架构，既保证了实时性，又利用了强大的云端推理能力。

#### 8. 不足与局限
*   **模型单一性**：实验仅使用了 GPT-4o，未对比其他多模态模型（如 Claude 3.5 或开源的 LLaVA），可能存在模型特有的偏差。
*   **API 依赖与延迟**：依赖闭源 API 存在隐私风险和网络延迟问题，可能限制其在极端实时干预场景下的应用。
*   **合成损伤偏差**：实验使用的视觉损伤是合成的，虽然具有代表性，但与真实物理环境下的复杂光学退化可能存在差异。
*   **成本问题**：大规模巡检中频繁调用 LLM API 会产生较高的运营成本。

（完）
