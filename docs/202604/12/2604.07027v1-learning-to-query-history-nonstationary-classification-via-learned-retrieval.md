---
title: "Learning to Query History: Nonstationary Classification via Learned Retrieval"
title_zh: 学习查询历史：基于学习检索的非平稳分类
authors: "Jimmy Gammell, Bishal Thapaliya, Yoon Jung, Riyasat Ohib, Bilel Fehri, Deepayan Chakrabarti"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07027v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 将非平稳分类重新定义为时间序列预测
tldr: 针对实际分类任务中普遍存在的非平稳性（分布偏移）问题，本文将非平稳分类重新定义为时间序列预测任务。研究提出了一种基于学习检索的机制，使分类器能够根据当前输入动态查询并参考历史标注样本。通过端到端训练的离散检索器，模型能在大规模历史数据中提取相关信息，显著提升了对分布偏移的鲁棒性，且显存占用随历史序列增长保持可预测的扩展性。
selection_source: fresh_fetch
motivation: 传统分类模型在面对随时间变化的非平稳分布时表现不佳，难以应对部署后出现的分布偏移问题。
method: 将分类任务转化为以历史标注序列为条件的预测任务，并引入一种可端到端训练的离散检索机制来动态获取相关历史样本。
result: "在合成基准和 Amazon Reviews '23 数据集上的实验证明，该方法显著提升了对分布偏移的鲁棒性，且显存占用随历史数据量增长表现稳定。"
conclusion: 通过学习查询历史数据，模型能够有效适应动态变化的环境，为非平稳分类任务提供了一种高效且可扩展的检索增强解决方案。
---

## 摘要
非平稳性在实际分类场景中无处不在，导致已部署的模型即使在训练时的留出集上泛化良好，其表现往往也较差。我们通过将非平稳分类重新定义为时间序列预测来解决这一问题：分类器不再仅根据当前输入进行预测，而是以超出训练截止时间的历史标记样本序列为条件。为了扩展到大规模序列，我们引入了一种学习到的离散检索机制，该机制通过依赖于输入的查询来采样相关的历史样本，并使用基于分数的梯度估计器与分类器进行端到端训练。这使得完整的历史数据语料库在训练和部署期间可以保留在任意文件系统中。在合成基准测试和 Amazon Reviews '23（电子产品类别）上的实验表明，与标准分类器相比，该方法提高了对分布偏移的鲁棒性，且显存（VRAM）占用随历史数据序列长度的增加呈可预测的扩展。

## Abstract
Nonstationarity is ubiquitous in practical classification settings, leading deployed models to perform poorly even when they generalize well to holdout sets available at training time. We address this by reframing nonstationary classification as time series prediction: rather than predicting from the current input alone, we condition the classifier on a sequence of historical labeled examples that extends beyond the training cutoff. To scale to large sequences, we introduce a learned discrete retrieval mechanism that samples relevant historical examples via input-dependent queries, trained end-to-end with the classifier using a score-based gradient estimator. This enables the full corpus of historical data to remain on an arbitrary filesystem during training and deployment. Experiments on synthetic benchmarks and Amazon Reviews '23 (electronics category) show improved robustness to distribution shift compared to standard classifiers, with VRAM scaling predictably as the length of the historical data sequence increases.

---

## 论文详细总结（自动生成）

这篇论文由普渡大学、亚马逊和佐治亚理工学院的研究人员合作完成，发表于 ICLR 2026 的 TSALM 工作坊。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在实际应用（如欺诈检测、合规审查）中，数据分布往往随时间动态变化（非平稳性）。传统分类器在训练集上表现良好，但在部署后面对分布偏移（Distribution Shift）时性能会迅速下降。
*   **核心思想**：将非平稳分类任务重新定义为“时间序列预测”。模型不再仅仅依赖当前的输入 $X_t$ 进行预测，而是通过检索机制，从一个不断增长的、包含训练截止日期之后数据的历史标注语料库中提取相关样本作为上下文，从而实现无需重新训练的动态自适应。

### 2. 方法论
*   **核心架构**：系统由查询生成器（Query Generator）、冻结的键生成函数（Key-generating function）、离散检索机制和下游分类器组成。
*   **关键技术细节**：
    *   **键值存储**：每个历史样本 $(X, Y)$ 通过冻结函数 $\psi$ 生成低维键（Key），存储在显存中；原始数据（Value）存储在磁盘文件系统。
    *   **学习检索**：查询生成器根据当前输入 $X_t$ 生成 $\kappa$ 个查询向量，与历史键计算相似度得分（Logits），通过**无放回随机采样**选出最相关的历史样本。
    *   **端到端训练**：由于采样过程不可微，研究者采用了**基于分数的梯度估计器（REINFORCE）**，并结合贪婪基准（Greedy Baseline）来降低方差，实现检索器与分类器的联合优化。
    *   **内存优化**：通过硬采样（Hard Sampling）和注意力掩码，仅将选中的样本加载到显存，使系统能处理远超显存容量的大规模历史语料。

### 3. 实验设计
*   **数据集与场景**：
    1.  **旋转决策边界（Synthetic）**：二分类问题，最优决策边界随时间旋转 $\pi$ 弧度。
    2.  **大海捞针（Needle in Haystack）**：标签信息隐藏在特定的历史样本中，测试检索的准确性。
    3.  **Amazon Reviews '23 (Electronics)**：情感分类任务。通过人为改变不同年份的评分阈值（如2014年前评分>3为正向，后改为>1）来模拟剧烈的分布偏移。
*   **基准方法（Benchmark）**：不带上下文的标准分类器（w/o context）。
*   **对比维度**：分类准确率随时间的变化、显存（VRAM）占用扩展性、检索机制的学习动态。

### 4. 资源与算力
*   **算力说明**：文中未明确给出具体的 GPU 型号和数量。
*   **训练细节**：
    *   Amazon 数据集训练了 100 个 Epoch，Batch Size 为 256（扩展性测试中达到 1024）。
    *   使用了 AdamW 优化器，并对检索器设置了 10 倍于分类器的学习率。
    *   显存占用实验显示，当历史数据量（K）达到 100 万量级时，显存占用约为 10-50GB，具体取决于查询维度和 Batch Size。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单合成环境到包含 4390 万条评论的大规模真实数据集。
*   **充分性**：
    *   进行了**消融实验**，对比了“全特征检索”与“仅标签检索”的效果。
    *   进行了**扩展性实验**，验证了显存占用与超参数（K, B, κ, d）的线性关系。
    *   实验设计较为客观，通过“大海捞针”实验有力证明了检索器确实学到了逻辑，而非仅仅依赖分类器的泛化。

### 6. 主要结论与发现
*   **鲁棒性提升**：在分布偏移发生后，该方法能显著减缓准确率的下降，利用历史上下文纠正预测。
*   **联合训练有效性**：仅通过分类目标函数，模型就能学会如何查询历史数据。
*   **检索策略差异**：在 Amazon 实验中发现，**仅检索历史标签**比检索“特征+标签”效果更好，因为复杂的历史特征可能引入噪声，干扰分类器。
*   **可扩展性**：显存占用随历史序列长度呈线性（而非平方）增长，支持超大规模语料库。

### 7. 优点
*   **无需重训的适应性**：通过改变上下文而非更新权重来应对非平稳性，避免了灾难性遗忘。
*   **显存友好**：巧妙的离散检索设计使得大规模语料可以驻留在磁盘，降低了硬件门槛。
*   **端到端优化**：检索逻辑直接服务于分类任务，比预定义的相似度检索更具针对性。

### 8. 不足与局限
*   **训练稳定性**：REINFORCE 估计器方差大，需要复杂的调参技巧（如温度衰减、学习率差异化、Dropout 策略）才能收敛。
*   **冻结键的限制**：键生成函数是预训练且冻结的，如果该函数无法捕捉到随时间变化的特征本质，检索效果将受限。
*   **推理开销**：相比标准分类器，增加了检索和加载磁盘数据的延迟。
*   **分布偏移的局限**：虽然缓解了性能下降，但在极端的 OOD（分布外）情况下，性能依然会随时间缓慢衰减。

（完）
