---
title: Hierarchical Reinforcement Learning with Runtime Safety Shielding for Power Grid Operation
title_zh: 结合运行时安全屏蔽的分层强化学习在电网运行中的应用
authors: Gitesh Malik
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14032v1"
tags: ["query:llm"]
score: 8.0
evidence: 用于电网运行和智能电网管理的强化学习
tldr: 本研究针对电网运行中强化学习面临的安全约束严苛、鲁棒性差及泛化能力弱等挑战，提出了一种带运行时安全屏蔽的分层强化学习框架。该框架将长周期决策与实时可行性强制执行解耦，利用高层策略进行抽象决策，并通过确定性安全屏蔽器过滤不安全动作。实验证明，该方法在极端压力测试和跨拓扑零样本迁移中表现优异，显著提升了电网运行的安全性和鲁棒性。
selection_source: fresh_fetch
motivation: 传统强化学习在电网控制中难以满足严苛的物理安全约束，且在面对罕见扰动和未知拓扑时表现脆弱。
method: 提出一种分层控制框架，将高层强化学习决策与基于快速前向模拟的确定性运行时安全屏蔽器相结合，确保动作始终满足安全不变性。
result: 在Grid2Op基准测试和大规模电网零样本部署中，该方法比传统RL更鲁棒，比纯安全方法更高效，实现了更长的存活时间和更低的线路负载。
conclusion: 电网控制的安全性和泛化性应通过架构设计而非复杂的奖励工程来实现，为实际能源系统的学习型控制器提供了可行路径。
---

## 摘要
强化学习在自动化电网运行任务（如拓扑控制和阻塞管理）中展现出巨大潜力。然而，由于严格的安全要求、在罕见扰动下的脆弱性以及对未知电网拓扑的泛化能力差，其在实际电力系统中的部署仍然受限。在安全至上的基础设施中，灾难性故障是不可容忍的，基于学习的控制器必须在严格的物理约束下运行。本文提出了一种用于电网运行的安全约束分层控制框架，该框架将长时程决策与实时可行性执行显式解耦。高层强化学习策略提出抽象控制动作，而确定性运行时安全屏蔽则利用快速前向仿真过滤不安全动作。安全性作为运行时不变性被强制执行，独立于策略质量或训练分布。所提框架在 Grid2Op 基准测试套件上进行了评估，涵盖了常规工况、强制线路停运压力测试，以及在不进行重新训练的情况下在 ICAPS 2021 大规模输电网上进行的零样本部署。结果表明，扁平化强化学习策略在压力下表现脆弱，而仅考虑安全的方法则过于保守。相比之下，所提的分层且安全感知的方法实现了更长的回合生存时间、更低的峰值线路负载，以及对未知电网稳健的零样本泛化。这些结果表明，电网控制中的安全性和泛化性最好通过架构设计而非日益复杂的奖励工程来实现，这为在现实能源系统中部署基于学习的控制器提供了一条切实可行的路径。

## Abstract
Reinforcement learning has shown promise for automating power-grid operation tasks such as topology control and congestion management. However, its deployment in real-world power systems remains limited by strict safety requirements, brittleness under rare disturbances, and poor generalization to unseen grid topologies. In safety-critical infrastructure, catastrophic failures cannot be tolerated, and learning-based controllers must operate within hard physical constraints.   This paper proposes a safety-constrained hierarchical control framework for power-grid operation that explicitly decouples long-horizon decision-making from real-time feasibility enforcement. A high-level reinforcement learning policy proposes abstract control actions, while a deterministic runtime safety shield filters unsafe actions using fast forward simulation. Safety is enforced as a runtime invariant, independent of policy quality or training distribution.   The proposed framework is evaluated on the Grid2Op benchmark suite under nominal conditions, forced line-outage stress tests, and zero-shot deployment on the ICAPS 2021 large-scale transmission grid without retraining. Results show that flat reinforcement learning policies are brittle under stress, while safety-only methods are overly conservative. In contrast, the proposed hierarchical and safety-aware approach achieves longer episode survival, lower peak line loading, and robust zero-shot generalization to unseen grids.   These results indicate that safety and generalization in power-grid control are best achieved through architectural design rather than increasingly complex reward engineering, providing a practical path toward deployable learning-based controllers for real-world energy systems.

---

## 论文详细总结（自动生成）

### 论文结构化总结：结合运行时安全屏蔽的分层强化学习在电网运行中的应用

#### 1. 核心问题与整体含义（研究动机和背景）
电网作为关键基础设施，对运行安全性有极高要求。虽然强化学习（RL）在电网拓扑控制和阻塞管理中表现出潜力，但面临三大挑战：
*   **安全性缺失**：标准RL优化期望奖励，无法提供物理约束（如热限值、电压波动）的硬性保证。
*   **极端工况下的脆弱性**：在面对线路停运等罕见扰动时，传统RL策略容易崩溃。
*   **泛化能力差**：在小规模电网训练的策略难以直接应用到拓扑不同的真实大规模电网。
**核心动机**：通过架构设计（而非复杂的奖励工程）将策略决策与安全执行解耦，实现既能长期规划又能实时保障安全的电网控制器。

#### 2. 方法论：核心思想与关键技术
论文提出了一种**安全约束的分层控制框架**，核心思想是“高层决策，底层过滤”：
*   **高层策略（High-level Policy）**：采用近端策略优化（PPO）算法，负责学习抽象的、长时程的控制策略（如拓扑调整目标）。它不直接处理安全约束，而是专注于战略目标。
*   **运行时安全屏蔽（Runtime Safety Shield）**：这是一个确定性的物理过滤层。
    *   **前向仿真**：在动作执行前，利用快速仿真器预测该动作执行后的线路负载率 $\rho_\ell$。
    *   **动作过滤**：若预测负载超过阈值（如1.0），屏蔽器会否决（Veto）该动作，并从安全动作空间中选择一个偏差最小的替代动作（如NO-OP或预设的安全回退动作）。
*   **解耦机制**：安全性被视为运行时的“不变性”（Invariant），独立于策略的训练分布，从而允许策略在未见过的电网拓扑中依然能被安全屏蔽器“保驾护航”。

#### 3. 实验设计
*   **数据集/场景**：基于 **Grid2Op** 仿真框架，使用了三个场景：
    1.  **Case14 Sandbox**：基础训练与评估环境。
    2.  **Case14 Stress Mode**：注入强制线路停运的压力测试。
    3.  **ICAPS 2021 Large Grid**：包含118个总线的大规模输电网，用于**零样本（Zero-shot）迁移**测试。
*   **对比方法（Baselines）**：
    *   **Flat RL**：无分层、无屏蔽的纯PPO策略。
    *   **CBF-only**：仅有安全屏蔽器，配合随机或启发式策略。
    *   **Hierarchy-only**：仅有分层架构，无运行时安全过滤。
    *   **Hierarchy + Safety Shield（本文方法）**：全功能框架。

#### 4. 资源与算力
*   **硬件**：论文提到实验在单台工作站上完成，使用 **CPU 驱动的仿真**。
*   **软件栈**：使用 Python、JAX、Flax 和 Optax 框架。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或具体的训练总时长，但强调了由于使用了 LightSim2Grid 加速器，推理和安全检查的开销相对于环境计算而言可以忽略不计。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   常规性能评估：30个独立回合。
    *   压力测试与零样本迁移：各20个独立回合。
    *   进行了详细的消融实验（Ablation Study），对比了分层与屏蔽各自的贡献。
*   **充分性评价**：实验设计较为全面，涵盖了从基础性能到极端鲁棒性，再到跨规模泛化性的维度。通过固定随机种子确保了对比的公平性。虽然回合数（20-30次）在统计学上属于中等规模，但对于复杂的电网仿真而言，已足以展示趋势性的差异。

#### 6. 主要结论与发现
*   **架构胜过奖励**：单纯通过奖励函数惩罚不安全行为无法消除灾难性故障，而架构上的解耦能提供硬性安全保障。
*   **零样本泛化可行**：在 Case14（小网）训练的策略，配合安全屏蔽器，可以在 ICAPS 2021（大网）上实现 200 步满额存活，证明了结构化设计是解决泛化问题的关键。
*   **性能提升**：本文方法在压力测试下实现了 100% 的存活率，且平均峰值线路负载显著低于其他方法。

#### 7. 优点与亮点
*   **硬性安全保证**：将安全从“学习目标”转变为“运行时约束”，解决了 RL 在工业部署中的最大顾虑。
*   **卓越的泛化性**：实现了从极小规模到大规模电网的零样本迁移，这在电网 RL 领域是非常显著的成果。
*   **模块化设计**：安全屏蔽器可以与任何 RL 算法结合，具有很强的通用性。

#### 8. 不足与局限
*   **模型依赖性**：安全屏蔽器依赖于准确的电网物理模型进行前向仿真，若模型参数与实际系统有偏差，屏蔽效果会打折扣。
*   **单步前向的局限**：目前仅采用单步（One-step）看后，可能无法识别多步之后才爆发的级联故障。
*   **动作空间限制**：为了保证安全性，高层策略被限制在预定义的拓扑原语中，这可能在某些极端动态场景下限制了最优性。
*   **多智能体扩展**：目前仅限于单智能体控制，尚未讨论多区域协调下的安全屏蔽问题。

（完）
