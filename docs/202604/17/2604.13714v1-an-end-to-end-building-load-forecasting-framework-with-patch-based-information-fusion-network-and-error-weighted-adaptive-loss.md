---
title: An End-to-end Building Load Forecasting Framework with Patch-based Information Fusion Network and Error-weighted Adaptive Loss
title_zh: 一种结合基于补丁的信息融合网络与误差加权自适应损失的端到端建筑负荷预测框架
authors: "Hang Fan, Ying Lu, Weican Liu, Dunnan Liu, Xiaotao Chen, Shengwei Mei"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13714v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 基于 Patch 信息融合的建筑负荷预测
tldr: 本文针对建筑负荷预测中复杂的时间依赖和高波动性问题，提出一种端到端预测框架。该框架结合LOF异常检测与SVM-SHAP特征选择进行预处理，并设计了基于分块信息融合的网络（PIF-Net），利用GRU和门控机制动态整合局部特征。此外，引入误差加权自适应损失函数（EWAL）提升极端工况下的鲁棒性，实验证明了该方法在提高预测精度方面的优越性。
selection_source: fresh_fetch
motivation: 建筑负荷具有复杂的时间依赖性和高波动性，传统预测方法难以在多变环境下保持高精度。
method: 提出一种集成异常检测、SVM-SHAP特征选择、分块GRU融合网络（PIF-Net）及自适应损失函数（EWAL）的端到端框架。
result: 大量实验结果表明，该框架在预测精度和应对极端负荷波动方面均优于现有主流模型。
conclusion: 通过分块特征提取与动态误差加权优化，该研究为提升建筑能源管理效率提供了高效且鲁棒的预测方案。
---

## 摘要
准确的建筑负荷预测在促进需求响应聚合和优化能源管理方面发挥着至关重要的作用。然而，建筑负荷复杂的时序依赖性和高波动性限制了预测精度的提升。为此，我们提出了一种新型的端到端建筑负荷预测框架。具体而言，该框架可分为两个主要阶段。在由可解释特征选择增强的两阶段数据预处理模块中，我们利用局部离群因子（LOF）算法准确检测并修正原始建筑负荷序列中的异常值。此外，我们采用 SVM-SHAP 特征分析来量化环境变量的影响，筛选出关键特征组合以减少冗余。在建筑负荷预测模块中，我们提出了基于补丁的信息融合网络（PIF-Net）。该模型应用补丁技术将输入序列处理为局部块，通过具有残差连接的共享门控循环单元（GRU）网络提取时序特征。随后，基于自定义门控机制的信息融合模块整合集成隐藏状态，以动态加权不同时序补丁的重要性。此外，该框架使用一种新型的误差加权自适应损失（EWAL）函数进行训练。通过结合有理二次函数和对数损失，根据实时预测误差分布动态调整惩罚权重，EWAL 显著增强了模型在极端负荷条件下的鲁棒性。最后，广泛的实验证明了我们所提框架的有效性和优越性。

## Abstract
Accurate building load forecasting plays a critical role in facilitating demand response aggregation and optimizing energy management. However, the complex temporal dependencies and high volatility of building loads limit the improvement of prediction accuracy. To this end, we propose a novel end-to-end building load forecasting framework. Specifically, the framework can be divided into two main stages. In the two-stage data preprocessing module enhanced by interpretable feature selection, we utilize the Local Outlier Factor (LOF) algorithm to accurately detect and correct anomalies in the original building load series. Furthermore, we employ SVM-SHAP feature analysis to quantify the impact of environmental variables, filtering out critical feature combinations to mitigate redundancy. In the building load forecasting module, we propose the patch-based information fusion network (PIF-Net). This model applies patching technology to process input series into local blocks, extracting temporal features through a shared Gated Recurrent Unit (GRU) network with residual connections. Subsequently, an information fusion module based on a customized gating mechanism integrates the ensemble hidden states to weight the importance of different temporal patches dynamically. Additionally, the framework is trained using a novel Error-weighted Adaptive Loss (EWAL) function. By combining a rational quadratic function and logarithmic loss to dynamically adjust penalty weights based on real-time prediction error distributions, EWAL significantly enhances the model's robustness under extreme load conditions. Finally, extensive experiments demonstrate the effectiveness and superiority of our proposed framework.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **PIF-Net** 的端到端建筑负荷预测框架，旨在解决建筑能源管理中负荷数据的高波动性和复杂时序依赖问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着电力市场改革和需求响应（DR）的发展，建筑负荷聚合商需要极高精度的负荷预测来制定投标策略。
*   **核心挑战**：建筑负荷受环境因素影响大，具有强非线性和高波动性；传统模型直接输入长序列易导致信息丢失，且常用的 MSE/MAE 损失函数在极端波动（如需求响应期间）时鲁棒性不足。
*   **整体含义**：论文试图通过集成“智能预处理”、“分块特征融合”和“自适应损失优化”，构建一个能够应对极端工况且具备可解释性的稳健预测系统。

### 2. 方法论
该框架由三个核心模块组成：
*   **两阶段数据预处理**：
    *   **异常检测**：利用 **LOF（局部离群因子）** 算法基于密度检测原始序列中的异常点，并使用移动平均法进行修正。
    *   **特征选择**：通过 **SVM-SHAP** 分析，量化气象变量（温度、气压、风速等）对负荷的影响，剔除冗余特征，提升模型泛化能力。
*   **PIF-Net 预测模型**：
    *   **Patching（分块技术）**：将多维时间序列切分为多个局部“补丁”（Patches），在保留局部语义的同时缩短了输入序列长度，缓解梯度消失。
    *   **共享 GRU 与残差连接**：使用参数共享的 GRU 提取各补丁的特征，并通过残差连接保留高频波动信息。
    *   **门控融合机制**：设计了一个自定义门控模块，通过 Softmax 动态分配不同时间补丁的权重，聚合生成全局特征表示。
*   **EWAL（误差加权自适应损失）函数**：
    *   核心思想是结合 **有理二次函数（Rational Quadratic）** 和 **对数损失（Logarithmic Loss）**。
    *   通过实时计算 Batch 内误差的标准差来动态调整权重系数 $\omega_t$。在误差较小时侧重拟合精度，在误差较大（极端波动）时平滑惩罚权重，防止模型过拟合离群点。

### 3. 实验设计
*   **数据集**：使用 Building Data Genome 2 (BDG2) 公开数据集，选取了两种不同功能的建筑：**Robin College Dormitory（宿舍楼）** 和 **Fox Office（办公楼）**，采样频率为小时。
*   **Benchmark（基准模型）**：
    *   经典深度学习：MLP, CNN, LSTM, GRU。
    *   前沿长序列预测模型：Transformer, DLinear, **PatchTST**（目前时序预测的 SOTA 模型）。
*   **评价指标**：MAE, MSE, RMSE, MAPE, $R^2$, IA（一致性指数）, U1（Theil's U1 统计量）。

### 4. 资源与算力
*   **硬件环境**：Intel Core i9 14900HX CPU, 16 GB RAM, **NVIDIA GeForce RTX 5060 GPU**（注：文中原文如此标注）。
*   **软件环境**：PyTorch 框架，PyCharm IDE。
*   **训练细节**：文中未明确给出具体的训练总时长，但提到了最大 Epoch 为 300，Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **对比实验**：在两个数据集上分别对比了 7 种主流模型。
    *   **消融实验**：设计了 5 组变体（分别移除 LOF、SHAP、Patching、Gating 和 EWAL），验证各组件的必要性。
    *   **敏感性分析**：针对 Epoch、时间步长、Batch Size 进行了单变量敏感性测试。
*   **充分性评价**：实验设计较为全面，不仅对比了经典模型，还对比了最新的 PatchTST。通过消融实验直观展示了 EWAL 损失函数在降低 MSE 方面的显著作用。实验涵盖了不同功能的建筑，具有较好的客观性和说服力。

### 6. 主要结论与发现
*   **精度领先**：PIF-Net 在所有 7 项指标上均优于所有对比模型。在 Data 1 上，$R^2$ 达到 0.9393；在波动更剧烈的 Data 2 上，其 MSE 比 PatchTST 降低了约 32.6%。
*   **鲁棒性增强**：EWAL 损失函数被证明是提升性能的关键，移除它会导致 MSE 大幅上升，说明自适应权重能有效处理极端负荷波动。
*   **局部特征的重要性**：Patching 机制结合门控融合，比传统的点对点预测更能捕捉建筑负荷的周期性和突发性规律。

### 7. 优点
*   **端到端流程**：实现了从原始数据清洗到最终预测的闭环，减少了人工干预。
*   **创新的损失函数**：EWAL 解决了传统 MSE 对离群值过度敏感的问题，是该论文的一大亮点。
*   **可解释性**：引入 SHAP 值分析，使模型不再是完全的黑盒，有助于理解环境因素对负荷的影响。

### 8. 不足与局限
*   **地理局限性**：实验数据集虽然包含不同建筑类型，但均来自 BDG2 项目，可能存在地域气候特征的单一性。
*   **计算复杂度**：虽然 Patching 减少了序列长度，但 SVM-SHAP 的特征计算和门控融合机制在处理超大规模建筑群实时预测时，可能会带来额外的计算开销。
*   **确定性预测**：目前仅提供点预测，未涉及概率预测或区间预测，无法量化预测结果的不确定性风险。

（完）
