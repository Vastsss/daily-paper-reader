---
title: "MambaSL: Exploring Single-Layer Mamba for Time Series Classification"
title_zh: MambaSL：探索用于时间序列分类的单层 Mamba
authors: "Yoo-Min Jung, Leekyung Kim"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15174v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 用于时间序列分类的Mamba架构
tldr: 针对状态空间模型（SSM）在时间序列分类（TSC）领域独立应用研究不足的问题，本文提出 MambaSL 框架。该框架基于四个 TSC 特定假设，对单层 Mamba 的选择性 SSM 和投影层进行了极简重构。通过在 30 个 UEA 数据集上统一评测 20 个强基准模型，MambaSL 实现了 SOTA 性能，并提供了公开检查点以确保可复现性，证明了 Mamba 架构作为 TSC 骨干网络的巨大潜力。
selection_source: fresh_fetch
motivation: 旨在探索 Mamba 架构在时间序列分类任务中的独立潜力，并解决现有基准测试配置受限且难以复现的问题。
method: 提出 MambaSL 框架，基于四个针对 TSC 的假设对单层 Mamba 的选择性 SSM 和投影层进行了最小化重新设计。
result: 在 30 个 UEA 数据集上的统一评估中，MambaSL 显著优于 20 个强基准模型，取得了 SOTA 性能。
conclusion: 研究证明了单层 Mamba 架构作为时间序列分类骨干网络的高效性，并为该领域建立了更完善的评测基准。
---

## 摘要
尽管状态空间模型（SSM，如 Mamba）在各种序列领域取得了最新进展，但关于其在时间序列分类（TSC）中独立能力的研究仍然有限。我们提出了 MambaSL，这是一个在四个针对 TSC 的假设指导下，对单层 Mamba 的选择性 SSM 和投影层进行极简重新设计的框架。为了解决基准测试的局限性——包括配置受限、东安格利亚大学（UEA）数据集覆盖不全以及实验设置的可复现性不足——我们在统一的协议下，对所有 30 个 UEA 数据集上的 20 个强基准模型进行了重新评估。结果表明，MambaSL 达到了最先进的性能，并取得了具有统计学显著性的平均提升，同时通过为所有评估模型提供公开检查点确保了可复现性。结合可视化分析，这些结果证明了基于 Mamba 的架构作为 TSC 骨干网络的潜力。

## Abstract
Despite recent advances in state space models (SSMs) such as Mamba across various sequence domains, research on their standalone capacity for time series classification (TSC) has remained limited. We propose MambaSL, a framework that minimally redesigns the selective SSM and projection layers of a single-layer Mamba, guided by four TSC-specific hypotheses. To address benchmarking limitations -- restricted configurations, partial University of East Anglia (UEA) dataset coverage, and insufficiently reproducible setups -- we re-evaluate 20 strong baselines across all 30 UEA datasets under a unified protocol. As a result, MambaSL achieves state-of-the-art performance with statistically significant average improvements, while ensuring reproducibility via public checkpoints for all evaluated models. Together with visualizations, these results demonstrate the potential of Mamba-based architectures as a TSC backbone.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MambaSL**，一个专门为时间序列分类（TSC）设计的单层 Mamba 框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管 Mamba 等状态空间模型（SSM）在序列建模中表现出色，但在时间序列分类（TSC）领域的应用研究严重滞后，且常被误认为性能不如 CNN 或 Transformer。
*   **研究动机**：
    *   **架构缺陷**：直接套用原始 Mamba 无法充分发挥其在 TSC 任务中的潜力。
    *   **基准测试局限**：现有的 TSC 评测存在数据集覆盖不全（仅用 UEA 的子集）、基准模型调优不充分（导致基准性能被低估）以及实验不可复现等问题。

### 2. 方法论：MambaSL 框架
MambaSL 基于四个针对 TSC 特性的假设（H1–H4）对单层 Mamba 进行了极简重构：
*   **H1：缩放输入投影（Scale Input Projection）**：根据序列长度 $L$ 动态调整卷积核大小 $k$（$k = \max(3, \lfloor 0.02L \rfloor)$），以扩大长序列的感受野。
*   **H2：模块化时间（不）变性（Modularize Time (In)variance）**：解耦 Mamba 中的时间步长 $\Delta$、输入矩阵 $B$ 和输出矩阵 $C$ 的时间相关性。通过开关控制它们是时间不变（TI）还是时间变体（TV），发现简单的 TI 配置在某些 TSC 任务中反而更优。
*   **H3：移除残差连接（Remove Skip Connection）**：在单层浅层网络中，移除残差连接能强制模型完全依赖 SSM 的状态演化来提取特征。
*   **H4：多头自适应池化（Aggregate via Adaptive Pooling）**：提出一种轻量级的多头自适应池化机制，通过学习不同时间步的权重来聚合特征，比传统的平均或最大池化更能捕捉关键事件。

### 3. 实验设计
*   **数据集**：涵盖了 **UEA 归档的全部 30 个多变量数据集**，涉及不同的序列长度（8 到 17,984）、维度（2 到 1,345）和样本量。
*   **基准模型（20 个）**：
    *   **非深度学习**：DTW、ROCKET、HC2、Hydra 等。
    *   **深度学习**：DLinear、TimesNet、ModernTCN、PatchTST、iTransformer、TSLANet、TSCMamba 等。
*   **实验协议**：统一的超参数搜索预算（每个模型在每个数据集上进行约 200 次组合搜索），确保对比的公平性。

### 4. 资源与算力
*   **硬件型号**：主要使用 **NVIDIA GTX 1080 Ti (11GB)**；对于内存需求极大的实验（如长序列或大维度），使用了 **NVIDIA A100 (40GB)**。
*   **软件环境**：Python 3.12, PyTorch 2.5.1，使用了 `aeon` 工具包和 `TSLib` 库。
*   **训练细节**：每个模型训练 100 个 epoch，早停策略（patience）为 10。

### 5. 实验数量与充分性
*   **实验规模**：非常庞大且充分。对 20 个基准模型在 30 个数据集上进行了详尽的超参数扫描。
*   **消融实验**：针对四个假设（H1-H4）、TI/TV 配置组合、模型深度（1 层 vs 多层）均做了深入的消融研究。
*   **客观性与公平性**：通过统一搜索协议，作者发现许多基准模型（如 TSF 迁移模型）在经过充分调优后性能提升了 3% 以上，这证明了之前文献中对基准模型的低估，也体现了本研究的客观性。

### 6. 主要结论与发现
*   **SOTA 性能**：MambaSL 在平均准确率和平均排名上均达到了最先进水平，显著优于之前的 TSCMamba。
*   **单层即足够**：在 TSC 任务中，精心配置的单层 Mamba 性能优于多层堆叠，且更高效。
*   **时间不变性的价值**：与语言模型不同，TSC 任务中 $\Delta$ 和 $B$ 的时间不变性（TI）配置在多个数据集上表现更好。
*   **自适应池化的有效性**：能够根据数据集特性自动聚焦于关键的时间片段。

### 7. 优点
*   **严谨的基准重测**：纠正了 TSC 领域长期存在的基准测试不统一、不公平的问题。
*   **极简且高效**：证明了不需要复杂的领域变换（如小波变换）或深层结构，仅靠优化 SSM 核心即可实现高性能。
*   **高可复现性**：公开了所有模型的检查点（Checkpoints）和搜索日志。

### 8. 不足与局限
*   **缺乏压倒性优势**：虽然平均性能最强，但在单个特定数据集上并不总是第一，说明仍需针对特定领域进行微调。
*   **模型版本限制**：研究仅基于 Mamba-1，未探讨 Mamba-2 的并行化改进在 TSC 中的应用。
*   **感受野缩放风险**：H1 中的感受野缩放比例（0.02L）是经验性的，在某些特定领域（如极高频采样）可能不适用。

（完）
