---
title: "Chronax: A Jax Library for Univariate Statistical Forecasting and Conformal Inference"
title_zh: Chronax：一个用于单变量统计预测与共形推理的 JAX 库
authors: "Xan Carey, Yash Deshmukh, Aileen Huang, Sunit Jadhav, Omkar Tekawade, Lorraine Yang, Anvesha Tiwary, Gerardo Riano, Amy Greenwald, Denizalp Goktas"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16719v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 用于能源系统单变量统计预测的库
tldr: Chronax 是一个基于 JAX 的原生时间序列预测库，旨在解决传统 Python 预测工具在并行化、加速器支持和可组合性方面的局限。它通过函数式编程范式重新构建了预处理、建模和多步预测流程，支持大规模多序列预测和模型无关的共形不确定性量化，实现了在 CPU、GPU 和 TPU 上的高效端到端优化，为现代科学计算提供了高性能预测方案。
selection_source: fresh_fetch
motivation: 传统预测库受限于解释器执行和面向对象抽象，难以高效利用现代硬件加速器并处理大规模异构时间序列数据。
method: 利用 JAX 的纯函数、即时编译和自动向量化特性，将预测流程重新设计为可组合的函数变换。
result: 实现了支持大规模并行执行的高效预测框架，并集成了灵活的共形推理机制以提供可靠的不确定性量化。
conclusion: Chronax 填补了 JAX 生态在时间序列预测领域的空白，为高性能、可扩展的预测任务提供了现代化的软件基础。
---

## 摘要
时间序列预测在能源系统、气候建模、金融和零售等许多科学和工业领域中处于核心地位。虽然预测方法已从经典的统计模型演变为自动化和神经网络方法，但相关的软件生态系统仍锚定在传统的 Python 数值计算栈上。现有库依赖于解释器驱动的执行和面向对象的抽象，限制了可组合性、大规模并行性以及与现代可微和面向加速器工作流的集成。同时，当今的预测任务越来越多地涉及大规模异构时间序列数据、不规则协变量和频繁的重训练，这对可扩展性和执行效率提出了新的要求。JAX 为传统的有状态数值计算框架提供了一种基于纯函数和程序变换（如即时编译和自动向量化）的替代范式，从而实现了跨 CPU、GPU 和 TPU 的端到端优化。然而，这种现代范式尚未完全融入预测系统的设计中。我们推出了 Chronax，这是一个 JAX 原生的时间序列预测库，它围绕函数纯度、可组合变换和加速器就绪执行重新构思了预测抽象。通过将预处理、建模和多步预测表示为纯 JAX 函数，Chronax 实现了可扩展的多序列预测、模型无关的共形不确定性量化，以及与现代机器学习和科学计算流水线的无缝集成。

## Abstract
Time-series forecasting is central to many scientific and industrial domains, such as energy systems, climate modeling, finance, and retail. While forecasting methods have evolved from classical statistical models to automated, and neural approaches, the surrounding software ecosystem remains anchored to the traditional Python numerical stack. Existing libraries rely on interpreter-driven execution and object-oriented abstractions, limiting composability, large-scale parallelism, and integration with modern differentiable and accelerator-oriented workflows. Meanwhile, today's forecasting increasingly involves large collections of heterogeneous time series data, irregular covariates, and frequent retraining, placing new demands on scalability and execution efficiency. JAX offers an alternative paradigm to traditional stateful numerical computation frameworks based on pure functions and program transformations such as just-in-time compilation and automatic vectorization, enabling end-to-end optimization across CPUs, GPUs, and TPUs. However, this modern paradigm has not yet been fully incorporated into the design of forecasting systems. We introduce Chronax, a JAX-native time-series forecasting library that rethinks forecasting abstractions around functional purity, composable transformations, and accelerator-ready execution. By representing preprocessing, modeling, and multi-horizon prediction as pure JAX functions, Chronax enables scalable multi-series forecasting, model-agnostic conformal uncertainty quantification, and seamless integration with modern machine learning and scientific computing pipelines.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Chronax**，一个专为单变量统计预测和共形推理（Conformal Inference）设计的 JAX 原生库。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的时间序列预测库（如 StatsForecast）主要基于 NumPy、Numba 或 C++ 构建，虽然在 CPU 上表现良好，但存在以下局限：
    *   **解释器限制**：依赖 Python 解释器驱动的执行流，难以充分利用现代硬件加速器（GPU/TPU）。
    *   **并行性不足**：在处理成千上万个独立序列时，往往依赖多进程或 Python 循环，效率较低。
    *   **集成困难**：采用面向对象的有状态设计，难以与现代可微、函数式的机器学习工作流（如自动微分、超参数优化）无缝集成。
*   **研究动机**：利用 JAX 的函数式编程范式（纯函数、JIT 编译、自动向量化），重新构建预测系统的底层抽象，以满足大规模、高性能和端到端可微的现代预测需求。

### 2. 方法论
*   **核心思想**：将预处理、模型计算和多步预测重构为**纯函数**，通过 JAX 的程序变换实现硬件加速和并行化。
*   **关键技术细节**：
    *   **自动向量化 (`vmap`)**：无需手动编写批处理逻辑，即可在数千个时间序列上并行执行预测任务。
    *   **即时编译 (`jit`)**：利用 XLA 编译器将 Python 代码转化为针对特定硬件优化的机器码。
    *   **循环融合 (`lax.scan`)**：优化递归预测过程中的循环开销。
    *   **共形推理（Conformal Prediction）**：集成了一种分布无关（Distribution-free）的不确定性量化方法。通过“向前走”交叉验证计算共形得分（Conformity Scores），生成具有统计覆盖保证的预测区间。
    *   **统一接口**：虽然底层是函数式的，但对外提供了类似 scikit-learn 的 `fit/predict` 接口，方便用户迁移。

### 3. 实验设计
*   **数据集**：
    1.  **Airline Passengers**（小规模）：144 条观测值，月度数据。
    2.  **Daily Female Births**（中规模）：365 条观测值，日度数据。
    3.  **Room Temperatures**（大规模）：7056 条观测值，小时数据。
*   **Benchmark（基准）**：与目前最先进的统计预测库 **StatsForecast** 进行对比。
*   **对比方法**：涵盖了 28 种模型类，包括 ARIMA、ETS、TBATS、Theta、GARCH 以及各种朴素基准模型。
*   **评估指标**：
    *   **效率指标**：冷启动时间（含编译）、热启动时间（纯推理）。
    *   **准确性指标**：MAPE、MAE、RMSE、MASE。

### 4. 资源与算力
*   **算力说明**：论文强调了 Chronax 支持 CPU、GPU 和 TPU，并提到在实验中使用了 GPU 进行加速。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号（如 A100 或 V100）、显存大小或具体的训练/推理总时长。实验重点在于对比 JAX 编译后的执行效率与 Numba 编译后的效率差异。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 个不同规模的数据集，对 28 种模型进行了详尽的对比测试，并提供了详细的比例分析（Ratio Analysis）。
*   **客观性与公平性**：
    *   采用了多环境架构隔离 JAX 和 Numba 的依赖冲突。
    *   在测量 JAX 时间时，强制执行 `.block_until_ready()` 以确保测量的是真实的硬件计算时间而非异步调度时间。
    *   预先处理了数据传输时间，以纯粹比较推理速度。
*   **充分性**：实验覆盖了从极小到较大的数据集，验证了模型在不同数据量下的表现，结果具有较强的说服力。

### 6. 主要结论与发现
*   **延迟权衡**：Chronax 的**冷启动时间**显著高于 StatsForecast（因为 JAX 需要进行 XLA 编译），但在**热启动（重复推理）**阶段，Chronax 极快。在大型数据集上，Chronax 的推理时间仅为 StatsForecast 的 16% 左右。
*   **准确性**：Chronax 在所有数据集上都表现出竞争力，且随着数据集规模增大，其准确性优势更加明显（在最大数据集上误差更低）。
*   **扩展性**：通过 `vmap` 实现的向量化预测在处理大规模序列集时具有天然的性能优势。

### 7. 优点
*   **现代化架构**：完全拥抱 JAX 生态，支持端到端自动微分，适合科研人员进行超参数梯度优化。
*   **高性能**：在生产环境（频繁推理）中，编译后的内核执行效率极高。
*   **鲁棒的区间预测**：内置的共形推理提供了比传统高斯假设更可靠的不确定性估计。

### 8. 不足与局限
*   **编译开销**：对于单次、小规模的预测任务，JIT 编译的等待时间可能超过计算本身，不适合“即用即走”的轻量场景。
*   **模型范围**：目前主要集中在单变量统计模型，尚未完全覆盖多变量预测和复杂的深度学习基础模型（尽管这是未来的计划）。
*   **实验多样性**：虽然涵盖了不同规模，但实验数据集仅限于 3 个，若能增加包含数百万个序列的超大规模工业数据集测试，将更能体现其 `vmap` 的威力。

（完）
