---
title: "GAMMA-Net: Adaptive Long-Horizon Traffic Spatio-Temporal Forecasting Model based on Interleaved Graph Attention and Multi-Axis Mamba"
title_zh: GAMMA-Net：基于交错图注意力与多轴 Mamba 的自适应长时交通时空预测模型
authors: "Dongyi He, Yuanquan Gao, Bin Jiang, He Yan"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16859v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 长程交通时空预测
tldr: 针对智能交通系统中传统模型难以捕捉复杂时空依赖的问题，本文提出GAMMA-Net。该模型创新性地结合了图注意力网络（GAT）与多轴选择性状态空间模型（Mamba），利用GAT动态调整节点影响以实现自适应空间建模，并借助Mamba高效处理长时序列。实验证明，GAMMA-Net在多个基准数据集上均优于现有最先进模型，显著提升了长时交通预测的准确性与效率。
selection_source: fresh_fetch
motivation: 传统交通预测模型在处理复杂的时空依赖和长时预测需求时，往往面临建模能力不足或计算成本过高的问题。
method: 提出一种集成图注意力网络（GAT）与多轴Mamba的架构，通过交替处理空间与时间维度来捕捉动态交通流特征。
result: "在METR-LA、PEMS等六个主流数据集上的实验显示，该模型相比基准模型在平均绝对误差（MAE）上最高降低了16.25%。"
conclusion: GAMMA-Net通过融合自适应空间建模与高效状态空间模型，为长时交通时空预测树立了新的性能标杆。
---

## 摘要
准确的交通预测对于智能交通系统至关重要，它支持有效的交通管理、减少拥堵以及明智的城市规划。然而，传统模型往往无法充分捕捉交通数据中复杂的时空依赖关系。为了克服这些局限性，我们提出了 GAMMA-Net，这是一种将图注意力网络（GAT）与多轴选择性状态空间模型（Mamba）相结合的新颖方法。GAT 组件利用自注意力机制动态调整交通网络中节点的影响力，从而实现基于实时状况的自适应空间依赖建模。同时，Mamba 模块能够高效地建模长期时空动态，且无需传统循环架构的高昂计算成本。在 METR-LA、PEMS-BAY、PEMS03、PEMS04、PEMS07 和 PEMS08 等多个基准交通数据集上的广泛实验表明，GAMMA-Net 在不同预测周期内均一致优于现有的最先进模型，与基准模型相比，平均绝对误差（MAE）最高降低了 16.25%。消融研究突出了空间和时间组件的关键贡献，强调了它们在提高预测精度方面的互补作用。总之，GAMMA-Net 模型树立了交通预测的新标准，为下一代交通管理和城市规划提供了强大的工具。本研究的代码可在 https://github.com/hdy6438/GAMMA-Net 获取。

## Abstract
Accurate traffic forecasting is crucial for intelligent transportation systems, supporting effective traffic management, congestion reduction, and informed urban planning. However, traditional models often fail to adequately capture the intricate spatio-temporal dependencies present in traffic data. To overcome these limitations, we introduce GAMMA-Net, a novel approach that integrates Graph Attention Networks (GAT) with multi-axis Selective State Space Models (Mamba). The GAT component uses a self-attention mechanism to dynamically adjust the influence of nodes within the traffic network, enabling adaptive spatial dependency modeling based on real-time conditions. Simultaneously, the Mamba module efficiently models long-term temporal and spatial dynamics without the heavy computational cost of conventional recurrent architectures. Extensive experiments on several benchmark traffic datasets, including METR-LA, PEMS-BAY, PEMS03, PEMS04, PEMS07, and PEMS08, show that GAMMA-Net consistently outperforms existing state-of-the-art models across different prediction horizons, achieving up to a 16.25% reduction in Mean Absolute Error (MAE) compared to baseline models. Ablation studies highlight the critical contributions of both the spatial and temporal components, emphasizing their complementary role in improving prediction accuracy. In conclusion, the GAMMA-Net model sets a new standard in traffic forecasting, offering a powerful tool for next-generation traffic management and urban planning. The code for this study is available at https://github.com/hdy6438/GAMMA-Net

---

## 论文详细总结（自动生成）

这是一份关于论文《GAMMA-Net: Adaptive Long-Horizon Traffic Spatio-Temporal Forecasting Model based on Interleaved Graph Attention and Multi-Axis Mamba》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
交通预测是智能交通系统（ITS）的核心，但面临两大挑战：
*   **复杂的时空依赖性**：交通流具有高度非线性、非平稳性，且空间上相互交织。
*   **计算效率与长程建模的矛盾**：传统 RNN 难以处理长序列且无法并行；Transformer 虽然能捕捉长程依赖，但其自注意力机制具有二次计算复杂度（$O(L^2)$）；现有的 Mamba 变体虽然实现了线性复杂度，但在处理稀疏传感器或动态变化的拓扑结构时，空间编码能力较弱。
**研究动机**：提出一种既能保持线性计算效率，又能自适应捕捉动态空间拓扑的长程交通预测模型。

### 2. 方法论：核心思想与关键技术
GAMMA-Net 的核心思想是**交错式架构（Interleaved Architecture）**，将图注意力网络（GAT）与多轴选择性状态空间模型（Mamba）深度融合。

*   **嵌入层（Embedding Layer）**：整合了原始特征（流量、时间、日期）、周期性嵌入以及时空自适应嵌入，生成高维隐藏表示。
*   **交错式 GAT-Mamba 块**：
    1.  **阶段一（空间自适应 + 时间建模）**：首先通过 GAT 动态调整物理路网的权重，生成拓扑感知特征；随后利用 **Temporal Mamba** 在时间轴上进行扫描，以线性复杂度提取长程序列信息。
    2.  **阶段二（动态反馈 + 空间传播）**：利用时间建模后的特征再次通过 GAT 更新图注意力权重（响应新发现的动态，如拥堵传播）；最后通过 **Spatial Mamba** 在空间轴上扫描，实现高效的空间信息混合。
*   **核心公式逻辑**：
    *   状态转移：$h_t = A h_{t-1} + B x_t$，利用 Mamba 的选择性机制过滤噪声并保留关键时空状态。
    *   动态权重：通过 GAT 的自注意力机制 $e_{ij} = a(Wh_i, Wh_j)$ 实时更新节点间的相互影响。

### 3. 实验设计
*   **数据集**：使用了 6 个主流基准数据集：METR-LA、PEMS-BAY、PEMS03、PEMS04、PEMS07 和 PEMS08。
*   **对比方法（Benchmark）**：涵盖了三类模型：
    *   **基于图神经网络/循环网络**：DCRNN, GWNet, AGCRN, STGCN, GTS, MTGNN。
    *   **基于 Transformer**：GMAN, PDFormer, STAEformer。
    *   **基于 Mamba 的最新变体**：ST-MambaSync, STGM 等。
*   **评估指标**：MAE（平均绝对误差）、RMSE（均方根误差）、MAPE（平均绝对百分比误差）。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA GeForce RTX 4090 GPU** 工作站。
*   **软件环境**：PyTorch 2.1.1。
*   **训练细节**：Batch size 为 16，学习率 $1 \times 10^{-3}$，采用 Adam 优化器和早停机制（Early-stopping）。不同数据集的收敛轮数（Best Epoch）在 12 到 47 之间。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同规模和区域的数据集上进行了测试，涵盖了 15/30/60 分钟等多个预测步长。
*   **消融实验**：设计了四组对比实验（移除 GAT、移除时间组件、移除空间组件、同时移除两者），验证了各模块的必要性。
*   **可视化分析**：
    *   使用 **SVD（奇异值分解）** 分析 Mamba 的状态转移矩阵，验证系统稳定性。
    *   使用 **Louvain 算法** 进行社区检测，可视化空间和时间维度的注意力权重。
    *   使用 **t-SNE** 可视化时间嵌入的聚类效果。
*   **评价**：实验设计非常充分且客观，通过多维度可视化增强了深度学习模型的可解释性。

### 6. 主要结论与发现
*   **性能领先**：GAMMA-Net 在所有数据集和所有预测周期上均优于 SOTA 模型，MAE 最高降低了 **16.25%**。
*   **长程能力**：在 60 分钟的长程预测中，GAMMA-Net 的误差增长曲线比 Transformer 和 RNN 类模型更平缓。
*   **稳定性**：SVD 分析显示所有奇异值均小于 1，证明了模型在处理长序列时的数值稳定性。
*   **鲁棒性**：预测可视化显示，即使在传感器数据存在缺失（零值）的情况下，模型仍能生成平滑且合理的预测曲线。

### 7. 优点（亮点）
*   **架构创新**：交错式设计允许时间模式的发现反过来指导空间权重的更新，实现了时空的深度耦合。
*   **高效性**：利用 Mamba 的线性复杂度解决了长程预测的计算瓶颈，适合实时 ITS 部署。
*   **可解释性工具**：引入 SVD 分解来量化分析 Mamba 内部状态，这在同类论文中较为少见，为理解状态空间模型提供了新视角。
*   **自适应性**：GAT 的引入解决了传统 Mamba 模型在固定拓扑下的僵化问题。

### 8. 不足与局限
*   **初始拓扑依赖**：虽然 GAT 可以调整权重，但模型仍依赖于预定义的物理连接图，对于完全缺失拓扑信息的场景（如纯虚拟传感器网络）可能需要额外改进。
*   **超参数敏感性**：论文未详细讨论 Mamba 内部参数（如 $d_{state}$、$d_{conv}$）对不同规模交通网络的敏感性。
*   **应用限制**：虽然计算复杂度低，但在极大规模网络（如数万个节点）上的内存占用（Memory Footprint）仍需进一步验证。

（完）
