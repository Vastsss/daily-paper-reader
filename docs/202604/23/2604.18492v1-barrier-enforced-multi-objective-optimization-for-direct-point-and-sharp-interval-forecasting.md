---
title: Barrier-enforced multi-objective optimization for direct point and sharp interval forecasting
title_zh: 基于障碍约束多目标优化的直接点预测与窄区间预测
authors: "Worachit Amnuaypongsa, Yotsapat Suparanonrat, Pana Wanitchollakit, Jitkomut Songsiri"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18492v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 点预测与区间预测的概率预测框架
tldr: 本研究提出一种基于神经网络的多步概率预测框架，旨在同时生成点预测和区间预测。通过引入扩展对数障碍函数的损失项和多目标优化算法，该方法在确保预测区间不交叉且严格满足目标覆盖率的同时，实现了区间宽度的最小化。该框架采用共享时间模型与特定步长子模型的混合架构，消除了手动调参的繁琐过程，在太阳能辐照度预测任务中表现优异，且具有良好的通用性。
selection_source: fresh_fetch
motivation: 针对现有概率预测方法中预测区间易交叉、覆盖率难以严格达标以及多目标损失函数权重难以手动调节的问题。
method: 提出一种基于扩展对数障碍函数的尺度无关损失函数，并结合多梯度下降算法和混合神经网络架构进行多目标优化。
result: 在太阳能辐照度预测实验中，该方法在满足目标覆盖率的前提下获得了最窄的预测区间，性能优于LSTM、Transformer及Chronos等模型。
conclusion: 该框架通过创新的损失函数和训练策略，实现了无需人工调参的高精度、高可靠性点与区间同步预测。
---

## 摘要
本文提出了一种多步概率预测框架，利用单一神经网络模型同时生成点预测和区间预测。该方法通过模型结构设计确保预测区间（PIs）不交叉，在严格满足目标覆盖概率（PICP）的同时最大化锐度。与依赖手动调节标量化损失函数权重的现有方法不同，我们将点预测和区间预测视为一个多目标优化问题，并利用多梯度下降算法自适应地选择最优权重。主要创新点包括：一种基于扩展对数障碍函数且带有自适应超参数的新型 PI 损失函数，用以保证覆盖率；一种包含共享时间模型和特定预测步长子模型的混合架构；以及相应的训练策略。所提出的损失函数具有尺度无关性且具有普适性；结合我们的训练算法，该框架消除了为平衡多个目标而进行的试错式超参数调优。通过日内太阳辐照度预测应用的验证，结果表明，所提出的损失函数在达到目标覆盖率的同时具有最窄的 PI 宽度，始终优于现有文献中的方法。此外，与 LSTM 编码器-解码器和 Transformer 架构（包括由 Chronos 基础模型增强的架构）相比，我们的方法仍然极具竞争力，并且可以无缝适配到任何深度学习结构中。

## Abstract
This paper proposes a multi-step probabilistic forecasting framework using a single neural-network based model to generate simultaneous point and interval forecasts. Our approach ensures non-crossing prediction intervals (PIs) through a model structure design that strictly satisfy a target coverage probability (PICP) while maximizing sharpness. Unlike existing methods that rely on manual weight tuning for scalarized loss functions, we treat point and PI forecasting as a multi-objective optimization problem, utilizing multi-gradient descent to adaptively select optimal weights. Key innovations include a new PI loss function based on an extended log-barrier with an adaptive hyperparameter to guarantee the coverage, a hybrid architecture featuring a shared temporal model with horizon-specific submodels, and a training strategy. The proposed loss is scale-independent and universally applicable; combined with our training algorithm, the framework eliminates trial-and-error hyperparameter tuning for balancing multiple objectives. Validated by an intra-day solar irradiance forecasting application, results demonstrate that our proposed loss consistently outperforms those in current literature by achieving target coverage with the narrowest PI widths. Furthermore, when compared against LSTM encoder-decoder and Transformer architectures--including those augmented with Chronos foundation models--our method remains highly competitive and can be seamlessly adapted to any deep learning structure.

---

## 论文详细总结（自动生成）

这是一份关于论文《Barrier-enforced multi-objective optimization for direct point and sharp interval forecasting》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在时间序列预测（如可再生能源管理）中，决策者不仅需要**点预测**（最可能的值）来平衡供需，还需要**区间预测**（PI）来量化不确定性并制定风险预案。
*   **现有挑战**：
    1.  **可靠性与锐度的权衡**：高覆盖率（PICP）往往导致区间过宽，失去实用价值。
    2.  **权重调优困难**：现有损失函数通常将多个目标（点误差、区间宽度、覆盖率惩罚）通过固定权重求和，这需要大量的试错。
    3.  **结构性缺陷**：许多模型无法保证预测区间不交叉，或者无法确保点预测值落在区间内部。
*   **研究动机**：提出一个统一的框架，通过结构设计确保区间合法性，通过多目标优化（MOO）自动平衡各项指标，在严格满足覆盖率的前提下获得最窄的预测区间。

### 2. 方法论：核心思想与关键技术
该论文提出了名为 **SolarPointPI** 的框架，核心由三个部分组成：

*   **结构化设计（Structural Enforcement）**：
    *   模型输出点预测 $\hat{y}$ 以及两个非负偏移量 $\Delta \hat{u}$ 和 $\Delta \hat{l}$（通过 Softplus 激活函数实现）。
    *   预测区间定义为 $[\hat{y} - \Delta \hat{l}, \hat{y} + \Delta \hat{u}]$。这种设计从物理结构上保证了点预测永远在区间内，且上界永远大于下界，解决了区间交叉问题。
*   **障碍约束损失函数（Barrier-enforced Loss）**：
    *   **覆盖率损失**：引入**扩展对数障碍函数（Extended Log-barrier）**。当覆盖率低于目标值时，该函数会施加极大的惩罚；通过自适应超参数 $r$，在训练初期平滑梯度，在后期严格强制满足约束。
    *   **宽度损失**：采用 **Sum-k 损失**，专门惩罚最大的 $k$ 个区间宽度，以减少预测的保守性。
    *   **尺度无关性**：所有损失项均通过四分位距（Interquantile Range）进行归一化，使其具有通用性。
*   **多目标训练算法（MGDA）**：
    *   将点预测损失和区间预测损失视为两个独立目标。
    *   利用**多梯度下降算法（MGDA）**在每一步迭代中动态计算两个目标的梯度权重。它寻找一个共同的下降方向，使得两个目标同时优化，无需人工干预权重系数。

### 3. 实验设计
*   **数据集与场景**：使用泰国 104 个站点的地面太阳辐照度观测数据（15分钟分辨率），结合了 CAMS 再分析数据、Himawari-8 卫星云图数据以及晴空模型（Clear-sky model）。
*   **预测任务**：多步预测（15分钟至4小时，共16个步长）。
*   **Benchmark 与对比方法**：
    *   **损失函数对比**：QR（分位数回归）、QD+（质量驱动损失增强版）、IPIV（集成预测区间与值预测）、EMQ（增强多分位数损失）。
    *   **架构对比**：LightGBM、标准 LSTM 编码器-解码器、Transformer、以及由亚马逊 **Chronos 2.0** 基础模型增强的变体。

### 4. 资源与算力
*   **硬件环境**：Intel Core i7-14700 处理器（20核），**NVIDIA GeForce RTX 5070 Ti GPU (16 GB VRAM)**，64 GB RAM。
*   **软件框架**：Ubuntu 24.04 LTS，Python 3.13，PyTorch 实现。
*   **训练细节**：文中未详细给出总训练时长，但提到了推理延迟（SolarPointPI 约为 0.789 ms/样本）和计算量（0.642 MFLOPs），证明了其轻量化特性。

### 5. 实验数量与充分性
*   **实验规模**：使用了超过 400 万个样本的大型数据集。
*   **实验维度**：
    1.  **损失函数消融/对比**：验证了提出的障碍损失在满足 PICP 方面的优越性。
    2.  **架构鲁棒性**：将该方法应用于 LSTM、Transformer 等不同架构，证明了其通用性。
    3.  **多步长验证**：从短时到长时（16个步长）全面评估。
*   **客观性**：采用了空间交叉验证（Spatial Cross-validation），确保模型在未见过的地理位置上具有泛化能力；评价指标涵盖了 PICP、PINAW（宽度）、Winkler Score（综合评分）及 MAE/RMSE（点预测精度）。

### 6. 主要结论与发现
*   **覆盖率保障**：提出的损失函数是唯一能在所有预测步长上稳定满足 90% 目标覆盖率的方法，而其他方法（如 QD+、IPIV）在长步长预测时覆盖率显著下降。
*   **区间锐度**：在满足覆盖率的前提下，该方法获得的区间宽度（PINAW）最窄，有效降低了决策的保守成本。
*   **点预测精度**：尽管引入了复杂的区间约束，点预测的 MAE 依然保持在 SOTA 水平，证明了 MGDA 算法在平衡多目标方面的有效性。
*   **基础模型增强**：将 Chronos 2.0 作为特征提取器集成到框架中，能进一步提升预测性能，尤其是在处理波动剧烈的云层遮挡场景时。

### 7. 优点（亮点）
*   **无需调参**：通过 MGDA 解决了多目标损失函数权重难以确定的痛点，实现了端到端的自动化训练。
*   **结构严谨**：通过 Softplus 激活函数从数学结构上消除了区间交叉和点预测越界的问题。
*   **自适应性**：扩展对数障碍函数能根据当前覆盖率偏差动态调整惩罚力度，训练过程更加稳定。
*   **计算高效**：相比 Transformer 架构，提出的共享 LSTM + 独立子模型架构在保持高精度的同时，计算开销更小。

### 8. 不足与局限
*   **应用领域局限**：实验主要集中在太阳辐照度预测，虽然理论上通用，但在其他非周期性或极高噪声的时间序列（如金融数据）上的表现尚未验证。
*   **偏差风险**：在某些步长上，模型表现出略高的平均偏差（MBE），这可能是由于 MGDA 在优化过程中过于偏向区间覆盖率而对点预测的对称性关注不足。
*   **对基础模型的依赖**：虽然 Chronos 增强了效果，但引入基础模型会显著增加推理延迟和显存占用，在嵌入式或实时性要求极高的场景下可能受限。

（完）
