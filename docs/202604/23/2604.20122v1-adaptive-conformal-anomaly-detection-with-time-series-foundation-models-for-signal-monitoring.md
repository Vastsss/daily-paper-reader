---
title: Adaptive Conformal Anomaly Detection with Time Series Foundation Models for Signal Monitoring
title_zh: 基于时间序列基础模型的信号监测自适应符合异常检测
authors: "Natalia Martinez Gil, Fearghal O'Donncha, Wesley M. Gifford, Nianjun Zhou, Dhaval C. Patel, Roman Vaculin"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20122v1"
tags: ["query:ts-fm"]
score: 10.0
evidence: 利用时序基础模型进行异常检测
tldr: 本研究提出一种基于时间序列基础模型的自适应符合异常检测方法，旨在解决工业信号监控中数据有限和模型训练难的问题。该方法无需微调，利用预训练模型生成预测，并通过加权分位数符合预测技术动态调整权重以应对分布偏移。其输出的异常评分具有明确的统计学意义（p值），在保证误报率可控的同时，实现了高效、稳健且易于部署的异常检测。
selection_source: fresh_fetch
motivation: 针对工业监控中缺乏标注数据、训练成本高以及需要实时推理且能应对分布偏移的挑战。
method: 提出一种模型无关的后处理方法，结合时间序列基础模型与自适应加权分位数符合预测，动态学习权重以控制误报率。
result: 在合成和真实数据集上的实验证明，该方法在保持简单性的同时，展现了强大的性能、鲁棒性和自适应能力。
conclusion: 该方法为资源受限环境下的时间序列异常检测提供了一种透明、高效且具备统计保障的解决方案。
---

## 摘要
我们提出了一种用于时间序列监测的事后自适应符合异常检测方法，该方法利用预训练基础模型的预测结果，且无需额外的微调。我们的方法产生一个可解释的异常得分，可直接解释为误报率（p值），从而促进透明且可操作的决策制定。该方法采用加权分位数符合预测边界，并从过去的预测中自适应地学习最优权重参数，从而实现在分布偏移下的校准和稳定的误报控制，同时保留了样本外保证。作为一种与模型无关的解决方案，它能与基础模型无缝集成，并支持在资源受限的环境中快速部署。该方法解决了诸如数据可用性有限、缺乏训练专业知识以及对即时推理的需求等关键工业挑战，同时利用了时间序列基础模型日益增长的可访问性。在合成数据集和真实世界数据集上的实验表明，所提方法表现出强大的性能，兼具简单性、可解释性、鲁棒性和自适应性。

## Abstract
We propose a post-hoc adaptive conformal anomaly detection method for monitoring time series that leverages predictions from pre-trained foundation models without requiring additional fine-tuning. Our method yields an interpretable anomaly score directly interpretable as a false alarm rate (p-value), facilitating transparent and actionable decision-making. It employs weighted quantile conformal prediction bounds and adaptively learns optimal weighting parameters from past predictions, enabling calibration under distribution shifts and stable false alarm control, while preserving out-of-sample guarantees. As a model-agnostic solution, it integrates seamlessly with foundation models and supports rapid deployment in resource-constrained environments. This approach addresses key industrial challenges such as limited data availability, lack of training expertise, and the need for immediate inference, while taking advantage of the growing accessibility of time series foundation models. Experiments on both synthetic and real-world datasets show that the proposed approach delivers strong performance, combining simplicity, interpretability, robustness, and adaptivity.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **$W_1$-ACAS**（基于 1-Wasserstein 距离的自适应符合异常得分）的方法，旨在结合时间序列基础模型（TSFMs）进行高效、可解释的信号监测。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在工业预测性维护和信号监测中，通常面临**高质量标注数据稀缺**、**缺乏训练复杂模型的专业知识**以及**数据分布随时间偏移（非平稳性）**等挑战。
*   **研究动机**：
    *   现有的异常检测方法往往需要大量训练数据，且输出的异常得分缺乏明确的概率含义（难以设定阈值）。
    *   时间序列基础模型（TSFMs）虽然具备强大的零样本预测能力，但如何将其预测误差转化为稳健、可解释且能自适应环境变化的异常指标仍是一个空白。
    *   标准的“符合预测”（Conformal Prediction）依赖于数据可交换性假设，这在具有时间依赖和分布偏移的时间序列中往往失效。

### 2. 核心方法论
论文提出了一种事后（Post-hoc）、模型无关的自适应框架：
*   **核心思想**：将预训练基础模型的预测误差转化为**符合 p 值（Conformal p-values）**。该 p 值直接对应于误报率，具有极强的可解释性。
*   **关键技术细节**：
    *   **加权分位数符合预测**：不平等地对待历史误差样本，通过学习权重 $w$ 来强调与当前数据分布更相似的历史观测。
    *   **$W_1$ 优化目标**：通过最小化预测得分分布与均匀分布（$U[0,1]$）之间的 **1-Wasserstein 距离**，动态学习最优权重。这确保了在非平稳环境下，异常得分（p 值）依然保持校准。
    *   **多预测步长聚合**：针对不同的预测界限（Horizon）并行运行算法，并取中位数作为最终异常得分，以提高对点异常和上下文异常的鲁棒性。
*   **算法流程**：输入预测误差 -> 计算加权分位数 -> 映射为 p 值 -> 根据 $W_1$ 距离梯度更新权重 -> 输出异常判定。

### 3. 实验设计
*   **数据集**：
    *   **单变量**：使用了 7 个基准数据集（YAHOO, NEK, NAB, MSL, IOPS, STOCK, WSD），涵盖了传感器、网络、金融等场景。
    *   **多变量**：使用了 TAO, GECCO, LTDB, Genesis 等数据集。
*   **基础模型（Forecasters）**：集成了 TTM、Chronos-Bolt-Small 和 TiRex 三种主流 TSFMs。
*   **对比方法（Baselines）**：
    *   **经典方法**：KShape, POLY, Sub-KNN, Sub-PCA, SAND。
    *   **深度学习半监督方法**：CNN, USAD, OmniAnomaly。
    *   **零样本基础模型**：MOMENT。
    *   **变体对比**：标准的符合预测（Conformal）和高斯模型（Gaussian）。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA V100 (32 GB)** GPU 上运行。
*   **推理效率**：在 15 步预测配置下，平均每个样本、每个特征的处理时间约为 **0.025 ± 0.012 秒**。
*   **训练时长**：由于该方法是事后处理且无需对基础模型进行微调，因此不存在传统意义上的大规模训练耗时，仅涉及在线权重更新的微量计算。

### 5. 实验数量与充分性
*   **实验规模**：论文在 11 个真实世界数据集和多个合成数据集上进行了测试，涵盖了单变量和多变量场景。
*   **消融与敏感性分析**：
    *   针对预测步长（Horizon）数量、学习率（$\gamma$）、批大小（$n_b$）以及临界误报率（$\alpha_c$）均做了详细的敏感性实验。
    *   通过合成实验验证了在“渐变偏移”和“突发偏移”下的校准能力。
*   **评价客观性**：采用了多种指标（PA-F1, Affiliation-F, AUC-PR, VUS-PR, FPR, CalErr），并使用了业界公认的“Oracle 阈值策略”进行公平比较，实验设计较为严谨客观。

### 6. 主要结论与发现
*   **性能领先**：$W_1$-ACAS 在依赖阈值的指标（如 PA-F1 和 Affiliation-F）上表现最优，显著超过了传统的统计方法和需要训练的深度学习模型。
*   **校准性强**：相比于其他方法，该方法生成的 p 值更接近理想的校准曲线，能够更保守地控制误报率（FPR）。
*   **自适应能力**：实验证明权重学习机制能有效捕捉误差的周期性和分布变化，使模型在环境改变后能迅速重新校准。

### 7. 优点（亮点）
*   **无需微调**：直接利用现有的 TSFMs，极大地降低了工业部署的门槛。
*   **统计保障**：基于符合预测理论，提供了有限样本下的泛化保证。
*   **高可解释性**：异常得分即为 p 值，运维人员可以根据业务需求直接设定“允许 1% 的误报”等直观规则。
*   **模型无关**：可以无缝切换任何底层的预测模型或异常评分函数。

### 8. 不足与局限
*   **依赖底层预测器**：如果基础模型（TSFM）在特定领域的零样本预测能力极差，该方法的检测效果也会受限。
*   **计算开销随维度增加**：在处理超高维多变量数据时，为每个维度独立运行权重更新可能会带来一定的计算压力（虽然文中提到可以并行化）。
*   **冷启动限制**：算法需要一小段初始数据（$n_c$ 个样本）来建立初始的符合边界，无法在第一秒就实现完全精准的检测。

（完）
