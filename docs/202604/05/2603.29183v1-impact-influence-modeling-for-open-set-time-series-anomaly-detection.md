---
title: "IMPACT: Influence Modeling for Open-Set Time Series Anomaly Detection"
title_zh: IMPACT：面向开集时间序列异常检测的影响建模
authors: "Xiaohui Zhou, Yijie Wang, Hongzuo Xu, Weixuan Liang, Xiaoli Li, Guansong Pang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29183v1"
tags: ["query:ts-fm"]
score: 8.0
evidence: 开集时间序列异常检测框架
tldr: 针对开集时间序列异常检测（OSAD）中伪异常生成不真实以及训练集存在污染的问题，本文提出了IMPACT框架。该框架通过引入影响力建模，利用影响力函数评估训练样本对模型的影响，进而生成具有语义差异且真实的时间序列伪异常，并识别高影响力样本以净化受污染的训练数据。实验证明，IMPACT在多种OSAD场景和污染率下均显著优于现有最先进方法。
selection_source: fresh_fetch
motivation: 现有的开集异常检测方法在处理时间序列时，生成的伪异常往往缺乏真实性且难以应对训练数据中的异常污染。
method: 提出一种基于影响力建模的框架，通过学习影响力函数来生成语义多样的伪异常，并利用影响力得分对训练集进行去污染处理。
result: 实验结果显示，IMPACT在不同开集设置和数据污染率下，检测性能均大幅领先于现有的SOTA方法。
conclusion: 影响力建模是提升时间序列开集异常检测鲁棒性和准确性的有效手段，能同时解决伪异常生成质量低和数据污染两大难题。
---

## 摘要
开集异常检测（OSAD）是一种新兴范式，旨在利用训练中已见异常类别的有限标注数据，在测试期间识别已见和未见的异常。目前的方法依赖简单的增强手段来生成模拟未见异常的伪异常。尽管这些方法在图像数据中表现良好，但在时间序列数据中却被发现效果不佳，因为它们未能保留其序列特性，导致产生平庸或不切实际的异常模式。此外，当训练数据被未标注的异常污染时，这些方法会面临进一步的困扰。本研究提出了 IMPACT，这是一个利用影响建模进行开集时间序列异常检测的新型框架，旨在应对这些挑战。其核心见解在于：i) 学习一个能够准确估计单个训练样本对建模影响的影响函数；ii) 利用这些影响分数，为时间序列生成语义发散且真实的未见异常，同时将高影响样本重新用作有监督异常，以进行异常去污。广泛的实验表明，IMPACT 显著优于现有的最先进方法，在不同的 OSAD 设置和污染率下均表现出卓越的准确性。

## Abstract
Open-set anomaly detection (OSAD) is an emerging paradigm designed to utilize limited labeled data from anomaly classes seen in training to identify both seen and unseen anomalies during testing. Current approaches rely on simple augmentation methods to generate pseudo anomalies that replicate unseen anomalies. Despite being promising in image data, these methods are found to be ineffective in time series data due to the failure to preserve its sequential nature, resulting in trivial or unrealistic anomaly patterns. They are further plagued when the training data is contaminated with unlabeled anomalies. This work introduces $\textbf{IMPACT}$, a novel framework that leverages $\underline{\textbf{i}}$nfluence $\underline{\textbf{m}}$odeling for o$\underline{\textbf{p}}$en-set time series $\underline{\textbf{a}}$nomaly dete$\underline{\textbf{ct}}$ion, to tackle these challenges. The key insight is to $\textbf{i)}$ learn an influence function that can accurately estimate the impact of individual training samples on the modeling, and then $\textbf{ii)}$ leverage these influence scores to generate semantically divergent yet realistic unseen anomalies for time series while repurposing high-influential samples as supervised anomalies for anomaly decontamination. Extensive experiments show that IMPACT significantly outperforms existing state-of-the-art methods, showing superior accuracy under varying OSAD settings and contamination rates.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **IMPACT** 的新型框架，旨在解决时间序列开集异常检测（Open-Set Anomaly Detection, OSAD）中的核心挑战。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在实际应用中，通常只能获得极少量的标注异常样本（已见异常）。开集异常检测的目标是利用这些有限的先验知识，既能识别已见的异常，又能泛化到测试中可能出现的未见异常（Open-set anomalies）。
*   **核心问题**：
    1.  **数据污染（Contamination）**：训练集中所谓的“正常数据”往往含有未标注的异常，这会误导模型学习错误的决策边界。
    2.  **伪异常生成质量低**：现有的 OSAD 方法多借鉴图像领域的增强手段（如旋转、裁剪），这破坏了时间序列的序列特性，生成的伪异常不切实际或过于简单，导致模型泛化能力差。

### 2. 方法论：IMPACT 框架
IMPACT 引入了**影响力建模（Influence Modeling）**，通过理论推导将样本对测试风险的影响量化，其核心分为三个阶段：

*   **阶段 I：测试风险驱动的影响力评分 (TIS)**
    *   **多通道偏差损失**：设计了一种基于各向同性高斯先验的多通道损失函数，理论上等价于潜在分布的熵最小化，促使模型学习紧凑的正常表示。
    *   **影响力函数**：利用影响力函数（Influence Function）计算每个训练样本对验证集测试风险的贡献。如果移除某个样本能降低测试风险，则该样本被视为“有害”的（可能是污染异常）。
*   **阶段 II：基于风险削减的异常去污与生成 (RADG)**
    *   **标签翻转（去污）**：将识别出的高影响力（有害）样本的标签从“正常”翻转为“异常”，变废为宝，增加监督信息。
    *   **影响力引导的特征扰动（生成）**：选择对风险削减贡献最小的边界正常样本，在特征空间中沿着增加测试风险的方向进行扰动，从而生成语义上发散且符合物理逻辑的“未见异常”。
*   **阶段 III：双头模型训练**
    *   采用双头架构：一个头负责学习**已见异常**的判别，另一个头负责学习**未见异常**的泛化。

### 3. 实验设计
*   **数据集**：涵盖了 8 个主流真实世界数据集，包括单异常类（UCR, ASD, PSM, SMD）和多异常类（CT, SAD, PTBXL, TUSZ）。
*   **实验场景**：
    *   **通用设置（General Setting）**：标注异常随机采样自所有类别。
    *   **困难设置（Hard Setting）**：标注异常仅来自单一类别，测试时需检测完全不同的未见类别。
*   **对比方法（Benchmark）**：
    *   **无监督方法**：TCN-AE, THOC, TranAD, DCdetector, GPT4TS, COUTA, DADA。
    *   **开集/监督方法**：DevNet, DSAD, NCAD, DRA, MOSAD, InvAD, WSAD-DT。

### 4. 资源与算力
*   **硬件环境**：Intel Core i9-10980XE CPU，单张 **NVIDIA GeForce RTX 3090 GPU**。
*   **训练细节**：总训练轮数为 10 个 epoch（9 轮初始训练 + 1 轮重训练/微调）。
*   **效率**：论文进行了复杂度分析，证明 IMPACT 在处理高维数据和大规模样本时具有接近线性的时间复杂度，执行速度优于多数深度学习基准方法。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个数据集上进行了 5 次独立运行取平均值，涵盖了 AUC、Affiliation-F1 和 VUS-ROC 等多项指标。
*   **消融实验**：详细验证了标签翻转、特征扰动、双头损失以及特征偏差评分等每个组件的贡献。
*   **鲁棒性测试**：针对 2% 到 10% 的不同数据污染率进行了敏感性分析。
*   **可视化分析**：通过 t-SNE 特征可视化和样本形态展示，直观证明了生成伪异常的真实性以及影响力评分的准确性。
*   **评价**：实验设计非常全面，对比了从经典到 2025 年最新的 SOTA 方法，充分考虑了实际场景中的污染和开集泛化问题，具有高度的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：IMPACT 在所有数据集上均显著优于现有方法，平均 AUC 提升幅度达 6.37% 至 44.52%。
*   **去污有效性**：通过影响力引导的标签翻转，IMPACT 能够有效抵抗训练集污染，甚至在污染率增加时性能保持稳定或略有提升。
*   **泛化能力**：在“困难设置”下，IMPACT 展现了极强的未见异常检测能力，证明了基于影响力扰动的特征生成比传统数据增强更有效。

### 7. 优点
*   **理论支撑强**：将影响力函数引入时间序列异常检测，并提供了关于熵最小化和风险削减的数学证明。
*   **双重功效**：一个影响力指标同时解决了“数据清洗”和“样本生成”两个痛点。
*   **真实性高**：特征空间的扰动避免了传统增强方法破坏时间序列物理特性的问题。
*   **多通道策略**：多视角评估异常程度，提高了对细微异常的敏感度。

### 8. 不足与局限
*   **验证集依赖**：影响力计算依赖于一个相对干净或具有代表性的验证集，如果验证集本身存在严重偏差，可能会影响影响力评分的准确性。
*   **超参数敏感性**：虽然论文给出了默认值，但扰动强度 $\alpha$ 和样本选择数量 $k$ 在极端不同的数据集上可能仍需微调。
*   **计算开销**：尽管有近似算法，但 Hessian 矩阵的逆运算或其近似在模型参数量极大（如使用大型 Transformer 基础模型）时，计算成本可能会显著增加。

（完）
