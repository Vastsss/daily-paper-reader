---
title: Execution-Verified Reinforcement Learning for Optimization Modeling
title_zh: 面向优化建模的执行验证强化学习
authors: "Runda Guan, Xiangqing Shen, Jiajun Zhang, Yifan Zhang, Jian Cheng, Rui Xia"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00442v1"
tags: ["query:llm"]
score: 8.0
evidence: 使用大语言模型自动化优化建模以实现决策智能
tldr: 本文提出EVOM框架，旨在解决大模型在优化建模中依赖闭源模型或昂贵过程监督的问题。该方法将数学求解器视为确定性验证器，通过执行生成的建模代码并将结果转化为标量奖励，利用GRPO和DAPO算法进行闭环强化学习。EVOM无需过程级标注，支持跨求解器泛化，在NL4OPT和OptiBench等多个基准测试中表现优异，实现了高效且低成本的求解器适配。
selection_source: fresh_fetch
motivation: 现有方法依赖高延迟的闭源模型或昂贵的过程监督，且容易对特定求解器API产生过拟合。
method: 将求解器作为交互式验证器，通过执行反馈生成奖励，并利用强化学习算法进行闭环优化。
result: 在多个主流数据集和求解器上，EVOM的效果达到或超过了基于过程监督的微调模型，并支持零样本求解器迁移。
conclusion: 基于执行验证的强化学习是提升优化建模能力并实现跨求解器泛化的有效路径。
---

## 摘要
利用大语言模型（LLM）实现优化建模自动化是通往可扩展决策智能的一条极具前景的路径。然而，现有方法要么依赖于基于闭源 LLM 构建的智能体流水线，导致推理延迟较高；要么使用昂贵的过程监督对较小规模的 LLM 进行微调，这往往会导致对单一求解器 API 的过拟合。受具有可验证奖励的强化学习启发，我们提出了执行验证优化建模（EVOM），这是一种执行验证学习框架，将数学规划求解器视为确定性的交互式验证器。针对自然语言描述的问题和目标求解器，EVOM 生成特定于求解器的代码，在沙箱环境中执行，并将执行结果转换为标量奖励，通过 GRPO 和 DAPO 在闭环的“生成-执行-反馈-更新”过程中进行优化。这种纯结果导向的表述消除了对过程级监督的需求，并通过切换验证环境而非重建特定于求解器的数据集来实现跨求解器的泛化。在 NL4OPT、MAMO、IndustryOR 和 OptiBench 数据集上，针对 Gurobi、OR-Tools 和 COPT 的实验表明，EVOM 的表现达到或超过了经过过程监督微调（SFT）的模型，支持零样本求解器迁移，并通过在目标求解器后端持续训练实现了高效且低成本的求解器适配。

## Abstract
Automating optimization modeling with LLMs is a promising path toward scalable decision intelligence, but existing approaches either rely on agentic pipelines built on closed-source LLMs with high inference latency, or fine-tune smaller LLMs using costly process supervision that often overfits to a single solver API. Inspired by reinforcement learning with verifiable rewards, we propose Execution-Verified Optimization Modeling (EVOM), an execution-verified learning framework that treats a mathematical programming solver as a deterministic, interactive verifier. Given a natural-language problem and a target solver, EVOM generates solver-specific code, executes it in a sandboxed harness, and converts execution outcomes into scalar rewards, optimized with GRPO and DAPO in a closed-loop generate-execute-feedback-update process. This outcome-only formulation removes the need for process-level supervision, and enables cross-solver generalization by switching the verification environment rather than reconstructing solver-specific datasets. Experiments on NL4OPT, MAMO, IndustryOR, and OptiBench across Gurobi, OR-Tools, and COPT show that EVOM matches or outperforms process-supervised SFT, supports zero-shot solver transfer, and achieves effective low-cost solver adaptation by continuing training under the target solver backend.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：面向优化建模的执行验证强化学习 (EVOM)

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**自动化优化建模**任务，即如何将自然语言描述的实际问题准确转化为数学规划代码（如 Python 调用 Gurobi）。
*   **研究动机**：现有的方法存在两大瓶颈：
    1.  **推理成本高**：基于 Prompting 的智能体系统依赖闭源大模型（如 GPT-4o），多轮交互导致高延迟和高成本。
    2.  **数据成本与过拟合**：基于微调（SFT）的方法需要昂贵的“过程监督”数据（即每一步的变量定义、推导过程和参考代码），且模型容易过拟合到特定求解器的 API 语法，导致跨求解器（如从 Gurobi 迁移到 OR-Tools）的泛化能力极差。
*   **核心目标**：利用强化学习（RL）和求解器的自动验证能力，摆脱对过程标注的依赖，提升模型的泛化与适配能力。

#### 2. 方法论：核心思想与关键技术
论文提出了 **EVOM (Execution-Verified Optimization Modeling)** 框架，其核心思想是将数学求解器视为一个**确定性的交互式验证器**。

*   **核心流程（生成-执行-反馈-更新）**：
    1.  **生成**：模型根据问题描述和目标求解器标识，生成包含 `<think>`（推理）和 `<code>`（代码）的结构化响应。
    2.  **执行**：在沙箱环境中运行生成的代码，调用目标求解器（如 Gurobi）。
    3.  **反馈**：根据执行结果（是否报错、求解状态、目标函数值）与标准答案进行比对，生成标量奖励。
    4.  **更新**：利用强化学习算法优化模型参数。
*   **关键技术细节**：
    *   **结果导向奖励 (Outcome-only Reward)**：奖励函数仅由格式奖励（确保标签闭合）和结果正确性奖励（数值误差在容差范围内）组成，完全不参考中间推理步骤。
    *   **求解器条件化**：在 Prompt 中加入求解器标识，使模型学习不同 API 的用法。
    *   **优化算法**：采用了 **GRPO**（组相对策略优化，无需 Critic 模型，通过组内奖励归一化估计优势）和 **DAPO**（解耦裁剪与动态采样，缓解长链推理中的熵崩溃）。

#### 3. 实验设计
*   **数据集/基准测试**：
    *   **NL4OPT**：经典的自然语言转线性规划基准。
    *   **MAMO**：包含 EasyLP 和 ComplexLP，侧重数学建模能力。
    *   **IndustryOR**：工业级真实场景优化问题。
    *   **OptiBench**：涵盖线性与非线性规划的综合基准。
*   **对比方法**：
    *   **Prompting 类**：DeepSeek-R1, OpenAI o1, GPT-4o (CoT/CoE), OptiMUS (智能体系统)。
    *   **Training 类**：Qwen2.5-7B 基座模型、ORLM (基于过程监督的 SFT 模型)。
*   **求解器覆盖**：Gurobi, Google OR-Tools, COPT（杉数求解器）。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA A800 GPU**。
*   **软件框架**：基于 **verl** 强化学习框架进行训练。
*   **训练配置**：学习率 $1 \times 10^{-6}$，Batch Size 为 64，组采样大小 $G=8$，最大序列长度 8192 词元。
*   **训练时长**：文中未明确给出总训练小时数，但提到仅需 1 个 Epoch 即可观察到显著提升。

#### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的客观性和说服力：
*   **多规模验证**：测试了 Qwen2.5 系列从 0.5B、1.5B、3B 到 7B 的表现。
*   **跨求解器迁移**：专门设计了“零样本迁移”实验（Gurobi 训练，OR-Tools 测试）和“环境切换适配”实验。
*   **消融实验**：验证了 `<think>` 模块对逻辑建模的必要性。
*   **人工评估**：对 500 个样本进行了细粒度的约束和目标函数正确性的人工审核。
*   **数据修正**：作者指出了 MAMO 数据集中的原答案错误并进行了修正，体现了严谨性。

#### 6. 主要结论与发现
*   **结果监督等效性**：仅靠执行结果反馈的 RL 训练，效果即可达到或超过依赖昂贵过程标注的 SFT 方法。
*   **强大的迁移能力**：EVOM 训练的模型在跨求解器迁移时表现稳健，而 SFT 模型在切换求解器 API 时性能会发生崩溃（准确率降至接近 0）。
*   **推理能力的涌现**：随着训练进行，模型的“思考”长度增加，代码长度减少，表明模型学会了在生成代码前进行更深入的逻辑规划。
*   **小模型潜力**：经过 EVOM 训练的 3B 模型在多个指标上接近或超过了 7B 的基座模型。

#### 7. 优点（亮点）
*   **低成本数据方案**：通过求解器自动生成反馈，极大降低了对专家级过程标注的需求。
*   **解耦逻辑与语法**：通过结果导向的奖励，迫使模型学习通用的数学建模逻辑，而非死记硬背特定 API 的语法。
*   **闭环自进化**：提供了一种让模型在特定求解器环境下通过“试错”不断自我提升的路径。

#### 8. 不足与局限
*   **冷启动依赖**：对于预训练阶段完全未见过的求解器（如文中提到的 COPT），纯 RL 难以起步，仍需少量（约 100 条）SFT 数据进行引导。
*   **复杂约束挑战**：错误分析显示，尽管代码语法错误大幅减少，但在处理极其复杂的语义约束时，模型仍存在逻辑遗漏。
*   **奖励稀疏性**：对于非常难的问题，如果模型始终无法求得正确解，RL 将面临奖励过于稀疏、难以收敛的问题。

（完）
