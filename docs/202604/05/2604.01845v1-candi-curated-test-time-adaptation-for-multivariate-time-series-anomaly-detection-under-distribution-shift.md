---
title: "CANDI: Curated Test-Time Adaptation for Multivariate Time-Series Anomaly Detection Under Distribution Shift"
title_zh: CANDI：分布偏移下多变量时间序列异常检测的精选测试时自适应
authors: "HyunGi Kim, Jisoo Mok, Hyungyu Lee, Juhyeon Shin, Sungroh Yoon"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01845v1"
tags: ["query:ts-fm"]
score: 8.0
evidence: 多变量时间序列异常检测
tldr: 针对多变量时间序列异常检测在实际应用中面临的分布偏移问题，本文提出了CANDI框架。该框架采用测试时自适应（TTA）技术，通过虚假阳性挖掘（FPM）策略筛选关键样本，并结合时空感知正态性自适应（SANA）模块进行模型更新。CANDI在保留预训练知识的同时，能有效识别并纠正分布偏移引起的误报，在减少样本需求的前提下显著提升了检测精度。
selection_source: fresh_fetch
motivation: 旨在解决多变量时间序列异常检测模型在实际部署中因环境变化导致的分布偏移和性能退化问题。
method: 提出一种结合虚假阳性挖掘策略与时空感知正态性自适应模块的测试时自适应框架，实现对模型的高效在线更新。
result: "实验结果显示，CANDI在分布偏移场景下将AUROC最高提升了14%，且仅需更少的适应样本即可达到优异性能。"
conclusion: CANDI证明了通过选择性样本适应和结构化模型更新，可以有效增强异常检测模型在动态环境下的鲁棒性和准确性。
---

## 摘要
多变量时间序列异常检测（MTSAD）旨在识别多变量时间序列中偏离正常状态的情况，在现实应用中至关重要。然而，在实际部署中，分布偏移无处不在，会导致预训练异常检测器的性能严重下降。测试时自适应（TTA）仅使用无标签测试数据即时更新预训练模型，这使其成为应对这一挑战的有效方法。在本研究中，我们提出了 CANDI（分布偏移下多变量时间序列异常检测的精选测试时自适应），这是一个新颖的 TTA 框架，它在保留预训练知识的同时，有选择地识别并适应潜在的误报。CANDI 引入了一种误报挖掘（FPM）策略，根据异常分数和潜在相似性来筛选自适应样本，并结合了一个即插即用的时空感知正常性自适应（SANA）模块，用于结构化信息的模型更新。广泛的实验表明，CANDI 显著提升了分布偏移下 MTSAD 的性能，在减少自适应样本使用的同时，将 AUROC 提升了高达 14%。

## Abstract
Multivariate time-series anomaly detection (MTSAD) aims to identify deviations from normality in multivariate time-series and is critical in real-world applications. However, in real-world deployments, distribution shifts are ubiquitous and cause severe performance degradation in pre-trained anomaly detector. Test-time adaptation (TTA) updates a pre-trained model on-the-fly using only unlabeled test data, making it promising for addressing this challenge. In this study, we propose CANDI (Curated test-time adaptation for multivariate time-series ANomaly detection under DIstribution shift), a novel TTA framework that selectively identifies and adapts to potential false positives while preserving pre-trained knowledge. CANDI introduces a False Positive Mining (FPM) strategy to curate adaptation samples based on anomaly scores and latent similarity, and incorporates a plug-and-play Spatiotemporally-Aware Normality Adaptation (SANA) module for structurally informed model updates. Extensive experiments demonstrate that CANDI significantly improves the performance of MTSAD under distribution shift, improving AUROC up to 14% while using fewer adaptation samples.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CANDI** 的新型测试时自适应（Test-Time Adaptation, TTA）框架，旨在解决多变量时间序列异常检测（MTSAD）在实际部署中面临的分布偏移（Distribution Shift）问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在现实场景中，多变量时间序列数据具有非平稳性（如传感器漂移、环境变化）。预训练的异常检测模型通常假设训练和测试数据分布一致，当发生“正态性偏移”（Normality Shift）时，模型会将新的正常模式误判为异常，导致**虚假阳性（误报）大幅增加**。
*   **研究背景**：现有的 TTA 方法（如 M2N2）通常更新整个模型参数，且仅利用预测为正常的样本进行自适应。这存在两个缺陷：一是容易产生灾难性遗忘，破坏预训练知识；二是忽略了那些被误判为异常的“困难”正常样本（虚假阳性），而这些样本往往包含关键的分布偏移信息。

### 2. 方法论：CANDI 框架
CANDI 的核心思想是**有选择地识别并利用潜在的虚假阳性样本进行轻量化自适应**。
*   **虚假阳性挖掘 (FPM)**：
    *   在验证集（全正常数据）上计算异常分数分布，提取高分（前 $\alpha$ 分位）正常样本的潜在表示，构建参考集 $R_{fp}$。
    *   在测试阶段，若某样本异常分数较高但其潜在表示在马氏距离（Mahalanobis distance）上接近 $R_{fp}$，则将其识别为“潜在虚假阳性”并加入自适应集合。
*   **时空感知正态性自适应 (SANA)**：
    *   **结构**：采用即插即用的残差模块，包含一维卷积（捕捉时间依赖）和注意力机制（捕捉变量间相关性）。
    *   **策略**：保持预训练的编码器-解码器（Backbone）**冻结**，仅更新 SANA 模块的轻量化参数。
    *   **流程**：输入端 SANA 调整原始信号以对齐预训练分布；输出端 SANA 修正重建结果以匹配当前测试分布。

### 3. 实验设计
*   **数据集**：
    *   **SWaT**：工业水处理系统数据集。
    *   **SMD**：服务器机器数据集（选取了 5 个具有明显分布偏移的子集）。
    *   **TSB-AD**：包含 200 个多变量时间序列的大规模基准测试。
*   **对比基准 (Benchmarks)**：
    *   **Pre-trained**：无自适应的预训练模型。
    *   **M2N2**：现有的时间序列 TTA 领先方法。
*   **评估指标**：AUROC、AUPRC、以及不同阈值（$\alpha=0.5\%, 1\%, 5\%$）下的 F1 分数。

### 4. 资源与算力
*   **硬件**：实验在单张 **NVIDIA A40 GPU** 上运行。
*   **参数设置**：Batch Size 为 256，预训练 30 个 Epoch。
*   **自适应开销**：SANA 模块非常轻量，实验展示了在隐藏层维度 $h=32$（仅增加 0.09MB 参数）时仍能取得显著提升，证明了其在边缘设备部署的潜力。

### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个核心子集和 200 个 TSB-AD 数据集上进行了测试，涵盖了工业、IT 等多种场景。
*   **消融实验**：详细对比了“仅 FPM”、“仅 SANA”、“全模型更新”等组合，验证了各组件的必要性。
*   **鲁棒性分析**：针对学习率、门控初始化、Batch Size 以及不同的模型架构（MLP 和 TimesNet）做了敏感性分析。
*   **评价**：实验设计全面、客观，通过多随机种子取平均值并提供 p 值检验，确保了结果的统计显著性。

### 6. 主要结论与发现
*   **性能提升**：CANDI 在分布偏移严重的 SMD 3-2 数据集上将 AUROC 提升了 **59%**（相对值），在其他数据集上也有显著增益。
*   **样本效率**：CANDI 仅需使用不到 **2%** 的测试数据进行自适应，性能却优于使用大量数据的 M2N2。
*   **稳定性**：通过冻结 Backbone，CANDI 有效避免了灾难性遗忘，在长期运行中表现更稳健。

### 7. 优点与亮点
*   **样本精选策略**：首次提出挖掘“虚假阳性”作为自适应信号，这比单纯使用“预测正常”样本更具信息量。
*   **即插即用且安全**：SANA 模块不改变原模型结构，且通过冻结主干网络保证了潜在空间的稳定性。
*   **时空协同**：相比于简单的线性层，SANA 考虑了时间动态和变量间关系，更适合多变量场景。

### 8. 不足与局限
*   **训练数据假设**：假设训练集是纯净的（无异常），但在现实中训练集可能包含噪声或未标记的异常。
*   **阈值固定**：虽然自适应了模型参数，但检测阈值（Threshold）在测试期间是固定的，未来可探索动态阈值调整。
*   **极端偏移风险**：在极端剧烈的分布偏移下，FPM 依赖的马氏距离可能会失效，导致误将真实异常用于自适应（尽管实验显示其具有一定的抗污染鲁棒性）。

（完）
