---
title: ADAPTive Input Training for Many-to-One Pre-Training on Time-Series Classification
title_zh: 用于时间序列分类多对一预训练的自适应输入训练
authors: "Paul Quinlan, Qingguo Li, Xiaodan Zhu"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08398v1"
tags: ["query:ts-fm"]
score: 10.0
evidence: 时间序列基础模型与预训练
tldr: 针对时间序列预训练中多数据集联合训练面临的输入尺寸和通道维度差异巨大的挑战，本文提出了ADAPT预训练范式。该方法通过对齐不同数据集的物理属性，实现了高效的混合批次预训练。在162个分类数据集上的实验表明，ADAPT刷新了SOTA性能，为构建通用的时间序列基础模型奠定了重要基础。
selection_source: fresh_fetch
motivation: 现有的时间序列预训练方法在处理多个具有不同维度和长度的数据集时表现不佳，限制了通用基础模型的开发。
method: 提出ADAPT范式，通过对齐时间序列的物理属性，支持在输入尺寸和通道数迥异的数据集上进行混合批次预训练。
result: 在162个时间序列分类数据集上进行训练，成功刷新了分类基准测试的SOTA性能。
conclusion: ADAPT有效解决了多源异构数据的对齐问题，是构建时间序列领域通用基础模型的关键里程碑。
---

## 摘要
近期时间序列模型的研究利用自监督训练来学习有意义的特征和模式，旨在提升下游任务性能并泛化至未见模态。尽管这些预训练方法在“一对多”场景（即模型在单一数据集上预训练并在下游数据集上微调）中展现出巨大潜力，但当预训练阶段引入更多数据集时，它们在泛化至新数据集方面表现不佳。这是构建时间序列基础模型的一个根本性挑战，因为它限制了模型从大量多样化数据集中学习的能力。为解决这一挑战，我们提出了一种名为 ADAPT 的时间序列数据预训练新范式，该范式能有效对齐时间序列领域的物理属性，从而在预训练数据的输入尺寸和通道维度存在极端差异的情况下，实现混合批次预训练。我们在 162 个时间序列分类数据集上进行了训练，并在分类基准测试中取得了新的最先进性能。我们成功地在时间序列领域内同时对广泛的数据集训练了一个模型，这是构建时间序列领域通用基础模型的重要基石。

## Abstract
Recent work on time-series models has leveraged self-supervised training to learn meaningful features and patterns in order to improve performance on downstream tasks and generalize to unseen modalities. While these pretraining methods have shown great promise in one-to-many scenarios, where a model is pre-trained on one dataset and fine-tuned on a downstream dataset, they have struggled to generalize to new datasets when more datasets are added during pre-training. This is a fundamental challenge in building foundation models for time-series data, as it limits the ability to develop models that can learn from a large variety of diverse datasets available. To address this challenge, we present a new pre-training paradigm for time-series data called ADAPT, which can efficiently align the physical properties of data in the time-series domain, enabling mixed-batch pre-training despite the extreme discrepancies in the input sizes and channel dimensions of pre-training data. We trained on 162 time-series classification datasets and set new state-of-the-art performance for classification benchmarks. We successfully train a model within the time-series domain on a wide range of datasets simultaneously, which is a major building block for building generalist foundation models in time-series domains.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ADAPT** 的时间序列预训练新范式，旨在解决时间序列基础模型在处理多源异构数据时的对齐与泛化难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：时间序列数据具有极高的异构性（不同的长度、通道数、采样率和模态）。现有的预训练方法多为“一对多”（在一个数据集上预训练，在多个下游任务微调），但在“多对一”（同时在多个不同数据集上预训练）场景下表现极差。
*   **研究背景**：在 NLP 和 CV 领域，基础模型通过大规模多源数据训练获得了极强的泛化力。然而，以往的研究发现，在时间序列预训练中简单增加数据集反而会导致性能显著下降（如 TF-C 模型在多数据集训练时性能下降约 25%），这限制了时间序列通用基础模型的发展。

### 2. 论文提出的方法论：ADAPT 范式
*   **核心思想**：通过**自适应输入训练（Adaptive Input Training）**对齐不同数据集的物理属性，使模型能够处理任意维度和长度的输入，并支持现代并行计算中的混合批次（Mixed-batch）训练。
*   **关键技术细节**：
    *   **自适应平均池化（Average Adaptive Pooling）**：在数据加载阶段，将所有输入（无论原始长度和通道数如何）统一池化到相同的表征空间维度。这解决了截断或填充带来的信息丢失或稀释问题。
    *   **时间-频率双域掩码**：受掩码语言模型（MLM）启发，对时间域和经过快速傅里叶变换（FFT）的频率域同时进行**跨度掩码（Span Masking）**。
    *   **模型架构**：采用模型无关设计。实验中使用了堆叠的 Transformer 编码器，分别对时域和频域进行编码并融合。
    *   **训练目标**：通过两个独立的 MLP 层重建被掩码的时间和频率分量，计算双域重构损失（Reconstruction Loss）。

### 3. 实验设计
*   **预训练数据集**：使用了极其广泛的数据源，共计 **162 个分类数据集**，包括 UEA/UCR 存档（158 个）、SleepEEG、FD-A、HAR 和 ECG，总样本量约 55 万。
*   **测试基准（Benchmark）**：在 Epilepsy（癫痫）、FD-B（故障检测）、Gesture（手势识别）和 EMG（肌电信号）四个具有挑战性的下游任务上评估泛化能力。
*   **对比方法**：包括 TS-SD、TS2vec、CLOCS、MIXING-UP、TS-TCC、SimCLR 以及当前最先进的 TF-C。

### 4. 资源与算力
*   **硬件设备**：使用了 **2 台 NVIDIA A40 GPU**。
*   **训练参数**：训练了 1000 个 epoch，批次大小（Batch Size）为 1024。
*   **优化器**：使用 AdamW，初始学习率为 5e-4，采用余弦退火调度和 40 个 epoch 的预热（warmup）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 162 个数据集上进行预训练，并在多个跨领域任务上进行微调，实验规模在时间序列领域属于顶尖水平。
*   **消融与对比**：专门对比了仅在单一数据集（SleepEEG）训练的 ADAPT(EEG) 与在全量数据集训练的 ADAPT，证明了多数据集联合训练的增益。
*   **客观性**：通过 T-SNE 可视化验证了自适应池化对原始数据分布的保留情况，并分析了模型准确率与数据集物理属性（长度、通道等）的相关性，结果显示模型性能具有鲁棒性，实验设计较为客观公平。

### 6. 论文的主要结论与发现
*   **打破性能退化魔咒**：ADAPT 证明了通过合理的物理属性对齐，增加预训练数据集不仅不会降低性能，反而能显著提升模型在未见领域（Zero-shot/Few-shot 迁移）的泛化能力。
*   **刷新 SOTA**：在多个泛化基准测试（如 FD-B 和 EMG）中，ADAPT 显著超过了之前的 SOTA 模型（如 TF-C）。
*   **属性无关性**：实验发现模型准确率与输入长度、通道复杂度的相关性极弱，说明 ADAPT 是一种通用的时间序列嵌入策略。

### 7. 优点（亮点）
*   **高效对齐**：自适应池化解决了异构时间序列无法进入同一 Batch 训练的工程难题。
*   **模型无关性**：该框架可以适配未来任何更先进的时间序列模型架构。
*   **双域融合**：同时考虑时域和频域的特征重建，捕捉了更丰富的信号动力学信息。

### 8. 不足与局限
*   **细粒度信息丢失风险**：虽然 T-SNE 显示分布保持良好，但对于极长序列下采样到固定维度，理论上仍可能丢失某些瞬态的高频微小特征。
*   **任务局限性**：目前主要集中在**分类任务**，尚未在时间序列预测（Forecasting）或异常检测（Anomaly Detection）等其他核心任务上进行大规模验证。
*   **计算开销**：尽管使用了 A40，但 1000 个 epoch 的大规模预训练对普通研究者来说仍有较高的算力门槛。

（完）
