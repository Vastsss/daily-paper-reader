---
title: Zero-shot Multivariate Time Series Forecasting Using Tabular Prior Fitted Networks
title_zh: 使用表格先验拟合网络的零样本多变量时间序列预测
authors: "Mayuka Jayawardhana, Nihal Sharma, Kazem Meidani, Bayan Bruss, Tom Goldstein, Doron Bergman"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08400v1"
tags: ["query:ts-fm"]
score: 10.0
evidence: 使用表格基础模型进行零样本多变量时间序列预测
tldr: 本研究提出了一种利用表格基础模型（如TabPFN）进行多变量时间序列预测的新框架。针对以往方法将多变量序列拆分为独立单变量子问题而忽略通道间交互的缺陷，本文将多变量预测重构为一系列标量回归问题，使表格模型能以零样本方式捕获变量间关系。实验证明，该方法在TabPFN-TS骨干网络上表现出色，超越了现有的表格预测技术。
selection_source: fresh_fetch
motivation: 旨在解决现有表格基础模型在时间序列预测中忽略多变量间相互作用的问题。
method: 通过将多变量预测任务重构为一系列标量回归问题，利用表格模型的回归能力实现零样本预测。
result: 实验结果显示，该方法在使用TabPFN-TS骨干网络时，性能优于当前最先进的表格预测方法。
conclusion: 该框架成功扩展了表格基础模型在多变量时间序列领域的应用，证明了其处理复杂通道交互的潜力。
---

## 摘要
表格基础模型，特别是像 TabPFN 这样的先验数据拟合网络（Prior-data Fitted Networks），已在从数据插补到标签预测的多种表格数据任务中脱颖而出，成为领先的竞争者，超越了树模型在历史上的成功。这引发了对其在时间序列数据预测中适用性的研究，因为时间序列预测可以被表述为一个表格问题。虽然最近在这方面的工作展示了积极的结果，但大多数工作将多变量时间序列问题的处理局限于几个独立的单变量时间序列预测子问题，从而忽略了通道间的相互作用。为了克服这一局限性，我们引入了一个使用表格基础模型进行多变量时间序列预测的通用框架。我们通过将多变量时间序列预测问题重新表述为一系列标量回归问题来实现这一点，这些问题随后可以由任何具有回归能力的表格基础模型以零样本方式解决。我们展示了使用 TabPFN-TS 作为骨干网络的方法结果，并将其性能与当前最先进的表格方法进行了比较。

## Abstract
Tabular foundation models, particularly Prior-data Fitted Networks like TabPFN have emerged as the leading contender in a myriad of tasks ranging from data imputation to label prediction on the tabular data format surpassing the historical successes of tree-based models. This has led to investigations on their applicability to forecasting time series data which can be formulated as a tabular problem. While recent work to this end has displayed positive results, most works have limited their treatment of multivariate time series problems to several independent univariate time series forecasting subproblems, thus ignoring any inter-channel interactions. Overcoming this limitation, we introduce a generally applicable framework for multivariate time series forecasting using tabular foundation models. We achieve this by recasting the multivariate time series forecasting problem as a series of scalar regression problems which can then be solved zero-shot by any tabular foundation model with regression capabilities. We present results of our method using the TabPFN-TS backbone and compare performance with the current state of the art tabular methods.

---

## 论文详细总结（自动生成）

这篇论文提出了一种利用表格基础模型（Tabular Foundation Models）进行零样本多变量时间序列预测的新框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：多变量时间序列（MTS）预测不仅需要捕捉时间依赖性，还需要捕捉变量间（跨通道）的相互作用。
*   **核心问题**：现有的将表格模型（如 TabPFN）应用于时间序列的方法，大多采用“通道独立性（Channel Independence, CI）”策略，即把多变量问题拆解为多个独立的单变量预测任务。这种做法虽然简单，但忽略了通道间的空间相关性，限制了模型在复杂系统中的预测能力。
*   **整体含义**：本文旨在不改变模型架构、不进行重新训练的前提下，通过数据重构使表格基础模型能够以零样本（Zero-shot）方式处理多变量间的依赖关系。

### 2. 提出的方法论：TabPFN-TS-MV
*   **核心思想**：将多变量时间序列预测重构为一个通用的标量回归问题。
*   **关键技术细节**：
    *   **序列展开（Rolling Out）**：将多变量向量在每个时间步进行扁平化处理。例如，一个包含 $x, y, z$ 三个变量的时间序列，被转化为多行数据，每行包含：时间戳（Timestamp）、通道指示符（Channel Indicator，如 $\eta \in \{x, y, z\}$）以及对应的数值。
    *   **特征工程**：引入一个类别特征（通道索引）来标识当前行属于哪个变量。这样，模型在预测某一变量的未来值时，其上下文（Context）中包含了其他变量的历史信息。
    *   **独立标准化**：在输入模型前，对每个通道独立进行 Z-score 标准化，预测后再进行逆变换。实验证明，这比一阶差分更能有效处理不同变量间的量级差异。
    *   **零样本推理**：直接使用预训练好的 TabPFN（如 v2 或 v2.5）作为骨干网络，利用其强大的上下文学习（In-Context Learning）能力进行回归。

### 3. 实验设计
*   **数据集**：使用了 **Gift-Eval** 基准测试集，涵盖 23 个数据集、14.4 万条时间序列、1.77 亿个数据点，涉及金融、气象、工程等 7 个领域。
*   **评估指标**：点预测使用 **MASE**（平均绝对等比例误差），概率预测使用 **WQL**（加权分位数损失）。
*   **对比方法（Baselines）**：
    *   **TabPFN-TS**：原有的通道独立（CI）表格预测方法。
    *   **SOTA 时间序列模型**：包括 Chronos-2、TempoPFN、TimePFN 等。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练时长。
*   **资源限制**：作者提到该方法受限于 TabPFN 的**上下文长度（Context Length）**。由于将多变量展开，输入表格的行数会随变量数量线性增加，这在处理高维变量时会面临计算瓶颈。

### 5. 实验数量与充分性
*   **实验规模**：在 Gift-Eval 的 97 个不同任务（结合了不同频率和预测长度）上进行了测试。
*   **充分性**：实验覆盖了长短期预测和多种采样频率，并对比了多种 SOTA 模型。此外，还进行了关于数据标准化策略的消融实验（对比了原始值、差分和标准化）。
*   **客观性**：通过平均排名和多数据集对比，展示了方法在不同场景下的优劣，承认了在某些数据集上 CI 方法可能仍然占优。

### 6. 主要结论与发现
*   **性能提升**：在 60% 的多变量数据集上，该方法（TabPFN-TS-MV）的 MASE 优于通道独立的 TabPFN-TS。
*   **跨通道交互的重要性**：通过将空间相关性转化为序列依赖，模型能够利用跨通道上下文提升预测精度。
*   **标准化策略**：独立通道标准化对提升表格模型在多变量任务上的表现至关重要，而一阶差分反而可能降低性能。
*   **概率预测挑战**：虽然点预测表现优异，但在概率预测（WQL）上，该方法相比 CI 基准仍存在一定差距，可能需要更好的校准策略。

### 7. 优点
*   **零样本能力**：无需针对特定领域数据进行微调或重新训练，具有极强的通用性。
*   **模型无关性**：该框架可以应用于任何具有回归能力的表格基础模型。
*   **简单高效**：仅通过轻量级的数据重构（特征工程）就实现了对多变量依赖的建模。

### 8. 不足与局限
*   **计算开销与扩展性**：上下文长度随变量数线性增长，导致在处理变量极多（如 Jena Weather 数据集有 21 个变量）的任务时，计算成本高且受限于模型输入上限。
*   **概率预测精度**：在分位数损失（WQL）指标上表现不如点预测稳定，存在校准不足的风险。
*   **适用场景不确定性**：论文提到，何时选择通道独立（CI）或通道依赖（CD）仍是一个开放性问题，本方法并非在所有数据集上都优于 CI。

（完）
