---
title: In-Place Test-Time Training
title_zh: 原位测试时训练
authors: "Guhao Feng, Shengjie Luo, Kai Hua, Ge Zhang, Di He, Wenhao Huang, Tianle Cai"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06169v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 通过测试时训练使LLM适应连续的新信息流
tldr: 针对大语言模型在推理阶段无法动态适应新信息的问题，本文提出 In-Place TTT 框架。该方法将 MLP 层的投影矩阵作为可更新的“快速权重”，无需从头训练即可实现即插即用的增强。通过引入与下文预测任务对齐的优化目标和高效的分块更新机制，显著提升了模型在长文本处理和持续学习中的表现，为 LLM 动态适应环境提供了新路径。
selection_source: fresh_fetch
motivation: 传统的静态训练模式限制了 LLM 动态适应新信息的能力，而现有 TTT 方法面临架构不兼容、计算低效及目标函数不匹配的挑战。
method: 将 MLP 块的最终投影矩阵作为可更新权重，并结合与下文预测任务对齐的优化目标及支持上下文并行的分块更新机制。
result: 该框架使 4B 参数模型在 128k 长上下文任务中表现卓越，且在从头预训练对比中一致优于现有的 TTT 相关方法。
conclusion: In-Place TTT 证明了通过原位更新模型权重实现持续学习的可行性，是迈向动态适应型大语言模型的重要一步。
---

## 摘要
静态的“先训练后部署”范式从根本上限制了大语言模型（LLMs）根据现实任务中固有的持续新信息流动态调整其权重的能力。测试时训练（TTT）通过在推理阶段更新模型参数的一个子集（快权重）提供了一种引人注目的替代方案，然而其在当前 LLM 生态系统中的潜力受到了一些关键障碍的阻碍，包括架构不兼容、计算效率低下以及语言建模中快权重目标的不对齐。在这项工作中，我们引入了原位测试时训练（In-Place TTT），这是一个能够无缝赋予 LLMs 测试时训练能力的框架。In-Place TTT 将普遍存在的 MLP 模块中的最终投影矩阵视为其可自适应的快权重，从而实现了对 LLMs 的“即插即用”式增强，而无需昂贵的从头开始重新训练。此外，我们将 TTT 通用的重构目标替换为一个量身定制的、具有理论依据的目标，该目标与主导自回归语言建模的下一标记预测（Next-Token-Prediction）任务显式对齐。这一基于原则的目标结合高效的分块更新机制，产生了一种与上下文并行性兼容的高可扩展算法。广泛的实验验证了我们框架的有效性：作为一种原位增强，它使一个 4B 参数模型在上下文高达 128k 的任务上实现了卓越的性能；当从头开始预训练时，它始终优于具有竞争力的 TTT 相关方法。消融研究结果进一步为我们的设计选择提供了更深入的见解。总的来说，我们的结果确立了 In-Place TTT 是迈向 LLMs 持续学习范式的有前景的一步。

## Abstract
The static ``train then deploy" paradigm fundamentally limits Large Language Models (LLMs) from dynamically adapting their weights in response to continuous streams of new information inherent in real-world tasks. Test-Time Training (TTT) offers a compelling alternative by updating a subset of model parameters (fast weights) at inference time, yet its potential in the current LLM ecosystem is hindered by critical barriers including architectural incompatibility, computational inefficiency and misaligned fast weight objectives for language modeling. In this work, we introduce In-Place Test-Time Training (In-Place TTT), a framework that seamlessly endows LLMs with Test-Time Training ability. In-Place TTT treats the final projection matrix of the ubiquitous MLP blocks as its adaptable fast weights, enabling a ``drop-in" enhancement for LLMs without costly retraining from scratch. Furthermore, we replace TTT's generic reconstruction objective with a tailored, theoretically-grounded objective explicitly aligned with the Next-Token-Prediction task governing autoregressive language modeling. This principled objective, combined with an efficient chunk-wise update mechanism, results in a highly scalable algorithm compatible with context parallelism. Extensive experiments validate our framework's effectiveness: as an in-place enhancement, it enables a 4B-parameter model to achieve superior performance on tasks with contexts up to 128k, and when pretrained from scratch, it consistently outperforms competitive TTT-related approaches. Ablation study results further provide deeper insights on our design choices. Collectively, our results establish In-Place TTT as a promising step towards a paradigm of continual learning in LLMs.

---

## 论文详细总结（自动生成）

### 论文总结：In-Place Test-Time Training (原位测试时训练)

#### 1. 核心问题与整体含义（研究动机和背景）
传统大语言模型（LLM）遵循“先训练后部署”的静态范式，模型权重在推理阶段保持不变。这限制了模型动态适应长文本、实时流式信息或演化任务的能力。
**测试时训练（Test-Time Training, TTT）** 允许模型在推理时更新部分参数（快权重）以压缩上下文信息。然而，现有 TTT 方法面临三大挑战：
*   **架构不兼容**：通常需要引入特殊层，导致无法直接利用现有的预训练模型，必须从头训练。
*   **计算低效**：传统的逐标记（Per-token）更新是串行的，难以利用 GPU 的并行算力。
*   **目标函数不匹配**：常用的自监督重构目标与语言模型的“下一标记预测（NTP）”任务不一致。

#### 2. 方法论：核心思想与技术细节
论文提出了 **In-Place TTT** 框架，旨在无缝赋予现有 LLM 动态适应能力：
*   **原位（In-Place）设计**：不添加新层，而是直接复用 Transformer 中普遍存在的 **MLP 块**。具体而言，将 MLP 的最终投影矩阵 $W_{down}$ 视为可更新的“快权重”，而将 $W_{up}$ 和 $W_{gate}$ 冻结为“慢权重”。这种设计实现了“即插即用”，支持对现有模型进行微调。
*   **分块更新机制（Chunk-wise Update）**：为了提高效率，将序列划分为非重叠的块（如 512 或 1024 个标记）。模型先应用当前权重处理该块，然后根据该块的信息进行一次梯度更新。该过程通过**关联扫描（Parallel Scan）**算法实现，与上下文并行（Context Parallelism）完全兼容。
*   **语言模型对齐目标（LM-Aligned Objective）**：摒弃了传统的重构目标，设计了一个与 NTP 任务对齐的目标。通过 1D 卷积（Conv1D）和投影矩阵，使目标向量 $V$ 包含未来标记的信息。
*   **更新公式**：更新规则简化为 $W^{(i)}_{down} = W^{(i-1)}_{down} + \eta \hat{V}^\top_{[i]} Z_{[i]}$，其中 $Z$ 是 MLP 的中间激活，$\hat{V}$ 是对齐后的目标，$\eta$ 是学习率。

#### 3. 实验设计
论文通过两种场景验证有效性：
*   **场景 A：作为预训练模型的增强（Drop-in Enhancement）**
    *   **基础模型**：Qwen3-4B-Base, Llama-3.1-8B, Qwen3-14B-Base。
    *   **Benchmark**：RULER（长文本压力测试，涵盖 4k 到 256k 上下文）。
    *   **对比**：原始模型 vs. 经过少量持续预训练（Continual Training）后的 In-Place TTT 版本。
*   **场景 B：从头开始预训练（Pre-training from Scratch）**
    *   **规模**：500M, 1.5B, 4B 参数。
    *   **Benchmark**：Pile/Proof-Pile-2（困惑度 PPL）、常识推理（HellaSwag, ARC, MMLU, PIQA）。
    *   **对比方法**：滑动窗口注意力（SWA）、Gated Linear Attention (GLA)、DeltaNet、LaCT（另一种 TTT 方法）。

#### 4. 资源与算力
*   **硬件**：所有实验均在 **NVIDIA H800 GPU** 上进行。
*   **训练量**：
    *   从头预训练：500M 模型训练 20B tokens，1.5B 模型 60B tokens，4B 模型 120B tokens。
    *   持续预训练：Qwen3-4B 经历了两个阶段（20B + 15B tokens）。
*   **说明**：文中未明确给出具体的 GPU 数量和总训练时长（小时/天）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 500M 到 14B 不同量级的模型，验证了扩展性。
*   **消融实验**：非常充分。作者研究了**状态大小**（TTT 层数）、**分块大小**（Chunk Size 256-2048）以及**目标函数组件**（是否包含卷积和投影）对性能的影响。
*   **客观性**：对比了多种前沿的线性注意力和 TTT 变体，并在标准 Benchmark（如 RULER）上进行测试，实验设计较为公平、客观。

#### 6. 主要结论与发现
*   **长文本能力显著提升**：在 RULER 榜单上，In-Place TTT 使 4B 模型在 128k 长度下的表现大幅优于原始基座，并展现出极强的外推能力（256k）。
*   **架构兼容性**：证明了无需改变 Transformer 核心架构，仅通过 MLP 原位更新即可实现 TTT。
*   **目标函数的重要性**：理论分析和实验均证明，与 NTP 对齐的目标比传统的重构目标更能有效提升语言建模性能。
*   **效率优势**：在大分块（如 1024）下，In-Place TTT 的推理延迟和内存开销增加极小，且支持大规模并行。

#### 7. 优点
*   **极简且通用**：不改变模型拓扑结构，对现有 LLM 生态极其友好。
*   **理论支撑**：对 LM 对齐目标如何促进“感应头（Induction Heads）”机制进行了严谨的数学证明。
*   **高性能并行**：通过分块更新和关联扫描，解决了 TTT 长期以来的计算瓶颈。

#### 8. 不足与局限
*   **仍需微调**：虽然是“即插即用”，但为了激活 TTT 能力，仍需要对预训练模型进行一定规模（约数十亿 tokens）的持续预训练，并非完全“零成本”部署。
*   **超参数敏感性**：TTT 的学习率 $\eta$ 和更新阈值 $\tau$ 可能需要针对不同任务进行精细调优。
*   **实验覆盖范围**：虽然测试了 14B 模型，但尚未在超大规模（如 70B+）模型上验证其增益是否会随参数量增加而稀释。

（完）
