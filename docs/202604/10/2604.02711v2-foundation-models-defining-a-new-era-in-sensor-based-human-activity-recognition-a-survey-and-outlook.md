---
title: "Foundation Models Defining A New Era In Sensor-based Human Activity Recognition: A Survey And Outlook"
title_zh: 基础模型定义基于传感器的动作识别新时代：综述与展望
authors: "Sizhen Bian, Mengxi Liu, Lala Shakti Swarup Ray, Bo Zhou, Bin Guo, Zhiwen Yu, Thomas Ploetz, Paul Lukowicz, Siyu Yuan, Vitor Fortes Rey"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02711v2"
tags: ["query:ts-fm"]
score: 8.0
evidence: 基于传感器的序列数据基础模型
tldr: 本综述探讨了基础模型在基于传感器的个人活动识别（HAR）中的变革作用。针对传统模型在标签稀缺、传感器异构和泛化性差等方面的局限，本文系统梳理了HAR基础模型的生命周期，包括输入设计、预训练、适配与应用。通过分析九大技术维度的设计模式，总结了HAR专用模型、通用模型适配及大语言模型集成三大发展路径，为构建通用、可解释且以人为中心的活动理解系统指明了方向。
selection_source: fresh_fetch
motivation: 传统的HAR模型面临标注数据匮乏、传感器异构性强以及在不同用户和场景下泛化能力弱的严峻挑战。
method: 本文提出了一种面向生命周期的分类法，从输入设计、预训练、适配和利用四个阶段对现有的HAR基础模型进行系统综述。
result: 识别出HAR专用基础模型、通用时间序列/多模态模型适配以及大语言模型集成这三大主流技术发展轨迹。
conclusion: 基础模型开启了HAR的新纪元，但未来仍需在数据治理、多模态对齐、个性化及隐私保护等关键领域取得突破。
---

## 摘要
基于传感器的动作识别（HAR）是许多普适计算和可穿戴计算应用的基础，但目前的模型仍受限于标签稀缺、传感器异构性以及在不同用户、设备和场景下泛化能力弱等问题。基础模型通常利用自监督学习和多模态学习进行大规模预训练，通过学习可重用、可自适应的活动理解表示，为解决这些挑战提供了一种统一的范式。本综述系统梳理了面向传感器 HAR 的新兴基础模型。我们首先明确了基础概念、定义和评估标准，然后利用涵盖输入设计、预训练、适配和利用的生命周期导向分类法对现有工作进行了组织。我们没有简单地罗列单个模型，而是分析了跨越九个技术维度的重复设计模式和权衡，包括模态范围、分词（tokenization）、架构、学习范式、适配机制和部署设置。基于这一综合分析，我们确定了三条主要的发展轨迹：（1）在大型传感器语料库上从头训练的 HAR 专用基础模型；（2）将通用时间序列或多模态基础模型适配到基于传感器的 HAR；（3）集成大语言模型用于推理、标注和人机交互。最后，我们强调了在数据策展、多模态对齐、个性化、隐私和负责任部署方面的开放性挑战，并概述了迈向通用、可解释且以人为中心的活动理解基础模型的发展方向。完整且持续更新的论文和模型索引可在我们的配套仓库中找到：https://github.com/zhaxidele/Foundation-Models-Defining-A-New-Era-In-Human-Activity-Recognition。

## Abstract
Sensor-based Human Activity Recognition (HAR) underpins many ubiquitous and wearable computing applications, yet current models remain limited by scarce labels, sensor heterogeneity, and weak generalization across users, devices, and contexts. Foundation models, which are generally pretrained at scale using self-supervised and multimodal learning, offer a unifying paradigm to address these challenges by learning reusable, adaptable representations for activity understanding. This survey synthesizes emerging foundation models for sensor-based HAR. We first clarify foundational concepts, definitions, and evaluation criteria, then organize existing work using a lifecycle-oriented taxonomy spanning input design, pretraining, adaptation, and utilization. Rather than enumerating individual models, we analyze recurring design patterns and trade-offs across nine technical axes, including modality scope, tokenization, architectures, learning paradigms, adaptation mechanisms, and deployment settings. From this synthesis, we identify three dominant development trajectories: (1) HAR-specific foundation models trained from scratch on large sensor corpora, (2) adaptation of general time-series or multimodal foundation models to sensor-based HAR, and (3) integration of large language models for reasoning, annotation, and human-AI interaction. We conclude by highlighting open challenges in data curation, multimodal alignment, personalization, privacy, and responsible deployment, and outline directions toward general-purpose, interpretable, and human-centered foundation models for activity understanding. A complete, continuously updated index of papers and models is available in our companion repository: https://github.com/zhaxidele/Foundation-Models-Defining-A-New-Era-In-Human-Activity-Recognition.

---

## 论文详细总结（自动生成）

这篇论文《Foundation Models Defining A New Era In Sensor-based Human Activity Recognition: A Survey And Outlook》是对基于传感器的动作识别（HAR）领域引入基础模型（Foundation Models, FMs）的深度综述。以下是详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的基于传感器的动作识别（HAR）模型通常针对特定任务设计，依赖大量人工标注，且在不同用户、设备、身体部位或环境之间泛化能力极差。
*   **研究背景**：随着可穿戴设备和环境传感器的普及，产生了海量的未标注数据。受视觉（CV）和语言（NLP）领域基础模型的启发，HAR领域正经历从“特定任务的小模型”向“大规模预训练的基础模型”的范式转移，旨在通过自监督学习构建通用、可重用且具备语义理解能力的活动表示。

### 2. 方法论：核心思想与技术细节
论文提出了一个**面向生命周期的分类法**，将HAR基础模型的发展分为四个阶段，并围绕九大技术轴展开：
*   **输入建模（Prepare）**：
    *   **模态范围**：涵盖单模态（如仅IMU）、多模态（IMU+生理信号）和跨模态（传感器+文本/视频）。
    *   **分词策略（Tokenization）**：将连续信号转化为离散单元，包括窗口切片、频谱图补丁、统计特征聚合以及矢量量化（VQ）编码。
*   **预训练（Pretraining）**：
    *   **对比学习**：通过对齐不同视图或模态（如传感器与文本）来学习判别性特征。
    *   **生成式学习**：利用掩码信号建模（Masked Signal Modeling）重建缺失的传感器数据。
    *   **混合学习**：结合上述两者，提升特征的鲁棒性。
*   **适配（Adaptation）**：
    *   **参数高效微调（PEFT）**：如LoRA、适配器（Adapters），在冻结主干网络的情况下微调少量参数。
    *   **指令微调**：通过自然语言指令引导模型完成特定识别或推理任务。
*   **利用（Utilization）**：
    *   **三大轨迹**：(1) 从零训练HAR专用模型；(2) 适配通用时间序列模型（如Chronos）；(3) 集成大语言模型（LLM）作为推理引擎。

### 3. 实验设计：数据集、Benchmark 与对比
*   **数据集范围**：综述分析了132篇论文，涉及的数据集包括：
    *   **运动类**：PAMAP2, WISDM, RealWorld, Opportunity, UCI-HAR。
    *   **健康/生理类**：WESAD, Sleep-EDF, MIMIC-III/IV, MIT-BIH。
    *   **环境/射频类**：CASAS（智能家居）, WiFi/mmWave雷达数据。
*   **Benchmark 评估标准**：提出了六大诊断标准（语料覆盖度、跨域泛化、模态扩展性、标签效率、适配表面、涌现能力），并以雷达图形式对比了代表性模型（如SelfHAR-700k, SensorLM, IMU2CLIP等）。
*   **对比方法**：对比了传统深度学习模型（CNN/RNN）、简单的自监督学习（SSL）以及最新的跨模态对齐模型。

### 4. 资源与算力
*   **算力说明**：作为综述论文，文中未统一列出所有模型的具体算力消耗。
*   **关键提及**：论文引用了**LSM（Large Sensor Model）**的扩展性研究，指出当模型参数规模达到 $10^8$（亿级）时，在处理海量可穿戴数据时会出现明显的性能提升和Scaling Law（规模法则）效应。
*   **部署考量**：强调了云端大规模预训练与端侧（On-device）轻量化适配之间的算力权衡。

### 5. 实验数量与充分性
*   **充分性**：该综述涵盖了132篇代表性文献，时间跨度直至2025年后期，实验覆盖面极其广泛。
*   **客观性**：论文不仅关注识别准确率，还深入探讨了非独立同分布（Non-IID）情况下的跨用户/跨设备泛化实验，以及零样本（Zero-shot）和少样本（Few-shot）学习的表现，评估维度全面且客观。

### 6. 主要结论与发现
*   **范式转移**：HAR正从单纯的“分类任务”转向“语义理解与推理”。
*   **LLM的潜力**：LLM不仅能作为分类器，还能通过传感器-语言对齐，实现活动摘要生成、健康咨询和复杂行为逻辑推理。
*   **预训练优势**：在大规模未标注数据上进行掩码重建预训练，能显著提升模型在下游小数据集上的表现。

### 7. 优点与亮点
*   **系统性强**：首次为HAR基础模型定义了清晰的生命周期和技术分类轴。
*   **前瞻性**：不仅总结过去，还重点讨论了LLM在传感器领域的“具身”应用。
*   **资源丰富**：提供了配套的GitHub仓库，实时更新相关论文和模型索引，对研究者极具参考价值。

### 8. 不足与局限
*   **数据碎片化**：传感器领域仍缺乏像ImageNet或Common Crawl那样统一、标准化的超大规模开源语料库。
*   **时间层级挑战**：目前的模型在处理从“秒级动作”到“天级例程”的多尺度时间层级建模上仍显不足。
*   **隐私风险**：基础模型可能隐性学习到用户的生物特征或隐私信息，如何在预训练中去标识化仍是难题。
*   **端侧限制**：尽管有PEFT技术，但在极低功耗的可穿戴设备上运行大型基础模型仍存在硬件瓶颈。

（完）
