---
title: Instruction-Tuned LLMs for Parsing and Mining Unstructured Logs on Leadership HPC Systems
title_zh: 用于领先级高性能计算系统非结构化日志解析与挖掘的指令微调大语言模型
authors: "Ahmad Maroof Karimi, Jong Youl Choi, Charles Qing Cao, Awais Khan"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05168v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 经过指令微调的大语言模型用于解析和挖掘非结构化日志
tldr: 本研究针对高性能计算（HPC）系统产生的海量异构非结构化日志，提出了一种基于指令微调的LLM框架。通过结合思维链（CoT）推理和领域特定数据，对8B参数的LLaMA模型进行微调，实现了高效、隐私保护且可本地部署的日志解析。该方法在LogHub数据集上表现优异，并成功应用于Frontier超级计算机的6亿条生产日志分析，揭示了关键的系统运行模式。
selection_source: fresh_fetch
motivation: 旨在解决HPC系统日志格式异构且规模庞大，导致难以自动提取结构化信息并进行有效系统分析的问题。
method: 提出一种结合领域特定日志模板与思维链推理的指令微调框架，对8B参数的LLaMA模型进行适配。
result: 该模型在解析精度上达到了与LLaMA 70B和Claude相当的水平，并成功分析了Frontier超算四周内产生的6亿条生产日志。
conclusion: 研究证明了领域适配的小参数LLM在处理大规模HPC日志挖掘任务中具有极高的准确性、效率和实际应用价值。
---

## 摘要
领先级高性能计算（HPC）系统产生海量的异构且大部分为非结构化的系统日志。由于这些日志源自不同的软件、硬件和运行时层，其格式不一致，使得结构提取和模式发现极具挑战性。因此，稳健的日志解析与挖掘对于将原始遥测数据转化为可操作的见解至关重要，从而揭示运行模式、诊断异常，并实现可靠、高效且可扩展的系统分析。大语言模型（LLMs）的最新进展为领先级 HPC 环境中的自动化日志理解提供了一个极具前景的新方向。为了利用这一机遇，我们提出了一个领域自适应、遵循指令且由 LLM 驱动的框架，该框架利用思维链（CoT）推理以高保真度解析和结构化 HPC 日志。我们的方法结合了领域特定的日志模板数据与指令微调示例，对一个 80 亿参数（8B）的 LLaMA 模型进行微调，使其专门用于 HPC 日志分析。我们开发了一种混合微调方法，将通用 LLM 适配到领域特定的日志数据，从而实现一种保护隐私、可本地部署、快速且节能的日志挖掘方法。我们在 LogHub 仓库的多种日志数据集上进行了实验。评估结果证实，我们的方法实现的解析准确率与 LLaMA 70B 和 Anthropic 的 Claude 等大得多的模型相当。我们通过解析 Frontier 超级计算机在四周内产生的超过 6 亿条生产日志，进一步验证了微调后 LLM 模型的实际效用，揭示了时间动态、节点级异常以及工作负载与错误日志相关性方面的关键模式。

## Abstract
Leadership-class HPC systems generate massive volumes of heterogeneous, largely unstructured system logs. Because these logs originate from diverse software, hardware, and runtime layers, they exhibit inconsistent formats, making structure extraction and pattern discovery extremely challenging. Therefore, robust log parsing and mining is critical to transform this raw telemetry into actionable insights that reveal operational patterns, diagnose anomalies, and enable reliable, efficient, and scalable system analysis. Recent advances in large language models (LLMs) offer a promising new direction for automated log understanding in leadership-class HPC environments.   To capitalize on this opportunity, we present a domain-adapted, instruction-following, LLM-driven framework that leverages chain-of-thought (CoT) reasoning to parse and structure HPC logs with high fidelity. Our approach combines domain-specific log-template data with instruction-tuned examples to fine-tune an 8B-parameter LLaMA model tailored for HPC log analysis. We develop a hybrid fine-tuning methodology that adapts a general-purpose LLM to domain-specific log data, enabling privacy-preserving, locally deployable, fast, and energy-efficient log-mining approach. We conduct experiments on a diverse set of log datasets from the LogHub repository. The evaluation confirms that our approach achieves parsing accuracy on par with significantly larger models, such as LLaMA 70B and Anthropic's Claude. We further validate the practical utility of our fine-tuned LLM model by parsing over 600 million production logs from the Frontier supercomputer over a four-week window, uncovering critical patterns in temporal dynamics, node-level anomalies, and workload-error log correlations.

---

## 论文详细总结（自动生成）

这篇论文针对高性能计算（HPC）环境下的日志解析与挖掘问题，提出了一种基于指令微调（Instruction-Tuning）的大语言模型（LLM）框架。以下是详细的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：领先级HPC系统（如Frontier超算）产生海量、异构且非结构化的系统日志。传统解析方法（如正则表达式、Drain、Spell）依赖人工规则，难以应对动态演进的日志格式；而使用闭源商业LLM则面临**数据隐私泄露**、**高昂推理成本**以及**能源效率低下**的问题。
*   **研究动机**：开发一种可本地部署、隐私保护、高效且能处理复杂HPC领域知识的自动化日志解析工具，将原始遥测数据转化为可诊断故障和发现模式的结构化见解。

### 2. 论文提出的方法论
*   **核心思想**：利用较小规模的开源LLM（LLaMA 8B），通过**混合指令微调**和**思维链（CoT）推理**，使其在特定领域的解析能力达到甚至超过大规模模型。
*   **关键技术细节**：
    *   **混合微调策略**：构建了三个数据集：**SET 1**（领域特定日志模板对）、**SET 2**（通用指令遵循数据，如Alpaca）、**SET 3**（前两者混合）。实验证明混合数据集能同时提升领域准确性和指令理解力。
    *   **参数高效微调（LoRA）**：采用LoRA技术，通过冻结原模型权重并注入可训练的低秩矩阵（公式：$W' = W_0 + \alpha AB$），显著降低了算力需求。
    *   **推理工作流**：分为三个阶段：
        1.  **签名生成**：对原始日志聚类，提取代表性样本。
        2.  **模板生成**：利用微调后的LLM和CoT提示词生成带占位符的结构化JSON模板。
        3.  **序列生成**：将新日志与模板匹配，生成机器可读的结构化输出。

### 3. 实验设计
*   **数据集**：
    *   **LogHub基准**：包括HDFS、HPC、Hadoop、Linux、OpenSSH等5个公开数据集。
    *   **生产环境数据**：来自Frontier超算的**6.38亿条**真实生产日志（涵盖四周时间）。
*   **Benchmark（对比方法）**：
    *   **Claude 3.5 Sonnet (175B)**：作为性能上限参考。
    *   **LLaMA 3.3 70B**：作为高质量数据生成器和强力基准。
    *   **LLaMA 3.1 8B (Base)**：未微调的基础模型。
*   **评估指标**：覆盖率（Coverage Rate %）、平均相似度、编辑距离（Levenshtein）、词错率（WER）。

### 4. 资源与算力
*   **硬件环境**：在Frontier超算节点上运行，每个节点配备4个GPU（每个GPU包含2个AMD MI250X GCD，共64GB HBM内存）。
*   **算力对比**：
    *   **LLaMA 8B**：平均功耗235W，内存占用20.5GB，推理耗时约495秒。
    *   **LLaMA 70B**：平均功耗609W，内存占用252GB，推理耗时约3493秒。
*   **结论**：8B模型在总能量消耗上比70B模型节省了**20倍以上**（0.1 MJ vs 2.1 MJ）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对LogHub的5个子集进行了详尽测试。
    *   进行了**超参数消融实验**：涵盖了不同的训练轮数（2, 3, 5, 10, 50）、LoRA秩（8, 16）和缩放因子$\alpha$（8, 16, 32）。
    *   进行了**鲁棒性测试**：通过7种扰动（字符替换、拼写错误、空格、词序重排等）生成了21,903个测试样本。
*   **充分性评价**：实验设计非常充分且客观。不仅在公开基准上验证，还通过超大规模真实生产数据进行了实战检验，并对比了模型大小与能效比，具有很强的说服力。

### 6. 主要结论与发现
*   **性能对标**：经过微调的8B模型在解析准确率上与70B模型及Claude 3.5相当。
*   **混合微调的必要性**：仅用日志数据或仅用指令数据微调效果均有限，两者结合（SET 3）效果最佳。
*   **运维见解**：
    *   **故障级联效应**：识别出从“硬件压力 -> 关键触发 -> 资源耗尽 -> 网络崩溃”的四阶段级联模式。
    *   **大海捞针**：灾难性事件（如内核崩溃）仅占日志总量的0.0004%，被数千万条静默硬件纠错日志掩盖。
    *   **拓扑相关性**：发现网络错误呈现明显的机架/机柜层级分布，而非随机点对点故障。

### 7. 优点
*   **实用性强**：解决了HPC中心对数据隐私和本地部署的刚需。
*   **能效卓越**：证明了小参数模型通过精细微调可以在特定任务上替代超大模型，大幅降低运营成本。
*   **深度分析**：不仅停留在“解析”层面，还利用解析结果对Frontier超算的系统稳定性进行了深入的科学分析。

### 8. 不足与局限
*   **鲁棒性瓶颈**：在面对“额外词汇注入”和“标点符号大幅改变”时，模型的精确匹配率显著下降（尽管语义相似度仍较高）。
*   **领域局限**：微调数据主要集中在HPC和Linux环境，对于完全不同领域的日志（如金融、医疗专用协议）可能需要重新微调。
*   **实时性挑战**：虽然比70B快，但对于每秒产生数百万条日志的极端峰值，8B模型的推理速度可能仍难以做到完全实时处理。

（完）
