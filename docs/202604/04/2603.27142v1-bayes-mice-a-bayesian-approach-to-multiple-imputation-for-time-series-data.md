---
title: "Bayes-MICE: A Bayesian Approach to Multiple Imputation for Time Series Data"
title_zh: Bayes-MICE：一种针对时间序列数据的贝叶斯多重插补方法
authors: "Amuche Ibenegbu, Pierre Lafaye de Micheaux, Rohitash Chandra"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27142v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 时间序列数据的多重插补
tldr: 针对时间序列数据中常见的缺失值问题，本文提出 Bayes-MICE 框架，通过贝叶斯推理和 MCMC 采样改进了传统的多重插补方法。该方法引入时间滞后特征和时序初始化以尊重数据的序列本质，并能有效量化插补过程中的不确定性。在空气质量和医疗数据集上的实验表明，Bayes-MICE 显著降低了插补误差，且 MALA 采样器在收敛速度和后验探索一致性上优于传统采样器。
selection_source: fresh_fetch
motivation: 传统 MICE 方法在处理时间序列时，难以充分捕捉模型参数的不确定性并利用数据的时序依赖特征。
method: 在贝叶斯框架下结合 MCMC 采样（RWM 和 MALA）进行插补，并引入时间滞后特征和时序感知的初始化策略。
result: Bayes-MICE 在多个真实数据集上均优于基准方法，不仅降低了误差，还提供了更准确的不确定性度量。
conclusion: 该框架为环境和临床等领域的时间序列插补提供了一种平衡精度与不确定性量化的实用且高效的方案。
---

## 摘要
时间序列分析经常受到缺失数据的影响，这是医疗保健和环境监测等多个领域的常见问题。链式方程多重插补（MICE）因通过“全条件设定”来插补缺失值而备受关注。我们利用贝叶斯框架扩展了 MICE（即 Bayes-MICE），利用贝叶斯推理通过马尔可夫链蒙特卡洛（MCMC）采样来插补缺失值，从而考虑 MICE 模型参数和插补值中的不确定性。我们还在模型中加入了具有时间信息的初始化和时滞特征，以遵循时间序列数据的序列特性。我们使用两个真实世界数据集（AirQuality 和 PhysioNet）评估了 Bayes-MICE 方法，并采用了随机游走 Metropolis (RWM) 和 Metropolis 调整的朗之万算法 (MALA) 采样器。我们的结果表明，相对于基准方法，Bayes-MICE 在所有变量上都降低了插补误差，并考虑了插补过程中的不确定性，从而提供了更准确的插补误差衡量。我们还发现 MALA 比 RWM 收敛更快，在达到相当准确度的同时，提供了更一致的后验探索。总体而言，这些研究结果表明，Bayes-MICE 框架代表了一种实用且高效的时间序列插补方法，在各种环境和临床场景中平衡了准确性的提升与不确定性的有效量化。

## Abstract
Time-series analysis is often affected by missing data, a common problem across several fields, including healthcare and environmental monitoring.   Multiple Imputation by Chained Equations (MICE) has been prominent for imputing missing values through "fully conditional specification". We extend MICE using the Bayesian framework (Bayes-MICE), utilising Bayesian inference to impute missing values via Markov Chain Monte Carlo (MCMC) sampling to account for uncertainty in MICE model parameters and imputed values. We also include temporally informed initialisation and time-lagged features in the model to respect the sequential nature of time-series data. We evaluate the Bayes-MICE method using two real-world datasets (AirQuality and PhysioNet), and using both the Random Walk Metropolis (RWM) and the Metropolis-Adjusted Langevin Algorithm (MALA) samplers. Our results demonstrate that Bayes-MICE reduces imputation errors relative to the baseline methods over all variables and accounts for uncertainty in the imputation process, thereby providing a more accurate measure of imputation error. We also found that MALA converges faster than RWM, achieving comparable accuracy while providing more consistent posterior exploration. Overall, these findings suggest that the Bayes-MICE framework represents a practical and efficient approach to time-series imputation, balancing increased accuracy with meaningful quantification of uncertainty in various environmental and clinical settings.

---

## 论文详细总结（自动生成）

### Bayes-MICE：一种针对时间序列数据的贝叶斯多重插补方法

#### 1. 核心问题与整体含义（研究动机和背景）
时间序列数据在环境监测、医疗健康等领域广泛存在，但由于传感器故障、采样不规律等原因，数据缺失问题十分普遍。传统的**链式方程多重插补（MICE）**虽然在多变量数据处理中表现优异，但在处理时间序列时存在两个主要缺陷：
*   **忽略时序性**：默认观测值是可交换的，破坏了数据中的趋势和季节性。
*   **不确定性量化不足**：标准 MICE 在每一轮迭代中通常将模型参数视为固定值，而非随机变量，导致无法充分传播参数不确定性，容易低估插补值的方差。
本文提出的 **Bayes-MICE** 旨在通过贝叶斯框架解决这些问题，实现更精准的插补和更科学的不确定性度量。

#### 2. 方法论：核心思想与技术细节
Bayes-MICE 的核心是将 MICE 的“全条件设定（FCS）”结构与贝叶斯推理及马尔可夫链蒙特卡洛（MCMC）采样相结合。
*   **核心思想**：将缺失值插补视为从后验分布中采样的过程。不仅插补缺失值，还对模型参数（回归系数 $\theta$ 和残差方差 $\tau^2$）进行概率化更新。
*   **关键技术细节**：
    *   **时滞特征增强**：在预测变量中加入目标变量的过去和未来滞后项（Lagged features），使模型能够感知时间依赖性。
    *   **贝叶斯回归模型**：为每个变量建立高斯线性回归模型，并设定弱信息先验（多元高斯先验和逆伽马先验）。
    *   **MCMC 采样器**：实现了两种采样方案：**随机游走 Metropolis (RWM)** 和 **Metropolis 调整的朗之万算法 (MALA)**。其中 MALA 利用梯度信息引导搜索，提高在高维空间的采样效率。
    *   **自适应缩放**：根据参数维度自动调整提议分布（Proposal distribution）的步长，以达到最优接受率（RWM 约 23.4%，MALA 约 57.4%）。
    *   **初始化策略**：引入“时间感知初始化”（如利用 STL 分解捕捉趋势和季节性），为迭代提供更合理的起点。

#### 3. 实验设计
*   **数据集**：
    1.  **AirQuality（空气质量）**：包含 9,358 条记录，涉及 CO、苯、温度等 8 个特征，人工引入 20% 的完全随机缺失（MCAR）。
    2.  **PhysioNet（医疗 ICU）**：包含 4,000 名患者的生理指标（心率、葡萄糖、白细胞等），人工引入约 26.7% 的随机缺失（MAR）。
*   **Benchmark 与对比方法**：
    *   **传统统计方法**：均值/中位数插补、线性插值、KNN、季节性分解、最近观测值携带法（LOCF）。
    *   **先进模型**：标准 MICE、基于深度学习的循环神经网络插补模型 **BRITS**。
*   **评估指标**：归一化均方根误差（NRMSE）和归一化平均绝对误差（NMAE）。

#### 4. 资源与算力
*   论文提到实验运行在悉尼新南威尔士大学（UNSW）的 **Katana 高性能计算（HPC）集群**上。
*   文中明确指出对比方法 **BRITS 使用了 GPU** 进行加速。
*   **具体型号与时长**：文中未详细列出具体的 GPU 型号（如 A100 或 V100）以及确切的训练/采样总时长，但提到 MCMC 采样每条链运行了 30,000 次迭代。

#### 5. 实验数量与充分性
*   **实验规模**：针对每个数据集和每个变量，所有实验均独立运行了 **30 次**，并报告了均值和标准差，这保证了结果的统计显著性。
*   **消融实验**：对比了均值初始化（V1）与时间感知初始化（V2）的效果，验证了初始化策略对稳定性的贡献。
*   **充分性评价**：实验覆盖了环境和医疗两个截然不同的领域，涵盖了 MCAR 和 MAR 两种缺失机制，对比了从简单统计到深度学习的多种基准，实验设计较为全面、客观且公平。

#### 6. 主要结论与发现
*   **精度提升**：Bayes-MICE 在所有变量上的 NRMSE 和 NMAE 均显著低于标准 MICE 和 BRITS。
*   **不确定性量化**：Bayes-MICE 能够生成 95% 置信区间，且区间宽度随数据波动和测试阶段的预测难度动态调整，提供了更可靠的误差度量。
*   **采样器对比**：MALA 采样器在收敛速度和有效样本量（ESS）上显著优于 RWM，尤其是在处理复杂时序信号时表现更稳健。
*   **稳定性**：Bayes-MICE 在 30 次运行中的标准差较小，显示出比深度学习方法（BRITS）更高的鲁棒性。

#### 7. 优点
*   **理论严谨**：将 MICE 从启发式迭代提升到了完整的贝叶斯推理框架，解决了参数不确定性传播的问题。
*   **时序友好**：通过时滞特征和时间感知初始化，成功修复了传统 MICE 无法处理序列依赖的缺陷。
*   **可解释性强**：相比深度学习“黑箱”，贝叶斯模型提供了清晰的参数后验分布和预测区间。

#### 8. 不足与局限
*   **计算开销**：MCMC 采样过程（尤其是 30,000 次迭代）比传统 MICE 或简单的插值法耗时更长，在大规模高维数据集上的扩展性可能受限。
*   **变量类型限制**：目前的研究主要集中在**连续变量**，尚未扩展到分类变量或混合类型数据的处理。
*   **独立性假设**：虽然在 MICE 框架下考虑了其他变量作为预测因子，但本质上仍是逐个变量建模，未实现完全的联合多变量时序建模（如贝叶斯状态空间模型）。
*   **缺失模式**：实验主要针对 MCAR 和 MAR，对于更具挑战性的非随机缺失（MNAR）场景探讨较少。

（完）
