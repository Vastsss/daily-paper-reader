---
title: Physics Informed Reinforcement Learning with Gibbs Priors for Topology Control in Power Grids
title_zh: 基于吉布斯先验的物理信息强化学习用于电网拓扑控制
authors: "Pantelis Dogoulis, Maxime Cordy"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01830v1"
tags: ["query:llm"]
score: 8.0
evidence: 物理信息强化学习用于电网拓扑控制
tldr: 针对电网拓扑控制中动作空间巨大且仿真成本高的问题，本文提出一种结合物理信息的强化学习框架。该方法利用半马尔可夫控制仅在危险状态下决策，并通过图神经网络代理模型构建Gibbs先验，筛选候选动作并重权化策略概率。实验表明，该方法在保持高性能的同时显著降低了计算开销，在多个基准测试中优于传统PPO和工程基准，实现了控制质量与效率的平衡。
selection_source: fresh_fetch
motivation: 电网拓扑控制面临动作空间组合爆炸和在线仿真评估耗时长的双重挑战。
method: 结合半马尔可夫决策过程与基于图神经网络的Gibbs先验，通过物理信息引导动作筛选与策略优化。
result: 在多个现实基准测试中，该方法在计算速度提升数倍至数百倍的同时，达到了接近专家水平的控制性能。
conclusion: 该框架为电网拓扑控制提供了一种兼顾控制质量与计算效率的有效机制。
---

## 摘要
电网运行的拓扑控制是一个具有挑战性的序贯决策问题，因为动作空间随电网规模呈组合式增长，且通过仿真进行动作评估的计算成本极高。我们提出了一种物理信息强化学习框架，该框架将半马尔可夫控制与动作空间上的吉布斯先验相结合，其中吉布斯先验编码了系统的物理特性。仅当电网进入危险状态时才进行决策，同时利用图神经网络代理模型预测可行拓扑动作执行后的过载风险。这些预测被用于构建物理信息吉布斯先验，该先验既能筛选出一个较小的依赖于状态的候选动作集，又能在动作选择前对策略逻辑值（logits）进行重加权。通过这种方式，我们的方法在保留学习策略灵活性的同时，降低了探索难度和在线仿真成本。我们在三个难度递增的现实基准环境中评估了该方法。在所有设置中，所提方法在控制质量和计算效率之间实现了强大的平衡：在第一个基准测试中，它在达到专家级性能的同时速度快了约 6 倍；在第二个基准测试中，它达到了专家奖励的 94.6%，而决策时间缩短了约 200 倍；在最具挑战性的基准测试中，相比 PPO 基准，其奖励提升了高达 255%，存活步数提升了 284%，同时比强大的专业工程基准快约 2.5 倍。这些结果表明，我们的方法为电网拓扑控制提供了一种有效的机制。

## Abstract
Topology control for power grid operation is a challenging sequential decision making problem because the action space grows combinatorially with the size of the grid and action evaluation through simulation is computationally expensive. We propose a physics-informed Reinforcement Learning framework that combines semi-Markov control with a Gibbs prior, that encodes the system's physics, over the action space. The decision is only taken when the grid enters a hazardous regime, while a graph neural network surrogate predicts the post action overload risk of feasible topology actions. These predictions are used to construct a physics-informed Gibbs prior that both selects a small state-dependent candidate set and reweights policy logits before action selection. In this way, our method reduces exploration difficulty and online simulation cost while preserving the flexibility of a learned policy. We evaluate the approach in three realistic benchmark environments of increasing difficulty. Across all settings, the proposed method achieves a strong balance between control quality and computational efficiency: it matches oracle-level performance while being approximately $6\times$ faster on the first benchmark, reaches $94.6\%$ of oracle reward with roughly $200\times$ lower decision time on the second one, and on the most challenging benchmark improves over a PPO baseline by up to $255\%$ in reward and $284\%$ in survived steps while remaining about $2.5\times$ faster than a strong specialized engineering baseline. These results show that our method provides an effective mechanism for topology control in power grids.

---

## 论文详细总结（自动生成）

以下是对论文《Physics Informed Reinforcement Learning with Gibbs Priors for Topology Control in Power Grids》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：电网拓扑控制（如开关变电站总线配置）面临两个主要挑战：一是**动作空间巨大**，随电网规模呈组合爆炸式增长；二是**评估成本高昂**，依赖电力流仿真来验证动作安全性非常耗时。
*   **研究背景**：现代电网运行接近热极限，局部过载易引发连锁故障。强化学习（RL）虽有潜力，但由于动作空间过大，纯端到端的 RL 难以有效探索，且在线决策时频繁调用仿真器会导致响应过慢。

### 2. 核心方法论
论文提出了一种结合物理信息的强化学习框架，核心步骤如下：
*   **危险门控半马尔可夫过程（Hazard-gated semi-MDP）**：智能体并非每步都行动，仅当系统进入“危险状态”（如线路最大负载率超过阈值 $\delta_h$）时才触发决策。这减少了不必要的干预并简化了决策序列。
*   **图神经网络（GNN）代理模型**：采用改进的 KCLNet 架构，以电网拓扑为图输入，预测执行特定动作后的“一步风险”（即预测动作后的最大负载率）。
*   **物理信息吉布斯先验（Gibbs Prior）**：
    *   利用 GNN 预测的风险值构建吉布斯分布：$p_{phys} \propto \exp(- \frac{1}{\tau} \text{Risk})$。
    *   **动作筛选**：根据先验分数选取前 $K$ 个最优候选动作（Top-K），将搜索空间从数万缩减至几十或几百。
    *   **策略重权化**：将 RL 策略生成的 Logits 与物理先验分数线性结合，引导智能体选择物理上更安全的动作。
*   **训练流程**：分为两个阶段。第一阶段通过仿真数据训练 GNN 风险代理模型；第二阶段冻结代理模型，使用 PPO 算法在缩减后的动作空间上训练策略网络。

### 3. 实验设计
*   **实验场景**：基于 `Grid2Op` 框架，使用了三个难度递增的 IEEE 标准改进模型：
    1.  **case14**：小型电网，209 个动作。
    2.  **case36**：中型电网，包含维护约束，66,978 个动作。
    3.  **case118**：大型电网（模拟 2035 年法国电网），包含可再生能源波动、储能和对手攻击，73,357 个动作。
*   **对比基准（Benchmarks）**：
    *   **Random**：随机动作。
    *   **PPO**：标准强化学习算法。
    *   **Greedy Oracle**：通过穷举仿真评估所有动作的“贪婪专家”。
    *   **LJN (topology-only)**：针对 case118 的强力工程基准（2023 L2RPN 竞赛优胜方案的变体）。
*   **评估指标**：平均奖励、平均存活步数、单步决策耗时（ms/step）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。
*   **效率体现**：论文重点强调了推理阶段的效率提升，例如在 case36 中比 Oracle 快 200 倍。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同规模的基准环境上进行了测试，涵盖了从简单拓扑到包含攻击和维护的复杂场景。
*   **充分性评价**：实验设计较为充分，通过与“贪婪专家”和“竞赛级工程方案”对比，证明了方法在不同规模下的泛化能力。然而，文中缺乏对超参数（如 $K$ 值大小、温度参数 $\tau$）的详细消融实验分析。

### 6. 主要结论与发现
*   **性能与效率的平衡**：该方法在小型电网（case14）上达到了专家级性能且速度快 6 倍；在中型电网（case36）上达到了专家 94.6% 的表现且速度快 200 倍。
*   **大型电网的优越性**：在最难的 case118 中，该方法比 PPO 奖励提升了 255%，存活步数提升 284%，且比复杂的 LJN 基准快 2.5 倍。
*   **物理先验的作用**：物理信息先验能有效引导 RL 避开高风险动作，显著提升了在大规模离散动作空间中的学习效率。

### 7. 优点
*   **物理启发式设计**：通过 Gibbs 先验将电网物理特性（负载风险）直接引入 RL 决策过程，而非仅依赖黑盒学习。
*   **计算高效性**：通过 Top-K 筛选和半 MDP 建模，极大地降低了在线决策时的计算开销，具有实际部署潜力。
*   **通用性**：方法不依赖于特定电网结构，在三个不同规模的环境中均表现出良好的适应性。

### 8. 不足与局限
*   **动作预算限制**：实验中限制每次干预仅执行单个动作，而实际运行中可能需要多个并发动作（如 case118 差距的部分原因）。
*   **代理模型精度依赖**：性能高度依赖于 GNN 风险预测的准确性，若代理模型在极端分布外数据上失效，可能导致错误决策。
*   **超参数敏感性**：Top-K 的 $K$ 值选取和先验权重 $\beta$ 可能需要针对不同电网进行精细调优，文中未给出通用的调优指导。

（完）
