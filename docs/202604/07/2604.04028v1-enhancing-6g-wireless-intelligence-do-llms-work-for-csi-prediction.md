---
title: "Enhancing 6G Wireless Intelligence: Do LLMs Work for CSI Prediction?"
title_zh: 增强6G无线智能：大语言模型（LLM）在CSI预测中是否有效？
authors: "Mohsen Kazemian, Jürgen Jasperneite"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04028v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 基于大语言模型的信道状态信息 (CSI) 预测框架
tldr: 针对6G高移动性场景下信道状态信息（CSI）预测难题，本文提出一种基于大语言模型（LLM）的物理感知预测框架。该方法利用LLM强大的序列建模能力，并结合最大多普勒频率等物理描述符，学习OTFS系统中信道系数的时变规律。仿真结果表明，在100-500 km/h的高速环境下，该框架的预测精度显著优于传统深度学习模型，证明了物理知识与LLM结合在无线通信领域的巨大潜力。
selection_source: fresh_fetch
motivation: 传统导频估计方法在6G高移动性场景下存在信息滞后或开销过大的问题，亟需更高效的信道预测技术。
method: 提出一种物理感知的LLM预测框架，将移动性相关的物理描述符与历史信道观测数据结合，利用LLM进行序列建模。
result: 在100至500 km/h的移动速度下，该方法的归一化均方误差（NMSE）一致低于传统深度学习模型和不含物理描述符的LLM模型。
conclusion: 将物理信道知识与LLM的序列建模能力相结合，能有效提升高动态OTFS系统的信道预测准确性。
---

## 摘要
在高移动性6G场景中，快速时变的信道导致相干时间非常短，这使得传统的基于导频的信道状态信息（CSI）估计方法容易出现信息过时或导频开销过大的问题。因此，信道预测在此类动态无线系统中变得至关重要。为了应对这一挑战，大语言模型（LLM）作为新兴的学习框架，因其强大的序列建模能力和跨环境泛化能力，近期在CSI预测领域引起了关注。本文提出了一种基于LLM的框架，用于高移动性正交时频空（OTFS）通信系统中的信道预测。在这项工作中，我们开发了一种物理感知的基于LLM的预测器，该预测器从历史信道观测中学习OTFS信道系数的时间演化，同时结合了与移动性相关的物理描述符（例如最大多普勒频率），以在快速时变环境中实现对未来信道状态的准确预测。通过在100至500 km/h的用户速度下进行的大量仿真，评估了所提框架的有效性。数值结果表明，与经典的深度学习预测器以及不含物理信道描述符的基于LLM的预测器相比，所提方法始终能获得更低的归一化均方误差（NMSE）。这些结果证明了在高度动态的OTFS系统中，将移动性相关的信道知识与基于LLM的序列建模相结合进行信道预测的优势。

## Abstract
In high-mobility 6G scenarios, rapidly time-varying channels lead to very short coherence times, which makes conventional pilot-based channel state information (CSI) estimation approaches prone to outdated information or excessive pilot overhead. Therefore, channel prediction becomes essential in such dynamic wireless systems. To address this challenge, large language models (LLMs) are emerging learning frameworks that have recently attracted attention for CSI prediction due to their strong sequence modeling capability and ability to generalize across different environments. This paper proposes an LLM-based framework for channel prediction in high-mobility orthogonal time frequency space (OTFS) communication systems. In this work, we develop a physics-aware LLM-based predictor that learns the temporal evolution of OTFS channel coefficients from historical channel observations while incorporating mobility-related physical descriptors (e.g., maximum Doppler frequency) to achieve accurate prediction of future channel states in rapidly time-varying environments. The effectiveness of the proposed framework is evaluated through extensive simulations under user velocities ranging from 100 to 500 km/h. Numerical results show that the proposed method consistently achieves lower normalized mean square error (NMSE) compared with both classical deep learning predictors and LLM-based predictors without physical channel descriptors. These results demonstrate the advantage of integrating mobility-related channel knowledge with LLM-based sequence modeling for channel prediction in highly dynamic OTFS systems.

---

## 论文详细总结（自动生成）

以下是对论文《Enhancing 6G Wireless Intelligence: Do LLMs Work for CSI Prediction?》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在6G高移动性场景（如高铁、无人机、低轨卫星）中，无线信道随时间极速变化，导致信道相干时间极短。传统的基于导频的信道状态信息（CSI）估计方法面临“导频开销过大”或“信息过时（信道老化）”的矛盾。
*   **研究动机**：
    *   **OTFS技术的兴起**：正交时频空（OTFS）调制在时延-多普勒（DD）域处理信号，比传统OFDM更适合高移动性环境。
    *   **LLM的潜力**：大语言模型（LLM）在处理长程序列依赖和跨域泛化方面表现卓越，研究者试图探索其是否能有效预测复杂的非线性信道演化。
    *   **物理感知的缺失**：现有的AI预测方法往往纯靠数据驱动，忽略了移动速度、多普勒频率等关键物理参数的指导作用。

### 2. 论文提出的方法论
论文提出了一种**物理感知的基于LLM的OTFS信道预测框架**，核心流程如下：
*   **核心思想**：将历史信道观测数据与物理动态描述符（如最大多普勒频率）共同输入预训练的LLM，利用LLM的序列建模能力预测未来信道系数。
*   **关键技术细节**：
    1.  **预处理模块**：将复数信道系数拆分为实部和虚部，进行归一化处理，并与物理描述符（Doppler/SNR）对齐。
    2.  **特征嵌入（Embedding）**：通过全连接层将信道序列和物理描述符映射到统一的嵌入空间，并加入位置编码（Positional Encoding）以保留时间顺序。
    3.  **LLM主干网络**：采用预训练的**GPT-2**架构。在训练时冻结大部分参数，仅微调层归一化（Layer Norm）和嵌入层，以保留其通用的序列建模能力并适应通信任务。
    4.  **输出模块**：将LLM输出的特征映射回目标维度，重构为复数信道系数。
*   **算法流程**：输入过去 $N_P$ 帧的信道矩阵和物理参数 $\rightarrow$ 特征融合 $\rightarrow$ LLM处理 $\rightarrow$ 预测未来 $N_F$ 帧的信道状态。

### 3. 实验设计
*   **场景与数据集**：使用 **QuaDRiGa** 仿真器生成符合3GPP UMa（城市宏站）标准的非视距（NLOS）时变信道数据。
*   **系统参数**：载波频率3GHz，子载波间隔15kHz，OTFS参数为 $M=64$（时延轴）和 $N=16$（多普勒轴）。
*   **对比基准（Benchmarks）**：
    *   **传统深度学习模型**：LSTM、Transformer。
    *   **消融模型（Ablation）**：不带物理描述符的纯LLM预测器（类似于现有的LLM4CP方法）。
    *   **变体对比**：带SNR描述符的LLM vs 带多普勒描述符的LLM。
*   **测试变量**：用户移动速度（100 km/h 至 500 km/h）、预测步长（Prediction Horizon）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的GPU型号、数量、训练时长或内存消耗。
*   **模型规模**：提到了使用GPT-2作为主干，并采用了参数高效的微调策略（仅微调一小部分参数），暗示了其对算力有一定要求，但通过微调降低了训练负担。

### 5. 实验数量与充分性
*   **实验规模**：生成了10,000个样本（8k训练，1k验证，1k测试）。
*   **实验维度**：
    *   针对不同速度（100-500 km/h）进行了NMSE（归一化均方误差）性能评估。
    *   针对预测步长（1-10帧）进行了鲁棒性测试。
    *   进行了物理描述符的对比实验（Doppler vs SNR）。
*   **充分性评价**：实验设计较为客观，涵盖了极高移动性场景，且通过消融实验清晰地展示了物理参数对LLM预测性能的增益。但实验主要基于仿真数据，缺乏真实场景数据的验证。

### 6. 主要结论与发现
*   **LLM有效性**：LLM在CSI预测任务中表现优异，其强大的序列建模能力能够捕捉信道的非线性动态。
*   **物理感知的关键性**：引入**多普勒频率**作为辅助输入显著提升了预测精度。在450 km/h时，物理感知LLM的NMSE约为 $4 \times 10^{-3}$，而无物理信息的模型NMSE高达 $4 \times 10^{-2}$。
*   **参数选择**：多普勒频率比SNR更有效，因为多普勒直接反映了信道随时间演化的物理本质，而SNR主要反映噪声水平。
*   **长程预测**：随着预测步长增加，所有模型性能均下降，但所提方法在长步长下依然保持了领先的稳定性。

### 7. 优点
*   **跨学科融合**：成功将LLM的通用序列处理能力与无线通信的物理特性（Doppler）相结合。
*   **高移动性支持**：专门针对500 km/h的极端场景设计，填补了OTFS系统下LLM应用的研究空白。
*   **参数高效**：通过微调预训练模型而非从头训练，利用了LLM已有的模式识别能力，提高了学习效率。

### 8. 不足与局限
*   **实时性挑战**：LLM（即使是GPT-2）的推理延迟对于微秒级的无线信道处理可能仍然过高，文中未讨论推理时延。
*   **数据依赖**：高度依赖仿真生成的理想数据，实际环境中多普勒频率的估计误差可能会影响预测效果。
*   **复杂度**：相比于轻量级的线性预测器或小型CNN/RNN，LLM的存储和计算开销在基站或终端侧部署时可能面临限制。
*   **场景单一**：目前仅验证了单用户场景，未讨论多用户干扰及更复杂的异构网络环境。

（完）
