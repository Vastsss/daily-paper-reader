---
title: A Family of Open Time-Series Foundation Models for the Radio Access Network
title_zh: 无线接入网的一系列开源时间序列基础模型
authors: "Ioannis Panitsas, Leandros Tassiulas"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04271v1"
tags: ["query:ts-fm"]
score: 9.0
evidence: 用于无线接入网任务的时间序列基础模型
tldr: 针对无线接入网（RAN）中AI模型碎片化和泛化能力差的问题，本文提出了TimeRAN，一个统一的多任务时间序列基础模型框架。通过在包含0.56B测量数据的大规模开源数据集TimeRAN DataPile上进行预训练，该模型能高效适配异常检测、预测等多种任务。实验证明TimeRAN在极少微调下即可达到SOTA性能，并在5G测试床中验证了其实际运行的高效性。
selection_source: fresh_fetch
motivation: 现有的RAN智能模型多为特定任务设计，导致模型碎片化、知识共享受限且系统复杂度高。
method: 提出了TimeRAN框架，利用轻量级时间序列基础模型和少量任务特定头，通过大规模预训练学习可迁移的特征表示。
result: 在异常检测、分类、预测和填补等任务中均达到最先进水平，且仅需极少甚至无需特定任务的微调。
conclusion: TimeRAN证明了基础模型在RAN领域的有效性，为构建高效、通用的无线网络智能提供了新范式。
---

## 摘要
无线接入网 (RAN) 正在演进为一种可编程且解耦的基础设施，越来越依赖 AI 原生算法进行优化和闭环控制。然而，目前的 RAN 智能在很大程度上仍由针对特定功能的任务专用模型构建，导致了模型碎片化、跨任务知识共享受限、泛化能力差以及系统复杂度增加。为了解决这些局限性，我们推出了 TimeRAN，这是一个用于 RAN 时间序列建模的统一多任务学习框架。TimeRAN 利用一个带有少量任务专用头的轻量级时间序列基础模型，来学习可迁移的表示，这些表示可以在有限的监督下高效地适配到各种任务中。为了实现大规模预训练，我们进一步整理并开源了 TimeRAN DataPile，这是迄今为止用于 RAN 分析的最大时间序列语料库，包含超过 35.5 万条时间序列和 5.6 亿次测量，涵盖了多种遥测源、协议层和部署场景。我们在包括异常检测、分类、预测和填补在内的一系列全面的 RAN 分析任务中对 TimeRAN 进行了评估，结果表明，在极少或无需任务专用微调的情况下，它就能达到最先进的性能。最后，我们将 TimeRAN 集成到一个概念验证的 5G 测试平台中，并证明它在真实场景中能够以有限的资源需求高效运行。

## Abstract
The Radio Access Network (RAN) is evolving into a programmable and disaggregated infrastructure that increasingly relies on AI-native algorithms for optimization and closed-loop control. However, current RAN intelligence is still largely built from task-specific models tailored to individual functions, resulting in model fragmentation, limited knowledge sharing across tasks, poor generalization, and increased system complexity. To address these limitations, we introduce TimeRAN, a unified multi-task learning framework for time-series modeling in the RAN. TimeRAN leverages a lightweight time-series foundation model with few task-specific heads to learn transferable representations that can be efficiently adapted across diverse tasks with limited supervision. To enable large-scale pretraining, we further curate and open-source TimeRAN DataPile, the largest time-series corpus for RAN analytics to date, comprising over 355K time series and 0.56B measurements across diverse telemetry sources, protocol layers, and deployment scenarios. We evaluate TimeRAN across a comprehensive set of RAN analytics tasks, including anomaly detection, classification, forecasting, and imputation, and show that it achieves state-of-the-art performance with minimal or no task-specific fine-tuning. Finally, we integrate TimeRAN into a proof-of-concept 5G testbed and demonstrate that it operates efficiently with limited resource requirements in real-world scenarios.

---

## 论文详细总结（自动生成）

### 论文总结：TimeRAN —— 无线接入网（RAN）的开源时间序列基础模型

#### 1. 核心问题与整体含义（研究动机和背景）
随着 6G 演进，无线接入网（RAN）正向解耦、可编程的 AI-RAN 架构转变。然而，当前的 RAN 智能面临三大挑战：
*   **模型碎片化**：针对特定任务（如异常检测、负载预测）开发独立模型，导致系统复杂度高、维护成本大。
*   **泛化能力差**：特定环境训练的模型难以迁移至流量模式或信道条件不同的新基站。
*   **资源受限与数据匮乏**：边缘侧算力有限，且高质量、大规模的公开 RAN 遥测数据集极度稀缺。
**核心目标**：构建一个统一的、轻量级的时间序列基础模型框架，通过大规模预训练实现“一个编码器适配多种任务”，降低部署门槛并提升泛化性能。

#### 2. 方法论：核心思想与技术细节
TimeRAN 采用基于 Transformer 的多任务学习架构，核心流程如下：
*   **多通道分片（Multi-channel Patching）**：将多变量遥测数据 $X \in \mathbb{R}^{C \times T}$ 切分为固定长度 $P$ 的时间片（Patches），减少输入序列长度并捕捉局部语义。
*   **投影与位置编码**：通过线性层将分片映射到 $d$ 维潜在空间，并加入正弦绝对位置编码以保留时间顺序。
*   **Transformer 编码器主干**：采用轻量级 Transformer 架构（分为 Small, Base, Large 三种规模），利用自注意力机制建模跨通道相关性和长程时间依赖。
*   **自监督预训练**：在预训练阶段，激活掩码模块（Masking），随机遮盖 30% 的分片，通过重建原始序列（最小化 MSE 损失）来学习通用的特征表示。
*   **任务特定头（Task-Specific Heads）**：在冻结或微调主干的基础上，连接轻量级线性层，支持异常检测、分类、预测和填补四类下游任务。

#### 3. 实验设计
*   **数据集（TimeRAN DataPile）**：作者整理并开源了目前最大的 RAN 时间序列语料库，包含 35.5 万条序列、5.6 亿个测量值（约 29GB），涵盖 5G 协议栈各层。
*   **Benchmark 任务**：
    *   **异常检测**：对比 Autoformer, Informer, TimesNet 等。
    *   **分类**：对比 1D-CNN, LSTM, MOMENT 等。
    *   **预测**：对比统计模型（ARIMA, ETS）及深度学习模型。
    *   **填补**：对比均值填补、线性插值等传统方法。
*   **评估模式**：包括零样本推理（Zero-shot）、线性探测（Linear Probing，仅训练头）和全微调（Full Fine-tuning）。

#### 4. 资源与算力
*   **硬件环境**：实验使用了配备 NVIDIA RTX 3090、RTX 4090 和 RTX A6000 GPU 的服务器。
*   **模型规模**：提供了三种变体：Small (40M参数)、Base (125M参数)、Large (385M参数)。
*   **训练细节**：使用 Adam 优化器，采用混合精度训练（bfloat16/float32）和梯度检查点技术以优化内存。
*   **说明**：文中明确给出了推理延迟（在 A6000 上约为 30-120ms），但**未明确说明总的预训练时长（小时/天）**，仅提到预训练进行了一个 Epoch。

#### 5. 实验数量与充分性
*   **实验规模**：在 25 个不同的数据集上进行了广泛测试，涵盖了从实验室测试床到真实运营商网络的多种场景。
*   **充分性**：实验设计较为全面，不仅对比了 SOTA 时间序列模型，还进行了跨域泛化实验（如在 WiFi、蓝牙数据上测试）。
*   **客观性**：采用了标准的 Adjusted F1、MSE、MAE 等指标，并提供了 5G 真实测试床（基于 OAI 和 USRP）的过空（Over-the-air）验证，增强了结果的说服力。

#### 6. 主要结论与发现
*   **SOTA 性能**：TimeRAN 在所有下游任务中均达到或超过了现有专用模型的水平，尤其是在线性探测模式下表现强劲。
*   **强大的泛化性**：模型能够学习到可分离的特征表示，在未见过的无线环境（如非 RAN 领域的 WiFi 信号）中依然能实现高精度的零样本填补。
*   **高效性**：Base 版本模型（125M）在 GPU 上的推理延迟极低，且显存占用仅约 1.1GB，证明了其在边缘侧部署的可行性。

#### 7. 优点
*   **开源贡献**：提供了大规模的 TimeRAN DataPile 数据集和预训练权重，填补了电信领域基础模型的空白。
*   **统一架构**：成功验证了“一个编码器适配多个 RAN 任务”的可行性，显著降低了网络运维的复杂度。
*   **轻量化设计**：相比于动辄数十亿参数的 LLM，TimeRAN 的规模更适合对时延敏感的电信边缘计算场景。

#### 8. 不足与局限
*   **异常检测阈值**：文中提到异常检测依赖重建误差，但未深入讨论在动态网络环境下如何自动设定最优阈值。
*   **PEFT 效果有限**：作者提到尝试过 LoRA 等参数高效微调技术，但效果不如线性探测或全微调，这反映出时间序列模型在适配策略上可能与 NLP 模型存在差异。
*   **长序列挑战**：虽然引入了 Patching，但在极长序列预测任务中，性能随预测窗口增加仍有明显下降。
*   **应用限制**：目前主要针对非实时（Non-RT）或近实时（Near-RT）任务，对于物理层极低时延（微秒级）的控制任务尚待验证。

（完）
