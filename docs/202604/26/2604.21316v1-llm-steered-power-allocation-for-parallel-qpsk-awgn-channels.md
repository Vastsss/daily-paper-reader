---
title: LLM-Steered Power Allocation for Parallel QPSK-AWGN Channels
title_zh: 面向并行 QPSK-AWGN 信道的 LLM 引导功率分配
authors: Tadashi Wadayama
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21316v1"
tags: ["query:llm"]
score: 9.0
evidence: LLM引导的功率分配与决策支持
tldr: 本研究针对LLM在闭环控制中的安全性挑战，提出一种用于并行信道功率分配的双过程架构。该架构由数值优化器负责底层计算，LLM导航器负责解析自然语言策略并调整优化权重。通过多层护栏机制，LLM在不直接操作变量的情况下实现了安全引导。实验表明，该系统能灵活响应多种指令，并在信道突变时显著提升自适应性能，为通信系统的安全重构提供了新思路。
selection_source: fresh_fetch
motivation: 解决大语言模型在闭环控制系统中因随机性导致的安全性挑战，实现通信系统功率分配的灵活重构。
method: 提出一种借鉴双系统理论的架构，由数值优化器执行底层计算，LLM作为导航器解析自然语言策略并调整优化参数。
result: "实验显示系统能根据自然语言指令实现多种运行模式，并在信道增益反转时将互信息偏差降低了60%。"
conclusion: LLM可作为安全且灵活的策略解释器，在无需重新编写控制器代码的情况下实现通信优化器的动态重构。
---

## 摘要
大语言模型（LLM）正越来越多地被探索作为闭环系统中的高级决策模块，但其随机性使得安全集成面临挑战。在本文中，我们提出了一种受卡尼曼（Kahneman）系统 1/系统 2 框架启发的并行 QPSK 信道双过程架构——LLM 引导的功率分配。快速数值优化器（系统 1）对加权互信息目标持续执行投影梯度上升，而 LLM 导航器（系统 2）则定期解释自然语言策略，并仅更新信道权重和运行功率预算。LLM 从不直接操作功率分配变量，约束条件的满足由优化器在结构上强制执行。为了缓解 LLM 的不可靠性，我们进一步整合了多层护栏，包括归一化、指数移动平均平滑以及回退机制。在 8 信道系统上的数值实验表明，在优化核心固定且系统提示词不变的情况下，不同的自然语言策略会诱导出性质迥异的工作点，包括吞吐量导向分配、信道优先级排序、功率感知运行以及信道关闭。此外，在信道增益发生突变反转的情况下，所提系统能够自主重新配置其引导信号，与仅使用优化器相比，最终互信息分布的离散度降低了 60%。这些结果表明，LLM 可以作为策略解释器，在无需重新实现控制器的情况下，实现通信系统优化器安全、灵活的重新配置。

## Abstract
Large language models (LLMs) are increasingly being explored as high-level decision modules in closed-loop systems, but their stochastic nature makes safe integration challenging. In this paper, we propose LLM-Steered Power Allocation, a dual-process architecture for parallel QPSK channels inspired by Kahneman's System 1/System 2 framework. A fast numerical optimizer (System 1) continuously performs projected gradient ascent on a weighted mutual-information objective, while an LLM navigator (System 2) periodically interprets natural-language policies and updates only the channel weights and the operational power budget. The LLM never manipulates the power-allocation variables directly, and constraint satisfaction is enforced structurally by the optimizer. To mitigate LLM unreliability, we further incorporate multi-layer guardrails including normalization, exponential moving-average smoothing, and fallback mechanisms. Numerical experiments on an 8-channel system show that, with a fixed optimization core and unchanged system prompt, different natural-language policies induce qualitatively different operating points, including throughput-oriented allocation, channel prioritization, power-aware operation, and channel shutdown. In addition, under an abrupt channel-gain reversal, the proposed system autonomously reconfigures its steering signals and reduces the final mutual-information spread by 60% compared with the optimizer alone. These results suggest that LLMs can serve as policy interpreters for safe, flexible reconfiguration of communication-system optimizers without controller reimplementation.

---

## 论文详细总结（自动生成）

这篇论文提出了一种将大语言模型（LLM）集成到无线通信功率分配系统中的新框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在利用 LLM 的高级推理和自然语言处理能力的同时，克服其固有的随机性和不可靠性，确保其在闭环控制系统中不会违反物理约束（如功率限制）。
*   **研究动机**：传统的通信优化器（如注水算法）虽然高效，但缺乏语义理解能力，难以根据复杂的业务需求（如“优先保障特定信道”或“在满足速率前提下节能”）灵活重构。而直接让 LLM 输出控制变量容易导致系统崩溃或违反约束。

### 2. 方法论：双过程架构（System 1 / System 2）
论文借鉴了卡尼曼的认知心理学理论，构建了一个双层架构：
*   **系统 1（快速循环/优化引擎）**：
    *   **功能**：负责毫秒级的数值优化。
    *   **技术细节**：使用**投影梯度上升（Projected Gradient Ascent）**算法，通过自动微分技术最大化“加权互信息（MI）”目标函数。
    *   **约束强制**：在每一步迭代中，通过投影算子强制满足总功率约束，确保物理安全性。
*   **系统 2（慢速循环/LLM 导航器）**：
    *   **功能**：负责秒级的策略解释。
    *   **技术细节**：LLM 定期读取系统状态（信道增益、当前速率等），解析用户的**自然语言策略**，并输出两个控制参数：信道权重向量 $w$ 和运行功率预算 $P_{total}$。
    *   **间接控制**：LLM 不直接操作具体的功率分配值，而是通过修改优化器的“目标”和“边界”来间接引导系统。
*   **多层护栏机制**：
    *   **结构安全**：物理约束由系统 1 保证。
    *   **后处理**：对 LLM 输出进行归一化、范围裁剪。
    *   **平滑处理**：使用指数移动平均（EMA）滤除 LLM 输出的瞬时波动。
    *   **回退机制**：若 LLM 响应失败，系统沿用上一时刻参数，实现平滑降级。

### 3. 实验设计
*   **场景设置**：一个包含 8 个并行子信道的 QPSK-AWGN 系统。
*   **对比基准（Benchmarks）**：
    1.  **B0（仅系统 1）**：固定均等权重，不使用 LLM。
    2.  **B1（注水算法）**：基于高斯输入假设的传统闭式解（在离散星座图下通常次优）。
*   **实验内容**：
    *   **策略有效性测试**：测试四种不同指令（最大化吞吐量、优先特定信道、最小化功率、关闭弱信道）。
    *   **稳态恢复（Homeostasis）实验**：在运行中途突然反转所有信道的增益，观察系统能否自主调整。

### 4. 资源与算力
*   **模型**：使用了本地部署的 200 亿参数开源模型 **GPT-OSS-20B**。
*   **推理环境**：通过 LM Studio 提供服务，使用 OpenAI 兼容的 API 进行异步调用。
*   **算力细节**：文中未明确给出具体的 GPU 型号、数量或训练时长，但强调了 LLM 运行在秒级尺度，与毫秒级的优化器解耦。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 种典型策略进行了时间序列轨迹分析，并进行了一次极端的信道环境突变实验。
*   **充分性评价**：实验设计较为客观，涵盖了从静态策略执行到动态环境自适应的多个维度。虽然 8 信道的规模较小，但作为原理验证（Proof-of-Concept）足以证明 LLM 引导机制的有效性。

### 6. 主要结论与发现
*   **策略灵活性**：无需重新编写任何代码，仅通过更改自然语言指令，系统就能在吞吐量优先、公平性优先和节能模式之间切换。
*   **性能提升**：在信道增益反转的极端情况下，LLM 引导的系统相比固定权重的优化器，将各信道间的互信息偏差（Spread）降低了 **60%**。
*   **解释性**：LLM 输出的“推理过程（Reasoning）”字段为人类操作员提供了决策依据，增强了系统的透明度。

### 7. 优点
*   **安全性高**：通过“间接控制”和“物理投影”确保了 LLM 无论如何幻觉都不会导致功率超限。
*   **零样本适配**：展示了 LLM 作为“策略解释器”在无需重新训练或微调的情况下处理多样化任务的能力。
*   **架构优雅**：双过程架构解决了 LLM 推理延迟与实时通信控制之间的矛盾。

### 8. 不足与局限
*   **规模限制**：实验仅限于 8 个并行信道，尚未在更大规模的网络（如多用户 MIMO 或大规模组网）中验证。
*   **调制模式单一**：目前仅针对 QPSK 进行了深入分析，虽然理论上可扩展，但更高阶调制下的计算复杂度会增加。
*   **LLM 成本**：尽管是异步调用，但在大规模部署时，频繁调用 LLM 的计算开销和能耗仍是实际应用中的障碍。

（完）
