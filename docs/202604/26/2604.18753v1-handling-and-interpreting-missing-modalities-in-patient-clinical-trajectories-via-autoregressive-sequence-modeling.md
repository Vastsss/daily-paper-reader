---
title: Handling and Interpreting Missing Modalities in Patient Clinical Trajectories via Autoregressive Sequence Modeling
title_zh: 通过自回归序列建模处理和解释患者临床轨迹中的缺失模态
authors: "Andrew Wang, Ellie Pavlick, Ritambhara Singh"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18753v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 利用大语言模型的因果解码器对患者多模态轨迹进行建模
tldr: 本研究针对医疗临床数据中模态缺失和时序稀疏的挑战，提出将临床诊断建模为自回归序列任务。通过引入缺失感知的对比预训练目标，利用大语言模型的因果解码器在共享潜空间中整合多模态轨迹。实验证明该方法在MIMIC-IV和eICU基准上优于现有模型，并通过可解释性分析揭示了预训练对缓解模态缺失影响的作用，为构建安全透明的临床AI提供了新框架。
selection_source: fresh_fetch
motivation: 旨在解决临床多模态数据中因模态缺失和时序稀疏性导致的预测性能下降及模型解释性不足的问题。
method: 采用基于Transformer的因果解码器进行自回归序列建模，并结合一种缺失感知的对比预训练目标来对齐多模态特征。
result: 在MIMIC-IV和eICU数据集上取得了超越基线的性能，并证明对比预训练能有效减少因模态缺失引起的模型行为分歧。
conclusion: 将临床诊断抽象为序列建模不仅提升了处理缺失模态的能力，还通过可解释性技术增强了临床AI的透明度与安全性。
---

## 摘要
在医疗保健领域开发多模态机器学习（ML）模型的一个活跃挑战是在训练和部署过程中处理缺失模态。由于临床数据集在模态存在方面具有固有的时间性和稀疏性，通过诊断性多模态机器学习模型捕捉潜在的预测信号，同时保持模型的可解释性，仍然是一个持续的挑战。在这项工作中，我们通过将临床诊断重新定义为自回归序列建模任务来解决这一问题，利用大语言模型（LLMs）中的因果解码器来建模患者的多模态轨迹。我们首先引入了一种缺失感知的对比预训练目标，该目标将具有缺失情况的数据集中的多种模态整合到一个共享的潜在空间中。然后，我们展示了基于 Transformer 架构的自回归序列建模在 MIMIC-IV 和 eICU 微调基准测试中优于基准模型。最后，我们利用可解释性技术超越性能提升，发现在不同的患者住院期间，移除模态会导致发散行为，而我们的对比预训练可以缓解这种行为。通过将临床诊断抽象为序列建模并解释患者住院轨迹，我们开发了一个框架来分析和处理缺失模态，同时满足了安全、透明的临床人工智能这一基本诉求。

## Abstract
An active challenge in developing multimodal machine learning (ML) models for healthcare is handling missing modalities during training and deployment. As clinical datasets are inherently temporal and sparse in terms of modality presence, capturing the underlying predictive signal via diagnostic multimodal ML models while retaining model explainability remains an ongoing challenge. In this work, we address this by re-framing clinical diagnosis as an autoregressive sequence modeling task, utilizing causal decoders from large language models (LLMs) to model a patient's multimodal trajectory. We first introduce a missingness-aware contrastive pre-training objective that integrates multiple modalities in datasets with missingness in a shared latent space. We then show that autoregressive sequence modeling with transformer-based architectures outperforms baselines on the MIMIC-IV and eICU fine-tuning benchmarks. Finally, we use interpretability techniques to move beyond performance boosts and find that across various patient stays, removing modalities leads to divergent behavior that our contrastive pre-training mitigates. By abstracting clinical diagnosis as sequence modeling and interpreting patient stay trajectories, we develop a framework to profile and handle missing modalities while addressing the canonical desideratum of safe, transparent clinical AI.

---

## 论文详细总结（自动生成）

这是一份关于论文《Handling and Interpreting Missing Modalities in Patient Clinical Trajectories via Autoregressive Sequence Modeling》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：临床数据具有天然的**多模态性**（影像、实验室检查、笔记）、**时序性**和**高度稀疏性**（模态缺失严重）。
*   **核心问题**：现有的临床预测模型多采用静态的“特征袋”（bag-of-features）方法，忽略了诊断过程中的动态推理。此外，处理缺失模态的传统方法（如插补或零填充）要么产生幻觉信号，要么破坏潜在空间的几何结构，且缺乏透明度，难以满足临床安全需求。
*   **整体含义**：本研究旨在将临床诊断重新定义为**自回归序列建模任务**，通过模拟临床医生不断更新信念的过程，在处理缺失数据的同时提供机械性的可解释性。

### 2. 方法论：核心思想与关键技术
该框架分为两个阶段：
*   **缺失感知对比预训练（Masked Global Alignment）**：
    *   **核心思想**：在共享潜空间中对齐所有可用模态，并显式学习“缺失标记”。
    *   **技术细节**：引入一组可学习的模态标记 $\{t_i\}$。若某模态缺失，则直接注入该标记而非零填充。
    *   **损失函数**：采用改进的 **InfoNCE 损失**。对于每个锚点模态，计算其与该患者其他所有可用模态的“互补质心”（Complementary Centroid）的对齐度。这种方法强制不同模态在潜空间中收敛到模态无关的区域。
*   **自回归序列建模**：
    *   **核心思想**：利用大语言模型（LLM）的因果解码器（Causal Decoder）处理按时间排序的临床事件流。
    *   **流程**：将多模态特征投影到 LLM 的隐藏维度，在每个临床事件后插入一个特殊的 `[PREDICT]` 标记。模型通过自回归方式，在每个时间步输出更新后的预测状态（如死亡风险）。

### 3. 实验设计
*   **数据集**：
    *   **MIMIC-IV & MIMIC-CXR**：包含时间序列、胸部X光、出院小结、放射报告和人口统计学数据。
    *   **eICU**：包含诊断、治疗、药物、实验室检查和急性生理评分。
*   **Benchmark 任务**：
    *   **表型识别（Phenotyping）**：25种急性/慢性病的多标签分类。
    *   **住院死亡率（In-Hospital Mortality）**：二分类任务。
    *   **住院时长（Length of Stay, LoS）**：连续回归任务。
*   **对比方法**：
    *   **静态基线**：多层感知机（MLP）、长短期记忆网络（LSTM）。
    *   **序列模型**：六种主流 LLM 解码器（Llama-3-8B, Mistral-7B, DeepSeek-7B, Phi-3-mini, BioMistral-7B, Meditron-7B），分别对比“从头训练”与“对比预训练初始化”的效果。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA RTX 3090 GPU**。
*   **技术优化**：采用 **4-bit 量化（NF4）** 加载 LLM 骨干网络，并使用 **LoRA（低秩自适应）** 进行参数高效微调。
*   **训练细节**：使用 AdamW 优化器，开启梯度检查点（Gradient Checkpointing）和 Flash Attention 2 以提高吞吐量。文中未明确给出总训练时长，但提到预训练进行了 100 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两个大型独立数据库（MIMIC 和 eICU）。
    *   对比了 6 种不同的 LLM 架构。
    *   针对 3 种不同的临床任务进行了验证。
*   **充分性与公平性**：
    *   采用了严格的**患者级划分**（Patient-Level Split）以防止数据泄露。
    *   进行了**消融实验**（如模态丢弃率对潜空间的影响）。
    *   不仅关注性能指标（AUROC, AUPRC），还进行了**机械性可解释性分析**（通过注意力矩阵追踪推理路径），实验设计较为全面且客观。

### 6. 主要结论与发现
*   **性能提升**：序列化建模在所有任务上均优于静态基线。在高度稀疏的 MIMIC-IV 数据集上，对比预训练带来的提升尤为显著（如死亡率预测 AUROC 从 0.57 提升至 0.84）。
*   **缺失模态的鲁棒性**：对比预训练使模型在模态缺失时表现更稳定。
*   **安全风险发现**：通过解释性分析发现，**非对比预训练模型在缺失关键临床模态时，会产生“注意力汇聚”现象，过度依赖人口统计学特征（如种族、性别）进行预测**，这可能导致严重的算法偏见。而对比预训练能有效缓解这种依赖，保持临床依据的优先权。

### 7. 优点与亮点
*   **范式创新**：将临床诊断从静态分类转向自回归信念更新，更符合实际医疗逻辑。
*   **缺失处理**：提出的“掩码全局对齐”（Masked Global Alignment）有效解决了多模态数据不齐的问题，避免了数据浪费或幻觉。
*   **深度解释性**：不仅给出了预测结果，还通过注意力机制揭示了模型在模态缺失时的“行为分歧”，为临床 AI 的安全性提供了审计手段。

### 8. 不足与局限
*   **算力限制**：受限于计算资源，仅评估了 3B 到 8B 参数规模的模型，更大规模模型的涌现行为尚待研究。
*   **长序列挑战**：Transformer 的二次方复杂度使得建模患者长达数年的完整生命周期轨迹在计算上仍然昂贵。
*   **因果性缺失**：虽然观察到了注意力模式的改变，但尚未开发出针对观察到的偏见模式的严格因果干预策略。
*   **数据集差异**：在数据相对完整的 eICU 数据集上，对比预训练的优势不明显，显示出该方法在不同数据分布下的收益存在差异。

（完）
