---
title: "ARFBench: Benchmarking Time Series Question Answering Ability for Software Incident Response"
title_zh: ARFBench：评估软件故障响应中时间序列问答能力的基准测试
authors: "Stephan Xie, Ben Cohen, Mononito Goswami, Junhong Shen, Emaad Khwaja, Chenghao Liu, David Asker, Othmane Abou-Amal, Ameet Talwalkar"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21199v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 基础模型的时间序列问答能力基准测试
tldr: 本研究针对软件故障响应中的时间序列问答（TSQA）能力，推出了ARFBench基准测试集。该数据集包含来自Datadog内部监控的63个生产事故、142条时间序列及750个问题。研究评估了多种大模型，发现前沿多模态模型表现优异，并提出了一种结合时间序列模型与视觉语言模型的混合原型。结果显示模型与人类专家具有互补性，联合预测可达到超越人类的水平。
selection_source: fresh_fetch
motivation: 旨在评估和提升基础模型在软件故障响应场景下，对时间序列异常进行自然语言推理和问答的能力。
method: 构建了包含真实生产事故数据的ARFBench基准，并开发了一种结合时间序列基础模型（TSFM）与视觉语言模型（VLM）的混合架构。
result: "领先的VLM模型达到62.7%的准确率，而模型与人类专家的组合（Oracle）可将准确率提升至87.2%。"
conclusion: ARFBench填补了TSQA在工业故障诊断领域的空白，证明了多模态混合模型及人机协作在处理复杂时间序列任务中的巨大潜力。
---

## 摘要
时间序列问答（TSQA）是指通过提出自然语言问题来推断和推理时间序列的属性，这是基础模型（foundation models）中一个极具前景但尚未得到充分探索的能力。在这项工作中，我们提出了 ARFBench，这是一个 TSQA 基准测试，用于评估多模态基础模型（FMs）对软件故障数据中常见的时间序列异常的理解能力。ARFBench 包含 750 个问题，涵盖 142 条时间序列和 538 万个数据点，这些数据源自 Datadog 内部遥测系统的 63 个生产环境故障。我们评估了领先的闭源和开源大语言模型（LLMs）、视觉语言模型（VLMs）以及时间序列基础模型（TSFMs），并观察到前沿 VLMs 的表现显著优于现有基准；领先的模型（GPT-5）达到了 62.7% 的准确率和 51.9% 的 F1 分数。随后，我们展示了专用多模态方法的前景。我们开发了一种新型的 TSFM + VLM 混合原型，并在少量合成数据和真实数据上进行了后训练，其整体 F1 分数和准确率与前沿模型相当。最后，我们发现模型与人类领域专家表现出互补的优势。我们定义了一个“模型-专家 Oracle”，即在模型和专家答案中进行“二选一”的最优选择器，其 F1 分数达到 82.8%，准确率达到 87.2%，为未来的 TSQA 模型确立了一个新的超越人类水平的前沿目标。该基准测试可在 https://huggingface.co/datasets/Datadog/ARFBench 获取。

## Abstract
Time series question-answering (TSQA), in which we ask natural language questions to infer and reason about properties of time series, is a promising yet underexplored capability of foundation models. In this work, we present ARFBench, a TSQA benchmark that evaluates the understanding of multimodal foundation models (FMs) on time series anomalies prevalent in software incident data. ARFBench consists of 750 questions across 142 time series and 5.38M data points from 63 production incidents sourced exclusively from internal telemetry at Datadog. We evaluate leading proprietary and open-source LLMs, VLMs, and time series FMs and observe that frontier VLMs perform markedly better than existing baselines; the leading model (GPT-5) achieves a 62.7% accuracy and 51.9% F1. We next demonstrate the promise of specialized multimodal approaches. We develop a novel TSFM + VLM hybrid prototype which we post-train on a small set of synthetic and real data that yields comparable overall F1 and accuracy with frontier models. Lastly, we find models and human domain experts exhibit complementary strengths. We define a model-expert oracle, a best-of-2 oracle selector over model and expert answers, yielding 82.8% F1 and 87.2% accuracy and establishing a new superhuman frontier for future TSQA models. The benchmark is available at https://huggingface.co/datasets/Datadog/ARFBench.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ARFBench** 的基准测试，旨在评估基础模型在软件故障响应（Software Incident Response）场景下对时间序列进行推理和问答的能力。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：软件系统故障每年造成巨额损失。工程师在排查故障时，通常需要通过观察监控指标（时间序列数据，如 CPU、延迟、错误率）并提出问题（如“这个模式异常吗？”、“这两个指标相关吗？”）来定位根因。
*   **核心问题**：现有的基础模型（LLM/VLM）是否能像专家一样理解并回答这些关于时间序列的复杂推理问题？
*   **整体含义**：论文填补了真实世界、多变量、专家标注的时间序列问答（TSQA）基准测试的空白，并探索了专用多模态模型在工业运维领域的潜力。

### 2. 论文提出的方法论
*   **ARFBench 构建**：
    *   **数据源**：来自 Datadog 内部生产环境的 63 个真实故障案例，包含 142 条时间序列和 538 万个数据点。
    *   **三层推理架构**：
        *   **Tier I (存在性)**：检测是否存在异常。
        *   **Tier II (属性识别)**：识别异常通道、开始/结束时间、幅度和类别。
        *   **Tier III (关系推理)**：分析两个序列间的相关性及领先/滞后关系。
*   **混合模型架构 (Toto-1.0-QA-Experimental)**：
    *   **核心思想**：结合时间序列基础模型（TSFM，如 Toto）的数值理解能力和视觉语言模型（VLM，如 Qwen3-VL）的推理能力。
    *   **关键技术**：使用多层感知机（MLP）将时间序列嵌入投影到 VLM 的特征空间，避免了长序列导致文本上下文溢出的问题。
    *   **训练流程**：采用三阶段后训练：
        1.  **合成数据 SFT**：在 1.2 万个合成异常样本上进行监督微调。
        2.  **真实数据 SFT**：在少量手动标注的真实故障数据上微调。
        3.  **RLVR 阶段**：使用可验证奖励的强化学习（RLVR），通过 DAPO 算法优化模型输出的准确性。

### 3. 实验设计
*   **数据集/场景**：ARFBench（750 个多选题），涵盖应用、基础设施、网络、数据库和安全五个运维子领域。
*   **对比方法**：
    *   **LLMs**：Qwen3-32B, GPT-5 (纯文本版)。
    *   **VLMs**：GPT-4o, GPT-5, Claude 3.5 Sonnet/Opus, Gemini 1.5 Pro, Qwen3-VL。
    *   **TS-LLMs**：OpenTSLM, ChatTS。
    *   **基准线**：随机选择、频率最高项选择、人类专家（领域专家与非专家）。
*   **评估指标**：准确率（Accuracy）和多类宏观 F1 分数（Macro-F1）。

### 4. 资源与算力
*   **推理评估**：使用 8 台 NVIDIA A100 GPU。
*   **VLM 后训练**：使用 8 台 NVIDIA H200 GPU（FP16 精度）。
*   **混合模型训练**：使用 8 台 NVIDIA B200 GPU（BF16 精度）。
*   **框架**：使用了 veRL、TRL 和 vLLM 等开源框架。

### 5. 实验数量与充分性
*   **实验规模**：对 10 余种主流模型进行了全面评估，涵盖了从 1B 到前沿闭源模型的不同规模。
*   **消融实验**：进行了输入模态消融（无图像 vs 无文本描述）和训练阶段消融（SFT vs RL），验证了各组件的必要性。
*   **客观性**：通过 1000 次 Bootstrap 重采样计算 95% 置信区间；引入了 4 名人类研究员进行对比实验，确保了基准测试的难度参考具有客观性。

### 6. 主要结论与发现
*   **VLM 领先**：前沿视觉语言模型（如 GPT-5）表现显著优于纯文本 LLM 和现有的时间序列专用 LLM。GPT-5 达到了 62.7% 的准确率。
*   **混合模型潜力**：作者提出的 TSFM+VLM 混合模型在经过少量数据训练后，表现可与最强闭源模型持平，证明了专用架构的有效性。
*   **人机互补**：模型在处理细粒度感知（如精确时间点）上优于人类，而人类在领域知识推理上更强。两者结合的“Oracle”准确率可达 87.2%，远超单一主体。
*   **上下文至关重要**：缺失时间序列的文本描述（Caption）会导致模型性能大幅下降，说明语义上下文对异常判断不可或缺。

### 7. 优点
*   **真实性极高**：数据源于真实的工业生产事故，而非实验室合成，具有极高的实际应用参考价值。
*   **多模态融合**：不仅提供数值，还提供专家讨论上下文和图像，符合实际运维场景。
*   **任务设计科学**：三层难度递进的任务设计能够细粒度地剖析模型的推理瓶颈。

### 8. 不足与局限
*   **单轮问答限制**：目前仅限于单轮 QA，无法评估模型在多轮对话或代理（Agentic）排障中的表现。
*   **样本规模**：虽然数据点多，但涉及的独立事故案例（63个）相对较少，可能存在特定领域的分布偏差。
*   **静态评估**：基准测试是静态的，无法模拟故障排查中动态获取新证据的过程。

（完）
