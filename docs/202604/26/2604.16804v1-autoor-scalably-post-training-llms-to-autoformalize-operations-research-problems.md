---
title: "AutoOR: Scalably Post-training LLMs to Autoformalize Operations Research Problems"
title_zh: AutoOR：通过可扩展的后训练使大语言模型能够自动形式化运筹学问题
authors: "Sumeet Ramesh Motwani, Chuan Du, Aleksander Petrov, Christopher Davis, Philip Torr, Antonio Papania-Davis, Weishi Yan"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16804v1"
tags: ["query:llm"]
score: 8.0
evidence: 使用LLM自动形式化运筹学和调度问题
tldr: 针对运筹优化问题建模门槛高、难以规模化的问题，本文提出 AutoOR 框架。该框架通过合成数据生成和强化学习流水线，利用求解器反馈作为奖励信号，训练 LLM 将自然语言描述自动转化为数学模型。实验表明，经过 AutoOR 训练的 8B 模型在多个基准测试中达到了与大型前沿模型相当的水平，并利用课程强化学习攻克了复杂的非线性物理动力学建模难题，显著提升了工业决策效率。
selection_source: fresh_fetch
motivation: 将复杂的自然语言描述转化为求解器可用的数学模型需要极高的专业知识，限制了运筹优化在工业场景中的规模化应用。
method: 开发了一套结合合成数据生成与求解器执行反馈强化学习的流水线，并针对非线性物理动力学问题引入了课程学习策略。
result: "AutoOR 训练的 8B 模型在六个运筹优化基准上表现优异，不仅匹配了大型前沿模型，还在复杂的非线性问题上实现了从 0% 到可解的突破。"
conclusion: AutoOR 证明了通过可扩展的后训练和强化学习，中等规模的语言模型能够高效且精准地处理复杂的工业建模任务。
---

## 摘要
优化问题是制造业、物流、调度及其他工业场景中决策的核心。将这些问题的复杂描述转化为求解器可用的数学公式需要专业的运筹学（OR）知识，这使得其难以大规模扩展。我们提出了 AutoOR，这是一个可扩展的合成数据生成和强化学习流水线，用于训练大语言模型（LLM）将以自然语言描述的优化问题（涵盖线性、混合整数和非线性类别）自动形式化。AutoOR 从标准优化形式生成经过验证的训练数据，并利用求解器的执行反馈作为强化学习（RL）后训练的奖励信号。将 AutoOR 应用于 8B 模型后，在六个成熟的运筹学基准测试中达到了最先进或具有竞争力的结果，性能媲美规模大得多的前沿模型。针对涉及物理动力学的非线性问题类别（前沿模型在此类问题上的得分接近 0%），我们引入了一种课程强化学习策略，通过有限的初始训练数据进行引导，使该类别在后训练中变得可处理。我们相信，像 AutoOR 这样的方法可以显著加速人工智能驱动的工业决策。

## Abstract
Optimization problems are central to decision-making in manufacturing, logistics, scheduling, and other industrial settings. Translating complicated descriptions of these problems into solver-ready formulations requires specialized operations research (OR) expertise, making it hard to scale. We present AutoOR, a scalable synthetic data generation and reinforcement learning pipeline that trains LLMs to autoformalize optimization problems specified in natural language across linear, mixed-integer, and non-linear categories. AutoOR generates verified training data from standard optimization forms and uses solver execution feedback as the reward signal for RL post-training. AutoOR applied to an 8B model achieves state-of-the-art or competitive results across six established OR benchmarks, matching significantly larger frontier models. For a non-linear problem class involving physical dynamics, where frontier models score near 0%, we introduce a curriculum RL strategy that bootstraps from limited initial training data to make this class tractable for post-training. We believe that methods such as AutoOR can significantly accelerate industrial decision-making with AI.

---

## 论文详细总结（自动生成）

以下是对论文《AutoOR: Scalably Post-training LLMs to Autoformalize Operations Research Problems》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何将复杂的工业优化问题（运筹学问题，OR）从自然语言描述自动转化为求解器可用的数学模型（自动形式化）。
*   **研究背景**：
    *   **专业门槛高**：建模过程需要深厚的运筹学背景，难以在工业界大规模推广。
    *   **数据稀缺**：高质量的“描述-代码”对数据非常匮乏，且现有合成数据准确性低。
    *   **非线性难题**：现有研究多集中在线性（LP）和混合整数规划（MILP），对于涉及物理动力学的非线性规划（NLP），前沿模型（如 Gemini 3 Pro）的成功率几乎为 0%。
    *   **冷启动困境**：在复杂问题上，模型初始输出全错，导致强化学习（RL）因缺乏正向奖励信号而无法收敛。

### 2. 方法论
AutoOR 提出了一套结合**可扩展合成数据生成**与**带验证奖励的强化学习**的流水线：
*   **核心思想**：利用建模过程中的“结构不对称性”——从标准数学形式生成描述（反向）比从描述生成代码（正向）更容易验证。
*   **关键技术细节**：
    1.  **可扩展合成数据生成（Backtranslation）**：
        *   **实例化**：从标准模板出发，随机生成参数，构建“世界描述符”（World Descriptor）和对应的正确求解代码。
        *   **反向翻译**：利用 LLM 将结构化的世界描述符转化为自然语言描述。
        *   **组件化验证**：由于世界描述符是结构化的，可以低成本验证生成的自然语言描述是否包含所有约束和目标。
    2.  **带验证奖励的强化学习（RLVR）**：
        *   使用 **GRPO（组相对策略优化）** 算法，无需奖励模型。
        *   **奖励函数**：由代码可执行性（$R_{exec}$）、约束满足性（$R_{feas}$）和目标值最优性（$R_{opt}$）三部分组成，通过求解器执行结果直接反馈。
    3.  **课程强化学习（Curriculum RL）**：针对非线性问题，分阶段训练：
        *   **阶段 1**：提供求解器语法和约束作为“特权信息”引导探索。
        *   **阶段 2**：移除引导信息，让模型独立建模。
        *   **阶段 3**：在更复杂的非线性分布上进行训练。
    4.  **多轮对话训练**：训练模型在描述信息缺失时主动提问，而非盲目建模。

### 3. 实验设计
*   **数据集/场景**：
    *   **线性规划（LP）**：资源分配、生产计划等。
    *   **混合整数规划（MILP）**：调度、路径规划、背包问题等。
    *   **非线性规划（NLP）**：离心泵网络合成问题（涉及复杂的物理动力学方程）。
*   **基准测试（Benchmarks）**：
    *   6 个成熟基准：NL4Opt, NL4LP, MAMO-Easy, MAMO-Complex, IndustryOR, ComplexOR。
    *   3 个新提出的挑战性基准：Hard-LP, Hard-MIP, Pump-NLP。
*   **对比方法**：
    *   **基础模型**：Qwen3-8B, Gemini 2.5 Pro, Gemini 3 Pro。
    *   **领域专用模型**：ORLM, Step-Opt, OptMATH, OR-R1 等。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A100 GPU**。
*   **模型配置**：以 Qwen3-8B 为基础模型，采用 **LoRA** 适配器进行微调。
*   **训练细节**：
    *   LP/MILP 训练步数在 500-800 步之间，每组采样 16 个样本（G=16）。
    *   NLP 课程学习分为三个阶段，总步数约 600 步。
    *   使用 bfloat16 精度。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单线性到极难非线性的 9 个不同维度的基准测试。
*   **消融与对比**：对比了不同规模的前沿模型（如 Gemini 3 Pro）以及最新的 SFT 和 RL 训练方法。
*   **充分性评价**：实验设计较为充分，不仅验证了模型在标准任务上的性能，还通过课程学习解决了“冷启动”问题，并探索了多轮交互场景。实验结果显示 8B 模型在多个指标上超越了参数量大得多的前沿模型，证明了该方法的有效性。

### 6. 主要结论与发现
*   **小模型逆袭**：经过 AutoOR 训练的 8B 模型在 OR 自动形式化任务上达到或超过了 Gemini 3 Pro 等前沿模型的水平。
*   **RL 优于 SFT**：在复杂约束和长描述问题上，在线 RL 展现出比监督微调（SFT）更强的泛化能力。
*   **非线性突破**：通过课程强化学习，模型在 Pump-NLP 任务上从 0% 的准确率提升至 48.98%，实现了零的突破。
*   **多轮交互有效**：多轮训练显著提升了模型处理不完整信息的能力（准确率从 18.5% 提升至 66.75%）。

### 7. 优点
*   **闭环验证**：利用求解器作为客观“裁判”，避免了 LLM 训练中常见的奖励偏置（Reward Hacking）。
*   **数据生成效率**：通过反向翻译解决了高质量 OR 数据获取难的问题，实现了训练信号的规模化扩展。
*   **攻克非线性**：课程学习策略为 LLM 处理具有复杂物理约束的硬核工程问题提供了可行路径。

### 8. 不足与局限
*   **领域局限性**：非线性实验目前仅限于泵网络合成，尚未验证在其他物理领域（如电力系统、化学过程）的通用性。
*   **求解器依赖**：模型生成的代码高度依赖特定的求解器 API（如 Google OR-Tools, Gekko），跨求解器的迁移能力未评估。
*   **建模质量**：奖励函数仅关注“正确性”，未考虑“建模质量”（如公式是否简洁、是否易于求解器快速收敛）。
*   **多轮对话简化**：目前的多轮实验仅限于 2 轮，且使用了模拟的 Oracle，与真实的工业交互仍有差距。

（完）
