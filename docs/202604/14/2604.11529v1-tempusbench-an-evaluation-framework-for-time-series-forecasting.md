---
title: "TempusBench: An Evaluation Framework for Time-Series Forecasting"
title_zh: TempusBench：一种时间序列预测评估框架
authors: "Denizalp Goktas, Gerardo Riaño-Briceño, Alif Abdullah, Aryan Nair, Chenkai Shen, Beatriz de Lucio, Alexandra Magnusson, Farhan Mashrur, Ahmed Abdulla, Shawrna Sen, Mahitha Thippireddy, Gregory Schwartz, Amy Greenwald"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11529v1"
tags: ["query:ts-fm"]
score: 10.0
evidence: 时序基础模型（TSFM）的评估框架
tldr: 针对时间序列基础模型（TSFM）缺乏统一评估框架的问题，本文推出TempusBench。该框架解决了现有基准数据集陈旧且与预训练集重叠、评估维度单一、模型调优不一致及可视化缺失等核心痛点。通过引入全新数据集、多维度评估任务、标准化调优协议及可视化界面，TempusBench为TSFM的公平对比和性能分析提供了全面且开源的解决方案。
selection_source: fresh_fetch
motivation: 现有时间序列预测评估框架在数据集时效性、任务全面性、对比公平性及可视化支持方面存在显著缺陷。
method: 提出包含新数据集、统计特性导向任务、标准化超参数调优协议及Tensorboard可视化界面的综合评估框架。
result: 建立了一个能够有效区分模型性能、避免数据泄露并支持深度分析的标准化时间序列预测基准体系。
conclusion: TempusBench为时间序列基础模型的研究提供了科学的评估标准，有助于推动该领域模型的规范化开发与性能提升。
---

## 摘要
基础模型已经变革了自然语言处理和计算机视觉领域，而关于时间序列基础模型（TSFMs）的文献正迅速增长，旨在预测领域复制这一成功。尽管最近的开源模型展示了 TSFMs 的潜力，但该领域仍缺乏一个全面且被社区认可的模型评估框架。我们认为至少有四个主要问题阻碍了此类框架的发展。首先，当前的评估框架包含源自陈旧数据集（如 M3）的基准预测任务，其中许多数据集缺乏清晰的元数据，且与用于预训练 TSFMs 的语料库存在重叠。其次，现有框架仅根据预测长度或领域等狭义定义的基准预测任务来评估模型，却忽视了非平稳性和季节性等核心统计特性。第三，特定领域模型（如 XGBoost）往往面临不公平的比较，因为现有框架忽视了针对所有模型的系统且一致的超参数调优规范。第四，缺乏用于解释对比性能的可视化工具。为了解决这些问题，我们推出了 TempusBench，这是一个针对 TSFMs 的开源评估框架。TempusBench 包含：1) 未包含在现有 TSFM 预训练语料库中的新数据集；2) 一系列超越现有任务的新型基准任务；3) 具有标准化超参数调优协议的模型评估流水线；4) 基于 TensorBoard 的可视化界面。我们在 GitHub 上提供了代码访问权限：https://github.com/Smlcrm/TempusBench。

## Abstract
Foundation models have transformed natural language processing and computer vision, and a rapidly growing literature on time-series foundation models (TSFMs) seeks to replicate this success in forecasting. While recent open-source models demonstrate the promise of TSFMs, the field lacks a comprehensive and community-accepted model evaluation framework. We see at least four major issues impeding progress on the development of such a framework. First, current evaluation frameworks consist of benchmark forecasting tasks derived from often outdated datasets (e.g., M3), many of which lack clear metadata and overlap with the corpora used to pre-train TSFMs. Second, existing frameworks evaluate models along a narrowly defined set of benchmark forecasting tasks such as forecast horizon length or domain, but overlook core statistical properties such as non-stationarity and seasonality. Third, domain-specific models (e.g., XGBoost) are often compared unfairly, as existing frameworks neglect a systematic and consistent hyperparameter tuning convention for all models. Fourth, visualization tools for interpreting comparative performance are lacking. To address these issues, we introduce TempusBench, an open-source evaluation framework for TSFMs. TempusBench consists of 1) new datasets which are not included in existing TSFM pretraining corpora, 2) a set of novel benchmark tasks that go beyond existing ones, 3) a model evaluation pipeline with a standardized hyperparameter tuning protocol, and 4) a tensorboard-based visualization interface. We provide access to our code on GitHub: https://github.com/Smlcrm/TempusBench.

---

## 论文详细总结（自动生成）

这是一份关于论文《TempusBench: An Evaluation Framework for Time-Series Forecasting》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着时间序列基础模型（TSFM）的爆发式增长，领域内缺乏一个统一且公认的评估标准。作者指出当前评估体系存在四大痛点：
*   **数据陈旧与泄露：** 常用基准（如 M3/M4）缺乏元数据，且大多已被包含在 TSFM 的预训练语料中，导致“零样本”泛化能力的评估虚高。
*   **评估维度单一：** 现有框架多关注预测长度或领域，忽视了非平稳性、季节性等核心统计特性。
*   **对比不公平：** 传统机器学习模型（如 XGBoost）的性能高度依赖超参数，而现有框架往往不对其进行系统调优，导致与 TSFM 的对比不公平。
*   **缺乏可视化：** 仅依赖数值指标（如 MSE），难以直观理解模型在不同任务下的优劣。

**TempusBench** 的提出旨在建立一个开源、标准化、抗泄露且具备深度统计洞察的评估框架。

### 2. 方法论：核心思想与算法流程
TempusBench 构建了一个端到端的评估流水线，其核心组成部分包括：
*   **标准化超参数调优协议（ETB）：** 采用三步走策略：
    1.  **滚动窗口生成：** 沿时间序列滑动固定大小窗口，划分为上下文、训练和验证段，步长等于验证段长度。
    2.  **自动调优：** 在每个窗口内遍历超参数组合，选择验证损失（如 MSE）最低的配置。
    3.  **测试评估：** 使用前一窗口选定的超参数在当前窗口进行测试，最后取所有窗口的平均指标。这种方法严格遵循时间顺序，避免了随机采样带来的“前瞻偏差”。
*   **多维度任务分类：** 除了传统的领域分类，还引入了基于**平稳性、季节性（循环、不规则等）、数据质量（噪声、测量误差）、稀疏性、变量类型（连续、计数、分类、二进制）**的新型基准任务。
*   **结果聚合机制：** 引入“平均胜率（Win Rate）”和“技能得分（Skill Score，相对于季节性朴素预测的改进程度）”，并使用几何平均值以确保跨任务评估的鲁棒性。

### 3. 实验设计
*   **数据集：** 包含 20 多个新数据集，涵盖能源、交通、气候、软件、销售、医疗等 10 个领域。这些数据经过筛选，确保未进入现有 TSFM 的预训练集。
*   **对比方法（共 20 种模型）：**
    *   **基础模型（TSFMs）：** Moirai, TimesFM, Chronos, Lag-Llama, Toto, MOMENT, TTM, TabPFN-TS 等。
    *   **传统机器学习：** LSTM, Random Forest, XGBoost, SVR。
    *   **统计模型：** ARIMA, Holt-Winters, Prophet, Theta, Croston, Seasonal Naive。
*   **评估指标：** 点预测（MAE, MSE, MASE, MAPE）及概率预测（CRPS, WIS, Quantile Score）。

### 4. 资源与算力
论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。由于该论文侧重于评估框架而非模型训练，其算力消耗主要集中在 20 种模型在数十个数据集上的推理及传统模型的超参数搜索上。

### 5. 实验数量与充分性
*   **实验规模：** 评估了 20 种模型在单变量和多变量场景下的表现，涵盖了从秒级到年级的不同频率，以及从短程到长程的预测长度。
*   **充分性与公平性：** 实验设计非常充分且客观。通过引入自动调优协议，解决了长期以来传统模型因未调优而处于劣势的问题。同时，通过“胜率”和“技能得分”多维度衡量，避免了单一指标的偏差。

### 6. 主要结论与发现
*   **TSFM 的优势：** TimesFM 在点预测（MAE/RMSE）中表现最稳健；Toto 和 Moirai 在概率预测指标上具有显著优势。
*   **小模型的潜力：** 仅有 0.4M 参数的 LAFN 模型在多变量任务中表现惊人，在多个数据集上击败了参数量大其 2000 倍的基础模型，证明了架构效率的重要性。
*   **传统模型的竞争力：** 在经过适当调优后，统计模型（如 Varmax）在某些 MAPE 指标上仍能胜过部分 TSFM。
*   **基准线挑战：** 季节性朴素预测（Seasonal Naive）依然是一个极具竞争力的基准，许多复杂模型在特定任务下甚至无法超越它。

### 7. 优点与亮点
*   **去污染（Decontamination）：** 专门针对 TSFM 的数据泄露问题设计，确保了评估的真实性。
*   **统计深度：** 首次系统性地将非平稳性和季节性类型纳入大规模基准测试。
*   **公平对比：** 强制性的超参数自动调优协议为 TSFM 与传统 ML 模型的对比树立了新标杆。
*   **工具链完备：** 提供了基于 TensorBoard 的可视化界面，方便研究者分析模型失败的具体场景。

### 8. 不足与局限
*   **条件预测缺失：** 当前版本主要关注无条件预测，尚未充分涵盖包含外生变量的条件预测任务（作者表示将在未来版本加入）。
*   **动态性不足：** 虽然提出了动态基准的设想，但目前仍主要依赖静态数据集，随着时间推移，这些新数据也可能被纳入未来 TSFM 的训练集。
*   **算力成本：** 自动调优协议虽然公平，但对于大规模基准测试而言，其计算开销显著高于简单的推理评估。

（完）
