---
title: "MAFIG: Multi-agent Driven Formal Instruction Generation Framework"
title_zh: MAFIG：多智能体驱动的形式化指令生成框架
authors: "Shixing Zhao, Zheng Si, Pengpeng Ouyang, Zhengqing Hu, Wanqi Zhu, Dong Chen, Yibo Guo, Mingliang Xu"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.10989v1"
tags: ["query:llm"]
score: 9.0
evidence: 大语言模型用于复杂调度任务和电力系统指令生成
tldr: 针对调度系统在突发状况下难以预测且现有方法响应慢的问题，本文提出MAFIG框架。该框架通过感知和决策双智能体将决策范围限制在受影响的局部模块，并生成形式化指令快速修复调度逻辑。此外，引入SFL局部蒸馏机制，将云端大模型的能力迁移至本地轻量化模型，显著降低了推理延迟。实验证明，该方法在多种调度场景下均表现出极高的成功率和极短的处理时间，增强了系统的鲁棒性。
selection_source: fresh_fetch
motivation: 传统调度方法难以应对不可预测的突发状况，而大语言模型在处理此类任务时面临高延迟和长上下文的挑战。
method: 采用多智能体驱动的形式化指令生成框架，并结合跨度聚焦损失驱动的局部蒸馏机制（SFL）提升本地模型性能。
result: "在港口、仓储和甲板调度任务中分别达到98.49%、94.97%和97.50%的成功率，且平均响应时间均低于0.33秒。"
conclusion: MAFIG框架通过局部决策和模型蒸馏，有效解决了调度系统应对紧急情况时的实时性与准确性平衡问题。
---

## 摘要
调度系统中的突发状况往往会引发局部功能故障，破坏系统稳定性，甚至导致系统崩溃。现有方法主要依赖于鲁棒调度或反应式调度，通过预定义规则或重新调度策略来处理突发状况。然而，现实世界中突发状况的多样性和不可预测性使其难以预料，这限制了这些方法在复杂场景中的适应性。最近的研究表明，大语言模型（LLMs）凭借其广泛的先验知识和强大的推理能力，在复杂调度任务中展现出巨大潜力。尽管如此，LLMs 的高推理延迟以及调度系统冗长的上下文信息严重阻碍了其在突发状况处理中的应用。为了缓解这些问题，我们提出了多智能体驱动的形式化指令生成框架（MAFIG）。该框架将决策范围限制在受突发状况影响的局部功能模块，并通过生成形式化指令快速修复调度逻辑。MAFIG 包含感知智能体（Perception Agent）和突发决策智能体（Emergency Decision Agent），从而减轻了冗长系统上下文对突发决策的不利影响。我们进一步引入了跨度聚焦损失驱动的局部蒸馏机制（SFL），将强大的云端大语言模型（C-LLMs）的决策能力迁移到轻量级本地模型中，在保持决策有效性的同时降低推理延迟。在港口、仓储和甲板调度数据集上的实验显示，成功率分别达到 98.49%、94.97% 和 97.50%，平均处理时间分别为 0.33 秒、0.23 秒和 0.19 秒。这些结果表明，MAFIG 有效地减轻了突发状况的影响，并提高了调度系统的鲁棒性和适应性。

## Abstract
Emergency situations in scheduling systems often trigger local functional failures that undermine system stability and even cause system collapse. Existing methods primarily rely on robust scheduling or reactive scheduling, handling emergencies through predefined rules or rescheduling strategies. However, the diversity and unpredictability of real-world emergencies make them difficult to anticipate, which limits the adaptability of these methods in complex scenarios. Recent studies have shown that Large Language Models (LLMs) possess strong potential for complex scheduling tasks because of their extensive prior knowledge and strong reasoning capabilities. Nevertheless, the high inference latency of LLMs and the lengthy contextual information of scheduling systems significantly hinder their application for emergency handling. To mitigate these issues, we propose the Multi-agent Driven Formal Instruction Generation Framework (MAFIG). The framework constrains the decision scope to local functional modules affected by emergency situations and repairs scheduling logic rapidly by generating formal instructions. MAFIG contains a Perception Agent and an Emergency Decision Agent, which mitigates the adverse impact of lengthy system contexts on emergency decision-making. We further introduce span-focused loss-driven local distillation mechanism (SFL) to transfer the decision-making capability of powerful Cloud Large Language Models (C-LLMs) to lightweight local models, reducing inference latency while preserving decision-making effectiveness. Experiments in the Port, Warehousing, and Deck scheduling datasets show success rates of 98.49\%, 94.97\%, and 97.50\%, with average processing times of 0.33 s, 0.23 s, and 0.19 s. These results demonstrate that MAFIG effectively mitigates the impact of emergencies and improves the robustness and adaptability of scheduling systems.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MAFIG** 的多智能体驱动形式化指令生成框架，旨在解决工业调度系统在面对突发状况时的鲁棒性与实时响应问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
现代工业调度系统（如港口、仓储）通常采用紧耦合架构，面对不可预测的突发状况（如设备故障、天气延迟）时，局部故障极易引发连锁反应导致系统崩溃。
*   **现有挑战**：
    *   **传统方法局限**：鲁棒调度过于保守（牺牲效率），反应式调度响应滞后且计算负担重。
    *   **大语言模型（LLM）应用瓶颈**：直接引入 LLM 面临“延迟-质量权衡”（模型大则慢，模型小则弱）以及“执行失调”（长上下文导致模型难以精准定位受影响的逻辑模块）。
*   **研究目标**：开发一个既能利用 LLM 逻辑推理能力，又能满足工业级实时性（低延迟）和准确性要求的调度修复框架。

### 2. 方法论：核心思想与关键技术
MAFIG 的核心思想是将全局调度修复简化为**局部原子函数修复**，并通过知识蒸馏提升本地轻量化模型的性能。
*   **原子函数库（Atomic Function Library）**：利用云端大模型（C-LLM）将复杂的调度逻辑解耦为多个功能独立的原子函数，并生成对应的功能规范。
*   **感知智能体（Perception Agent, $A_P$）**：负责语义解析突发状况，并从函数库中精准定位受影响的原子函数，避免模型被无关的系统上下文干扰。
*   **突发决策智能体（Emergency Decision Agent, $A_E$）**：针对定位到的函数进行代码级修复或生成新函数。
*   **关键技术 - SFL 局部蒸馏机制**：
    *   **跨度聚焦损失（Span-Focused Loss）**：通过 `diff` 策略识别代码修改区域，引入特殊标记（如 `<<EDIT_START>>`），并对修改区域分配更高的损失权重（$\lambda_{edit}$），使模型集中学习核心逻辑修改而非背景代码。
    *   **统计初始化**：对新增的特殊 Token 进行基于原嵌入矩阵分布的统计初始化，保证训练稳定性。

### 3. 实验设计
*   **数据集/场景**：构建了三个复杂度递增的仿真数据集：
    *   **EvalPort（港口）**：8个原子函数，侧重泊位分配。
    *   **EvalWare（仓储）**：15个原子函数，侧重路径规划与多任务协同。
    *   **EvalDeck（航母甲板）**：25个原子函数，空间受限且约束极强，复杂度最高。
*   **对比方法（Baseline）**：
    *   云端大模型：Qwen3-32B、Qwen3-Coder-480B、DeepSeek-V3.2、GLM-4.7。
    *   本地基础模型：Qwen2.5-Coder-7B（作为 MAFIG 的骨干模型）。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX A6000 GPU**。
*   **训练细节**：
    *   使用 FP16 精度，LoRA 微调（Rank=8）。
    *   训练 3 个 Epoch，学习率 $5 \times 10^{-5}$，采用 AdamW 优化器。
    *   推理端使用 vLLM 加速框架。

### 5. 实验数量与充分性
论文进行了多维度的实验验证，实验设计较为充分且客观：
*   **总体性能对比**：在三个场景下对比了 5 种主流模型。
*   **架构有效性实验**：验证了 MAFIG 框架在不同骨干模型（Qwen、Llama、GLM）上的普适性提升。
*   **消融实验**：对比了有无 SFL 机制的性能差异。
*   **样本效率分析**：分析了不同训练集规模（如 60 到 170 个样本）对模型成功率的影响。
*   **案例研究**：展示了在复杂甲板调度场景下处理并发突发状况的具体过程。

### 6. 主要结论与发现
*   **性能卓越**：MAFIG 在三个场景下的成功率均超过 94%，最高达 **98.49%**。
*   **极低延迟**：平均处理时间在 **0.19s - 0.33s** 之间，远优于云端大模型（通常需数秒至数十秒）。
*   **局部修复的优势**：通过将决策范围限制在局部模块，有效解决了大模型在长上下文下的“执行失调”问题。
*   **蒸馏效果**：SFL 机制显著优于传统的 LoRA 全序列优化，尤其在小样本情况下表现出更强的逻辑捕捉能力。

### 7. 优点（亮点）
*   **解耦设计**：将复杂的调度系统模块化，使得 LLM 能够像“外科手术”一样进行精准修复。
*   **SFL 机制创新**：针对代码编辑任务设计的跨度聚焦损失，解决了轻量化模型参数量不足导致的注意力分散问题。
*   **本地化部署**：兼顾了工业场景对数据隐私的要求和对响应速度的极致追求。

### 8. 不足与局限
*   **依赖云端模型初始化**：系统初期的解耦和蒸馏数据的生成仍高度依赖强大的云端模型（如 GPT-4 或 GLM-4）。
*   **代码执行风险**：虽然论文提到了系统试运行验证，但 LLM 生成的代码若存在隐蔽逻辑漏洞，在高度敏感的工业环境下仍存在安全风险。
*   **场景局限**：目前主要针对基于代码逻辑驱动的调度系统，对于纯数学规划或黑盒优化算法驱动的系统适配性尚未讨论。

（完）
