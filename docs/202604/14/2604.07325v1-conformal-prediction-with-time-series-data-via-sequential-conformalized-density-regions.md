---
title: Conformal Prediction with Time-Series Data via Sequential Conformalized Density Regions
title_zh: 基于序列符合化密度区域的时间序列数据符合预测
authors: "M. Sampson, K. S. Chan"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07325v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 具有覆盖保证的时间序列数据符合性预测方法
tldr: 针对时间序列数据的非交换性，本文提出SCDR方法，通过结合条件最高密度区域与分位数随机森林调整，实现了具有渐近条件覆盖保证的预测。该方法能生成灵活的预测区间或非连续预测集以应对分叉现象，具备双重稳健性，在模拟和真实数据实验中均展现出比现有方法更高的覆盖精度和更小的预测集规模。
selection_source: fresh_fetch
motivation: 旨在解决传统符合预测在处理非交换性时间序列数据时难以保证条件覆盖率以及无法生成非连续预测集的问题。
method: 利用条件最高密度区域构建初始预测集，并采用分位数随机森林进行符合性调整，以自适应地处理时间序列的动态特性。
result: 实验结果显示SCDR在经验覆盖率和预测集大小上均优于现有方法，并能成功捕捉如老忠实泉喷发时长中的双峰分布特征。
conclusion: SCDR为时间序列提供了一种灵活且具有理论保障的预测框架，在模型设定正确或得分序列满足非线性自回归模型时均表现稳健。
---

## 摘要
我们提出了一种针对时间序列数据的具有渐近条件覆盖率保证的新型符合预测方法，即序列符合化密度区域（Sequential Conformalized Density Regions, SCDR）。该方法足够灵活，既能生成预测区间，也能生成不连续的预测集合，从而表征分叉现象的出现。我们的方法利用现有的估计条件最高密度预测区域来形成初始预测区域。随后，我们采用分位数随机森林符合调整来提供覆盖率保证，同时通过自适应调整以考虑时间序列数据的非可交换性。我们证明了在一定的正则性条件下，所提方法能够渐近地达到保证的覆盖率。特别地，该方法具有双重稳健性——即在预测密度模型设定正确，和/或评分遵循设定了正确阶数的非线性自回归模型的情况下，该方法均有效。仿真实验表明，所提方法在经验覆盖率和集合大小方面优于现有方法。我们利用老忠实间歇泉数据集和澳大利亚电力消耗数据集这两个真实数据集对该方法进行了演示。对于间歇泉喷发持续时间，使用 SCDR 构建的预测集合既包含单个区间，也包含两个区间的并集，而现有方法生成的预测集合则是更宽且信息量较少的单一区间。

## Abstract
We propose a new conformal prediction method for time-series data with a guaranteed asymptotic conditional coverage rate, Sequential Conformalized Density Regions (SCDR), which is flexible enough to produce both prediction intervals and disconnected prediction sets, signifying the emergence of bifurcations. Our approach uses existing estimated conditional highest density predictive regions to form initial predictive regions. We then use a quantile random forest conformal adjustment to provide guaranteed coverage while adaptively changing to take the non-exchangeable nature of time-series data into account.   We show that the proposed method achieves the guaranteed coverage rate asymptotically under certain regularity conditions. In particular, the method is doubly robust -- it works if the predictive density model is correctly specified and/or if the scores follow a nonlinear autoregressive model with the correct order specified.   Simulations reveal that the proposed method outperforms existing methods in terms of empirical coverage rates and set sizes. We illustrate the method using two real datasets, the Old Faithful geyser dataset and the Australian electricity usage dataset. Prediction sets formed using SCDR for the geyser eruption durations include both single intervals and unions of two intervals, whereas existing methods produce wider, less informative, single-interval prediction sets.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SCDR（Sequential Conformalized Density Regions，序列符合化密度区域）** 的新方法，旨在解决时间序列数据在符合预测（Conformal Prediction）中的条件覆盖保证和复杂分布表达问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的符合预测方法通常假设数据具有“可交换性”（Exchangeability），而时间序列数据具有时间依赖性和不可逆性，违反了这一假设。
*   **研究动机**：
    *   **条件覆盖率缺失**：现有时间序列预测方法多关注边际覆盖率，难以保证在特定特征值下的条件覆盖率。
    *   **预测集形式单一**：现有方法（如 SPCI, PID）通常只能生成连续的预测区间，无法处理多峰分布或分叉现象（Bifurcations）。
    *   **模型鲁棒性不足**：当底层预测模型失效时，预测集的有效性和紧凑性难以平衡。

### 2. 论文提出的方法论：SCDR
*   **核心思想**：结合**条件最高密度区域（CHDS）**与**分位数随机森林（QRF）**，通过一种“双重稳健”的架构，对初始预测区域进行动态符合化调整。
*   **关键技术细节**：
    1.  **初始密度估计**：使用正态混合模型（Normal Mixture）估计条件概率密度 $\hat{f}(y|x)$。
    2.  **符合性得分（Score）**：定义一种基于除法的得分 $V_j = \hat{f}(Y_j|X_j) / \hat{c}(X_j)$，其中 $\hat{c}$ 是未调整的密度阈值。该得分衡量了观测值相对于预测密度的“典型性”。
    3.  **序列调整**：利用 QRF 模型学习过去 $w$ 个得分的依赖关系，预测未来得分的 $\alpha$ 分位数 $\hat{q}_{T+1}$。
    4.  **预测集构建**：最终预测集为 $\{y : \hat{f}(y|X_{T+1}) > \hat{c}(X_{T+1}) \times \hat{q}_{T+1}\}$。
*   **双重稳健性（Double Robustness）**：理论证明，只要“条件密度模型”或“分位数调整模型”中有一个设定正确，SCDR 就能保证渐近条件覆盖率。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据**：非平稳时间序列模拟、AR(2) 线性过程模拟。
    *   **真实数据**：老忠实泉（Old Faithful）喷发时长数据（具有明显的双峰特征）、澳大利亚电力消耗数据（高频、非线性）。
*   **Benchmark 与对比方法**：
    *   **SPCI**（Sequential Predictive Conformal Inference）：基于分位数回归的序列预测。
    *   **PID**（Conformal PID Control）：基于控制理论的覆盖率反馈调节。
    *   **BCI**（Bellman Conformal Inference）：基于贝尔曼方程的最优预测区间调节。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号或训练时长。由于核心算法涉及正态混合模型和随机森林，通常在标准 CPU 或中等算力的工作站上即可完成。实验中提到了使用 R 语言的 `forecast` 等包进行基础模型构建。

### 5. 实验数量与充分性
*   **实验规模**：模拟实验采用了 1000 次重复试验，样本量从 300 到 2000 不等。
*   **充分性评价**：实验设计较为充分。作者不仅测试了平均覆盖率（Marginal Coverage），还重点评估了不同时间点/特征值下的条件覆盖率。通过对比预测集的大小（Lebesgue measure），证明了 SCDR 在维持覆盖率的同时，预测集更窄、更高效。

### 6. 主要结论与发现
*   **覆盖率保证**：SCDR 在非平稳和非线性场景下，比 SPCI 和 BCI 更接近标称覆盖率（如 90%）。
*   **预测集紧凑性**：在相同覆盖率下，SCDR 生成的预测集通常比 PID 等方法小 2-3 倍。
*   **捕捉多峰性**：在老忠实泉实验中，SCDR 成功生成了不连续的两个区间（反映了喷发机制的两种状态），而其他方法只能给出一个包含中间低概率区域的宽泛区间。

### 7. 优点（亮点）
*   **理论突破**：首次在时间序列符合预测中引入了“双重稳健性”概念。
*   **灵活性**：能够生成非连续预测集，这对于存在状态切换或分叉的动力系统预测至关重要。
*   **渐近最优性**：证明了在正则条件下，该方法生成的预测集在所有满足覆盖率要求的集合中具有最小的测度（即最精确）。

### 8. 不足与局限
*   **维度诅咒**：虽然正态混合模型是通用近似器，但在处理极高维度的特征空间时，密度估计的准确性会迅速下降。
*   **计算开销**：在每一时间步更新 QRF 模型和密度估计可能在超大规模实时流数据中存在计算延迟。
*   **模型依赖**：虽然有双重稳健性，但如果密度模型和分位数模型都严重偏离真实分布，有限样本下的表现可能会受影响。

（完）
