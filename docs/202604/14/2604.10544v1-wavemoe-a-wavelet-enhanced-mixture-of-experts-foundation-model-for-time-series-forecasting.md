---
title: "WaveMoE: A Wavelet-Enhanced Mixture-of-Experts Foundation Model for Time Series Forecasting"
title_zh: WaveMoE：一种用于时间序列预测的小波增强混合专家基础模型
authors: "Shunyu Wu, Jiawei Huang, Weibin Feng, Boxin Li, Xiao Zhang, Erli Meng, Dan Li, Jian Lou, See-Kiong Ng"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10544v1"
tags: ["query:ts-fm"]
score: 10.0
evidence: 用于时间序列预测的小波增强混合专家基础模型
tldr: 本研究针对时间序列基础模型在处理复杂时间模式（如周期性和局部高频动态）方面的局限，提出了WaveMoE。该模型将小波变换引入混合专家网络（MoE）架构，通过双路径设计同时处理时域和频域特征。WaveMoE利用共享专家路由机制实现高效的模型扩展和专家专业化，在16个基准数据集上展现了优异的预测性能，为时间序列基础模型提供了新的频域增强视角。
selection_source: fresh_fetch
motivation: 旨在通过引入显式的频域表示（如小波变换），增强时间序列基础模型对复杂周期性和局部高频动态的建模能力。
method: 提出了一种双路径架构，通过共享的混合专家（MoE）路由机制协同处理对齐的时域和波域Token，实现模型容量的高效扩展。
result: 在16个多样化的基准数据集上的初步实验表明，WaveMoE通过整合小波域语料库显著提升了时间序列的预测性能。
conclusion: WaveMoE证明了将小波增强与MoE架构结合是提升时间序列基础模型泛化能力和预测精度的有效途径。
---

## 摘要
时间序列基础模型 (TSFMs) 最近通过在多样化的时间序列数据上利用大规模预训练，在通用预测方面取得了显著成功。作为这一进展的补充，引入频域信息在增强复杂时间模式（如现实世界时间序列中普遍存在的周期性和局部高频动态）建模方面表现出了极具前景的性能。为了推进这一方向，我们提出了一种将显式频域表示集成到可扩展基础模型中的新视角，并介绍了 WaveMoE，这是一种用于时间序列预测的小波增强混合专家基础模型。WaveMoE 采用双路径架构，共同处理沿统一时间轴对齐的时间序列 token 和小波 token，并通过共享的专家路由机制对它们进行协调，该机制在高效扩展模型容量的同时实现了持续的专家专业化。在 16 个多样化基准数据集上的初步实验结果表明，通过引入小波域语料库，WaveMoE 具有进一步提高预测性能的潜力。

## Abstract
Time series foundation models (TSFMs) have recently achieved remarkable success in universal forecasting by leveraging large-scale pretraining on diverse time series data. Complementing this progress, incorporating frequency-domain information yields promising performance in enhancing the modeling of complex temporal patterns, such as periodicity and localized high-frequency dynamics, which are prevalent in real-world time series. To advance this direction, we propose a new perspective that integrates explicit frequency-domain representations into scalable foundation models, and introduce WaveMoE, a wavelet-enhanced mixture-of-experts foundation model for time series forecasting. WaveMoE adopts a dual-path architecture that jointly processes time series tokens and wavelet tokens aligned along a unified temporal axis, and coordinates them through a shared expert routing mechanism that enables consistent expert specialization while efficiently scaling model capacity. Preliminary experimental results on 16 diverse benchmark datasets indicate that WaveMoE has the potential to further improve forecasting performance by incorporating wavelet-domain corpora.

---

## 论文详细总结（自动生成）

以下是对论文《WaveMoE: A Wavelet-Enhanced Mixture-of-Experts Foundation Model for Time Series Forecasting》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：时间序列基础模型（TSFMs）通过大规模预训练在通用预测领域取得了显著进展。然而，现有模型大多仅在原始时域（Time-domain）进行缩放，忽略了频域信息（如周期性、局部高频波动）在捕捉复杂时间模式中的重要性。
*   **核心问题**：论文探讨了一个关键的科学问题——**在预训练语料库中引入频域（如小波域）表示，是否能进一步提升时间序列基础模型的预测性能？**
*   **整体含义**：WaveMoE 旨在通过将显式的小波变换（Wavelet Transform）集成到可扩展的混合专家（MoE）架构中，构建一个能够同时理解时域和频域特征的通用预测模型。

### 2. 论文提出的方法论
WaveMoE 采用了一种**双路径（Dual-path）架构**，核心思想是协同处理时域 Token 和小波域 Token：
*   **双路径 Token 化与对齐**：
    *   **时域路径**：将输入序列切分为固定长度的补丁（Patches）。
    *   **小波域路径**：利用离散小波变换（DWT）将序列分解为多尺度近似（Approximation）和细节（Detail）系数，并将其打包成与时域路径在时间轴上完全对齐的 Token。
*   **稀疏注意力机制**：在两条路径中分别使用 Transformer 风格的自注意力层，并通过 Top-k 注意力评分激活信息量最大的 Token，以降低长序列处理的计算开销。
*   **统一混合专家路由（Unified MoE Routing）**：
    *   **共享路由**：不为两个领域设置独立路由，而是将相同位置的时域和频域 Token 拼接后，由一个共享的 MLP 门控网络决定专家分配。
    *   **双分支专家**：每个专家内部包含两个独立的反馈网络（FFN），分别处理时域和频域 Token，从而在保持结构一致性的同时保留领域特有的归纳偏置。
*   **联合监督学习**：输出层设有独立预测头，分别预测未来数值和未来的小波系数，通过 Huber Loss 进行联合优化。

### 3. 实验设计
*   **数据集**：涵盖了 16 个主流基准数据集（如 ETT, Exchange Rate, M1-M5, Traffic, Weather, UK Covid 等），并在附录中额外测试了 18 个数据集（如电力、旅游、零售等），共计 **34 个数据集**。
*   **Benchmark（基准模型）**：
    *   **Time-MoE**：具有相似 MoE 架构但仅处理时域的模型（主要对比对象）。
    *   **Timer、Chronos、Sundial**：代表性的 Transformer 或扩散模型类 TSFMs。
*   **实验设置**：固定上下文长度为 512，预测长度为 96。评价指标为均方误差（MSE）和平均绝对误差（MAE）。

### 4. 资源与算力
*   **预训练数据**：基于 Time-300B 语料库，经过领域平衡、质量过滤和 IoT 数据增强，最终构建了约 **980 亿（98 Billion）时间步**的预训练数据集。
*   **模型规模**：总参数量为 **2.26 亿（226M）**，每次推理激活的参数量约为 1 亿（100M）。
*   **训练细节**：使用 AdamW 优化器，学习率 $2 \times 10^{-4}$，Batch Size 为 128，训练 100,000 步。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体使用的显卡数量。

### 5. 实验数量与充分性
*   **实验数量**：论文在正文和附录中总共展示了在 34 个不同领域数据集上的表现，并提供了预测结果的可视化对比。
*   **充分性与公平性**：
    *   实验覆盖了能源、金融、医疗、交通、IoT 等多个真实场景，具有较强的代表性。
    *   通过与 Time-MoE 的直接对比，有效地证明了小波增强设计的增量贡献。
    *   对比实验均在相同的上下文和预测长度下进行，评估较为客观。但缺乏针对不同小波基函数或分解层数的消融实验。

### 6. 主要结论与发现
*   **性能领先**：WaveMoE 在 16 个主要基准数据集中的 14 个上取得了最佳 MSE，在 11 个上取得了最佳 MAE。
*   **频域优势**：引入小波域语料库确实能显著提升模型对复杂振荡和局部动态的捕捉能力。
*   **鲁棒性**：可视化分析显示，相比于 Chronos 或 Timer，WaveMoE 在处理高频波动和剧烈趋势转折时，预测曲线与真实值（Ground Truth）的拟合度更高，且延迟更低。

### 7. 优点
*   **架构创新**：巧妙地将小波变换的局部时频分析能力与 MoE 的高效扩展能力结合，解决了传统 TSFM 在频域建模上的短板。
*   **高效对齐**：通过共享路由和双分支专家设计，实现了时频信息的深度协同，而非简单的特征拼接。
*   **泛化能力强**：在大规模异构数据上预训练后，展现出优异的零样本（Zero-shot）预测潜力。

### 8. 不足与局限
*   **变量间依赖建模**：目前采用通道独立（Channel-independent）策略，尚未充分探索多变量之间的相互依赖关系。
*   **算力细节缺失**：未披露具体的硬件资源消耗，难以评估训练成本。
*   **超参数敏感性**：小波基的选择（如文中使用的 bior2.2）和分解层数对不同类型数据的适应性可能存在差异，文中未深入讨论其自适应性。
*   **解释性局限**：虽然引入了频域，但对于 MoE 专家如何具体分工处理时频特征的内在逻辑仍缺乏深度的可解释性分析。

（完）
