---
title: "QARIMA: A Quantum Approach To Classical Time Series Analysis"
title_zh: QARIMA：一种用于经典时间序列分析的量子方法
authors: "Nishikanta Mohanty, Bikash K. Behera, Badshah Mukherjee, Pravat Dash"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08277v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 用于经典时间序列分析的量子启发式ARIMA方法
tldr: 本文提出QARIMA，一种将量子辅助滞后发现与固定配置变分量子电路（VQC）相结合的量子启发式ARIMA方法。该框架利用量子自相关（QACF）和偏自相关（QPACF）确定模型阶数，并采用VQC进行参数估计与弱滞后细化。通过在环境和工业数据集上的实验，证明了该方法在减少元优化开销的同时，能有效提升时间序列分析的预测精度。
selection_source: fresh_fetch
motivation: 旨在利用量子计算的并行性与表达能力，解决传统ARIMA在滞后识别和复杂参数估计中的局限性，并降低超参数泄露风险。
method: 集成量子自相关与偏自相关函数进行阶数识别，并利用固定配置的变分量子电路（VQC）分别执行AR/MA参数估计与滞后项细化。
result: 在多项实际数据集的滚动预测中，QARIMA在MSE和MAPE指标上均优于自动化经典ARIMA，并通过了Diebold-Mariano显著性检验。
conclusion: 该研究系统性地展示了量子技术在时间序列分析各环节的应用潜力，为构建高效、精准的量子增强统计模型提供了新路径。
---

## 摘要
我们提出了一种受量子启发的 ARIMA 方法论，该方法将量子辅助的滞后发现与用于参数估计和弱滞后细化的固定配置变分量子电路（VQC）相结合。差分和候选滞后项通过基于交换测试（swap-test）驱动的量子自相关（QACF）和量子偏自相关（QPACF）进行识别，并采用延迟矩阵构建将量子投影与时域回归量对齐，随后遵循标准的基于信息准则的简洁性原则。在给定筛选出的阶数 (p,d,q) 后，我们保留固定的 VQC 拟设（ansatz）、优化器和训练预算，以防止超参数泄漏，并将电路部署在两个估计角色中：用于自回归系数的 VQC-AR 和用于移动平均系数的 VQC-MA。在筛选和估计之间，轻量级的 VQC 弱滞后细化在不改变 (p,d,q) 的情况下，对筛选出的 AR 滞后项进行重新加权或剪枝。在环境和工业数据集上，我们针对自动化的经典 ARIMA 进行了滚动起点评估，报告了样本外均方误差（MSE）、平均绝对百分比误差（MAPE）以及针对 MSE 和 MAE 的 Diebold-Mariano 检验。从经验上看，这七项量子贡献——(1) 差分选择、(2) QACF、(3) QPACF、(4) 带有延迟矩阵构建的交换测试原语、(5) VQC-AR、(6) VQC 弱滞后细化以及 (7) VQC-MA——共同减少了元优化开销，并明确了量子效应在阶数发现、滞后细化以及 AR/MA 参数估计中的切入点。

## Abstract
We present a quantum-inspired ARIMA methodology that integrates quantum-assisted lag discovery with \emph{fixed-configuration} variational quantum circuits (VQCs) for parameter estimation and weak-lag refinement. Differencing and candidate lags are identified via swap-test-driven quantum autocorrelation (QACF) and quantum partial autocorrelation (QPACF), with a delayed-matrix construction that aligns quantum projections to time-domain regressors, followed by standard information-criterion parsimony. Given the screened orders $(p,d,q)$, we retain a fixed VQC ansatz, optimizer, and training budget, preventing hyperparameter leakage, and deploy the circuit in two estimation roles: VQC-AR for autoregressive coefficients and VQC-MA for moving-average coefficients. Between screening and estimation, a lightweight VQC weak-lag refinement re-weights or prunes screened AR lags without altering $(p,d,q)$. Across environmental and industrial datasets, we perform rolling-origin evaluations against automated classical ARIMA, reporting out-of-sample mean squared error (MSE), mean absolute percentage error (MAPE), and Diebold--Mariano tests on MSE and MAE. Empirically, the seven quantum contributions -- (1) differencing selection, (2) QACF, (3) QPACF, (4) swap-test primitives with delayed-matrix construction, (5) VQC-AR, (6) VQC weak-lag refinement, and (7) VQC-MA -- collectively reduce meta-optimization overhead and make explicit where quantum effects enter order discovery, lag refinement, and AR/MA parameter estimation.

---

## 论文详细总结（自动生成）

### QARIMA：一种用于经典时间序列分析的量子方法——论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
时间序列预测在经济、能源、医疗等领域至关重要。经典的自回归差分移动平均模型（ARIMA）虽然具有良好的可解释性和计算效率，但在捕捉**长程依赖、非线性动力学及随机效应**方面存在局限。此外，ARIMA 的阶数识别（p, d, q）高度依赖启发式方法（如 PACF、AIC/BIC 栅格搜索），在噪声环境下较为脆弱。
**研究动机**：利用量子计算的原理（如量子相似度度量、变分量子电路 VQC）来增强 ARIMA 的各个环节，在保持模型可解释性的同时，提升其在复杂数据上的预测精度和鲁棒性。

#### 2. 方法论：核心思想与技术细节
QARIMA 提出了一个包含七个协调组件的混合量子-经典框架，核心思想是将量子投影逻辑嵌入到 ARIMA 的阶数发现和参数估计中：
*   **量子相似度度量（Compact Swap Test）**：利用紧凑交换测试估计向量间的余弦相似度，作为量子版本的自相关（QACF）和偏自相关（QPACF）基础。
*   **阶数识别（p, d, q）**：
    *   **差分选择（d）**：通过量子启发式投影框架（替代传统的 ADF 检验）确定使序列平稳的最小差分阶数。
    *   **滞后项筛选（p, q）**：利用 QACF 和 QPACF 识别候选滞后项，并结合信息准则（Parsimony）确定阶数。
*   **参数估计（VQC 优化）**：
    *   **VQC-AR/MA**：使用固定配置的变分量子电路（Ry 旋转层 + CNOT 纠缠层）来估计自回归（AR）和移动平均（MA）系数。
    *   **损失函数**：结合了预测误差、余弦对齐惩罚（经典与量子相似度的差异）以及基于交换测试概率的二元熵不确定性正则项。
*   **弱滞后细化（Weak-Lag Refinement）**：在不改变模型阶数的前提下，利用轻量级 VQC 对筛选出的 AR 滞后项进行重新加权或剪枝，以增强稀疏性和泛化能力。

#### 3. 实验设计
*   **数据集**：涵盖了环境、工业和天文等 5 个公共数据集：
    1.  **Sunspots**（太阳黑子）：具有长记忆和准周期性。
    2.  **Mauna Loa CO2**：具有强趋势和季节性。
    3.  **Australian Woollen Yarn**（羊毛纱产量）：短历史、低波动工业数据。
    4.  **Australian Beer Production**（啤酒产量）：强季节性季度数据。
    5.  **Sydney 2024 Weather**（悉尼天气）：短期气象数据。
*   **Benchmark（基准）**：经典自动化 ARIMA 模型（使用 Python 的 `pmdarima` 库）。
*   **对比指标**：样本外（OOS）均方误差（MSE）、平均绝对百分比误差（MAPE）以及 **Diebold-Mariano (DM) 统计检验**（用于验证预测准确性的显著性差异）。

#### 4. 资源与算力
*   **算力平台**：论文明确提到使用了 **IBM Quantum Experience** 平台及其 **Qiskit** 模拟器后端执行量子电路。
*   **具体细节**：文中**未明确说明**具体的 GPU/CPU 型号、核心数量或总训练时长。由于目前处于模拟阶段，主要依赖经典计算机模拟量子比特的行为。

#### 5. 实验数量与充分性
*   **实验规模**：针对 5 个不同特征的数据集进行了详尽的测试，每个数据集都对比了多个 QARIMA 候选配置（不同 p, d, q 组合）。
*   **充分性与公平性**：
    *   实验采用了**滚动起点评估（Rolling-origin evaluation）**，确保了评估的动态性和客观性。
    *   引入了 DM 检验，从统计学角度证明了改进并非偶然。
    *   **固定配置原则**：在估计阶段保持 VQC 拟设、优化器和预算固定，有效防止了超参数泄露，保证了对比的公平性。

#### 6. 主要结论与发现
*   **性能提升**：在大多数数据集（尤其是 Sunspots、CO2 和 Beer 产量）上，QARIMA 的 MSE 和 MAPE 显著低于经典 ARIMA。
*   **统计显著性**：DM 检验显示，QARIMA 在长记忆和非线性动力学明显的序列上具有统计学意义上的优势。
*   **适用场景**：QARIMA 在处理具有复杂趋势和长程依赖的数据时表现优异；但在极短历史或线性结构极其明显的工业数据（如 Woollen Yarn）上，量子灵活性可能导致轻微过拟合，经典模型仍具竞争力。

#### 7. 优点（亮点）
*   **模块化集成**：并非简单的“量子化”，而是将量子原语精准嵌入到 ARIMA 的各个标准步骤中，保留了统计模型的解释性。
*   **创新的损失函数**：引入量子熵正则化和相位校正机制，能够更好地处理量子测量中的随机性和不确定性。
*   **鲁棒的阶数发现**：通过量子投影（QACF/QPACF）提供了比经典启发式更系统的滞后项识别方法。

#### 8. 不足与局限
*   **硬件限制**：目前主要基于量子模拟器，尚未在真实的大规模 NISQ 硬件上验证其在大规模数据集上的扩展性。
*   **计算开销**：虽然论文声称减少了元优化开销，但 VQC 的混合优化循环在经典模拟器上通常比纯经典回归算法更耗时。
*   **短序列风险**：在样本量极小的情况下，VQC 的表达能力可能引入过拟合风险，需更精细的正则化控制。

（完）
