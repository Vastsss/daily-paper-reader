---
title: "WaveSFNet: A Wavelet-Based Codec and Spatial--Frequency Dual-Domain Gating Network for Spatiotemporal Prediction"
title_zh: WaveSFNet：一种用于时空预测的基于小波的编解码器和空域-频域双域门控网络
authors: "Xinyong Cai, Runming Xie, Hu Chen, Yuankai Wu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23284v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 时空预测与未来帧预测
tldr: 针对时空预测中长程动态建模与高频细节保留的挑战，本文提出WaveSFNet。该框架结合小波编解码器以减少下采样中的纹理损失，并设计了空频双域门控翻译器，通过注入帧间差异增强动态信息，融合大卷积核空间建模与频域全局调制。实验证明，该方法在多个数据集上实现了高精度预测与低计算复杂度的平衡。
selection_source: fresh_fetch
motivation: 现有时空预测模型在下采样过程中易丢失纹理细节，且难以在局部交互与全局传播之间取得平衡。
method: 提出基于小波变换的编解码器以保留高频特征，并结合空频双域门控网络进行动态增强与多尺度特征融合。
result: 在Moving MNIST、TaxiBJ和WeatherBench等数据集上，该模型以较低的计算复杂度实现了领先的预测精度。
conclusion: WaveSFNet证明了结合小波变换与空频双域建模能有效提升时空预测的清晰度与长程建模效率。
---

## 摘要
时空预测学习旨在以无监督的方式从历史观测中预测未来帧，对广泛的应用至关重要。其关键挑战在于建模长程动力学，同时保留高频细节以实现清晰的多步预测。现有的高效无循环框架通常依赖步长卷积或池化进行采样，这往往会丢弃纹理和边界，而纯空间算子通常难以平衡局部交互与全局传播。为了解决这些问题，我们提出了 WaveSFNet，这是一个将基于小波的编解码器与空域-频域双域门控时空转换器相结合的高效框架。基于小波的编解码器在下采样和重建过程中保留了高频子带线索。同时，转换器首先注入相邻帧差分以显式增强动态信息，然后在基于大卷积核的空间局部建模与频域全局调制之间进行双域门控融合，并结合门控通道交互进行跨通道特征交换。大量实验表明，WaveSFNet 在 Moving MNIST、TaxiBJ 和 WeatherBench 上实现了具有竞争力的预测精度，同时保持了较低的计算复杂度。我们的代码可在 https://github.com/fhjdqaq/WaveSFNet 获取。

## Abstract
Spatiotemporal predictive learning aims to forecast future frames from historical observations in an unsupervised manner, and is critical to a wide range of applications. The key challenge is to model long-range dynamics while preserving high-frequency details for sharp multi-step predictions. Existing efficient recurrent-free frameworks typically rely on strided convolutions or pooling for sampling, which tends to discard textures and boundaries, while purely spatial operators often struggle to balance local interactions with global propagation. To address these issues, we propose WaveSFNet, an efficient framework that unifies a wavelet-based codec with a spatial--frequency dual-domain gated spatiotemporal translator. The wavelet-based codec preserves high-frequency subband cues during downsampling and reconstruction. Meanwhile, the translator first injects adjacent-frame differences to explicitly enhance dynamic information, and then performs dual-domain gated fusion between large-kernel spatial local modeling and frequency-domain global modulation, together with gated channel interaction for cross-channel feature exchange. Extensive experiments demonstrate that WaveSFNet achieves competitive prediction accuracy on Moving MNIST, TaxiBJ, and WeatherBench, while maintaining low computational complexity. Our code is available at https://github.com/fhjdqaq/WaveSFNet.

---

## 论文详细总结（自动生成）

以下是对论文《WaveSFNet: A Wavelet-Based Codec and Spatial–Frequency Dual-Domain Gating Network for Spatiotemporal Prediction》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在时空预测（如天气预报、交通流预测）中，如何在建模长程动态规律的同时，保留高频细节（如边缘和纹理），以避免多步预测中的模糊和误差累积。
*   **研究背景**：
    *   **循环模型（Recurrent-based）**：如 ConvLSTM、PredRNN，因串行计算导致并行化差、计算成本高。
    *   **无循环模型（Recurrent-free）**：如 SimVP、TAU，虽然并行效率高，但通常依赖步长卷积或池化进行下采样，容易丢失高频信息。
    *   **算子局限性**：纯空间算子难以平衡局部交互与全局传播。

### 2. 方法论
WaveSFNet 采用“编码器-翻译器-解码器”的无循环架构，核心创新点包括：
*   **基于小波的多尺度编解码器（Wavelet-based Codec）**：
    *   使用**离散小波变换（DWT）**替代传统的步长卷积进行下采样。DWT（如 Haar 小波）能将图像分解为低频结构和高频子带（水平、垂直、对角线细节），通过 $1 \times 1$ 卷积融合这些子带，从而在压缩表征时最大程度保留结构细节。
    *   解码阶段使用**逆离散小波变换（IDWT）**对称恢复分辨率。
*   **时空翻译器（Spatiotemporal Translator）**：
    *   **时间差分注入（TDI Block）**：通过计算相邻帧的潜在表征差异并进行门控调制，显式增强模型对快速动态变化的敏感度。
    *   **空频双域门控块（ST Block）**：
        *   **空间分支**：利用 $9 \times 9$ 大卷积核深度卷积捕获局部邻域结构。
        *   **频域分支**：利用 2D 快速傅里叶变换（FFT）在谱域进行全局调制，捕获长程依赖。
        *   **门控通道交互**：通过 SiLU 门控机制实现跨通道的非线性信息交换。

### 3. 实验设计
*   **数据集**：
    *   **Moving MNIST**：合成运动基准，测试处理简单几何运动的能力。
    *   **TaxiBJ**：北京出租车流量数据，测试处理复杂城市时空动态的能力。
    *   **WeatherBench**：全球天气预报基准，包括单变量（温度、湿度、风速等）和多变量预测。
*   **对比方法**：涵盖了循环类（ConvLSTM, PredRNN, VMRNN 等）和无循环类（SimVP, TAU, MogaNet, WaST 等）共十余种主流模型。
*   **评价指标**：MSE、MAE、RMSE、SSIM、PSNR，以及模型效率指标（Params, FLOPs）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量及总训练时长。
*   **训练配置**：提供了详细的超参数表（Table I），包括 Batch Size（均为 16）、学习率（$9.2 \times 10^{-4}$ 到 $4.15 \times 10^{-3}$ 不等）、训练轮数（Moving MNIST 为 2000 Epochs，其余为 50 Epochs）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同领域的基准数据集上进行了测试，其中 WeatherBench 涵盖了 4 个单变量任务和 1 个 12 变量的多变量任务。
*   **消融实验**：设计了 5 组消融实验，分别验证了空间分支、频域分支、TDI 模块、小波编解码器以及门控交互机制的有效性。
*   **充分性评价**：实验设计较为全面，不仅对比了预测精度，还通过频谱分析（RAPSD）验证了模型在保留高频分量上的优越性，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：WaveSFNet 在所有测试数据集上均达到了 SOTA 或同等水平的精度。例如在 Moving MNIST 上 MSE 达到 15.8，显著优于其他模型。
*   **效率优势**：在保持高精度的同时，参数量和计算量远低于传统的循环模型。在 TaxiBJ 任务中，其 FLOPs 仅为 1.1G，远低于 PredRNN 的 42.4G。
*   **频谱对齐**：频谱分析显示，WaveSFNet 的预测结果在功率谱密度上与真实值（Ground Truth）最接近，证明了小波变换和双域建模对缓解“平滑偏差”（模糊感）的有效性。

### 7. 优点
*   **高频保留**：通过数学上可逆的小波变换解决了下采样过程中的信息丢失问题。
*   **双域融合**：巧妙结合了卷积的局部性和傅里叶变换的全局性，提升了长程建模能力。
*   **轻量高效**：无循环设计使其具备极高的推理并行度，适合资源受限的边缘设备部署。

### 8. 不足与局限
*   **长程预测依赖策略**：对于超长序列预测，仍需采用自回归（Rollout）策略，这可能导致长期的误差累积。
*   **小波基选择局限**：实验主要基于 Haar 小波，未探讨其他更复杂的小波基（如 Daubechies）对性能的影响。
*   **硬件适配性**：虽然 FLOPs 较低，但 FFT 操作在某些不具备专用 DSP 或优化库的硬件上可能存在实际延迟高于理论值的情况。

（完）
