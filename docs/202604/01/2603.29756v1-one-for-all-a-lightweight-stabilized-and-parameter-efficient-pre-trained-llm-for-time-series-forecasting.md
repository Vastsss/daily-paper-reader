---
title: "One-for-All: A Lightweight Stabilized and Parameter-Efficient Pre-trained LLM for Time Series Forecasting"
title_zh: One-for-All：一种用于时间序列预测的轻量级、稳定且参数高效的预训练大语言模型
authors: "Prasanjit Dey, Soumyabrata Dev, Bianca Schoen-Phelan"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29756v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 针对多变量时间序列分析的冻结大语言模型参数高效微调
tldr: "本研究针对预训练大语言模型（LLM）在多变量时间序列分析中计算和内存开销巨大的挑战，提出了One-for-All框架。该框架引入了高斯秩稳定低秩适配器（rsLoRA），通过在位置嵌入和输出层注入极低秩的可训练矩阵并冻结自注意力权重，实现了参数高效的微调。One-for-All在显著降低参数量（减少98.3%）和内存占用（缩小168-1776倍）的同时，保持了与SOTA模型相当的预测精度，为边缘设备部署提供了可能。"
selection_source: fresh_fetch
motivation: 预训练大语言模型在处理多变量时间序列时面临极高的计算资源和内存需求，限制了其在实际场景及边缘设备上的部署。
method: 提出One-for-All框架，利用具有数学稳定性保证的rsLoRA技术对冻结的LLM进行极低秩微调，仅在特定层注入少量可训练参数。
result: 在多个基准任务中实现了与SOTA相当的精度，且参数效率提升5.5至21倍，内存占用大幅降低至2.2MiB。
conclusion: 该研究通过秩稳定机制实现了极高的参数效率和数值稳定性，证明了在资源受限环境下部署高性能LLM时间序列模型的可行性。
---

## 摘要
我们解决了将预训练大语言模型（LLMs）适配于多变量时间序列分析的挑战，这类模型的部署通常受限于极高的计算和内存需求。我们的解决方案 One-for-All 引入了高斯秩稳定低秩适配器（rsLoRA），以实现对冻结 LLM 的参数高效微调。虽然受到 LoRA 的启发，但 rsLoRA 引入了一种具有数学依据的秩稳定机制，能够在低秩情况下实现可证明的梯度稳定性，这是以往参数高效微调（PEFT）方法中所不具备的新颖贡献。我们的框架在保持自注意力权重固定的同时，将可训练的秩分解矩阵（秩为 16）注入到位置嵌入和输出层中。这种设计将可训练参数量分别减少了 6.8 倍（对比 TimesNet）、21 倍（对比 GPT4TS）和 11.8 倍（对比 TIME-LLM），同时实现了 168 至 1,776 倍的内存占用缩减（2.2MiB 对比 SOTA 模型的 340MiB-4.18GiB）。在六个时间序列任务上的严格评估表明，One-for-All 实现了最先进的效率与准确度权衡：参数效率比 TimesNet 高 5.5 倍，比 GPT4TS 高 21 倍，同时达到了相当的预测准确度（MSE=0.33）。该框架的稳定性通过在不同预测长度（96-720 步）和数据集（ETT、Weather、M3、M4）上的一致表现得到了验证，其参数量比传统 Transformer 减少了 98.3%。这些进展使得在不牺牲性能的前提下，将模型部署到医疗、金融和环境监测等领域的边缘设备上成为可能。

## Abstract
We address the challenge of adapting pre-trained Large Language Models (LLMs) for multivariate time-series analysis, where their deployment is often hindered by prohibitive computational and memory demands. Our solution, One-for-All, introduces Gaussian Rank-Stabilized Low-Rank Adapters (rsLoRA) to enable parameter-efficient fine-tuning of frozen LLMs. While inspired by LoRA, rsLoRA introduces a mathematically grounded rank-stabilization mechanism that enables provable gradient stability at low ranks a novel contribution absent in prior PEFT methods. Our framework injects trainable rank decomposition matrices (rank 16) into positional embeddings and output layers, while keeping self-attention weights fixed. This design reduces trainable parameters by 6.8$\times$ (vs. TimesNet), 21$\times$ (vs. GPT4TS), and 11.8$\times$ (vs. TIME-LLM), while achieving a 168-1,776$\times$ smaller memory footprint (2.2MiB vs. 340MiB-4.18GiB in SOTA models). Rigorous evaluation across six time-series tasks demonstrates that One-for-All achieves state-of-the-art efficiency-accuracy trade-offs: 5.5$\times$ higher parameter efficiency (MSE=5.50) than TimesNet and 21$\times$ better than GPT4TS, while matching their forecasting accuracy (MSE=0.33). The framework's stability is validated through consistent performance across diverse horizons (96-720 steps) and datasets (ETT, Weather, M3, M4), with 98.3% fewer parameters than conventional transformers. These advances enable deployment on edge devices for healthcare, finance, and environmental monitoring without compromising performance.

---

## 论文详细总结（自动生成）

### One-for-All：一种用于时间序列预测的轻量级、稳定且参数高效的预训练大语言模型

#### 1. 核心问题与研究动机
*   **核心问题**：如何高效地将预训练大语言模型（LLM）适配到多变量时间序列分析任务中，同时克服其巨大的计算开销、内存需求以及时间序列数据特有的非平稳性和多尺度依赖问题。
*   **研究动机**：
    *   **数据稀缺**：时间序列数据量远小于 NLP 语料，难以支持全参数微调。
    *   **资源受限**：现有 LLM 适配方法（如 GPT4TS, TIME-LLM）虽然有效，但参数量和内存占用依然过高，难以在边缘设备部署。
    *   **适配失配**：标准适配器（如 LoRA）在处理非平稳时间序列时，低秩下的梯度稳定性不足，且难以处理不规则采样。

#### 2. 核心方法论
*   **核心思想**：提出 **One-for-All** 框架，通过冻结 LLM 主干网络，仅在位置嵌入层和输出层注入改进的**高斯秩稳定低秩适配器（rsLoRA）**。
*   **关键技术细节**：
    *   **rsLoRA (Rank-Stabilized LoRA)**：这是本文的核心创新。不同于标准 LoRA 使用固定缩放因子，rsLoRA 引入了基于秩 $r$ 的动态缩放因子 $\beta_r = \alpha / \sqrt{r}$。
    *   **数学稳定性证明**：论文通过定理证明，当 $r \to \infty$ 时，该缩放因子能确保适配器输出和梯度的矩（Moments）保持稳定（$\Theta_r(1)$），从而在极低秩（如 Rank 16）下实现优异的收敛性。
    *   **架构设计**：使用冻结的 GPT-2 作为主干，仅微调 0.55M 的参数。
    *   **预处理流程**：采用实例归一化（Instance Normalization）应对非平稳性，通过分块（Patching）将原始序列转化为 Token，以提高计算效率并捕捉局部趋势。

#### 3. 实验设计
*   **实验场景**：涵盖了六大核心任务：长/短期预测、少样本（Few-shot）预测、零样本（Zero-shot）预测、分类及异常检测。
*   **数据集**：
    *   **预测**：ETT (ETTh1, ETTh2, ETTm1, ETTm2), Weather, M3, M4。
    *   **分类**：UEA 多变量分类数据集（如 Japanese Vowels, Heartbeat 等）。
    *   **异常检测**：SMD, MSL, SMAP, SWaT, PSM。
*   **对比方法（Benchmarks）**：
    *   **LLM 相关**：GPT4TS, TIME-LLM, TEST, TEMPO。
    *   **经典/SOTA 模型**：TimesNet, FEDformer, Autoformer, Informer, ETSformer, LightTS 等。

#### 4. 资源与算力
*   **硬件环境**：实验使用了 **NVIDIA A100 80GB GPU**。
*   **训练细节**：论文提供了详细的超参数表（如学习率 $10^{-3}$，Batch Size 256-512，训练 10 个 Epochs）。虽然未明确给出总训练时长，但强调了其极低的内存占用（仅 **2.2MiB**），远低于对比模型的数百 MiB 甚至 GiB 级别。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括在 5 个数据集上的长/短期预测、不同比例（5%, 10%）的少样本实验、跨数据集（M3 $\leftrightarrow$ M4）的零样本迁移实验。
*   **消融实验**：针对 **秩（Rank）** 进行了从 2 到 1024 的深度消融分析，验证了性能在 Rank 16 时即达到饱和。
*   **充分性评价**：实验设计非常充分，涵盖了时间序列分析的几乎所有主流子领域，且对比了从传统 Transformer 到最新 LLM-based 的各类模型，结果具有高度的客观性和说服力。

#### 6. 主要结论与发现
*   **效率突破**：One-for-All 仅需 0.55M 可训练参数，比传统 Transformer 减少 98.3%，内存占用比 TIME-LLM 小 1776 倍。
*   **性能持平**：在参数量极小的情况下，其预测精度（MSE=0.33）与参数量巨大的 GPT4TS 和 TIME-LLM 相当，甚至在某些任务（如 Weather 数据集）上表现更优。
*   **稳定性**：rsLoRA 成功解决了低秩下的梯度消失/爆炸问题，使得模型在不同预测长度（96-720 步）下表现极其稳定。

#### 7. 优点
*   **极高的资源效率**：2.2MiB 的内存占用使其成为目前最适合边缘设备部署的 LLM 时间序列模型。
*   **理论支撑**：不仅是经验性的改进，还为 rsLoRA 的稳定性提供了严格的数学证明。
*   **通用性强**：一个统一的框架（One-for-All）即可处理预测、分类和异常检测，无需针对特定任务大幅修改架构。

#### 8. 不足与局限
*   **零样本性能波动**：在某些特定的零样本迁移方向（如 M4 $\to$ M3）上，性能虽具竞争力但未达到绝对领先，说明跨域知识迁移仍有优化空间。
*   **主干依赖**：目前主要基于 GPT-2 进行验证，虽然理论上通用，但在更大规模（如 Llama-3）或其他架构 LLM 上的表现尚未在文中充分展示。
*   **不规则采样处理**：虽然提到了 Patching 可以处理不规则采样，但实验部分主要还是基于标准基准数据集，对极端缺失值或异构频率的实战表现缺乏深入探讨。

（完）
