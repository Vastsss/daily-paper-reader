---
title: "MsFormer: Enabling Robust Predictive Maintenance Services for Industrial Devices"
title_zh: MsFormer：为工业设备实现稳健的预测性维护服务
authors: "Jiahui Zhou, Dan Li, Ruibing Jin, Jian Lou, Yanran Zhao, Zhenghua Chen, Zigui Jiang, See-Kiong Ng"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23076v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 基于Transformer的工业传感器时间序列模型
tldr: 针对工业设备预测性维护中传感器数据存在多尺度时间相关性及训练数据稀缺的挑战，本文提出了一种轻量化多尺度Transformer模型MsFormer。该模型通过多尺度采样模块和定制位置编码捕捉复杂序列关联，并利用基于池化操作的轻量化注意力机制降低对数据量的依赖。实验证明，MsFormer在多个真实数据集上显著优于现有方法，具有极强的泛化能力和高可靠的服务质量。
selection_source: fresh_fetch
motivation: 现有深度学习模型难以有效处理工业物联网数据中的多尺度时间相关性，且在数据稀缺环境下难以实现鲁棒的预测服务。
method: 提出MsFormer模型，通过多尺度采样模块捕捉序列关联，并采用基于池化操作的轻量化注意力机制替代自注意力以适应小样本环境。
result: 在真实工业数据集上的广泛实验表明，MsFormer在预测精度和泛化能力上均显著优于现有的最先进方法。
conclusion: MsFormer为工业预测性维护提供了一个轻量、鲁棒且高效的通用AI服务框架，能够有效应对复杂工况和数据稀缺问题。
---

## 摘要
提供可靠的预测性维护是一项关键的工业人工智能服务，对于确保制造设备的高可用性至关重要。现有的深度学习方法在这些任务上表现出了具有竞争力的结果，但缺乏一个通用的面向服务的框架来捕捉工业物联网传感器数据中的复杂依赖关系。虽然基于 Transformer 的模型展现了强大的序列建模能力，但将其直接部署为稳健的 AI 服务面临着重大瓶颈。具体而言，在真实服务环境中收集的流式传感器数据通常表现出由机器工作原理驱动的多尺度时间相关性。此外，用于训练故障前时间预测服务的可用数据集通常规模有限。这些问题为将现有模型直接应用为稳健的预测服务带来了巨大挑战。为了应对这些挑战，我们提出了 MsFormer，这是一种轻量级的多尺度 Transformer，被设计为用于可靠工业预测性维护的统一 AI 服务模型。MsFormer 结合了多尺度采样（MS）模块和定制的位置编码机制，以捕捉多流服务数据中的序列相关性。此外，为了适应数据稀缺的服务环境，MsFormer 采用了一种带有简单池化操作的轻量级注意力机制，而非自注意力机制。在真实数据集上的广泛实验表明，所提出的框架相比现有最先进的方法实现了显著的性能提升。此外，MsFormer 在不同工业设备和运行工况下均表现优异，在保持高度可靠的服务质量（QoS）的同时，展现了强大的泛化能力。

## Abstract
Providing reliable predictive maintenance is a critical industrial AI service essential for ensuring the high availability of manufacturing devices. Existing deep-learning methods present competitive results on such tasks but lack a general service-oriented framework to capture complex dependencies in industrial IoT sensor data. While Transformer-based models show strong sequence modeling capabilities, their direct deployment as robust AI services faces significant bottlenecks. Specifically, streaming sensor data collected in real-world service environments often exhibits multi-scale temporal correlations driven by machine working principles. Besides, the datasets available for training time-to-failure predictive services are typically limited in size. These issues pose significant challenges for directly applying existing models as robust predictive services. To address these challenges, we propose MsFormer, a lightweight Multi-scale Transformer designed as a unified AI service model for reliable industrial predictive maintenance. MsFormer incorporates a Multi-scale Sampling (MS) module and a tailored position encoding mechanism to capture sequential correlations across multi-streaming service data. Additionally, to accommodate data-scarce service environments, MsFormer adopts a lightweight attention mechanism with straightforward pooling operations instead of self-attention. Extensive experiments on real-world datasets demonstrate that the proposed framework achieves significant performance improvements over state-of-the-art methods. Furthermore, MsFormer outperforms across industrial devices and operating conditions, demonstrating strong generalizability while maintaining a highly reliable Quality of Service (QoS).

---

## 论文详细总结（自动生成）

以下是对论文《MsFormer: Enabling Robust Predictive Maintenance Services for Industrial Devices》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在工业物联网（IIoT）环境下，针对传感器数据构建稳健的预测性维护（PdM）服务，特别是剩余使用寿命（RUL）的预测。
*   **研究动机**：
    *   **多尺度相关性**：工业传感器数据具有复杂的时间依赖性，受机器工作原理影响，其退化模式存在于不同的时间尺度上，传统Transformer难以有效捕捉。
    *   **数据稀缺性**：工业特定设备的故障数据集通常规模较小，而标准Transformer模型参数量大、深度深，在小样本环境下容易过拟合，难以保证服务质量（QoS）。
    *   **现有方法局限**：CNN难以建模长程依赖，RNN计算延迟高且易梯度消失，而现有Transformer变体在处理语义稀疏的传感器数据时效率低下。

### 2. 核心方法论
论文提出了 **MsFormer**，一个轻量级的多尺度Transformer框架，主要包含以下核心组件：
*   **四阶段分层架构**：模型分为四个阶段。前两个阶段侧重于初步特征抽象，后两个阶段侧重于深度时间依赖建模。
*   **多尺度采样（MS）模块**：通过设置不同的下采样因子（$\lambda_w$），将原始序列重组为多个具有不同时间跨度的子序列。在四个阶段中，$\lambda_w$ 分别设为 4, 4, 2, 1，实现从粗粒度到细粒度的特征提取。
*   **轻量级注意力（LA）块**：在模型的前两个阶段，使用简单的**池化操作（Pooling）**替代复杂的自注意力机制。这种设计降低了计算复杂度，并增强了模型在小规模工业数据集上的泛化能力。
*   **相对位置编码（RPE）块**：在后两个阶段引入，结合自注意力机制。
*   **多尺度位置编码机制**：针对采样后序列顺序的变化，设计了一个分段索引函数。该函数根据下采样因子调整位置权重，能够更准确地描述采样序列中元素间的空间/时间关系。

### 3. 实验设计
*   **数据集**：
    *   **C-MAPSS**：航空发动机退化数据集（包含FD001-FD004四个子集，涵盖不同工况和故障模式）。
    *   **NASA Battery Dataset**：锂离子电池充放电循环数据集，用于验证跨设备的泛化性。
*   **Benchmark（基准方法）**：对比了包括 BiLSTM、CNN-based (PE-Net)、Transformer-based (TF-SCN, FEDformer, Autoformer, iTransformer) 以及最新的图神经网络方法 (GAT-DAT, DVGTformer) 等十余种 SOTA 模型。
*   **评价指标**：均方根误差 (RMSE)、评分函数 (Score，对延迟预测惩罚更高) 和平均绝对误差 (MAE)。

### 4. 资源与算力
*   **硬件环境**：使用了一台 **NVIDIA A100-SXM4-80GB GPU**。
*   **训练配置**：Batch size 为 128，训练 300 个 epoch，使用 Adam 优化器，学习率为 0.001。
*   **模型复杂度**：MsFormer 的参数量仅为 **0.66M**，远低于 FEDformer (>114M) 或 Informer (>11M) 等通用时间序列模型。

### 5. 实验数量与充分性
*   **实验规模**：在两个大类数据集（共 8 个子场景）上进行了详尽测试。
*   **消融实验**：
    *   验证了 MS 模块在不同阶段的作用。
    *   测试了不同下采样因子 $\lambda_w$ 的组合效果。
    *   对比了多种位置编码机制（如 Shaw 等、T5 的 RPE）。
    *   分析了 LA 块与 RPE 块的堆叠策略对性能的影响。
*   **充分性评价**：实验设计客观、公平，通过跨数据集验证和参数敏感性分析，充分证明了模型在不同工况下的稳健性和轻量化优势。

### 6. 主要结论与发现
*   **性能领先**：MsFormer 在 C-MAPSS 的复杂子集（FD002/FD004）上显著优于所有基准模型，证明了其处理复杂工况的能力。
*   **轻量高效**：通过引入池化操作和多尺度采样，模型在保持极低参数量（0.66M）的同时，实现了比重型 Transformer 更好的预测精度。
*   **泛化性强**：在 NASA 电池数据集上的优异表现说明该模型是一个通用的工业 AI 服务框架，不局限于特定设备。
*   **位置编码的重要性**：实验发现，针对采样序列定制的位置编码对提升 RUL 预测精度至关重要。

### 7. 优点
*   **架构创新**：巧妙地结合了多尺度采样与分层注意力机制，解决了工业数据“语义稀疏”和“尺度多样”的问题。
*   **实用性高**：轻量化设计使其非常适合部署在资源受限的工业边缘计算节点中。
*   **鲁棒性**：在数据稀缺和复杂工况下表现稳定，符合工业级 AIaaS（AI即服务）的要求。

### 8. 不足与局限
*   **窗口长度固定**：实验中滑动窗口长度固定为 28，未深入探讨不同窗口长度对多尺度采样效果的影响。
*   **任务覆盖面**：论文主要聚焦于回归任务（RUL 预测），尚未验证其在故障分类或异常检测等其他 PdM 任务中的表现。
*   **超参数依赖**：多尺度采样因子 $\lambda_w$ 和位置编码阈值 $c_1, c_2$ 需要根据经验或特定数据集进行调整，缺乏自动化的参数搜索机制。

（完）
