---
title: A Comparative Study of Machine Learning Models for Hourly Forecasting of Air Temperature and Relative Humidity
title_zh: 气温与相对湿度逐小时预测的机器学习模型对比研究
authors: Jiaqi Dong
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23282v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 机器学习在逐小时时间序列预测中的对比研究
tldr: 本研究针对重庆等地形复杂城市的短时气象预测需求，对比了XGBoost、随机森林、LSTM等七种机器学习模型在逐小时气温和相对湿度预测中的表现。通过统一的数据预处理、滞后特征构建及滚动统计特征，系统评估了各模型的准确性与鲁棒性。结果表明，XGBoost在各项指标上均表现最优，为山地城市智能气象预报提供了高效可靠的技术方案。
selection_source: fresh_fetch
motivation: 针对地形复杂城市对高精度短时气温和湿度预测的迫切需求，探索最优的机器学习预测模型。
method: 在统一框架下对比了XGBoost、随机森林、SVR、MLP、决策树、LSTM及CNN-LSTM七种模型，并结合滞后特征与滚动统计进行验证。
result: "XGBoost模型表现最佳，气温预测MAE为0.302°C，湿度预测MAE为1.271%，平均R²达到0.989。"
conclusion: 基于树的集成学习模型在处理结构化气象时间序列预测任务中具有显著优势，为山地城市气象预报提供了实践指导。
---

## 摘要
准确的气温和相对湿度短期预测对于城市管理至关重要，特别是在中国重庆等地形复杂的城市。本研究利用真实世界的开放数据，对比了七种用于逐小时预测的机器学习模型：极端梯度提升（XGBoost）、随机森林（Random Forest）、支持向量回归（SVR）、多层感知器（MLP）、决策树（Decision Tree）、长短期记忆网络（LSTM）以及卷积神经网络-长短期记忆网络（CNN-LSTM）。基于数据预处理、滞后特征构建、滚动统计特征和时间序列验证的统一框架，本研究从预测准确性和鲁棒性两个方面对模型进行了系统评估。结果表明，XGBoost 取得了最佳的综合性能，其气温预测的测试平均绝对误差（MAE）为 0.302 °C，相对湿度的 MAE 为 1.271%，两项预测任务的平均 R² 达到 0.989。这些研究结果证明了基于树的集成学习在结构化气象时间序列预测中的强大有效性，并为山地城市的智能气象预报提供了实践指导。

## Abstract
Accurate short-term forecasting of air temperature and relative humidity is critical for urban management, especially in topographically complex cities such as Chongqing, China. This study compares seven machine learning models: eXtreme Gradient Boosting (XGBoost), Random Forest, Support Vector Regression (SVR), Multi-Layer Perceptron (MLP), Decision Tree, Long Short-Term Memory (LSTM) networks, and Convolutional Neural Network (CNN)-LSTM (CNN-LSTM), for hourly prediction using real-world open data. Based on a unified framework of data preprocessing, lag-feature construction, rolling statistical features, and time-series validation, the models are systematically evaluated in terms of predictive accuracy and robustness. The results show that XGBoost achieves the best overall performance, with a test mean absolute error (MAE) of 0.302 °C for air temperature and 1.271% for relative humidity, together with an average R2 of 0.989 across the two forecasting tasks. These findings demonstrate the strong effectiveness of tree-based ensemble learning for structured meteorological time-series forecasting and provide practical guidance for intelligent meteorological forecasting in mountainous cities.

---

## 论文详细总结（自动生成）

以下是对论文《A Comparative Study of Machine Learning Models for Hourly Forecasting of Air Temperature and Relative Humidity》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：准确的短时气象预测（气温与湿度）对城市管理、能源调度及防灾减灾至关重要。重庆作为典型的山地城市，地形复杂且具有亚热带季风气候，传统的数值天气预报（NWP）模型在捕捉高时空分辨率的微气候演变方面面临巨大挑战。
*   **核心问题**：在复杂地理背景下，哪种机器学习或深度学习模型能够最有效地处理结构化气象时间序列，实现高精度的逐小时预测？

### 2. 方法论
*   **核心思想**：构建一个统一的端到端框架，通过系统化的特征工程（捕捉时间依赖和局部动态）来增强传统与深度学习模型的预测能力。
*   **关键技术细节**：
    *   **数据预处理**：严格的时间排序、缺失值前后填充、异常值剔除及标准化。
    *   **特征工程**：
        *   **滞后特征（Lag Features）**：引入过去 2, 3, 6, 12, 24 小时的观测值，捕捉自相关性。
        *   **滚动统计（Rolling Statistics）**：计算 3, 6, 12, 24 小时窗口的移动平均值和标准差，表征局部趋势和波动。
        *   **外部协变量**：整合降水、风速、海平面气压、云量和太阳辐射等辅助变量。
    *   **模型构建**：对比了回归（SVR）、集成学习（RF, XGBoost）、浅层神经网络（MLP）及深度学习（LSTM, CNN-LSTM）等七种范式。
    *   **多目标策略**：针对气温和湿度双目标，采用 `MultiOutputRegressor` 封装或多神经元输出层。

### 3. 实验设计
*   **数据集**：使用来自 Visual Crossing 的重庆真实观测数据，时间跨度为 2024 年 9 月 9 日至 2026 年 1 月 13 日，涵盖完整的季节周期。
*   **对比方法（Benchmark）**：
    *   **传统/浅层模型**：决策树（DT）、支持向量回归（SVR）、多层感知器（MLP）。
    *   **集成模型**：随机森林（RF）、XGBoost。
    *   **深度学习模型**：LSTM、CNN-LSTM（混合架构）。
*   **评估指标**：MAE、RMSE、$R^2$、MAPE。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号或训练时长。
*   **实现细节**：提到使用 Python 3.9 开发，传统模型基于 `scikit-learn`，深度学习模型基于 `TensorFlow/Keras`。在超参数优化中启用了 `n_jobs=-1` 进行多核并行计算。

### 5. 实验数量与充分性
*   **实验规模**：针对 7 种模型分别在气温和湿度两个任务上进行了训练与测试。
*   **充分性与客观性**：
    *   采用了 **5 折时间序列交叉验证（TimeSeriesSplit）**，有效防止了时间序列预测中的“未来信息泄露”问题。
    *   使用了网格搜索（Grid Search）进行超参数优化，确保了各模型在对比时的公平性。
    *   实验涵盖了完整的季节循环，数据具有较好的代表性。

### 6. 主要结论与发现
*   **XGBoost 表现最优**：在所有指标上均排名第一，气温预测 MAE 为 0.302 °C，湿度预测 MAE 为 1.271%，平均 $R^2$ 达 0.989。
*   **深度学习表现不及预期**：在当前数据规模下，CNN-LSTM 表现最差（气温预测 $R^2$ 仅 0.526），LSTM 也逊色于集成学习模型。这表明对于中等规模的结构化气象数据，基于树的集成模型比复杂的深度架构更具鲁棒性。
*   **模型稳定性**：SVR 和 MLP 表现稳健，紧随 XGBoost 之后；而随机森林（RF）存在一定的过拟合倾向。

### 7. 优点
*   **特征工程精细**：系统地构建了滞后和滚动特征，充分挖掘了气象数据的非线性动力学特征。
*   **验证严谨**：使用了专门针对时间序列的验证框架，而非随机划分，保证了结果在实际业务中的参考价值。
*   **实用性强**：针对重庆这一特定复杂地形给出了明确的模型选择建议，具有较强的工程指导意义。

### 8. 不足与局限
*   **空间信息缺失**：研究仅基于单站点的历史数据，未考虑周边气象站的空间相关性（Spatial Correlation）。
*   **深度学习调优空间**：CNN-LSTM 的低性能可能源于数据量尚不足以支撑深度架构的训练，或超参数搜索空间仍有局限。
*   **物理约束缺乏**：作为纯数据驱动研究，模型未引入气象学物理方程（如热力学约束），在极端天气下的外推能力可能受限。

（完）
