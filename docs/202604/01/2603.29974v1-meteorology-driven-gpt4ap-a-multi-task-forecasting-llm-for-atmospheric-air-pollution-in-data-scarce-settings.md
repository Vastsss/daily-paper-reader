---
title: "Meteorology-Driven GPT4AP: A Multi-Task Forecasting LLM for Atmospheric Air Pollution in Data-Scarce Settings"
title_zh: 气象驱动的 GPT4AP：一种用于数据稀缺场景下大气空气污染多任务预测的大语言模型
authors: "Prasanjit Dey, Soumyabrata Dev, Bianca Schoen-Phelan"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29974v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 基于预训练GPT-2的时间序列多任务预测LLM
tldr: 针对大气污染预测在数据稀缺地区泛化能力差的问题，本文提出GPT4AP框架。该模型基于预训练GPT-2，利用rsLoRA技术实现参数高效微调，仅调整轻量化位置和输出模块。实验表明，GPT4AP在少样本、零样本跨站点及长期预测任务中表现优异，显著提升了模型在受限数据环境下的鲁棒性与泛化性能，为环境监测提供了高效的决策支持。
selection_source: fresh_fetch
motivation: 旨在解决传统数据驱动模型在观测数据稀缺地区进行大气污染预测时泛化能力不足的问题。
method: 提出一种基于预训练GPT-2和rsLoRA技术的参数高效多任务预测框架，通过冻结核心层并微调轻量化模块来降低训练开销。
result: 在少样本和零样本跨站点实验中，GPT4AP的预测精度显著优于DLinear和ETSformer等主流基准模型。
conclusion: GPT4AP证明了预训练大语言模型在数据受限和领域偏移场景下进行大气污染预测的强大鲁棒性和高效性。
---

## 摘要
准确的空气污染预测对于环境监测和政策支持至关重要，然而，数据驱动模型在观测稀疏的地区往往面临泛化能力有限的问题。本文提出了气象驱动的空气污染 GPT（GPT4AP），这是一种基于预训练 GPT-2 主干网络和高斯秩稳定低秩自适应（rsLoRA）的参数高效多任务预测框架。该模型冻结了自注意力层和前馈层，并对轻量级位置模块和输出模块进行适配，从而大幅减少了可训练参数的数量。GPT4AP 在少样本（few-shot）、零样本（zero-shot）和长期预测设置下，通过六个真实世界的空气质量监测数据集进行了评估。在仅使用 10% 训练数据的少样本方案中，GPT4AP 实现了 0.686/0.442 的平均 MSE/MAE，优于 DLinear (0.728/0.530) 和 ETSformer (0.734/0.505)。在零样本跨站迁移中，所提模型达到了 0.529/0.403 的平均 MSE/MAE，展示了相比现有基准模型更强的泛化能力。在全量训练数据的长期预测中，GPT4AP 保持了竞争力，平均 MAE 为 0.429，而专门的时间序列模型表现出略低的误差。这些结果表明，GPT4AP 提供了一种数据高效的预测方法，在有限监督和领域偏移下表现稳健，同时在数据充足的场景中仍能保持具有竞争力的准确性。

## Abstract
Accurate forecasting of air pollution is important for environmental monitoring and policy support, yet data-driven models often suffer from limited generalization in regions with sparse observations. This paper presents Meteorology-Driven GPT for Air Pollution (GPT4AP), a parameter-efficient multi-task forecasting framework based on a pre-trained GPT-2 backbone and Gaussian rank-stabilized low-rank adaptation (rsLoRA). The model freezes the self-attention and feed-forward layers and adapts lightweight positional and output modules, substantially reducing the number of trainable parameters. GPT4AP is evaluated on six real-world air quality monitoring datasets under few-shot, zero-shot, and long-term forecasting settings. In the few-shot regime using 10% of the training data, GPT4AP achieves an average MSE/MAE of 0.686/0.442, outperforming DLinear (0.728/0.530) and ETSformer (0.734/0.505). In zero-shot cross-station transfer, the proposed model attains an average MSE/MAE of 0.529/0.403, demonstrating improved generalization compared with existing baselines. In long-term forecasting with full training data, GPT4AP remains competitive, achieving an average MAE of 0.429, while specialized time-series models show slightly lower errors. These results indicate that GPT4AP provides a data-efficient forecasting approach that performs robustly under limited supervision and domain shift, while maintaining competitive accuracy in data-rich settings.

---

## 论文详细总结（自动生成）

以下是对论文《Meteorology-Driven GPT4AP: A Multi-Task Forecasting LLM for Atmospheric Air Pollution in Data-Scarce Settings》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：空气污染预测对公共健康和政策制定至关重要。虽然深度学习模型在预测精度上取得了进步，但现有模型（如 FourCastNet）通常是“数据密集型”的，依赖大规模、高质量的数据集。
*   **核心问题**：在监测站稀疏、观测数据稀缺（Data-Scarce）的地区，传统模型泛化能力差，难以在有限样本下实现准确预测。
*   **研究动机**：探索如何利用预训练大语言模型（LLM）的强大表征能力，通过参数高效的微调技术，解决大气污染预测在少样本、零样本及跨站点迁移中的挑战。

### 2. 方法论
*   **核心思想**：构建名为 **GPT4AP** 的框架，以预训练的 **GPT-2** 为主干网络，通过冻结核心层并引入改进的低秩自适应技术，实现对气象和污染物多变量时间序列的处理。
*   **关键技术细节**：
    *   **输入表示与分块（Patching）**：将 6 种污染物和 4 种气象变量组合成多维输入，采用滑动窗口分块技术将时间序列转化为“时间 Token”。
    *   **冻结主干**：完全冻结 GPT-2 的自注意力层和前馈网络层，以保留预训练的通用知识并防止在小数据集上过拟合。
    *   **高斯秩稳定低秩自适应（rsLoRA）**：在位置编码模块和输出预测头中应用 rsLoRA。其核心公式为 $\Delta W = \frac{\alpha}{\sqrt{r}} YX$，其中 $r$ 是秩。相比标准 LoRA，rsLoRA 通过秩相关的缩放因子稳定了不同秩下的学习动态。
    *   **多任务预测头**：使用轻量级 MLP 作为预测头，通过均值池化汇总 Token 信息，实现一次前向传播输出多步预测结果。

### 3. 实验设计
*   **数据集**：使用中国 6 个空气质量监测站（北京奥体中心、东四、顺义、天坛、海淀万柳、万寿西宫）的真实数据，涵盖 2013-2017 年的逐小时观测。
*   **实验场景**：
    1.  **少样本预测（Few-shot）**：仅使用 10% 的训练数据。
    2.  **零样本预测（Zero-shot）**：在源站点训练，直接在未见的靶站点评估（跨域迁移）。
    3.  **长期预测（Long-term）**：使用全量数据预测未来 24、36、48、60 小时的浓度。
*   **对比基准（Benchmarks）**：对比了 6 种最先进的时间序列模型：**DLinear、ETSformer、FiLM、Informer、Pyraformer** 和标准 **Transformer**。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA A100 GPU** 进行训练和推理。
*   **软件框架**：基于 PyTorch 1.13.1 实现。
*   **训练细节**：采用 Adam 优化器，学习率为 $1 \times 10^{-4}$，Batch Size 为 16，训练 10 个 Epoch，并设有早停机制（Patience=3）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个数据集、4 种预测跨度（24h-60h）、3 种主要预测任务，并进行了详细的**秩敏感性消融实验**（$r \in \{4, 8, 16, 32, 64\}$）。
*   **充分性评价**：实验设计非常充分。通过跨站点迁移实验验证了泛化性，通过少样本实验验证了数据效率，通过消融实验找到了性能与参数量之间的最优平衡点（$r=32$）。对比方法均为近年来时序预测领域的 SOTA 模型，评估指标（MSE/MAE）客观公正。

### 6. 主要结论与发现
*   **少样本领先**：在 10% 数据下，GPT4AP 的平均 MSE 为 0.686，显著优于 DLinear (0.728) 和 ETSformer (0.734)。
*   **零样本泛化极强**：在跨站点迁移中，GPT4AP 在 5/6 的迁移方向上排名第一，平均 MSE (0.529) 远低于其他基准，证明了 LLM 捕获了通用的气象-污染物理关系。
*   **长期预测稳健**：在全量数据下，虽然专门的 Informer 模型略占优势（MAE 0.416 vs GPT4AP 0.429），但 GPT4AP 表现依然稳健且参数量更少。
*   **参数效率**：当 $r=32$ 时，可训练参数仅占总量的 **0.309%**，却能达到 99.9% 的峰值精度。

### 7. 优点与亮点
*   **参数极其高效**：通过冻结 LLM 并使用 rsLoRA，极大地降低了微调成本，适合边缘设备部署。
*   **卓越的泛化性**：在完全未见过的监测站上表现优异，解决了环境监测中“新设站点无历史数据”的痛点。
*   **多任务统一性**：一个架构同时支持少样本、零样本和长期预测，无需针对特定任务大幅修改结构。

### 8. 不足与局限
*   **数据充足时的瓶颈**：在数据非常丰富的场景下，其表现略逊于专门针对长程依赖设计的时序模型（如 Informer），说明 LLM 在处理极长同质化时序数据时可能缺乏特定的归纳偏置。
*   **地理覆盖范围**：实验目前仅限于中国的监测站，尚未在不同气候带或全球范围内验证其鲁棒性。
*   **黑盒特性**：尽管引入了气象驱动，但 LLM 内部如何处理大气物理化学过程的解释性仍有待加强。

（完）
