---
title: "TimeAPN: Adaptive Amplitude-Phase Non-Stationarity Normalization for Time Series Forecasting"
title_zh: TimeAPN：面向时间序列预测的自适应幅相非平稳性归一化
authors: "Yue Hu, Jialiang Tang, Siwei Yu, Baosheng Yu, Jing Zhang, Dacheng Tao"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17436v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 多变量长期时间序列预测
tldr: 针对多变量长时序列预测中的非平稳性挑战（振幅和相位快速变化），本文提出 TimeAPN 框架。该框架通过在时域和频域显式建模并预测均值、相位差异和振幅等非平稳因子，克服了传统归一化方法仅依赖低阶统计量的局限。TimeAPN 具有模型无关性，能与多种预测骨干网络无缝集成，显著提升了长时预测的准确度，在多个真实数据集上表现优于现有技术。
selection_source: fresh_fetch
motivation: 现有归一化方法主要依赖一阶和二阶统计量，忽略了细粒度的时间动态，难以应对非平稳序列中剧烈的振幅和相位波动。
method: 提出一种自适应时频域归一化框架，通过联合建模均值、相位差异和振幅信息来预测非平稳因子，并与骨干网络输出协同重构序列。
result: 在七个真实世界数据集上的广泛实验证明，TimeAPN 在多个预测周期内均能显著提升预测精度，优于目前最先进的可逆归一化方法。
conclusion: TimeAPN 是一种模型无关的通用框架，通过显式建模时频域的非平稳特征，有效解决了时间序列预测中的分布偏移问题。
---

## 摘要
非平稳性是多变量长期时间序列预测中的一个根本挑战，通常表现为振幅和相位的快速变化。这些变化导致了严重的分布偏移，从而降低了预测性能。现有的基于归一化的方法主要依赖于一阶和二阶统计量，隐含地假设分布平滑演变，忽略了细粒度的时域动态。为了解决这些局限性，我们提出了 TimeAPN，一种自适应幅相非平稳性归一化框架，该框架从时域和频域显式地建模并预测非平稳因子。具体而言，TimeAPN 首先在时域和频域联合建模均值序列，然后预测其在未来时间范围内的演变。同时，在频域提取相位信息，并显式建模预测序列与真实未来序列之间的相位差异，以捕捉时间错位。此外，TimeAPN 将振幅信息整合到自适应归一化机制中，使模型能够有效应对信号能量的突发波动。随后，预测的非平稳因子通过协同反归一化过程与骨干预测输出相结合，以重构最终的非平稳时间序列。所提出的框架与模型无关，可以无缝集成到各种预测骨干网络中。在七个真实世界多变量数据集上的广泛实验表明，TimeAPN 在多个预测周期内一致地提高了长期预测精度，并优于最先进的可逆归一化方法。

## Abstract
Non-stationarity is a fundamental challenge in multivariate long-term time series forecasting, often manifested as rapid changes in amplitude and phase. These variations lead to severe distribution shifts and consequently degrade predictive performance. Existing normalization-based methods primarily rely on first- and second-order statistics, implicitly assuming that distributions evolve smoothly and overlooking fine-grained temporal dynamics. To address these limitations, we propose TimeAPN, an Adaptive Amplitude-Phase Non-Stationarity Normalization framework that explicitly models and predicts non-stationary factors from both the time and frequency domains. Specifically, TimeAPN first models the mean sequence jointly in the time and frequency domains, and then forecasts its evolution over future horizons. Meanwhile, phase information is extracted in the frequency domain, and the phase discrepancy between the predicted and ground-truth future sequences is explicitly modeled to capture temporal misalignment. Furthermore, TimeAPN incorporates amplitude information into an adaptive normalization mechanism, enabling the model to effectively account for abrupt fluctuations in signal energy. The predicted non-stationary factors are subsequently integrated with the backbone forecasting outputs through a collaborative de-normalization process to reconstruct the final non-stationary time series. The proposed framework is model-agnostic and can be seamlessly integrated with various forecasting backbones. Extensive experiments on seven real-world multivariate datasets demonstrate that TimeAPN consistently improves long-term forecasting accuracy across multiple prediction horizons and outperforms state-of-the-art reversible normalization methods.

---

## 论文详细总结（自动生成）

以下是对论文《TimeAPN: Adaptive Amplitude-Phase Non-Stationarity Normalization for Time Series Forecasting》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
长期多变量时间序列预测（TSF）面临的主要挑战是**非平稳性（Non-stationarity）**。这种非平稳性通常表现为信号**振幅（能量强度）**和**相位（周期位置）**的快速波动，导致历史数据与未来数据之间存在严重的分布偏移。
*   **研究动机：** 现有的归一化方法（如 RevIN, SAN, DDN）主要依赖均值和方差等一阶/二阶统计量，且大多假设分布是平滑演变的。这种做法忽略了细粒度的时域动态，无法有效应对非平稳序列中剧烈的振幅跳变和相位错位。
*   **核心目标：** 提出一种能够显式建模并预测振幅与相位动态变化的通用归一化框架。

### 2. 方法论
TimeAPN 提出了一种**自适应时频域联合建模**的框架，其核心流程如下：
*   **双域归一化：**
    *   **频域：** 利用离散小波变换（DWT）将序列分解为高频和低频分量，分别提取滑动窗口均值，以捕捉不同频率下的非平稳因子。
    *   **时域：** 直接从原始序列提取点对点的滑动窗口均值和相位信息。
    *   **融合：** 通过可学习参数将时域和频域提取的平稳序列进行加权融合，输入预测骨干网络（FM）。
*   **非平稳因子预测（MPPM）：**
    *   设计专门的均值预测分支（基于 MLP）和相位预测分支（基于 TCN），显式预测未来时间窗口内的均值演变和相位偏移（Phase Drift）。
*   **协同反归一化：**
    *   将骨干网络的输出与预测的未来均值、相位补偿因子相结合。通过逆傅里叶变换（IFT）和能量补偿机制，重构出具有原始非平稳特性的预测序列。
*   **训练策略：** 采用两阶段协同训练，先优化非平稳因子预测模块，再进行端到端微调，以稳定训练过程并减少误差累积。

### 3. 实验设计
*   **数据集：** 使用了 7 个主流真实世界基准数据集：ETT（包含 ETTh1, ETTh2, ETTm1, ETTm2 四个子集）、Weather、Electricity 和 Traffic。
*   **预测场景：** 长期预测，预测长度 $T \in \{96, 192, 336, 720\}$。
*   **骨干网络（Backbones）：** 为了验证通用性，集成了四种代表性模型：FEDformer（基于 Transformer）、DLinear（基于线性模型）、PatchTST（基于 Patch）和 S Mamba（基于状态空间模型）。
*   **对比方法：** 包括 RevIN、NST、Dish-TS、SAN 和 DDN 等最先进的可逆归一化方法。

### 4. 资源与算力
*   **算力说明：** 论文中**未明确提及**具体的 GPU 型号、数量、训练总时长或内存消耗。
*   **实现细节：** 提到使用了 ADAM 优化器，小波基初始化为 bior3.5，代码已在 GitHub 开源。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 7 个数据集上针对 4 种骨干网络进行了详尽测试，涵盖了不同的预测长度，实验组数超过 100 组。
*   **消融实验：** 专门针对振幅（Amp）、相位（Phase）和频率分解（FD）模块进行了消融研究，验证了各组件对性能的贡献。
*   **充分性评价：** 实验设计较为充分且客观。通过在不同架构（Transformer vs. Linear vs. Mamba）上的一致性提升，有力证明了该框架的**模型无关性（Model-agnostic）**。

### 6. 主要结论与发现
*   **性能提升：** TimeAPN 在所有基准测试中均显著优于现有的归一化方法。在大型数据集（如 Weather 和 Electricity）上，相比原始骨干网络，误差（MSE）降低幅度最高可达 28.6%。
*   **相位与振幅的重要性：** 实验证明，显式建模相位偏移能有效解决时间错位问题，而振幅建模则能更好地应对能量突变。
*   **优于双域方法：** 相比于同样利用频域的 DDN 方法，TimeAPN 通过更细粒度的点对点自适应调整，在处理高度非平稳数据时表现更稳健。

### 7. 优点
*   **多维度建模：** 突破了传统方法仅关注均值/方差的限制，引入了相位和振幅维度。
*   **插件式设计：** 作为一个归一化外壳，可以轻松集成到任何现有的深度学习预测模型中，无需修改骨干网络结构。
*   **时频结合：** 结合了小波变换的局部化特性和傅里叶变换的全局周期特性，特征提取更全面。

### 8. 不足与局限
*   **计算复杂度：** 引入 DWT、FFT 以及额外的 MPPM 预测分支，会增加模型的参数量和推理延迟，论文未对推理效率进行深入量化分析。
*   **超参数敏感性：** 框架涉及滑动窗口大小、小波基选择以及时频融合权重 $\alpha$ 等多个超参数，在不同特性的数据集上可能需要精细调优。
*   **极端异常值：** 虽然能处理非平稳性，但对于极端的、不可预测的结构性断裂（如金融危机等突发事件），基于历史模式的相位预测可能失效。

（完）
