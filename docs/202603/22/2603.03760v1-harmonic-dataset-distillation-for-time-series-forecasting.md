---
title: Harmonic Dataset Distillation for Time Series Forecasting
title_zh: 用于时间序列预测的谐波数据集蒸馏
authors: "Seungha Hong, Sanghwan Jang, Wonbin Kweon, Suyeon Kim, Gyuseok Lee, Hwanjo Yu"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03760v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 时间序列预测的数据集蒸馏
tldr: 针对大规模时间序列预测中计算和存储成本高昂的问题，本文提出了一种名为HDT的谐波数据集蒸馏方法。传统蒸馏方法在处理时间序列时存在架构过拟合和扩展性差的问题，HDT通过快速傅里叶变换（FFT）将序列分解为正弦基，并在频域内通过谐波匹配对齐核心周期结构。该方法能全局更新蒸馏数据且不破坏时间依赖性，在跨架构泛化和大规模应用中表现优异，为高效时序建模提供了新思路。
selection_source: carryover_cache
motivation: 传统数据集蒸馏方法难以有效处理时间序列的复杂时间依赖性，且存在严重的架构过拟合和扩展性不足问题。
method: 提出HDT框架，通过FFT将时序数据转换至频域，利用谐波匹配对齐周期结构，实现全局更新并保留时序特征。
result: 实验结果显示HDT在保持预测精度的同时显著减少了数据规模，并展现出卓越的跨架构泛化能力和扩展性。
conclusion: HDT证明了在频域进行数据集蒸馏的有效性，为大规模实时时间序列预测任务提供了一种实用的轻量化解决方案。
---

## 摘要
现代时间序列预测（TSF）由于现实世界数据的海量规模，面临着巨大的计算和存储成本挑战。数据集蒸馏（DD）作为一种合成小型紧凑数据集以实现与原始数据集相当的训练性能的范式，已成为一种极具前景的解决方案。然而，传统的数据集蒸馏方法并非针对时间序列量身定制，且存在架构过拟合和可扩展性有限的问题。为了解决这些问题，我们提出了用于时间序列预测的谐波数据集蒸馏（HDT）。HDT 通过快速傅里叶变换（FFT）将时间序列分解为其正弦基函数，并通过谐波匹配（Harmonic Matching）对齐核心周期结构。由于该过程在频域中运行，蒸馏过程中的所有更新都是全局应用的，不会破坏时间序列的时间依赖性。大量实验表明，HDT 实现了强大的跨架构泛化能力和可扩展性，验证了其在大规模现实应用中的实用性。

## Abstract
Time Series forecasting (TSF) in the modern era faces significant computational and storage cost challenges due to the massive scale of real-world data. Dataset Distillation (DD), a paradigm that synthesizes a small, compact dataset to achieve training performance comparable to that of the original dataset, has emerged as a promising solution. However, conventional DD methods are not tailored for time series and suffer from architectural overfitting and limited scalability. To address these issues, we propose Harmonic Dataset Distillation for Time Series Forecasting (HDT). HDT decomposes the time series into its sinusoidal basis through the FFT and aligns the core periodic structure by Harmonic Matching. Since this process operates in the frequency domain, all updates during distillation are applied globally without disrupting temporal dependencies of time series. Extensive experiments demonstrate that HDT achieves strong cross-architecture generalization and scalability, validating its practicality for large-scale, real-world applications.

---

## 论文详细总结（自动生成）

以下是对论文《Harmonic Dataset Distillation for Time Series Forecasting》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代时间序列预测（TSF）面临海量数据带来的存储压力和计算瓶颈。**数据集蒸馏（Dataset Distillation, DD）**旨在合成极小规模的紧凑数据集来替代原始大数据集进行模型训练。
*   **研究动机**：
    *   **现有方法局限性**：传统的蒸馏方法（多源于图像领域）将时间序列切分为独立的局部窗口进行“点对点”匹配，忽略了时序数据的**全局结构、长程依赖和周期性**。
    *   **架构过拟合**：局部窗口的优化导致合成数据过度拟合特定模型的归纳偏置，跨架构泛化能力差。
    *   **扩展性不足**：增加合成数据量往往只能增加局部模式的重复，无法捕捉更宏观的全局信息。

### 2. 方法论：HDT (Harmonic Dataset Distillation)
HDT 的核心思想是将蒸馏过程从**时域**转移到**频域**，通过对齐“谐波”来保留全局结构。
*   **核心思想**：利用快速傅里叶变换（FFT）将序列分解为正弦基函数。通过更新频域中的主导成分（谐波），实现对合成序列的全局更新，从而不破坏时间依赖性。
*   **关键技术细节**：
    1.  **谐波提取**：对原始数据子集和合成数据应用 FFT，选择振幅最大的前 $k$ 个频率成分作为“谐波（Harmonics）”。
    2.  **谐波匹配（Harmonic Matching）**：引入谐波损失 $L_{harm}$（振幅的 $L_p$ 范数距离），强制合成数据的周期结构与原始数据对齐。
    3.  **梯度匹配（Gradient Matching）**：在时域中，通过逆 FFT（iFFT）还原信号，计算合成数据与原始数据在模型参数更新方向上的梯度距离 $L_{grad}$。
    4.  **理论支撑**：论文通过定理证明了：**最小化频域中的谐波误差，可以有效约束时域中自相关函数（ACF）的误差**，从而在理论上保证了时间依赖性的保留。

### 3. 实验设计
*   **数据集**：使用了 7 个主流 TSF 基准数据集，包括 **ETT (h1, h2, m1, m2)**、**Electricity**、**Traffic**，以及一个超大规模交通数据集 **CA (LargeST)**（包含 20 万时间点和 8600 个特征）。
*   **Benchmark 与对比方法**：
    *   **基准**：全量数据训练（Full Data）和随机采样（Random）。
    *   **对比方法**：DC、MTT、TESLA（图像蒸馏迁移）以及 **CondTSF**（专门针对时序的 SOTA 方法）。
*   **评估场景**：
    *   **固定架构评估**：蒸馏和测试使用相同模型。
    *   **跨架构泛化评估**：使用 DLinear 蒸馏，但在 iTransformer 或 xPatch 上测试。

### 4. 资源与算力
*   **硬件环境**：实验明确指出使用了一台 **NVIDIA A100 80GB GPU**。
*   **效率表现**：
    *   **蒸馏耗时**：HDT 的 FFT 计算复杂度为 $O(M \log M)$，相比模型梯度计算开销极小，总蒸馏时间与 CondTSF 相当。
    *   **训练加速**：在蒸馏后的数据集上训练，相比全量数据实现了 **800x 到 1800x 的加速**。
    *   **大模型微调**：对 3.11 亿参数的 **Moirai-Large** 基础模型进行微调，HDT 方案比全量微调快 **80 倍**，且精度损失仅 2.5%。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 6 个数据集在 3 种不同架构（线性、Transformer、CNN）下的交叉验证（共计 54 组核心对比实验）。
*   **消融实验**：验证了频域分解和主导谐波匹配各自的贡献。
*   **扩展性实验**：测试了合成数据规模 $M$ 从 192 到 768 的性能变化。
*   **充分性评价**：实验设计非常客观且全面，涵盖了从轻量级模型到大规模基础模型（Foundation Models）的验证，足以证明其方法的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：HDT 在几乎所有数据集和架构组合上均达到了 SOTA 性能，显著优于之前的 CondTSF。
*   **跨架构泛化极强**：HDT 蒸馏出的数据具有“模型无关”的特性，在不同架构间切换时，MSE 的增加远小于其他方法。
*   **解决扩展性瓶颈**：随着合成数据量 $M$ 的增加，HDT 的性能持续提升，而传统方法很快进入平台期。
*   **基础模型适配**：证明了数据集蒸馏是微调大型时序基础模型的一种极具成本效益的手段。

### 7. 优点与亮点
*   **视角独特**：跳出了时域局部窗口的限制，从频域全局视角解决时序蒸馏问题。
*   **理论与实践结合**：不仅有显著的实验提升，还提供了严谨的数学证明（定理 1）来解释为何频域匹配能保留时域依赖。
*   **实用性强**：在超大规模数据集（CA）和超大参数模型（Moirai）上的成功应用，证明了其工业级应用的潜力。

### 8. 不足与局限
*   **超参数敏感性**：虽然论文做了分析，但谐波数量 $k$ 和损失权重 $\lambda$ 在不同特性的数据集上仍需手动调优。
*   **周期性假设**：该方法高度依赖于 FFT，对于周期性极弱或纯随机噪声驱动的时间序列，其优势可能会减弱（尽管现实中大多数时序数据具有周期性）。
*   **计算开销**：虽然比全量训练快，但蒸馏过程本身（Outer-loop 优化）仍需要一定的计算资源，对于实时性要求极高的在线蒸馏场景可能存在挑战。

（完）
