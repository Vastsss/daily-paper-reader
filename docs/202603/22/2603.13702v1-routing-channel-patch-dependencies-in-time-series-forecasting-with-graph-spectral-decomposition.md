---
title: Routing Channel-Patch Dependencies in Time Series Forecasting with Graph Spectral Decomposition
title_zh: 基于图谱分解的时间序列预测通道-补丁依赖路由
authors: "Dongyuan Li, Shun Zheng, Chang Xu, Jiang Bian, Renhe Jiang"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13702v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 具有通道-补丁依赖关系的时间序列预测
tldr: 针对时间序列预测中通道独立（CI）和通道依赖（CD）策略的局限性，本文提出通用插件xCPD。该方法利用图谱分解将多变量信号投影至频域，并根据频谱能量将Patch划分为不同频段。通过通道自适应路由机制，xCPD能动态调整通道间的交互程度，实现对平滑趋势、局部波动和突变的精细化建模，显著增强了现有模型的预测精度与泛化能力。
selection_source: fresh_fetch
motivation: 现有的CI策略忽略了通道间交互，而CD策略易引入噪声，且缺乏在两者间灵活平衡的机制。
method: 提出xCPD插件，通过图谱分解将Patch划分为不同频段，并利用通道自适应路由机制动态调节各频段的通道交互程度。
result: 在多个基准数据集上，xCPD能无缝集成到现有CI和CD模型中，并持续提升预测准确性与泛化性能。
conclusion: xCPD通过频域视角下的细粒度输入感知建模，有效解决了通道依赖建模的灵活性问题，是提升时间序列预测性能的有效方案。
---

## 摘要
时间序列预测在人工智能领域引起了广泛关注。先前的研究表明，通道独立（CI）策略通过对每个通道进行单独建模来提高预测性能，但它往往面临泛化能力差的问题，并忽略了有意义的通道间交互。相反，通道依赖（CD）策略聚合了所有通道，这可能会引入无关信息并导致过度平滑。尽管最近取得了进展，但现有的方法中很少有能够根据变化的通道依赖性灵活地自适应平衡 CI 和 CD 策略。为此，我们提出了一个通用插件 xCPD，它可以从图谱分解的角度自适应地建模通道-补丁（channel-patch）依赖关系。具体而言，xCPD 首先使用共享的图傅里叶基将多元信号投影到频域，并根据其频谱能量响应将补丁（patches）分为低频、中频和高频段。随后，xCPD 应用了一种通道自适应路由机制，动态调整每个补丁的通道间交互程度，从而实现对特定频率专家的选择性激活。这有助于对平滑趋势、局部波动和突发转变进行细粒度的输入感知建模。xCPD 可以无缝集成到现有的 CI 和 CD 预测模型之上，在各个基准测试中持续提高准确性和泛化能力。代码可在 https://github.com/Clearloveyuan/xCPD 获取。

## Abstract
Time series forecasting has attracted significant attention in the field of AI. Previous works have revealed that the Channel-Independent (CI) strategy improves forecasting performance by modeling each channel individually, but it often suffers from poor generalization and overlooks meaningful inter-channel interactions. Conversely, Channel-Dependent (CD) strategies aggregate all channels, which may introduce irrelevant information and lead to oversmoothing. Despite recent progress, few existing methods offer the flexibility to adaptively balance CI and CD strategies in response to varying channel dependencies. To address this, we propose a generic plugin xCPD, that can adaptively model the channel-patch dependencies from the perspective of graph spectral decomposition. Specifically, xCPD first projects multivariate signals into the frequency domain using a shared graph Fourier basis, and groups patches into low-, mid-, and high-frequency bands based on their spectral energy responses. xCPD then applies a channel-adaptive routing mechanism that dynamically adjusts the degree of inter-channel interaction for each patch, enabling selective activation of frequency-specific experts. This facilitates fine-grained input-aware modeling of smooth trends, local fluctuations, and abrupt transitions. xCPD can be seamlessly integrated on top of existing CI and CD forecasting models, consistently enhancing both accuracy and generalization across benchmarks. The code is available https://github.com/Clearloveyuan/xCPD.

---

## 论文详细总结（自动生成）

以下是对论文《Routing Channel-Patch Dependencies in Time Series Forecasting with Graph Spectral Decomposition》的结构化深入总结：

### 1. 核心问题与整体含义
论文探讨了多变量时间序列预测（MTSF）中**通道依赖建模策略**的权衡问题。
*   **研究背景**：现有的建模策略分为通道独立（CI，鲁棒但忽略交互）、通道依赖（CD，捕获全局信息但易引入噪声和过度平滑）以及通道部分依赖（CP，试图平衡两者）。
*   **核心痛点**：现有的 CP 方法存在两个局限：
    1.  **粒度过粗**：通常在整个通道级别建模，无法捕获局部补丁（Patch）级别的细粒度交互。
    2.  **频率耦合**：在时域建模，导致低频趋势和高频噪声的依赖关系混杂在一起，产生伪相关。
*   **整体含义**：提出 **xCPD** 插件，通过**图谱分解**视角，在频域内对“通道-补丁”进行细粒度、自适应的依赖路由。

### 2. 方法论
xCPD 是一个轻量级、模型无关的插件，核心流程如下：
*   **通道-补丁图构建**：将每个通道的每个时间补丁视为图中的一个节点，利用余弦相似度构建初始图。
*   **共享图傅里叶基（Shared Graph Fourier Basis）**：为了解决不同 Batch 间谱空间不一致的问题，论文证明并引入了共享基 $U$，确保所有样本在统一的频域内进行比较。
*   **频谱节点分组**：
    *   定义**频谱能量响应**函数，量化每个节点对不同频段（低、中、高）的响应强度。
    *   根据能量最大值将节点自动归类到对应的频段组。
*   **动态混合专家路由（DyMoE）**：
    *   设计三个特定频率的过滤器（专家）：低频（捕获趋势）、中频（局部波动）、高频（检测突变/异常）。
    *   路由网络根据输入动态选择专家组合，为每个节点构建稀疏的、频率感知的邻接矩阵。
*   **预测与优化**：通过 GNN 路径（捕获跨变量依赖）和线性路径（保留 CI 精炼）的门控残差连接输出最终预测。损失函数包含 MSE、熵损失（鼓励专家多样性）和平衡损失。

### 3. 实验设计
*   **数据集**：
    *   **长期预测**：9 个主流基准（ETT 系列、Weather、Electricity、Traffic、Exchange、Solar-Energy）。
    *   **短期预测**：M4（单变量多领域）、Stock（高波动股票数据）。
*   **基准模型（Backbones）**：涵盖线性（DLinear）、Transformer（PatchTST、iTransformer）、卷积（TimesNet）和 MLP（TSMixer、TimeMixer）。
*   **对比方法**：LIFT、CCM、PCD、PRReg 等最新的通道依赖优化方法。
*   **场景**：长期/短期预测、零样本（Zero-shot）迁移预测、消融实验、效率评估、可视化分析。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX A100 80GB GPU**。
*   **软件**：基于 PyTorch 和 Time-Series-Library 实现。
*   **效率表现**：xCPD 具有线性时间复杂度。在 Traffic 数据集上，相比于 CCM 等方法，xCPD 的显存占用显著降低（约 7GB），训练耗时仅增加骨干网络的 9%-11%，体现了极高的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：在长期预测中进行了 **144 组**实验设置；在零样本迁移中涵盖了 **48 组**设置。
*   **充分性**：
    *   对比了 6 种以上的不同架构骨干网络，证明了插件的通用性。
    *   进行了详尽的消融实验（9 种变体），验证了共享基、频率分区、节点分组等每个组件的有效性。
    *   **客观性**：所有实验均取 5 次随机种子的平均值，并使用了统一的测试框架，对比基准涵盖了 2023-2025 年的最前沿方法，实验设计严谨、公平。

### 6. 主要结论与发现
*   **性能提升**：xCPD 在所有 9 个长期预测数据集上均显著提升了 CI 和 CD 模型的精度，尤其在变量众多的 Electricity 和 Traffic 数据集上提升最明显。
*   **细粒度优势**：证明了“通道-补丁”级别的建模比传统的“通道”级别建模更能捕获复杂的动态交互。
*   **频域解耦有效性**：通过可视化发现，模型能自动将平滑趋势分配给低频专家，将突变分配给高频专家，有效抑制了噪声传播。
*   **强泛化性**：在零样本迁移实验中，xCPD 表现出优异的跨数据集知识迁移能力。

### 7. 优点
*   **即插即用（Model-agnostic）**：无需修改骨干网络架构，可直接作为后处理/增强模块集成。
*   **理论支撑扎实**：提供了关于共享傅里叶基和频谱能量守恒的数学证明（定理 4.1 & 4.2）。
*   **高效且可扩展**：线性复杂度使其能够处理拥有数百个变量的大规模时间序列数据。
*   **可解释性**：通过频域路由机制，为模型决策提供了直观的物理含义（趋势 vs 噪声）。

### 8. 不足与局限
*   **非平稳性挑战**：虽然论文讨论了鲁棒性，但在极端非平稳（分布剧烈漂移）的数据上，共享谱基的假设可能面临挑战。
*   **超参数敏感性**：尽管实验表明对 $k$-NN 比例不敏感，但补丁大小（Patch Size）的选择仍需根据具体任务进行一定程度的调优。
*   **跨域迁移限制**：目前的零样本实验主要集中在同领域（如 ETT 内部）迁移，跨领域（如从电力到交通）的通用性仍有待进一步探索。

（完）
