---
title: "AcoustEmo: Open-Vocabulary Emotion Reasoning via Utterance-Aware Acoustic Q-Former"
title_zh: AcoustEmo：基于话语感知声学 Q-Former 的开放词汇情感推理
authors: "Liyun Zhang, Xuanmeng Sha, Shuqiong Wu, Fengkai Liu"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20894v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 捕捉局部时间动态的时间敏感多模态大模型
tldr: AcoustEmo 是一种针对开放词汇情感推理的时间敏感型多模态大模型，旨在解决现有模型在捕捉微观韵律和语调变化等细粒度声学特征方面的不足。该模型引入了话语感知声学 Q-Former，通过时间戳同步的滑动窗口提取片段级音频标记，从而能够显式追踪声学线索的时间演变。在 EMER 任务上的实验证明，AcoustEmo 在复杂情感推理和上下文准确性方面显著优于现有基准模型。
selection_source: fresh_fetch
motivation: 现有的多模态大模型通常采用全局音频编码，难以捕捉话语内部如微观韵律和语调偏移等细粒度的局部时间动态。
method: 提出 AcoustEmo 模型，利用时间戳同步的滑动窗口和话语感知声学 Q-Former 动态提取片段级音频特征。
result: 在可解释多模态情感识别（EMER）任务中，AcoustEmo 在复杂情感推理方面表现优异，显著超越了现有基准。
conclusion: 通过显式建模声学特征的时间演变，AcoustEmo 有效提升了多模态大模型在对话中捕捉深层情感上下文和进行复杂推理的能力。
---

## 摘要
多模态大语言模型 (MLLMs) 在开放词汇 (OV) 情感识别方面表现出色，但往往忽略了细粒度的声学建模。现有方法通常使用全局音频编码器，无法捕捉单个话语内微韵律和语调变化等细微的局部时间动态。为解决这一问题，我们提出了 AcoustEmo，这是一种具有新型话语感知声学 Q-Former 的时间敏感型 MLLM。我们的方法利用时间戳同步的滑动窗口动态提取片段级音频标记，而非粗糙的全局表示。这使得模型能够显式追踪细微声学线索的时间演变，并捕捉对话中的深层上下文依赖。在可解释多模态情感识别 (EMER) 任务上的实验表明，AcoustEmo 显著增强了复杂情感推理能力，在保持稳健上下文准确性的同时优于基准模型。

## Abstract
Multimodal Large Language Models (MLLMs) excel in Open-Vocabulary (OV) emotion recognition but often neglect fine-grained acoustic modeling. Existing methods typically use global audio encoders, failing to capture subtle, local temporal dynamics like micro-prosody and intonation shifts within individual utterances. To address this, we propose AcoustEmo, a time-sensitive MLLM featuring a novel Utterance-Aware Acoustic Q-Former. Our approach utilizes a timestamp-synchronized sliding window to dynamically extract segment-level audio tokens instead of coarse global representations. This enables the model to explicitly trace the temporal evolution of subtle acoustic clues and capture deep contextual dependencies in dialogues. Experiments on the Explainable Multimodal Emotion Recognition (EMER) task show that AcoustEmo significantly enhances complex emotion reasoning, outperforming baselines while maintaining robust contextual accuracy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AcoustEmo** 的时间敏感型多模态大语言模型（MLLM），专门用于开放词汇的情感推理任务。以下是对该论文的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的多模态大模型在处理情感识别时，通常采用“全局音频编码”方式，即将整段视频的音频压缩为粗粒度的全局标记。这种方式忽略了话语内部（Utterance-level）的**细粒度声学动态**，如微观韵律（micro-prosody）、瞬间的语调偏移、颤音或语速变化。
*   **研究背景**：情感往往隐藏在极短的时间片段中。如果只进行全局建模，这些关键的瞬时声学线索会被平滑或稀释，导致模型在处理复杂、动态变化的情感推理（如 EMER 任务）时表现不足。

### 2. 核心方法论
AcoustEmo 的核心思想是通过“话语感知”机制，将声学特征与文本时间戳显式对齐。
*   **时间戳同步滑动窗口（Timestamp-Synchronized Sliding Window）**：利用语音转文本提供的时间戳，动态地从原始音频序列中截取与每句对话匹配的局部声学特征片段，确保声学信息与语义边界严格一致。
*   **话语感知声学 Q-Former（Utterance-Aware Acoustic Q-Former）**：
    *   为每个话语片段引入一组可学习的查询（Queries，文中设为 32 个）。
    *   通过交叉注意力机制，将局部声学特征压缩为精炼的声学标记（Tokens），提取最具情感辨识度的微观韵律细节。
*   **多尺度特征融合**：模型同时保留一个“全局声学 Q-Former”来捕捉宏观背景背景，并将全局标记与多个局部话语标记拼接。
*   **LLM 推理引擎**：使用 LLaMA-2 (7B) 作为核心，通过 LoRA（低秩自适应）进行微调，将视觉标记、多尺度声学标记和指令文本输入模型，生成开放词汇的情感描述。

### 3. 实验设计
*   **数据集**：使用 **EMER-Fine** 评估集，这是一个包含丰富面部表情、语音语调和语义上下文标注的严苛基准。
*   **评估指标**：采用针对 EMER 任务定制的语义相似度准确率（Accuracy）和召回率（Recall）。
*   **对比基准（Benchmarks）**：
    *   **音频中心模型**：Qwen-Audio, SALMONN 等。
    *   **视频中心模型**：Video-LLaMA, VideoChat2, Video-LLaVA 等。
    *   **情感专用模型**：AffectGPT, MicroEmo（侧重局部视觉动态的模型）。

### 4. 资源与算力
*   **模型规模**：基础模型为 LLaMA-2 (7B)，视觉和音频编码器（如 ImageBind）保持冻结。
*   **算力设备**：实验在 **单张 NVIDIA GPU** 上完成（具体型号未标明，但考虑到 7B 模型和 1024 的序列长度，推测为 A100 或同级别显卡）。
*   **训练细节**：使用 AdamW 优化器，学习率 2e-5，训练 3 个 Epoch，采用 LoRA 微调技术以降低显存占用并保持通用知识。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了主实验对比（涵盖 20 余种 SOTA 模型组合）、消融实验以及定性案例分析。
*   **消融实验**：分别验证了“全局 Q-Former”、“固定长度窗口”和“话语感知 Q-Former”的作用。结果显示，移除话语感知模块后性能下降最剧烈（Avg 评分从 67.55 降至 61.20）。
*   **充分性评价**：实验设计较为客观、公平。通过与视觉增强模型（MicroEmo）的对比，证明了声学细粒度建模的独特价值。定性分析部分直观展示了模型捕捉“颤音”等局部异常信号的能力。

### 6. 主要结论与发现
*   **局部动态至关重要**：在情感推理中，局部声学特征的贡献甚至超过了全局背景。
*   **时间戳对齐的优越性**：相比于固定长度（如每 2 秒一跳）的滑动窗口，基于话语边界的同步机制能有效避免语义截断，显著提升推理准确性。
*   **性能提升**：AcoustEmo 在 EMER-Fine 任务上达到了 67.55% 的平均得分，优于现有的多模态情感推理模型（如 AffectGPT 和 MicroEmo）。

### 7. 优点（亮点）
*   **创新性对齐**：首次在 MLLM 框架下将声学 Q-Former 与话语级时间戳结合，解决了音频特征在长序列中被过度压缩的问题。
*   **时间敏感性**：模型能够“定位”情感爆发的具体时刻，增强了情感识别的可解释性。
*   **高效性**：通过冻结主干网络并使用 LoRA，在有限算力下实现了高性能的跨模态对齐。

### 8. 不足与局限
*   **鲁棒性挑战**：在低信噪比（SNR）环境下，背景噪音可能干扰局部声学标记的提取。
*   **复杂语义矛盾**：对于“反讽”或“阴阳怪气”等声学语调与文本语义完全相反的情况，模型仍可能被文本误导。
*   **实时性限制**：动态片段提取和多 Q-Former 处理增加了推理延迟，目前尚不清楚其在实时交互场景下的表现。
*   **覆盖范围**：实验主要集中在 EMER 任务，尚未在更广泛的通用音频理解任务上验证其泛化性。

（完）
