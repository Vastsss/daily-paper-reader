---
title: On Neural Scaling Laws for Weather Emulation through Continual Training
title_zh: 关于通过持续训练实现天气模拟的神经标度律研究
authors: "Shashank Subramanian, Alexander Kiefer, Arnur Nigmetov, Amir Gholami, Dmitriy Morozov, Michael W. Mahoney"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25687v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 天气预报基础模型的神经缩放法则
tldr: 本研究探讨了气象预报模型中的神经缩放法则。研究者采用通用的Swin Transformer架构，并结合恒定学习率与周期性冷却的持续训练策略，系统分析了模型、数据和计算规模对性能的影响。结果表明，该方法不仅遵循可预测的缩放趋势，且在多步预测和预测清晰度上优于传统方案。通过构建IsoFLOP曲线，研究确定了计算最优的训练方案，为气象大模型的资源分配和性能预测提供了重要参考。
selection_source: fresh_fetch
motivation: 旨在探索科学机器学习（尤其是气象预测）中神经缩放法则的适用性，以解决大模型训练中的资源分配优化问题。
method: 采用可扩展的Swin Transformer架构，并结合恒定学习率与周期性冷却阶段的持续训练策略进行实验。
result: 实验证明模型遵循可预测的缩放趋势，且通过IsoFLOP曲线成功识别出在不同计算预算下的最优模型与数据规模组合。
conclusion: 神经缩放法则是气象模拟领域有效的诊断工具，能够指导高效的资源分配并预测更大规模模型的性能极限。
---

## 摘要
神经标度律在某些领域可以根据模型、数据和计算规模预测大型神经网络的性能，是自然语言处理和计算机视觉领域构建基础模型的基石。我们研究了科学机器学习中的神经标度，重点关注天气预报模型。为了在尽可能简单的设置下分析标度行为，我们采用了一种极简、可扩展且通用的 Swin Transformer 架构，并使用带有恒定学习率和周期性冷却（cooldowns）的持续训练作为一种高效的训练策略。我们表明，以这种极简方式训练的模型遵循可预测的标度趋势，甚至优于标准的余弦学习率调度。冷却阶段可以被重新利用以提高下游性能，例如，通过频谱损失调整实现更长预测时效下的准确多步推演（rollouts）以及更清晰的预测。我们还系统地探索了各种计算预算下广泛的模型和数据集规模，以构建等 FLOPs（IsoFLOP）曲线，并确定了计算最优的训练方案。将这些趋势外推到更大规模突显了潜在的性能极限，证明了神经标度可以作为高效资源分配的重要诊断工具。我们开源了代码以确保可重复性。

## Abstract
Neural scaling laws, which in some domains can predict the performance of large neural networks as a function of model, data, and compute scale, are the cornerstone of building foundation models in Natural Language Processing and Computer Vision. We study neural scaling in Scientific Machine Learning, focusing on models for weather forecasting. To analyze scaling behavior in as simple a setting as possible, we adopt a minimal, scalable, general-purpose Swin Transformer architecture, and we use continual training with constant learning rates and periodic cooldowns as an efficient training strategy. We show that models trained in this minimalist way follow predictable scaling trends and even outperform standard cosine learning rate schedules. Cooldown phases can be re-purposed to improve downstream performance, e.g., enabling accurate multi-step rollouts over longer forecast horizons as well as sharper predictions through spectral loss adjustments. We also systematically explore a wide range of model and dataset sizes under various compute budgets to construct IsoFLOP curves, and we identify compute-optimal training regimes. Extrapolating these trends to larger scales highlights potential performance limits, demonstrating that neural scaling can serve as an important diagnostic for efficient resource allocation. We open-source our code for reproducibility.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了天气预报领域科学机器学习（SciML）模型的神经标度律（Neural Scaling Laws）。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在自然语言处理（NLP）和计算机视觉（CV）领域，神经标度律已成为构建基础模型的基石，能够预测模型性能随计算量、数据量和参数规模增长的变化趋势。
*   **核心问题**：在天气预报这一复杂的科学机器学习领域，是否也存在类似的标度律？现有的天气模型往往依赖复杂的领域特定架构，难以厘清性能提升是源于架构设计还是规模效应。
*   **研究动机**：通过极简的通用架构和高效的训练策略，系统性地建立天气模拟的计算最优（Compute-optimal）训练方案，为未来大规模气象基础模型的资源分配提供理论指导。

### 2. 方法论
*   **核心思想**：采用“极简主义”原则，使用通用的 **Swin Transformer** 作为骨干网络，剔除所有领域特定的修改，专注于研究规模（Scale）的影响。
*   **关键技术细节**：
    *   **持续训练与冷却策略（Continual Training with Cooldowns）**：放弃传统的余弦学习率调度，采用恒定学习率（Constant LR）加周期性快速冷却（Cooldown）至零的策略。这种方法允许从同一检查点继续训练以探索更高的计算预算，而无需从头开始。
    *   **冷却阶段的重用（Alignment）**：将简短的冷却阶段（约占总迭代次数的5%）用于下游任务对齐，如引入多步自回归（AR）损失或频谱调整损失（AMSE），以提升长时效预测准确性和空间清晰度。
    *   **混合并行策略**：为了处理高分辨率（0.25°）的 ERA5 数据，实现了 **2D 空间并行（Spatial Parallelism）** 与数据并行的结合，有效缓解了高分辨率输入带来的显存压力。

### 3. 实验设计
*   **数据集**：使用 **ERA5** 再分析数据集，空间分辨率为 0.25°（721x1440 栅格），包含 71 个大气变量，时间跨度从 1979 年到 2022 年（约 35 万个样本）。
*   **基准模型（Benchmarks）**：
    *   **HRES**：欧洲中期天气预报中心（ECMWF）的金标准数值天气预报系统。
    *   **GraphCast**：目前最先进的基于图神经网络的深度学习天气预报模型。
*   **对比实验**：
    *   对比了余弦调度与恒定学习率+冷却调度的性能。
    *   构建了多条 **IsoFLOP 曲线**，涵盖从 6E+17 到 6E+19 FLOPs 的计算预算。
    *   模型参数规模跨越两个数量级（3M 到 456M），并外推至 1.3B 参数。

### 4. 资源与算力
*   **硬件设备**：使用了 NVIDIA **A100 GPU**（包含 40GB 和 80GB 显存版本）。
*   **算力规模**：
    *   较小模型使用 16 个 GPU 进行数据并行。
    *   较大模型（如 456M 和 1.3B）使用了多达 **64 个 GPU**，并启用了空间并行。
*   **计算预算**：实验覆盖了 6E+17 至 6E+19 FLOPs 的范围，最终外推实验达到了 **2.25E+21 FLOPs**。

### 5. 实验数量与充分性
*   **实验规模**：作者训练了数十个不同规模的模型（3M, 7M, 15M, 26M, 39M, 60M, 87M, 115M, 153M, 204M, 304M, 456M, 1.3B），并在每个计算预算下拟合了抛物线以寻找最优参数。
*   **充分性与客观性**：实验设计非常严谨，通过 IsoFLOP 曲线系统地解耦了模型大小和数据大小对性能的影响。通过在测试集（2020年数据）上进行 732 次初始条件的全面评估，确保了结果的统计显著性。

### 6. 主要结论与发现
*   **标度律存在性**：天气模拟模型遵循清晰的幂律缩放趋势。计算最优的模型大小 $N^*$ 和样本数 $S^*$ 与计算预算 $C$ 的关系分别为 $N^* \propto C^{0.59}$ 和 $S^* \propto C^{0.41}$。
*   **策略优越性**：恒定学习率+冷却策略不仅比余弦调度更灵活，且在相同计算量下表现更好。
*   **性能极限与饱和**：当模型扩展到 1.3B 参数（2.25E+21 FLOPs）时，性能出现了饱和迹象。这表明在当前数据分辨率和样本量下，单纯增加模型规模已遇到瓶颈，可能需要增加数据多样性或分辨率。
*   **对齐效果**：通过在冷却阶段进行微调，极简的 Swin Transformer 能够达到甚至超过 GraphCast 等复杂专用架构的精度。

### 7. 优点与亮点
*   **高效性**：提出的持续训练策略极大地降低了探索标度律的成本，无需为每个预算点从头训练。
*   **解耦设计**：成功将预训练标度与下游任务对齐（如长时效预测、频谱保持）解耦，证明了基础模型在科学领域的潜力。
*   **工程贡献**：开源了支持空间并行的分布式训练代码，解决了高分辨率气象数据训练的显存瓶颈。

### 8. 不足与局限
*   **确定性限制**：目前研究仅限于确定性预测，未深入探讨概率预报（如扩散模型或集合预报）的标度律，而后者在气象领域至关重要。
*   **架构单一**：主要关注 Transformer 架构，虽然证明了其有效性，但未对比神经算子（Neural Operators）等其他 SciML 架构的标度行为。
*   **数据瓶颈**：实验揭示了在 1B 参数量级以上会出现数据饱和，这意味着未来的突破可能不仅依赖算力，更依赖于更高分辨率或更多元的数据集成。

（完）
