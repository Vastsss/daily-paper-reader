---
title: Spatiotemporal System Forecasting with Irregular Time Steps via Masked Autoencoder
title_zh: 基于掩码自编码器的非规则时间步长时空系统预测
authors: "Kewei Zhu, Yanze Xin, Jinwei Hu, Xiaoyuan Cheng, Yiming Yang, Sibo Cheng"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25597v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 时空系统预测与不规则时间序列分析
tldr: 针对高维动力系统中因数据缺失或稀疏观测导致的非均匀时间步长预测难题，本文提出一种物理时空掩码自编码器。该方法结合卷积自编码器提取空间特征，并利用针对不规则时间序列优化的掩码自编码器及注意力机制，在无需数据插值的情况下实现物理序列的一次性重构。实验证明该模型在模拟和真实海洋数据上均表现出优异的预测精度、鲁棒性和计算效率，为复杂时空系统建模提供了新方案。
selection_source: fresh_fetch
motivation: 传统数据驱动算法在处理具有不规则时间步长的高维动力系统时，往往因数据缺失或采样不均而导致预测精度下降。
method: 提出一种物理时空掩码自编码器，通过卷积提取空间特征并结合注意力机制，在不进行数据插补的情况下重构整个物理序列。
result: 在模拟数据集和真实海洋温度数据上的实验表明，该方法在预测精度、非线性鲁棒性和计算效率方面显著优于传统的卷积和循环神经网络。
conclusion: 该模型能够有效捕捉复杂的时空模式且无需特定领域知识，在气候建模、流体力学及环境监测等科学计算领域具有广泛的应用前景。
---

## 摘要
对具有非规则时间步长的高维动力系统进行预测，对当前的数据驱动算法提出了重大挑战。这些非规则性源于数据缺失、稀疏观测或自适应计算技术，从而降低了预测精度。为了解决这些局限性，我们提出了一种新方法：物理-时空掩码自编码器（Physics-Spatiotemporal Masked Autoencoder）。该方法将用于空间特征提取的卷积自编码器与针对非规则时间序列优化的掩码自编码器相结合，利用注意力机制在单次预测过程中重建整个物理序列。该模型避免了数据插补的需求，同时保持了系统的物理完整性。此处的“物理”是指由底层动力系统生成的高维场，而非强制执行显式的物理约束或偏微分方程（PDE）残差。我们在多个模拟数据集和真实海洋温度数据上评估了该方法。结果表明，与传统的卷积和循环网络方法相比，我们的方法在预测精度、对非线性的鲁棒性以及计算效率方面取得了显著提升。该模型展示了在无需领域特定知识的情况下捕捉复杂时空模式的潜力，可应用于气候建模、流体动力学、海洋预报、环境监测和科学计算等领域。

## Abstract
Predicting high-dimensional dynamical systems with irregular time steps presents significant challenges for current data-driven algorithms. These irregularities arise from missing data, sparse observations, or adaptive computational techniques, reducing prediction accuracy. To address these limitations, we propose a novel method: a Physics-Spatiotemporal Masked Autoencoder. This method integrates convolutional autoencoders for spatial feature extraction with masked autoencoders optimised for irregular time series, leveraging attention mechanisms to reconstruct the entire physical sequence in a single prediction pass. The model avoids the need for data imputation while preserving physical integrity of the system. Here, 'physics' refers to high-dimensional fields generated by underlying dynamical systems, rather than the enforcement of explicit physical constraints or PDE residuals. We evaluate this approach on multiple simulated datasets and real-world ocean temperature data. The results demonstrate that our method achieves significant improvements in prediction accuracy, robustness to nonlinearities, and computational efficiency over traditional convolutional and recurrent network methods. The model shows potential for capturing complex spatiotemporal patterns without requiring domain-specific knowledge, with applications in climate modelling, fluid dynamics, ocean forecasting, environmental monitoring, and scientific computing.

---

## 论文详细总结（自动生成）

这是一份关于论文《Spatiotemporal System Forecasting with Irregular Time Steps via Masked Autoencoder》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**高维动力系统在非规则时间步长下的预测难题**。在科学模拟（如自适应步长求解器）和现实观测（如传感器故障、卫星过境间隔不均）中，数据往往不是等间隔采样的。
*   **研究动机**：传统的深度学习模型（如 RNN、LSTM、ConvLSTM）通常假设时间步长是均匀固定的。面对非规则数据时，传统做法依赖于数据插值或填补等预处理手段，但这会引入人为偏差、掩盖真实的动力学特征，并增加计算开销。
*   **整体含义**：本文旨在开发一种无需预处理、能直接从非规则采样的高维物理场中学习时空演化规律的模型。

### 2. 论文提出的方法论：P-STMAE
论文提出了一种名为 **Physics-Spatiotemporal Masked Autoencoder (P-STMAE)** 的框架，其核心思想是将空间降维与潜空间的掩码时间建模相结合。

*   **核心架构**：
    1.  **空间编码器（CAE Encoder）**：使用卷积神经网络将高维物理状态 $x_t$ 压缩到低维潜空间 $z_t$，以降低 Transformer 处理高维数据的计算复杂度。
    2.  **掩码 Transformer（Masked Transformer）**：在潜空间中运行。对于缺失的时间步或需要预测的未来步，使用**占位符（Placeholder）**和**可学习的掩码 Token**。
    3.  **注意力机制**：利用自注意力（Self-attention）同时处理所有观测到的时间点，捕捉长程依赖，而非像 RNN 那样顺序处理。
    4.  **空间解码器（CAE Decoder）**：将 Transformer 输出的潜状态重构回原始的高维物理空间。
*   **关键技术细节**：
    *   **非自回归预测**：模型通过单次前向传播（Single Pass）重构整个序列（包括缺失值和预测值），避免了 RNN 类模型在多步预测中的误差累积。
    *   **位置编码（Positional Embeddings）**：使用正弦/余弦编码来标记非规则时间点在序列中的相对位置。
*   **损失函数**：
    $$L = \frac{1}{T_{in} + T_{out}} \sum_{t=1}^{T_{in} + T_{out}} (\mathbb{E}[\|\hat{x}_t - x_t\|^2] + \lambda \cdot \mathbb{E}[\|\hat{z}_t - z_t\|^2])$$
    该公式联合优化了物理空间和潜空间的均方误差（MSE），其中 $\lambda$ 是平衡系数。

### 3. 实验设计
*   **数据集/场景**：
    1.  **浅水方程（Shallow Water）**：模拟非线性流体动力学，具有混沌特性。
    2.  **反应扩散方程（Diffusion Reaction）**：模拟生物模式形成，涉及非线性耦合变量。
    3.  **NOAA 海洋表面温度（SST）**：真实的全球气候观测数据，包含噪声和复杂的长程依赖。
*   **Benchmark 与对比方法**：
    *   **ConvRAE**：卷积自编码器 + LSTM（潜空间建模的代表）。
    *   **ConvLSTM**：全空间时空序列学习的代表。
    *   **CAE（Baseline）**：仅作为空间重构的上限参考。
*   **评估指标**：MSE（像素级精度）、SSIM（结构相似性）、PSNR（峰值信噪比）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 RAdam 优化器（学习率 $3 \times 10^{-4}$）训练 P-STMAE，使用 Adam 优化器训练 RNN 基准模型，Batch size 为 32。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个异构数据集上进行了测试，涵盖了从理想 PDE 模拟到复杂现实数据的跨度。
    *   **缺失率分析**：测试了从 1 到 6 个时间步缺失的不同情况（输入序列长度为 10）。
    *   **非线性鲁棒性分析**：通过改变采样扩张率（Dilation）来测试模型处理不同时间跨度动力学的能力。
    *   **消融实验**：针对损失函数权重 $\lambda$ 进行了参数敏感性分析。
*   **充分性评价**：实验设计较为充分且客观。通过引入不同程度的数据缺失和采样间隔，验证了模型在极端不规则情况下的稳定性。对比实验涵盖了潜空间和全空间的主流架构，具有较强的说服力。

### 6. 主要结论与发现
*   **精度领先**：P-STMAE 在所有数据集上的 MSE 均优于 ConvRAE 和 ConvLSTM，尤其在 SST 真实数据上表现突出。
*   **鲁棒性强**：随着缺失数据比例增加或采样间隔变大（非线性增强），P-STMAE 的性能下降远比 RNN 类模型缓慢，显示出 Transformer 结构在处理非规则序列时的天然优势。
*   **效率优势**：由于采用非自回归的一次性预测，P-STMAE 在推理时比需要逐步迭代的 RNN 模型更高效。
*   **物理一致性**：虽然没有显式加入 PDE 约束，但模型通过潜空间建模有效地捕捉了物理场的空间相干性和时间演化逻辑。

### 7. 优点（亮点）
*   **端到端处理**：彻底摆脱了对插值等预处理步骤的依赖，直接处理原始非规则序列。
*   **架构创新**：成功将图像领域的 Masked Autoencoder 思想迁移至高维物理动力学预测，并结合了 CAE 降维以解决计算瓶颈。
*   **灵活性**：占位符策略使得模型可以灵活处理任意位置的缺失值和任意长度的预测区间。

### 8. 不足与局限
*   **计算复杂度**：Transformer 的自注意力机制具有 $O(T^2)$ 的复杂度，虽然在潜空间运行，但面对极长序列时仍可能面临内存挑战。
*   **重构瓶颈**：模型性能高度依赖于第一阶段 CAE 的压缩质量，如果 CAE 丢失了关键的小尺度物理特征，后续的 Transformer 无法弥补。
*   **结构保持性**：在反应扩散方程实验中，P-STMAE 的 SSIM 指标略低于全空间的 ConvLSTM，说明潜空间压缩可能对某些精细的空间结构造成微小损失。
*   **物理约束缺失**：目前仍属于纯数据驱动方法，未引入显式的物理先验（如守恒律），在极小样本场景下的泛化性有待进一步验证。

（完）
