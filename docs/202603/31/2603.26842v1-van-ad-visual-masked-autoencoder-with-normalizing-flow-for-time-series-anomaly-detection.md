---
title: "VAN-AD: Visual Masked Autoencoder with Normalizing Flow For Time Series Anomaly Detection"
title_zh: VAN-AD：结合归一化流的视觉掩码自编码器用于时间序列异常检测
authors: "PengYu Chen, Shang Wan, Xiaohou Shi, Yuan Chang, Yan Sun, Sajal K. Das"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26842v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 时间序列异常检测的基础模型
tldr: 本研究针对时间序列异常检测（TSAD）中模型泛化能力不足的问题，提出了一种基于视觉掩码自编码器（MAE）的新型框架VAN-AD。该框架通过自适应分布映射模块（ADMM）缓解MAE的过度泛化问题，并结合归一化流模块（NFM）增强全局分布感知能力。实验证明，VAN-AD在九个真实数据集上均优于现有最先进方法，展示了视觉预训练模型在时序任务中的巨大潜力。
selection_source: fresh_fetch
motivation: 现有TSAD方法泛化性差，且现有大模型在处理时序数据时面临跨模态鸿沟或域内异质性挑战。
method: 将ImageNet预训练的视觉MAE适配至TSAD任务，并引入ADMM模块放大异常差异及NFM模块估计全局概率密度。
result: 在九个真实世界数据集上的广泛实验表明，VAN-AD在多项评估指标上均持续优于现有的最先进方法。
conclusion: 通过结合视觉预训练模型、分布映射和归一化流，VAN-AD有效解决了时序异常检测中的泛化与感知局限。
---

## 摘要
时间序列异常检测 (TSAD) 对于维护启用物联网的服务系统的可靠性和安全性至关重要。现有方法需要为每个数据集训练一个特定模型，这在不同目标数据集上的泛化能力有限，阻碍了在训练数据稀缺的各种场景下的异常检测性能。为了解决这一局限性，基础模型已成为一个极具前景的方向。然而，现有方法要么重新利用大语言模型 (LLMs)，要么构建大规模时间序列数据集来开发通用的异常检测基础模型，但仍面临严重的跨模态差距或域内异质性带来的挑战。在本文中，我们研究了大规模视觉模型在 TSAD 中的适用性。具体而言，我们将预训练于 ImageNet 的视觉掩码自编码器 (MAE) 适配到 TSAD 任务中。然而，将 MAE 直接迁移到 TSAD 会引入两个关键挑战：过度泛化和局部感知受限。为了应对这些挑战，我们提出了 VAN-AD，一种基于 MAE 的新型 TSAD 框架。为了缓解过度泛化问题，我们设计了一个自适应分布映射模块 (ADMM)，它将 MAE 重建前后的结果映射到一个统一的统计空间中，以放大由异常模式引起的差异。为了克服局部感知的局限性，我们进一步开发了一个归一化流模块 (NFM)，它将 MAE 与归一化流相结合，以估计当前窗口在全局分布下的概率密度。在九个真实世界数据集上的广泛实验表明，VAN-AD 在多个评估指标上始终优于现有的最先进方法。我们在 https://github.com/PenyChen/VAN-AD 提供了代码和数据集。

## Abstract
Time series anomaly detection (TSAD) is essential for maintaining the reliability and security of IoT-enabled service systems. Existing methods require training one specific model for each dataset, which exhibits limited generalization capability across different target datasets, hindering anomaly detection performance in various scenarios with scarce training data. To address this limitation, foundation models have emerged as a promising direction. However, existing approaches either repurpose large language models (LLMs) or construct largescale time series datasets to develop general anomaly detection foundation models, and still face challenges caused by severe cross-modal gaps or in-domain heterogeneity. In this paper, we investigate the applicability of large-scale vision models to TSAD. Specifically, we adapt a visual Masked Autoencoder (MAE) pretrained on ImageNet to the TSAD task. However, directly transferring MAE to TSAD introduces two key challenges: overgeneralization and limited local perception. To address these challenges, we propose VAN-AD, a novel MAE-based framework for TSAD. To alleviate the over-generalization issue, we design an Adaptive Distribution Mapping Module (ADMM), which maps the reconstruction results before and after MAE into a unified statistical space to amplify discrepancies caused by abnormal patterns. To overcome the limitation of local perception, we further develop a Normalizing Flow Module (NFM), which combines MAE with normalizing flow to estimate the probability density of the current window under the global distribution. Extensive experiments on nine real-world datasets demonstrate that VAN-AD consistently outperforms existing state-of-the-art methods across multiple evaluation metrics.We make our code and datasets available at https://github.com/PenyChen/VAN-AD.

---

## 论文详细总结（自动生成）

这是一份关于论文《VAN-AD: Visual Masked Autoencoder with Normalizing Flow For Time Series Anomaly Detection》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的时间序列异常检测（TSAD）方法大多针对特定数据集训练，泛化能力差。虽然近期出现了基于大语言模型（LLM）或大规模时序预训练的基础模型，但前者存在严重的跨模态鸿沟，后者则面临高质量、大规模异常标注数据稀缺的问题。
*   **核心问题**：论文探讨了**大规模视觉预训练模型**在 TSAD 中的适用性。作者发现自然图像与时间序列在结构（趋势、季节性、突变）上具有相似性，因此尝试将 ImageNet 预训练的视觉掩码自编码器（MAE）迁移至时序任务。
*   **面临挑战**：直接迁移存在两个痛点：
    1.  **过度泛化**：MAE 重建能力太强，导致异常点也能被完美重建，缩小了正常与异常的差异。
    2.  **局部感知局限**：受限于滑动窗口大小，模型难以识别占据整个窗口的长程异常。

### 2. 方法论：核心思想与关键技术
VAN-AD 框架由四个核心模块组成：
*   **前向模块（Forward Module）**：将多变量时间序列编码为“变量-时间”热图矩阵，并调整为 224x224 的灰度图，以适配预训练 MAE 的输入要求。
*   **重建模块（Reconstruction Module）**：采用**棋盘格掩码（Checkerboard Masking）**策略生成互补图像对。利用冻结的预训练 MAE 重建缺失区域，从而捕捉变量间依赖和时间相关性。
*   **自适应分布映射模块（ADMM）**：
    *   **核心思想**：MAE 输出通常比原信号平滑。ADMM 计算原始序列的均值和标准差，将重建序列映射回原始统计空间。
    *   **作用**：通过统计对齐，放大异常模式引起的重建差异，解决过度泛化问题。
*   **归一化流模块（NFM）**：
    *   **核心思想**：在 MAE 重建序列之上构建掩码自回归流（MAF）。
    *   **技术细节**：通过可逆变换估计当前窗口在全局分布下的概率密度。异常得分由“重建误差”和“负对数似然（密度得分）”加权组成，以此克服局部感知限制。

### 3. 实验设计
*   **数据集**：涵盖 9 个真实世界数据集（CalIt2, CICIDS, DLR, GECCO, MSL, NYC, PUMP, PSM, SWAN），涉及物联网、交通、航天、水处理等多个领域。
*   **Benchmark 与对比方法**：
    *   **对比方法（15 种）**：包括时序基础模型（DADA, Timer）、文本基础模型（CALF, GPT4TS）、深度学习 SOTA（CATCH, MtsCID, ContraAD 等）以及经典方法（LOF, PCA, KNN 等）。
    *   **评估指标**：采用不受阈值影响的 AUC-ROC、AUC-PR 以及更可靠的 VUS-ROC、VUS-PR。

### 4. 资源与算力
*   **硬件设备**：实验在一台拥有 40GB 显存的 **NVIDIA A800 GPU** 上运行。
*   **训练细节**：
    *   MAE 骨干网络（MAE-base, 112M 参数）保持冻结。
    *   仅训练归一化流模块，默认训练 5 个 epoch。
    *   学习率为 0.005，Batch Size 为 128。
    *   推理效率：使用热图编码时，推理速度极快（如 PSM 数据集仅需 2.49 秒）。

### 5. 实验数量与充分性
*   **实验规模**：在 9 个数据集上进行了全量对比实验，每项实验均包含 4 种评估指标。
*   **消融实验**：针对 ADMM、NFM 以及 Zero-shot 设置进行了详细对比，验证了各模块的必要性。
*   **扩展分析**：
    *   **骨干网络分析**：对比了 MAE-base/large/huge，发现模型并非越大越好。
    *   **编码策略分析**：对比了 Seg, GAF, STFT, Wavelet, RP, Heatmap 六种成像方法。
    *   **微调分析**：探讨了冻结 MAE 与微调（ln, bias, attn, mlp, full）的效果差异。
*   **评价**：实验设计全面、客观，通过多维度对比和参数敏感性分析，充分证明了方法的鲁棒性和公平性。

### 6. 主要结论与发现
*   **性能领先**：VAN-AD 在所有数据集上均显著优于现有的时序和文本基础模型。相比 2025 年的 SOTA 方法 CATCH，其平均 V-P 指标提升了 36.4%。
*   **视觉迁移有效性**：证明了视觉预训练模型提供的结构先验比 LLM 更适合时序异常检测。
*   **模块协同**：ADMM 有效放大了异常差异，而 NFM 通过全局密度估计弥补了 MAE 窗口感知的不足。
*   **模型规模悖论**：更大的视觉模型（Large/Huge）在时序任务上反而可能因过度拟合图像特征而导致性能下降。

### 7. 优点
*   **跨模态创新**：成功将视觉 MAE 引入 TSAD，避开了时序数据标注不足的难题。
*   **高效性**：通过热图编码实现多变量并行处理，推理效率远高于通道独立（Channel-independent）的方法。
*   **即插即用**：ADMM 和 NFM 的设计思路清晰，能够有效解决生成式模型在异常检测中的固有缺陷。

### 8. 不足与局限
*   **频率信息建模有限**：热图编码虽然高效，但相比 STFT 或小波变换，对显式频率特征的捕捉能力较弱。
*   **超参数依赖**：虽然实验显示模型对 $\lambda$（平衡重建与密度得分）相对鲁棒，但在极端不同的场景下仍需手动调优。
*   **视觉模型选择**：目前仅探索了 MAE，尚未深入研究如 CLIP 等其他视觉基础模型在 TSAD 中的对比学习潜力。

（完）
