---
title: "MemGuard-Alpha: Detecting and Filtering Memorization-Contaminated Signals in LLM-Based Financial Forecasting via Membership Inference and Cross-Model Disagreement"
title_zh: MemGuard-Alpha：通过成员推理与跨模型不一致性检测并过滤基于大语言模型的金融预测中的记忆污染信号
authors: "Anisha Roy, Dip Roy"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.26797v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 基于大语言模型的金融预测与时间数据污染
tldr: 针对大语言模型在金融预测中因记忆训练数据导致的“前瞻偏差”问题，本文提出MemGuard-Alpha框架。该框架包含MCS评分（结合成员推理攻击与时间特征）和CMMD算法（利用模型训练截止日期差异），旨在实时检测并过滤受污染的预测信号。实验表明，该方法显著提升了量化策略的夏普比率和样本外表现，揭示了记忆效应虽能提高表观准确率，却会损害模型的泛化能力。
selection_source: fresh_fetch
motivation: 解决大语言模型因记忆历史金融数据而产生的虚假预测准确性及前瞻偏差，且现有补救措施成本过高或信息损失严重。
method: 提出结合成员推理攻击与时间特征的MCS评分，以及利用不同模型训练截止日期差异进行信号筛选的CMMD算法。
result: 过滤后的信号使夏普比率从2.76提升至4.11，日均收益率提升7倍，并证实了记忆污染会导致样本外泛化性能下降。
conclusion: MemGuard-Alpha提供了一种低成本、实时的信号过滤方案，有效区分了模型真正的推理能力与数据记忆，保障了量化交易的有效性。
---

## 摘要
大语言模型（LLMs）越来越多地被用于生成金融 Alpha 信号，但越来越多的证据表明，LLMs 会记忆其训练语料库中的历史金融数据，从而产生虚假的预测准确率，这种准确率在样本外会发生崩溃。这种由记忆引起的先窥偏差威胁到基于 LLM 的量化策略的有效性。之前的补救措施——模型重训练和输入匿名化——要么成本极高，要么会引入显著的信息损失。目前尚无方法能为实时交易提供实用、零成本的信号级过滤。我们推出了 MemGuard-Alpha，这是一个生成后处理框架，包含两种算法：(i) MemGuard 综合评分（MCS），它通过逻辑回归将五种成员推理攻击（MIA）方法与时间邻近特征相结合，在污染分离方面实现了 Cohen's d = 18.57（仅使用 MIA 特征时 d = 0.39-1.37）；(ii) 跨模型记忆不一致性（CMMD），它利用不同 LLM 训练截止日期的差异，将记忆信号与真实的推理区分开来。在对 7 个 LLM（1.24 亿至 70 亿参数）、50 只标准普尔 100 指数成份股、42,800 个提示词以及 5 种 MIA 方法进行的跨越 5.5 年（2019-2024 年）的评估中，CMMD 实现了 4.11 的夏普比率，而未过滤信号的夏普比率为 2.76（提升了 49%）。清洁信号产生的平均日收益率为 14.48 bps，而受污染信号仅为 2.13 bps（相差 7 倍）。研究发现了一个显著的交叉模式：样本内准确率随污染程度增加而上升（从 40.8% 升至 52.5%），而样本外准确率则下降（从 47% 降至 42%），这为记忆以牺牲泛化能力为代价夸大表观准确率提供了直接证据。

## Abstract
Large language models (LLMs) are increasingly used to generate financial alpha signals, yet growing evidence shows that LLMs memorize historical financial data from their training corpora, producing spurious predictive accuracy that collapses out-of-sample. This memorization-induced look-ahead bias threatens the validity of LLM-based quantitative strategies. Prior remedies -- model retraining and input anonymization -- are either prohibitively expensive or introduce significant information loss. No existing method offers practical, zero-cost signal-level filtering for real-time trading. We introduce MemGuard-Alpha, a post-generation framework comprising two algorithms: (i) the MemGuard Composite Score (MCS), which combines five membership inference attack (MIA) methods with temporal proximity features via logistic regression, achieving Cohen's d = 18.57 for contamination separation (d = 0.39-1.37 using MIA features alone); and (ii) Cross-Model Memorization Disagreement (CMMD), which exploits variation in training cutoff dates across LLMs to separate memorized signals from genuine reasoning. Evaluated across seven LLMs (124M-7B parameters), 50 S&P 100 stocks, 42,800 prompts, and five MIA methods over 5.5 years (2019-2024), CMMD achieves a Sharpe ratio of 4.11 versus 2.76 for unfiltered signals (49% improvement). Clean signals produce 14.48 bps average daily return versus 2.13 bps for tainted signals (7x difference). A striking crossover pattern emerges: in-sample accuracy rises with contamination (40.8% to 52.5%) while out-of-sample accuracy falls (47% to 42%), providing direct evidence that memorization inflates apparent accuracy at the cost of generalization.

---

## 论文详细总结（自动生成）

### 论文总结：MemGuard-Alpha —— 检测与过滤金融预测中的大模型记忆污染

#### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLMs）在金融预测中存在严重的“记忆”现象，即模型记住了训练集中的历史股价和新闻，导致在回测中表现出虚假的超高准确率（前瞻偏差/Look-ahead bias）。一旦进入真实的样本外环境，这种性能会迅速崩溃。
*   **研究动机**：现有的解决方法（如重新训练模型、数据匿名化）要么成本极高（数百万美元），要么会导致严重的信息损失（如抹去公司名和日期）。研究者旨在寻找一种**零成本、实时、无需修改模型**的信号级过滤框架，以识别并剔除受记忆污染的预测信号。

#### 2. 方法论
MemGuard-Alpha 框架由两个核心算法组成：
*   **MemGuard 综合评分 (MCS)**：
    *   **核心思想**：通过逻辑回归模型，将五种成员推理攻击（MIA）指标与时间邻近特征结合，生成每个信号的污染概率。
    *   **关键技术**：集成了 Loss（平均负对数似然）、Min-K% Prob（关注低概率词）、Min-K%++（校准后的 Min-K%）、Zlib Ratio（压缩熵比率）和 Reference Model（与基准模型 GPT-2 的损失比）。
*   **跨模型记忆不一致性 (CMMD)**：
    *   **核心思想**：利用不同 LLM 具有不同训练截止日期（Cutoff Date）的天然特性。
    *   **流程**：对于同一个交易日和股票，获取多个模型的预测。根据 MCS 分数将模型分为“清洁组”和“污染组”（以中位数为界）。最终只采用清洁组模型的共识信号进行交易。

#### 3. 实验设计
*   **数据集**：
    *   50 只标准普尔 100 指数成份股的日线数据（2019 年 1 月至 2024 年 6 月）。
    *   Financial PhraseBank（作为确定被记忆的参考组）。
    *   42,800 个提示词（包含价格召回、情感分析和前瞻预测）。
*   **对比方法 (Benchmarks)**：
    *   **Raw Alpha**：未经过滤的 7 个模型平均信号。
    *   **Debiased Alpha**：基于固定阈值剔除高污染信号。
    *   **其他基准**：等权重买入持有（EW Buy-Hold）、20 日动量策略（Momentum-20d）、随机预测（Random）。
*   **评估模型**：涵盖 4 个家族的 7 个模型（GPT-2 系列、TinyLlama、Phi-2、Qwen2.5），参数量从 1.24 亿到 70 亿不等。

#### 4. 资源与算力
*   **硬件**：使用 RunPod 云端实例，配备单张 **NVIDIA GeForce RTX 5090 GPU** (32 GB GDDR7)。
*   **耗时**：
    *   MIA 评分计算：约 4.5 小时（处理 29.9 万个提示词-模型对）。
    *   Alpha 信号生成：约 6 小时。
*   **技术细节**：对 Qwen 7B 模型使用了 4-bit 量化以适应显存。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5.5 年的市场跨度，经历了疫情崩溃、复苏、加息周期及 AI 牛市，具有较强的市场代表性。
*   **消融实验**：对 MCS 的各个特征贡献进行了消融分析，并测试了不同模型组合对 CMMD 效果的影响。
*   **充分性评价**：实验设计较为客观，通过时间截止日期作为“地面真值”（Ground Truth）来验证 MIA 的有效性。但由于 Alpha 信号生成的计算成本，仅抽取了 93 个交易日进行组合回测，样本量在统计效力上略显不足。

#### 6. 主要结论与发现
*   **记忆误导性**：发现了一个“交叉模式”——随着污染程度增加，样本内准确率从 40.8% 升至 52.5%，但样本外准确率却从 47% 降至 42%。这证明记忆是以牺牲泛化能力为代价来夸大表观性能。
*   **策略提升**：CMMD 算法将夏普比率从 2.76 提升至 **4.11**（提升 49%）。
*   **收益差异**：清洁信号的平均日收益率为 14.48 bps，而受污染信号仅为 2.13 bps，两者相差 **7 倍**。
*   **规模效应**：模型越大，记忆效应越强；且训练数据的时效性（Recency）对记忆的影响超过了模型参数量。

#### 7. 优点
*   **实用性强**：作为后处理插件，无需重新训练模型，适用于任何开源权重的 LLM。
*   **信息保真**：相比匿名化方法，保留了所有金融实体和数值信息，避免了信号损失。
*   **动态适应**：CMMD 算法无需手动设置硬阈值，能自动适应不同模型和日期的污染分布。

#### 8. 不足与局限
*   **统计效力限制**：组合测试仅覆盖 93 个交易日，虽然结果显著，但更长周期的验证会更具说服力。
*   **闭源模型限制**：由于无法获取 GPT-4 等闭源 API 的逐词概率（Log-probs），部分 MIA 指标无法直接应用。
*   **幸存者偏差**：实验使用了当前的标普 100 成分股，未考虑历史上被剔除的公司。
*   **市场环境依赖**：测试期主要处于牛市，策略在长期熊市中的表现尚待验证。

（完）
