---
title: Dual-Space Knowledge Distillation with Key-Query Matching for Large Language Models with Vocabulary Mismatch
title_zh: 针对词表不匹配大语言模型的键-查询匹配双空间知识蒸馏
authors: "Stella Eva Tsiapali, Cong-Thanh Do, Kate Knill"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22056v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 分析了大语言模型的知识蒸馏
tldr: 针对大语言模型在词表不匹配情况下的知识蒸馏问题，本文深入分析了现有SOTA方法DSKD-CMA的内部机制，揭示了其在键值分布匹配上的局限性。为此，作者提出了一种基于生成对抗学习的新方法DSKD-CMA-GA，通过优化跨模型注意力机制，显著提升了文本生成质量，特别是在分布外数据上表现优异，有效缩小了异构词表蒸馏与同构词表蒸馏之间的性能差距。
selection_source: fresh_fetch
motivation: 现有的异构词表知识蒸馏方法DSKD-CMA内部机制不明，且存在跨模型键值分布不匹配导致的性能瓶颈。
method: 提出DSKD-CMA-GA方法，引入生成对抗学习机制来对齐来自不同模型的键（Key）和查询（Query）分布。
result: 实验显示该方法在文本生成质量上获得持续提升，尤其在分布外数据上ROUGE-L平均提高了0.37。
conclusion: 通过对抗学习优化跨模型注意力匹配，能有效解决词表不匹配带来的蒸馏难题并增强模型的泛化性能。
---

## 摘要
大语言模型（LLMs）在各项语言任务中均达到了最先进（SOTA）的性能，但由于其规模和资源需求，部署成本高昂。知识蒸馏（KD）通过训练较小的学生模型来模仿较大的教师模型，从而在不显著损失性能的情况下提高效率。带有跨模型注意力机制的双空间知识蒸馏（DSKD-CMA）已成为具有不同分词器的 LLM 之间进行知识蒸馏的一种 SOTA 方法，但其内部运作机制在很大程度上仍不透明。在这项工作中，我们通过手动词元对齐探测和热图可视化系统地分析了 DSKD-CMA 的注意力机制，揭示了其优势和局限性。在此基础上，我们提出了一种基于生成对抗（GA）学习的新方法 DSKD-CMA-GA，旨在解决不同模型计算出的键（keys）和查询（queries）之间分布不匹配的问题。实验表明，该方法在文本生成质量上带来了适度但持续的 ROUGE-L 提升，特别是在分布外数据上（平均提升 0.37），缩小了跨分词器和同分词器知识蒸馏之间的差距。

## Abstract
Large language models (LLMs) achieve state-of-the-art (SOTA) performance across language tasks, but are costly to deploy due to their size and resource demands. Knowledge Distillation (KD) addresses this by training smaller Student models to mimic larger Teacher models, improving efficiency without significant performance loss. Dual-Space Knowledge Distillation with Cross-Model Attention (DSKD-CMA) has emerged as a SOTA method for KD between LLMs with distinct tokenizers, yet its internal workings remain largely opaque. In this work, we systematically analyse the attention mechanism of DSKD-CMA through manual token alignment probing and heatmap visualisations, revealing both strengths and limitations. Building on this, we introduce a novel method, DSKD-CMA-GA, based on Generative Adversarial (GA) learning, to address the mismatched distributions between the keys and queries computed from distinct models. Experiments show modest but consistent ROUGE-L gains in text generation quality, particularly on out-of-distribution data (+0.37 on average), narrowing the gap between cross- and same-tokenizer KD.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）在知识蒸馏过程中面临的“词表不匹配（Vocabulary Mismatch）”问题提出了改进方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当教师模型（Teacher）和学生模型（Student）使用不同的分词器（Tokenizer）和词表时，传统的逻辑值（Logit）对齐方法失效。
*   **研究背景**：现有的 SOTA 方法 DSKD-CMA 通过跨模型注意力机制（CMA）在隐藏空间进行对齐，但其内部运作机制不透明，且由于键（Key，来自教师）和查询（Query，来自学生）源自不同模型，存在分布不匹配的问题，限制了蒸馏效果。
*   **研究动机**：通过深入分析 CMA 的对齐行为，引入分布匹配技术来优化跨模型注意力，从而缩小异构词表蒸馏与同构词表蒸馏之间的性能差距。

### 2. 论文提出的方法论
论文提出了两个层面的贡献：**分析框架**与**改进算法**。

*   **分析框架（Chunk-based Probing）**：
    *   **手动块对齐（Manual Chunk Alignment）**：通过字符偏移量将不同分词器的序列划分为跨度相同的“块（Chunks）”。
    *   **CLP（块级投影）**：强制在对齐的块之间进行均匀投影，用于测试简单对齐是否足够。
    *   **CLA（块级注意力）**：将注意力限制在对齐的块内，用于测试局部化注意力的效果。
*   **改进算法（Key-Query Matching）**：
    *   **DSKD-CMA-GA（生成对抗对齐）**：引入判别器 $D$ 尝试区分教师的 Key 和学生的 Query，学生通过投影器 $P_Q$ 学习欺骗判别器，从而使两者的分布在特征空间对齐。
    *   **DSKD-CMA-CT（条件传输对齐）**：利用最优传输（Optimal Transport）的约束版本，通过可训练的评论器（Critic）最小化 Key 和 Query 之间的传输成本。
    *   **总损失函数**：$L_{train} = L_{DSKD-CMA} + L_{KQ}$，其中 $L_{KQ}$ 为对抗或传输损失。

### 3. 实验设计
*   **模型设置**：
    *   **学生模型**：GPT-2 (124M)。
    *   **教师模型**：GPT-2 XL (1.5B，同分词器基准)；Qwen1.5 (1.8B，异构分词器挑战)。
*   **数据集**：
    *   **训练/分布内（ID）**：Dolly 15K。
    *   **分布外（OOD）**：Self-Instruct、Vicuna-Eval、Super-Natural Instructions (S-NI)、Unnatural Instructions (UnNI)。
*   **对比基准（Baselines）**：
    *   SFT（监督微调）、MinED（最小编辑距离对齐）、ULD（通用逻辑值蒸馏）、原始 DSKD-CMA。
*   **评估指标**：ROUGE-L 分数。

### 4. 资源与算力
*   **硬件**：使用了一个 Ampere 节点，配备 **4 张 NVIDIA A100 GPU (80GB)** 和 1TB RAM。
*   **时长**：每次训练会话耗时 **2-3 小时**，评估约需 1 小时。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 **5 个随机种子**取平均值，保证了结果的统计显著性。
    *   测试了 **5 个不同的评估数据集**（1 个 ID，4 个 OOD），覆盖了超过 26,000 个测试样本。
    *   对比了 **6 种不同的散度函数**（KL, RKL, SKL 等），以寻找每种方法的最优配置。
*   **充分性评价**：实验设计非常客观且公平。作者不仅对比了异构词表的方法，还对比了同构词表的上限，并进行了消融实验（CLP/CLA）来验证注意力机制的有效性。

### 6. 主要结论与发现
*   **CMA 的本质**：通过热图可视化发现，原始 CMA 确实能隐式捕捉到块级结构，但在处理重复词元（如数字）时存在噪声。
*   **GA 的优越性**：DSKD-CMA-GA 在所有数据集上均取得了持续的提升，尤其在 OOD 数据上平均提升了 **0.37 ROUGE-L**。
*   **缩小差距**：改进后的方法在某些任务上甚至超越了同分词器蒸馏的性能，证明了解决 Key-Query 分布不匹配的重要性。
*   **散度函数的影响**：发现不同的对齐机制需要配合特定的散度函数（如 GA 配合 Skewed KL 效果最好）。

### 7. 优点
*   **可解释性强**：通过手动块对齐探测，将“黑盒”的跨模型注意力变得可分析、可视化。
*   **方法论严谨**：引入对抗学习来解决特征分布不匹配问题，具有较强的理论支撑。
*   **开源贡献**：提供了公开的代码库，便于社区复现和后续研究。

### 8. 不足与局限
*   **提升幅度有限**：虽然结果持续提升，但 ROUGE-L 的绝对分值提升（约 0.3-1.0 点）相对较小。
*   **模型覆盖面**：实验主要集中在 GPT-2 和 Qwen1.5 这一对组合上，尚未在更大规模（如 7B 以上）或更多样化的模型架构上验证通用性。
*   **任务类型**：主要评估了文本生成质量，对于逻辑推理等更复杂的下游任务探讨较少。

（完）
