---
title: A Foundation Model for Instruction-Conditioned In-Context Time Series Tasks
title_zh: 一种用于指令调节的上下文内时间序列任务的基础模型
authors: "Anish Saha, Konstantin Shmakov"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22586v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 指令调节的上下文时间序列任务基础模型
tldr: 本研究提出了一种基于指令调节的时间序列基础模型，旨在通过上下文学习实现推理时的快速适配。该模型采用分层Transformer架构和分位数回归T5编码器-解码器，通过结构化分词处理历史示例和查询。在包含预测、填补、分类等多种任务的大规模数据集上进行预训练，使其无需微调即可在预测、分类和异常检测等任务中表现出色，显著提升了模型对局部结构的适应能力。
selection_source: fresh_fetch
motivation: 现有时间序列模型缺乏对显式指令和示例演示的利用，限制了其在推理阶段通过上下文学习进行灵活适配的能力。
method: 提出一种基于T5的分层Transformer架构，结合结构化分词方案和多任务预训练，实现对示例对的条件解码。
result: 在fev-bench和GIFT-Eval等多个预测基准上超越了强基线模型，并在分类和异常检测任务中保持了竞争力。
conclusion: 通过多任务预训练学习任务映射分布，该模型证明了显式指令调节能有效增强时间序列基础模型的通用性和零样本适配能力。
---

## 摘要
上下文学习 (ICL) 允许模型在推理阶段通过以示例为条件进行自适应，而无需更新参数。现有的时间序列基础模型通常使用隐式位置上下文、检索或特定任务的目标函数，但很少采用显式的指令调节演示。我们提出了一种基于分位数回归 T5 编码器-解码器的基础模型，用于指令调节的上下文内时间序列任务。历史示例和查询采用结构化分词方案进行编码，该方案标注了目标序列、协变量、上下文以及特定任务的未来信息。通过具有逐示例编码、示例级融合和跨示例注意力的分层 Transformer，模型以演示对为条件进行解码，从而在无需特定任务微调的情况下实现预测及相关任务。我们在大规模真实和合成时间序列上进行训练，结合了监督预测和自监督任务，包括插补、重构、分类、异常检测和源分离。这种多任务训练学习了任务映射的分布，并增强了推理时对局部结构的适应能力。在多种数据集、频率和预测长度下，我们的方法在点预测和概率预测基准（包括 fev-bench 和 GIFT-Eval）上优于强大的基础模型基准，同时在分类和异常检测任务中也保持了竞争力。

## Abstract
In-context learning (ICL) allows a model to adapt at inference time by conditioning on examples rather than updating parameters. Existing time-series foundation models use implicit positional context, retrieval, or task-specific objectives, but rarely explicit instruction-conditioned demonstrations. We present a foundation model for instruction-conditioned in-context time-series tasks based on a quantile-regression T5 encoder-decoder. Historical examples and queries are encoded with a structured tokenization scheme that marks target series, covariates, context, and task-specific future information. A hierarchical Transformer with per-example encoding, example-level fusion, and cross-example attention conditions decoding on demonstration pairs, enabling forecasting and related tasks without task-specific fine-tuning. We train on large-scale real and synthetic time series using supervised forecasting plus self-supervised tasks, including imputation, reconstruction, classification, anomaly detection, and source demixing. This multi-task training learns a distribution over task mappings and improves adaptation to local structure at inference time. Across diverse datasets, frequencies, and horizons, our method outperforms strong foundation baselines on point and probabilistic forecasting benchmarks, including fev-bench and GIFT-Eval, while remaining competitive on classification and anomaly detection.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Foundation Model for Instruction-Conditioned In-Context Time Series Tasks》的深度结构化总结：

---

# 论文分析报告：一种用于指令调节的上下文内时间序列任务的基础模型 (iAmTime)

## 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管大语言模型（LLM）已通过上下文学习（ICL）实现了强大的任务适配能力，但现有的时间序列基础模型（如 TimesFM, Chronos 等）大多仅将上下文用于提供历史数值，而非显式地通过“示例演示”来定义任务映射。
*   **研究动机**：在实际应用中，用户不仅需要预测，还需要异常检测、分类、填补等多种任务。传统的做法是为每个任务微调模型或设计特定磁头。
*   **整体含义**：本文提出了 **iAmTime** 模型，旨在通过“指令调节（Instruction-Conditioned）”的方式，让模型能够像 LLM 一样，仅通过推理时的几个输入-输出示例（Examples），就能理解并执行特定的时间序列任务，实现真正的零样本（Zero-shot）任务适配。

## 2. 方法论：核心思想与技术细节
*   **核心思想**：将时间序列任务建模为一种“提示词（Prompt）”工程。通过结构化的分词方案和分层 Transformer 架构，模型学习如何从示例中提取“任务规则”，并将其应用于查询（Query）。
*   **关键技术细节**：
    1.  **结构化分词（Structured Tokenization）**：引入专门的语义 Token（如 `[START]`, `[TARGET SERIES]`, `[EXOG]`, `[MID]`, `[END]`），显式区分目标序列、协变量、历史背景和未来信息，防止表示泄露。
    2.  **分层 Transformer 架构**：
        *   **阶段 1：个体编码**：对每个单变量序列进行 Patching 和编码。
        *   **阶段 2：示例内融合**：在单个示例内部，让目标序列与协变量进行交互。
        *   **阶段 3：跨示例融合（ICL 规则提取）**：通过均值池化提取示例的全局特征，生成“ICL 规则表示”，并将其注入到查询序列的解码过程中。
    3.  **概率解码器**：采用基于 T5 的编码器-解码器结构，配合混合专家（MoE）交叉注意力和分位数回归（Quantile Regression），直接输出 9 个分位数的概率预测。
    4.  **摊销元学习（Amortized Meta-Learning）**：在预训练阶段，模型不仅学习预测，还学习填补、重构、分类、异常检测和源分离等多种任务，使其在推理时能识别不同的任务模式。

## 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：使用了 Chronos 和 GIFT-Eval 的子集（约 6600 万条序列），涵盖零售、能源、气候、交通等领域。
    *   **合成数据**：利用 TSMixup、KernelSynth 以及自定义的多元协变量合成逻辑，增强模型对复杂因果和非线性关系的理解。
*   **Benchmark（基准测试）**：
    *   **fev-bench**：包含 100 个真实预测任务，涵盖单变量、多变量及带协变量的场景。
    *   **GIFT-Eval**：包含 24 个数据集、97 个评估设置，涵盖短、中、长期预测。
*   **对比方法**：
    *   **基础模型**：Chronos-2, TimesFM-2.5, TiRex, Moirai-2.0, TabPFN-TS, Toto-1.0 等。
    *   **传统统计/深度学习**：AutoARIMA, AutoETS, PatchTST, TFT, N-BEATS 等。

## 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **实现细节**：提到模型基于 T5 架构，采用了 Patching 技术以提高效率，并使用了课程学习（Curriculum Learning）策略，从短上下文/简单任务逐渐过渡到长上下文/复杂任务。

## 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型基准测试（fev-bench 和 GIFT-Eval）上进行了详尽测试，涉及数百个子任务。
    *   进行了**消融实验**，分别验证了示例（Examples）、语义 Token 和元学习目标对性能的贡献。
    *   进行了**分布偏移（Distribution Shift）**实验，验证模型在未见领域示例下的鲁棒性。
*   **充分性评价**：实验设计非常充分且客观。通过在完全未见的测试集上进行零样本评估，并与当前最先进的多个基础模型进行对比，有力地证明了其方法的有效性。

## 6. 主要结论与发现
*   **性能领先**：iAmTime 在 fev-bench 和 GIFT-Eval 上的综合评分（CRPS 和 MASE）均位居第一，显著优于 Chronos-2 和 TimesFM-2.5。
*   **ICL 的有效性**：消融实验证明，显式的示例演示和语义 Token 是模型能够进行任务适配的关键，移除这些组件会导致性能大幅下降。
*   **多任务通用性**：同一个架构在无需微调的情况下，在分类和异常检测任务中也表现出了极具竞争力的性能，证明了“摊销元学习”的成功。

## 7. 优点：亮点总结
*   **创新的 ICL 范式**：首次在时间序列领域系统性地引入了类似 NLP 的指令调节和结构化示例演示。
*   **分层融合机制**：巧妙解决了多变量、多示例带来的计算复杂度和表示混淆问题。
*   **强大的合成数据流水线**：通过数学手段合成具有因果关系的多元时间序列，弥补了高质量多元标注数据的不足。
*   **任务不可知解码**：实现了真正的统一模型，能够通过上下文切换任务模式。

## 8. 不足与局限
*   **推理延迟**：分层注意力机制虽然提高了质量，但相比于简单的单层 Transformer，其推理时的计算开销可能更大（尽管文中提到其速度优于部分模型）。
*   **示例依赖性**：模型的表现高度依赖于所提供的上下文示例的质量和相关性，如何自动选择最佳示例仍是一个挑战。
*   **非预测任务的评估深度**：虽然提到了分类和异常检测，但主要的 Benchmark 仍集中在预测任务上，对于其他任务的覆盖广度略显不足。
*   **算力透明度**：缺乏具体的算力消耗数据，不便于其他研究者评估其训练成本。

---
（完）
