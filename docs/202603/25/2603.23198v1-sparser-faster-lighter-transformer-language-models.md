---
title: "Sparser, Faster, Lighter Transformer Language Models"
title_zh: 更稀疏、更快、更轻量的 Transformer 语言模型
authors: "Edoardo Cetin, Stefano Peluchetti, Emilio Castillo, Akira Naruse, Mana Murakami, Llion Jones"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23198v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 通过稀疏化优化大语言模型 (LLM)
tldr: "针对大语言模型推理和训练的高昂计算成本，本文提出了一种利用前馈层（FFN）非结构化稀疏性的优化方案。通过引入新型稀疏打包格式和定制化CUDA内核，结合L1正则化诱导出的99%以上稀疏度，在保持模型性能的同时，显著提升了吞吐量、能效并降低了内存占用，为大模型的高效扩展提供了新路径。"
selection_source: fresh_fetch
motivation: 旨在解决自回归大语言模型在规模扩展过程中面临的巨大计算成本、能耗及内存压力。
method: 提出一种新型稀疏打包格式和配套CUDA内核，并利用L1正则化在FFN层诱导极高比例的非结构化稀疏性。
result: "在稀疏度超过99%的情况下，模型性能几乎无损，且在吞吐量、能效和内存使用上均获得随模型规模增长的显著收益。"
conclusion: 证明了非结构化稀疏性是提升现代基础模型效率和可扩展性的实用且高效的途径。
---

## 摘要
扩展自回归大语言模型（LLM）推动了前所未有的进步，但也带来了巨大的计算成本。在这项工作中，我们通过利用 LLM 前馈层中的非结构化稀疏性来解决这些成本问题，前馈层是占据模型大部分参数和执行 FLOPs 的核心组件。为实现这一目标，我们引入了一种新的稀疏打包格式和一组 CUDA 算子，旨在与现代 GPU 的优化执行流水线无缝集成，从而在 LLM 推理和训练期间实现高效的稀疏计算。为了证实我们的收益，我们对 LLM 稀疏性进行了定量研究，证明简单的 L1 正则化可以诱导超过 99% 的稀疏性，且对下游性能的影响微乎其微。当与我们的算子结合使用时，我们展示了这些稀疏水平可以转化为显著的吞吐量、能效和内存使用优势，且这些优势随模型规模的扩大而增加。我们将以开源许可证发布所有代码和算子，以促进采用并加速相关研究，旨在将稀疏性确立为提高现代基础模型效率和可扩展性的实用维度。

## Abstract
Scaling autoregressive large language models (LLMs) has driven unprecedented progress but comes with vast computational costs. In this work, we tackle these costs by leveraging unstructured sparsity within an LLM's feedforward layers, the components accounting for most of the model parameters and execution FLOPs. To achieve this, we introduce a new sparse packing format and a set of CUDA kernels designed to seamlessly integrate with the optimized execution pipelines of modern GPUs, enabling efficient sparse computation during LLM inference and training. To substantiate our gains, we provide a quantitative study of LLM sparsity, demonstrating that simple L1 regularization can induce over 99% sparsity with negligible impact on downstream performance. When paired with our kernels, we show that these sparsity levels translate into substantial throughput, energy efficiency, and memory usage benefits that increase with model scale. We will release all code and kernels under an open-source license to promote adoption and accelerate research toward establishing sparsity as a practical axis for improving the efficiency and scalability of modern foundation models.

---

## 论文详细总结（自动生成）

这是一份关于论文《Sparser, Faster, Lighter Transformer Language Models》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）规模的持续扩张，其计算成本、能耗和内存需求呈爆炸式增长。
*   **核心问题**：前馈网络（FFN）层占据了模型约 2/3 的参数和 80% 的计算量（FLOPs）。虽然研究表明 FFN 存在天然的激活稀疏性，但由于非结构化稀疏与现代 GPU 架构（针对稠密矩阵优化）之间存在严重的硬件不匹配，导致“理论计算量减少，实际运行速度变慢”的悖论。
*   **整体含义**：本文旨在通过创新的稀疏数据格式和定制化 CUDA 内核，打破这一瓶颈，使非结构化稀疏性成为提升 LLM 训练和推理效率的实用手段。

### 2. 论文提出的方法论
*   **核心思想**：利用简单的 L1 正则化诱导极高比例的激活稀疏性，并通过新型稀疏格式 **TwELL** 及其配套算子，在不引入昂贵同步开销的前提下实现硬件加速。
*   **关键技术细节**：
    *   **TwELL (Tile-wise ELLPACK) 格式**：传统 ELL 格式需要全局行对齐，与 GPU 的分块（Tiled）计算冲突。TwELL 将矩阵划分为水平 1D 分块（Tile），在分块内部进行局部 ELL 压缩，允许在矩阵乘法内核的 Epilogue 阶段直接生成稀疏格式，无需额外同步。
    *   **推理优化（算子融合）**：将 Gate 投影与稀疏化过程融合，并将随后的 Up 投影和 Down 投影融合为一个单一内核。通过在内核内部隐式物化激活值，大幅减少了对 DRAM 的访问。
    *   **训练优化（混合格式 Hybrid Format）**：针对训练中稀疏模式不均匀的问题，提出混合格式。将大部分稀疏行存入紧凑的 ELL 矩阵，而将少数非零元素过多的“溢出行”存入稠密备份，从而在保证内存节省的同时兼顾计算效率。
    *   **稀疏诱导方案**：采用 ReLU 激活函数，并在标准交叉熵损失基础上增加 L1 正则化项，通过调节系数 $L_1$ 来控制稀疏度。

### 3. 实验设计
*   **数据集**：使用 FineWeb (FineWeb-Edu 分支) 进行预训练。
*   **Benchmark**：在 7 个下游任务上评估逻辑和常识推理能力，包括 ARC (Easy/Hard)、HellaSwag、OpenBookQA、PIQA、WinoGrande 和 CommonsenseQA。
*   **对比方法**：
    *   **稠密基准**：标准的 Transformer++ 架构（ReLU 和 SiLU 激活）。
    *   **不同稀疏水平**：对比了 8 种不同的 L1 正则化系数（从 0 到 $10^{-4}$）。
    *   **架构变体**：对比了 Gated FFN（现代主流）和 Non-gated FFN（原始 Transformer）。

### 4. 资源与算力
*   **硬件型号**：主要实验在单节点 **8x NVIDIA H100 PCIe GPU** 上完成。
*   **辅助测试**：在 **RTX 6000 Ada** GPU 上验证了对消费级/工作站硬件的加速效果。
*   **训练规模**：遵循 Chinchilla 最佳实践，训练量涵盖 10B 到 40B tokens。
*   **模型规模**：涵盖 0.5B、1B、1.5B 和 2B 参数规模。

### 5. 实验数量与充分性
*   **实验组数**：论文进行了多维度实验，包括 4 种模型规模的扩展性实验、8 种稀疏梯度的消融实验、ReLU vs SiLU 的性能对比、以及针对“死神经元”缓解策略的专项实验。
*   **充分性与公平性**：实验设计较为充分，不仅关注了吞吐量（Throughput），还测量了实际能耗（Energy）和峰值内存（Peak Memory）。对比实验均在相同的超参数和 Token 预算下进行，确保了客观性。

### 6. 论文的主要结论与发现
*   **性能无损的高稀疏度**：通过轻微的 L1 正则化，可以诱导超过 **99%** 的激活稀疏性，而对下游任务准确率的影响微乎其微（性能损失在 2% 以内）。
*   **显著的效率提升**：在 2B 规模模型上，稀疏算子使推理吞吐量提升了 **20.5%**，训练步长提速 **21.9%**，峰值内存降低了 **22.3%**。
*   **规模效应**：随着模型规模增大，稀疏性带来的收益（提速比和内存节省）呈现增加趋势，证明了该方法在大模型上的潜力。
*   **计算分配特性**：模型倾向于在序列开头的 Token 和具有高信息含量的 Token（如特定名词、动词）上分配更多的激活神经元。

### 7. 优点
*   **实用性强**：不同于以往只停留在理论 FLOPs 减少的研究，本文提供了可在现代 GPU 上真实提速的开源 CUDA 内核。
*   **全生命周期优化**：方案同时覆盖了训练和推理阶段，特别是混合格式解决了训练中稀疏模式不均导致的内存瓶颈。
*   **兼容性**：对现有 Transformer 架构改动极小（仅需更换激活函数和增加正则化），易于集成到现有流水线。

### 8. 不足与局限
*   **模型规模上限**：实验目前最高仅验证至 2B 参数，尚未在百亿或千亿级（如 70B+）模型上验证其极端稀疏下的稳定性。
*   **激活函数限制**：虽然论文证明了 ReLU 与 SiLU 差距不大，但目前主流 LLM 多采用 SiLU/SwiGLU，切换到 ReLU 可能会在某些特定领域任务上存在微小性能偏差。
*   **超参数敏感性**：L1 正则化系数需要根据模型规模和任务进行手动调优，缺乏一种自动化的稀疏度控制机制。

（完）
