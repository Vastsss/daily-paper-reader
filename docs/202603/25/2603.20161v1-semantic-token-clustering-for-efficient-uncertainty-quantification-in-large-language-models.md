---
title: Semantic Token Clustering for Efficient Uncertainty Quantification in Large Language Models
title_zh: 大语言模型中高效不确定性量化的语义 Token 聚类
authors: "Qi Cao, Andrew Gambardella, Takeshi Kojima, Yutaka Matsuo, Yusuke Iwasawa"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20161v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 侧重于大语言模型中的不确定性量化
tldr: 针对大语言模型（LLM）输出不可靠且现有不确定性量化方法计算开销大的问题，本文提出语义标记聚类（STC）方法。该方法利用LLM内在的语义信息，通过嵌入聚类和前缀匹配将标记划分为语义一致的簇，并基于簇的概率质量量化不确定性。STC仅需单次生成且无需辅助模型，在保持与最先进方法相当性能的同时，显著降低了计算成本，为提升LLM可靠性提供了一种高效方案。
selection_source: fresh_fetch
motivation: 现有的大语言模型不确定性量化方法通常依赖多次采样或辅助模型，导致计算开销巨大且效率低下。
method: 提出语义标记聚类（STC），通过嵌入聚类和前缀匹配将标记分组，并利用语义簇的聚合概率来量化不确定性。
result: 实验结果显示，STC在不确定性量化性能上可比肩现有最先进基准，同时大幅减少了计算资源消耗。
conclusion: STC证明了仅通过单次生成和模型内在语义信息即可实现高效且准确的不确定性量化。
---

## 摘要
大语言模型（LLMs）在各种任务中展现出了卓越的能力。然而，其输出的真实性无法得到保证，且其过度自信的倾向进一步限制了可靠性。不确定性量化为识别潜在不可靠输出提供了一种有前景的方法，但大多数现有方法依赖于重复采样或辅助模型，从而引入了巨大的计算开销。为了解决这些局限性，我们提出了语义 Token 聚类（STC），这是一种利用 LLMs 中固有编码的语义信息的高效不确定性量化方法。具体而言，我们利用嵌入聚类和前缀匹配将 Token 分组为语义一致的簇，并基于在相应语义簇上聚合的概率质量来量化不确定性。我们的方法仅需单次生成，且不依赖于辅助模型。实验结果表明，STC 在显著降低计算开销的同时，达到了与最先进基准方法相当的性能。

## Abstract
Large language models (LLMs) have demonstrated remarkable capabilities across diverse tasks. However, the truthfulness of their outputs is not guaranteed, and their tendency toward overconfidence further limits reliability. Uncertainty quantification offers a promising way to identify potentially unreliable outputs, but most existing methods rely on repeated sampling or auxiliary models, introducing substantial computational overhead. To address these limitations, we propose Semantic Token Clustering (STC), an efficient uncertainty quantification method that leverages the semantic information inherently encoded in LLMs. Specifically, we group tokens into semantically consistent clusters using embedding clustering and prefix matching, and quantify uncertainty based on the probability mass aggregated over the corresponding semantic cluster. Our approach requires only a single generation and does not depend on auxiliary models. Experimental results show that STC achieves performance comparable to state-of-the-art baselines while substantially reducing computational overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semantic Token Clustering for Efficient Uncertainty Quantification in Large Language Models》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是大语言模型（LLM）的**可靠性与不确定性量化（Uncertainty Quantification, UQ）**。
*   **背景**：LLM 虽强大但常出现幻觉且表现出“过度自信”，在医疗、法律等高风险领域应用受限。
*   **核心问题**：现有的不确定性量化方法存在严重的效率瓶颈。例如，采样法（Sampling-based）需要多次生成响应，计算成本高；辅助模型法（Auxiliary-model-based）依赖外部自然语言推理（NLI）模型，引入了额外的推理延迟。
*   **研究动机**：探索一种既能捕捉语义一致性，又能在单次生成（Single-generation）中完成、且无需外部模型的高效量化方案。

### 2. 方法论：语义 Token 聚类（STC）
STC 的核心思想是利用 LLM 内部已有的语义表示，将概率分布从“Token 级别”聚合到“语义簇级别”。

*   **预计算阶段（Offline）**：
    *   **嵌入聚类**：提取 LLM 词表中每个 Token 的输入和输出层嵌入并拼接，使用**层次聚类（Agglomerative Clustering）**算法将数万个 Token 划分为语义一致的簇（如“TV”、“television”、“televis”被归为一类）。
*   **推理阶段（Online）**：
    *   **前缀匹配（Prefix Matching）**：在解码每一步，识别词表中哪些 Token 是后续生成内容的合法前缀，以增强语义一致性。
    *   **概率聚合**：在每个解码步 $i$，将属于同一语义簇或符合前缀匹配的所有候选 Token 的概率进行求和，得到该步的“簇概率质量” $\hat{p}_c$。
    *   **得分计算**：整个序列的不确定性得分 $S(x, y)$ 定义为 $1$ 减去各步簇概率的连乘积。

### 3. 实验设计
*   **数据集**：TriviaQA (TQA)、Natural Questions (NQ) 和 WebQuestions (WQ)，涵盖了常识和事实性问答。
*   **评估指标**：AUROC（衡量区分正确与错误回答的能力）和 PRR（预测拒绝率）。
*   **基准模型（Backbones）**：Llama-2-7B、Llama-3-8B、Mistral-7B 以及 Qwen2.5 系列（3B, 7B, 14B）。
*   **对比方法**：共对比了 14 种方法，包括：
    *   **单次生成类**：Perplexity、tokenSAR、CCP。
    *   **采样类**：Predictive Entropy、Semantic Entropy、EigenScore、SAR 等。
    *   **提示类**：P(True)。

### 4. 资源与算力
*   **硬件环境**：
    *   **GPU**：8 张 Nvidia A100 (40GB)。
    *   **CPU**：2 颗 Intel Xeon Platinum 8368。
*   **算力特点**：
    *   **STC 的优势**：该方法在推理时**不需要 GPU**，可在 CPU 上高效运行。
    *   **预计算开销**：聚类过程根据模型词表大小不同，耗时在 1 分钟（Llama-2）到 33 分钟（Qwen2.5-14B）之间，且每个模型仅需计算一次。
    *   **推理开销**：相比于依赖 NLI 模型的 CCP 方法，STC 的推理时间开销平均降低了 **98%**。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流数据集上对 6 个不同架构/规模的模型进行了全面测试。
*   **消融实验**：分别验证了“嵌入聚类”和“前缀匹配”的必要性，结果显示两者具有互补作用。
*   **敏感性分析**：针对聚类算法（K-means vs Agglomerative）、距离度量（欧氏距离 vs 余弦距离）、簇数量（8k-16k）以及嵌入类型进行了详尽的鲁棒性测试。
*   **公平性**：使用了 GPT-4.1 作为自动评测器来判定回答的正确性，避免了传统 ROUGE 指标的局限性，实验设计较为客观、严谨。

### 6. 主要结论与发现
*   **性能卓越**：STC 在所有测试模型和数据集上的表现均达到或超过了现有的 SOTA（最先进）水平。
*   **效率极高**：STC 是目前唯一一个既能感知语义（Semantic-aware）、又支持单次采样、且无需外部模型的方法。
*   **语义结构有效性**：实验证明 LLM 的静态 Token 嵌入中已经包含了足够的语义信息，足以支持有效的不确定性量化，而无需复杂的动态推理。

### 7. 优点
*   **计算低廉**：单次生成即可获得得分，极大地降低了延迟和算力成本，适合资源受限场景。
*   **自包含性**：不依赖外部 NLI 模型或额外的监督训练，易于部署到任何白盒 LLM 上。
*   **即插即用**：无需微调模型，直接利用模型词表特征。

### 8. 不足与局限
*   **白盒限制**：该方法需要访问模型的 Logits（概率分布）和 Embedding（嵌入层），因此**无法直接应用于闭源 API 模型**（如 GPT-4 原始接口）。
*   **上下文缺失**：聚类基于静态嵌入，未考虑 Token 在特定语境下的多义性（虽然实验表明 LLM 的概率分配能部分缓解此问题）。
*   **校准问题**：论文未深入探讨不确定性得分的绝对校准（Calibration），即得分与实际错误率的数值对齐。

（完）
