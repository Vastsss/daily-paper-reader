---
title: "Rethinking Multimodal Fusion for Time Series: Auxiliary Modalities Need Constrained Fusion"
title_zh: 重新思考时间序列的多模态融合：辅助模态需要受限融合
authors: "Seunghan Lee, Jun Seo, Jaehoon Lee, Sungdong Yoo, Minjae Kim, Tae Yoon Lim, Dongwan Kang, Hwanil Choi, SoonYoung Lee, Wonbin Ahn"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22372v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 文本与时间序列的跨模态融合预测
tldr: 本研究探讨了在时间序列预测中集成文本等辅助模态的挑战，指出简单的融合策略常因引入无关信息而导致性能下降。为此，作者提出了受限融合的概念，并开发了受控融合适配器（CFA）。CFA 作为一个即插即用的模块，利用低秩适配器过滤无关文本信息，确保仅融合与时间序列动态相关的特征。通过 2 万多次实验，证明了该方法在多种数据集和模型上的有效性。
selection_source: fresh_fetch
motivation: 现有的多模态时间序列模型常因简单的融合策略引入无关噪声，导致其表现甚至不如单模态模型。
method: 提出了受控融合适配器（CFA），通过低秩适配器在不改变主干网络的情况下过滤并融合相关的文本信息。
result: 在多种数据集和模型组合上的大规模实验表明，受限融合方法（尤其是 CFA）显著优于传统的简单融合策略。
conclusion: 辅助模态的集成需要受控的融合机制，以有效过滤噪声并提升多模态时间序列预测的泛化能力。
---

## 摘要
近期多模态学习的进展推动了将文本或视觉等辅助模态集成到时间序列（TS）预测中。然而，大多数现有方法带来的收益有限，通常仅在特定数据集上提高性能，或者依赖于限制泛化能力的特定架构设计。在本文中，我们展示了采用朴素融合策略（如简单的加法或拼接）的多模态模型往往表现不如单模态 TS 模型，我们将此归因于辅助模态的无控集成，这可能会引入无关信息。受此观察启发，我们探索了旨在控制此类集成的各种受限融合方法，并发现它们始终优于朴素融合方法。此外，我们提出了受控融合适配器（CFA），这是一种简单的插件式方法，能够在不修改 TS 骨干网络的情况下实现受控的跨模态交互，仅集成与 TS 动态对齐的相关文本信息。CFA 采用低秩适配器在将文本信息融合进时间表示之前过滤掉无关信息。我们在各种数据集和 TS/文本模型上进行了超过 2 万次实验，证明了包括 CFA 在内的受限融合方法的有效性。代码已公开：https://github.com/seunghan96/cfa/。

## Abstract
Recent advances in multimodal learning have motivated the integration of auxiliary modalities such as text or vision into time series (TS) forecasting. However, most existing methods provide limited gains, often improving performance only in specific datasets or relying on architecture-specific designs that limit generalization. In this paper, we show that multimodal models with naive fusion strategies (e.g., simple addition or concatenation) often underperform unimodal TS models, which we attribute to the uncontrolled integration of auxiliary modalities which may introduce irrelevant information. Motivated by this observation, we explore various constrained fusion methods designed to control such integration and find that they consistently outperform naive fusion methods. Furthermore, we propose Controlled Fusion Adapter (CFA), a simple plug-in method that enables controlled cross-modal interactions without modifying the TS backbone, integrating only relevant textual information aligned with TS dynamics. CFA employs low-rank adapters to filter irrelevant textual information before fusing it into temporal representations. We conduct over 20K experiments across various datasets and TS/text models, demonstrating the effectiveness of the constrained fusion methods including CFA. Code is publicly available at: https://github.com/seunghan96/cfa/.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rethinking Multimodal Fusion for Time Series: Auxiliary Modalities Need Constrained Fusion》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着大语言模型（LLM）的发展，将文本等辅助模态集成到时间序列（TS）预测中成为热点。然而，研究发现采用简单的“朴素融合”（如直接加法或拼接）往往导致多模态模型表现甚至不如仅使用时间序列的单模态模型。
*   **核心问题**：作者认为时间序列是主模态，而文本是辅助模态。朴素融合没有考虑到模态间的对齐问题，容易引入与预测无关的噪声或冲突信息，从而破坏了时间序列原有的时间特征表示。因此，论文探讨了如何通过“受限融合”（Constrained Fusion）来有效集成辅助信息。

### 2. 论文提出的方法论
*   **核心思想**：提出**受控融合适配器（CFA, Controlled Fusion Adapter）**。这是一种即插即用的插件方法，通过低秩瓶颈（Low-rank Bottleneck）限制文本信号的容量，从而过滤无关信息，仅保留对预测有益的引导信号。
*   **关键技术细节**：
    *   **低秩瓶颈**：借鉴 LoRA 的思想，将高维文本嵌入先投影到低维空间（$D/r$），再投影回原维度。这种设计限制了文本信号的影响范围。
    *   **残差连接**：将处理后的文本信号作为微小的残差项加到 TS 骨干网络的每一层编码器输出中。
    *   **初始化策略**：将上采样权重矩阵初始化为接近零，确保训练初期文本模态对 TS 骨干的影响最小，保持训练稳定性。
*   **算法流程**：文本经编码器生成嵌入 $\rightarrow$ CFA 模块（下采样 $\rightarrow$ 非线性激活/归一化 $\rightarrow$ 上采样） $\rightarrow$ 与 TS 嵌入进行残差相加 $\rightarrow$ 进入下一层 TS 编码器。

### 3. 实验设计
*   **数据集**：使用了 **Time-MMD** 基准中的 9 个多模态数据集，涵盖农业、气候、经济、能源、环境、公共卫生、安全、社会公益和交通领域。
*   **Benchmark 与对比方法**：
    *   **单模态基准**：14 种主流 TS 模型（如 PatchTST, iTransformer, DLinear, TiDE 等）。
    *   **朴素融合**：在不同位置（First, Middle, Last 层）进行加法或拼接融合。
    *   **受限融合对比**：门控机制（Gating）、FiLM（特征线性调制）、正交融合（Orthogonal）。
    *   **文本模型**：BERT, GPT-2, Llama-3, Doc2Vec。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   但从实验规模（超过 20,000 组配置）可以推断，该研究消耗了巨大的计算资源。作者提到每种设置都经过了 10 个学习率的超参数搜索，以确保对比的公平性。

### 5. 实验数量与充分性
*   **实验规模**：进行了超过 **20,000 次**实验（14 TS 模型 × 4 文本模型 × 9 数据集 × 4 预测长度 × 10 种融合策略）。
*   **充分性与公平性**：实验设计极其详尽，覆盖了从 Transformer 到线性模型的各种架构，并对每种配置进行了严格的调优（10 个学习率取最优）。此外，还进行了针对“无关文本”的鲁棒性实验和合成数据的消融实验，论证过程非常客观且全面。

### 6. 论文的主要结论与发现
*   **朴素融合的局限性**：在大多数情况下，简单的拼接或加法融合会导致性能下降，甚至不如单模态模型。
*   **受限融合的优越性**：受限融合方法（尤其是 CFA）在几乎所有设置下都能稳定提升性能。
*   **CFA 的有效性**：CFA 在 9 个数据集中的 7 个排名第一，在 14 个 TS 模型中的 13 个上实现了性能提升。
*   **低秩的作用**：理论和实验均证明，低秩瓶颈能有效抑制误导性信号（如冲突文本），同时保留有用的趋势信息。

### 7. 优点
*   **通用性强**：CFA 是模型无关的插件，无需修改原 TS 模型的架构即可应用。
*   **参数高效**：相比于复杂的跨模态注意力机制，CFA 仅增加约 0.61% 的参数量和 0.04% 的 FLOPs。
*   **鲁棒性高**：在输入完全无关的随机文本时，CFA 能自动“忽略”噪声，保持与单模态相当的性能，不会像朴素融合那样导致崩溃。

### 8. 不足与局限
*   **模态局限**：目前的研究主要集中在“时间序列 + 文本”的融合，尚未验证其在视觉或表格等其他辅助模态上的表现。
*   **理论深度**：虽然提供了低秩子空间的理论视角，但对于不同领域数据集为何需要特定秩（Reduction Ratio）的深层数学解释仍有待加强。
*   **实时性**：虽然推理开销小，但在超大规模实时预测场景下的表现未做专门讨论。

（完）
