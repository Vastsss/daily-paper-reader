---
title: Scaling Recurrence-aware Foundation Models for Clinical Records via Next-Visit Prediction
title_zh: 通过下次就诊预测扩展临床记录的循环感知基础模型
authors: "Haresh Rengaraj Rajamohan, Xiang Gao, Weicheng Zhu, Shih-Lun Huang, Long Chen, Gabe Schulman, Huizhen Jin, Shengduo Li, Yixuan Wang, Huidi Yang, Kyunghyun Cho, Cem M. Deniz, Narges Razavian"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24562v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 通过下一次就诊预测构建临床记录基础模型
tldr: 本文提出RAVEN，一种针对结构化电子健康记录（EHR）的生成式预训练模型。通过在百万级患者数据上进行下一次就诊事件预测，RAVEN引入了重复事件正则化，解决了评估中因重复记录导致的性能虚高问题。研究探讨了数据受限下的模型缩放行为，并在零样本疾病预测任务中展现出媲美微调模型的性能及跨机构的泛化能力。
selection_source: fresh_fetch
motivation: 旨在解决结构化EHR数据在大规模预训练中的应用不足，以及现有评估方法中重复事件导致的性能偏差问题。
method: 提出基于下一次就诊事件预测的自回归生成策略，并引入针对重复临床事件的正则化机制。
result: RAVEN在零样本疾病预测中达到了与全微调模型相当的水平，并能有效泛化至存在特征差异的外部患者群体。
conclusion: 在数据受限环境下单纯增加模型参数并非最优，而结合重复事件感知的生成式预训练是提升医疗基础模型性能的关键。
---

## 摘要
尽管大规模预训练彻底改变了语言建模，但在使用结构化电子健康档案（EHR）的医疗保健领域，其潜力仍未得到充分挖掘。我们提出了 RAVEN，这是一种基于循环感知下次就诊事件预测的新型序列化 EHR 数据生成式预训练策略。利用超过一百万名独立个体的资料集，我们的模型学习在给定患者历史记录的情况下，自回归地生成下次就诊的标记化临床事件。我们引入了针对重复事件预测的正规化，并指出了基于 EHR 的基础模型评估中的一个关键陷阱：当新发病例与后续发生未加区分时，重复的事件标记会虚高性能指标。此外，我们实证研究了在数据受限、计算饱和状态下的扩展行为，结果表明，在没有相应增加数据量的情况下，单纯增加模型规模并非最优选择。我们通过零样本预测评估了模型对多种疾病发病率的预测能力，其表现可与完全微调的基于表示的 Transformer 模型相媲美，并优于广泛使用的基于模拟的下个标记方法。最后，在无需额外参数更新的情况下，我们展示了 RAVEN 在存在有损临床代码映射和特征覆盖缺口的情况下，仍能泛化到外部患者队列。

## Abstract
While large-scale pretraining has revolutionized language modeling, its potential remains underexplored in healthcare with structured electronic health records (EHRs). We present RAVEN, a novel generative pretraining strategy for sequential EHR data based on Recurrence-Aware next-Visit EveNt prediction. Leveraging a dataset of over one million unique individuals, our model learns to autoregressively generate tokenized clinical events for the next visit conditioned on patient history. We introduce regularization on predicting repeated events and highlight a key pitfall in EHR-based foundation model evaluations: repeated event tokens can inflate performance metrics when new onsets are not distinguished from subsequent occurrences. Furthermore, we empirically investigate the scaling behaviors in a data-constrained, compute-saturated regime, showing that simply increasing model size is suboptimal without commensurate increases in data volume. We evaluate our model via zero-shot prediction for forecasting the incidence of a diverse set of diseases, where it rivals fully fine-tuned representation-based Transformer models and outperforms widely used simulation-based next-token approaches. Finally, without additional parameter updates, we show that RAVEN can generalize to an external patient cohort under lossy clinical code mappings and feature coverage gaps.

---

## 论文详细总结（自动生成）

### 论文总结：通过下次就诊预测扩展临床记录的循环感知基础模型 (RAVEN)

#### 1. 核心问题与整体含义（研究动机和背景）
传统的电子健康记录（EHR）预测模型通常针对特定疾病和固定时间窗口训练判别式模型，这种方式难以扩展且统计效率低下。虽然生成式基础模型（Foundation Models）在自然语言处理领域取得了成功，但在 EHR 领域面临三大挑战：
*   **就诊内无序性**：单次就诊内的临床事件（诊断、药物、化验）通常没有严格的时间顺序，传统的“下一个标记预测（Next-token prediction）”并不自然。
*   **重复事件干扰（循环陷阱）**：慢性病诊断在患者记录中会反复出现。如果模型仅通过“重复”历史诊断就能获得高分，则无法真正学习预测“新发疾病（New onset）”的能力。
*   **数据规模限制**：医疗数据量远小于互联网文本，盲目增加模型参数可能导致严重的过拟合。

#### 2. 方法论
论文提出了 **RAVEN**（Recurrence-Aware next-Visit EveNt prediction），其核心思想包括：
*   **下次就诊事件预测**：将预训练目标设定为预测下一次就诊的完整临床事件集合（多标签分类），而非单个标记。这尊重了就诊内事件的无序性，并允许通过单次前向传播进行多时间跨度的风险评估。
*   **循环感知正则化（Recurrence-Aware Regularization）**：引入历史依赖的权重方案。对于目标就诊中已在历史记录中出现的标记，根据其出现频率进行指数级下采样（通过衰减因子 $\lambda$ 控制）。公式上，通过降低重复标记在损失函数中的权重，强迫模型关注临床轨迹中的“新变化”。
*   **架构优化**：采用 Decoder-only Transformer，修改因果注意力机制使其在“就诊级别”生效（就诊内标记可互相查看，但不能查看未来就诊）。使用旋转位置嵌入（RoPE）来编码从首次就诊开始的绝对时间。

#### 3. 实验设计
*   **数据集**：
    *   **内部数据**：纽约大学朗格尼医学中心（NYU Langone）的 129 万名患者记录（2013-2023年），包含 4.2 万个唯一临床标记。
    *   **外部验证**：斯坦福大学的 EHRSHOT 公共基准数据集（6,739 名患者），用于测试跨机构的零样本（Zero-shot）迁移能力。
*   **评估场景**：针对 7 种多样化疾病（痴呆症、膝骨关节炎、胰腺癌、前列腺癌、急性心梗、充血性心衰、慢阻肺）进行 2 年和 5 年期的发病预测。
*   **对比方法（Benchmark）**：
    *   **生成式基线**：标准下个标记模型（Multiclass）、基于集合损失的模型（SeqLoss）、联合间隙与事件生成模型（EGE）。
    *   **监督学习基线**：经过全微调的 BERT 基础模型。
    *   **外部基线**：EHRSHOT 中的 GBM、逻辑回归、随机森林等。

#### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   但文中提到为了研究扩展定律（Scaling Laws），训练了超过 **80 个不同配置的模型**，参数量跨度从 68 万到 8.48 亿。这表明实验消耗了相当规模的计算资源。

#### 5. 实验数量与充分性
*   **实验规模**：实验设计非常详尽，涵盖了从 10% 到 100% 的数据预算缩放、8 种模型规模的对比、以及不同正则化强度 $\lambda$ 的消融实验。
*   **客观性与公平性**：采用了患者级别的划分以防止信息泄露；在零样本评估中，通过排除预测点后一年内发病的病例来减少标签泄露风险；使用了 AUROC、AUPRC 和新提出的“准时率（On-time rate）”等指标，评估维度全面且客观。

#### 6. 主要结论与发现
*   **零样本能力强**：RAVEN 在无需任何微调的情况下，其零样本预测性能在多数疾病上优于需要多次模拟生成的自回归基线，甚至在某些任务上接近或超过了全微调的 BERT 模型。
*   **正则化的必要性**：引入循环感知正则化显著提升了模型预测“新发疾病”的准确性（On-time rate 翻倍），证明了抑制重复信息对医疗模型的重要性。
*   **计算饱和下的扩展定律**：在数据受限的情况下，模型性能随参数增加呈 **U 型曲线**。这意味着在医疗领域，单纯增加模型大小而不增加数据多样性会导致过拟合，必须实现模型容量与数据规模的匹配。
*   **跨机构泛化**：尽管存在编码映射损失和特征缺失，RAVEN 在斯坦福 EHRSHOT 数据集上仍表现出竞争力，证明了其学习到了通用的临床逻辑。

#### 7. 优点
*   **效率高**：相比需要 100 次蒙特卡洛模拟的传统生成模型，RAVEN 只需一次前向传播即可预测未来风险。
*   **临床对齐**：通过正则化机制解决了 EHR 建模中长期被忽视的“慢性病重复记录”导致的评估虚高问题。
*   **实证深入**：对数据受限环境下的 Scaling Law 进行了深入探讨，为未来医疗大模型的构建提供了实用指南。

#### 8. 不足与局限
*   **数据单一性**：预训练主要基于单一医疗系统的数据，虽然进行了外部验证，但不同机构间的编码习惯差异（如 ICD-10 映射损耗）仍是障碍。
*   **特征覆盖**：目前模型尚未整合心电图、影像或非结构化文本等模态。
*   **代理指标限制**：EHR 中的“记录时间”仅是临床发病的代理指标，可能存在滞后，模型无法完全捕捉真实的生物学发病时间。
*   **干预模拟不足**：目前仅关注预测，尚未探索模型在模拟治疗干预效果（因果推断）方面的潜力。

（完）
