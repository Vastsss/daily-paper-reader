---
title: "ReLaMix: Residual Latency-Aware Mixing for Delay-Robust Financial Time-Series Forecasting"
title_zh: ReLaMix：用于延迟鲁棒金融时间序列预测的残差延迟感知混合网络
authors: "Tianyou Lai, Wentao Yue, Jiayi Zhou, Chaoyuan Hao, Lingke Chang, Qingyu Mao, Zhibo Niu, Qilei Li"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20869v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 金融时间序列预测
tldr: 针对高频金融市场中因数据传输延迟导致的观测值滞后问题，本文提出ReLaMix模型。该模型在TimeMixer基础上引入可学习的瓶颈压缩与残差细化机制，旨在从受零阶保持（ZOH）干扰的信号中恢复有效市场动态。实验证明，ReLaMix在处理不同延迟比例的数据时表现优异，以更少的参数量在多个基准测试中达到了SOTA水平，展现了极强的鲁棒性。
selection_source: fresh_fetch
motivation: 现实高频交易中异步数据采集和传输延迟会导致观测值停滞，严重影响预测准确性。
method: 提出一种轻量级的残差延迟感知混合网络，通过瓶颈压缩抑制冗余陈旧值，并利用残差混合增强信号恢复。
result: 在PAXGUSDT和BTCUSDT等大规模高频数据集上，该方法在多种延迟场景下均显著优于现有的Mixer和Transformer基准模型。
conclusion: ReLaMix证明了残差瓶颈混合机制在处理具有现实延迟特征的金融时间序列预测任务中的有效性和鲁棒性。
---

## 摘要
现实高频市场中的金融时间序列预测常受到异步数据采集和传输延迟导致的观测值延迟或部分陈旧的阻碍。为了更好地反映此类实际情况，我们研究了一种模拟延迟设置，其中部分历史信号受到零阶保持（ZOH）机制的破坏，通过阶梯状停滞伪影显著增加了预测难度。在本文中，我们提出了 ReLaMix（残差延迟感知混合网络），这是 TimeMixer 的一种轻量级扩展，它集成了可学习的瓶颈压缩与残差细化，用于在延迟观测下实现鲁棒的信号恢复。ReLaMix 显式地抑制了来自重复陈旧值的冗余，同时通过残差混合增强保留了具有信息量的市场动态。在大型秒级分辨率 PAXGUSDT 基准测试上的实验表明，ReLaMix 在多种延迟比例和预测时界下始终保持最先进的准确度，以显著更少的参数量优于强大的 Mixer 和 Transformer 基准模型。此外，在 BTCUSDT 上的额外评估证实了所提框架的跨资产泛化能力。这些结果突显了残差瓶颈混合在现实延迟导致的陈旧情况下进行高频金融预测的有效性。

## Abstract
Financial time-series forecasting in real-world high-frequency markets is often hindered by delayed or partially stale observations caused by asynchronous data acquisition and transmission latency. To better reflect such practical conditions, we investigate a simulated delay setting where a portion of historical signals is corrupted by a Zero-Order Hold (ZOH) mechanism, significantly increasing forecasting difficulty through stepwise stagnation artifacts. In this paper, we propose ReLaMix (Residual Latency-Aware Mixing Network), a lightweight extension of TimeMixer that integrates learnable bottleneck compression with residual refinement for robust signal recovery under delayed observations. ReLaMix explicitly suppresses redundancy from repeated stale values while preserving informative market dynamics via residual mixing enhancement. Experiments on a large-scale second-resolution PAXGUSDT benchmark demonstrate that ReLaMix consistently achieves state-of-the-art accuracy across multiple delay ratios and prediction horizons, outperforming strong mixer and Transformer baselines with substantially fewer parameters. Moreover, additional evaluations on BTCUSDT confirm the cross-asset generalization ability of the proposed framework. These results highlight the effectiveness of residual bottleneck mixing for high-frequency financial forecasting under realistic latency-induced staleness.

---

## 论文详细总结（自动生成）

### ReLaMix: 用于延迟鲁棒金融时间序列预测的残差延迟感知混合网络

#### 1. 核心问题与整体含义（研究动机和背景）
在高频交易（HFT）等现实金融场景中，由于网络拥塞、硬件性能限制或交易所接口瓶颈，数据采集往往存在物理延迟。这种延迟导致观测到的信号出现**零阶保持（Zero-Order Hold, ZOH）**现象，即在真实值更新前，观测值保持不变，形成“阶梯状”的停滞伪影。
*   **研究动机**：现有的深度学习模型（如 LSTM、Transformer 或通用的 Mixer 模型）在处理此类陈旧（Stale）数据时，容易过拟合停滞噪声，或因计算复杂度过高而无法满足实时交易的超低延迟要求。
*   **核心问题**：如何在存在延迟诱导的陈旧观测值的情况下，以极低的计算开销准确恢复并预测真实的金融市场动态。

#### 2. 论文提出的方法论
论文提出了 **ReLaMix (Residual Latency-Aware Mixing Network)**，这是一种轻量级的端到端架构，核心思想是利用“信息瓶颈”原则过滤冗余，并通过残差机制强化信号恢复。
*   **信息瓶颈压缩（Bottleneck Compression）**：首先将高维的受损输入投影到一个极低维的瓶颈空间（如将维度压缩至 32）。这一步旨在显式抑制由 ZOH 产生的重复冗余值，迫使模型只保留最具预测性的特征。
*   **残差延迟感知混合块（Residual Mixing Block）**：
    *   **时间混合（Time Mixing）**：在时间维度上进行线性混合，捕捉全局时间依赖，平滑 ZOH 引入的局部不连续性。
    *   **特征混合（Feature Mixing）**：采用“扩展-压缩（Expand-Compress）”策略（例如维度从 32 扩展到 64 再压缩回 32），类似于轻量级网络中的倒置残差结构，以增强非线性建模能力。
*   **多级跳跃连接（Multi-Level Skip Connections）**：通过累积残差连接将各层特征聚合，形成“信息高速公路”，防止深层网络中的梯度消失，并确保低层预测线索能直接传递到输出端。

#### 3. 实验设计
*   **数据集**：
    *   **PAXGUSDT**：主要的秒级高频交易数据集（来自币安交易所），包含 OHLCV 等多维特征。
    *   **BTCUSDT**：用于验证跨资产泛化能力的辅助数据集。
*   **实验场景**：模拟了 15%、25%、35% 三种不同的随机延迟比例（ZOH 干扰）。
*   **Benchmark（对比方法）**：
    *   循环神经网络：LSTM, GRU。
    *   卷积/混合架构：TimesNet, TimeMixer。
    *   Transformer 架构：PatchTST。
*   **评估指标**：MSE（均方误差）、MAE（平均绝对误差）、$R^2$（确定系数）。

#### 4. 资源与算力
*   **硬件**：实验在一台配备单张 **NVIDIA GeForce RTX 4090 GPU** 的服务器上完成。
*   **软件/训练细节**：使用 PyTorch 框架，Adam 优化器，学习率为 $1 \times 10^{-3}$，Batch Size 为 32。采用了早停机制（Early Stopping）以防止过拟合。
*   **训练时长**：文中未明确给出具体的训练总时长，但强调了该模型参数量极小，暗示了其训练和推理的高效性。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个不同资产的数据集上进行了测试，涵盖了 3 种延迟比例和 4 种预测时界（k=1, 5, 7, 10），总计进行了数十组对比实验。
*   **消融实验**：专门设计了“无压缩模块”和“无残差细化”的变体实验，验证了各核心组件的必要性。
*   **客观性与公平性**：采用了严格的非重叠分段评估协议以防止时间泄漏（Temporal Leakage），并与当前最先进的（SOTA）模型在相同环境下对比，实验设计较为客观、严谨。

#### 6. 主要结论与发现
*   **性能领先**：ReLaMix 在所有延迟比例和预测步长下均显著优于 Transformer 和 Mixer 基准模型，尤其在处理严重延迟（35%）时表现出极强的鲁棒性。
*   **极度轻量化**：ReLaMix 的参数量仅为 **13K~15K** 左右，而对比模型如 TimesNet（~229K）和 PatchTST（~102K）的参数量是其数倍甚至数十倍。
*   **泛化能力**：在 BTCUSDT 上的实验证明了该模型在不同波动率和市场环境下的跨资产有效性。
*   **关键发现**：残差连接对于防止信息丢失至关重要，而瓶颈压缩能有效过滤掉 ZOH 产生的伪影噪声。

#### 7. 优点
*   **针对性强**：专门针对金融高频交易中的 ZOH 延迟问题建模，而非通用的时间序列预测。
*   **效率极高**：通过“Less-is-More”的设计哲学，在提升精度的同时大幅降低了计算开销，非常适合对推理延迟极其敏感的实盘交易环境。
*   **端到端恢复**：无需先进行缺失值插补再预测，减少了分布偏移风险。

#### 8. 不足与局限
*   **延迟模式单一**：实验主要基于随机 ZOH 模拟，现实中的延迟可能具有更复杂的自相关性或突发性（如网络风暴导致的连续长停滞），模型在极端异常延迟下的表现尚待验证。
*   **资产覆盖面**：虽然测试了加密货币，但未在股票、期货等具有不同微观结构的其他金融市场进行验证。
*   **硬件部署细节**：论文虽提到适合实时部署，但未给出在 FPGA 或嵌入式交易系统上的实际推理延迟（Latency）具体数值。

（完）
