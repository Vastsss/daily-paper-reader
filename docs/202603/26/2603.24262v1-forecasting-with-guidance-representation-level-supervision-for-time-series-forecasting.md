---
title: "Forecasting with Guidance: Representation-Level Supervision for Time Series Forecasting"
title_zh: 引导式预测：时间序列预测的表示级监督
authors: "Jiacheng Wang, Liang Fan, Baihua Li, Luyan Zhang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24262v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 利用预训练时间序列基础模型进行预测
tldr: 针对深度学习时间序列预测中因误差导向训练导致极端模式丢失、预测过于平滑的问题，本文提出ReGuider插件式方法。该方法利用预训练时间序列大模型作为语义导师，通过表示层监督将目标模型的编码器嵌入与大模型的中间层嵌入对齐，从而学习更具表现力的时间特征。实验证明该方法能显著提升多种架构在不同数据集上的预测精度，具有极强的通用性。
selection_source: fresh_fetch
motivation: 传统的误差最小化训练目标容易导致模型忽略极端但重要的模式，使得预测结果过于平滑且表征能力不足。
method: 提出ReGuider插件，通过将目标模型的编码器嵌入与预训练大模型的中间层语义嵌入进行对齐，实现表示层监督。
result: 在多种数据集和模型架构上的广泛实验表明，ReGuider能持续且显著地提升时间序列预测的准确性。
conclusion: ReGuider是一种通用且有效的增强手段，通过引入大模型的表示级指导，显著提升了时间序列预测模型的表征学习能力。
---

## 摘要
如今，时间序列预测主要通过使用基于误差的目标函数对深度学习架构进行端到端训练来实现。虽然这种方法在最小化平均损失方面非常有效，但它会促使编码器丢弃具有信息量但极端的模式。这导致预测结果过于平滑，且时间表示难以捕捉显著的动态变化。为了解决这一问题，我们提出了 ReGuider，这是一种可以无缝集成到任何预测架构中的插件式方法。ReGuider 利用预训练的时间序列基础模型作为语义教师。在训练过程中，输入序列由目标预测模型和预训练模型共同处理。我们并非直接使用预训练模型的输出，而是提取其富含时间与语义信息的中间嵌入，并通过表示级监督将其与目标模型的编码器嵌入进行对齐。这一对齐过程使编码器能够学习到更具表达力的时间表示，从而提高下游预测的准确性。在不同数据集和架构上的广泛实验表明，我们的 ReGuider 能够持续提升预测性能，证实了其有效性和通用性。

## Abstract
Nowadays, time series forecasting is predominantly approached through the end-to-end training of deep learning architectures using error-based objectives. While this is effective at minimizing average loss, it encourages the encoder to discard informative yet extreme patterns. This results in smooth predictions and temporal representations that poorly capture salient dynamics. To address this issue, we propose ReGuider, a plug-in method that can be seamlessly integrated into any forecasting architecture. ReGuider leverages pretrained time series foundation models as semantic teachers. During training, the input sequence is processed together by the target forecasting model and the pretrained model. Rather than using the pretrained model's outputs directly, we extract its intermediate embeddings, which are rich in temporal and semantic information, and align them with the target model's encoder embeddings through representation-level supervision. This alignment process enables the encoder to learn more expressive temporal representations, thereby improving the accuracy of downstream forecasting. Extensive experimentation across diverse datasets and architectures demonstrates that our ReGuider consistently improves forecasting performance, confirming its effectiveness and versatility.

---

## 论文详细总结（自动生成）

以下是对论文《Forecasting with Guidance: Representation-Level Supervision for Time Series Forecasting》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的深度学习时间序列预测（TSF）模型主要依赖基于误差（如 MSE/MAE）的端到端训练。这种目标函数虽然能最小化平均损失，但会导致编码器倾向于丢弃“极端但具有信息量”的模式，从而产生过于平滑的预测，且学习到的潜在表示（Embeddings）缺乏丰富的语义信息，难以捕捉显著的时间动态。
*   **研究背景**：时间序列基础模型（TSFM）通过大规模数据预训练，已经掌握了通用的时间动态和丰富的语义特征。本文旨在探讨如何利用这些大模型的“知识”来指导特定任务的小模型，而不仅仅是直接使用大模型进行推理。

### 2. 方法论（ReGuider）
*   **核心思想**：提出一种名为 **ReGuider** 的插件式方法，将预训练的时间序列基础模型作为“语义导师”，通过**表示级监督（Representation-Level Supervision）**，将目标预测模型的编码器嵌入与基础模型的中间层嵌入进行对齐。
*   **关键技术细节**：
    *   **双路径架构**：输入序列同时输入“目标预测模型”和“基础模型”。
    *   **表示提取**：从目标模型提取编码器输出 $H_f$，从基础模型提取富含语义的中间层嵌入 $H_g$。
    *   **对齐损失**：引入辅助损失函数 $L_{TSRA}$（如欧氏距离），最小化 $H_f$ 与 $H_g$ 之间的差异。
    *   **不对称梯度流**：在训练过程中，基础模型的参数被冻结（Stop Gradient），仅作为静态的语义锚点，防止表示漂移。
    *   **联合优化**：总损失函数为预测损失与表示对齐损失的加权和：$L_{total} = L_{Pred} + \lambda L_{TSRA}$。

### 3. 实验设计
*   **数据集**：涵盖 7 个主流基准数据集，包括 ETT 系列（ETTh1/2, ETTm1/2）、Weather（天气）、Electricity（电力/ECL）和 Traffic（交通）。
*   **Benchmark 与对比方法**：
    *   **基础架构（Backbones）**：选择了四种代表性模型：iTransformer、PatchTST、DLinear 和 TimeMixer。
    *   **基础模型（Guider）**：主要使用 Time-MoE（包括 Base、Large、Ultra 三个版本）。
    *   **对比场景**：对比了各模型在原始状态下与集成 ReGuider 插件后的预测表现（MSE 和 MAE）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。
*   **效率备注**：作者提到 ReGuider 仅在训练阶段调用基础模型，**推理阶段不需要基础模型参与**，因此不会增加模型部署后的推理延迟和算力负担。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在 7 个数据集、4 种预测长度（96, 192, 336, 720）上对 4 种架构进行了全面测试。
    *   **消融实验**：对比了三种距离度量（欧氏距离、KL 散度、余弦相似度）的效果。
    *   **模型规模实验**：测试了不同参数量（Base 到 Ultra）的基础模型作为导师的影响。
    *   **可视化分析**：通过 t-SNE 降维展示了受引导后编码器表示的聚类效果。
*   **充分性评价**：实验设计较为客观、公平，覆盖了从线性到 Transformer 的多种架构，以及从低维到高维的不同领域数据集，验证了方法的通用性。

### 6. 主要结论与发现
*   **性能提升**：ReGuider 在所有测试架构上均实现了性能提升，平均准确率提高超过 5%。
*   **高维优势**：在变量极多（如 Traffic 数据集，862 个变量）的复杂场景下，提升尤为显著，说明基础模型的“通用时间词汇表”有助于捕捉复杂的变量间耦合。
*   **长程稳定性**：在长预测周期（T=720）下，ReGuider 作为语义锚点，有效缓解了传统模型容易出现的潜在状态漂移问题。
*   **度量偏好**：欧氏距离（Euclidean Distance）在表示对齐任务中表现最稳定，优于 KL 散度和余弦相似度。

### 7. 优点与亮点
*   **即插即用（Plug-in）**：不改变原模型的推理结构，具有极强的通用性和灵活性。
*   **推理零成本**：巧妙地通过训练阶段的知识迁移提升性能，而无需在生产环境中部署庞大的基础模型。
*   **语义增强**：突破了单一 MSE 损失的局限，通过外部语义监督解决了“表示坍缩”问题，使模型能学习到更具判别力的特征。

### 8. 不足与局限
*   **训练开销增加**：虽然推理很快，但训练时需要同时运行基础模型，会显著增加显存占用和单步训练时间。
*   **依赖导师质量**：该方法的效果高度依赖于所选基础模型的预训练质量。如果基础模型与目标领域分布差异过大，引导效果可能会打折扣。
*   **超参数敏感性**：对齐损失的权重 $\lambda$ 可能需要针对不同数据集进行精细调优，论文未给出通用的参数设置指导。
*   **输入长度限制**：实验主要固定输入长度为 96，未深入探讨在极短或极长回顾窗口下的表现。

（完）
