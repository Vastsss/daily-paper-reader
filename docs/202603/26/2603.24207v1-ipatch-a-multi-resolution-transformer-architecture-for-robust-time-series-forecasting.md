---
title: "IPatch: A Multi-Resolution Transformer Architecture for Robust Time-Series Forecasting"
title_zh: IPatch：一种用于稳健时间序列预测的多分辨率 Transformer 架构
authors: "Aymane Harkati, Moncef Garouani, Olivier Teste, Julien Aligon, Mohamed Hamlich"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24207v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 用于稳健时间序列预测的 Transformer 架构
tldr: 针对多元时间序列预测中捕捉短期波动与长期依赖的挑战，本文提出IPatch架构。传统Transformer模型在点对点表示（细粒度但计算量大且缺乏上下文）与分块表示（高效但丢失细节）之间存在权衡。IPatch通过集成点对点和分块两种Token，实现了多分辨率的时间信息建模。在7个基准数据集上的实验证明，该方法在预测精度、抗噪能力和跨预测周期的泛化性方面均优于单一表示模型。
selection_source: fresh_fetch
motivation: 传统时间序列Transformer在细粒度点表示与高效但粗糙的分块表示之间难以兼顾，限制了对复杂动态的建模能力。
method: 提出IPatch架构，通过同时整合点对点和分块Token，在多个分辨率上对时间序列进行多尺度建模。
result: 在7个基准数据集上的实验结果显示，IPatch在预测准确性、鲁棒性和泛化能力上均显著优于现有的单表示基准模型。
conclusion: 多分辨率表示的结合能有效平衡局部细节与全局上下文，是提升长程时间序列预测性能的关键。
---

## 摘要
由于需要同时捕捉短期波动和长期时间依赖关系，多变量时间序列的准确预测仍然具有挑战性。基于 Transformer 的模型已成为一种强大的方法，但其性能关键取决于时间数据的表示方式。传统的逐点（point-wise）表示保留了单个时间步的信息，从而实现细粒度建模，但其计算成本往往较高，且在建模更广泛的上下文依赖关系方面效果较差，限制了其在长序列上的可扩展性。逐块（patch-wise）表示将连续的时间步聚合为紧凑的标记（tokens），以提高效率并建模局部时间动态，但它们往往会丢弃细粒度的时间细节，而这些细节对于波动较大或复杂的时间序列的准确预测至关重要。我们提出了 IPatch，这是一种多分辨率 Transformer 架构，它集成了逐点和逐块标记，在多个分辨率下对时间信息进行建模。在 7 个基准数据集上的实验表明，与单一表示的基准模型相比，IPatch 在各种预测周期内一致地提高了预测准确性、对噪声的稳健性以及泛化能力。

## Abstract
Accurate forecasting of multivariate time series remains challenging due to the need to capture both short-term fluctuations and long-range temporal dependencies. Transformer-based models have emerged as a powerful approach, but their performance depends critically on the representation of temporal data. Traditional point-wise representations preserve individual time-step information, enabling fine-grained modeling, yet they tend to be computationally expensive and less effective at modeling broader contextual dependencies, limiting their scalability to long sequences. Patch-wise representations aggregate consecutive steps into compact tokens to improve efficiency and model local temporal dynamics, but they often discard fine-grained temporal details that are critical for accurate predictions in volatile or complex time series. We propose IPatch, a multi-resolution Transformer architecture that integrates both point-wise and patch-wise tokens, modeling temporal information at multiple resolutions. Experiments on 7 benchmark datasets demonstrate that IPatch consistently improves forecasting accuracy, robustness to noise, and generalization across various prediction horizons compared to single-representation baselines.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **IPatch** 的新型 Transformer 架构，旨在解决多变量时间序列预测中“局部细节”与“全局上下文”难以兼顾的矛盾。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在时间序列预测中，现有的 Transformer 模型通常采用两种数据表示方式：
    *   **逐点表示（Point-wise）**：保留了每个时间步的细粒度信息，但计算开销大，且难以捕捉长程上下文。
    *   **逐块表示（Patch-wise）**：通过聚合连续时间步提高效率并捕捉局部语义，但在波动剧烈或复杂的数据中会丢失关键的微观细节。
*   **研究动机**：作者发现简单的“重叠分块（Overlapping Patches）”策略并不能有效弥补信息损失。因此，需要一种能够同时在多个分辨率（多尺度）上建模时间动态的统一架构。

### 2. 方法论：IPatch 架构
IPatch 采用了**双流（Dual-Stream）架构**，核心思想是将全局分块注意力与局部分块自相关相结合：
*   **分块与位置编码**：将输入序列划分为固定长度的 Patch，并加入位置编码以保留时间顺序。
*   **流 A：全局分块注意力（Patch-based Attention）**：
    *   使用标准的 Transformer 编码器，在 Patch 级别计算自注意力。
    *   **目的**：捕捉不同时间段（块与块之间）的长程依赖关系和粗粒度趋势。
*   **流 B：局部分块自相关（Patch's Point-wise Autocorrelation）**：
    *   在每个 Patch 内部进行逐点建模。
    *   **技术细节**：利用快速傅里叶变换（FFT）将信号转至频域，识别周期性模式。
    *   **创新点**：引入了 **Fourier-KAN（FKAN）** 层代替传统的 Softmax。FKAN 利用可学习的周期性傅里叶基函数，能够更灵活地处理正向和负向的时间相关性。
*   **融合与预测**：将两个流的输出进行拼接（Concatenate），展平后通过线性层直接映射到预测长度 $H$。

### 3. 实验设计
*   **数据集**：涵盖了 7 个主流基准数据集：ETT 系列（ETTh1, ETTh2, ETTm1, ETTm2）、Electricity（电力）、Weather（天气）和 ILI（流感病例）。
*   **预测场景**：长程预测（Long-term forecasting），Look-back 窗口固定为 96，预测长度 $H$ 在 $\{96, 192, 336, 720\}$ 之间（ILI 为 $\{24, 36, 48, 60\}$）。
*   **对比方法（Benchmark）**：对比了 11 种最先进的模型，包括：
    *   **Transformer 类**：PatchTST, Autoformer, FEDformer, Informer, Crossformer。
    *   **线性/混合类**：DLinear, TimeMixer, TimesNet, MICN, FiLM, Stationary。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率分析**：文中提供了一张“预测效果 vs. 训练效率”的对比图（图 3）。结果显示 IPatch 在保持高精度的同时，训练速度显著快于 Crossformer 和 FEDformer 等复杂模型，其效率与轻量级的 DLinear 接近，但精度远高。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集、4 种预测长度下进行了测试，共计 28 组核心实验。
*   **消融实验**：
    *   验证了“仅 Patch”和“仅自相关”的效果，证明了双流结合的必要性。
    *   对比了 Fourier-KAN 与 Softmax 激活函数的性能差异。
*   **充分性评价**：实验设计较为全面，涵盖了不同领域（电力、气象、医疗）和不同特性的序列（周期性、非平稳性），并提供了定性的可视化预测曲线，客观地展示了模型在捕捉尖峰和波动方面的优势。

### 6. 主要结论与发现
*   **性能领先**：IPatch 在 MAE 指标上于 26/28 个配置中排名第一，在 MSE 指标上于 21/28 个配置中排名第一。
*   **多分辨率优势**：结合点对点和块对块的表示，能显著提升对非平稳、高波动数据的预测能力（如在 ILI 数据集上提升尤为明显）。
*   **FKAN 的有效性**：在长程预测中，基于傅里叶的 KAN 层比 Softmax 能更好地捕捉频域依赖，在某些任务中（如 ETTh1）使 MSE 降低了 21%。

### 7. 优点与亮点
*   **架构平衡**：成功统一了细粒度精度与长程上下文建模，解决了 PatchTST 等模型容易平滑掉局部特征的问题。
*   **引入 FKAN**：将最新的 Kolmogorov-Arnold Network (KAN) 变体应用于时间序列频域建模，增强了模型的表达能力。
*   **高性价比**：在计算效率和预测精度之间取得了极佳的平衡，适合实际工业应用。

### 8. 不足与局限
*   **超参数敏感性**：Patch 的长度和重叠度对结果有影响，虽然文中进行了优化，但尚未实现自适应的 Patch 划分。
*   **输入长度限制**：实验主要基于固定 96 的 Look-back 窗口，未深入探讨极长输入窗口下的表现。
*   **可解释性**：尽管 FKAN 具有一定的数学美感，但双流融合后的决策过程对于领域专家来说仍是一个“黑盒”，缺乏深度的可解释性分析。

（完）
