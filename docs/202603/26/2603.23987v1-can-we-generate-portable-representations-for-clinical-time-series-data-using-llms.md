---
title: Can we generate portable representations for clinical time series data using LLMs?
title_zh: 我们能否利用大语言模型（LLMs）为临床时间序列数据生成可移植的表示？
authors: "Zongliang Ji, Yifei Sun, Andre Amaral, Anna Goldenberg, Rahul G. Krishnan"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23987v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 利用 LLM 进行临床时间序列表示与预测
tldr: 针对临床机器学习模型在跨医院部署时因分布偏移导致性能下降的问题，本文提出利用大语言模型（LLM）生成可移植的患者表示。该方法将不规则的ICU时间序列数据转化为自然语言摘要，再通过文本嵌入模型生成固定长度向量。在多个数据集和任务上的实验表明，该方法在保持竞争力的同时，显著提升了跨医院迁移的鲁棒性和少样本学习能力，且未增加隐私风险，为临床预测模型的可扩展部署提供了新思路。
selection_source: fresh_fetch
motivation: 解决临床预测模型在不同医院间迁移时因分布偏移导致的性能下降和高昂的重新训练成本。
method: 利用冻结的LLM将ICU时间序列转化为自然语言摘要，再通过文本嵌入模型提取特征作为下游预测器的输入。
result: 该方法在多项任务中表现与主流基准相当，但在跨医院迁移时性能下降更小，且在少样本场景下表现更优。
conclusion: LLM生成的文本表示具有良好的可移植性和鲁棒性，能有效降低临床预测模型的工程开销并促进其规模化应用。
---

## 摘要
部署临床机器学习（ML）既缓慢又脆弱：在一家医院运行良好的模型在另一家医院往往会因分布偏移而性能下降。在这项工作中，我们研究了一个简单的问题——大语言模型（LLMs）能否创建可移植的患者嵌入（即患者表示），使得在一家医院构建的下游预测器能够在其他地方使用，且只需极少甚至无需重新训练和微调。为此，我们使用冻结的 LLM 将不规则的 ICU 时间序列映射为简洁的自然语言摘要，然后使用冻结的文本嵌入模型对每个摘要进行嵌入，从而获得一个固定长度的向量，该向量可作为各种下游预测器的输入。在三个队列（MIMIC-IV、HIRID、PPICU）的多个临床预测和分类任务中，我们发现我们的方法简单易用，且在分布内性能上与网格插补（grid imputation）、自监督表示学习和时间序列基础模型相当，同时在迁移到新医院时表现出更小的相对性能下降。我们研究了不同提示词设计下的性能变化，发现结构化提示词对于在不改变平均准确率的情况下降低预测模型的方差至关重要。我们发现，使用这些可移植表示可以提高少样本学习能力，且相对于基准模型，并未增加年龄或性别等人口统计学信息的可恢复性，这表明几乎没有额外的隐私风险。我们的工作指出了 LLM 作为工具在通过减少工程开销来实现生产级预测模型的可扩展部署方面的潜力。

## Abstract
Deploying clinical ML is slow and brittle: models that work at one hospital often degrade under distribution shifts at the next. In this work, we study a simple question -- can large language models (LLMs) create portable patient embeddings i.e. representations of patients enable a downstream predictor built on one hospital to be used elsewhere with minimal-to-no retraining and fine-tuning. To do so, we map from irregular ICU time series onto concise natural language summaries using a frozen LLM, then embed each summary with a frozen text embedding model to obtain a fixed length vector capable of serving as input to a variety of downstream predictors. Across three cohorts (MIMIC-IV, HIRID, PPICU), on multiple clinically grounded forecasting and classification tasks, we find that our approach is simple, easy to use and competitive with in-distribution with grid imputation, self-supervised representation learning, and time series foundation models, while exhibiting smaller relative performance drops when transferring to new hospitals. We study the variation in performance across prompt design, with structured prompts being crucial to reducing the variance of the predictive models without altering mean accuracy. We find that using these portable representations improves few-shot learning and does not increase demographic recoverability of age or sex relative to baselines, suggesting little additional privacy risk. Our work points to the potential that LLMs hold as tools to enable the scalable deployment of production grade predictive models by reducing the engineering overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《Can we generate portable representations for clinical time series data using LLMs?》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：临床机器学习模型在跨医院部署时表现脆弱，由于不同医院的传感器模式、采样频率、临床策略和数据缺失模式存在差异（分布偏移），模型往往需要大量的重新训练和校准。
*   **研究动机**：作者提出“可移植的输入表示（Portable Input Representations）”这一概念。与其让模型去适应不同的数据格式，不如将异构的医疗记录映射到一个语义对齐、与站点无关的通用接口（自然语言），从而降低跨站点迁移的工程开销。

### 2. 论文提出的方法论：Record2Vec
*   **核心思想**：采用“先摘要、后嵌入（Summarize-then-Embed）”的流水线，利用大语言模型（LLM）作为信息转换层，将不规则的时间序列转化为标准化的临床语义向量。
*   **关键技术流程**：
    1.  **临床摘要（Summarization）**：使用冻结的（Frozen）LLM（如 Gemini-2.0 Flash, MedGemma, Llama-3.1）将 48 小时不规则的 ICU 时间序列记录转化为简洁的自然语言摘要。
    2.  **文本嵌入（Embedding）**：使用冻结的文本嵌入模型（如 Qwen3-Embedding）将生成的摘要映射为固定长度的向量 $z_i$。
    3.  **下游预测**：将该向量输入到标准的预测器（如 PatchTSMixer, MLP, LSTM）中，执行预测任务，无需针对特定医院修改架构。
*   **提示词设计**：对比了零样本、思维链（CoT）、趋势关注和基于 ICD 编码的结构化提示词。

### 3. 实验设计
*   **数据集**：使用了三个大型 ICU 队列：MIMIC-IV、HiRID 和 PPICU（外部私有数据集）。
*   **预测任务**：包括多变量预测（未来 24h）、剩余住院时间（LoS）、院内死亡率、未来 24h 药物使用（血管加压素/抗生素）以及实验室检查开立预测。
*   **对比基准（Benchmarks）**：
    *   **网格插补流水线**：均值填充、前向填充（Right-shift）、线性插值。
    *   **自监督表示学习**：TSDE（时间序列扩散嵌入）。
    *   **时间序列基础模型**：TimesFM。
    *   **通用医疗预测框架**：GenHPF 的变体。

### 4. 资源与算力
*   **硬件**：实验主要在 NVIDIA L40S GPU（24GB 显存）上运行。
*   **算力分配**：
    *   单卡 L40S 可完成大部分训练和推理任务。
    *   大规模参考运行使用了 4 × L40S 以提高吞吐量。
    *   LLM 推理使用了 vLLM 框架进行加速。
*   **成本分析**：Gemini-2.0 Flash 的推理成本约为 0.7 美元/1000 个样本，每个患者的推理延迟约为 0.26 秒。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个队列、7 个下游任务（含 2 个隐私探测任务）、6 个迁移方向。
*   **充分性**：作者进行了大量的消融实验，包括不同 LLM 模型对比、提示词敏感性分析、嵌入模型选择、少样本（Few-shot）适应性分析以及隐私风险评估。
*   **客观性**：实验采用了 4 个随机种子取平均值，并与当前最先进的时间序列基础模型（TimesFM）和自监督模型进行了直接对比，评估标准客观。

### 6. 主要结论与发现
*   **分布内性能强劲**：Record2Vec 在 15 个分布内任务中赢得了 13 个，证明了语义摘要能有效捕捉临床关键信息。
*   **跨中心迁移卓越**：在跨医院迁移实验中，Record2Vec 显著优于所有基准方法，性能下降幅度最小，证明了自然语言作为中间表示的鲁棒性。
*   **少样本效率高**：仅需 16 个目标站点的标注样本，Record2Vec 就能接近全量数据训练的性能。
*   **隐私风险未增加**：实验表明，Record2Vec 并没有比原始数据更容易泄露患者的年龄和性别信息。

### 7. 优点
*   **高移植性**：通过将数值数据转化为文本，消除了单位不统一、特征名称差异等工程障碍。
*   **标准化语义**：LLM 能够像医生交班一样，从杂乱的数据中提取出“状态、趋势、显著事件”等高层语义。
*   **架构无关**：生成的固定长度向量可以适配任何下游分类或回归模型。

### 8. 不足与局限
*   **隐私合规性**：虽然实验证明了人口统计学信息未泄露，但将数据发送至云端 API（如 Gemini）在医疗行业仍面临严格的合规性挑战。
*   **计算开销**：相比于简单的数值插补，LLM 的推理成本和延迟较高，可能限制其在实时预测场景中的应用。
*   **理论缺失**：研究主要基于经验实验，缺乏关于 LLM 摘要为何能实现站点不变性（Site-invariance）的严谨理论证明（如因果图分析）。

（完）
