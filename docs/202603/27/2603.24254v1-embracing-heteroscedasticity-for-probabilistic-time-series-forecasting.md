---
title: Embracing Heteroscedasticity for Probabilistic Time Series Forecasting
title_zh: 拥抱异方差性：面向概率时间序列预测
authors: "Yijun Wang, Qiyuan Zhuang, Xiu-Shen Wei"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24254v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 概率时间序列预测与异方差性
tldr: 针对概率时间序列预测中现实数据具有异方差性（随时间变化的方差）而现有非自回归生成模型多基于均方误差（隐含同方差假设）的局限，本文提出LSG-VAE框架。该方法通过位置-尺度似然公式显式参数化预测均值和时变方差，能有效捕捉异方差偶然不确定性，并通过自适应衰减机制提高趋势预测的鲁棒性，在多个基准数据集上表现优于现有模型且效率极高。
selection_source: fresh_fetch
motivation: 现有非自回归生成式预测模型通常隐含同方差假设，无法有效建模现实时间序列中随时间变化的条件方差。
method: 提出LSG-VAE框架，通过位置-尺度高斯似然显式参数化预测均值和时变方差，并引入自适应衰减机制以自动降低高波动观测值的权重。
result: 在九个基准数据集上的实验证明，LSG-VAE在保持高计算效率的同时，性能一致优于15种先进的生成式基准模型。
conclusion: 显式建模异方差性对于提升概率时间序列预测的准确性和鲁棒性至关重要，LSG-VAE为此提供了一个简单且有效的解决方案。
---

## 摘要
概率时间序列预测 (PTSF) 旨在对未来观测值的完整预测分布进行建模，从而实现准确的预测和有原则的不确定性量化。PTSF 的一个核心要求是处理异方差性，因为现实世界的时间序列由于非平稳动力学、状态切换和不断变化的外部条件而表现出随时间变化的条件方差。然而，大多数现有的非自回归生成式 PTSF 方法（如 TimeVAE 和 $K^2$VAE）依赖于基于均方误差 (MSE) 的训练目标，这些目标隐含地施加了同方差假设，从而从根本上限制了它们建模时间异方差性的能力。为了解决这一局限性，我们提出了位置-尺度高斯变分自编码器 (LSG-VAE)，这是一个简单但有效的框架，通过位置-尺度似然公式显式地对预测均值和随时间变化的方差进行参数化。这种设计使 LSG-VAE 能够忠实地捕捉异方差偶然不确定性，并引入了一种自适应衰减机制，在训练过程中自动降低高波动观测值的权重，从而提高了趋势预测的鲁棒性。在九个基准数据集上的广泛实验表明，LSG-VAE 在保持适用于实时部署的高计算效率的同时，始终优于十五个强劲的生成式基准模型。

## Abstract
Probabilistic time series forecasting (PTSF) aims to model the full predictive distribution of future observations, enabling both accurate forecasting and principled uncertainty quantification. A central requirement of PTSF is to embrace heteroscedasticity, as real-world time series exhibit time-varying conditional variances induced by nonstationary dynamics, regime changes, and evolving external conditions. However, most existing non-autoregressive generative approaches to PTSF, such as TimeVAE and $K^2$VAE, rely on MSE-based training objectives that implicitly impose a homoscedastic assumption, thereby fundamentally limiting their ability to model temporal heteroscedasticity. To address this limitation, we propose the Location-Scale Gaussian VAE (LSG-VAE), a simple but effective framework that explicitly parameterizes both the predictive mean and time-dependent variance through a location-scale likelihood formulation. This design enables LSG-VAE to faithfully capture heteroscedastic aleatoric uncertainty and introduces an adaptive attenuation mechanism that automatically down-weights highly volatile observations during training, leading to improved robustness in trend prediction. Extensive experiments on nine benchmark datasets demonstrate that LSG-VAE consistently outperforms fifteen strong generative baselines while maintaining high computational efficiency suitable for real-time deployment.

---

## 论文详细总结（自动生成）

### 论文总结：拥抱异方差性：面向概率时间序列预测 (LSG-VAE)

#### 1. 核心问题与整体含义（研究动机和背景）
概率时间序列预测（PTSF）的核心目标是建模未来的完整概率分布，而非单一数值。现实世界的数据通常具有**异方差性（Heteroscedasticity）**，即条件方差随时间动态变化（如金融市场的波动、极端天气）。
*   **研究动机**：现有的非自回归生成模型（如 TimeVAE, $K^2$VAE）大多依赖于**均方误差（MSE）**作为训练目标。从数学上讲，最小化 MSE 隐含了**同方差假设**（假设方差恒定）。
*   **后果**：这种假设导致模型在面对高波动数据时，无法区分“趋势”和“噪声”，使得均值预测器容易过拟合随机波动，导致泛化性能下降且不确定性估计不准。

#### 2. 方法论：核心思想与技术细节
论文提出了 **LSG-VAE (Location-Scale Gaussian VAE)** 框架，核心思想是显式地将预测分布参数化为位置（均值）和尺度（方差）。
*   **核心架构**：
    *   **分块变分编码（Patch-based Encoding）**：将输入序列切分为 Patch，通过编码器映射到随机潜空间，捕捉局部语义。
    *   **非自回归潜动力学（Non-autoregressive Latent Dynamics）**：通过一个轻量级的全局变换（Flatten + MLP）直接将过去潜状态投影到未来，避免了自回归模型的误差累积和高延迟。
    *   **位置-尺度概率解码器（Location-Scale Decoder）**：采用双头机制，一个头预测均值（Location），另一个头通过 Softplus 激活预测时变标准差（Scale）。
*   **关键机制：自适应衰减（Adaptive Attenuation）**：
    *   训练目标采用**高斯负对数似然（NLL）**。
    *   **数学原理**：均值项的梯度与 $1/\sigma^2$ 成正比。当模型遇到高波动或离群点时，会通过增大 $\sigma$ 来自动降低该样本在损失函数中的权重。这使得均值预测器能专注于底层趋势，而不被瞬时噪声干扰。

#### 3. 实验设计
*   **数据集**：涵盖 9 个真实世界基准数据集，包括能源（ETT 系列、Electricity）、交通（Traffic）、天气（Weather）、经济（Exchange）和医疗（ILI）。
*   **对比方法（15 种）**：
    *   **点预测增强模型**：FITS, PatchTST, iTransformer, Koopa（均外接高斯头）。
    *   **生成式/概率模型**：TSDiff, TimeGrad, CSDI, GRU NVP/MAF, Trans MAF, $K^2$VAE, TimeDiff, DiffusionTS, TMDM, NsDiff。
*   **评估指标**：CRPS（分布拟合质量）、NMAE（点预测精度）、QICE（置信区间校准度）。

#### 4. 资源与算力
*   **硬件环境**：实验在一台拥有 **24GB 显存的 NVIDIA GeForce RTX 4090 GPU** 上完成。
*   **软件框架**：基于 PyTorch 实现。
*   **训练细节**：默认 Batch Size 为 32，采用 Adam 优化器，初始学习率 $10^{-3}$，最大训练 50 轮，并配有早停机制（Patience=10）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模实验，包括 9 个数据集在 4 种不同预测长度（96, 192, 336, 720）下的表现。
*   **消融实验**：验证了预测损失、重构损失、异方差建模以及 RevIN（可逆归一化）的必要性。
*   **泛化性验证**：将 LSG 机制作为插件引入 GAN（TimeGAN）和 $K^2$VAE，证明了该思想的普适性。
*   **客观性**：实验遵循了 ProbTS 和 NsDiff 两种主流评估协议，对比了从经典流模型到最新扩散模型的所有强基准，实验设计严谨、公平。

#### 6. 主要结论与发现
*   **性能领先**：LSG-VAE 在 CRPS 和 NMAE 指标上均显著优于现有 SOTA 模型（如 $K^2$VAE 和 NsDiff）。
*   **鲁棒性强**：通过显式建模方差，模型在点预测精度（NMAE）上甚至超过了专门优化的确定性模型（如 PatchTST），证明了异方差建模对趋势预测的辅助作用。
*   **效率极高**：由于是非自回归单次前向生成，其推理速度比扩散模型快几个数量级，且内存占用极低，适合实时部署。

#### 7. 优点
*   **理论优雅且简单**：通过回归到基础的概率似然理论，解决了复杂生成模型（如扩散模型）在 PTSF 任务中的效率痛点。
*   **自适应性**：自适应衰减机制为模型提供了天然的抗噪能力。
*   **模型无关性**：提出的位置-尺度建模原则可以轻松集成到其他架构中，具有很强的工程实用价值。

#### 8. 不足与局限
*   **分布假设限制**：目前主要基于高斯分布假设。虽然引入了时变方差，但对于具有极度多峰分布或重尾分布的特殊场景，单一高斯分布可能仍显不足。
*   **长程依赖建模**：潜动力学部分采用了相对简单的 MLP 投影，虽然在实验中表现良好，但在处理极长历史窗口或极复杂非线性演化时，可能不如 Transformer 或 Mamba 等结构精细。
*   **应用场景**：主要针对连续型数值预测，未讨论在离散或分类时间序列中的适用性。

（完）
