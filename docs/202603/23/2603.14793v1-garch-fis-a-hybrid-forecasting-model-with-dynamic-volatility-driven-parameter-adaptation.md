---
title: "GARCH-FIS: A Hybrid Forecasting Model with Dynamic Volatility-Driven Parameter Adaptation"
title_zh: GARCH-FIS：一种具有动态波动率驱动参数自适应机制的混合预测模型
authors: "Wen-Jing Li, Da-Qing Zhang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14793v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 金融时间序列的混合预测模型
tldr: 本研究提出GARCH-FIS混合模型，旨在解决金融时间序列多步预测中的非线性动力学和时变波动性问题。该模型通过GARCH模型实时估计波动率，并动态调整模糊推理系统（FIS）的隶属函数参数：在高波动期扩大范围以增强鲁棒性，在平稳期缩小范围以提高精度。实验表明，该方法在多步预测中显著优于LSTM和SVR等基准模型，有效缓解了递归预测中的误差累积。
selection_source: fresh_fetch
motivation: 针对金融市场中非线性特征和时变波动性导致的多步预测误差累积问题，寻求一种更具适应性的预测方案。
method: 结合GARCH模型与模糊推理系统，利用滚动窗口估计的波动率动态调整FIS隶属函数参数，并采用Wang-Mendel法自动构建规则库。
result: 在十种金融资产的多步预测实验中，GARCH-FIS在预测精度和稳定性上均显著优于SVR、LSTM及ARIMA-GARCH模型。
conclusion: 动态波动率驱动的参数调整机制能有效提升模糊系统在复杂市场环境下的预测性能，是金融时间序列建模的重要改进。
---

## 摘要
本文提出了一种名为 GARCH-FIS 的新型混合模型，用于金融时间序列的递归滚动多步预测。该模型将模糊推理系统 (FIS) 与广义自回归条件异方差 (GARCH) 模型相结合，以共同处理非线性动力学和时变波动率。其核心创新在于 FIS 的动态参数自适应机制，该机制在多步预测循环中被专门激活。在此过程中，由滚动窗口 GARCH 模型估计的条件波动率被持续转化为价格波动率度量。在每个预测步骤中，该度量与滑动窗口数据的更新均值（现已包含最新的预测价格）共同决定了下一次预测的 FIS 隶属函数参数。因此，模糊推理的粒度会随着预测时界的延长而自适应：在市场高波动时期，隶属函数会自动变宽以增强鲁棒性；在稳定时期则会变窄以提高精度。这构成了相对于静态单步预测设置的根本性进步。此外，该模型的模糊规则库是利用 Wang-Mendel 方法从数据中自动构建的，从而提升了可解释性和适应性。针对十种不同金融资产的多步预测性能进行的实证评估表明，所提出的 GARCH-FIS 模型在预测准确性和稳定性方面显著优于基准模型（包括支持向量回归 (SVR)、长短期记忆网络 (LSTM) 和 ARIMA-GARCH 计量经济模型），同时有效缓解了长期递归预测中的误差累积。

## Abstract
This paper proposes a novel hybrid model, termed GARCH-FIS, for recursive rolling multi-step forecasting of financial time series. It integrates a Fuzzy Inference System (FIS) with a Generalized Autoregressive Conditional Heteroskedasticity (GARCH) model to jointly address nonlinear dynamics and time-varying volatility. The core innovation is a dynamic parameter adaptation mechanism for the FIS, specifically activated within the multi-step forecasting cycle. In this process, the conditional volatility estimated by a rolling window GARCH model is continuously translated into a price volatility measure. At each forecasting step, this measure, alongside the updated mean of the sliding window data -- which now incorporates the most recent predicted price -- jointly determines the parameters of the FIS membership functions for the next prediction. Consequently, the granularity of the fuzzy inference adapts as the forecast horizon extends: membership functions are automatically widened during high-volatility market regimes to bolster robustness and narrowed during stable periods to enhance precision. This constitutes a fundamental advancement over a static one-step-ahead prediction setup. Furthermore, the model's fuzzy rule base is automatically constructed from data using the Wang-Mendel method, promoting interpretability and adaptability. Empirical evaluation, focused exclusively on multi-step forecasting performance across ten diverse financial assets, demonstrates that the proposed GARCH-FIS model significantly outperforms benchmark models -- including Support Vector Regression(SVR), Long Short-Term Memory networks(LSTM), and an ARIMA-GARCH econometric model -- in terms of predictive accuracy and stability, while effectively mitigating error accumulation in extended recursive forecasts.

---

## 论文详细总结（自动生成）

以下是对论文《GARCH-FIS: A Hybrid Forecasting Model with Dynamic Volatility-Driven Parameter Adaptation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
金融时间序列预测面临两大挑战：**非线性动力学特征**和**时变波动性（波动聚集性）**。
*   **研究动机**：传统的计量经济模型（如 GARCH）虽能捕捉波动性，但在处理非线性结构和极端市场情况时能力有限；而传统的模糊推理系统（FIS）通常采用静态的隶属函数参数，无法根据市场风险状态动态调整，导致在剧烈波动期容易过拟合，在平稳期灵敏度不足。
*   **核心问题**：如何在递归多步预测中，利用实时估计的波动率动态调节模糊系统的推理粒度，以缓解多步预测中的误差累积问题。

### 2. 方法论：核心思想与关键技术
论文提出了 **GARCH-FIS 混合模型**，其核心思想是“波动率驱动的参数自适应”。
*   **核心架构**：结合了 GARCH(1,1) 模型和基于 Wang-Mendel (WM) 方法的模糊推理系统。
*   **关键技术细节**：
    1.  **波动率转化**：利用滚动窗口 GARCH(1,1) 估计条件方差 $\sigma_t$，并将其转化为与价格量纲一致的价格波动率度量 $\hat{\sigma}_t$。
    2.  **动态参数调整**：FIS 的隶属函数（MF）采用三角形分布。其中心点根据滑动窗口的均值（包含最新预测值）确定，而**隶属函数的宽度则由 $\hat{\sigma}_t$ 动态决定**。
    3.  **自适应机制**：在高波动时期，隶属函数自动变宽以增强鲁棒性；在平稳时期，隶属函数变窄以提高预测精度。
    4.  **规则库构建**：采用 Wang-Mendel 方法从历史数据中自动提取“IF-THEN”规则，确保了模型的解释性和数据驱动特性。
    5.  **递归滚动预测**：在多步预测循环中，每一步生成的预测值都会反馈到窗口中，重新估计 GARCH 参数并更新 FIS 的隶属函数参数。

### 3. 实验设计
*   **数据集**：涵盖了 2015 年至 2024 年间 10 种不同类型的金融资产，包括股票指数（沪深300、标普500、纳斯达克）、商品期货（黄金、铜）、外汇（美日、欧美元）、基金及债券指数。
*   **实验场景**：侧重于**递归滚动多步预测**（最高达 100 步）。
*   **基准模型 (Benchmarks)**：
    *   **SVR**（支持向量回归）：代表传统的机器学习方法。
    *   **LSTM**（长短期记忆网络）：代表深度学习方法。
    *   **ARIMA-GARCH**：代表经典的计量经济学混合模型。
*   **评价指标**：MAE（平均绝对误差）、MAPE（平均绝对百分比误差）、$R^2$（确定系数）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提到**具体的 GPU 型号、数量或训练时长。由于 FIS 和 GARCH 模型的计算复杂度相对深度学习模型较低，通常在标准 CPU 或入门级 GPU 上即可完成训练与推理。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个跨度极大的数据集上进行了测试，涵盖了不同的市场环境和资产类别。
*   **充分性评价**：实验设计较为充分。除了常规的误差指标对比，论文还专门绘制了 **100 步预测的误差演变趋势图**，直观展示了模型在抑制误差累积方面的表现。此外，通过可视化隶属函数在不同波动时期的分布，验证了自适应机制的有效性。实验过程遵循了非泄漏原则（仅使用过去数据），具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **精度领先**：GARCH-FIS 在所有 10 种资产的预测中均取得了最低的 MAE 和 MAPE。例如在沪深300指数上，其 MAE 比 LSTM 降低了约 65%。
*   **稳定性强**：在多步预测中，GARCH-FIS 的误差增长曲线远比基准模型平缓，证明了动态参数调整能有效遏制递归预测中的“误差爆炸”。
*   **解释性高**：$R^2$ 结果显示该模型对数据动态具有极强的解释力（多个数据集上超过 0.9），而基准模型在长线预测中常出现负值。

### 7. 优点与亮点
*   **动态粒度调节**：首次在多步预测循环中引入波动率驱动的 FIS 参数实时更新，实现了“市场乱则宽，市场稳则精”的自适应推理。
*   **模型融合度高**：并非简单的串联，而是将 GARCH 的统计特性深度嵌入到模糊系统的隶属函数构造中。
*   **自动规则生成**：利用 WM 方法避免了人工设定模糊规则的主观性，增强了模型的自动化程度。

### 8. 不足与局限
*   **特征单一**：目前模型主要依赖历史价格数据，未考虑宏观经济指标、市场情绪等外部变量。
*   **单变量限制**：该模型目前设计为单资产预测，尚未扩展到多变量 GARCH 或考虑资产间的溢出效应（Spillover effects）。
*   **计算开销**：虽然单次推理快，但在长时界递归预测中，每一步都要重新估计 GARCH 参数，在大规模实时交易系统中可能存在计算压力。

（完）
