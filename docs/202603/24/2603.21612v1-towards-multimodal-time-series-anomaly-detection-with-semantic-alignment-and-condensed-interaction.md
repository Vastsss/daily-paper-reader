---
title: Towards Multimodal Time Series Anomaly Detection with Semantic Alignment and Condensed Interaction
title_zh: 面向语义对齐与压缩交互的多模态时间序列异常检测
authors: "Shiyan Hu, Jianxin Jin, Yang Shu, Peng Chen, Bin Yang, Chenjuan Guo"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21612v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 结合时序-文本语义对齐的多模态时间序列异常检测
tldr: 针对传统时间序列异常检测仅依赖单一数值模态而忽视互补信息的局限，本文提出MindTS模型。该模型通过细粒度时空语义对齐解决异构数据的一致性问题，并利用内容压缩重建机制过滤冗余信息以增强跨模态交互。在六个真实数据集上的实验证明，MindTS在多模态异常检测任务中取得了领先的性能，为复杂动态系统的监控提供了新思路。
selection_source: fresh_fetch
motivation: 传统时间序列异常检测方法忽视了多模态互补信息，且在处理异构数据对齐和冗余信息过滤方面存在挑战。
method: 提出MindTS模型，通过细粒度时空语义对齐模块整合内外文本信息，并利用内容压缩重建机制实现高效的跨模态交互。
result: 在六个真实世界多模态数据集上的广泛实验表明，MindTS的性能优于或等同于现有的先进方法。
conclusion: 通过语义对齐和压缩交互有效地结合多模态信息，可以显著提升时间序列异常检测的准确性和鲁棒性。
---

## 摘要
时间序列异常检测在许多动态系统中起着至关重要的作用。尽管其非常重要，但以往的方法主要依赖于单模态数值数据，忽视了来自其他模态的互补信息的重要性。在本文中，我们提出了一种新型多模态时间序列异常检测模型（MindTS），旨在解决两个关键挑战：（1）如何在异构多模态数据中实现语义一致的对齐；（2）如何过滤冗余的模态信息以有效增强跨模态交互。针对第一个挑战，我们提出了细粒度时间-文本语义对齐（Fine-grained Time-text Semantic Alignment）。它通过跨视图文本融合和多模态对齐机制整合了外源性和内源性文本信息，实现了时间与文本模态之间语义一致的对齐。针对第二个挑战，我们引入了内容压缩器重构（Content Condenser Reconstruction），该方法过滤了对齐文本模态中的冗余信息，并进行跨模态重构以实现交互。在六个真实世界多模态数据集上的广泛实验表明，所提出的 MindTS 与现有方法相比取得了具有竞争力的或更优的结果。代码可在以下地址获取：https://github.com/decisionintelligence/MindTS。

## Abstract
Time series anomaly detection plays a critical role in many dynamic systems. Despite its importance, previous approaches have primarily relied on unimodal numerical data, overlooking the importance of complementary information from other modalities. In this paper, we propose a novel multimodal time series anomaly detection model (MindTS) that focuses on addressing two key challenges: (1) how to achieve semantically consistent alignment across heterogeneous multimodal data, and (2) how to filter out redundant modality information to enhance cross-modal interaction effectively. To address the first challenge, we propose Fine-grained Time-text Semantic Alignment. It integrates exogenous and endogenous text information through cross-view text fusion and a multimodal alignment mechanism, achieving semantically consistent alignment between time and text modalities. For the second challenge, we introduce Content Condenser Reconstruction, which filters redundant information within the aligned text modality and performs cross-modal reconstruction to enable interaction. Extensive experiments on six real-world multimodal datasets demonstrate that the proposed MindTS achieves competitive or superior results compared to existing methods. The code is available at: https://github.com/decisionintelligence/MindTS.

---

## 论文详细总结（自动生成）

### 论文总结：面向语义对齐与压缩交互的多模态时间序列异常检测 (MindTS)

#### 1. 核心问题与整体含义
*   **研究动机**：传统的时间序列异常检测（TSAD）主要依赖单一的数值模态，忽略了文本等其他模态提供的互补背景信息（如金融报告、天气预报描述）。
*   **核心挑战**：
    1.  **语义对齐难**：时间序列（连续信号）与文本（离散符号）属于异构空间，难以实现精确的语义一致性对齐。
    2.  **信息冗余**：文本模态往往包含大量与异常检测无关的冗余信息，直接融合会稀释关键特征，干扰跨模态交互。
*   **研究意义**：提出 **MindTS** 模型，旨在通过精细化的对齐机制和内容压缩技术，有效整合多模态信息以提升异常检测的准确性。

#### 2. 方法论
MindTS 的核心架构包含以下关键技术：
*   **细粒度时间-文本语义对齐**：
    *   **内源文本生成**：利用大语言模型（LLM）根据时序片段（Patch）的统计特征（均值、极值、趋势）生成描述性文本，确保文本与时序特征在局部高度相关。
    *   **外源文本引入**：整合外部背景知识（如新闻、政策报告）。
    *   **跨视图融合**：以内源文本为 Query，外源文本为 Key/Value 进行注意力计算，提取与当前时序最相关的背景信息。
    *   **对比学习对齐**：通过对比损失函数（Contrastive Loss）将时序表示与融合后的文本表示拉近，实现统一空间的语义对齐。
*   **内容压缩器重构 (Content Condenser Reconstruction)**：
    *   **冗余过滤**：基于**信息瓶颈 (Information Bottleneck)** 原理，通过最小化对齐文本与压缩文本之间的互信息（使用 KL 散度作为上界近似），过滤掉文本中的冗余信息。
    *   **跨模态重构**：使用压缩后的文本特征来重构被掩码（Masked）的时序数据。这种“以文补数”的机制强迫模型捕捉深层的跨模态依赖关系。
*   **异常评分**：推理阶段通过计算原始输入与重构输出之间的均方误差（MSE）作为异常得分。

#### 3. 实验设计
*   **数据集**：使用了 6 个真实世界的多模态数据集，涵盖天气 (Weather)、能源 (Energy)、环境 (Environment) 以及金融领域 (KR, EWJ, MDT)。
*   **Benchmark 与对比方法**：
    *   **LLM 相关方法**：LLMMixer, UniTime, GPT4TS。
    *   **预训练/基础模型**：DADA, Timer, UniTS。
    *   **深度学习经典模型**：TimesNet, DCdetector, Anomaly Transformer, PatchTST, iTransformer 等。
    *   **传统方法**：PCA, Isolation Forest, LODA, HBOS。
*   **评价指标**：采用 Affiliated-F1、VUS-PR 和 VUS-ROC 等 16 个综合指标，以确保评估的全面性。

#### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA Tesla-A800-80GB GPU** 上运行。
*   **软件框架**：基于 PyTorch 实现，使用 Adam 优化器。
*   **算力细节**：文中提到了推理时间（如 MDT 数据集约为 0.23s）和内存占用（约 14.6GB），但未明确给出总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 6 个数据集上进行了详尽的性能对比。
    *   **消融实验**：设计了 6 种变体（移除外源文本、移除内源文本、移除对齐模块、移除压缩器等），验证了各组件的必要性。
    *   **鲁棒性测试**：测试了不同 LLM（GPT2, BERT, Llama, DeepSeek）作为编码器的影响，证明模型性能主要源于架构设计而非特定 LLM。
    *   **敏感性分析**：探讨了 Patch 大小、掩码比例和压缩强度 $\mu$ 的影响。
*   **公平性**：所有基线模型均在相同硬件上运行，并使用了统一的评估协议（如 TFB 协议），实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能领先**：MindTS 在所有 6 个数据集上均达到了 SOTA（最优）或极具竞争力的表现，显著优于纯数值模态的方法。
*   **多模态优势**：实验证明，结合内源和外源文本能提供比单一文本来源更丰富的语义。
*   **压缩的重要性**：内容压缩器能有效去除文本噪声，防止模型过拟合于无关的描述性文字。
*   **泛化能力**：MindTS 不仅限于异常检测，在扩展到时序预测任务时也表现出了良好的适应性。

#### 7. 优点
*   **创新的对齐视角**：通过内源文本作为中介，巧妙解决了时序与外源文本之间“语义鸿沟”大的问题。
*   **理论支撑扎实**：引入信息瓶颈理论来处理多模态冗余，比简单的拼接或加权融合更具科学性。
*   **模块化设计**：模型对底层 LLM 不敏感，具有较强的灵活性和可移植性。

#### 8. 不足与局限
*   **文本获取成本**：虽然论文称文本易于获取，但在某些工业或实时监控场景下，高质量的外源文本（如实时事件报告）可能存在延迟或缺失。
*   **计算开销**：引入 LLM 作为文本编码器虽然提升了效果，但相比轻量级单模态模型，其显存占用和计算延迟仍然较高，可能限制其在嵌入式设备上的应用。
*   **数据集偏差**：目前测试的数据集主要集中在天气、能源和金融，对于工业传感器等超高维、高频且缺乏文本描述的场景，其有效性尚待验证。

（完）
