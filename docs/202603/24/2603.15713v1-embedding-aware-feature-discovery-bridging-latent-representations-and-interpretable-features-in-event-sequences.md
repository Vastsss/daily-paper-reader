---
title: "Embedding-Aware Feature Discovery: Bridging Latent Representations and Interpretable Features in Event Sequences"
title_zh: 嵌入感知的特征发现：桥接事件序列中的潜表征与可解释特征
authors: "Artem Sakhno, Ivan Sergeev, Alexey Shestov, Omar Zoloev, Elizaveta Kovtun, Gleb Gusev, Andrey Savchenko, Maksim Makarenko"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15713v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 大语言模型驱动的时间事件序列特征发现
tldr: 针对工业金融系统中表征学习与手工特征之间的脱节问题，本文提出了嵌入感知特征发现（EAFD）框架。该框架将预训练事件序列嵌入与自反思的LLM驱动特征生成代理相结合，通过对齐（解释已有信息）和互补（识别缺失信号）两个维度，从原始序列中迭代发现并优化特征。实验表明，EAFD在多个基准测试中显著优于现有方法，实现了事件序列建模的新最先进性能。
selection_source: fresh_fetch
motivation: 工业系统在依赖手工特征的解释性与利用深度学习嵌入的强大表征能力之间存在持续的脱节。
method: 提出EAFD框架，利用LLM代理根据嵌入的对齐性和互补性准则，从原始事件序列中迭代生成和精炼特征。
result: "在开源和工业交易基准测试中，EAFD相比最先进的预训练嵌入实现了高达5.8%的相对提升。"
conclusion: EAFD成功桥接了潜在表征与可解释特征，为提升工业级事件序列建模的性能和透明度提供了有效途径。
---

## 摘要
工业金融系统运行在时间事件序列上，如交易、用户行为和系统日志。尽管最近的研究强调表征学习和大语言模型，但由于可解释性、有限监督下的鲁棒性以及严格的延迟限制，生产系统仍然严重依赖手工制作的统计特征。这导致了学习到的嵌入与基于特征的流水线之间存在持久的脱节。我们引入了嵌入感知特征发现（EAFD），这是一个统一的框架，通过将预训练的事件序列嵌入与自我反思的 LLM 驱动特征生成智能体相结合，从而弥合了这一差距。EAFD 利用两个互补的标准直接从原始事件序列中迭代地发现、评估和优化特征：对齐性（alignment），用于解释已编码在嵌入中的信息；互补性（complementarity），用于识别嵌入中缺失的预测信号。在开源和工业交易基准测试中，EAFD 的表现始终优于仅使用嵌入和基于特征的基准模型，相比最先进的预训练嵌入实现了高达 +5.8% 的相对提升，在事件序列数据集上达到了新的最先进性能。

## Abstract
Industrial financial systems operate on temporal event sequences such as transactions, user actions, and system logs. While recent research emphasizes representation learning and large language models, production systems continue to rely heavily on handcrafted statistical features due to their interpretability, robustness under limited supervision, and strict latency constraints. This creates a persistent disconnect between learned embeddings and feature-based pipelines. We introduce Embedding-Aware Feature Discovery (EAFD), a unified framework that bridges this gap by coupling pretrained event-sequence embeddings with a self-reflective LLM-driven feature generation agent. EAFD iteratively discovers, evaluates, and refines features directly from raw event sequences using two complementary criteria: \emph{alignment}, which explains information already encoded in embeddings, and \emph{complementarity}, which identifies predictive signals missing from them. Across both open-source and industrial transaction benchmarks, EAFD consistently outperforms embedding-only and feature-based baselines, achieving relative gains of up to $+5.8\%$ over state-of-the-art pretrained embeddings, resulting in new state-of-the-art performance across event-sequence datasets.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **EAFD（Embedding-Aware Feature Discovery，嵌入感知特征发现）** 的框架，旨在解决工业界在处理事件序列（如银行交易、系统日志）时，深度学习表征（嵌入）与传统手工特征之间存在的脱节问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在金融等工业场景中，事件序列建模至关重要。目前存在两类主流方法：一是基于深度学习的表征学习（产生紧凑但难以解释的嵌入）；二是基于领域知识的手工统计特征（可解释、鲁棒且延迟低）。
*   **核心问题**：这两者在生产系统中往往是分离的。研究发现，即使是最先进的嵌入模型也存在“盲点”（即无法捕捉某些关键信息），而传统的自动特征工程（AutoFE）又无法感知嵌入已经包含了哪些信息，导致特征冗余或关键信号缺失。
*   **研究动机**：建立一个桥梁，利用大语言模型（LLM）的推理能力，根据预训练嵌入的状态，迭代地发现既能解释嵌入、又能补充嵌入不足的可解释特征。

### 2. 方法论
EAFD 是一个统一的迭代框架，其核心思想是将预训练嵌入作为“锚点”，通过 LLM 驱动的智能体进行特征探索。
*   **核心流程**：
    1.  **特征生成**：LLM 智能体根据原始序列数据和先前的反馈，生成可执行的 Python 代码来定义新特征。
    2.  **双重评估准则**：
        *   **对齐性（Alignment）**：衡量生成的特征在多大程度上能被现有嵌入解释（通过计算特征与嵌入之间的 $R^2$ 相关性）。这有助于提高嵌入的可解释性。
        *   **互补性（Complementarity）**：衡量新特征是否捕捉到了嵌入中缺失的预测信号（通过对比“仅嵌入”与“嵌入+新特征”在下游任务中的性能增益）。
    3.  **自我反思与修正**：框架包含一个调试模式，如果生成的代码运行失败，LLM 会根据错误信息进行自我修复。
    4.  **反馈循环**：评估结果（得分和特征重要性）反馈给 LLM，指导下一轮迭代。

### 3. 实验设计
*   **数据集**：
    *   **开源基准**：Age Prediction（年龄预测）、Gender Prediction（性别预测）、Rosbank（流失预测）、DataFusion（流失预测）。
    *   **工业私有数据**：大规模多目标银行数据集（包含分类和回归任务）。
*   **对比方法**：
    *   **纯嵌入模型**：CoLES、NTP、LLM4ES。
    *   **传统/自动特征工程**：Featuretools、OpenFE、LightAutoML。
    *   **LLM 特征智能体**：CAAFE、LLMFE。
*   **实验设置**：将 EAFD 发现的特征与原始嵌入拼接，观察在 CatBoost 等下游模型上的性能提升。

### 4. 资源与算力
*   **LLM 模型**：主要使用 `gpt-oss-120b`（1200亿参数规模）。
*   **硬件支持**：使用了 **4 张 NVIDIA A100 GPU** 进行推理。
*   **推理配置**：上下文窗口 100K tokens，最大输出 16K tokens。
*   **迭代次数**：实验通常运行 5 轮迭代。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个公开数据集和 1 个大规模私有工业数据集上进行了测试，涵盖了从几万到数百万用户的规模。
*   **消融实验**：对比了不同 LLM 后端（如 Llama 3.1-8B, 3.3-70B 等）对特征生成质量的影响，以及在不同质量的初始嵌入（强/弱嵌入）上的表现。
*   **充分性评价**：实验设计非常充分，不仅验证了性能提升，还通过“盲点分析”指导了编码器架构的改进，并展示了在隐私保护（特征擦除）中的应用，体现了极高的客观性和实用价值。

### 6. 主要结论与发现
*   **性能突破**：EAFD 在所有基准测试中均优于仅使用嵌入或仅使用特征的方法。相比 SOTA 嵌入，实现了高达 **+5.8%** 的相对提升；在弱嵌入上提升甚至达到 **+19%**。
*   **揭示盲点**：发现 CoLES 嵌入在捕捉时间动态方面较弱，而 NTP 嵌入在处理交易金额和类别分布方面存在缺陷。
*   **闭环优化**：利用 EAFD 的反馈改进编码器（如加入 Time2Vec 或数值编码），使原始嵌入的性能直接提升了 1.2%。
*   **任务自适应**：LLM 能根据不同任务（如年龄 vs 性别）自动调整特征生成的侧重点（如侧重消费类别或侧重交易频率）。

### 7. 优点
*   **桥接能力强**：首次系统性地将 LLM 的符号推理与深度学习的向量表征结合起来，解决了工业界“黑盒”模型的痛点。
*   **实用性高**：生成的特征是 Python 代码，具有高度的可解释性，且易于部署到对延迟敏感的生产环境。
*   **多功能性**：不仅能刷榜（提升性能），还能用于模型诊断、架构改进和隐私合规（选择性擦除敏感属性）。

### 8. 不足与局限
*   **算力成本**：依赖 120B 规模的大模型和 A100 GPU，对于小型企业或实时性要求极高的在线特征发现场景，成本和延迟较高。
*   **LLM 依赖性**：消融实验显示，较小的模型（如 8B）在生成复杂逻辑代码时失败率较高，这意味着框架的效果高度依赖于底层 LLM 的代码生成能力。
*   **领域局限**：目前主要集中在金融交易序列，虽然理论上可扩展到医疗或轨迹数据，但尚未在这些领域进行广泛验证。

（完）
