---
title: "RS-WorldModel: a Unified Model for Remote Sensing Understanding and Future Sense Forecasting"
title_zh: RS-WorldModel：遥感理解与未来场景预测的统一模型
authors: "Linrui Xu, Zhongan Wang, Fei Shen, Gang Xu, Huiping Zhuang, Ming Li, Haifeng Li"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14941v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 文本引导的未来场景预测与时空先验
tldr: 针对遥感领域中变化理解与未来预测任务割裂的问题，本文提出了RS-WorldModel，这是一个统一的遥感世界模型。通过构建包含110万样本的RSWBench-1.1M数据集，并采用地理感知预训练、协同指令微调及可验证强化优化三阶段训练，该模型在仅2B参数量下，在时空变化问答和文本引导的未来场景预测任务上均超越了众多大规模开源模型及闭源模型。
selection_source: fresh_fetch
motivation: 现有的遥感模型通常将时空变化理解与未来场景预测分开处理，限制了任务间的知识迁移与时空先验共享。
method: 提出了包含地理感知预训练、协同指令微调和可验证强化优化的三阶段训练框架，并构建了大规模多模态遥感数据集。
result: 2B参数的模型在时空变化问答上优于大120倍的模型，在未来场景预测的FID指标上也超越了Gemini-2.5-Flash等基准。
conclusion: RS-WorldModel证明了统一理解与预测任务能有效提升遥感世界模型的性能，为该领域的时空建模提供了高效且统一的新方案。
---

## 摘要
遥感世界模型旨在解释观测到的变化并预测合理的未来，这两项任务共享时空先验。然而，现有方法通常将它们分开处理，限制了跨任务迁移。我们提出了 RS-WorldModel，这是一个用于遥感的统一世界模型，能够同时处理时空变化理解和文本引导的未来场景预测。此外，我们构建了 RSWBench-1.1M，这是一个包含 110 万个样本的数据集，具有涵盖这两项任务的丰富语言标注。RS-WorldModel 的训练分为三个阶段：(1) 地理感知生成式预训练 (GAGP)，将预测建立在地理和采集元数据的基础上；(2) 协同指令微调 (SIT)，联合训练理解与预测任务；(3) 可验证强化优化 (VRO)，利用可验证的任务特定奖励来优化输出。RS-WorldModel 仅拥有 2B 参数，在大多数时空变化问答指标上超越了比其大 120 倍的开源模型。在文本引导的未来场景预测中，它实现了 43.13 的 FID，优于所有开源基准模型以及闭源的 Gemini-2.5-Flash Image (Nano Banana)。

## Abstract
Remote sensing world models aim to both explain observed changes and forecast plausible futures, two tasks that share spatiotemporal priors. Existing methods, however, typically address them separately, limiting cross-task transfer. We present RS-WorldModel, a unified world model for remote sensing that jointly handles spatiotemporal change understanding and text-guided future scene forecasting, and we build RSWBench-1.1M, a 1.1 million sample dataset with rich language annotations covering both tasks. RS-WorldModel is trained in three stages: (1) Geo-Aware Generative Pre-training (GAGP) conditions forecasting on geographic and acquisition metadata; (2) synergistic instruction tuning (SIT) jointly trains understanding and forecasting; (3) verifiable reinforcement optimization (VRO) refines outputs with verifiable, task-specific rewards. With only 2B parameters, RS-WorldModel surpasses open-source models up to 120$ \times $ larger on most spatiotemporal change question-answering metrics. It achieves an FID of 43.13 on text-guided future scene forecasting, outperforming all open-source baselines as well as the closed-source Gemini-2.5-Flash Image (Nano Banana).

---

## 论文详细总结（自动生成）

### RS-WorldModel：遥感理解与未来场景预测的统一模型论文总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：遥感（RS）领域的世界模型旨在同时解释已观测的变化并预测合理的未来状态。然而，现有的遥感模型通常将“时空变化理解”与“未来场景预测”视为独立任务，导致两者共享的时空先验（如地理位置、季节性、传感器特性）无法有效互补。
*   **核心问题**：如何构建一个统一的遥感世界模型，使其既能通过语言描述复杂的时空变化，又能根据文本指令生成物理一致的未来卫星图像。

#### 2. 论文提出的方法论
论文提出了 **RS-WorldModel** 框架，基于 Qwen3-VL-2B 构建，采用统一的自回归序列生成范式处理文本和视觉标记（Tokens）。
*   **核心思想**：将理解与预测任务统一在同一个潜在空间中，通过共享的地理元数据（Metadata）约束，实现感知与模拟的闭环。
*   **关键技术细节（三阶段训练）**：
    1.  **地理感知生成式预训练 (GAGP)**：仅使用图像序列和地理元数据（坐标、太阳高度角、云量等），训练模型在无文本引导下预测未来视觉标记，建立初步的时空物理先验。
    2.  **协同指令微调 (SIT)**：在混合数据集上联合训练理解（输出文本）和预测（输出视觉标记）任务，使生成任务的空间知识迁移至理解任务，提升指令遵循能力。
    3.  **可验证强化优化 (VRO)**：利用 **GRPO**（群体相对策略优化）算法进行强化学习。
        *   **预测任务奖励**：计算生成图像与文本指令的余弦相似度，以及与原始图像的空间一致性。
        *   **理解任务奖励**：引入“LLM-as-a-Judge”，根据地理元数据和参考答案对模型生成的描述进行评分，惩罚违反物理常识的幻觉。

#### 3. 实验设计
*   **数据集**：构建了 **RSWBench-1.1M**，包含 110 万个样本，涵盖时空变化问答（ST-CQA）和文本引导未来场景预测（TFSF）。
*   **Benchmark**：
    *   **理解任务**：在 5K 测试集上评估，指标包括 GPT-Score、BLEU-1、ROUGE-L 及多种语义相似度指标。
    *   **预测任务**：在 1.6K 测试集上评估，指标包括 FID（感知质量）、CosSim（文本一致性）及 GPT 评分。
*   **对比方法**：
    *   **闭源模型**：GPT-5.1、Gemini-3-Flash、Gemini-2.5-Flash Image。
    *   **开源通用 VLM**：Qwen-VL 系列（最高 235B）、InternVL3.5、LLaVA-OV。
    *   **遥感专用模型**：EarthDial-RGB、TEOChat、CRS-Diff、FLUX.1-Kontext 等。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A800 (80GB) GPU**。
*   **软件框架**：采用 DeepSpeed ZeRO-3 和 Flash Attention 2 进行加速。
*   **训练细节**：第一阶段和第二阶段各训练 32 个 Epoch，第三阶段（VRO）在 16K 样本上进行微调。图像分辨率上限设为 524,288 像素。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 110 万量级的数据集上进行训练，并在数千个样本的基准上进行了详尽测试。
*   **消融实验**：针对强化学习中的奖励权重 $\lambda$、三个训练阶段的必要性、以及地理元数据在预训练中的作用分别做了消融实验。
*   **充分性评价**：实验设计非常充分，对比对象涵盖了从 2B 到 235B 不同量级的模型，且包含了最新的闭源商业模型，对比维度客观且全面。

#### 6. 论文的主要结论与发现
*   **以小博大**：仅有 2B 参数的 RS-WorldModel 在时空变化问答指标上超越了参数量大其 120 倍（235B）的开源模型。
*   **预测性能卓越**：在未来场景预测任务中，其 FID 指标（43.13）不仅优于所有开源基准，还超越了闭源的 Gemini-2.5-Flash。
*   **统一建模的优势**：消融实验证明，加入生成式预训练（GAGP）能显著提升理解任务的准确性，证明了预测能力有助于模型内化环境动力学。

#### 7. 优点
*   **任务统一性**：首次在遥感领域实现了理解与生成的深度融合，而非简单的多任务堆叠。
*   **元数据驱动**：巧妙地将数值型地理元数据转化为自然语言线索，增强了模型对物理环境（如阴影长度随太阳高度角变化）的建模能力。
*   **可验证奖励设计**：通过 LLM 裁判和物理约束奖励解决了遥感任务中难以通过传统指标（如 BLEU）衡量物理真实性的问题。

#### 8. 不足与局限
*   **基础模型限制**：虽然 2B 模型效率极高，但在处理极度复杂的长程推理或多步骤地理分析时，可能仍受限于基础语言模型的容量。
*   **离散化损失**：使用 MoVQGAN 将图像离散化为 Token 可能会导致生成图像在极细微纹理上不如纯扩散模型（Diffusion Models）细腻。
*   **应用限制**：目前主要基于光学遥感数据（fMoW），对于合成孔径雷达（SAR）或高光谱等其他模态遥感数据的泛化能力尚待验证。

（完）
