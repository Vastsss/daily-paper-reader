---
title: "FISformer: Replacing Self-Attention with a Fuzzy Inference System in Transformer Models for Time Series Forecasting"
title_zh: FISformer：在用于时间序列预测的 Transformer 模型中用模糊推理系统替换自注意力机制
authors: "Bulent Haznedar, Levent Karacan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21724v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 用于时间序列预测的Transformer模型
tldr: 针对传统Transformer在时间序列预测中难以建模不确定性和非线性依赖的问题，本文提出FISformer。该模型通过模糊推理系统（FIS）取代传统的点积注意力机制，利用可学习的隶属度函数和规则推理来估计Token间的关系强度。FISformer结合了模糊逻辑的解释性与Transformer的表征能力，在多个基准数据集上展现了卓越的预测精度、鲁棒性和可解释性。
selection_source: fresh_fetch
motivation: 传统Transformer依赖的确定性点积注意力机制在处理多变量时间序列的不确定性和非线性依赖方面存在局限。
method: 提出FIS交互机制，利用模糊推理过程和可学习的隶属度函数替代自注意力来计算Token间的交互权重。
result: 在多个基准数据集上的实验表明，FISformer在预测精度、抗噪能力和模型可解释性方面均优于现有的Transformer变体。
conclusion: 模糊推理系统是传统注意力机制的有效替代方案，能够显著提升时间序列预测模型的性能和可靠性。
---

## 摘要
Transformer 在时间序列预测方面取得了显著进展，但其对确定性点积注意力的依赖限制了其在多变量时间维度上建模不确定性和非线性依赖关系的能力。为了解决这一局限性，我们提出了 FISFormer，这是一种由模糊推理系统驱动的 Transformer，它用 FIS 交互机制取代了传统的注意力机制。在该框架中，每个查询-键（query-key）对在每个特征维度上都会经历一个模糊推理过程，其中可学习的隶属函数和基于规则的推理用于估计令牌（token）间的关系强度。这些由 FIS 导出的交互权重捕捉了不确定性，并提供了令牌之间可解释的连续映射。沿令牌轴应用 softmax 操作以归一化这些权重，然后通过逐元素乘法将其与相应的数值（value）特征结合，从而产生最终的上下文增强令牌表示。这种设计将模糊逻辑的可解释性和不确定性建模能力与 Transformer 的表征能力融合在一起。在多个基准数据集上的广泛实验表明，与最先进的 Transformer 变体相比，FISFormer 实现了更优的预测精度、噪声鲁棒性和可解释性，证明了模糊推理是传统注意力机制的有效替代方案。

## Abstract
Transformers have achieved remarkable progress in time series forecasting, yet their reliance on deterministic dot-product attention limits their capacity to model uncertainty and nonlinear dependencies across multivariate temporal dimensions. To address this limitation, we propose FISFormer, a Fuzzy Inference System-driven Transformer that replaces conventional attention with a FIS Interaction mechanism. In this framework, each query-key pair undergoes a fuzzy inference process for every feature dimension, where learnable membership functions and rule-based reasoning estimate token-wise relational strengths. These FIS-derived interaction weights capture uncertainty and provide interpretable, continuous mappings between tokens. A softmax operation is applied along the token axis to normalize these weights, which are then combined with the corresponding value features through element-wise multiplication to yield the final context-enhanced token representations. This design fuses the interpretability and uncertainty modeling of fuzzy logic with the representational power of Transformers. Extensive experiments on multiple benchmark datasets demonstrate that FISFormer achieves superior forecasting accuracy, noise robustness, and interpretability compared to state-of-the-art Transformer variants, establishing fuzzy inference as an effective alternative to conventional attention mechanisms.

---

## 论文详细总结（自动生成）

### 论文总结：FISformer —— 基于模糊推理系统的时间序列预测 Transformer 模型

#### 1. 核心问题与整体含义（研究动机和背景）
传统 Transformer 模型在多变量时间序列预测中主要依赖**确定性的点积自注意力机制（Dot-product Self-attention）**。然而，这种机制存在两个主要局限：
*   **难以建模不确定性**：现实世界的时间序列数据通常包含大量噪声和动态波动，点积计算出的“硬性”相似度难以捕捉其中的模糊性和不确定性。
*   **非线性表达受限**：标准注意力机制依赖线性投影后的点积，对于复杂的非线性时空依赖关系建模能力有限。
*   **计算复杂度**：标准注意力的复杂度随序列长度呈平方级增长。

**研究动机**：引入模糊逻辑（Fuzzy Logic）的推理能力，利用模糊推理系统（FIS）取代传统的注意力机制，以增强模型对不确定性的处理能力、提高预测精度并增强模型的可解释性。

#### 2. 方法论：核心思想与关键技术
FISformer 的核心是将 **Sugeno 型模糊推理系统** 嵌入到 Transformer 的 Token 交互层中。其核心流程分为四个阶段：

1.  **模糊化（Fuzzification）**：
    *   将 Query (Q) 和 Key (K) 的特征值通过**可学习的隶属度函数**（如高斯函数）映射为模糊隶属度。
    *   每个特征维度使用多个模糊集合（如 3 个），参数（均值和标准差）随网络端到端训练。
2.  **规则生成（Rule Generation）**：
    *   采用乘积型 T-范数（Product-based t-norm）计算规则的激活强度（Firing Strength），即 Q 和 K 隶属度的乘积。
    *   这模拟了“如果 Q 是 A 且 K 是 B”的逻辑推理过程。
3.  **解模糊化与聚合（Defuzzification & Aggregation）**：
    *   基于一阶 Sugeno 模型，每个规则产生一个依赖于输入的输出（后件）。
    *   通过对所有规则的激活强度进行归一化，并与规则输出进行加权求和，得到最终的**模糊交互图（Fuzzy Interaction Map）**。
4.  **FIS 交互输出（FIS Interaction）**：
    *   对模糊交互图沿 Token 轴进行 Softmax 归一化，生成交互权重。
    *   将权重与 Value (V) 向量进行逐元素相乘，生成增强后的上下文表示。

#### 3. 实验设计
*   **数据集**：使用了 7 个主流公开基准数据集，涵盖电力（ETT, ECL）、金融（Exchange）、气象（Weather）、交通（Traffic, PEMS）和能源（Solar-Energy）领域。
*   **预测设置**：固定回顾窗口（如 96），预测未来多个长度（96, 192, 336, 720；PEMS 为 12, 24, 48, 96）。
*   **对比方法（Benchmark）**：
    *   经典 Transformer 变体：Informer, Autoformer, FEDformer, Crossformer。
    *   最先进模型：iTransformer（本文的基础架构）。
    *   模糊增强模型：FANTF（2025 年最新的模糊注意力模型）。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 2080 Ti GPU (12GB)**。
*   **软件/框架**：PyTorch。
*   **训练细节**：Adam 优化器，初始学习率在 $\{10^{-4}, 10^{-3}\}$ 间搜索，Batch Size 为 32，训练 10 个 Epoch。
*   **复杂度分析**：文中指出 FIS 交互机制在规则数量 $R$ 远小于序列长度 $T$ 时，计算复杂度为 $O(T \cdot d \cdot R)$，相对于标准注意力的 $O(T^2 \cdot d)$ 实现了**线性复杂度**。

#### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集、多个预测步长下进行了详尽测试，总计产出了数百个实验结果点。
*   **消融实验**：对比了“自注意力”与“FIS 交互”的直接效果，并验证了将 FIS 模块集成到其他架构（如 Informer）中的泛化能力。
*   **敏感性分析**：研究了不同隶属度函数（高斯、三角、梯形）以及学习率对模型性能的影响。
*   **评价**：实验设计全面，涵盖了精度对比、鲁棒性测试和效率分析，对比基准包含了 2024-2025 年的最新成果，具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先**：FISformer 在绝大多数数据集（6/7）上均取得了最优（SOTA）的 MSE 和 MAE 指标。
*   **超越部分模糊模型**：相比于仅在注意力权重上做模糊化处理的 FANTF，FISformer 这种全流程模糊推理的设计表现更优。
*   **鲁棒性强**：模糊推理机制使模型在处理噪声数据和动态变化的序列时比确定性点积更稳定。
*   **可解释性**：通过模糊规则的激活程度，可以直观地理解 Token 之间的交互逻辑。

#### 7. 优点（亮点）
*   **理论融合**：成功将经典的模糊推理系统（FIS）与现代 Transformer 架构深度融合，而非简单的后处理。
*   **线性效率**：在保持强大表达能力的同时，将 Token 交互的复杂度降至线性，适合长序列预测。
*   **即插即用**：FIS 交互模块具有良好的通用性，可以替换任何 Transformer 变体中的注意力层。
*   **不确定性建模**：通过模糊集合有效缓解了时间序列中的随机噪声干扰。

#### 8. 不足与局限
*   **超参数敏感性**：模糊规则的数量和隶属度函数的选择可能需要针对不同领域进行精细调优。
*   **计算常数项**：虽然渐进复杂度是线性的，但模糊化和规则推理过程涉及大量的指数运算（高斯函数），在实际部署时，小规模序列下的常数级开销可能高于简单点积。
*   **应用范围**：目前仅在时间序列预测任务上验证，尚未在自然语言处理（NLP）或计算机视觉（CV）等更广泛的领域测试其通用性。

（完）
