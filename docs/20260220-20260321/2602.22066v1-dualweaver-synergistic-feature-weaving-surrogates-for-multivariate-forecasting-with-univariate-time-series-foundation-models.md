---
title: "DualWeaver: Synergistic Feature Weaving Surrogates for Multivariate Forecasting with Univariate Time Series Foundation Models"
title_zh: DualWeaver：基于单变量时间序列基础模型的协同特征编织代理多变量预测框架
authors: "Jinpeng Li, Zhongyi Pei, Huaze Xue, Bojian Zheng, Chen Wang, Jianmin Wang"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.22066v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 用于多变量预测的单变量时间序列基础模型
tldr: 针对单变量时间序列基础模型（Uni-TSFMs）难以有效扩展到多变量预测的问题，本文提出DualWeaver框架。该框架通过一对可学习且结构对称的代理序列，利用辅助特征融合模块捕捉变量间依赖关系，并将其映射为基础模型兼容的序列。其对称结构支持无参数重构预测结果，并引入正则化项增强鲁棒性。实验证明DualWeaver在准确性和稳定性上均优于现有最先进的多变量预测模型。
selection_source: fresh_fetch
motivation: 旨在解决如何将预训练的单变量时间序列基础模型有效扩展到多变量预测，并充分捕捉变量间的复杂依赖关系。
method: 提出DualWeaver框架，通过对称代理序列和特征融合模块将多变量信息织入单变量模型，实现无参数的预测重构。
result: 在多个真实世界数据集上的实验表明，DualWeaver在预测精度和稳定性方面均显著优于当前最先进的多变量预测方法。
conclusion: DualWeaver为利用单变量基础模型进行高效、稳健的多变量时间序列预测提供了一种创新的协同特征编织方案。
---

## 摘要
时间序列基础模型（TSFMs）通过大规模预训练在单变量预测方面取得了显著成功，但如何有效地将这一成功扩展到多变量预测仍然具有挑战性。为此，我们提出了 DualWeaver，这是一个新颖的框架，通过使用一对可学习的、结构对称的代理序列，使单变量 TSFMs（Uni-TSFMs）能够适应多变量预测。这些代理序列由一个捕获跨变量依赖关系的共享辅助特征融合模块生成，并通过预测目标映射为与 TSFM 兼容的序列。对称结构使得能够直接从代理序列中进行最终预测的无参数重构，而无需额外的参数化解码。此外，本文还引入了一个具有理论依据的正则化项，以增强针对适应性崩溃（adaptation collapse）的鲁棒性。在多种真实世界数据集上的广泛实验表明，DualWeaver 在准确性和稳定性方面均优于最先进的多变量预测模型。我们在 https://github.com/li-jinpeng/DualWeaver 发布了代码。

## Abstract
Time-series foundation models (TSFMs) have achieved strong univariate forecasting through large-scale pre-training, yet effectively extending this success to multivariate forecasting remains challenging. To address this, we propose DualWeaver, a novel framework that adapts univariate TSFMs (Uni-TSFMs) for multivariate forecasting by using a pair of learnable, structurally symmetric surrogate series. Generated by a shared auxiliary feature-fusion module that captures cross-variable dependencies, these surrogates are mapped to TSFM-compatible series via the forecasting objective. The symmetric structure enables parameter-free reconstruction of final predictions directly from the surrogates, without additional parametric decoding. A theoretically grounded regularization term is further introduced to enhance robustness against adaptation collapse. Extensive experiments on diverse real-world datasets show that DualWeaver outperforms state-of-the-art multivariate forecasters in both accuracy and stability. We release the code at https://github.com/li-jinpeng/DualWeaver.

---

## 论文详细总结（自动生成）

以下是对论文《DualWeaver: Synergistic Feature Weaving Surrogates for Multivariate Forecasting with Univariate Time Series Foundation Models》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的单变量时间序列基础模型（Uni-TSFMs）虽然在捕捉时间动态方面表现卓越，但在处理多变量预测时，由于其架构限制，往往忽略了变量间的交叉依赖关系（Cross-variable dependencies）。
*   **研究背景**：
    *   多变量预测中，不同变量间通常存在强相关性，建模这些相关性理论上能提升预测精度。
    *   现有的适配方法（如 AdaPTS）多采用“编码器-解码器”结构，但这会导致模型的大部分容量被消耗在从潜在变量重构原始预测上，而非学习预测友好的跨变量依赖。
    *   原生多变量基础模型（如 Chronos-2）虽然能处理变量间关系，但计算复杂度随变量数呈平方增长（$O(V^2)$），难以扩展。

### 2. 方法论
DualWeaver 提出了一种基于**双代理序列（Dual Surrogates）**的适配框架，核心思想是将多变量依赖“织入”代理序列中，利用冻结的 Uni-TSFM 进行预测。
*   **核心思想**：通过一个共享的特征融合模块生成一对对称的代理序列，利用 Uni-TSFM 对其进行预测，最后通过无参数的代数运算重构出最终结果。
*   **关键技术细节**：
    1.  **特征融合模块 $f(\cdot)$**：一个轻量级的模块（如 MLP 或 CNN），用于捕捉跨变量依赖，将原始输入 $X$ 映射到融合特征空间。
    2.  **双代理构建**：
        *   正向代理：$S_\alpha = f(X) + w_\alpha \odot X$
        *   负向代理：$S_\beta = f(X) - w_\beta \odot X$
        其中 $w_\alpha, w_\beta$ 是可学习的通道权重。
    3.  **无参数重构**：最终预测 $\hat{Y}_{our}$ 通过两个代理预测值的差值计算得出：$\hat{Y}_{our} = \frac{1}{w_\alpha + w_\beta} \odot (\hat{S}_\alpha - \hat{S}_\beta)$。这种设计消除了对参数化解码器的依赖，使模型专注于学习变量间关系。
    4.  **理论误差界正则化**：引入了一个基于理论推导的正则化项 $\Omega$，用于约束代理序列的 MSE，防止训练过程中的“适应性崩溃”（即误差指数级增长）。

### 3. 实验设计
*   **数据集**：
    *   核心基准：ETT（ETTh1, ETTh2, ETTm1, ETTm2）和 Weather。
    *   扩展数据集：Electricity (ECL, 321个变量)、Exchange、Solar-Energy 以及真实的工业数据集 IEAC。
*   **基础模型（Backbones）**：使用了 Sundial (128M) 和 TimerXL。
*   **对比方法（Benchmarks）**：
    *   **基础模型变体**：Vanilla（零样本）、SFT（全参数微调）。
    *   **适配器方法**：AdaPTS（SOTA 编码器-解码器框架）。
    *   **原生多变量预测器**：TimePro, SimpleTM, TimeMixer, iTransformer。
    *   **原生多变量基础模型**：Chronos-2。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H20 GPU (96GB)**。
*   **训练细节**：采用 PyTorch 实现，使用分布式数据并行（DDP）。训练 10 个 epoch，采用早停机制（patience=3）。
*   **效率表现**：实验表明 DualWeaver 的训练时间和显存占用随隐藏层维度增加保持平稳，而对比方法 AdaPTS 则呈线性增长。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的基准测试，涵盖了 8 个不同领域的数据集。
*   **消融与鲁棒性实验**：
    *   **噪声鲁棒性**：在数据中注入无关噪声，验证模型在弱相关场景下的稳定性。
    *   **维度扩展性**：在 ECL 数据集上测试了从 5 到 321 个变量的性能变化。
    *   **训练稳定性**：对比了有无正则化项时的验证误差曲线。
*   **评价**：实验设计非常充分且客观。通过跨时间-跨变量相关系数分析，解释了为何在某些数据集（如 Weather）上单变量模型反而优于多变量模型，增强了说服力。

### 6. 主要结论与发现
*   **性能领先**：DualWeaver 在所有评估数据集上均达到了 SOTA 性能。相比 TimerXL 原型，平均 MSE 降低了 8.95%；相比全微调（SFT），MSE 降低了 3.91%。
*   **高效扩展**：在处理高维数据（如 321 个变量）时，DualWeaver 的性能增益远比 AdaPTS 稳定，证明了无参数重构的优势。
*   **抗噪性强**：在引入无关噪声维度时，DualWeaver 的预测误差保持平稳，而 Chronos-2 和 AdaPTS 的误差显著上升。
*   **预测空间优化**：实验证明，代理空间中的信号比原始空间更易于被 Uni-TSFM 预测（MSE 降低了 85.44%）。

### 7. 优点
*   **架构优雅**：通过对称代理序列巧妙地抵消了辅助模块的负面影响，实现了无参数解码。
*   **理论支撑**：不仅有实验结果，还提供了误差界的理论分析，并据此设计了有效的正则化项。
*   **灵活性高**：特征融合模块 $f(\cdot)$ 是可插拔的，支持 MLP、CNN、GNN 等多种实现。
*   **计算友好**：保持了单变量模型线性复杂度 $O(V)$ 的优势，同时获得了多变量建模的能力。

### 8. 不足与局限
*   **推理开销**：虽然计算复杂度是线性的，但由于需要分别预测正向和负向两个代理序列，其推理时的前向传播次数是原始单变量模型的 **2 倍**。
*   **依赖 Backbone**：其性能上限仍受限于所使用的 Uni-TSFM 的质量。
*   **超参数敏感性**：虽然论文建议 $\lambda=1$，但在极端异构的数据集上，可能仍需要对特征融合模块的结构进行微调。

（完）
