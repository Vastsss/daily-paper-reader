---
title: "UTICA: Multi-Objective Self-Distllation Foundation Model Pretraining for Time Series Classification"
title_zh: UTICA：用于时间序列分类的多目标自蒸馏基础模型预训练
authors: "Yessin Moakher, Youssef Attia El Hili, Vasilii Feofanov"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01348v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 时间序列自蒸馏基础模型
tldr: 本研究针对时间序列领域非对比学习预训练方法探索不足的问题，提出了UTICA基础模型。该模型借鉴DINOv2的自蒸馏机制，结合Mantis分词器与Transformer架构，通过学生-教师框架同时学习增强裁剪的时间不变性和掩码补丁的局部结构特征。实验证明UTICA在UCR和UEA基准测试中达到了SOTA性能，为时间序列基础模型提供了有效的非对比预训练策略。
selection_source: fresh_fetch
motivation: 旨在探索在计算机视觉领域取得巨大成功的非对比自蒸馏方法在时间序列基础模型预训练中的潜力。
method: 采用基于DINOv2的学生-教师自蒸馏框架，结合数据增强裁剪和补丁掩码技术来捕捉时间不变性与局部细节。
result: 在UCR和UEA两个主流时间序列分类基准数据集上均取得了当前最先进的分类性能。
conclusion: 非对比学习方法是时间序列基础模型预训练中一种极具前景且与现有方法互补的策略。
---

## 摘要
自监督基础模型在包括时间序列在内的多个领域取得了显著成功。然而，非对比方法（一种推动了计算机视觉领域重大进展的范式）在时间序列中的潜力仍未得到充分探索。在这项工作中，我们借鉴 DINOv2 风格的自蒸馏方法来预训练时间序列基础模型，并以 Mantis 分词器和 Transformer 编码器架构作为骨干网络。通过学生-教师框架，我们的 Utica 方法学习到的表示既能通过增强裁剪捕捉时间不变性，又能通过补丁掩码捕捉细粒度的局部结构。我们的方法在 UCR 和 UEA 基准测试中均达到了最先进的分类性能。这些结果表明，非对比方法是时间序列基础模型中一种极具前景且互补的预训练策略。

## Abstract
Self-supervised foundation models have achieved remarkable success across domains, including time series. However, the potential of non-contrastive methods, a paradigm that has driven significant advances in computer vision, remains underexplored for time series. In this work, we adapt DINOv2-style self-distillation to pretrain a time series foundation model, building on the Mantis tokenizer and transformer encoder architecture as our backbone. Through a student-teacher framework, our method Utica learns representations that capture both temporal invariance via augmented crops and fine-grained local structure via patch masking. Our approach achieves state-of-the-art classification performance on both UCR and UEA benchmarks. These results suggest that non-contrastive methods are a promising and complementary pretraining strategy for time series foundation models.

---

## 论文详细总结（自动生成）

以下是对论文《UTICA: Multi-Objective Self-Distillation Foundation Model Pretraining for Time Series Classification》的深度结构化总结：

### 1. 研究动机与核心问题
*   **核心问题**：当前时间序列基础模型（TSFM）多侧重于预测任务，采用自回归或掩码重建目标，这往往只关注局部一致性而忽略了对分类至关重要的全局语义结构。
*   **研究动机**：
    *   **对比学习的局限性**：现有分类模型（如 Mantis）多依赖对比学习，但该方法假设批次内不同样本语义不同，这在时间序列中容易引入“伪负例”（语义相似的样本被强行推开），损害表示质量。
    *   **非对比学习的潜力**：在计算机视觉中大获成功的非对比自蒸馏方法（如 DINOv2）在时间序列领域尚未得到充分探索。
    *   **目标**：提出一种无需负例、结合多尺度裁剪和掩码建模的自蒸馏预训练框架，以提升时间序列分类性能。

### 2. 方法论
UTICA（Unlabeled Time-series Crop Augmented）采用**学生-教师（Student-Teacher）**框架，核心思想是通过多目标学习同时捕捉全局不变性和局部细节。
*   **骨干架构**：使用 Transformer 编码器，配合 Mantis 分词器。输入包含三种变换：实例归一化序列、一阶差分、补丁级的均值和标准差编码。
*   **预训练目标（Loss 函数）**：
    1.  **DINO Loss**：通过多裁剪（Multi-crop）策略，生成 2 个全局视图（覆盖 40%-100%）和 8 个局部视图（覆盖 10%-40%）。学生网络处理所有视图，教师网络仅处理全局视图，通过交叉熵迫使学生预测教师的输出分布，学习时间尺度不变性。
    2.  **iBOT Loss**：在全局视图上进行补丁级掩码。学生网络预测被掩码补丁的标记分布，教师网络观察完整信号，以此学习细粒度的局部结构。
    3.  **KoLeo 正则项**：应用微分熵估计器，鼓励特征在批次内均匀分布，防止特征崩溃（Collapse）。
*   **预训练数据**：完全在**合成数据**上预训练。利用因果有向无环图（DAG）和高斯过程（GP）生成具有复杂动力学特征的合成序列。
*   **更新机制**：教师网络的权重通过学生网络权重的指数移动平均（EMA）进行更新。

### 3. 实验设计
*   **数据集**：
    *   **UCR 归档**：包含 128 个单变量时间序列数据集。
    *   **UEA 归档**：包含 21 个多变量时间序列数据集。
*   **评估模式**：
    *   **线性探测（Linear Probing）**：冻结骨干网络，仅训练线性分类头。
    *   **全参数微调（Fine-tuning）**：端到端微调整个模型。
*   **对比基准（Baselines）**：
    *   **Mantis (8M)**：基于对比学习的轻量级模型。
    *   **MOMENT (385M)**：基于 T5 架构的大型掩码自编码器。
    *   **NuTime (2M)**：基于自蒸馏和数值多尺度嵌入的模型。
    *   **GPT4TS (80M)**：基于预训练 GPT-2 的微调模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **间接信息**：附录中提到在处理 UEA 数据集时，排除了内存需求超过 **24 GB GPU 显存**的数据集，暗示实验可能是在单张或多张 24GB 显存级别的显卡（如 RTX 3090/4090 或 A10）上完成的。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 149 个主流数据集（128 UCR + 21 UEA），实验规模非常庞大。
*   **消融实验**：专门针对 DINO、iBOT 和 KoLeo 组件进行了消融分析，并与 data2vec 等其他自蒸馏方法进行了对比。
*   **客观性**：每个实验均在 3 个随机种子下取平均值并报告标准差，遵循了 Mantis 的标准评估协议，对比过程较为公平、客观。

### 6. 主要结论与发现
*   **SOTA 性能**：UTICA 在 UCR 和 UEA 的线性探测和微调模式下均刷新了记录。在 UCR 线性探测中，其平均准确率（0.794）和获胜次数（52/128）均优于 Mantis 和 MOMENT。
*   **互补性**：消融实验证明，局部掩码预测（iBOT）和全局多裁剪对齐（DINO）提供了互补的监督信号，两者结合显著优于单一目标。
*   **合成数据有效性**：再次验证了时间序列基础模型可以仅通过高质量合成数据预训练，并在真实下游任务中表现出色。

### 7. 优点
*   **无需负例**：有效规避了对比学习中“伪负例”导致的表示退化问题。
*   **多尺度特征**：通过多裁剪策略，模型能够同时理解时间序列的宏观趋势和微观波动。
*   **轻量且高效**：尽管参数量远小于 MOMENT，但在分类任务上的表现却更优，证明了预训练目标的质量比单纯堆砌参数更重要。

### 8. 不足与局限
*   **骨干网络规模**：目前仅在 8M 参数规模上验证，尚未探讨该方法在更大规模参数（Scaling up）下的表现。
*   **合成数据偏差**：虽然合成数据预训练效果好，但对于某些具有极端非平稳性或特定领域噪声的真实数据，合成分布可能仍存在覆盖不足的风险。
*   **硬件限制**：由于显存限制，部分超大规模的多变量数据集（UEA 中的部分任务）未能包含在测试中。

（完）
