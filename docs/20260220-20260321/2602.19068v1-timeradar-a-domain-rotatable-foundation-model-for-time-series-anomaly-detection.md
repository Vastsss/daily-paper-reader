---
title: "TimeRadar: A Domain-Rotatable Foundation Model for Time Series Anomaly Detection"
title_zh: TimeRadar：一种用于时间序列异常检测的领域可旋转基础模型
authors: "Hui He, Hezhe Qiao, Yutong Chen, Kun Yi, Guansong Pang"
date: 2026-02-22
pdf: "https://arxiv.org/pdf/2602.19068v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 时间序列异常检测的基础模型
tldr: TimeRadar是一个面向时间序列异常检测（TSAD）的通用基础模型。针对现有模型在固定时频域难以区分正常与异常模式的问题，该研究提出了分数阶时频域旋转技术。通过FTFRecon组件自适应寻找最佳旋转角度进行数据重构，并结合CDL捕捉局部偏差，实现了在多种未见数据集上的高效异常检测。
selection_source: fresh_fetch
motivation: 现有的时间序列基础模型在固定时频域内难以区分罕见的异常模式与常规模式，限制了其在无监督异常检测任务中的表现。
method: 提出了TimeRadar模型，包含可学习分数阶旋转的FTFRecon组件用于自适应数据重构，以及CDL组件用于建模局部上下文偏差。
result: 该模型能够自适应地在最佳时频表示下区分正常与异常信号，在跨领域的未见数据集上展现出强大的泛化能力。
conclusion: TimeRadar通过引入可旋转的分数阶时频域，为构建通用的时间序列异常检测基础模型提供了一种创新的解决方案。
---

## 摘要
当前的时间序列基础模型（TSFMs）主要侧重于在预定义的时域或频域内学习普遍且规律的模式，以支持有监督的下游任务（例如预测）。因此，它们对于本质上属于无监督的下游任务——如旨在识别稀有、不规则模式的时间序列异常检测（TSAD）——往往效果不佳。这种局限性的产生是因为当在相同的时域/频域中呈现时，此类异常模式可能与常规模式非常相似。为了解决这一问题，我们推出了 TimeRadar，这是一种构建在分数阶时频域中的创新型 TSFM，旨在支持跨多种未知数据集的通用 TSAD。我们的核心见解是，将时间序列旋转到依赖于数据的分数阶时频表示中，可以自适应地区分不同数据集中的正常和异常信号。为此，TimeRadar 提出了一个名为分数调制时频重构（FTFRecon）的新组件，利用可学习的分数阶将时间序列旋转到连续时域和频域之间最显著的角度，以实现准确的数据重构。这为每个数据输入在最优时频域中提供了自适应数据重构，从而能够有效区分跨数据集（包括未知数据集）中无边界的异常模式与常规模式。为了让 TimeRadar 能够建模全局数据重构无法捕捉的局部异常，我们进一步引入了上下文偏差学习（CDL）组件，用于在可旋转域中建模输入相对于其上下文时间序列数据的局部偏差。

## Abstract
Current time series foundation models (TSFMs) primarily focus on learning prevalent and regular patterns within a predefined time or frequency domain to enable supervised downstream tasks (e.g., forecasting). Consequently, they are often ineffective for inherently unsupervised downstream tasks-such as time series anomaly detection (TSAD), which aims to identify rare, irregular patterns. This limitation arises because such abnormal patterns can closely resemble the regular patterns when presented in the same time/frequency domain. To address this issue, we introduce TimeRadar, an innovative TSFM built in a fractional time-frequency domain to support generalist TSAD across diverse unseen datasets. Our key insight is that rotating a time series into a data-dependent fractional time-frequency representation can adaptively differentiate the normal and abnormal signals across different datasets. To this end, a novel component, namely Fractionally modulated Time-Frequency Reconstruction (FTFRecon), is proposed in TimeRadar to leverage a learnable fractional order to rotate the time series to the most pronounced angle between a continuous time and frequency domain for accurate data reconstruction. This provides adaptive data reconstruction in an optimal time-frequency domain for each data input, enabling effective differentiation of the unbounded abnormal patterns from the regular ones across datasets, including unseen datasets. To allow TimeRadar to model local abnormality that is not captured by the global data reconstruction, we further introduce a Contextual Deviation Learning (CDL) component to model the local deviation of the input relative to its contextual time series data in the rotatable domain.

---

## 论文详细总结（自动生成）

### TimeRadar：一种用于时间序列异常检测的领域可旋转基础模型

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的时间序列基础模型（TSFM）大多针对预测任务设计，侧重于学习规律性模式。在异常检测（TSAD）中，异常模式在单一的时域或频域中往往与正常模式高度相似（例如：时域中的趋势异常、频域中的形状异常），导致模型难以区分。
*   **研究动机**：为了解决固定域建模的局限性，论文提出需要一种能够根据数据特征自适应调整“观察视角”（即时频域旋转角度）的模型，以最大化正常与异常信号之间的差异。
*   **整体含义**：TimeRadar 是首个专门为 TSAD 设计的、支持领域旋转的基础模型，旨在通过分数阶时频空间实现跨数据集的通用异常检测。

#### 2. 方法论
*   **核心思想**：利用**分数阶傅里叶变换（FRFT）**，将时间序列投影到一个介于时域和频域之间的连续旋转空间中，并学习一个数据依赖的旋转角度 $\alpha$，从而在最优的分数阶域内进行重构和检测。
*   **关键技术细节**：
    *   **FTFRecon（分数调制时频重构）**：
        1.  **自适应旋转**：通过一个小型 MLP 根据输入序列预测分数阶参数 $r$，计算旋转角度 $\alpha = r\pi/2$。
        2.  **补丁与掩码**：在分数域进行分段（Patching）并采用互补掩码策略。
        3.  **分数调制编码器**：使用复数卷积和调制/解调操作处理 FRFT 产生的复数表示，保持相位一致性。
    *   **CDL（上下文偏差学习）**：在隐藏层空间计算每个补丁与其上下文补丁的余弦相似度偏差，通过边际损失（Margin Loss）拉大正常与异常模式的距离，增强局部异常的识别能力。
*   **算法流程**：输入序列 $\rightarrow$ 预测旋转角 $\rightarrow$ FRFT 转换 $\rightarrow$ 掩码重构 + 上下文偏差计算 $\rightarrow$ 联合损失优化（重构损失 + CDL 损失）。

#### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 Monash 存储库的增强版（Monash+），包含约 4.08 亿个时间点，涵盖能源、自然、经济等多个领域。
    *   **评估**：8 个主流基准数据集（SMD, MSL, PSM, SWaT, SMAP, CICIDS, SWAN, Creditcard）以及 UCR 异常档案。
*   **Benchmark 与对比方法**：
    *   **基础模型**：TimesFM, Chronos-Bolt, Time-MoE, SEMPO, 以及专门用于异常检测的 DADA。
    *   **传统/深度模型**：Anomaly Transformer, DCdetector, TF-MAE, CATCH, GPT4TS 等 13 种 SOTA 方法。
*   **实验设置**：涵盖了 **Zero-shot（零样本）** 和 **Few-shot（少样本）** 两种推理场景。

#### 4. 资源与算力
*   **算力设备**：使用了 2 台 **NVIDIA Pro6000 (96G)** GPU。
*   **训练时长**：整个预训练过程耗时约 **3 小时**。
*   **训练细节**：采用 BF32 精度，Batch Size 为 2048，使用 AdamW 优化器。
*   **模型规模**：参数量仅为 **1.6M**，相比于动辄上亿参数的预测基础模型（如 TimesFM 500M），非常轻量化。

#### 5. 实验数量与充分性
*   **实验规模**：在 8 个不同领域的真实数据集上进行了详尽测试，并额外在包含 250 个子数据集的 UCR 档案上验证。
*   **消融实验**：设计了 4 种变体（纯时域、纯频域、无调制、无 CDL），验证了各组件的必要性。
*   **敏感性分析**：对窗口大小 $T$、边际参数 $\gamma$、损失权重 $\lambda$ 进行了参数搜索。
*   **充分性评价**：实验设计客观、公平，对比了不同架构（Encoder-only vs Decoder-only）和不同领域（时域 vs 频域）的模型，结果具有统计说服力。

#### 6. 主要结论与发现
*   **性能领先**：TimeRadar 在零样本设置下显著优于所有竞争对手，AUC-R 平均提升 10.5%，AUC-P 平均提升 29.4%。
*   **旋转的价值**：实验证明，不同数据集的最优旋转角度各不相同，TimeRadar 学习到的自适应角度能有效捕捉复杂异常（如形状和趋势异常）。
*   **泛化能力**：即使在数据极度稀缺的 Few-shot 场景下，TimeRadar 的表现也优于经过全量数据训练的传统模型。

#### 7. 优点
*   **理论创新**：首次将分数阶傅里叶变换引入时间序列基础模型，解决了固定域建模的盲区。
*   **高效轻量**：以极小的参数量（1.6M）实现了超越大规模模型的性能，推理速度快，适合实际部署。
*   **多尺度建模**：结合了全局重构（FTFRecon）和局部偏差（CDL），能够同时处理点异常和复杂的子序列异常。

#### 8. 不足与局限
*   **合成数据依赖**：预训练中的异常模式主要依赖人工注入（Monash+），虽然多样化，但可能与某些极端的真实异常存在分布差异。
*   **通道独立性假设**：模型采用通道独立（Channel Independence）策略处理多变量数据，虽然提高了通用性，但可能忽略了变量间的复杂交叉相关性。
*   **数学复杂度**：分数阶傅里叶变换的引入增加了模型实现的数学门槛和计算逻辑复杂度。

（完）
