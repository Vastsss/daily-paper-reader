---
title: "Bridging the High-Frequency Data Gap: A Millisecond-Resolution Network Dataset for Advancing Time Series Foundation Models"
title_zh: 弥补高频数据差距：一个用于推进时间序列基础模型的毫秒级分辨率网络数据集
authors: "Subina Khanal, Seshu Tirupathi, Merim Dzaferagic, Marco Ruffini, Torben Bach Pedersen"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16497v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 用于推进时间序列基础模型的高频数据集
tldr: 本研究针对时间序列基础模型（TSFM）在处理高频数据方面的短板，推出了一个具有毫秒级分辨率的5G无线网络数据集。该数据集填补了现有大规模数据集在秒级以下分辨率的空白，并引入了无线网络这一新领域。通过在短时预测任务上对传统模型和TSFM进行基准测试，发现现有TSFM在零样本和微调情况下均表现欠佳。此工作强调了将高频数据纳入预训练对于提升模型泛化性与鲁棒性的必要性。
selection_source: fresh_fetch
motivation: 现有时间序列基础模型的数据集多集中在秒级到年级的低频率，难以捕捉毫秒级的高频动态变化。
method: 引入了一个来自真实5G无线部署的毫秒级分辨率数据集，并提供了跨度为100毫秒至9.6秒的短时预测用例。
result: 基准测试表明，大多数现有的TSFM在处理这种高频新数据分布时，无论是零样本还是微调模式下表现都较差。
conclusion: 研究强调了在预训练阶段整合高频数据集对于优化TSFM架构、提升其在现实应用中泛化能力和鲁棒性的重要意义。
---

## 摘要
时间序列基础模型 (TSFMs) 需要多样化的真实世界数据集，以适应不同的领域和时间频率。然而，目前的大规模数据集主要集中在采样间隔（即时间分辨率）在秒到年范围内的低频时间序列，这阻碍了它们捕捉高频时间序列数据细微差别的能力。为了解决这一局限性，我们引入了一个新颖的数据集，该数据集捕获了来自运行中的 5G 无线部署的毫秒级分辨率无线和流量状况，扩展了 TSFMs 的范围，使其能够将高频数据纳入预训练。此外，该数据集引入了一个新领域——无线网络，从而补充了能源和金融等现有的更通用领域。该数据集还提供了短期预测的使用案例，预测范围从 100 毫秒（1 步）到 9.6 秒（96 步）。通过在该数据集上对传统机器学习模型和 TSFMs 进行预测任务的基准测试，我们证明了大多数 TSFM 模型配置在零样本和微调设置下，在此新数据分布上的表现均较差。我们的工作强调了在预训练和预测过程中纳入高频数据集的重要性，以增强 TSFMs 在实际应用中的架构、微调策略、泛化能力和鲁棒性。

## Abstract
Time series foundation models (TSFMs) require diverse, real-world datasets to adapt across varying domains and temporal frequencies. However, current large-scale datasets predominantly focus on low-frequency time series with sampling intervals, i.e., time resolution, in the range of seconds to years, hindering their ability to capture the nuances of high-frequency time series data. To address this limitation, we introduce a novel dataset that captures millisecond-resolution wireless and traffic conditions from an operational 5G wireless deployment, expanding the scope of TSFMs to incorporate high-frequency data for pre-training. Further, the dataset introduces a new domain, wireless networks, thus complementing existing more general domains like energy and finance. The dataset also provides use cases for short-term forecasting, with prediction horizons spanning from 100 milliseconds (1 step) to 9.6 seconds (96 steps). By benchmarking traditional machine learning models and TSFMs on predictive tasks using this dataset, we demonstrate that most TSFM model configurations perform poorly on this new data distribution in both zero-shot and fine-tuned settings. Our work underscores the importance of incorporating high-frequency datasets during pre-training and forecasting to enhance architectures, fine-tuning strategies, generalization, and robustness of TSFMs in real-world applications.

---

## 论文详细总结（自动生成）

这篇论文针对时间序列基础模型（TSFMs）在处理高频数据方面的局限性，提出了一个毫秒级分辨率的 5G 网络数据集，并进行了深入的基准测试。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的时间序列基础模型（TSFMs）虽然在通用任务上表现出色，但其预训练数据大多集中在低频领域（采样间隔为秒、小时、天或年）。这导致模型在面对具有瞬时波动、非平稳性和复杂动态特性的**高频数据（毫秒级）**时，泛化能力和预测精度大幅下降。
*   **研究背景**：随着 5G/6G 通信、高频交易等领域的发展，毫秒级预测变得至关重要。论文旨在通过引入一个真实世界的 5G 无线网络数据集，弥补高频数据的空白，并探索现有 TSFMs 在这一新领域和新频率下的表现。

### 2. 核心方法论
*   **数据集构建**：利用 OpenIreland 测试床，在真实的 5G O-RAN（开放无线接入网）环境下收集物理层和 MAC 层的性能指标（PMs）。
    *   **多样性场景**：涵盖了多种移动模式（静态、行人、汽车、公交、火车）和流量类型（网页、视频流、VoIP、IoT 以及 DDoS 等恶意攻击）。
    *   **关键特征**：包括信道质量指示（CQI）、调制编码方案（MCS）、信噪比（SINR）、吞吐量（Bitrate）等 47 个特征。
*   **预测任务定义**：设定为短期预测任务，将原始毫秒数据聚合为 100 毫秒间隔，预测未来 1 到 96 个步长（即最高预测未来 9.6 秒的状态）。
*   **模型对比框架**：对比了三类模型：
    1.  **传统机器学习（浅层模型）**：随机森林（RF）、XGBoost、自适应随机森林（ARF）。
    2.  **在线学习模型**：在线线性回归（OLR）。
    3.  **时间序列基础模型（TSFMs）**：TinyTimeMixer (TTM)、Chronos (Small/Base)、Lag-Llama。

### 3. 实验设计
*   **数据集/场景**：主要针对 5G 下行比特率（Downlink Bitrate）进行单变量和多变量预测。实验使用了过滤后的特定子集（如静态/行人移动模式下的视频流流量）。
*   **基准测试（Benchmark）**：
    *   **零样本（Zero-shot）**：直接使用预训练的 TSFMs 进行预测。
    *   **微调（Fine-tuning）**：在 5G 数据集上对 TSFMs 进行进一步训练。
*   **对比方法**：以 Naive（朴素预测）作为基准线，评估 RMSE（均方根误差）和 MAE（平均绝对误差）。
*   **消融实验**：研究了特征数量（4 个 vs 10 个）、时间分辨率（100ms 到 3000ms）以及不同的微调策略（仅头部微调 vs 适配器微调）对结果的影响。

### 4. 资源与算力
*   **硬件配置**：论文明确指出实验在一台本地机器上完成，配置为：Intel Core i7-1165G7 CPU (4核8线程)，32 GB RAM。
*   **算力说明**：文中**未提及**使用了高性能 GPU（如 A100/H100）。这反映了所选模型（如 TTM 和 Chronos-small）的轻量化特性，但也暗示了实验规模受限于单机算力。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 3 个不同的随机种子（42, 99, 123）以确保结果的可重复性。
    *   涵盖了单变量和多变量两种设置。
    *   进行了多组消融实验，探讨了分辨率和微调策略的影响。
*   **充分性评价**：实验设计较为**客观且公平**。作者不仅对比了零样本表现，还尝试了多种微调手段。通过与 ARF 等能够处理“概念漂移”的在线模型对比，揭示了 TSFMs 在处理非平稳高频数据时的本质短板。

### 6. 主要结论与发现
*   **TSFMs 表现不佳**：在毫秒级网络数据上，大多数 TSFMs 的表现甚至不如传统的自适应随机森林（ARF）。
*   **数据分布偏移**：TSFMs 在零样本情况下表现较差，主因是预训练数据缺乏高频、多噪声、具有突发性（Spiky）的分布特征。
*   **微调提升有限**：即使经过微调，TSFMs 仍难以完全捕捉 5G 网络中复杂的非线性动态。
*   **ARF 的优势**：自适应随机森林（ARF）由于能够动态更新树结构以应对概念漂移，在多变量预测任务中表现最优。

### 7. 优点与亮点
*   **填补空白**：首次引入了毫秒级分辨率的无线网络开源数据集，为 TSFM 社区提供了极具挑战性的新基准。
*   **领域扩展**：将时间序列研究从传统的能源、金融领域扩展到了复杂的无线通信网络领域。
*   **深入分析**：通过 STL 分解等统计手段，详细对比了网络数据与其他标准数据集（如 ETT, Electricity）的差异，科学地解释了模型失效的原因。

### 8. 不足与局限
*   **算力与模型规模限制**：实验主要在 CPU 环境下进行，未测试更大规模的 TSFM 变体（如 Chronos-Large）。
*   **超参数优化不足**：作者承认对浅层模型（XGBoost/RF）仅进行了有限的调优，未进行大规模的自动化超参数搜索（HPO）。
*   **单一高频领域**：虽然引入了无线网络数据，但结论是否能推广到其他高频领域（如高频地震数据或超高速工业传感器数据）仍需验证。
*   **微调策略单一**：未深入探索如 LoRA 等更先进的参数高效微调（PEFT）技术在 TSFMs 上的潜力。

（完）
