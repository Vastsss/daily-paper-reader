---
title: Integrating Inductive Biases in Transformers via Distillation for Financial Time Series Forecasting
title_zh: 通过蒸馏在 Transformer 中集成归纳偏置以进行金融时间序列预测
authors: "Yu-Chen Den, Kuan-Yu Chen, Kendro Vincent, Darby Tien-Hao Chang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16985v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 基于Transformer的金融时间序列预测模型
tldr: 针对金融时间序列的非平稳性，本文提出TIPS框架，通过知识蒸馏将因果性、局部性和周期性等多种归纳偏置整合到Transformer中。该方法利用注意力掩码训练特定偏置的教师模型，并将其知识蒸馏至学生模型，在四大股票市场中实现了显著的性能提升，在保持低计算成本的同时，展现了优异的收益率和鲁棒性。
selection_source: fresh_fetch
motivation: 传统Transformer在处理非平稳金融数据时表现不佳，且单一的归纳偏置难以适应多变的市场环境。
method: 提出TIPS框架，利用注意力掩码训练具备不同归纳偏置的教师模型，并通过知识蒸馏将这些偏置整合到单一学生模型中。
result: "在四大股票市场上，TIPS在年化收益、夏普比率和卡玛比率上显著优于强基准模型，且推理计算量仅为后者的38%。"
conclusion: 结合领域知识和动态归纳偏置的蒸馏方法，是提升Transformer在非平稳金融预测中泛化能力的有效途径。
---

## 摘要
由于 Transformer 模型具有极高的表征能力和架构灵活性，已被广泛应用于时间序列预测。然而，许多 Transformer 变体隐含地假设了平稳性和稳定的时间动态——这些假设在以状态切换（regime shifts）和非平稳性为特征的金融市场中经常被违背。实证研究表明，在金融任务上，最先进的时间序列 Transformer 往往甚至不如基础的 vanilla Transformer，而具有独特归纳偏置（inductive biases）的更简单架构（如 CNN 和 RNN）能以显著更低的复杂度实现更强的性能。同时，没有任何单一的归纳偏置能在不同市场或状态下占据主导地位，这表明稳健的金融预测需要集成互补的时间先验。我们提出了 TIPS（Transformer with Inductive Prior Synthesis），这是一个通过知识蒸馏框架在统一的 Transformer 中合成因果性、局部性和周期性等多种归纳偏置的模型。TIPS 通过注意力掩码（attention masking）训练具有特定偏置的 Transformer 教师模型，然后将其知识蒸馏到一个单一的学生模型中，并在不同归纳偏置之间实现状态依赖的对齐。在四个主要股票市场的实验中，TIPS 达到了最先进的性能，在年化收益率、夏普比率（Sharpe ratio）和卡玛比率（Calmar ratio）上分别超过了强大的集成基准模型 55%、9% 和 16%，同时仅需 38% 的推理计算量。进一步的分析表明，TIPS 产生了显著优于 vanilla Transformer 及其教师集成模型的统计超额收益，并在经典架构的盈利期表现出与其一致的状态依赖行为对齐。这些结果强调了在非平稳金融时间序列中，利用状态依赖的归纳偏置对于实现稳健泛化的重要性。

## Abstract
Transformer-based models have been widely adopted for time-series forecasting due to their high representational capacity and architectural flexibility. However, many Transformer variants implicitly assume stationarity and stable temporal dynamics -- assumptions routinely violated in financial markets characterized by regime shifts and non-stationarity. Empirically, state-of-the-art time-series Transformers often underperform even vanilla Transformers on financial tasks, while simpler architectures with distinct inductive biases, such as CNNs and RNNs, can achieve stronger performance with substantially lower complexity. At the same time, no single inductive bias dominates across markets or regimes, suggesting that robust financial forecasting requires integrating complementary temporal priors. We propose TIPS (Transformer with Inductive Prior Synthesis), a knowledge distillation framework that synthesizes diverse inductive biases -- causality, locality, and periodicity -- within a unified Transformer. TIPS trains bias-specialized Transformer teachers via attention masking, then distills their knowledge into a single student model with regime-dependent alignment across inductive biases. Across four major equity markets, TIPS achieves state-of-the-art performance, outperforming strong ensemble baselines by 55%, 9%, and 16% in annual return, Sharpe ratio, and Calmar ratio, while requiring only 38% of the inference-time computation. Further analyses show that TIPS generates statistically significant excess returns beyond both vanilla Transformers and its teacher ensembles, and exhibits regime-dependent behavioral alignment with classical architectures during their profitable periods. These results highlight the importance of regime-dependent inductive bias utilization for robust generalization in non-stationary financial time series.

---

## 论文详细总结（自动生成）

这是一份关于论文《Integrating Inductive Biases in Transformers via Distillation for Financial Time Series Forecasting》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了 **Transformer 模型在金融时间序列预测中的局限性**。尽管 Transformer 在通用时间序列任务中表现优异，但在金融领域往往不如简单的 RNN、CNN 或线性模型。
*   **核心矛盾**：金融市场具有高度的**非平稳性**和**状态切换（Regime Shifts）**特征。通用的 Transformer 变体通常假设时间动态是稳定的，而金融预测需要特定的“归纳偏置”（如因果性、局部性、周期性）。
*   **研究发现**：没有任何单一的归纳偏置能在所有市场或所有时期始终占优。
*   **目标**：如何在一个统一的 Transformer 框架内，集成多种互补的归纳偏置，并使其能够根据市场状态动态调整，同时保持高效的推理性能。

### 2. 方法论：TIPS 框架
论文提出了 **TIPS（Transformer with Inductive Prior Synthesis）**，一个基于知识蒸馏的框架，核心思想是“先专业化再综合”。

*   **第一阶段：训练偏置专业化的教师模型**
    *   使用统一的 Transformer 架构，但通过修改**注意力掩码（Attention Masking）**和**位置偏置**来注入不同的归纳偏置。
    *   **因果性（Causality）**：通过正向和反向因果掩码捕捉趋势。
    *   **局部性（Locality）**：通过 Patching（分块）和 ALiBi（距离衰减偏置）关注近期信息。
    *   **周期性（Periodicity）**：通过固定周期偏置和可学习的相对位置偏置捕捉季节性规律。
    *   共训练 7 个教师模型（含一个 Vanilla Transformer）。
*   **第二阶段：知识蒸馏与合成**
    *   **集成目标**：将 7 个教师模型的预测对数（Logits）进行平均，作为学生模型的学习目标。
    *   **激进正则化**：为了防止学生模型死记硬背某一个教师，采用了**低温度蒸馏**（保持排名信号）、**强标签平滑（Label Smoothing）**和**随机权重平均（SWA）**。
    *   **核心逻辑**：学生模型（Vanilla 架构）通过蒸馏学习到了在不同市场状态下应趋向于哪种教师的偏置，实现了“状态依赖的偏置激活”。

### 3. 实验设计
*   **数据集**：覆盖四大主流股票指数：**CSI300、CSI500（中国）、NI225（日本）、SP500（美国）**。时间跨度包含 2021-2024 年的测试期，涵盖了牛市、熊市和高波动期。
*   **Benchmark（基准方法）**：
    *   **通用时间序列 SOTA**：DLinear, TimeMixer, AutoFormer, PatchTST, iTransformer。
    *   **经典架构**：TCN, GRU, LSTM, Transformer, Mamba。
    *   **金融专用模型**：RankLSTM, MASTER, StockMixer。
    *   **集成模型**：上述各类模型的 Ensemble 版本。
*   **评价指标**：年化收益率（AR）、夏普比率（SR）、卡玛比率（CR）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA GeForce RTX 3090 GPU**。
*   **训练时长**：基准模型和教师模型训练 100 个 Epoch，学生模型仅需训练 20 个 Epoch。
*   **效率表现**：TIPS 的推理计算量（FLOPs）仅为教师集成模型的 **14%**（约 0.81G），推理延迟与基础 Transformer 持平（1.31ms/样本）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同特征的市场进行验证，每个实验运行 **5 个随机种子**取平均值，确保了结果的统计可靠性。
*   **消融实验**：详细测试了蒸馏过程中的各个组件（低温度、标签平滑、SWA）以及不同偏置组合对性能的影响。
*   **深入分析**：进行了**性能归因分析**（证明 TIPS 产生了显著的 Alpha 超额收益）和**行为对齐分析**（证明 TIPS 在特定时期确实表现出与特定经典架构相似的行为）。
*   **客观性**：对比了多种不同家族的模型，且在附录中补充了考虑交易成本后的性能表现，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：TIPS 在所有市场均达到 SOTA，平均夏普比率达到 1.454，显著超过了复杂的集成模型。
*   **学生超越教师**：蒸馏后的学生模型性能优于其教师的简单集成（AR 提升 29%），说明蒸馏过程实现了知识的有效合成而非简单模仿。
*   **状态依赖性**：TIPS 展现了“条件偏置激活”能力，例如在某些时期它更像 GRU（捕捉序列依赖），在另一些时期更像 TCN（捕捉局部特征）。
*   **效率优势**：以单模型成本实现了超越集成模型的鲁棒性。

### 7. 优点（亮点）
*   **架构简洁性**：不改变 Transformer 核心架构，仅通过掩码实现偏置注入，易于工程实现。
*   **解决“合并惩罚”**：论文指出直接在单一模型中多任务训练多种偏置会导致性能下降，而通过“先分离再蒸馏”成功规避了这一问题。
*   **深入的解释性**：通过行为对齐实验，从实证角度解释了深度学习模型如何应对金融市场的非平稳性。

### 8. 不足与局限
*   **特征工程较基础**：仅使用了 OHLCV 和移动平均线等 8 个基础特征，未探索量价因子之外的替代数据（如新闻、社交媒体）。
*   **超参数敏感性**：蒸馏过程依赖于精细调节的温度系数和标签平滑因子，不同市场的最优配置可能存在差异。
*   **应用限制**：目前主要针对股票排名（Ranking）任务，在其他金融任务（如波动率预测或期权定价）上的有效性尚待验证。
*   **交易成本**：虽然附录有讨论，但主实验未深度整合滑点和流动性限制等现实交易约束。

（完）
