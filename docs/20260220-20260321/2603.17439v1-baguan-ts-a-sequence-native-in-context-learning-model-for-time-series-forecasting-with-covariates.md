---
title: "Baguan-TS: A Sequence-Native In-Context Learning Model for Time Series Forecasting with Covariates"
title_zh: Baguan-TS：一种用于带协变量时间序列预测的原生序列上下文学习模型
authors: "Linxiao Yang, Xue Jiang, Gezheng Xu, Tian Zhou, Min Yang, ZhaoYang Zhu, Linyuan Geng, Zhipeng Zeng, Qiming Chen, Xinyue Gu, Rong Jin, Liang Sun"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17439v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 时间序列预测的上下文学习模型
tldr: Baguan-TS是一个统一的时间序列预测框架，旨在解决现有上下文学习（ICL）方法依赖人工特征或缺乏推理自适应的问题。它采用3D Transformer架构，在时间、变量和上下文维度上进行联合建模。通过引入基于检索的局部校准和上下文过拟合策略，解决了训练稳定性和输出过度平滑问题。在包含协变量的基准测试和能源数据集中表现优异，显著提升了预测精度和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的时间序列上下文学习方法要么依赖手工特征，要么缺乏推理时的自适应能力，难以直接处理原始序列。
method: 提出Baguan-TS框架，利用3D Transformer联合处理时间、变量和上下文，并结合局部校准与上下文过拟合策略优化性能。
result: 在公开基准测试和真实能源数据集上，该模型在点预测和概率预测指标上均显著优于现有基准模型。
conclusion: Baguan-TS成功将原始序列表示学习与上下文学习结合，为带协变量的时间序列预测提供了一种高效且鲁棒的解决方案。
---

## 摘要
Transformer 使得时间序列预测中的上下文学习（ICL）能够实现快速、无梯度的自适应，然而，大多数 ICL 风格的方法依赖于表格化的手工特征，而端到端序列模型则缺乏推理时的自适应能力。我们通过一个统一的框架 Baguan-TS 弥补了这一差距，该框架将原始序列表示学习与 ICL 相结合，并由一个在时间、变量和上下文轴上进行联合注意力的 3D Transformer 实例化。为了使这一高容量模型具有实用性，我们解决了两个关键障碍：(i) 校准和训练稳定性，通过一种与特征无关、基于目标空间检索的局部校准来改进；(ii) 输出过度平滑，通过上下文过拟合策略来缓解。在带有协变量的公开基准测试中，Baguan-TS 始终优于现有的基准模型，实现了最高的胜率，并在点预测和概率预测指标上均有显著下降。在多种真实世界能源数据集上的进一步评估证明了其鲁棒性，并带来了实质性的改进。

## Abstract
Transformers enable in-context learning (ICL) for rapid, gradient-free adaptation in time series forecasting, yet most ICL-style approaches rely on tabularized, hand-crafted features, while end-to-end sequence models lack inference-time adaptation. We bridge this gap with a unified framework, Baguan-TS, which integrates the raw-sequence representation learning with ICL, instantiated by a 3D Transformer that attends jointly over temporal, variable, and context axes. To make this high-capacity model practical, we tackle two key hurdles: (i) calibration and training stability, improved with a feature-agnostic, target-space retrieval-based local calibration; and (ii) output oversmoothing, mitigated via context-overfitting strategy. On public benchmark with covariates, Baguan-TS consistently outperforms established baselines, achieving the highest win rate and significant reductions in both point and probabilistic forecasting metrics. Further evaluations across diverse real-world energy datasets demonstrate its robustness, yielding substantial improvements.

---

## 论文详细总结（自动生成）

以下是对论文《Baguan-TS: A Sequence-Native In-Context Learning Model for Time Series Forecasting with Covariates》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖手工特征工程的前提下，实现针对原始多变量时间序列（带协变量）的**原生序列上下文学习（In-Context Learning, ICL）**。
*   **研究背景**：
    *   目前的 ICL 预测模型（如 TabPFN-TS）多将时间序列视为表格数据，依赖人工提取的特征，忽略了原始序列的结构。
    *   端到端序列模型（如 PatchTST）虽擅长提取原始序列特征，但通常缺乏推理时的无梯度自适应能力。
    *   在大容量模型中，ICL 容易出现**训练不稳定（校准差）**和**输出过度平滑（丢失周期性尖峰）**的问题。

### 2. 方法论
*   **核心思想**：提出 **Baguan-TS** 框架，将原始序列表示学习与 ICL 统一。通过一个 **3D Transformer** 架构，在时间（Temporal）、变量（Variable）和上下文（Context）三个维度上联合进行注意力计算。
*   **关键技术细节**：
    *   **3D Transformer 块**：采用分解注意力机制。时间轴使用 RoPE 处理位置信息；变量轴捕捉跨通道相关性；上下文轴实现支持集（Support Set）与查询集（Query）的信息共享。
    *   **Y-空间检索增强预测 (Y-space RBfcst)**：不同于传统的协变量空间（X-space）检索，该方法基于目标历史轨迹的相似性检索上下文。这种“特征无关”的局部校准机制提高了模型对分布偏移的鲁棒性。
    *   **上下文过拟合策略 (Context-Overfitting)**：为了解决模型倾向于预测平滑趋势而忽略尖峰的问题，训练时强制模型识别并重建上下文中完全匹配的片段，从而增强对周期性模式的捕捉。
    *   **自适应推理模式**：支持 3D 原生序列模式和 2D 表格化模式的集成（Ensemble），平衡时间依赖与特征相关性。

### 3. 实验设计
*   **数据集/场景**：
    *   **fev-bench-cov**：包含 30 个带协变量的任务（能源、零售、自然等）。
    *   **fev-bench-uni**：10 个单变量/多变量展平任务。
    *   **真实世界应用**：27 个电力负荷和光伏发电预测数据集（包含 NWP 天气预报和日历效应）。
*   **对比方法 (Baselines)**：
    *   **时间序列基础模型**：Chronos-Bolt, Moirai-2.0, TimesFM-2.5, TiRex, Toto-1.0, Sundial-Base。
    *   **协变量感知 ICL 模型**：TabPFN-TS。
*   **评估指标**：SQL（缩放分位数损失）、MASE、WAPE、WQL。

### 4. 资源与算力
*   **模型规模**：参数量为 **2240 万 (22.4M)**。
*   **超参数**：12 层 3D Transformer，6 个注意力头，嵌入维度 192。
*   **训练细节**：最大上下文长度 50，最大回顾窗口 2048，最大预测长度 192。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号（如 A100 数量）或总训练时长，但提到了消融实验中训练步数为 220K 步。

### 5. 实验数量与充分性
*   **实验规模**：在总计 **67 个任务**（30+10+27）上进行了测试，覆盖了从公开基准到实际工业场景的广泛领域。
*   **消融实验**：针对 RBfcst 检索空间（X vs Y vs XY）、上下文过拟合策略、以及 2D/3D 推理模式的互补性做了详尽分析。
*   **鲁棒性测试**：通过注入高斯白噪声、随机游走噪声和周期性噪声，验证了模型在极端干扰下的稳定性。
*   **评价**：实验设计非常充分且客观，通过“胜率（Win Rate）”和“技能得分（Skill Score）”多维度展示了相对于 SOTA 模型的优势。

### 6. 主要结论与发现
*   **性能领先**：Baguan-TS 在 fev-bench-cov 上显著优于所有基准，SQL 指标比 TabPFN-TS 降低了 4.8%。
*   **检索策略**：在时间序列中，基于目标值轨迹（Y-space）的检索比基于协变量（X-space）的检索更能提供有效的上下文信息。
*   **过拟合的正面作用**：引入辅助的自检索任务（上下文过拟合）能有效抑制 Transformer 的过度平滑倾向，恢复高频尖峰信号。
*   **模式互补**：2D 模式（关注特征间关系）和 3D 模式（关注时间演化）的集成能显著改善概率预测的校准度。

### 7. 优点
*   **原生序列支持**：摆脱了对复杂手工特征工程的依赖，直接处理原始多变量序列。
*   **创新的 3D 架构**：将 ICL 的上下文维度与时间、变量维度解耦又统一，逻辑清晰且有效。
*   **解决痛点**：针对 ICL 在时间序列中常见的“过度平滑”和“校准难”问题提出了针对性的算法方案。

### 8. 不足与局限
*   **推理开销**：3D 注意力机制及多模式集成（Ensemble）虽然提升了精度，但可能增加推理延迟和显存占用。
*   **算力细节缺失**：未披露具体的训练资源消耗，难以评估其训练成本。
*   **协变量限制**：虽然支持协变量，但实验中最大协变量数量限制在 80 个，对于极高维特征场景的扩展性有待验证。

（完）
