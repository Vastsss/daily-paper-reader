---
title: Time-Aware Prior Fitted Networks for Zero-Shot Forecasting with Exogenous Variables
title_zh: 时间感知的先验拟合网络：支持外生变量的零样本预测
authors: "Andres Potapczynski, Ravi Kiran Selvam, Tatiana Konstantinova, Shankar Ramasubramanian, Malcolm Wolff, Kin G. Olivares, Ruijun Ma, Mengfei Cao, Michael W. Mahoney, Andrew Gordon Wilson, Boris N. Oreshkin, Dmitry Efimov"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15802v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 时间序列基础模型的零样本预测
tldr: 针对现有时间序列基础模型忽略外部变量（如促销、天气）导致预测精度受限的问题，本文提出 ApolloPFN。这是一种时间感知型先验拟合网络（PFN），通过创新的合成数据生成流程和时间感知架构修改，实现了对外部协变量的原生支持。该模型在 M5 和电价预测等包含外部信息的基准测试中达到了最先进水平，显著提升了零样本预测能力。
selection_source: fresh_fetch
motivation: 现有时间序列基础模型大多仅依赖历史数值，忽略了对预测至关重要的外部协变量（如价格、天气等）。
method: 提出 ApolloPFN，结合了针对时间序列优化的合成数据生成方法和具有时间归纳偏置的架构改进。
result: 在 M5 竞赛和电力价格预测等多个包含外部变量的基准数据集上，ApolloPFN 取得了 SOTA 性能。
conclusion: ApolloPFN 证明了通过时间感知架构和合成数据训练，PFN 能有效处理带外部变量的零样本时间序列预测。
---

## 摘要
在许多时间序列预测场景中，目标时间序列通常伴随着外生协变量，例如零售需求中的促销和价格；能源负荷中的温度；交通或销售中的日历和节假日指标；以及电力定价中的电网负荷或燃料成本。忽略这些外生信号会显著降低预测准确性，特别是当它们导致目标序列出现峰值、不连续性或状态与相位变化时。目前大多数时间序列基础模型（如 Chronos、Sundial、TimesFM、TimeMoE、TimeLLM 和 LagLlama）都忽略了外生协变量，仅根据数值型时间序列历史进行预测，从而限制了它们的性能。在本文中，我们开发了 ApolloPFN，这是一种先验数据拟合网络（PFN），它具有时间感知能力（不同于以往的 PFN），并且原生支持外生协变量（不同于以往的单变量预测器）。我们的设计引入了两项主要进展：(i) 一种专门定制的合成数据生成程序，旨在解决将表格（非时间性）PFN 应用于时间序列时出现的失效模式；(ii) 时间感知的架构修改，嵌入了利用时间序列上下文所需的归纳偏置。我们证明了 ApolloPFN 在包含外生信息的基准测试（如 M5 和电价预测）中取得了最先进的结果。

## Abstract
In many time series forecasting settings, the target time series is accompanied by exogenous covariates, such as promotions and prices in retail demand; temperature in energy load; calendar and holiday indicators for traffic or sales; and grid load or fuel costs in electricity pricing. Ignoring these exogenous signals can substantially degrade forecasting accuracy, particularly when they drive spikes, discontinuities, or regime and phase changes in the target series. Most current time series foundation models (e.g., Chronos, Sundial, TimesFM, TimeMoE, TimeLLM, and LagLlama) ignore exogenous covariates and make forecasts solely from the numerical time series history, thereby limiting their performance. In this paper, we develop ApolloPFN, a prior-data fitted network (PFN) that is time-aware (unlike prior PFNs) and that natively incorporates exogenous covariates (unlike prior univariate forecasters). Our design introduces two major advances: (i) a synthetic data generation procedure tailored to resolve the failure modes that arise when tabular (non-temporal) PFNs are applied to time series; and (ii) time-aware architectural modifications that embed inductive biases needed to exploit the time series context. We demonstrate that ApolloPFN achieves state-of-the-art results across benchmarks, such as M5 and electric price forecasting, that contain exogenous information.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ApolloPFN** 的新型时间序列预测模型，旨在解决现有时间序列基础模型（TSFM）在处理外生变量（如价格、天气、促销等）方面的局限性。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究动机**：现实世界的时间序列（如零售需求、电力负荷）往往受到外部因素（外生变量）的强烈影响。忽略这些信号会导致预测精度大幅下降，尤其是在出现突发峰值或状态切换时。
*   **核心问题**：目前主流的时间序列基础模型（如 Chronos, TimesFM, LagLlama 等）大多是单变量模型，无法直接利用外生变量。虽然 TabPFN-TS 尝试引入外生变量，但它基于 i.i.d.（独立同分布）假设，缺乏时间感知的归纳偏置，导致其在处理趋势外推、周期性模式和近期偏差时表现不佳。
*   **整体含义**：ApolloPFN 通过改进先验拟合网络（PFN）的架构和合成数据生成流程，构建了一个既能原生支持外生变量，又具备强时间感知能力的零样本（Zero-Shot）预测模型。

### 2. 方法论：核心思想与技术细节
ApolloPFN 的核心在于将时间序列的特性注入到 PFN 的训练框架中：
*   **合成数据生成（数据层改进）**：
    *   **SRNGN 算法**：提出“单根节点随机生长网络”（Single Root Node Growing Network），相比传统的 RGN 算法，它能生成具有更长因果路径和单一根节点的图结构，更符合时间序列的逻辑依赖。
    *   **时间序列根节点激励**：不再随机采样根节点值，而是使用正弦/余弦函数的组合（随机频率和振幅）来生成根节点，从而在合成数据中引入显式的时间相关性。
*   **架构修改（模型层改进）**：
    *   **位置编码**：引入 **RoPE（旋转位置嵌入）** 以捕捉相对时间距离，并结合**绝对位置编码**，打破了传统 PFN 的排列不变性。
    *   **全注意力机制（Full Attention）**：修改了 TabPFN 的注意力掩码，允许测试点之间以及测试点对未来外生变量的关注，确保预测能充分利用未来的已知协变量信息。
    *   **参数规模**：模型非常轻量，仅有约 **1100 万（11M）参数**。

### 3. 实验设计
*   **数据集**：
    *   **含外生变量场景**：电力价格预测（NP, PJM, FR, BE, DE 五个市场）、M5 竞赛数据集（零售需求，含价格和促销信息）。
    *   **单变量场景**：M1, M3, M4 竞赛数据集，以及 Tourism 数据集。
*   **Benchmark 与对比方法**：
    *   **外生变量模型**：TabPFN-TS, Moirai-Large。
    *   **单变量基础模型**：Chronos-Large, Sundial-Base, TimeLLM 等。
*   **评估指标**：sCRPS（概率预测准确性）、RMSSE（点预测准确性）。

### 4. 资源与算力
*   **训练细节**：模型训练了 400,000 步，Batch Size 为 64。
*   **学习率策略**：初始 1e-4，包含 20,000 步线性预热，随后采用余弦退火降至 1e-6。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号和数量，但考虑到其 11M 的极小参数量和 400k 步的训练量，其算力消耗远低于 Chronos-Large 或 Moirai 等大型模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了超过 90,000 条具有外生变量的时间序列，以及经典的 M 系列单变量基准。
*   **消融实验**：针对 SRNGN 算法、RoPE 位置编码、全注意力机制分别做了消融研究，验证了各组件对解决失效模式（如趋势外推失败、缺乏近期偏差等）的贡献。
*   **客观性**：实验对比了不同规模的模型，并区分了零样本（0-shot）和已见数据（†）的情况，评价较为客观。

### 6. 主要结论与发现
*   **性能领先**：在电价预测中，ApolloPFN 比次优模型（TabPFN-TS）平均提升了 12%；在 M5 零售预测中也达到了 SOTA 水平。
*   **以小博大**：仅有 11M 参数的 ApolloPFN 在多个基准上击败了参数量大 30-70 倍的模型（如 700M+ 的 Moirai-Large 和 Chronos-Large）。
*   **零样本能力**：证明了通过精心设计的合成数据先验，PFN 可以在不进行任何下游微调的情况下，高效处理复杂的时间序列预测任务。

### 7. 优点（亮点）
*   **原生支持协变量**：解决了当前 TSFM 难以直接输入外部变量的痛点。
*   **推理效率极高**：作为 PFN，它在推理时不需要迭代优化，且模型极小，部署成本低。
*   **创新的数据合成**：SRNGN 和根节点激励机制为如何为时间序列任务生成高质量合成数据提供了新思路。

### 8. 不足与局限
*   **上下文长度限制**：由于采用标准的二次复杂度注意力机制，处理极长序列时会受到内存限制。
*   **合成数据依赖**：模型的表现高度依赖于预定义的合成数据分布（先验），如果现实数据的模式完全超出了合成数据的覆盖范围，模型可能失效。
*   **少样本困境**：作为上下文学习模型，在历史观测值极少的情况下，可能难以推断出序列特有的时间结构或外生效应。

（完）
