---
title: Data-Local Autonomous LLM-Guided Neural Architecture Search for Multiclass Multimodal Time-Series Classification
title_zh: 面向多类别多模态时间序列分类的数据本地化自主大语言模型引导神经架构搜索
authors: "Emil Hardarson, Luka Biedebach, Ómar Bessi Ómarsson, Teitur Hrólfsson, Anna Sigridur Islind, María Óskarsdóttir"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15939v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: LLM引导的时间序列神经架构搜索
tldr: 针对医疗等隐私敏感领域的多模态时间序列分类，本文提出一种数据本地化的LLM引导神经架构搜索（NAS）框架。该框架在严格的数据本地约束下运行，LLM控制器仅通过摘要信息（如指标和日志）远程指导搜索，无需接触原始数据。通过对专家架构和预处理的联合搜索，在UEA30和SleepEDFx数据集上实现了性能提升，有效减少了人工干预并保障了数据隐私。
selection_source: fresh_fetch
motivation: 敏感时间序列数据受限于隐私保护，难以在云端进行高效的机器学习模型迭代和架构搜索。
method: 提出一种数据本地化的LLM引导NAS框架，采用“一对多”二进制专家模型和轻量级融合MLP，仅通过试验摘要信息进行远程架构优化。
result: 在UEA30和SleepEDFx数据集上的实验表明，该方法优于强基准模型，且性能达到了现有文献的发布水平。
conclusion: 该框架在确保数据不出本地的前提下实现了自动化的架构搜索，显著降低了多模态时间序列建模的人工成本。
---

## 摘要
将机器学习应用于敏感的时间序列数据通常受限于迭代循环：性能在很大程度上取决于预处理和架构，但训练往往必须在严格的数据本地化约束下进行本地部署。这是医疗保健和其他受隐私限制的领域（例如，医院在患者脑电图（EEG）上开发深度学习模型）中的常见问题。这一瓶颈在多模态融合中尤为突出，因为传感器模态必须分别进行预处理，然后再进行组合。大语言模型（LLM）引导的神经架构搜索（NAS）可以使这种探索自动化，但大多数现有工作流都假设在云端执行，或者需要访问无法公开的数据衍生伪影。我们提出了一种新型的数据本地化、LLM 引导的搜索框架，该框架在远程处理候选流水线的同时，根据固定协议在本地执行所有训练和评估。控制器仅观察试验级摘要，例如流水线描述符、指标、学习曲线统计数据和失败日志，而无需访问原始样本或中间特征表示。我们的框架通过针对每个类别和模态的一对多（one-vs-rest）二元专家模型、轻量级融合多层感知机（MLP），以及对专家架构和特定模态预处理的联合搜索，来实现多类别、多模态学习。我们在两种方案上评估了我们的方法：UEA30（公开的多变量时间序列分类数据集）和 SleepEDFx 睡眠分期（异构临床模态，如 EEG、EOG 和 EMG）。结果表明，模块化基准模型表现强劲，而 LLM 引导的 NAS 进一步提升了其性能。值得注意的是，我们的方法发现的模型在大多数基准数据集上的表现均处于已发表的范围内。在这两种设置中，我们的方法通过实现无人值守的架构搜索，同时将敏感数据保留在本地，减少了人工干预。

## Abstract
Applying machine learning to sensitive time-series data is often bottlenecked by the iteration loop: Performance depends strongly on preprocessing and architecture, yet training often has to run on-premise under strict data-local constraints. This is a common problem in healthcare and other privacy-constrained domains (e.g., a hospital developing deep learning models on patient EEG). This bottleneck is particularly challenging in multimodal fusion, where sensor modalities must be individually preprocessed and then combined. LLM-guided neural architecture search (NAS) can automate this exploration, but most existing workflows assume cloud execution or access to data-derived artifacts that cannot be exposed.   We present a novel data-local, LLM-guided search framework that handles candidate pipelines remotely while executing all training and evaluation locally under a fixed protocol. The controller observes only trial-level summaries, such as pipeline descriptors, metrics, learning-curve statistics, and failure logs, without ever accessing raw samples or intermediate feature representations. Our framework targets multiclass, multimodal learning via one-vs-rest binary experts per class and modality, a lightweight fusion MLP, and joint search over expert architectures and modality-specific preprocessing.   We evaluate our method on two regimes: UEA30 (public multivariate time-series classification dataset) and SleepEDFx sleep staging (heterogeneous clinical modalities such as EEG, EOG, and EMG). The results show that the modular baseline model is strong, and the LLM-guided NAS further improves it. Notably, our method finds models that perform within published ranges across most benchmark datasets. Across both settings, our method reduces manual intervention by enabling unattended architecture search while keeping sensitive data on-premise.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Data-Local Autonomous LLM-Guided NAS** 的框架，旨在解决敏感多模态时间序列数据在隐私受限环境下的自动化建模问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在医疗保健等领域，时间序列数据（如脑电图 EEG）具有高度敏感性，必须遵循严格的“数据本地化”约束。传统的机器学习迭代（预处理、架构设计、调参）依赖人工或云端 AutoML 工具，前者耗时耗力，后者则存在隐私泄露风险。
*   **研究动机**：多模态时间序列（如同步的传感器、生理信号）的融合极其复杂。作者希望开发一种既能利用大语言模型（LLM）的推理能力进行神经架构搜索（NAS），又能确保原始数据不离开本地网络的自动化框架。

### 2. 方法论
该框架结合了模块化分解和远程 LLM 引导的闭环优化：
*   **核心思想**：将复杂的多类别多模态分类任务分解为多个“二元专家（Binary Experts）”。
*   **关键技术细节**：
    *   **专家分解**：为每个模态（$M$个）和每个类别（$C$个）训练一个独立的二元分类器（One-vs-Rest）。例如，在睡眠分期中，会有一个专门针对“REM期”和“EEG模态”的专家模型。
    *   **表示级融合**：移除专家模型的最后一层，提取倒数第二层的特征表示（Penultimate Representation），将其拼接后输入一个轻量级的多层感知机（MLP）进行最终的多类别预测。
    *   **LLM 引导的 NAS 循环**：
        1.  **远程控制器（LLM）**：根据历史实验摘要（指标、学习曲线、失败日志）提出新的架构代码或预处理方案。
        2.  **本地执行器**：在本地服务器上运行代码、训练模型并评估。
        3.  **隐私屏障**：LLM 仅接收聚合统计数据和文本摘要，绝不接触原始样本或中间特征。
    *   **本地调试**：引入一个小型本地 LLM 来处理简单的代码错误，防止包含敏感信息的错误日志发送给远程 LLM。

### 3. 实验设计
*   **数据集**：
    *   **UEA30 归档**：包含 21 个多变量时间序列分类任务（如动作识别、心跳监测等）。
    *   **SleepEDFx**：临床睡眠分期数据集，包含 EEG、EOG 和 EMG 等异构模态。
*   **Benchmark（基准）**：
    *   **Baseline end-to-end**：传统的端到端多类别训练。
    *   **Baseline staged**：采用专家分解但使用固定架构的基准。
    *   **Previous work**：引用了 2021-2025 年间相关文献中报告的性能范围。
*   **对比方法**：主要对比了上述三种配置，验证专家分解的有效性以及 LLM 引导搜索带来的增益。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或数量。
*   **训练时长/规模**：实验通过“搜索周期（Cycles）”来衡量。大多数 UEA 任务运行了 10 个周期；SleepEDFx 运行了 30 个周期；EthanolConcentration 任务运行了 60 个周期。由于涉及大量二元专家的独立训练，该方法对计算资源有一定要求，但论文强调其目标是减少“人工工时”而非单纯缩短计算时间。

### 5. 实验数量与充分性
*   **实验规模**：作者在 22 个不同的任务（21 个 UEA 任务 + 1 个临床任务）上进行了测试，涵盖了从极小规模到大规模、从单模态到多模态的多种场景。
*   **充分性与客观性**：
    *   实验设计较为全面，不仅对比了自身基准，还与近年来的 SOTA 性能范围进行了对标。
    *   **公平性**：所有任务的训练/验证/测试集划分在实验期间保持固定，确保了对比的严谨性。
    *   **消融性质**：通过对比“端到端”与“分阶段专家模型”，证明了架构分解本身的价值。

### 6 主要结论与发现
*   **专家分解的优越性**：即使不进行 NAS，分阶段的专家融合模型在 21 个 UEA 任务中的 12 个任务上优于传统的端到端模型。
*   **NAS 的增益**：LLM 引导的 NAS 能进一步提升性能。在 SleepEDFx 上，准确率从 84.7% 提升至 87.9%，进入了已发表文献的领先水平。
*   **适应性**：在基准模型表现较差的任务（如 EthanolConcentration）中，NAS 的提升最为显著（从 32.7% 提升至 44.1%），表明 LLM 能有效修复不匹配的初始架构。

### 7. 优点
*   **隐私保护（Privacy by Design）**：通过物理隔离和摘要式反馈，实现了在不泄露敏感数据的前提下利用强大的远程 LLM。
*   **降低门槛**：LLM 生成的建议具有可解释性（如“建议增加 EMG 专家的精度”），允许非专家用户理解并干预模型开发。
*   **模块化强**：专家分解机制使得模型对缺失模态或特定类别的错误诊断具有更好的鲁棒性和可解释性。

### 8. 不足与局限
*   **LLM 先验偏差**：LLM 可能在训练阶段见过某些公开基准数据集（如 UEA）的优秀架构，存在“数据泄露”或过度拟合已知基准的风险。
*   **计算开销**：为每个类别和模态训练独立专家（$M \times C$ 个模型）在类别数量极多时会导致计算成本激增。
*   **应用限制**：目前主要针对分类任务，尚未扩展到回归或预测任务。
*   **调试风险**：虽然有本地 LLM 过滤，但复杂的错误日志仍可能在无意中泄露数据的统计特征。

（完）
