---
title: "Two-Stage Active Distribution Network Voltage Control via LLM-RL Collaboration: A Hybrid Knowledge-Data-Driven Approach"
title_zh: 基于 LLM-RL 协作的两阶段主动配电网电压控制：一种知识-数据驱动混合方法
authors: "Xu Yang, Chenhui Lin, Xiang Ma, Dong Liu, Ran Zheng, Haotian Liu, Wenchuan Wu"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21715v1"
tags: ["query:llm-wd"]
score: 9.0
evidence: LLM协作用于主动配电网控制
tldr: 针对分布式光伏接入导致的主动配电网电压控制难题，本文提出一种结合大语言模型（LLM）与强化学习（RL）的混合驱动两阶段控制方法。LLM代理在日前阶段基于粗略预测调度有载调压变压器和电容器，RL代理在日内阶段基于实时测量优化光伏逆变器无功功率。该方法通过LLM的推理能力和RL的实时响应，显著提升了电压控制效率和性能。
selection_source: fresh_fetch
motivation: 传统数据驱动方法在处理异构信息和减少试错成本方面存在局限，难以满足实际配电网的复杂运行需求。
method: 提出一种LLM与RL协作的两阶段框架，包含LLM的自我进化机制和RL的预训练-微调流程，实现日前全局调度与日内局部精细调节。
result: 实验结果表明，该方法在训练效率和电压控制精度上均优于现有方法，且更符合实际运行特性。
conclusion: LLM与RL的协同工作能有效利用先验知识与实时数据，为主动配电网的电压稳定性提供了一种高效且实用的解决方案。
---

## 摘要
随着分布式光伏（PV）在主动配电网（ADN）中集成度的不断提高，运行挑战日益加剧，协调各种设备以缓解电压越限并提高电能质量变得至关重要。尽管现有的数据驱动方法在电压控制问题上已证明了其有效性，但它们通常需要大量的试错探索，且难以整合异构信息，如日前预测和基于语义的电网规范。考虑到实际 ADN 的运行场景和需求，本文提出了一种知识-数据驱动混合方法，利用大语言模型（LLM）智能体与强化学习（RL）智能体之间的动态协作来实现两阶段电压控制。在日前阶段，LLM 智能体接收粗粒度的区域级预测，并生成有载调压变压器（OLTC）和并联电容器（SC）的调度策略，以调节整体电压分布。随后在日内阶段，基于精确的节点级测量，RL 智能体通过推导光伏逆变器的无功功率生成策略来精细调节终端电压。在 LLM-RL 协作框架的基础上，我们进一步为 LLM 智能体提出了自进化机制，并为 RL 智能体提出了预训练-微调流水线，从而有效地增强并协调了两个智能体的策略。所提方法不仅更贴合实际运行特性，还充分利用了 LLM 智能体固有的知识和推理能力，显著提高了训练效率和电压控制性能。全面的对比实验和消融研究证明了所提方法的有效性。

## Abstract
The growing integration of distributed photovoltaics (PVs) into active distribution networks (ADNs) has exacerbated operational challenges, making it imperative to coordinate diverse equipment to mitigate voltage violations and enhance power quality. Although existing data-driven approaches have demonstrated effectiveness in the voltage control problem, they often require extensive trial-and-error exploration and struggle to incorporate heterogeneous information, such as day-ahead forecasts and semantic-based grid codes. Considering the operational scenarios and requirements in real-world ADNs, in this paper, we propose a hybrid knowledge-data-driven approach that leverages dynamic collaboration between a large language model (LLM) agent and a reinforcement learning (RL) agent to achieve two-stage voltage control. In the day-ahead stage, the LLM agent receives coarse region-level forecasts and generates scheduling strategies for on-load tap changer (OLTC) and shunt capacitors (SCs) to regulate the overall voltage profile. Then in the intra-day stage, based on accurate node-level measurements, the RL agent refines terminal voltages by deriving reactive power generation strategies for PV inverters. On top of the LLM-RL collaboration framework, we further propose a self-evolution mechanism for the LLM agent and a pretrain-finetune pipeline for the RL agent, effectively enhancing and coordinating the policies for both agents. The proposed approach not only aligns more closely with practical operational characteristics but also effectively utilizes the inherent knowledge and reasoning capabilities of the LLM agent, significantly improving training efficiency and voltage control performance. Comprehensive comparisons and ablation studies demonstrate the effectiveness of the proposed method.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：基于 LLM-RL 协作的两阶段主动配电网电压控制

#### 1. 核心问题与整体含义（研究动机和背景）
随着分布式光伏（PV）的大规模接入，主动配电网（ADN）面临严重的电压越限和波动挑战。现有的数据驱动方法（如强化学习 RL）虽然在电压控制中表现出色，但在实际应用中存在四大瓶颈：
*   **信息异构与不完整**：日前阶段仅有粗粒度的区域级预测，且包含非结构化的历史记录或报告，传统 RL 难以处理。
*   **语义化运行约束**：电网运行规程（如“OLTC 每日调节次数上限”）通常以语义形式存在，RL 需复杂的奖励设计或动作掩码才能处理。
*   **长尾指令适应性差**：RL 难以理解自然语言指令，且对未见场景的泛化能力弱。
*   **策略改进方向模糊**：RL 仅依赖标量奖励，缺乏明确的逻辑推理来指导策略进化。

**核心目标**：提出一种知识-数据驱动的混合框架，利用大语言模型（LLM）的推理能力处理日前调度，利用 RL 的快速响应处理日内控制。

#### 2. 方法论：核心思想与技术细节
论文提出了 **LLM-RL 协作框架**，将电压控制分为两个阶段：
*   **日前阶段（LLM 智能体）**：
    *   **输入**：每小时区域级负荷/光伏预测、语义化电网规程、历史相似案例。
    *   **核心机制**：采用 **Reflexion（自我反思）机制**。LLM 通过“执行-评估-反思-记忆”的循环，根据环境反馈（电压分布和奖励）调整 OLTC（有载调压变压器）和 SC（并联电容器）的调度计划。
    *   **知识库更新**：建立历史知识库，通过计算时间序列相似度和幅值相似度，检索最相关的案例作为 Few-shot 示例嵌入 Prompt。
*   **日内阶段（RL 智能体）**：
    *   **输入**：实时节点级测量数据、LLM 预先确定的机械设备状态。
    *   **核心机制**：采用 **PPO 算法** 优化光伏逆变器的无功功率。
    *   **关键技术**：提出 **“预训练-微调”（Pretrain-Finetune）流水线**。RL 先在随机环境下预训练以获得通用控制能力，再在 LLM 确定的特定策略环境下进行微调，加速两者之间的协调收敛。

#### 3. 实验设计
*   **测试场景**：标准 IEEE 33 节点系统和 141 节点系统。
*   **数据来源**：使用为期一年的真实光伏和负荷数据进行潮流模拟。
*   **对比方法（Benchmarks）**：
    1.  **Original**：无主动控制。
    2.  **No-LLM / No-RL**：缺失某一阶段控制的消融实验。
    3.  **Pure-RL**：日前和日内均由 RL 智能体负责（加入动作掩码和惩罚项以处理约束）。
    4.  **No-PT**：无预训练的 RL。
    5.  **No-Reflexion**：LLM 不进行自我反思改进。

#### 4. 资源与算力
*   **硬件环境**：2.2GHz Intel Core i9-14900HX CPU，32GB RAM。
*   **模型使用**：LLM 采用了 **Qwen-Plus**（版本 2025-12-01）。
*   **算力说明**：文中**未明确提到使用 GPU**，考虑到 LLM 是通过 API 调用（Qwen-Plus），主要的本地计算压力在于 RL 的训练和潮流计算。实验在高性能笔记本/工作站级别即可完成。

#### 5. 实验数量与充分性
*   **实验规模**：在两个不同规模的配电网系统上进行了测试，涵盖了全年的运行数据。
*   **重复性与客观性**：每个实验使用 **3 个独立随机种子**运行，并绘制了带有误差阴影的训练曲线。
*   **消融实验**：针对 LLM 的反思机制、RL 的预训练机制以及协作框架的必要性均做了详细的消融对比，实验设计较为充分且逻辑严密。

#### 6. 主要结论与发现
*   **LLM 优势显著**：在日前调度任务中，LLM 凭借内置的电力系统先验知识，初始策略即优于训练后的 RL，且能更自然地处理语义约束。
*   **协作增效**：LLM-RL 协作框架在降低电压偏差和消除电压越限方面均优于纯 RL 方法。
*   **训练效率提升**：Reflexion 机制显著提高了 LLM 的“样本效率”，而预训练-微调流水线使 RL 能在几百个回合内迅速适应 LLM 的决策。
*   **鲁棒性**：即使在日前预测存在 5% 噪声的情况下，该框架依然能保持良好的控制性能。

#### 7. 优点（亮点）
*   **混合驱动范式**：成功将 LLM 的“逻辑推理/语义理解”与 RL 的“精确数值计算/快速响应”结合，解决了传统方法难以处理非结构化约束的痛点。
*   **自进化能力**：引入 Reflexion 机制，使 LLM 能够像 RL 一样从失败中学习，但具有更强的可解释性。
*   **贴近实战**：明确区分了日前粗粒度信息和日内精细化信息的差异，符合电网实际调度流程。

#### 8. 不足与局限
*   **计算成本与延迟**：虽然日内 RL 响应极快，但日前阶段 LLM 的多次反思迭代（Reflexion）依赖 API 调用，可能存在网络延迟或较高的 Token 成本。
*   **LLM 数值局限性**：论文承认 LLM 在精确数值计算上较弱，虽然通过 RL 进行了补偿，但在更复杂的拓扑下，LLM 对潮流的直观感知可能受限。
*   **Prompt 依赖性**：系统性能高度依赖 Prompt 的设计，对于不同规模的电网，可能需要重新调整 Prompt 模板。
*   **安全性验证**：虽然 LLM 能理解规程，但在极端故障场景下的“幻觉”风险（生成错误指令）仍需更严格的硬性安全约束机制来兜底。

（完）
