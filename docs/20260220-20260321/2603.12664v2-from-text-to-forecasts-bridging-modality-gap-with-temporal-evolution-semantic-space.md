---
title: "From Text to Forecasts: Bridging Modality Gap with Temporal Evolution Semantic Space"
title_zh: 从文本到预测：利用时间演化语义空间弥合模态差距
authors: "Lehui Li, Yuyao Wang, Jisheng Yan, Wei Zhang, Jinliang Deng, Haoliang Sun, Zhongyi Han, Yongshun Gong"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12664v2"
tags: ["query:llm-ts"]
score: 10.0
evidence: 弥合文本与时间序列之间的模态间隙
tldr: "本研究针对时间序列预测中由于文本与数值模态差异导致的融合难题，提出了TESS框架。该框架通过引入“时间演化语义空间”作为中间瓶颈，利用大语言模型从文本中提取均值偏移、波动、形状和滞后等可解释的数值化时间原语，并配合置信度门控机制进行过滤。实验证明，TESS在多个真实数据集上显著优于现有基准，最高降低了29%的预测误差，为多模态时间序列预测提供了新思路。"
selection_source: fresh_fetch
motivation: 文本描述的定性特征与时间序列的定量信号之间存在模态鸿沟，导致现有模型难以有效捕捉文本中的数值线索。
method: 提出TESS框架，利用LLM提取均值偏移、波动等可解释的时间演化原语作为中间语义空间，并采用置信度门控进行筛选。
result: "在四个真实数据集上的实验显示，该方法相比现有最先进模型最高可降低29%的预测误差。"
conclusion: 通过构建结构化的时间演化语义空间，可以有效弥合文本与数值序列间的模态差异，显著提升预测性能。
---

## 摘要
将文本信息融入时间序列预测有望解决事件驱动的非平稳性；然而，一个根本性的模态差距阻碍了有效的融合：文本描述隐式且定性地表达时间影响，而预测模型则依赖于显式且定量的信号。通过受控的半合成实验，我们表明现有方法过度关注冗余标记，且难以可靠地将文本语义转化为可用的数值线索。为了弥合这一差距，我们提出了 TESS，它引入了一个时间演化语义空间作为模态之间的中间瓶颈。该空间由可解释的、具有数值基础的时间原语（均值偏移、波动性、形状和滞后）组成，这些原语由大语言模型（LLM）通过结构化提示从文本中提取，并通过置信度感知门控进行过滤。在四个真实世界数据集上的实验表明，与最先进的单模态和多模态基准模型相比，预测误差降低了高达 29%。代码将在论文接收后发布。

## Abstract
Incorporating textual information into time-series forecasting holds promise for addressing event-driven non-stationarity; however, a fundamental modality gap hinders effective fusion: textual descriptions express temporal impacts implicitly and qualitatively, whereas forecasting models rely on explicit and quantitative signals. Through controlled semi-synthetic experiments, we show that existing methods over-attend to redundant tokens and struggle to reliably translate textual semantics into usable numerical cues. To bridge this gap, we propose TESS, which introduces a Temporal Evolution Semantic Space as an intermediate bottleneck between modalities. This space consists of interpretable, numerically grounded temporal primitives (mean shift, volatility, shape, and lag) extracted from text by an LLM via structured prompting and filtered through confidence-aware gating. Experiments on four real-world datasets demonstrate up to a 29 percent reduction in forecasting error compared to state-of-the-art unimodal and multimodal baselines. The code will be released after acceptance.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **TESS**（Temporal Evolution Semantic Space）的新型多模态时间序列预测框架，旨在解决文本信息与数值序列之间存在的“模态鸿沟”问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态时间序列预测方法（融合新闻、社交媒体等文本）虽然有潜力应对事件驱动的“非平稳性”（如突发事件导致的趋势突变），但存在严重的**模态鸿沟**。
*   **研究动机**：
    *   **信息不对称**：文本是定性、隐式且冗余的；时间序列是定量、显式且结构化的。
    *   **注意力分散**：作者通过半合成实验发现，现有模型容易被文本中的冗余Token干扰，无法聚焦于预测相关的信号。
    *   **表征失配**：即使去除了冗余，模型也难以直接将“显著上涨”等定性描述转化为精确的数值增幅。

### 2. 方法论：TESS 框架
核心思想是引入一个**中间瓶颈层**——“时间演化语义空间”，将非结构化文本压缩为结构化的“时间原语”。
*   **关键技术细节**：
    1.  **定义时间原语（TSPs）**：预定义了四类具有数值基础的可解释原语：
        *   **均值偏移（Mean Shift）**：水平变化的幅度和方向。
        *   **波动性（Volatility）**：方差的变化。
        *   **形状（Shape）**：趋势形态（如上升、峰值、震荡等）。
        *   **滞后与衰减（Lag & Decay）**：影响发生的时间点和持续性。
    2.  **文本到原语的投影**：利用冻结的大语言模型（LLM）通过结构化提示（Prompting）对文本进行分类，提取上述原语。
    3.  **置信度感知门控（Confidence-aware Gating）**：由于LLM提取可能存在错误，引入一个可学习的门控网络，根据LLM输出的对数概率边际（Margin）评估可靠性，对不可靠的原语进行软加权抑制。
    4.  **原语条件预测**：以 PatchTST 为骨干网络，将提取并过滤后的语义向量作为“前缀 Token”注入 Transformer，引导数值预测。

### 3. 实验设计
*   **数据集**：
    *   **金融领域**：FNSPID（综合股票新闻）、Bitcoin（比特币价格）。
    *   **通用领域**：Electricity（用电量）、Environment（空气质量）。
*   **对比基准（Baselines）**：
    *   **单模态模型**：TimeMixer, TSMixer, Non-stationary Transformer, TimesNet, FEDformer, PatchTST 等。
    *   **多模态模型**：TimeLLM, ChatTime, NewsForecasting。
*   **实验场景**：涵盖了形状转换、波动变化和均值偏移三种典型的非平稳场景。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 80GB GPU**。
*   **软件实现**：基于 PyTorch，使用 AdamW 优化器。
*   **训练细节**：学习率在 1e-4 到 1e-3 之间搜索，采用早停机制（Patience=10）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个真实数据集上进行了详尽的性能对比。
*   **消融实验**：
    *   验证了 TESS 整体及门控机制的必要性。
    *   验证了四种不同时间原语的互补性（均值偏移对性能提升贡献最大）。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了最终指标，还通过半合成实验诊断了现有模型的失败原因，并展示了训练收敛曲线和案例分析，证明了方法的鲁棒性和公平性。

### 6. 主要结论与发现
*   **性能提升**：TESS 在金融数据集上表现尤为突出，相比最强基准模型，MSE（均方误差）最高降低了 **29%**。
*   **弥合鸿沟**：通过将文本转化为数值化的原语，模型能更有效地利用外部信息，且训练过程比直接融合文本更稳定、收敛更快。
*   **门控有效性**：实验证明门控机制能准确识别 LLM 提取错误的情况，并赋予较低的权重。

### 7. 优点与亮点
*   **可解释性强**：将模糊的文本转化为明确的物理意义（如均值、波动），使多模态融合过程不再是黑盒。
*   **诊断深入**：论文开篇通过受控实验揭示了“注意力干扰”和“表征失配”两个根本瓶颈，为后续方法论提供了坚实的理论支撑。
*   **即插即用**：该语义空间逻辑可以适配多种时间序列骨干网络。

### 8. 不足与局限
*   **LLM 依赖性**：原语提取的质量高度依赖于 LLM 的推理能力，对于极度专业或生僻的领域，LLM 可能无法准确映射。
*   **原语定义的完备性**：虽然涵盖了四类主要特征，但对于某些特定任务（如具有复杂周期性的信号），可能需要定义更细粒度的原语。
*   **推理开销**：虽然 LLM 是冻结的，但在推理阶段仍需调用 LLM 进行文本处理，这比纯数值模型增加了计算延迟。

（完）
