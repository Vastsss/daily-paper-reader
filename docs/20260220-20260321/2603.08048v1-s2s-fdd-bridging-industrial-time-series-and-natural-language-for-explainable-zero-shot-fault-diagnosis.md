---
title: "S2S-FDD: Bridging Industrial Time Series and Natural Language for Explainable Zero-shot Fault Diagnosis"
title_zh: S2S-FDD：连接工业时间序列与自然语言，实现可解释的零样本故障诊断
authors: "Baoxue Li, Chunhui Zhao"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08048v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 连接工业时间序列与自然语言
tldr: 针对工业故障诊断中传统模型缺乏可解释性及大语言模型难以处理高维传感器信号的问题，本文提出S2S-FDD框架。该框架通过信号到语义算子将时间序列转化为自然语言描述，并结合多轮树状诊断方法，利用历史文档和动态查询实现可解释的零样本故障诊断，支持人工反馈优化，在多相流过程实验中验证了其有效性。
selection_source: fresh_fetch
motivation: 传统故障诊断模型输出过于抽象，且大语言模型在处理高维工业时间序列信号时存在语义鸿沟。
method: 提出S2S-FDD框架，利用信号到语义算子生成文本摘要，并结合多轮树状结构与历史文档进行动态诊断。
result: 在多相流过程上的实验证明了该方法在实现可解释零样本故障诊断方面的可行性与有效性。
conclusion: 该研究成功桥接了工业信号与自然语言语义，为工业系统提供了一种具备推理能力和人工交互特性的新型诊断范式。
---

## 摘要
故障诊断对于工业系统的安全运行至关重要。传统的诊断模型通常产生抽象的输出，如异常评分或故障类别，无法回答诸如“为什么”或“如何修复”等关键运维问题。虽然大语言模型（LLMs）具有强大的泛化和推理能力，但它们在离散文本语料库上的训练使得在处理高维、时序工业信号时存在语义鸿沟。为了应对这一挑战，我们提出了一个信号到语义故障诊断（S2S-FDD）框架，通过两项关键创新将高维传感器信号与自然语言语义连接起来：首先，我们设计了一个信号到语义算子，将抽象的时间序列信号转换为自然语言摘要，捕捉趋势、周期性和偏差。基于这些描述，我们设计了一种多轮树状结构诊断方法，通过参考历史维护文档并动态查询额外信号来进行故障诊断。该框架进一步支持人机回环反馈，以进行持续优化。在多相流过程上的实验证明了所提方法在可解释零样本故障诊断方面的可行性和有效性。

## Abstract
Fault diagnosis is critical for the safe operation of industrial systems. Conventional diagnosis models typically produce abstract outputs such as anomaly scores or fault categories, failing to answer critical operational questions like "Why" or "How to repair". While large language models (LLMs) offer strong generalization and reasoning abilities, their training on discrete textual corpora creates a semantic gap when processing high-dimensional, temporal industrial signals. To address this challenge, we propose a Signals-to-Semantics fault diagnosis (S2S-FDD) framework that bridges high-dimensional sensor signals with natural language semantics through two key innovations: We first design a Signal-to-Semantic operator to convert abstract time-series signals into natural language summaries, capturing trends, periodicity, and deviations. Based on the descriptions, we design a multi-turn tree-structured diagnosis method to perform fault diagnosis by referencing historical maintenance documents and dynamically querying additional signals. The framework further supports human-in-the-loop feedback for continuous refinement. Experiments on the multiphase flow process show the feasibility and effectiveness of the proposed method for explainable zero-shot fault diagnosis.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **S2S-FDD** 的框架，旨在通过大语言模型（LLM）桥接工业时间序列信号与自然语言语义，实现可解释的零样本（Zero-shot）故障诊断。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的工业故障诊断模型（如深度学习或统计方法）输出过于抽象（仅提供异常评分或类别），无法解释“为什么发生故障”或“如何维修”。
*   **研究背景**：虽然 LLM 具备强大的推理能力，但其训练数据主要是离散文本，而工业传感器信号是高维、连续且动态的时间序列，两者之间存在巨大的**语义鸿沟**。
*   **整体含义**：论文试图建立一种机制，将原始传感器信号转化为 LLM 可理解的文本描述，并结合领域知识实现无需故障样本训练的、具备解释能力的诊断流程。

### 2. 方法论
该框架主要由两个核心组件构成：
*   **信号到语义（S2S）算子**：
    *   **重构机制**：利用正常工况下的数据通过 K-means 聚类提取代表性样本，构建状态矩阵 $D$。对于输入信号 $W_{in}$，通过最小二乘法计算权重向量 $\omega$，得到重构信号 $W_{out} = D\omega$。
    *   **异常识别**：通过计算原始信号与重构信号的残差（Residual）来识别异常。论文还从理论上证明了残差能量边界与故障可检测性之间的关系。
    *   **文本转化**：将识别出的异常趋势、周期性、偏差百分比等数值信息，根据预设模板转化为自然语言摘要（如“测量值高于理想值”、“呈现下降趋势”等）。
*   **多轮树状结构诊断方法**：
    *   **知识检索**：将生成的文本描述与历史维护记录（知识库）进行向量匹配，检索相关故障案例。
    *   **动态推理**：LLM 根据初始描述和检索知识进行推理。如果信息不足，LLM 可以通过“函数调用（Function Calling）”主动请求其他传感器的详细数据。
    *   **人机回环**：支持专家对诊断报告进行验证和修正，并将结果反馈至知识库，实现闭环优化。

### 3. 实验设计
*   **数据集/场景**：使用了英国克兰菲尔德大学（Cranfield University）的**多相流过程（Multiphase Flow Process）**实验设施数据。该设施模拟了水、油、气混合输送的工业环境。
*   **实验设置**：选取了 5 种典型的工业故障（如气路堵塞、水路堵塞、分离器堵塞等），共 13 个测试案例。
*   **对比方法（Benchmark）**：
    *   **非推理模型**：Qwen2.5 (7B/72B), DeepSeek-V3。
    *   **推理模型（Reasoning LLMs）**：DeepSeek-R1-Distill-Qwen (7B/32B), QwQ-32B, DeepSeek-R1。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）或训练/推理的具体时长。由于该方法侧重于零样本推理和 Prompt Engineering，其核心开销主要集中在 LLM 的 API 调用或本地部署推理上，而非大规模模型训练。

### 5. 实验数量与充分性
*   **实验规模**：针对 13 个故障案例，每个案例重复进行 5 次推理，并通过投票机制确定最终诊断结果。
*   **充分性评价**：
    *   实验覆盖了不同规模（7B 到 671B 参数）和不同类型（通用型 vs 推理型）的 LLM，对比维度较为全面。
    *   虽然案例数量（13个）在统计学上不算庞大，但涵盖了多相流过程中的主要故障类型，足以验证“信号转语义”这一路径的可行性。
    *   缺乏与其他非 LLM 驱动的零样本诊断方法的直接数值对比。

### 6. 主要结论与发现
*   **有效性**：S2S-FDD 框架在完全没有故障样本训练的情况下，最高达到了 **76.92%** 的诊断准确率（使用 DeepSeek-R1）。
*   **推理能力的重要性**：推理型 LLM（如 DeepSeek-R1）的表现显著优于普通指令微调模型（如 Qwen2.5-7B），因为前者能通过思维链（CoT）排除矛盾项并结合工艺流程进行逻辑推演。
*   **参数规模效应**：在推理模型中，参数量越大（如从 7B 到 32B 再到满血版 R1），诊断的准确性和逻辑严密性越高。

### 7. 优点（亮点）
*   **桥接能力**：通过重构残差算子，巧妙地将连续数值信号降维并转化为具备物理意义的文本，解决了 LLM 无法直接处理高维工业数据的问题。
*   **可解释性强**：诊断输出不仅包含故障类别，还包含详细的推理过程（如“传感器 A 下降而阀门开度增加，说明存在堵塞”），符合工业运维需求。
*   **零样本特性**：仅需少量正常数据构建基准，无需昂贵且难以获取的故障标注数据。

### 8. 不足与局限
*   **实时性挑战**：多轮对话和树状推理机制可能导致诊断延迟，对于需要毫秒级响应的工业场景可能不适用。
*   **描述精度损失**：将复杂的时间序列简化为 100 字以内的文本摘要，可能会丢失某些细微的动态特征（如高频振动特征）。
*   **依赖 Prompt 质量**：诊断效果高度依赖于 [PROCESS_INFO] 等背景知识的完备性，如果工艺描述不准确，LLM 容易产生幻觉。
*   **实验场景单一**：目前仅在多相流过程这一特定实验装置上验证，其在更复杂、更大规模工业系统中的泛化能力有待观察。

（完）
