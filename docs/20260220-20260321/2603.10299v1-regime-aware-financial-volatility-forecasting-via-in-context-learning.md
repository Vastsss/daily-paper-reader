---
title: Regime-aware financial volatility forecasting via in-context learning
title_zh: 基于上下文学习的状态感知金融波动率预测
authors: "Saba Asaad, Shayan Mohajer Hamidi, Ali Bereyhi"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10299v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 利用大语言模型通过上下文学习进行金融波动率预测
tldr: 本研究针对非平稳市场环境，提出了一种基于大语言模型（LLM）的情境感知（Regime-aware）上下文学习框架，用于金融波动率预测。该方法利用预训练LLM对历史波动模式进行推理，无需参数微调。通过预言机引导的精炼程序构建情境感知示例，并根据估计的市场标签进行条件采样，使模型能通过上下文推理适应不同市场状态下的波动动态。实验证明，该框架在多个金融数据集上优于传统方法和直接单次学习，尤其在高波动时期表现卓越。
selection_source: fresh_fetch
motivation: 传统金融波动率预测方法难以应对非平稳市场环境下的状态切换和动态变化。
method: 利用预训练大语言模型的上下文学习能力，结合预言机引导的条件示例采样，实现无需微调的情境感知预测。
result: 在多个金融数据集上的实验表明，该方法在预测精度上优于经典模型，且在市场剧烈波动时表现尤为稳健。
conclusion: 证明了LLM仅通过上下文推理即可有效捕捉金融市场的状态依赖动态，为非平稳时间序列预测提供了新思路。
---

## 摘要
本研究提出了一种状态感知的上下文学习框架，利用大语言模型（LLMs）在非平稳市场条件下进行金融波动率预测。该方法部署预训练的 LLMs 对历史波动模式进行推理，并在无需参数微调的情况下调整其预测。我们开发了一种由 Oracle 引导的精炼程序，从训练数据中构建状态感知的示例。随后，LLM 被部署为上下文学习器，利用根据估计的市场标签进行条件采样的示例，从输入序列中预测下一步的波动率。这种条件采样策略使 LLM 能够仅通过上下文推理，使其预测适应与状态相关的波动率动态。在多个金融数据集上的实验表明，所提出的状态感知上下文学习框架优于传统的波动率预测方法和直接的单样本学习，尤其是在高波动时期。

## Abstract
This work introduces a regime-aware in-context learning framework that leverages large language models (LLMs) for financial volatility forecasting under nonstationary market conditions. The proposed approach deploys pretrained LLMs to reason over historical volatility patterns and adjust their predictions without parameter fine-tuning. We develop an oracle-guided refinement procedure that constructs regime-aware demonstrations from training data. An LLM is then deployed as an in-context learner that predicts the next-step volatility from the input sequence using demonstrations sampled conditional to the estimated market label. This conditional sampling strategy enables the LLM to adapt its predictions to regime-dependent volatility dynamics through contextual reasoning alone. Experiments with multiple financial datasets show that the proposed regime-aware in-context learning framework outperforms both classical volatility forecasting approaches and direct one-shot learning, especially during high-volatility periods.

---

## 论文详细总结（自动生成）

### 论文总结：基于上下文学习的状态感知金融波动率预测

#### 1. 核心问题与整体含义（研究动机和背景）
金融波动率预测是风险管理和投资组合分配的核心，但其面临的主要挑战在于市场的**非平稳性（Non-stationarity）**和**状态切换（Regime Shifts）**。传统的计量经济学模型（如 GARCH）和深度学习序列模型在市场剧烈波动或结构性变化时，预测精度往往大幅下降。
本文探讨了大语言模型（LLM）作为**上下文学习器（In-context Learner）**在金融预测中的潜力。研究动机在于：LLM 具备强大的数值推理和模式识别能力，能否通过精心设计的“状态感知”提示词（Prompts），在不进行参数微调的情况下，实时捕捉并适应不同的市场波动状态？

#### 2. 方法论：核心思想与关键技术
该框架的核心思想是利用预训练 LLM 的推理能力，结合“预言机（Oracle）”引导的示例精炼技术，实现状态感知的预测。

*   **核心流程：**
    1.  **输入表示：** 将过去 $w$ 天的对数收益率 $r_t$ 和已实现方差 $\nu_t$ 转化为结构化文本 Prompt。
    2.  **预言机引导的精炼（Oracle-guided Refinement）：** 在训练阶段，LLM 先进行初始预测，随后由 Oracle 提供反馈（包括真实值、预测误差和启发式修正建议）。通过多轮迭代，生成高质量的预测示例。
    3.  **状态感知示例池（Regime-labeled Pool）：** 根据波动率水平（高/低）为精炼后的示例打上标签，构建分类示例池。
    4.  **条件采样预测：**
        *   **Proposal I（固定先验）：** 按固定比例从高/低波动池中抽取示例。
        *   **Proposal II（估计标签）：** 根据测试输入近期的波动均值实时估计当前市场状态，并据此动态调整示例的采样比例。
    5.  **推理：** 将选定的示例与当前输入拼接，由 LLM 输出下一步波动率预测 $\hat{\nu}_{t+1}$。

#### 3. 实验设计
*   **数据集：** 涵盖了三种不同特性的金融资产：
    *   **S&P500 指数：** 代表大盘股市场。
    *   **NASDAQ 指数：** 代表高增长/科技股市场。
    *   **EUR/USD 汇率：** 代表高流动性的外汇市场。
*   **Benchmark（对比方法）：**
    *   **传统模型：** 滚动平均（Rolling Mean）、异构自回归模型（HAR）、GARCH(1,1)、GJR-GARCH（考虑非对称效应）。
    *   **LLM 基准：** 直接单次学习（One-shot learning，无示例）。
    *   **消融变体：** 随机示例选择（Random selection）。

#### 4. 资源与算力
*   **模型：** 实验主要基于 `gpt-4o-mini` 作为基础模型。
*   **算力说明：** 论文**未明确说明**具体的本地 GPU 型号或训练时长。由于该方法属于“In-context Learning”，核心计算压力在于调用 LLM API 的推理过程，而非本地模型训练。示例池构建涉及 500 个样本的 3 轮迭代精炼。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 3 个跨类别数据集上进行了测试，每个数据集均划分了 70% 训练（用于构建示例池）和 30% 测试。
*   **评估维度：** 采用了 MAE（平均绝对误差）和 RMSE（均方根误差），并专门针对“高波动”和“低波动”状态进行了分段 MAE 评估。
*   **充分性评价：** 实验设计较为客观，对比了金融领域最经典的基准模型。通过分段评估证明了该方法在“高波动”这一难点场景下的优越性，实验结果具有统计说服力。

#### 6. 主要结论与发现
*   **性能超越：** 状态感知的 ICL 框架在所有数据集上均优于经典模型。在 S&P500 数据集上，整体预测误差较最强的经典基准降低了约 **27%**。
*   **高波动适应性：** 该方法最大的优势在于高波动时期（MAE_high 显著降低），证明了 LLM 能够通过上下文示例识别出市场状态的转变。
*   **权衡关系：** 实验观察到类似于 GARCH 模型的权衡——提高对高波动状态的适应性可能会轻微牺牲低波动时期的精度，但整体误差（RMSE/MAE）依然更优。

#### 7. 优点（亮点）
*   **无需微调：** 纯 Prompt-based 方法，避免了金融数据稀疏导致的过拟合风险，且易于部署。
*   **Oracle 闭环：** 创新性地引入 Oracle 反馈机制来“离线”提升示例质量，解决了 LLM 直接预测数值不准的问题。
*   **状态显式建模：** 通过条件采样将金融领域的“状态切换”理论与 LLM 的上下文学习有效结合。

#### 8. 不足与局限
*   **API 依赖与成本：** 依赖闭源模型 API，对于高频预测场景，Token 消耗和延迟可能是瓶颈。
*   **阈值敏感性：** 状态划分的阈值 $\tau$ 依赖于历史数据的分位数，若未来市场发生极端结构性变化（超出历史范围），该阈值可能失效。
*   **示例选择策略：** 目前采用的是基于标签的随机采样，未来若引入基于相似度的检索（Retrieval-based）可能会进一步提升精度。
*   **数据泄露风险：** 虽然文中强调了测试过程无泄露，但预训练 LLM 本身可能在训练数据中见过相关的历史金融指数。

（完）
