---
title: Interventional Time Series Priors for Causal Foundation Models
title_zh: 因果基础模型的干预性时间序列先验
authors: "Dennis Thumm, Ying Chen"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11090v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 时间序列的因果基础模型
tldr: 针对时间序列因果推理缺乏干预数据的问题，本文提出了 CausalTimePrior 框架。该框架能生成包含观测和干预配对数据的合成时间结构因果模型（TSCM），支持非线性动力学、机制切换及多种干预类型。通过在该先验数据上训练先验数据拟合网络（PFN），实现了在未知模型上的上下文因果效应估计，为构建时间序列因果基础模型奠定了基础。
selection_source: fresh_fetch
motivation: 现有时间序列基准仅提供观测数据和因果图，缺乏训练因果基础模型所需的干预目标数据。
method: 开发了 CausalTimePrior 框架，通过合成具有复杂非线性动力学和多种干预模式的时间结构因果模型来生成训练数据。
result: 在合成先验数据上训练的 PFN 模型能够成功在未见过的测试模型上执行上下文因果效应估计。
conclusion: 该研究通过提供原则性的合成数据生成方案，为时间序列领域的因果基础模型开发开辟了新路径。
---

## 摘要
先验数据拟合网络 (PFNs) 已成为表格因果推断的强大基础模型，但由于缺乏提供干预目标的合成数据生成器，其在时间序列上的扩展仍然受到限制。现有的时间序列基准虽然生成了具有真实因果图的观测数据，但缺乏训练因果基础模型所需的干预数据。为了解决这一问题，我们提出了 CausalTimePrior，这是一个用于生成具有成对观测和干预时间序列的合成时间结构因果模型 (TSCMs) 的规范化框架。我们的先验支持可配置的因果图结构、非线性自回归机制、状态切换动力学以及多种干预类型（硬干预、软干预、随时间变化的干预）。我们证明了在 CausalTimePrior 上训练的 PFNs 可以在留出的 TSCMs 上执行上下文内因果效应估计，从而为时间序列因果推断的基础模型开辟了道路。

## Abstract
Prior-data fitted networks (PFNs) have emerged as powerful foundation models for tabular causal inference, yet their extension to time series remains limited by the absence of synthetic data generators that provide interventional targets. Existing time series benchmarks generate observational data with ground-truth causal graphs but lack the interventional data required for training causal foundation models. To address this, we propose \textbf{CausalTimePrior}, a principled framework for generating synthetic temporal structural causal models (TSCMs) with paired observational and interventional time series. Our prior supports configurable causal graph structures, nonlinear autoregressive mechanisms, regime-switching dynamics, and multiple intervention types (hard, soft, time-varying). We demonstrate that PFNs trained on CausalTimePrior can perform in-context causal effect estimation on held-out TSCMs, establishing a pathway toward foundation models for time series causal inference.

---

## 论文详细总结（自动生成）

以下是对论文《Interventional Time Series Priors for Causal Foundation Models》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在表格数据领域，先验数据拟合网络（PFNs）已证明能通过合成数据预训练实现强大的因果推理能力。然而，在**时间序列**领域，现有的基准测试（如 CausalTime, TimeGraph）仅提供观测数据和因果图，缺乏**干预数据（Interventional Data）**。
*   **核心问题**：没有干预目标（即“如果对变量 A 进行干预，变量 B 会如何变化”的真实结果），就无法训练能够预测干预后果的时间序列基础模型。
*   **整体含义**：本文提出了 **CausalTimePrior** 框架，旨在通过生成包含“观测-干预”配对的时间结构因果模型（TSCMs），为训练时间序列因果基础模型提供必要的“燃料”。

### 2. 方法论
核心思想是构建一个能够采样多样化、非线性且包含干预逻辑的合成时间序列生成器。
*   **时间结构因果模型 (TSCM)**：基于离散时间无环图，定义变量 $X_t$ 由其瞬时父母节点、滞后父母节点以及噪声项决定。
*   **先验分布设计**：
    *   **图先验 ($\Pi_G$)**：采样变量数（3-10个）、最大滞后阶数，利用 Erdős-Rényi 模型生成图，并随滞后阶数增加衰减边概率。
    *   **机制先验 ($\Pi_F$)**：支持多种非线性函数（sin, cos, tanh, exp 等）的组合，确保动力学的多样性。
    *   **噪声先验 ($\Pi_\epsilon$)**：支持高斯、均匀和拉普拉斯分布。
*   **干预类型**：支持**硬干预**（do-算子，固定某值）、**软干预**（改变机制或增加扰动）和**随时间变化的干预**（如阶跃、正弦信号）。
*   **状态切换 (Regime-Switching)**：引入马尔可夫链驱动结构断点，模拟现实中因果关系随时间发生改变的情况（如金融危机导致的政策变化）。
*   **生成流程**：采样 TSCM -> 采样干预配置 -> 前向模拟生成观测序列 -> 再次模拟生成干预序列 -> 形成训练元组。

### 3. 实验设计
*   **数据集/场景**：生成了 10 万个 TSCMs 用于训练，1000 个留出模型用于测试。涵盖了简单非线性、链式结构和状态切换等场景。
*   **模型实现**：训练了一个简单的 2 层 GRU 编码器作为 PFN 的概念验证模型。
*   **对比方法 (Benchmarks)**：
    *   **VAR-OLS**：向量自回归模型（基于最小二乘法）。
    *   **PCMCI+**：经典的时间序列因果发现算法。
    *   **Mean Prediction**：均值预测基准。
*   **评估维度**：因果效应估计的 RMSE、MAE、效应方向准确率、效应大小相关性，以及针对非因果查询的区分能力。

### 4. 资源与算力
*   **训练效率**：该研究强调了高效性。在 **CPU**（支持 AVX2 指令集的 Intel/AMD 处理器）上训练 10 万个样本仅需约 **11 分钟**。
*   **模型规模**：模型权重文件仅为 **330KB**。
*   **GPU 使用**：文中未明确提到使用 GPU，主要展示了在轻量级硬件上的可行性。

### 5. 实验数量与充分性
*   **实验规模**：进行了 10 万量级的先验采样，验证了先验分布的稳定性（0% 发散率）和多样性。
*   **消融与泛化实验**：
    *   **干预类型消融**：对比了“混合干预训练”与“仅硬干预训练”，证明多样性有助于提升因果推理能力。
    *   **分布外 (OOD) 泛化**：在更大、更密集的图以及更复杂的非线性机制上测试模型，验证了其鲁棒性。
    *   **控制实验**：通过随机打乱干预目标的对照实验，确认模型确实学习了因果结构而非简单的统计分布。
*   **充分性评价**：作为研讨会论文（Workshop paper），实验设计较为全面且逻辑严密，但在模型架构（仅使用 GRU 而非 Transformer）和真实数据验证上仍有提升空间。

### 6. 主要结论与发现
*   **上下文因果推理可行**：PFN 能够仅通过观测数据作为上下文，在测试时预测未见过的干预结果。
*   **区分因果与相关**：模型能有效识别非因果路径。在存在虚假相关（Spurious Correlation）的情况下，PFN 的预测误差远低于传统的 VAR 模型（误差小 177 倍）。
*   **状态切换支持**：该框架是首个结合了状态切换动力学与干预数据生成的生成器。

### 7. 优点
*   **填补空白**：解决了时间序列因果基础模型缺乏干预训练数据的核心痛点。
*   **生成速度快**：采用解析式结构方程生成，无需像扩散模型（如 CaTSG）那样进行复杂的生成模型预训练。
*   **灵活性高**：支持从简单线性到复杂非线性、从静态到动态因果结构的广泛覆盖。

### 8. 不足与局限
*   **假设限制**：目前假设噪声为马尔可夫性且为离散时间动力学，尚未充分处理非马尔可夫混杂因素。
*   **图结构偏差**：Erdős-Rényi 随机图虽然覆盖广，但可能无法完全模拟现实世界中特定的因果拓扑（如特定的物理或生物网络结构）。
*   **缺乏真实数据验证**：虽然在合成数据上表现良好，但尚未在包含真实干预的时间序列数据集上进行基准测试。
*   **模型架构简单**：目前的 GRU 架构可能限制了处理长程依赖和复杂因果模式的能力，未来需扩展至 Transformer 架构。

（完）
