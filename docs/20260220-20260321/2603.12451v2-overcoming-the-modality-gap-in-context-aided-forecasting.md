---
title: Overcoming the Modality Gap in Context-Aided Forecasting
title_zh: 克服上下文辅助预测中的模态差距
authors: "Vincent Zhihao Zheng, Étienne Marcotte, Arjun Ashok, Andrew Robert Williams, Lijun Sun, Alexandre Drouin, Valentina Zantedeschi"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12451v2"
tags: ["query:llm-ts"]
score: 9.0
evidence: 文本上下文与时间序列数据之间的跨模态学习
tldr: 本研究针对上下文辅助预测（CAF）中多模态模型表现不如单模态模型的“模态差距”问题，提出数据集质量而非架构是主要瓶颈。研究者开发了一种半合成数据增强方法，生成了包含700万个时间序列窗口的大规模数据集CAF-7M，确保了上下文与数值历史的互补性。实验证明，该预训练方法能有效提升模型在现实任务中的表现，并显著增强了对上下文信息的利用能力。
selection_source: fresh_fetch
motivation: 现有的上下文辅助预测模型由于数据集质量差且难以验证，导致多模态性能往往无法超越单模态模型。
method: 提出一种半合成数据增强方法来生成与数值历史互补的高质量上下文，并构建了包含700万样本的CAF-7M数据集。
result: 实验证明半合成预训练能有效迁移至真实世界评估，并提供了模型有效利用上下文信息的明确证据。
conclusion: 研究表明数据集质量而非模型架构是上下文辅助预测的主要瓶颈，高质量数据能有效克服模态差距。
---

## 摘要
上下文辅助预测 (CAF) 在整合领域知识和前瞻性信息方面具有潜力，使 AI 系统能够超越传统的统计方法。然而，最近的实证研究揭示了一个令人困惑的差距：多模态模型往往无法超越其单模态对应模型。我们假设这种性能不佳源于现有数据集中上下文质量较差，因为验证具有挑战性。为了解决这些局限性，我们引入了一种半合成数据增强方法，该方法生成的上下文既能描述时间动态，又能被验证为与数值历史记录互补。这种方法能够实现大规模数据集的创建，从而产生了 CAF-7M，这是一个包含 700 万个上下文增强时间序列窗口的语料库，其中包括一个经过严格验证的测试集。我们证明了半合成预训练可以有效地迁移到现实世界的评估中，并展示了上下文利用的明确证据。我们的结果表明，数据集质量而非架构限制，一直是上下文辅助预测的主要瓶颈。

## Abstract
Context-aided forecasting (CAF) holds promise for integrating domain knowledge and forward-looking information, enabling AI systems to surpass traditional statistical methods. However, recent empirical studies reveal a puzzling gap: multimodal models often fail to outperform their unimodal counterparts. We hypothesize that this underperformance stems from poor context quality in existing datasets, as verification is challenging. To address these limitations, we introduce a semi-synthetic data augmentation method that generates contexts both descriptive of temporal dynamics and verifiably complementary to numerical histories. This approach enables massive-scale dataset creation, resulting in CAF-7M, a corpus of 7 million context-augmented time series windows, including a rigorously verified test set. We demonstrate that semi-synthetic pre-training transfers effectively to real-world evaluation, and show clear evidence of context utilization. Our results suggest that dataset quality, rather than architectural limitations, has been the primary bottleneck in context-aided forecasting.

---

## 论文详细总结（自动生成）

这是一篇关于上下文辅助预测（Context-Aided Forecasting, CAF）的深度研究论文。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管将外部文本信息（如新闻、报告）与时间序列（TS）结合具有巨大潜力，但现有研究发现多模态模型往往无法超越纯数值的单模态模型，这种现象被称为“模态差距”（Modality Gap）。
*   **研究动机**：作者假设性能瓶颈不在于模型架构，而在于**数据集质量**。现有数据集中的文本上下文往往与数值动态关联性弱、预测价值不明且难以验证。
*   **整体含义**：通过构建高质量、可验证的半合成数据集，证明只要数据足够好，多模态模型就能显著超越单模态基准。

### 2. 方法论：核心思想与技术细节
该论文提出了一个包含生成、验证和模型构建的完整框架：
*   **半合成数据增强（CAF-7M 构建）**：
    1.  **生成阶段**：利用大语言模型（Llama-3.3-70B）根据时间序列的历史和未来数值，生成能够解释动态变化（如趋势转变、异常波动）的虚构但合理的场景描述。
    2.  **验证阶段**：使用最先进的预测器（如 GPT-5.2）进行“回测”。只有当加入该文本上下文后，预测误差（CRPS 指标）显著降低的样本才会被保留。这确保了上下文与数值历史之间存在**互补性**。
*   **DoubleCast 架构**：
    *   **骨干网络**：基于 Chronos（T5 架构）时间序列基础模型。
    *   **文本编码器**：集成预训练的 Qwen3-14B 提取文本特征。
    *   **关键技术（DualT5 Decoder）**：在解码器的每个块中插入一个额外的**双重交叉注意力层**（DualT5 Cross-Attention），使模型在生成预测数值时能同时关注时间序列编码和文本上下文编码。

### 3. 实验设计
*   **数据集**：
    *   **CAF-7M**：包含 700 万个训练窗口，涵盖电力、交通、经济等 11 个领域、65 个原始数据集。
    *   **测试集**：包含 904 个经过严格验证的窗口，分为 HARD（单模态难以预测）和 EASY 两个子集。
    *   **现实世界基准**：ChatTime (CGTSF) 数据集，用于验证模拟到现实（Sim-to-Real）的迁移能力。
*   **对比方法**：
    *   **单模态基准**：Chronos (Large)。
    *   **多模态基准**：TimeLLM、ChatTime、Direct Prompt (DP) 方法（使用 GPT-5.2, Llama-3.3 等）。
    *   **消融实验**：无上下文（No Context）和交换上下文（Swapped Context）对比。

### 4. 资源与算力
*   **算力设备**：实验主要在单台 **NVIDIA H100 (80GB)** GPU 上完成。
*   **训练细节**：
    *   预训练步数：400,000 步。
    *   批量大小：8（梯度累积为 2，等效 Batch Size 为 16）。
    *   精度：使用 bfloat16 进行训练和推理。
    *   微调：在 CGTSF 任务上额外微调了 50,000 步。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模实验，包括 700 万样本的全量训练、不同比例（0.1% 到 100%）的数据缩放定律（Scaling Laws）分析。
*   **充分性与公平性**：
    *   实验涵盖了零样本（Zero-shot）和微调（Fine-tuning）两种场景。
    *   通过“交换上下文”实验排除了模型仅靠文本统计特性获益的可能性，证明了模型确实理解了上下文与特定序列的关联。
    *   在纯数值基准（GIFT-Eval）上测试，证明了加入多模态能力不会损害模型原有的数值预测能力。实验设计客观且对比严谨。

### 6. 主要结论与发现
*   **数据是关键**：数据集质量是 CAF 的主要瓶颈。使用经过验证的半合成数据训练，模型能显著超越单模态基准。
*   **有效迁移**：在合成数据 CAF-7M 上预训练的模型，在未见过的现实世界数据集（ChatTime）上表现优异，证明了合成场景的泛化价值。
*   **架构影响**：虽然数据重要，但架构也关键。DoubleCast 相比 TimeLLM 能更有效地利用上下文信息。
*   **缩放效应**：随着训练数据量的增加，模型利用上下文的能力持续提升，且在 HARD 任务上提升更明显。

### 7. 优点
*   **创新的验证机制**：通过预测性能提升来反向筛选文本质量，解决了 CAF 领域长期存在的“上下文是否有用”的量化难题。
*   **大规模资源贡献**：发布了 CAF-7M 数据集，为多模态时间序列研究提供了宝贵的基准。
*   **Sim-to-Real 验证**：成功证明了 LLM 生成的合成上下文可以作为现实世界预测任务的有效代理。

### 8. 不足与局限
*   **验证成本高昂**：使用 GPT-5.2 等顶级模型进行验证的成本极高，导致目前 700 万训练数据中大部分未经过最终验证（仅测试集经过严格验证）。
*   **缺乏细粒度对齐**：DoubleCast 架构虽然有效，但尚未实现文本中的特定时间点（如“第 3 天发生中断”）与预测窗口索引之间的显式对齐，可能导致时间定位不准。
*   **验证器偏差**：测试集的筛选依赖于特定的验证模型（GPT-5.2），可能存在模型偏好偏差，导致测试集对其他架构的评估不够全面。

（完）
