---
title: "Timer-S1: A Billion-Scale Time Series Foundation Model with Serial Scaling"
title_zh: Timer-S1：具有序列缩放能力的十亿级时间序列基础模型
authors: "Yong Liu, Xingjian Su, Shiyu Wang, Haoran Zhang, Haixuan Liu, Yuxuan Wang, Zhou Ye, Yang Xiang, Jianmin Wang, Mingsheng Long"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04791v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 十亿级参数的时间序列基础模型
tldr: Timer-S1是一个拥有83亿参数的大规模时间序列基础模型，采用混合专家（MoE）架构。为了解决现有模型的扩展性瓶颈，研究者提出了“串行缩放”策略，涵盖模型架构、万亿级数据集TimeBench及训练流程。通过引入串行标记预测（STP）目标，模型在保持推理效率的同时显著提升了长短期预测能力，在GIFT-Eval基准测试中取得了SOTA性能。
selection_source: fresh_fetch
motivation: 旨在克服现有预训练时间序列基础模型在模型规模、数据质量及长序列预测性能上的扩展性瓶颈。
method: 提出了包含TimeMoE和TimeSTP模块的架构，利用万亿级数据集TimeBench进行串行标记预测（STP）训练，并结合后期长上下文扩展。
result: Timer-S1在GIFT-Eval大规模排行榜上取得了预训练模型中最佳的MASE和CRPS评分，展现了卓越的预测性能。
conclusion: Timer-S1证明了通过串行缩放和高质量大规模数据，可以构建出具备强大泛化能力和长上下文处理能力的时间序列基础模型。
---

## 摘要
我们介绍了 Timer-S1，这是一个强大的混合专家（MoE）时间序列基础模型，拥有 83 亿总参数，每个 token 激活 7.5 亿参数，上下文长度为 11.5K。为了克服现有预训练时间序列基础模型中的可扩展性瓶颈，我们在模型架构、数据集和训练流水线三个维度上进行了序列缩放（Serial Scaling）。Timer-S1 集成了稀疏 TimeMoE 模块和通用的 TimeSTP 模块，用于序列 Token 预测（STP），这是一种遵循预测序列特性的通用训练目标。该范式引入了序列计算以改进长期预测，同时避免了标准下文预测（next-token prediction）中昂贵的滚动式推理和显著的误差累积。为了追求高质量且无偏的训练数据集，我们构建了包含一万亿个时间点的语料库 TimeBench，并应用精细的数据增强来减轻预测偏差。我们还开创了后训练阶段，包括持续预训练和长上下文扩展，以增强短期和长上下文性能。在大规模 GIFT-Eval 排行榜的评估中，Timer-S1 实现了最先进的预测性能，作为预训练模型获得了最佳的 MASE 和 CRPS 分数。Timer-S1 将被发布以促进进一步的研究。

## Abstract
We introduce Timer-S1, a strong Mixture-of-Experts (MoE) time series foundation model with 8.3B total parameters, 0.75B activated parameters for each token, and a context length of 11.5K. To overcome the scalability bottleneck in existing pre-trained time series foundation models, we perform Serial Scaling in three dimensions: model architecture, dataset, and training pipeline. Timer-S1 integrates sparse TimeMoE blocks and generic TimeSTP blocks for Serial-Token Prediction (STP), a generic training objective that adheres to the serial nature of forecasting. The proposed paradigm introduces serial computations to improve long-term predictions while avoiding costly rolling-style inference and pronounced error accumulation in the standard next-token prediction. Pursuing a high-quality and unbiased training dataset, we curate TimeBench, a corpus with one trillion time points, and apply meticulous data augmentation to mitigate predictive bias. We further pioneer a post-training stage, including continued pre-training and long-context extension, to enhance short-term and long-context performance. Evaluated on the large-scale GIFT-Eval leaderboard, Timer-S1 achieves state-of-the-art forecasting performance, attaining the best MASE and CRPS scores as a pre-trained model. Timer-S1 will be released to facilitate further research.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Timer-S1**，一个拥有 83 亿参数的时间序列基础模型（TSFM）。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的时间序列基础模型在扩展到大规模参数时面临“扩展瓶颈”。
*   **研究动机**：
    *   **预测的序列本质**：时间序列预测本质上是序列化的（长期的准确性依赖于步步推导），但现有的“并行预测”模型缺乏足够的序列计算能力，而“自回归模型”在长程预测时推理成本极高且误差累积严重。
    *   **数据异质性**：不同领域的时间序列在频率、形状和分布上存在巨大差异，难以统一建模。
    *   **缺乏高质量大规模数据**：现有的预训练数据集规模和质量难以支撑十亿级参数模型的充分训练。

### 2. 方法论
Timer-S1 提出了**序列缩放（Serial Scaling）**范式，从三个维度进行优化：
*   **模型架构（TimeMoE + TimeSTP）**：
    *   **TimeMoE**：采用稀疏混合专家架构（32个专家，每个 token 激活 2 个），以 8.3B 的总参数量实现 0.75B 的激活参数，平衡了模型容量与推理效率。
    *   **TimeSTP（序列标记预测）**：这是核心创新。不同于一次性输出多个点的并行预测，TimeSTP 模块通过多个连续的 Block 逐步细化预测。每个 Block 负责预测更远的一个步长，并结合初始输入和前一层的表示。这种设计在单次前向传播中引入了序列计算，既避免了自回归的滚动开销，又缓解了误差累积。
*   **数据集（TimeBench）**：
    *   构建了包含 **1 万亿（1 Trillion）**个时间点的语料库，涵盖金融、医疗、IoT、气象及合成数据。
    *   **数据增强**：引入重采样（Resampling）和数值翻转（Value-Flipping）来减轻预测偏差（如对特定趋势的盲目追随）。
*   **训练流水线**：
    *   **预训练**：使用 STP 目标进行全量训练。
    *   **后训练（Post-training）**：包括**持续预训练（CPT）**，使用带权重的 STP 损失（随预测步长衰减）来强化短期预测能力；以及**长上下文扩展（LCE）**，利用 RoPE 旋转位置编码将上下文从 2880 扩展到 11520。

### 3. 实验设计
*   **基准测试**：使用 **GIFT-Eval** 排行榜，包含 24 个数据集、14.4 万条序列。
*   **评估指标**：MASE（点预测准确性）和 CRPS（概率预测准确性）。
*   **对比方法**：
    *   **统计方法**：AutoARIMA, AutoETS, Seasonal Naive 等。
    *   **基础模型**：Chronos-2, TimesFM-2.5, TiRex, Moirai2, Sundial (Timer-3) 等。
*   **消融实验**：对比了 NTP（下文预测）、MTP（多标记预测）与 STP 的效果，以及数据增强和预训练阶段的贡献。

### 4. 资源与算力
*   **训练框架**：使用了 **VeOmni** 统一分布式训练框架。
*   **精度与存储**：采用 BF16 精度训练；TimeBench 原始数据约 4TB（Parquet 格式）。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号（如 H100/A100）、GPU 数量及总训练时长。仅提到通过混合内存-磁盘加载策略来处理大规模数据。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了从模型配置（Block 数量缩放）、训练目标对比到下游基准测试的全方位评估。
*   **充分性**：
    *   **客观性**：通过在第三方权威排行榜 GIFT-Eval 上进行测试，保证了结果的客观性。
    *   **对比维度**：不仅对比了最终性能，还深入分析了模型在短、中、长期预测上的表现差异。
    *   **消融充分**：针对 TimeSTP 的设计变体（如是否保留 Block、是否进行 Token 偏移）做了详尽对比，证明了其设计的必要性。

### 6. 主要结论与发现
*   **SOTA 性能**：Timer-S1 在 GIFT-Eval 上取得了预训练模型中的最佳 MASE (0.693) 和 CRPS (0.485) 评分。
*   **序列计算的价值**：实验证明，增加序列计算（TimeSTP）对中长期预测的提升显著优于单纯增加模型深度或使用并行预测。
*   **缩放定律**：模型性能随 TimeMoE 和 TimeSTP 模块数量的增加而持续提升，验证了时间序列模型在十亿级规模下的扩展性。
*   **后训练必要性**：持续预训练和长上下文扩展能显著进一步压低误差。

### 7. 优点
*   **架构创新**：TimeSTP 巧妙地结合了自回归的序列逻辑和并行模型的高效性，解决了长程预测的痛点。
*   **数据质量**：TimeBench 的万亿级规模和精细的预处理（如平稳性测试、预测性评估）为模型提供了坚实基础。
*   **系统性优化**：从数据增强到多阶段训练，提供了一套完整的构建大规模 TSFM 的技术方案。

### 8. 不足与局限
*   **外生变量缺失**：目前模型主要关注单变量上下文，尚未原生支持外生协变量（Exogenous Covariates），这在处理受外部因素影响大的数据时可能受限。
*   **算力透明度**：未披露具体的算力消耗，使得其他研究者难以评估复现成本。
*   **固定 Patch 限制**：实验发现模型在特定频率（与 Patch Size 接近时）会出现误差峰值，说明固定 Patch 机制仍有改进空间。

（完）
