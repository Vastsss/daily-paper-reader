---
title: An LLM-Enabled Frequency-Aware Flow Diffusion Model for Natural-Language-Guided Power System Scenario Generation
title_zh: 一种用于自然语言引导的电力系统场景生成的 LLM 赋能频率感知流扩散模型
authors: "Zhenghao Zhou, Yiyan Li, Fei Xie, Lu Wang, Bo Wang, Jiansheng Wang, Zheng Yan, Mo-Yuen Chow"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.19522v1"
tags: ["query:llm-wd"]
score: 10.0
evidence: LLM 赋能的风力与电力系统场景生成
tldr: 本研究针对电力系统场景生成中传统方法控制灵活性不足的问题，提出了一种名为LFFD的自然语言引导生成框架。该框架利用预训练大语言模型（LLM）解析用户指令，并结合流扩散模型实现高效、高质量的场景生成。通过引入频率感知多目标优化算法解决了频率偏差问题，实验证明该方法在光伏和负荷数据集上具有显著的生成效果和用户交互便利性。
selection_source: fresh_fetch
motivation: 现有的电力系统场景生成方法主要依赖固定长度的数值向量进行控制，难以满足用户对生成灵活性和交互便利性的需求。
method: 提出LFFD框架，利用LLM提取自然语言语义，结合流匹配目标的流扩散模型进行生成，并采用频率感知优化算法缓解频率偏差。
result: 在光伏和负荷数据集上的实验验证了该方法能够准确理解自然语言指令，并生成高质量、符合物理特征的电力场景。
conclusion: 该研究实现了自然语言驱动的电力场景生成，显著提升了生成过程的可控性与效率，为电力系统规划与运行提供了有力支持。
---

## 摘要
多样化且可控的场景生成（如风电、光伏、负荷等）对于稳健的电力系统规划和运行至关重要。随着基于人工智能的场景生成方法成为主流，现有方法（如条件生成对抗网络）主要依赖固定长度的数值条件向量来控制生成结果，在用户便利性和生成灵活性方面面临挑战。本文提出了一种名为 LLM 赋能的频率感知流扩散（LFFD）的自然语言引导场景生成框架，使用户能够使用普通人类语言生成所需的场景。首先，引入预训练的 LLM 模块，将非结构化自然语言描述的生成请求转换为有序的语义空间。其次，不同于标准的扩散模型，本文引入了采用修正流匹配目标的流扩散模型，以 LLM 输出作为模型输入，实现高效且高质量的场景生成。在模型训练过程中，引入了一种频率感知的多目标优化算法，以缓解频率偏差问题。同时，设计了一个双智能体框架来创建文本-场景训练样本对，并标准化语义评估。基于大规模光伏和负荷数据集的实验证明了所提方法的有效性。

## Abstract
Diverse and controllable scenario generation (e.g., wind, solar, load, etc.) is critical for robust power system planning and operation. As AI-based scenario generation methods are becoming the mainstream, existing methods (e.g., Conditional Generative Adversarial Nets) mainly rely on a fixed-length numerical conditioning vector to control the generation results, facing challenges in user conveniency and generation flexibility. In this paper, a natural-language-guided scenario generation framework, named LLM-enabled Frequency-aware Flow Diffusion (LFFD), is proposed to enable users to generate desired scenarios using plain human language. First, a pretrained LLM module is introduced to convert generation requests described by unstructured natural languages into ordered semantic space. Second, instead of using standard diffusion models, a flow diffusion model employing a rectified flow matching objective is introduced to achieve efficient and high-quality scenario generation, taking the LLM output as the model input. During the model training process, a frequency-aware multi-objective optimization algorithm is introduced to mitigate the frequency-bias issue. Meanwhile, a dual-agent framework is designed to create text-scenario training sample pairs as well as to standardize semantic evaluation. Experiments based on large-scale photovoltaic and load datasets demonstrate the effectiveness of the proposed method.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LFFD（LLM-enabled Frequency-aware Flow Diffusion）** 的新型电力系统场景生成框架。以下是对该论文的结构化总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：现代电力系统（风电、光伏、负荷）的场景生成对于规划和运行至关重要，但现有方法存在三大痛点：
    1.  **交互不便**：传统模型（如 GAN、扩散模型）依赖固定长度的数值向量作为控制条件，用户难以直观表达复杂需求。
    2.  **生成效率低**：标准扩散模型（DDPM）推理速度慢，需要大量迭代步骤。
    3.  **频率偏差（Spectral Bias）**：现有模型倾向于学习低频趋势而忽略高频瞬态波动，导致生成的电力曲线过于平滑，缺乏真实感。
*   **研究背景**：随着大语言模型（LLM）的发展，利用自然语言引导生成任务（如 Text-to-Image）已成熟，但在电力系统时间序列生成领域尚属空白。

### 2. 论文提出的方法论
核心思想是将 **LLM 的语义理解能力** 与 **修正流匹配（Rectified Flow Matching）** 的高效生成能力相结合。
*   **语义编码层**：使用预训练且冻结的 LLM（如 Qwen-8B）将非结构化的自然语言指令转换为高维语义嵌入空间。
*   **修正流匹配目标**：不同于传统扩散模型的随机微分方程，LFFD 学习从噪声到数据的直线概率流轨迹。这使得模型可以通过求解常微分方程（ODE）实现更快的推理。
*   **频率感知多目标优化**：
    *   引入**频谱一致性损失**（基于 FFT 的 $L_1$ 范数），强制模型捕捉高频波动。
    *   采用 **MGDA（多梯度下降算法）** 动态平衡时域 MSE 损失和频域损失，寻找 Pareto 最优解，解决两者梯度冲突的问题。
*   **文本导向去噪网络**：采用 1D U-Net 架构，集成残差块和**跨模态注意力机制**，将 LLM 提取的文本特征注入到时间序列生成过程中。
*   **双智能体框架**：设计了“标注智能体”（Annotator）自动为历史数据生成文本描述，以及“评审智能体”（Judge）对生成结果的语义一致性进行量化评分。

### 3. 实验设计
*   **数据集**：
    1.  **大规模光伏（PV）数据集**：超过 32,000 条日场景，5 分钟分辨率。
    2.  **电力负荷数据集**：约 2,000 条日场景，15 分钟分辨率（涵盖工业和居民负荷）。
*   **对比方法（Benchmarks）**：
    *   VAE（变分自编码器）
    *   DCGAN（深度卷积生成对抗网络）
    *   WGAN（基于 Wasserstein 距离的 GAN）
    *   DDPM（标准去噪扩散概率模型）
*   **评估指标**：KL 散度、最大均值差异（MMD）、Fréchet 距离（FD）、动态时间规整（DTW）以及功率谱密度距离（PSDD）。

### 4. 资源与算力
*   **硬件环境**：使用高性能计算集群，配备 **NVIDIA A100 GPU (80GB)**。
*   **训练细节**：模型训练了 5,000 个 epoch，全局 batch size 为 256。
*   **软件环境**：PyTorch 框架，Python 3.10。
*   **推理效率**：LFFD 在 10 步采样下即可达到高质量结果，相比 DDPM 实现了约 **87 倍**的加速。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个具有不同波动特征和样本规模的数据集上进行了验证，实验设置较为全面。
*   **验证维度**：
    *   **语义空间验证**：通过线性探测（Linear Probing）证明了 LLM 嵌入空间能够线性解码出天气、峰值、波动率等物理属性。
    *   **消融与对比**：对比了多种生成范式，并展示了不同训练阶段（400 vs 5000 epoch）的效果。
    *   **可控性测试**：量化分析了文本指令（如“12:40 出现峰值”）与生成结果的匹配度。
*   **客观性**：引入了第三方 LLM 智能体作为“法官”进行语义评分，减少了人工主观评价的偏差。

### 6. 主要结论与发现
*   **高效性**：LFFD 仅需 5-10 步采样即可超越 DDPM 1000 步的效果。
*   **高保真度**：在 PSDD 指标上比最强基准降低了一个数量级，证明其能完美还原高频波动特征。
*   **强可控性**：模型能够精准响应复杂的文本指令，如特定的功率跌落时间点、波动程度（平稳/中等/高波动）以及负荷类型（工业/居民）。
*   **语义对齐**：LLM 能够将“多云”、“暴雨”等抽象词汇准确映射到电力曲线的形态特征上。

### 7. 优点与亮点
*   **交互范式创新**：首次在电力场景生成中引入自然语言控制，极大降低了非专业用户的使用门槛。
*   **技术融合**：巧妙结合了流匹配（速度）、MGDA（频率平衡）和 LLM（语义），解决了扩散模型在工业应用中的核心痛点。
*   **评估体系完备**：提出的双智能体框架为缺乏“文本-序列”对的数据集提供了自动化的标注和评估方案。

### 8. 不足与局限
*   **数据规模差异**：负荷数据集规模（2k）远小于光伏数据集（32k），可能导致模型在复杂负荷模式下的泛化能力受限。
*   **LLM 依赖性**：模型性能高度依赖预训练 LLM 的质量，且冻结 LLM 可能无法捕捉某些极度专业的电力领域术语（虽然线性探测显示其表现良好）。
*   **计算开销**：虽然推理快，但训练阶段涉及 LLM 推理和多目标梯度计算，对算力（A100）有一定要求。
*   **应用限制**：目前仅展示了单变量生成，尚未讨论多区域、多能源品种之间的时空相关性生成。

（完）
