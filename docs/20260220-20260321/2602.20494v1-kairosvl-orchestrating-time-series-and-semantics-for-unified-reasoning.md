---
title: "KairosVL: Orchestrating Time Series and Semantics for Unified Reasoning"
title_zh: KairosVL：编排时间序列与语义以实现统一推理
authors: "Haotian Si, Changhua Pei, Xiao He, Zeyan Li, Zhe Xie, Zexin Wang, Jiyao Hu, Zhaoyang Yu, Tieying Zhang, Dan Pei, Jianhui Li, Gaogang Xie"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20494v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 协调时间序列与语义进行推理
tldr: 本研究针对复杂决策需求，提出了“语义条件时间序列推理”任务，旨在将数值建模与上下文语义理解相结合。为此开发了 KairosVL 模型，通过两轮强化学习框架进行训练：首轮增强对基础时间原语的感知，次轮聚焦于语义驱动的推理。实验表明，KairosVL 在合成与真实任务中均表现优异，不仅提升了推理性能，还显著增强了对未知场景的泛化能力，为时间序列智能化分析提供了新路径。
selection_source: fresh_fetch
motivation: 传统时间序列分析局限于数值建模，缺乏对复杂决策所需的上下文语义理解能力。
method: 提出 KairosVL 模型及两阶段强化学习框架，依次优化时间原语感知和语义条件推理。
result: 模型在多项任务中达到竞争性水平，并展现出卓越的泛化性能和内在推理能力。
conclusion: 结合语义推理与时间建模是提升时间序列智能的关键，本框架为实际应用提供了有效方案。
---

## 摘要
受时间序列分析日益复杂且面向决策的需求驱动，我们引入了语义条件时间序列推理（Semantic-Conditional Time Series Reasoning）任务，该任务将传统的时间序列分析从纯数值建模扩展到包含上下文和语义理解。为了进一步增强模型在复杂时间序列问题上的推理能力，我们提出了一个两轮强化学习框架：第一轮加强了模型对基础时间原语（temporal primitives）的感知，而第二轮则侧重于语义条件推理。由此产生的模型 KairosVL 在合成任务和真实世界任务中均取得了具有竞争力的性能。大量的实验和消融研究表明，我们的框架不仅提升了性能，还保留了内在的推理能力，并显著提高了对未知场景的泛化能力。总之，我们的工作突出了将语义推理与时间建模相结合的潜力，并为迫切需要的真实世界时间序列智能提供了一个实用的框架。

## Abstract
Driven by the increasingly complex and decision-oriented demands of time series analysis, we introduce the Semantic-Conditional Time Series Reasoning task, which extends conventional time series analysis beyond purely numerical modeling to incorporate contextual and semantic understanding. To further enhance the mode's reasoning capabilities on complex time series problems, we propose a two-round reinforcement learning framework: the first round strengthens the mode's perception of fundamental temporal primitives, while the second focuses on semantic-conditioned reasoning. The resulting model, KairosVL, achieves competitive performance across both synthetic and real-world tasks. Extensive experiments and ablation studies demonstrate that our framework not only boosts performance but also preserves intrinsic reasoning ability and significantly improves generalization to unseen scenarios. To summarize, our work highlights the potential of combining semantic reasoning with temporal modeling and provides a practical framework for real-world time series intelligence, which is in urgent demand.

---

## 论文详细总结（自动生成）

这是一份关于论文《KairosVL: Orchestrating Time Series and Semantics for Unified Reasoning》的结构化深度总结：

### 1. 研究动机与核心问题
*   **研究背景**：传统的时间序列（TS）分析（如预测、分类、异常检测）主要局限于纯数值建模。但在现实场景（如AIOps故障诊断）中，仅靠数值往往无法做出正确决策。例如，CPU利用率的周期性飙升在数值上是异常，但如果结合“每日定时备份脚本”这一语义上下文，它就是正常的。
*   **核心问题**：如何让模型具备“语义条件时间序列推理”能力，即同时理解时间序列的数值模式和与之相关的自然语言上下文（如操作规程SOP、外部事件描述），并给出可解释的推理过程。

### 2. 方法论
*   **核心思想**：提出 **KairosVL** 模型，通过**两轮强化学习（RLVR）**框架，先建立基础感知能力，再提升高级推理能力。
*   **关键技术细节**：
    *   **任务分类**：将推理任务分为四类：事实锚定特征提取、预测性任务、事件感知特征过滤、反事实推理。
    *   **KairosDataPipe**：一个由LLM驱动的多智能体数据生成流水线。它生成“文本描述 -> 数据生成代码 -> 可视化代码 -> QA对”，并通过“LLM评审组”（数据必要性、一致性、格式评审）和人工校验确保高质量。
    *   **两轮强化学习框架**：
        *   **第一轮（感知增强）**：在规则驱动的原始数据集上训练，让模型掌握噪声、周期性、异常值（OOD）等基础概念，并引入KL散度约束以稳定训练。
        *   **第二轮（推理增强）**：在复杂的语义推理数据集上训练，取消KL约束并增加探索空间，鼓励模型生成长链推理（CoT）。
    *   **可验证奖励（RLVR）**：利用多选题准确率、数值误差、F1分数等客观指标作为奖励函数，而非依赖人类偏好。

### 3. 实验设计
*   **数据集**：
    *   **Dataset A (Real-World)**：包含约100个来自生产环境的真实样本，涉及故障升级规则（SOP）和指标快照，用于测试零样本泛化能力。
    *   **Dataset B (KairosBench)**：包含约600个由KairosDataPipe生成的针对性推理样本。
*   **对比方法（Baselines）**：
    *   **开源模型**：InternVL3-8B, Qwen2.5VL (7B/32B/72B)。
    *   **商业模型**：GPT-4o, Gemini-2.5 Flash, GLM4V-Plus。
    *   **传统模型**：Donut, LSTMAD, TranAD等纯数值异常检测模型。

### 4. 资源与算力
*   **硬件配置**：使用了1台配备 **8张 NVIDIA A800-SXM4-80G GPU** 的服务器，以及 112 核 Intel Xeon Platinum CPU。
*   **训练细节**：基于 Qwen2.5VL-7B-Instruct 进行全参数微调。第一轮和第二轮强化学习均在 4 张 GPU 上运行，Rollout Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成数据到真实世界场景的全面评估。
*   **消融实验**：对比了“单轮训练 vs 两轮训练”、“有无KL散度约束”、“SFT vs RL”等多种配置。
*   **客观性**：通过多次采样取平均值（报告了误差范围），并与不同参数规模（从7B到72B）的模型及顶尖商业模型进行了公平对比。实验设计较为充分，验证了方法在不同维度上的有效性。

### 6. 主要结论与发现
*   **性能卓越**：KairosVL (7B) 在真实场景和基准测试中均表现优异，甚至在某些任务上超过了参数量大得多的模型（如Qwen2.5VL-72B）和商业模型（GPT-4o）。
*   **感知是推理的基础**：第一轮感知增强训练至关重要，若直接进行复杂推理训练，模型容易陷入浅层模式匹配，导致推理链条空洞。
*   **RL优于SFT**：在时间序列推理任务中，强化学习比监督微调（SFT）具有更强的泛化能力，能产生更具逻辑一致性的推理步骤。

### 7. 优点
*   **任务定义前瞻性**：首次系统性定义了“语义条件时间序列推理”任务，贴合工业界实际需求。
*   **训练策略科学**：两阶段RL框架有效解决了多模态模型在处理抽象数值与具体语义对齐时的困难。
*   **数据质量高**：通过代码生成数据而非直接生成图像，保证了时间序列的严谨性和可验证性。

### 8. 不足与局限
*   **数值精度限制**：作为视觉多模态模型，在处理极细微的数值差异（如150.1与150.01）时仍存在局限。
*   **时间定位偏差**：在某些失败案例中，模型无法精确定位到具体的时间戳，倾向于引用邻近的刻度。
*   **推理幻觉**：尽管经过强化学习，模型偶尔仍会产生逻辑不一致的推理路径（如错误的数值大小比较）。
*   **模型规模限制**：7B参数量在处理极其复杂的反事实推理时，上限仍受限于基础模型的认知能力。

（完）
