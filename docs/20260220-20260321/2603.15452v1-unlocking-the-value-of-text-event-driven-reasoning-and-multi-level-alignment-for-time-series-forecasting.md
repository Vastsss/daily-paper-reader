---
title: "Unlocking the Value of Text: Event-Driven Reasoning and Multi-Level Alignment for Time Series Forecasting"
title_zh: 释放文本价值：用于时间序列预测的事件驱动推理与多级对齐
authors: "Siyuan Wang, Peng Chen, Yihang Wang, Wanghui Qiu, Chenjuan Guo, Bin Yang, Yang Shu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15452v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 利用大语言模型进行文本与时间序列的跨模态学习
tldr: 针对现有时间序列预测方法过度依赖数值数据且对文本信息利用不足的问题，本文提出VoT框架。该框架通过事件驱动推理结合大语言模型的推理能力，并利用历史上下文学习进行引导。同时，通过多级对齐技术在表示层融合内生文本，在预测层通过自适应频率融合数值与文本预测结果。实验证明VoT在10个领域的预测性能显著优于现有方法。
selection_source: fresh_fetch
motivation: 现有的多模态时间序列预测方法未能充分挖掘文本信息中的复杂模式和推理价值。
method: 提出VoT框架，包含基于大模型的事件驱动推理、历史上下文学习引导以及多级表示与预测对齐机制。
result: 在涵盖10个领域的真实世界数据集上，VoT相比现有最先进方法取得了显著的性能提升。
conclusion: 结合大模型的推理能力与多级文本对齐技术能有效释放文本价值，显著增强时间序列预测的准确性。
---

## 摘要
现有的时间序列预测方法主要依赖于数值数据本身。然而，现实世界的时间序列展现出与多模态信息相关的复杂模式，仅凭数值数据难以对其进行预测。尽管已经出现了一些多模态时间序列预测方法，但它们要么利用补充信息有限的文本，要么仅关注表示提取，为预测提取的文本信息极少。为了释放文本价值（Value of Text），我们提出了 VoT，一种结合了事件驱动推理和多级对齐的方法。事件驱动推理将外生文本中的丰富信息与大语言模型（LLM）强大的推理能力相结合，用于时间序列预测。为了引导 LLM 进行有效推理，我们提出了历史上下文学习（Historical In-context Learning），通过检索并应用历史示例作为上下文引导。为了最大化文本的利用率，我们提出了多级对齐。在表示层级，我们利用内生文本对齐（Endogenous Text Alignment）将内生文本信息与时间序列相融合。在预测层级，我们设计了自适应频率融合（Adaptive Frequency Fusion），以融合事件驱动预测和数值预测的频率分量，从而实现优势互补。在涵盖 10 个领域的真实世界数据集上的实验表明，该方法较现有方法有显著改进，验证了我们方法在文本利用方面的有效性。代码已在 https://github.com/decisionintelligence/VoT 开源。

## Abstract
Existing time series forecasting methods primarily rely on the numerical data itself. However, real-world time series exhibit complex patterns associated with multimodal information, making them difficult to predict with numerical data alone. While several multimodal time series forecasting methods have emerged, they either utilize text with limited supplementary information or focus merely on representation extraction, extracting minimal textual information for forecasting. To unlock the Value of Text, we propose VoT, a method with Event-driven Reasoning and Multi-level Alignment. Event-driven Reasoning combines the rich information in exogenous text with the powerful reasoning capabilities of LLMs for time series forecasting. To guide the LLMs in effective reasoning, we propose the Historical In-context Learning that retrieves and applies historical examples as in-context guidance. To maximize the utilization of text, we propose Multi-level Alignment. At the representation level, we utilize the Endogenous Text Alignment to integrate the endogenous text information with the time series. At the prediction level, we design the Adaptive Frequency Fusion to fuse the frequency components of event-driven prediction and numerical prediction to achieve complementary advantages. Experiments on real-world datasets across 10 domains demonstrate significant improvements over existing methods, validating the effectiveness of our approach in the utilization of text. The code is made available at https://github.com/decisionintelligence/VoT.

---

## 论文详细总结（自动生成）

这是一份关于论文《Unlocking the Value of Text: Event-Driven Reasoning and Multi-Level Alignment for Time Series Forecasting》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的时间序列预测方法主要依赖历史数值数据，难以应对由外部突发事件（如金融危机、疫情、政策变动）引起的非线性波动和剧烈变化。
*   **研究动机**：
    *   **文本价值未充分挖掘**：现有结合文本的方法要么仅使用简单的统计描述（内生文本），要么仅将文本作为特征嵌入，忽略了文本中蕴含的深层语义逻辑和事件驱动的因果关系。
    *   **模态对齐不充分**：文本与数值序列之间存在巨大的模态鸿沟，简单的拼接或融合难以发挥两者的互补优势。
*   **整体含义**：本文旨在通过大语言模型（LLM）的推理能力捕捉“外生事件”，并结合多级对齐机制，将文本的宏观逻辑与数值的微观波动深度融合。

### 2. 方法论：VoT 框架
VoT 采用双分支架构，通过**事件驱动推理**和**多级对齐**来提升预测精度。

*   **事件驱动推理分支（处理外生文本）**：
    *   **生成流水线**：包含模板生成（定义数据集特征）、摘要提取（过滤冗余信息）和逻辑推理（由 LLM 生成数值预测及解释）。
    *   **历史上下文学习（HIC）**：这是核心创新点。在训练阶段，模型记录预测错误并由 LLM 生成“修正后的推理”存入知识库；在推理阶段，通过检索相似的历史案例，引导 LLM 避免重复错误，实现“错而能改”的上下文引导。
*   **数值预测分支（处理内生文本）**：
    *   **内生文本对齐（ETA）**：将时间序列的统计特征（均值、趋势等）转化为文本，利用**分解模式提取**（趋势/季节性）和**分解对比学习**，在表示层级实现数值特征与文本语义的深度对齐。
*   **自适应频率融合（AFF）**：
    *   在预测输出层级，利用快速傅里叶变换（FFT）将两个分支的预测结果分解到频域。
    *   通过可学习的权重，动态调整不同频率分量的融合比例（例如：事件驱动分支在低频趋势上权重更高，数值分支在高频波动上权重更高）。

### 3. 实验设计
*   **数据集**：涵盖 10 个真实世界领域，包括农业、气候、经济、能源、环境、健康、安全、社会公益、交通及新增的天气数据集。
*   **Benchmark 与对比方法**：
    *   **纯数值模型**：iTransformer, PatchTST, RaFT。
    *   **文本增强变体**：上述模型在 Time-MMD 框架下的文本增强版本（如 iTransformer*）。
    *   **多模态模型**：GPT4TS, GPT4MTS, TaTS, Time-VLM, CALF。
*   **评估指标**：均方误差（MSE）和平均绝对误差（MAE）。

### 4. 资源与算力
*   **软件环境**：PyTorch 2.5.0, CUDA 12.1, Ubuntu 20.04。
*   **模型使用**：
    *   摘要与模板生成：Meta-Llama-3-8B-Instruct。
    *   嵌入检索：Qwen2-Embedding-0.6B。
    *   核心推理器：DeepSeek API。
    *   数值分支骨干：PatchTST。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，但提供了推理延迟数据：总推理延迟约为 5.9 秒（主要消耗在 LLM 推理分支），数值分支仅需 0.002 秒。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个数据集上进行了全方位的对比测试，覆盖了不同的预测长度（如 48, 96, 192, 336 等）。
*   **消融实验**：针对 HIC（历史上下文学习）、ETA（内生文本对齐）、AFF（频率融合）以及不同文本来源（随机文本 vs 统计文本 vs 外生文本）进行了详尽的消融分析。
*   **充分性与公平性**：
    *   实验对比了从纯数值到最新多模态的各类模型，基准线设置全面。
    *   针对经济数据集的时间顺序问题进行了修正，确保了实验的客观性。
    *   进行了鲁棒性测试（针对噪声和稀疏文本），验证了模型在不理想数据下的表现。

### 6. 主要结论与发现
*   **性能领先**：VoT 在 10 个数据集的 20 项指标中，有 19 项达到了最优（SOTA），显著优于现有的多模态预测模型。
*   **文本价值**：外生文本提供的事件信息对于捕捉数值序列中的“突变”至关重要，而 HIC 机制能显著提升 LLM 处理时间序列推理的准确性。
*   **频率互补**：实验证实，事件驱动分支在捕捉长期趋势（低频）方面更强，而数值分支在捕捉短期波动（高频）方面更具优势，AFF 融合机制有效结合了两者。

### 7. 优点（亮点）
*   **创新的 HIC 机制**：不同于简单的 RAG（检索增强生成），HIC 强调对“错误修正过程”的检索，使 LLM 具备了从历史失败中学习的能力。
*   **多级对齐策略**：从表示层的对比学习到预测层的频域融合，构建了从底层特征到顶层输出的完整对齐链路。
*   **跨领域泛化**：在 10 个完全不同的领域均表现出色，证明了框架的通用性。

### 8. 不足与局限
*   **推理延迟**：由于引入了 LLM 推理环节，单次预测需要数秒时间，这对于高频交易或毫秒级实时响应场景（如某些工业控制）可能存在限制。
*   **API 依赖**：核心推理依赖于 DeepSeek 等外部大模型 API，这可能涉及数据隐私风险及调用成本问题。
*   **文本获取难度**：在某些特定领域，高质量、实时对齐的外生文本（如即时新闻）可能难以获取，这会限制事件驱动分支的发挥。

（完）
