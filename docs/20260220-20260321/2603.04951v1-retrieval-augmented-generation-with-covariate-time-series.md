---
title: Retrieval-Augmented Generation with Covariate Time Series
title_zh: 基于协变量时间序列的检索增强生成
authors: "Kenny Ye Liang, Zhongyi Pei, Huan Zhang, Yuhui Liu, Shaoxu Song, Jianmin Wang"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04951v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 时间序列基础模型的检索增强生成
tldr: 本研究针对工业场景（如PRSOV维护）中时间序列数据稀缺、序列短且协变量耦合的挑战，提出了RAG4CTS框架。该框架是一种无需训练的检索增强生成方案，通过构建层次化原生知识库实现无损存储，并设计了双阶段双加权检索机制以对齐历史趋势。此外，引入代理驱动策略动态优化上下文。实验及南航实际部署结果证明，该方法在预测精度和故障识别方面表现卓越，实现了零误报。
selection_source: fresh_fetch
motivation: 现有的时间序列RAG方法在处理数据稀缺、短序列及复杂协变量耦合的工业预测场景时，难以有效区分相似状态。
method: 提出了RAG4CTS框架，利用层次化原生知识库、双阶段双加权检索机制和代理驱动的自监督上下文优化来增强预测。
result: 实验显示该框架在PRSOV预测精度上显著超过现有SOTA模型，并在实际航空维护部署中成功识别故障且无误报。
conclusion: RAG4CTS证明了结合物理感知检索与动态上下文优化的RAG范式在复杂协变量时间序列分析中的有效性与实用性。
---

## 摘要
尽管检索增强生成（RAG）极大地增强了大型语言模型（LLMs），但将这一范式扩展到时间序列基础模型（TSFMs）仍然面临挑战。这一点在调压切断阀（PRSOV）的预测性维护中尤为突出，这是一个具有（1）数据稀缺、（2）短暂态序列和（3）协变量耦合动力学特征的高风险工业场景。不幸的是，现有的时间序列 RAG 方法主要依赖于生成的静态向量嵌入和可学习的上下文增强器，在这些稀缺、暂态和协变量耦合的场景中，可能无法有效区分相似的状态（regimes）。为了解决这些局限性，我们提出了 RAG4CTS，这是一个针对协变量时间序列的、状态感知的、无需训练的 RAG 框架。具体而言，我们构建了一个分层的时间序列原生知识库，以实现原始历史状态的无损存储和受物理启发的检索。我们设计了一种两阶段双加权检索机制，通过逐点相似性和多变量相似性来对齐历史趋势。在上下文增强方面，我们引入了一种智能体驱动的策略，以自监督的方式动态优化上下文。在 PRSOV 上的广泛实验表明，我们的框架在预测准确性方面显著优于现有的最先进基准模型。该系统已部署在中国南方航空公司的 Apache IoTDB 中。自部署以来，我们的方法在两个月内成功识别了一次 PRSOV 故障，且保持零误报。

## Abstract
While RAG has greatly enhanced LLMs, extending this paradigm to Time-Series Foundation Models (TSFMs) remains a challenge. This is exemplified in the Predictive Maintenance of the Pressure Regulating and Shut-Off Valve (PRSOV), a high-stakes industrial scenario characterized by (1) data scarcity, (2) short transient sequences, and (3) covariate coupled dynamics. Unfortunately, existing time-series RAG approaches predominantly rely on generated static vector embeddings and learnable context augmenters, which may fail to distinguish similar regimes in such scarce, transient, and covariate coupled scenarios. To address these limitations, we propose RAG4CTS, a regime-aware, training-free RAG framework for Covariate Time-Series. Specifically, we construct a hierarchal time-series native knowledge base to enable lossless storage and physics-informed retrieval of raw historical regimes. We design a two-stage bi-weighted retrieval mechanism that aligns historical trends through point-wise and multivariate similarities. For context augmentation, we introduce an agent-driven strategy to dynamically optimize context in a self-supervised manner. Extensive experiments on PRSOV demonstrate that our framework significantly outperforms state-of-the-art baselines in prediction accuracy. The proposed system is deployed in Apache IoTDB within China Southern Airlines. Since deployment, our method has successfully identified one PRSOV fault in two months with zero false alarm.

---

## 论文详细总结（自动生成）

### 论文总结：基于协变量时间序列的检索增强生成 (RAG4CTS)

#### 1. 核心问题与研究动机
*   **核心问题**：现有的时间序列基础模型（TSFM）在处理高风险工业场景（如飞机调压切断阀 PRSOV 的预测性维护）时存在能力鸿沟。
*   **研究背景与挑战**：
    *   **数据稀缺**：关键运行状态（Regimes）极少，深度模型难以学习有效表示。
    *   **短暂态序列**：状态转换极快（如10秒内仅18个点），信息密度低。
    *   **协变量耦合**：目标变量（如歧管压力 MP）受外部协变量（如发动机转速 N2、中间压力 IP）被动驱动，忽视物理逻辑会导致预测失效。
*   **现有 RAG 的局限**：依赖可学习适配器（在稀缺数据下难收敛）、静态向量化（导致短序列精度损失）以及仅针对目标的检索（忽视了协变量的驱动逻辑）。

#### 2. 方法论
论文提出了 **RAG4CTS**，一个状态感知、无需训练的协变量时间序列 RAG 框架：
*   **层次化原生知识库 (Hierarchical KB)**：采用树状结构（根-机型-机号-设备-传感器）存储原始序列，避免了向量化带来的信息损失，确保物理完整性。
*   **双阶段双加权检索机制**：
    *   **双加权 (Bi-weighted)**：结合“关键点权重”（对近期状态和未来控制输入加权）和“协变量权重”（利用互信息 MI 衡量协变量对目标的物理耦合程度）。
    *   **两阶段过滤**：第一阶段通过加权余弦相似度进行“形状对齐”；第二阶段通过加权矩阵轮廓（Matrix Profile）距离进行“状态精修”，确保检索结果在趋势和数值上均高度匹配。
*   **智能体驱动的上下文增强 (Agent-driven Augmentation)**：
    *   **核心思想**：将检索到的 Top-1 样本作为“代理查询”，利用其已知的真实值作为验证集。
    *   **算法流程**：通过贪心搜索动态确定最佳的上下文拼接数量 $k^*$，使模型在推理前自监督地优化上下文长度，解决固定 $k$ 值带来的噪声问题。

#### 3. 实验设计
*   **数据集**：中国南方航空（CSA）提供的 PRSOV 真实数据集，分为 B777L/R 和 A320L/R 四个子集。
*   **任务设置**：短序列预测（输入6点，预测未来12点）。
*   **对比方法 (Benchmarks)**：
    *   **深度学习模型**：DLinear, PatchTST, iTransformer, TimeMixer, TimeXer, Pyraformer。
    *   **零样本 TSFM**：Chronos-2, Chronos-Bolt, Sundial, TimesFM。
    *   **时间序列 RAG**：TS-RAG（基于嵌入的方法）。
*   **消融实验**：涵盖了知识库范围、检索指标（DTW vs. MP vs. Euclidean）、加权方案以及动态 $k$ 值的有效性。

#### 4. 资源与算力
*   **训练时长**：论文强调该框架是 **Training-free（无需训练）** 的，因此不存在针对特定工业任务的训练过程。
*   **算力细节**：文中未详细列出具体的 GPU 型号和推理耗时，但提到该系统已部署在 Apache IoTDB 的 AINode 模块中，支持通过标准 SQL 进行数据库内推理。
*   **模型骨干**：实验中使用了 Chronos-2 作为 TSFM 的骨干网络。

#### 5. 实验数量与充分性
*   **实验规模**：在四个不同的工业子数据集上进行了测试，样本量从几千到数万不等。
*   **充分性**：实验设计非常全面，不仅对比了三类主流方法，还深入探讨了协变量的影响、知识库的边界（单机 vs. 同型机 vs. 全机队）以及检索算法的敏感性。
*   **客观性**：通过实际部署（2025年11月起）的在线案例验证了模型的工业可靠性，证明了实验结果并非仅停留在离线指标上。

#### 6. 主要结论与发现
*   **性能卓越**：RAG4CTS 在所有子集上的 MSE 和 MAE 均显著低于 SOTA 基准，将短序列预测问题转化为长上下文推理问题。
*   **物理一致性**：引入协变量权重能显著提升预测精度，证明了物理驱动逻辑在工业预测中的必要性。
*   **动态增强有效**：智能体驱动的策略能根据查询的复杂程度自动调整参考样本数量，优于固定长度的拼接。
*   **工业价值**：在南航部署两个月内，成功识别出 1 起真实故障，且实现了零误报。

#### 7. 优点
*   **无需训练**：极大地降低了工业部署的门槛，解决了小样本下的模型收敛难题。
*   **原生存储与检索**：摒弃了向量嵌入，直接在原始数据空间操作，保留了工业信号的微小暂态特征。
*   **闭环优化**：通过 Top-1 代理进行自监督校准，增强了 RAG 系统的鲁棒性。
*   **端到端集成**：与 Apache IoTDB 深度集成，展示了从数据库到 AI 推理的完整工业链路。

#### 8. 不足与局限
*   **计算开销**：虽然无需训练，但双阶段检索（尤其是 Matrix Profile）在超大规模知识库下的检索延迟可能成为瓶颈。
*   **骨干模型依赖**：性能高度依赖于底层 TSFM（如 Chronos-2）的上下文学习能力，若基础模型对长序列处理不佳，RAG 效果会受限。
*   **场景局限**：目前主要针对 PRSOV 这一特定物理系统进行了验证，对于非协变量驱动或非暂态特征明显的通用时间序列，其优势可能减弱。

（完）
