---
title: In-context Pre-trained Time-Series Foundation Models adapt to Unseen Tasks
title_zh: 上下文预训练时间序列基础模型适应未知任务
authors: "Shangqing Xu, Harshavardhan Kamarthi, Haoxin Liu, B. Aditya Prakash"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.20307v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 上下文预训练时间序列基础模型
tldr: 现有的时间序列基础模型在处理未见任务时通常需要微调。本文提出ICTP框架，通过重构预训练数据赋予模型上下文学习能力，使其能根据输入输出示例动态适应新任务。该方法在无需微调的情况下，显著提升了模型在未见任务上的泛化性能和推理效率。
selection_source: fresh_fetch
motivation: 现有时间序列基础模型在面对未见任务时，通常需要繁琐的微调才能实现良好的泛化表现。
method: 提出ICTP框架，通过重构预训练数据结构，使模型能够利用上下文中的输入输出关系进行零样本推理。
result: "在未见任务上，该方法使最先进的时间序列基础模型性能提升了约11.4%，且无需任何微调。"
conclusion: 通过上下文预训练增强模型学习能力，是提升时间序列基础模型任务适应性的有效途径。
---

## 摘要
时间序列基础模型 (TSFMs) 在多种数据集和任务中展现出强大的泛化能力。然而，现有的基础模型通常针对特定任务的性能提升进行预训练，在不进行微调的情况下往往难以泛化到未知任务。为了解决这一局限性，我们提出为 TSFMs 增强上下文学习 (ICL) 能力，使其能够通过动态适应上下文中提供的输入输出关系来执行测试时推理。我们的框架——上下文时间序列预训练 (ICTP)，通过重构原始预训练数据，使骨干 TSFM 具备 ICL 能力，从而实现对未知任务的适应。实验表明，在无需微调的情况下，ICT 将最先进的 TSFMs 在未知任务上的性能提升了约 11.4%。

## Abstract
Time-series foundation models (TSFMs) have demonstrated strong generalization capabilities across diverse datasets and tasks. However, existing foundation models are typically pre-trained to enhance performance on specific tasks and often struggle to generalize to unseen tasks without fine-tuning. To address this limitation, we propose augmenting TSFMs with In-Context Learning (ICL) capabilities, enabling them to perform test-time inference by dynamically adapting to input-output relationships provided within the context. Our framework, In-Context Time-series Pre-training (ICTP), restructures the original pre-training data to equip the backbone TSFM with ICL capabilities, enabling adaptation to unseen tasks. Experiments demonstrate that ICT improves the performance of state-of-the-art TSFMs by approximately 11.4% on unseen tasks without requiring fine-tuning.

---

## 论文详细总结（自动生成）

以下是对论文《In-context Pre-trained Time-Series Foundation Models adapt to Unseen Tasks》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的时间序列基础模型（TSFMs）虽然在特定任务（如预测）上表现优异，但缺乏**多任务自适应能力**。在面对未见过的任务（Unseen Tasks）时，通常需要繁琐的微调（Fine-tuning），这增加了计算开销和数据需求。
*   **研究背景**：大语言模型（LLMs）通过在大规模多样化文本上预训练，自然涌现出了**上下文学习（In-Context Learning, ICL）**能力。然而，时间序列数据具有严格的时间顺序且缺乏天然的任务多样性，导致传统的单目标预训练（如仅预测未来）无法让模型学会如何通过参考示例来理解新任务。

### 2. 方法论：ICTP 框架
*   **核心思想**：提出“上下文时间序列预训练”（In-Context Time-series Pre-training, **ICTP**），通过重构原始数据集，将其转化为“多任务上下文跟随”格式，显式地教会模型进行上下文推理。
*   **关键技术细节**：
    1.  **任务候选池构建**：定义多种时间序列任务（如预测 Forecasting、填补 Imputation、回溯 Backtracing），并将它们统一为序列到序列（Seq-to-Seq）的形式。
    2.  **上下文示例生成**：对于每一个训练样本，随机采样一个任务，并从数据集中抽取 $m$ 个该任务的输入-输出对作为“演示示例”（Demonstrations）。
    3.  **序列拼接**：将这些示例与当前输入拼接成一个长序列：$C = (x_1, y_1) \oplus (x_2, y_2) \oplus \dots \oplus (x_m, y_m) \oplus x_{target}$。
    4.  **预训练/微调**：使用这种包含“上下文”的数据对骨干模型进行训练，使其学会模仿上下文中的映射关系来生成目标输出 $y_{target}$。

### 3. 实验设计
*   **骨干模型（Backbones）**：涵盖了三种主流架构：
    *   **MOMENT**（基于掩码重构的 Encoder-only 架构）
    *   **TimesFM**（基于自回归生成的 Decoder-only 架构）
    *   **LPTM**（基于自适应分段的架构）
*   **数据集**：使用了 6 个基准数据集，包括 Informer 系列（ETTh1, ETTm1, Exchange, Weather）和交通预测系列（PEMS-Bay, METR-LA）。
*   **Benchmark 与对比方法**：
    *   **评估协议**：采用“留一法”评估未见任务。例如，在预测和填补任务上训练，然后在回溯任务上测试。
    *   **对比基准**：任务感知的朴素输入重编程（Task-aware naive input reprogramming），即在不微调的情况下，通过手动翻转序列或掩码处理来强行适配任务。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及训练时长。考虑到其基于现有的基础模型（如 TimesFM, MOMENT）进行二次预训练或微调，且处理的是长序列拼接，预计需要中大型 GPU 集群（如 A100 或 H100 级别）支持。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 6 个不同领域的数据集。
    *   测试了 2 种不同的输出长度（96 和 192）。
    *   针对 3 种不同的模型架构进行了验证。
*   **充分性评价**：实验设计较为合理，通过跨任务验证（在 A 任务训练，在 B 任务测试）客观地证明了 ICL 能力的迁移。消融实验分析了 ICTP 如何增强模型能力，整体论证逻辑闭环。

### 6. 主要结论与发现
*   **性能提升**：在无需任何微调的情况下，ICTP 使基础模型在未见任务上的表现平均提升了约 **11.4%**。
*   **架构差异**：ICTP 的效果受模型架构影响。例如，Decoder-only 模型（如 TimesFM）在填补任务上提升有限，而 Encoder-only 模型（如 MOMENT）在预测和回溯任务上进步显著。
*   **任务泛化**：即使模型在原始预训练阶段已经见过某些数据集（如 TimesFM 见过 Weather 数据），ICTP 依然能显著提升其在该数据集未见任务上的表现，证明了现有预训练方案在多任务适配上的缺陷。

### 7. 优点
*   **创新性**：首次系统性地将上下文学习（ICL）引入时间序列基础模型，解决了 TSFM 难以零样本适配新任务的痛点。
*   **通用性**：ICTP 是一个通用的数据重构框架，可以应用于任何现有的时间序列骨干模型，具有很强的适配性。
*   **效率**：推理时无需更新参数，仅通过更换 Prompt（上下文示例）即可切换任务，极大地降低了部署成本。

### 8. 不足与局限
*   **任务覆盖有限**：实验仅涵盖了预测、填补、回溯三种基础任务，对于更复杂的异常检测、分类等任务的泛化能力尚待验证。
*   **上下文长度限制**：拼接多个示例会显著增加输入序列长度，受限于 Transformer 的二次方复杂度，这可能导致计算开销随示例数量增加而剧增。
*   **数据集简单性**：作者提到在 Exchange 等平滑数据集上提升较小，说明该方法在处理极高噪声或非平稳性极强的数据时可能存在瓶颈。

（完）
