---
title: "Dissecting Chronos: Sparse Autoencoders Reveal Causal Feature Hierarchies in Time Series Foundation Models"
title_zh: 剖析 Chronos：稀疏自编码器揭示时间序列基础模型中的因果特征层级
authors: Anurag Mishra
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10071v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: Chronos时间序列基础模型分析
tldr: 本研究首次将稀疏自编码器（SAE）应用于时间序列基础模型（TSFM），旨在揭示Chronos-T5-Large模型的内部表示机制。通过对六个层级进行TopK SAE训练和392次消融实验，研究发现模型存在深度依赖的特征层级：从早期的频率特征到中期的突变检测，再到末层的复杂语义概念。结果表明，中间层的突变检测特征对预测至关重要，而模型对周期性模式的依赖程度较低，这为理解TSFM的决策逻辑提供了机械可解释性证据。
selection_source: fresh_fetch
motivation: 时间序列基础模型在关键领域应用日益广泛，但其内部黑盒机制导致其决策过程缺乏透明度和可解释性。
method: 在Chronos-T5-Large模型上训练TopK稀疏自编码器，并通过单特征消融实验建立特征与预测性能之间的因果联系。
result: 揭示了从低级频率到关键突变检测再到丰富语义的特征层级，发现中间编码器层的突变检测特征具有最高的因果重要性。
conclusion: 机械可解释性方法能有效迁移至时间序列模型，证实了Chronos-T5主要依赖突变动力学而非周期性模式进行预测。
---

## 摘要
时间序列基础模型 (TSFM) 正越来越多地部署在关键领域，但其内部表示仍然是不透明的。我们展示了稀疏自编码器 (SAE) 在 TSFM 上的首次应用，在 Chronos-T5-Large（7.1 亿参数）六个层的激活值上训练了 TopK SAE。通过 392 次单特征消融实验，我们确定每个被消融的特征都会导致正向的 CRPS 退化，从而证实了因果相关性。我们的分析揭示了一个依赖于深度的层级结构：编码器早期层编码低级频率特征，中间层集中了因果关键的变化检测特征，而最终层则压缩了丰富但因果重要性较低的时间概念分类。最关键的特征存在于中间层（最大单特征 Delta CRPS = 38.61），而非语义最丰富的最终层，在最终层中，渐进式消融反而矛盾地提高了预测质量。这些发现表明，机械可解释性可以有效地迁移到 TSFM，并且 Chronos-T5 依赖于突发动态检测而非周期性模式识别。

## Abstract
Time series foundation models (TSFMs) are increasingly deployed in high-stakes domains, yet their internal representations remain opaque. We present the first application of sparse autoencoders (SAEs) to a TSFM, training TopK SAEs on activations of Chronos-T5-Large (710M parameters) across six layers. Through 392 single-feature ablation experiments, we establish that every ablated feature produces a positive CRPS degradation, confirming causal relevance. Our analysis reveals a depth-dependent hierarchy: early encoder layers encode low-level frequency features, the mid-encoder concentrates causally critical change-detection features, and the final encoder compresses a rich but less causally important taxonomy of temporal concepts. The most critical features reside in the mid-encoder (max single-feature Delta CRPS = 38.61), not in the semantically richest final encoder layer, where progressive ablation paradoxically improves forecast quality. These findings demonstrate that mechanistic interpretability transfers effectively to TSFMs and that Chronos-T5 relies on abrupt-dynamics detection rather than periodic pattern recognition.

---

## 论文详细总结（自动生成）

以下是对论文《Dissecting Chronos: Sparse Autoencoders Reveal Causal Feature Hierarchies in Time Series Foundation Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：时间序列基础模型（TSFMs，如 Chronos-T5）虽然在预测任务中表现优异，但其内部表示机制如同“黑盒”，缺乏透明度。
*   **研究动机**：借鉴自然语言处理（NLP）中的机械可解释性（Mechanistic Interpretability）方法，探索是否可以使用稀疏自编码器（SAE）来分解 TSFM 的高维激活值，从而识别出具有物理意义且具备因果影响的时间序列特征。
*   **整体含义**：这是首次将 SAE 应用于大规模时间序列基础模型的研究，旨在揭示模型如何处理频率、趋势、突变等时间概念。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：利用 TopK 稀疏自编码器（SAE）将 Chronos-T5 的稠密激活向量分解为稀疏的、可解释的特征方向。
*   **关键技术细节**：
    *   **目标模型**：Chronos-T5-Large（7.1 亿参数），基于 T5 架构，将时间序列量化为 4096 个离散 token。
    *   **SAE 架构**：采用 TopK SAE，设置特征维度 $d_{sae} = 8 \times d_{model} = 8192$，仅保留激活值最大的前 $k=64$ 个特征。
    *   **提取点**：在编码器（第 5、11、23 块）和解码器（第 11、23 块及交叉注意力输出）共 6 个位置提取残差流激活。
    *   **特征分类（Taxonomy）**：定义了 11 种时间概念（如趋势上/下、周期性、水平位移、高/低频、波动率等），通过计算特征激活与合成诊断数据之间的皮尔逊相关性进行自动标注。
    *   **因果验证**：通过“单特征消融”和“渐进式消融”观察 CRPS（连续排名概率分数）的变化，量化特征对预测结果的实际贡献。

### 3. 实验设计
*   **数据集**：
    *   **合成诊断套件**：用于验证特征分类的准确性（包含趋势、季节性、噪声等受控属性）。
    *   **ETT（电力变压器温度）数据集**：用于因果消融实验，评估特征对真实预测任务的影响。
*   **Benchmark**：以原始 Chronos-T5-Large 的预测性能为基准。
*   **对比方法**：主要对比不同层级（早、中、晚）特征的因果重要性和语义丰富度。

### 4. 资源与算力
*   **训练细节**：每个 SAE 在约 10 万个时间序列窗口上训练 50,000 步。
*   **优化器**：Adam 优化器，学习率 $3 \times 10^{-4}$，采用余弦衰减。
*   **算力说明**：文中未明确提及具体的 GPU 型号和数量，但考虑到 710M 参数模型的推理和 6 个 SAE 的训练，通常需要工业级 GPU（如 A100 或 H100）支持。

### 5. 实验数量与充分性
*   **实验规模**：进行了 392 次单特征消融实验，并针对编码器第 23 层进行了扩展实验（1024 个窗口，200 个特征）。
*   **充分性**：实验覆盖了模型从输入到输出的多个关键层级，且 100% 的消融实验均显示出正向的性能退化（$\Delta CRPS > 0$），有力证明了特征的因果相关性。
*   **客观性**：采用了合成数据标注与真实数据消融相结合的方法，避免了纯主观的特征解释。

### 6. 主要结论与发现
*   **特征层级结构**：
    *   **早期编码器**：提取低级频率和局部波动特征。
    *   **中期编码器（第 11 层）**：**因果最关键层**，集中了大量“水平位移（Level Shift）”检测特征，对预测性能影响最大。
    *   **末期编码器**：语义最丰富，涵盖了复杂的周期性和趋势概念，但因果重要性反而较低。
*   **因果与语义的背离**：发现中间层是计算瓶颈，而消融末层特征有时反而能提高预测质量（可能起到了隐式领域自适应的作用）。
*   **预测逻辑**：Chronos-T5 更多地依赖于**突发动态检测**（如突变点），而非长期的周期性模式识别。

### 7. 优点
*   **创新性**：填补了时间序列基础模型机械可解释性研究的空白。
*   **因果性验证**：不满足于相关性分析，通过大规模消融实验确立了特征与模型输出之间的因果联系。
*   **揭示深度规律**：清晰地展示了时间序列特征在 Transformer 深度维度上的演化过程。

### 8. 不足与局限
*   **标注覆盖率有限**：全球仅 17.2% 的特征被成功标注，解码器特征的标注率极低（<6%），仍有大量“未知”特征。
*   **数据偏差**：因果实验主要集中在 ETT 数据集上，结论在其他领域（如金融、医疗）的泛化性有待验证。
*   **模型单一**：仅分析了 Chronos-T5，尚未与其他架构（如 TimesFM 或 Moirai）进行横向对比。
*   **算力限制**：部分消融实验样本量较小（256 个窗口），可能存在统计波动。

（完）
