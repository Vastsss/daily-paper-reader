---
title: "TimeSqueeze: Dynamic Patching for Efficient Time Series Forecasting"
title_zh: TimeSqueeze：用于高效时间序列预测的动态分块技术
authors: "Sravan Kumar Ankireddy, Nikita Seleznev, Nam H. Nguyen, Yulun Wu, Senthil Kumar, Furong Huang, C. Bayan Bruss"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11352v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 基于Transformer的时间序列基础模型
tldr: TimeSqueeze 针对 Transformer 时间序列预测中点对点嵌入效率低和固定长度分块破坏局部动态的问题，提出了一种动态分块机制。该方法通过轻量级状态空间编码器提取特征，并根据信号复杂度自适应调整分块边界，在保留关键时间结构的同时大幅减少 Token 数量。实验证明其在预训练效率和预测精度上均显著优于现有基线。
selection_source: fresh_fetch
motivation: 现有的时间序列分块方法在保持时间保真度与计算效率之间存在权衡，固定长度分块容易模糊局部动态并破坏自然过渡。
method: 引入动态分块机制，利用轻量级状态空间编码器提取特征，并根据信号复杂度自适应地为信息密集区分配短分块，为平滑区分配长分块。
result: 在大规模预训练中实现了高达 20 倍的收敛加速和 8 倍的数据效率提升，且在长程预测基准测试中一致优于同类架构。
conclusion: TimeSqueeze 通过内容感知的变分辨率压缩，有效解决了长序列预测中的效率与精度平衡问题，是高效时间序列建模的新方案。
---

## 摘要
基于 Transformer 的时间序列基础模型在分词（tokenization）策略的选择上面临着根本性的权衡：逐点嵌入（point-wise embeddings）能够保持时间保真度，但其计算规模随序列长度增长的扩展性较差；而固定长度分块（fixed-length patching）虽然通过施加统一边界提高了效率，却可能破坏自然过渡并模糊具有信息量的局部动态。为了解决这些局限性，我们引入了 TimeSqueeze，这是一种动态分块机制，能够根据局部信号复杂度自适应地选择每个序列中的分块边界。TimeSqueeze 首先应用轻量级状态空间编码器（state-space encoder）提取全分辨率的逐点特征，随后通过将短分块分配给信息密集区域、将长分块分配给平滑或冗余片段，实现内容感知分割。这种可变分辨率压缩在保留关键时间结构的同时，显著减少了输入到 Transformer 骨干网络的 token 序列长度。特别是在大规模预训练中，与等效的逐点 token 基线相比，TimeSqueeze 实现了高达 20 倍的收敛加速和 8 倍的数据效率提升。在长程预测基准测试上的实验表明，TimeSqueeze 的性能始终优于使用逐点分词或固定大小分块的同类架构。

## Abstract
Transformer-based time series foundation models face a fundamental trade-off in choice of tokenization: point-wise embeddings preserve temporal fidelity but scale poorly with sequence length, whereas fixed-length patching improves efficiency by imposing uniform boundaries that may disrupt natural transitions and blur informative local dynamics. In order to address these limitations, we introduce TimeSqueeze, a dynamic patching mechanism that adaptively selects patch boundaries within each sequence based on local signal complexity. TimeSqueeze first applies a lightweight state-space encoder to extract full-resolution point-wise features, then performs content-aware segmentation by allocating short patches to information-dense regions and long patches to smooth or redundant segments. This variable-resolution compression preserves critical temporal structure while substantially reducing the token sequence presented to the Transformer backbone. Specifically for large-scale pretraining, TimeSqueeze attains up to 20x faster convergence and 8x higher data efficiency compared to equivalent point-token baselines. Experiments across long-horizon forecasting benchmarks show that TimeSqueeze consistently outperforms comparable architectures that use either point-wise tokenization or fixed-size patching.

---

## 论文详细总结（自动生成）

### TimeSqueeze: 用于高效时间序列预测的动态分块技术 —— 论文深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：Transformer 架构在时间序列预测中面临**分词（Tokenization）策略的权衡**。
    *   **逐点嵌入（Point-wise）**：保留了高频细节，但计算复杂度随序列长度呈平方级增长，难以处理长上下文。
    *   **固定长度分块（Fixed-length Patching）**：提高了效率，但忽略了信号内部的信息密度差异，容易在剧烈变化处模糊特征，或在平滑区域浪费计算资源。
*   **研究动机**：开发一种既能保持高保真度又能实现超高计算效率的 tokenizer，使大规模时间序列基础模型的预训练变得更加可行。

#### 2. 方法论：TimeSqueeze 架构
*   **核心思想**：引入**内容感知（Content-aware）的动态分块机制**，根据信号的局部复杂度自适应分配 Token。
*   **关键技术流程**：
    1.  **SSM 编码器**：使用轻量级的 Mamba（状态空间模型）在全分辨率下提取局部细粒度特征。
    2.  **相对偏差分块（Relative Deviation-based Patching）**：
        *   通过计算相邻样本的绝对差值与局部平均功率（滑动窗口内）的比值来确定边界。
        *   公式逻辑：若 $|x_i - x_{i-1}| > \tau \sqrt{P_i}$，则在该点开启新分块。$\tau$ 是控制压缩率的阈值。
    3.  **动态下采样**：仅保留分块边界处的嵌入，丢弃中间冗余信息，显著减少输入 Transformer 的 Token 数量。
    4.  **MoE Transformer 骨干**：采用混合专家模型（Time-MoE）处理压缩后的序列，捕捉长程依赖。
    5.  **SSM 解码器与残差连接**：通过“反分块”（重复边界 Token）恢复分辨率，并结合编码器的残差特征，由 SSM 解码器生成最终预测表示。
    6.  **多预测头**：支持同时预测多个未来时间步长。

#### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 Time-300B 数据集（包含能源、金融、天气等 3000 亿个数据点）。
    *   **下游评估**：ETT 系列（h1, h2, m1, m2）和 Weather 数据集。
*   **实验场景**：零样本预测（Zero-shot）和全样本微调预测（Full-shot）。
*   **对比方法（Benchmark）**：
    *   **基础模型**：Time-MoE（逐点嵌入基线）、Moirai、TimesFM、Moment、Chronos。
    *   **传统/深度学习模型**：PatchTST、iTransformer、DLinear、TimesNet 等。

#### 4. 资源与算力
*   **硬件环境**：使用 **2 张 NVIDIA A100 80GB GPU**。
*   **训练效率**：
    *   在相同性能下，TimeSqueeze 的预训练速度比逐点嵌入基线快约 **20 倍**。
    *   数据效率提升 **8 倍**（仅需 1/8 的数据即可达到同等效果）。
    *   显存占用降低约 **3.4 倍**。
*   **推理速度**：在长预测周期下，推理吞吐量提升高达 **10.5 倍**。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流基准数据集，测试了 4 种预测长度（96, 192, 336, 720）。
*   **消融实验**：非常充分，对比了“动态 vs 固定分块”、“SSM vs 线性编码器”、残差连接的作用以及位置编码的影响。
*   **客观性**：通过在不同骨干网络（如非 MoE 的 Encoder-only 架构）和不同预训练数据集（如 GiftEvalPretrain）上重复实验，验证了该方法的通用性。

#### 6. 主要结论与发现
*   **效率与精度并存**：TimeSqueeze 在大幅减少计算开销的同时，预测精度（MSE/MAE）与最先进的逐点嵌入模型持平，甚至在某些长程任务中更优。
*   **长上下文优势**：预训练时使用更长的上下文（如 2048）能显著提升短上下文推理的性能。
*   **动态性至关重要**：实验证明，根据信号波动动态调整分块边界，比简单的固定步长下采样能更有效地保留关键时间结构。

#### 7. 优点
*   **内容感知**：解决了固定分块破坏自然过渡的问题，对非平稳时间序列（如金融、电力）更友好。
*   **极高效率**：20 倍的加速使得在有限算力下训练“十亿级”参数的时间序列模型成为可能。
*   **即插即用**：作为一种 tokenizer 方案，它可以无缝集成到现有的各种 Transformer 骨干网络中。

#### 8. 不足与局限
*   **超参数敏感性**：虽然 $\tau$ 具有一定的鲁棒性，但针对不同领域的数据，仍可能需要微调该阈值以达到最优的压缩比与精度平衡。
*   **极端压缩风险**：当目标压缩率过高（如 >8x）时，性能会出现较明显的下降，可能需要更复杂的层次化压缩技术来缓解。
*   **多变量建模**：虽然支持多变量，但论文主要侧重于通道独立（Channel Independence）假设，对于极高维且变量间强耦合的场景探讨较少。

（完）
