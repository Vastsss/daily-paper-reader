---
title: "Grammar of the Wave: Towards Explainable Multivariate Time Series Event Detection via Neuro-Symbolic VLM Agents"
title_zh: 波动语法：通过神经符号 VLM 智能体实现可解释的多变量时间序列事件检测
authors: "Sky Chenwei Wan, Tianjun Hou, Yifei Wang, Xiqing Chang, Aymeric Jan"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11479v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 桥接语言描述与物理时间序列
tldr: 针对多变量时间序列事件检测中语义复杂且标注数据稀缺的挑战，本文提出了“知识引导的TSED”新任务，旨在通过自然语言描述在信号中定位事件。研究引入了事件逻辑树（ELT）框架，利用神经符号VLM智能体将信号可视化原语与逻辑约束相结合，实现了高准确度且具可解释性的零样本事件检测，并有效缓解了视觉语言模型的幻觉问题。
selection_source: fresh_fetch
motivation: 传统的统计检测方法难以处理具有复杂语义结构的事件，且在现实应用中往往面临标注数据严重不足的困境。
method: 提出事件逻辑树（ELT）知识表示框架，并构建神经符号VLM智能体，通过迭代实例化信号原语并按逻辑规则进行组合。
result: 在真实世界基准测试中，该方法在检测性能和解释忠实度上均显著优于监督微调基线及现有的零样本LLM/VLM推理框架。
conclusion: ELT框架成功架起了语言描述与物理信号之间的桥梁，为实现可解释且知识驱动的时间序列分析提供了有效路径。
---

## 摘要
时间序列事件检测（TSED）长期以来是一项重要任务，在许多高风险领域具有关键应用。与统计异常不同，事件由具有复杂内部结构的语义定义，在现实场景中很难从稀缺的标注数据中归纳学习。鉴于此，我们引入了知识引导的 TSED，这是一种新的设定，即在极少或没有训练数据的情况下，给定模型自然语言事件描述，并要求其将其定位到多变量信号的时间区间内。为了应对这一挑战，我们提出了事件逻辑树（ELT），这是一种新颖的知识表示框架，通过建模事件内在的时序逻辑结构，架起了语言描述与物理时间序列数据之间的桥梁。基于 ELT，我们提出了一个神经符号 VLM 智能体框架，该框架从信号可视化中迭代地实例化原语，并在 ELT 约束下对其进行组合，从而生成检测到的区间以及实例化树形式的忠实解释。为了验证我们方法的有效性，我们发布了一个基于真实世界时间序列数据、包含专家知识和标注的基准测试。实验和人工评估表明，与监督微调基准以及现有的基于 LLM/VLM 的零样本时间序列推理框架相比，我们的方法具有优越性。我们还表明，ELT 在减轻 VLM 在匹配信号形态与事件语义时的固有幻觉方面至关重要。

## Abstract
Time Series Event Detection (TSED) has long been an important task with critical applications across many high-stakes domains. Unlike statistical anomalies, events are defined by semantics with complex internal structures, which are difficult to learn inductively from scarce labeled data in real-world settings. In light of this, we introduce Knowledge-Guided TSED, a new setting where a model is given a natural-language event description and must ground it to intervals in multivariate signals with little or no training data. To tackle this challenge, we introduce Event Logic Tree (ELT), a novel knowledge representation framework to bridge linguistic descriptions and physical time series data via modeling the intrinsic temporal-logic structures of events. Based on ELT, we present a neuro-symbolic VLM agent framework that iteratively instantiates primitives from signal visualizations and composes them under ELT constraints, producing both detected intervals and faithful explanations in the form of instantiated trees. To validate the effectiveness of our approach, we release a benchmark based on real-world time series data with expert knowledge and annotations. Experiments and human evaluation demonstrate the superiority of our method compared to supervised fine-tuning baselines and existing zero-shot time series reasoning frameworks based on LLMs/VLMs. We also show that ELT is critical in mitigating VLMs' inherent hallucination in matching signal morphology with event semantics.

---

## 论文详细总结（自动生成）

### 论文总结：Grammar of the Wave (波动语法)

#### 1. 论文的核心问题与整体含义
*   **研究背景**：时间序列事件检测（TSED）在医疗、能源等关键领域具有重要意义。与简单的统计异常检测不同，语义事件（如“压力骤升后保持平稳”）具有复杂的内部逻辑结构。
*   **核心痛点**：
    1.  **数据稀缺**：在现实工业场景中，获取高质量的标注数据成本极高。
    2.  **缺乏可解释性**：传统的深度学习模型（归纳学习）往往是黑盒，难以获得领域专家的信任。
*   **研究动机**：人类专家通常通过阅读描述并结合专业知识来识别信号。论文据此提出了**知识引导的时间序列事件检测（K-TSED）**任务，旨在通过自然语言描述实现零样本（Zero-shot）的事件定位与解释。

#### 2. 方法论
论文提出了 **SELA**（神经符号 VLM 智能体系统），其核心由 **ELT**（事件逻辑树）驱动：
*   **核心思想**：将非结构化的文本描述转化为结构化的逻辑树，通过“演绎推理”而非“模式识别”来定位事件。
*   **事件逻辑树 (ELT)**：
    *   **基元 (Primitives)**：叶节点，定义单通道上的原子形态（如“线性增加”）。
    *   **复合节点 (Composites)**：内部节点，定义时序逻辑算子。
    *   **逻辑算子**：包括 `SEQ`（顺序）、`SYNC`（同步）、`GUARD`（包含）、`OR`（析取）。这些算子基于 Allen 区间代数，具备操作完备性。
*   **SELA 智能体架构**：
    *   **逻辑分析智能体 (Logic Analyst)**：负责将文本解析为 ELT 模式（Schema）。
    *   **信号巡检智能体 (Signal Inspector)**：利用主动可视化工具（如局部缩放、放置参考线）在信号中寻找匹配区间，并根据 ELT 约束计算置信度。
*   **神经符号融合**：利用 VLM 的视觉感知能力实例化基元，利用符号逻辑（模糊逻辑计算）进行全局一致性验证，从而抑制幻觉。

#### 3. 实验设计
*   **数据集 (KITE)**：首个基于真实工业场景（北海油气田压力测试）的 K-TSED 基准测试。
    *   **KITE-easy**：包含简单顺序关系的子阶段事件。
    *   **KITE-hard**：包含复杂的层次嵌套、类别不平衡及互斥事件。
*   **对比方法 (Baselines)**：
    *   **监督学习**：CNN、Transformer，以及时间序列大模型（Timer, Moment, Chronos）。
    *   **LLM/VLM 推理**：Numeric（数字输入）、VL-Time（直接视觉推理）。
    *   **人类基准**：具有行业背景的数据科学家。
*   **评估指标**：采用 IoU 阈值为 0.5 和 0.9 的 F1 分数（F1@0.5, F1@0.9）。

#### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该方法主要基于闭源大模型（如 GPT-4.1, GPT-5）的 API 进行零样本推理，不涉及大规模模型训练，其算力消耗主要集中在推理阶段的 Token 消耗和 API 调用。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个难度等级的数据集上进行了完整对比。
    *   进行了**消融实验**，验证了 ELT 框架在处理复杂逻辑时对缓解 VLM 幻觉的关键作用。
    *   引入了 **Oracle 实验**（使用人工修正的逻辑树），以区分“逻辑解析错误”和“信号定位错误”。
*   **充分性评价**：实验设计较为客观、公平。通过与多种监督学习模型在低资源设定下的对比，证明了演绎推理在小样本/零样本场景下的优越性。引入人类对比进一步增强了结果的说服力。

#### 6. 论文的主要结论与发现
*   **性能卓越**：SELA（尤其是基于 GPT-5 的版本）在零样本设定下显著优于所有现有的 LLM/VLM 推理方法，且在 F1@0.5 指标上接近人类专家水平。
*   **逻辑约束的重要性**：消融实验显示，如果没有 ELT 的逻辑约束，VLM 极易陷入局部形态的误导（幻觉），导致在复杂任务中系统性崩溃。
*   **解释性增强**：系统不仅输出时间区间，还输出实例化的逻辑树，为专家提供了清晰的证据链。

#### 7. 优点
*   **架构创新**：成功将符号逻辑的严谨性与 VLM 的视觉感知能力结合，解决了时间序列语义落地的难题。
*   **实用性强**：针对工业界标注数据匮乏的痛点，提供了无需训练的解决方案。
*   **工具集成**：智能体能够主动使用可视化工具（缩放、标记），模拟了人类专家的分析行为。

#### 8. 不足与局限
*   **高精度定位瓶颈**：在 F1@0.9（极高精度定位）指标上，模型与人类专家仍有较大差距，说明 VLM 在处理细微边界时仍存在视觉精度限制。
*   **模型依赖性**：系统性能高度依赖于底层大模型的指令遵循能力和逻辑推理能力（如 GPT-4 与 GPT-5 表现差异巨大）。
*   **领域覆盖**：目前仅在能源领域验证，对于其他领域（如医疗心电图、金融波动）的泛化能力尚待进一步验证。

（完）
