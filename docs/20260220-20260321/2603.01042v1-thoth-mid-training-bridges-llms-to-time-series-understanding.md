---
title: "Thoth: Mid-Training Bridges LLMs to Time Series Understanding"
title_zh: Thoth：中段训练架起大语言模型与时间序列理解的桥梁
authors: "Jiafeng Lin, Yuxuan Wang, Jialong Wu, Huakun Luo, Zhongyi Pei, Jianmin Wang"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01042v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 通过中期训练连接LLM与时间序列理解
tldr: 本研究提出Thoth，首个通过“中期训练”（mid-training）使大语言模型具备通用时间序列理解能力的模型系列。针对LLM在处理时间序列数据时的局限性，构建了高质量语料库Book-of-Thoth，实现了时序与文本的双向对齐。此外，提出了KnoTS基准测试以评估知识密集型时序推理。实验证明Thoth在多项问答任务中表现优异，且在数据稀缺情况下具有更强的微调潜力。
selection_source: fresh_fetch
motivation: 尽管大语言模型推理能力强大，但在理解和推理时间序列数据方面仍存在显著短板，限制了其在决策场景中的应用。
method: 引入中期训练阶段，利用构建的Book-of-Thoth语料库实现任务无关的时序与自然语言对齐，并提出KnoTS基准进行评估。
result: Thoth在多个时间序列问答基准测试中显著优于基础模型及其他先进LLM，并在小样本微调场景下展现出卓越的性能。
conclusion: 中期训练是弥合时间序列与大语言模型之间鸿沟的有效途径，为构建具备通用时序理解能力的模型奠定了基础。
---

## 摘要
大语言模型（LLMs）在通用推理方面取得了显著成功。然而，它们在理解和推理时间序列数据方面仍然面临挑战，这限制了它们在依赖时间动态的决策场景中的有效性。在本文中，我们提出了 Thoth，这是首个具备通用时间序列理解能力的中段训练（mid-trained）大语言模型系列。作为关键的中间阶段，中段训练实现了时间序列与自然语言之间任务无关且领域无关的对齐。为此，我们构建了 Book-of-Thoth，一个以时间序列为中心的高质量中段训练语料库。Book-of-Thoth 支持时间序列到文本以及文本到时间序列的双向生成，使大语言模型具备了对时间模式的基础掌握。为了更好地评估高级推理能力，我们进一步提出了 KnoTS，这是一个全新的知识密集型时间序列理解基准，旨在对时间模式和领域知识进行联合推理。大量实验表明，通过 Book-of-Thoth 进行的中段训练使 Thoth 在一系列时间序列问答基准测试中显著优于其基础模型和先进的大语言模型。此外，Thoth 在数据稀缺的情况下进行微调时表现出卓越的能力，强调了中段训练对于时间序列理解的有效性。代码可在以下地址获取：https://github.com/thuml/Thoth。

## Abstract
Large Language Models (LLMs) have demonstrated remarkable success in general-purpose reasoning. However, they still struggle to understand and reason about time series data, which limits their effectiveness in decision-making scenarios that depend on temporal dynamics. In this paper, we propose Thoth, the first family of mid-trained LLMs with general-purpose time series understanding capabilities. As a pivotal intermediate stage, mid-training achieves task- and domain-agnostic alignment between time series and natural language, for which we construct Book-of-Thoth, a high-quality, time-series-centric mid-training corpus. Book-of-Thoth enables both time-series-to-text and text-to-time-series generation, equipping LLMs with a foundational grasp of temporal patterns. To better evaluate advanced reasoning capabilities, we further present KnoTS, a novel benchmark of knowledge-intensive time series understanding, designed for joint reasoning over temporal patterns and domain knowledge. Extensive experiments demonstrate that mid-training with Book-of-Thoth enables Thoth to significantly outperform its base model and advanced LLMs across a range of time series question answering benchmarks. Moreover, Thoth exhibits superior capabilities when fine-tuned under data scarcity, underscoring the effectiveness of mid-training for time series understanding. Code is available at: https://github.com/thuml/Thoth.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Thoth** 的模型系列，旨在通过“中段训练”（Mid-Training）提升大语言模型（LLM）对时间序列数据的通用理解与推理能力。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：尽管 LLM 在文本推理上表现卓越，但在处理现实世界中广泛存在的时间序列（TS）数据（如金融、医疗、交通）时，往往难以捕捉细粒度的时序依赖和复杂动态。
*   **现有局限**：目前的方法大多依赖于针对特定任务的监督微调（SFT），这导致模型在数据稀缺时表现不佳，且缺乏跨领域、跨任务的泛化能力。
*   **核心目标**：在预训练和特定任务微调之间引入一个“中段训练”阶段，通过大规模、高质量的时序-文本对齐语料库，赋予 LLM 通用的时序语义理解能力。

### 2. 方法论：中段训练与 Book-of-Thoth
*   **核心思想**：将时间序列视为一种“外语”，通过双向生成任务（时序到文本、文本到时序）实现时序模式与自然语言语义的深度对齐。
*   **关键技术细节**：
    *   **Book-of-Thoth 语料库构建**：
        *   **TS-to-Text**：利用 KernelSynth 合成具有多样化模式（趋势、季节性、噪声等）的时序，并调用 GPT-5.2 生成结构化（详细统计特征）和非结构化（简明摘要）的描述。
        *   **Text-to-TS**：将上述过程反转，训练模型根据文本描述重建时序数值。
        *   **知识集成**：整合经典时序分析教科书（如 Hyndman & Athanasopoulos）的理论知识。
    *   **训练策略**：采用混合训练模式，在时序语料中加入少量通用文本数据（如 C4、No Robots），以缓解“灾难性遗忘”，保持模型的通用推理能力。
    *   **模型架构**：基于 Qwen3 架构（8B 和 30B-A3B 变体），保持骨干网络不变，进行全参数微调。

### 3. 实验设计
*   **数据集与基准（Benchmark）**：
    *   **ChatTime**：评估基础时序模式识别（趋势、波动、季节性、离群点）。
    *   **Time-MQA**：评估真实场景下的多任务问答（预测、填补、分类、异常检测等）。
    *   **KnoTS（新提出）**：知识密集型时序问答基准，要求模型结合领域背景知识（如“气压泵效应”）和时序证据进行联合推理。
*   **对比方法**：涵盖了 15 种模型，包括闭源顶尖模型（Gemini-3-Flash, GPT-4o-mini, Grok-4.1）、开源 LLM（Qwen3 系列, DeepSeek-R1, Llama3.1）、多模态模型（Qwen3-VL）以及时序专用模型（ChatTS, ChatTime-7B 等）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A800 (80GB) GPU**。
*   **训练细节**：采用 DeepSpeed ZeRO-3 进行内存优化；使用 AdamW 优化器；bfloat16 精度；最大序列长度 4096；训练 3 个 epoch，约 1600 个优化步。
*   **时长**：文中未明确给出总训练小时数，但根据参数量和步数推断，属于中等规模的微调实验。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括主流基准测试、新提出的 KnoTS 推理测试、数据缩放（Scaling）实验（从 10% 到 100% 数据量）、以及针对 5% 小样本数据的下游 SFT 潜力测试。
*   **消融实验**：分别移除了 TS-to-Text、Text-to-TS 和通用数据部分，验证了各组件对最终性能的贡献。
*   **客观性**：引入了“LLM-as-a-judge”（使用 Gemini-3-Pro-Preview）进行开放式问题的评分，并进行了 3 次独立运行的误差棒分析（Error Bar Analysis），确保结果的统计可靠性。

### 6. 主要结论与发现
*   **中段训练的有效性**：Thoth-30B 在多个基准上达到了与 235B 参数模型相当甚至更优的水平。
*   **小样本微调潜力**：经过中段训练的 Thoth 在仅使用 5% 下游数据进行微调时，表现远超直接微调的基础 LLM，证明了其作为“热身”阶段的价值。
*   **数据缩放效应**：模型性能随中段训练数据量的增加而稳步提升，显示出良好的可扩展性。
*   **推理能力**：在 KnoTS 基准上，Thoth 展现了将领域知识与时序观察相结合的深度推理能力，优于大多数开源模型。

### 7. 优点与亮点
*   **填补空白**：首次系统性地探讨了“中段训练”在时间序列领域的应用，而非仅仅依赖末端的指令微调。
*   **双向对齐**：通过 Text-to-TS 任务增强了模型对时序数值的敏感度，这在以往研究中较少见。
*   **高质量语料**：Book-of-Thoth 的自动化构建流程结合了合成数据的多样性和前沿 LLM 的语义生成能力。
*   **新基准 KnoTS**：相比于简单的模式识别，KnoTS 更贴近真实世界的复杂决策场景。

### 8. 不足与局限
*   **合成数据偏差**：Book-of-Thoth 主要基于合成数据，虽然模式多样，但可能与极其复杂的真实噪声环境存在分布差异。
*   **生成任务挑战**：消融实验显示 Text-to-TS 的提升效果不如 TS-to-Text 显著，说明精确数值生成对 LLM 仍是巨大挑战。
*   **多任务权衡**：在下游多任务微调时，仍观察到某些任务性能提升伴随其他任务轻微下降的现象（尽管比基础模型更稳健）。
*   **计算成本**：全参数微调 30B 规模的模型对算力仍有一定门槛。

（完）
