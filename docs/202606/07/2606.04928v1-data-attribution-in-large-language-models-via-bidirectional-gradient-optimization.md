---
title: Data Attribution in Large Language Models via Bidirectional Gradient Optimization
title_zh: 基于双向梯度优化的大型语言模型数据归因
authors: "Frédéric Berdoz, Luca A. Lanzendörfer, Kaan Bayraktar, Roger Wattenhofer"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04928v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 直接研究大语言模型的数据归因问题
tldr: 大型语言模型（LLMs）的部署引发数据溯源问题。本文针对自回归LLM的训练数据归因（TDA），提出通过双向梯度优化（梯度上升和下降）扰动生成文本，测量训练样本损失变化。该方法支持细粒度归因，在已知数据集预训练模型上超过基线，提升了模型可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04928v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 470}, {\"url\": \"assets/figures/arxiv/2606.04928v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 831, \"height\": 661}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04928v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1847, \"height\": 330}, {\"url\": \"assets/tables/arxiv/2606.04928v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 261}, {\"url\": \"assets/tables/arxiv/2606.04928v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1842, \"height\": 321}, {\"url\": \"assets/tables/arxiv/2606.04928v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1841, \"height\": 1154}]"
motivation: 理解训练数据对LLM输出的影响是治理与问责的关键，现有TDA方法在自回归模型中效果有限。
method: 对生成样本进行双向梯度优化（上升和下降）扰动模型，计算训练样本损失的变化，实现数据归因。
result: 在预训练模型与已知数据集上，该方法在影响力指标上优于现有基线。
conclusion: 该方法增强LLM可解释性，支持事实和风格归因，促进AI问责系统发展。
---

## 摘要
大型语言模型（LLMs）正被日益广泛地部署于各种应用场景，引发了关于治理、问责和数据溯源的关键问题。理解哪些训练数据对模型输出影响最大，仍然是一个基本的开放性问题。我们通过扩展逆向表述来应对自回归LLMs的训练数据归因（TDA）挑战：如果模型在训练期间看到了生成的输出，训练数据会受到怎样的影响？我们的方法通过对生成文本样本进行双向梯度优化（梯度上升和下降）来扰动基础模型，并测量训练样本上损失的相应变化。我们的框架支持任意数据粒度的归因，能够实现事实性和风格性归因。我们在已知数据集的预训练模型上，将我们的方法与基线进行了评估，并表明其在影响力指标上优于先前的工作，从而增强了模型的可解释性，这是负责任AI系统的关键要求。

## Abstract
Large Language Models (LLMs) are increasingly deployed across diverse applications, raising critical questions for governance, accountability, and data provenance. Understanding which training data most influenced a model's output remains a fundamental open problem. We address this challenge through training data attribution (TDA) for auto-regressive LLMs by expanding upon the inverse formulation: How would training data be affected if the model had seen the generated output during training? Our method perturbs the base model using bidirectional gradient optimization (gradient ascent and descent) on a generated text sample and measures the resulting change in loss across training samples. Our framework supports attribution at arbitrary data granularity, enabling both factual and stylistic attribution. We evaluate our method against baselines on pretrained models with known datasets, and show that it outperforms previous work on influence metrics, thereby enhancing model interpretability, an essential requirement for accountable AI systems.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLM）的输出受其海量训练数据驱动，但如何追溯并量化具体哪些训练样本对某个生成结果最具有影响力，是一个尚未解决的开放性挑战。这一问题关乎模型的治理、问责和数据溯源，对于构建可解释和可信赖的AI系统至关重要。
- **整体含义**：现有的训练数据归因（TDA）方法在自回归LLM上效果有限，尤其是针对开放式的文本生成场景。论文旨在提出一种高效、可扩展且支持细粒度归因的方法，填补这一空白。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：基于“镜像影响假设”（mirrored influence hypothesis），即训练样本对模型输出的影响可以通过模型输出对训练样本的反向影响来估计。具体地，对模型进行双向梯度优化（梯度上升和下降）来扰动生成文本，然后测量训练样本在扰动前后的损失变化，变化越大表明该训练样本对生成结果的影响越大。
- **关键技术细节**：
  - **模型扰动**：给定预训练模型参数θ*和生成的样本ˆx，分别使用Fisher信息矩阵（FIM）对角近似正则化的梯度上升和梯度下降更新参数，得到两个新模型θˆx⁺（上升）和θˆx⁻（下降）。更新公式为：
    \[
    θ^{(m+1)}_\pm = θ^{(m)}_\pm \pm \alpha N F^{-1}_{θ*} \nabla \ell(\hat{x}, θ)
    \]
    其中FIM采用对角近似，降低计算复杂度。
  - **双向影响得分（BIS）**：对于每个训练样本xi，计算其在两个扰动模型下的损失绝对值之差：
    \[
    I(x_i; \hat{x}) = |\ell(x_i, \theta_{\hat{x}}^-) - \ell(x_i, \theta_{\hat{x}}^+)|
    \]
    按BIS降序排列训练样本，得到影响排名。
  - **支持任意粒度归因**：除了样本级，还可计算token级影响：每个token的log-likelihood在扰动模型下的绝对差值。

## 3. 实验设计

- **数据集与场景**：
  1. **事实归因**：Wikipedia摘要（230M tokens），风格统一，迫使归因依赖事实内容。
  2. **风格归因**：Project Gutenberg文学作品（2M tokens），含有丰富的作者风格差异。
- **模型与训练**：两个GPT-2架构模型（上下文窗口256，滑动窗口步长128）分别从零开始在以上语料上预训练。
- **评估基准**：
  - 主要指标：**Tail-Patch Absolute（TPA）分数**——用top-k归因样本对模型做一次梯度更新后，计算生成样本ˆx的似然变化幅度。变化越大认为归因越准确。
  - 额外验证：**完整重训练**——移除top-k归因样本后重新训练模型，观察生成样本损失的上升程度，作为ground truth验证。
- **对比方法**：
  - BM25（基于词项检索）
  - TRACK STAR（基于梯度投影的Hessian近似）
  - Gecko（基于文本嵌入检索）
  - 随机样本基线
- **实验设置**：梯度上升/下降步数M=10，学习率α=1e-4。

## 4. 资源与算力

- 论文未明确说明使用的GPU型号、数量以及训练时长。仅提到训练了两个GPT-2规模模型（Wikipedia: 230M tokens, Gutenberg: 2M tokens）。由于实验规模较小（单机可运行），未详细披露算力成本。这一信息缺失是论文的一个小不足。

## 5. 实验数量与充分性

- **实验数量**：
  - 事实归因：25个生成样本，覆盖k=1~20的多种top-k取值。
  - 风格归因：同样多个样本和k值。
  - 消融实验：比较仅用梯度下降、仅用梯度上升与双向结合的效果（Table 2）。
  - 完整重训练验证：对4个风格样本，移除k=20,50,100归因样本后重训练模型，观察loss变化（Figure 2）。
  - 定性分析：展示事实和风格归因的top proponent示例及token级高亮（Table 4）。
- **充分性与公平性**：
  - 实验覆盖了事实和风格两种典型场景，k值跨度较大，并且与多种基线对比，结果客观。
  - 完整重训练验证提供了强有力的ground truth支撑，避免了TPA指标自身可能存在的偏差。
  - 消融实验证实双向结合优于单向，论证合理。
  - 不足之处：仅在GPT-2规模的模型上验证，未在更大LLM（如Llama 3）上实验；风格归因仅用Gutenberg一个语料库，多样性有限。

## 6. 论文的主要结论与发现

- DABGO在事实和风格归因任务上均显著优于BM25、TRACK STAR和Gecko基线，尤其在事实归因中TPA分数领先，在风格归因中随着k增大表现更优。
- 双向梯度优化（上升+下降）相比单向（仅上升或仅下降）能捕获更完整的信号，归因效果更好。
- 定性结果展示了DABGO能准确命中生成文本中的关键事实短语（如“centered on the city of Rome”）以及同一作者/同一作品中的类似叙事段落。
- 通过完整重训练验证，证明了TPA指标与真实反事实影响高度一致。

## 7. 优点

- 方法简洁且无需存储每个训练样本的梯度，计算成本可控（仅需两次遍历训练语料的计算损失）。
- 支持任意粒度归因，包括样本级和token级，可解释性强。
- 不仅适用于事实性输出，也适用于风格性输出，适用范围广。
- 提供了完整重训练验证，增强了结果的可信度。
- 开源代码，利于复现和后续研究。

## 8. 不足与局限

- **计算成本仍较高**：需要对整个训练语料计算两次损失（两个扰动模型），对于工业级大模型（数百B token）可能不现实。
- **未考虑样本间交互**：归因基于单个样本，无法捕获样本组合的协同或对抗影响。
- **token级归因缺乏因果验证**：仅基于损失变化高亮token，未证明这些token的因果角色。
- **实验规模有限**：仅在GPT-2级模型上测试，未在更大规模LLM（如Llama、GPT-3）上验证。
- **风格归因语料单一**：仅使用了Gutenberg文学作品，未测试其他风格（新闻、技术文档等）。
- **缺少算力消耗报告**：不利于评估方法的实际资源需求。

（完）
