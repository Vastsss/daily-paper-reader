---
title: "From Talking Words to Sharing Thoughts: Scalable Multi-LLM Aggregation via Structured Message Passing"
title_zh: 从言语到思想共享：基于结构化消息传递的可扩展多LLM聚合
authors: "Niloufar Mehrabi, Sayed Pedram Haeri Boroujeni, Abolfazl Razi"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2606.00405v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 直接研究大语言模型集成方法
tldr: "当前多LLM集成方法因反复调用导致高计算成本和锚点损坏。本文提出基于二部因子图的语义层集成框架，通过消息传递协议和不对称阻尼机制迭代解决分歧。在MMLU等基准上，该方法减少97% token使用和6倍API调用，推理时间从分钟降至毫秒，性能优于多智能体基线。该工作证明了图信念传播作为多LLM系统可扩展替代方案的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00405v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 593, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00405v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1032, \"height\": 315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00405v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1296, \"height\": 710, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00405v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1042, \"height\": 297, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00405v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1107, \"height\": 396, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00405v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1263, \"height\": 1876, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00405v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 560, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00405v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1420, \"height\": 359, \"label\": \"Table\"}]"
motivation: 现有集成方法计算成本高且因噪声传播导致锚点损坏。
method: 将LLM作为变量节点，检查节点评估一致性，通过消息传递和不对称阻尼迭代聚合。
result: "四个基准上减少97% token、6倍API调用，推理时间从分钟降至毫秒，性能领先。"
conclusion: 图信念传播是鲁棒、高速、可扩展的多LLM系统替代方案。
---

## 摘要
专门化、领域调优的大型语言模型（LLM）的出现表明，较小的模型能在特定任务中达到专家级性能，但在领域外场景中则表现不佳。当前组合它们互补专长的集成方法主要依赖迭代重提示或跨模型精炼。这些方法存在高计算成本和延迟的问题，因为它们需要重复的LLM推理调用。此外，简单的聚合常常导致锚点损坏，即从较弱模型传播的噪声会降低最准确专家的性能。为了应对这些挑战，我们提出一个框架，使用二部因子图在语义层集成模型预测。在该架构中，单个LLM表示为变量节点，而一组校验节点基于不同的认知标准评估它们的一致性。我们开发了一种受错误恢复系统启发的消息传递协议，以迭代解决分歧。此外，我们引入了一种非对称阻尼机制，保护高可靠性锚点不被集成多数所覆盖。与现有方法不同，我们的方法在输出分布上操作，在精炼阶段不需要额外的LLM调用。在包括MMLU、MMLU-Pro、GPQA和MedMCQA的四个基准测试上评估，我们的方法展示了97%的令牌使用减少和高达6倍的API调用减少，将推理时间从几分钟减少到仅仅几毫秒，同时持续领先于领先的多智能体基线。这些结果表明，基于图的信念传播是当前多智能体LLM系统的一种稳健、高速且可扩展的替代方案。完整的流程和代码将公开。

## Abstract
The emergence of specialized, domain-tuned Large Language Models (LLMs) has demonstrated that smaller models can achieve expert-level performance in specific tasks, while struggling in out-of-domain settings. Current ensemble methods to combine their complementary expertise primarily rely on iterative re-prompting or cross-model refinement. These approaches suffer from high computational costs and latency because they require repeated LLM inference calls. Furthermore, naive aggregation often leads to anchor corruption, in which noise propagated from weaker models degrades the performance of the most accurate expert. To address these challenges, we propose a framework that integrates model predictions at the semantic layer using a bipartite factor graph. In this architecture, individual LLMs are represented as variable nodes, while a set of check nodes assess their consistency based on diverse epistemic criteria. We develop a message-passing protocol inspired by error-recovery systems to resolve disagreements iteratively. Furthermore, we introduce an asymmetric damping mechanism that protects high-reliability anchor nodes from being overridden by the ensemble majority. Unlike existing methods, our approach operates on output distributions and requires no additional LLM calls during the refinement phase. Evaluating on four benchmarks, including MMLU, MMLU-Pro, GPQA, and MedMCQA, our method demonstrates a 97% reduction in token usage and up to a 6X decrease in API calls, reducing inference time from several minutes to mere milliseconds while consistently outperforming leading multi-agent baselines. These results suggest that graph-based belief propagation is a robust, high-speed, and scalable alternative to the current multi-agent LLM systems. The full pipeline and code will be made public.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 核心问题与整体含义（研究动机和背景）
- **问题**：现有的大语言模型（LLM）集成方法（如多智能体辩论、迭代抄写与精炼）依赖反复的自然语言交互，导致 **O(L·N·d)** 的令牌开销和 **O(L·N)** 的串行推理调用，计算成本极高、延迟长达数分钟。同时，直接将弱模型与强模型投票聚合容易引发 **“锚点损坏”**——弱模型噪声会污染强模型（锚点）的预测，反而降低整体性能。
- **背景**：不同领域调优的小模型（如医学、法律、代码）在各自领域表现专家级性能，但跨领域能力弱；如何高效组合它们互补优势成为关键。现有方法（如 MoA、GoA）虽能提升精度，但以高昂推理代价为代价。

### 2. 方法论：核心思想、关键技术细节与算法流程
- **核心思想**：将多 LLM 集成重构为**输出 logit 层的分布融合问题**，用二部因子图进行信念传播，避免任何额外的 LLM 调用。
- **技术细节**：
  - **因子图构建**：每个 LLM 作为变量节点，持有初始 softmax 分布；一组校验节点（检查节点）施加软约束，包括：
    - 分布轮廓节点（ψprof）：根据形状（尖峰/双峰/扩散）加权
    - 校准置信度节点（ψconf）：置信度与历史准确率对齐
    - 决策边际节点（ψmarg）：top-2 概率差结合模型准确率
    - 分歧节点（ψdiss）：少数服从多数，但保护历史准确的少数
    - 领域专长节点（ψdom）：根据查询领域加权
    - 领域关系节点（ψdom-rel）：组合领域专长与成对同意模式
  - **信念软化和再锐化**：在传播前用 T_soft>1 温度软化过置信的分布，传播后用 T_sharp<1 恢复决策性。
  - **非对称自适应阻尼**：锚点模型（高证据×高置信）的阻尼因子 α→0，使其几乎不受外来消息影响；弱模型 α→1，完全接受纠正。更新公式为 `log b^{(t+1)} = log b^{(0)} + α_i Σ log m_{f→i}`，锚定初始状态防止反馈循环。
  - **认知状态检测与路由**：根据熵、置信、边际、成对 KL 散度等计算认知签名，将查询分为共识（直接快速融合）和分歧（激活因子图）六大状态，仅在真正冲突时使用 BP。
- **算法流程**：① 每个模型一次前向 → ② 计算认知签名 → ③ 若共识则直接加权融合；否则 → ④ 软化分布 → ⑤ 迭代消息传递（变量到校验、校验到变量，仅需 CPU 毫秒级） → ⑥ 再锐化 → ⑦ 最终加权融合输出。

### 3. 实验设计
- **数据集**：MMLU（57 学科）、MMLU-Pro（10 选项，14 类别）、GPQA（研究生级科学，完整 Diamond 集）、MedMCQA（医学多选题，验证集）。均采用分层抽样确保统计意义。
- **基准方法**：单模型（General/Code/Math/Biomedical/Finance/Legal）、Debate、Self-Consistency、Refine、ReConcile、MoA、Self-MoA、GoA_max、GoA_mean。
- **对比指标**：准确率、API 调用次数、令牌数、推理时间。

### 4. 资源与算力
- **LLM 推理**：使用 NVIDIA A100 GPU（未说明具体数量或训练时长，因为本文是推理集成，无需训练）。
- **信念传播聚合**：在 CPU 上运行，每查询开销 <0.2 ms。
- **计算效率**：仅需 3 次并行 API 调用（每模型一次），而 MoA 需要 19 次、GoA 需要 11 次，令牌消耗减少 97%。

### 5. 实验数量与充分性
- **主要实验**：在四个不同难度/领域的基准上对比 10+ 种基线方法，报告准确率（表 2）和效率（表 3）。
- **消融实验**：逐一移除六个校验节点，验证每个组件的贡献（表 4）。
- **可扩展性实验**：在 GPQA 上使用 10 个 agent，对比 Refine、MoA、Self-MoA、ReConcile（表 6），显示本方法随规模保持稳健。
- **领域性能分析**：在 MMLU 按人文、社科、STEM、其他分类展示准确率（表 7）。
- **充分性判断**：实验覆盖多种推理复杂度、广泛对比、消融全面、扩展性验证，设计客观公平。但缺少超参数敏感性分析（如 γ 和温度）。

### 6. 主要结论与发现
- 本文提出基于因子图和消息传递的语义层融合框架，在所有四个基准上取得最高准确率（MMLU 79.80%, MMLU-Pro 55.36%, GPQA 43.64%, MedMCQA 62.83%），同时推理时间从分钟降至毫秒。
- 非对称阻尼有效防止锚点损坏：锚点模型仅产生微小偏差（定理 1 保证），而弱模型被大幅纠正。
- 效率飞跃：令牌使用减少 97%，API 调用减少 3–6 倍，无需任何额外 LLM 生成。
- 可扩展性：10 个 agent 时仍优于所有文本基线，而 MoA 等退化。

### 7. 优点
- **新颖视角**：将 LLM 集成映射为 LDPC 式信念传播，引入编码理论成熟技术。
- **效率极高**：彻底消除串行文本交互，计算瓶颈从 GPU 转为 CPU 毫秒级运算。
- **保护锚点**：自适应阻尼理论保证强模型不被弱模型污染。
- **模块化设计**：六个校验节点可组合/置换，适应不同认知状态。
- **轻量路由**：认知状态检测仅 O(N²K)，快速决策是否启用 BP。

### 8. 不足与局限
- **仅限多项选择**：方法依赖输出 softmax 分布，无法直接处理开放生成或自由文本推理。
- **分布可得性**：需模型暴露 logits/softmax，部分 API 限制或闭源模型可能无法提供。
- **独立性假设**：校验节点设计假设模型间条件独立（如成对关系），但实际错误可能相关，论文未讨论相关性建模。
- **超参数依赖**：温度（T_soft, T_sharp）、γ 等需手动设定，论文未给出鲁棒性分析。
- **规模局限**：实验仅用 7-8B 模型，更大模型（>70B）或更多样化组合未验证。
- **缺乏推理链分析**：未说明信念传播如何影响最终解释性或可解释性。

（完）
