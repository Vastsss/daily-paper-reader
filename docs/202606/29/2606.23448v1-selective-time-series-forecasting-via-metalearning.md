---
title: Selective Time Series Forecasting via Metalearning
title_zh: 基于元学习的选择性时间序列预测
authors: "Ricardo Inácio, Vitor Cerqueira, Marília Barandas, Carlos Soares"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23448v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 通过元学习进行选择性时间序列预测
tldr: 深度学习方法在时间序列预测中性能差异大，现有拒绝机制依赖领域特定代理难以泛化。本文提出基于元学习的选择性预测框架，通过建模预测误差经验百分位数，利用滞后结构特征解耦拒绝决策。实验表明，在域内和迁移学习中，拒绝困难样本持续提升不同覆盖率下的预测精度。该框架实现了跨异构时间序列的有效弃权迁移。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23448v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1260, \"height\": 382, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23448v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1265, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23448v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1250, \"height\": 412, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23448v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1227, \"height\": 294, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23448v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1263, \"height\": 367, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23448v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 758, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23448v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1209, \"height\": 1030, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23448v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1164, \"height\": 1149, \"label\": \"Table\"}]"
motivation: 现有拒绝策略依赖预测区间或置信度得分，受限于训练域，无法跨序列泛化。
method: 通过元学习从最近滞后提取结构特征，建模预测误差的尺度不变统计量，解耦拒绝决策。
result: 在域内和迁移学习实验中，拒绝困难样本持续提升各覆盖率下的预测精度。
conclusion: 选择性预测框架有效实现跨异构时间序列的弃权迁移，显著提升预测可靠性。
---

## 摘要
深度学习方法在时间序列预测中取得了最先进的结果，但其准确性在不同样本之间存在显著差异，因为某些实例本质上难以预测。拒绝选项机制允许模型放弃高风险预测，在分类和回归中已经得到充分研究，但在预测中探索不足。现有的放弃策略通常依赖于代理，如预测区间的宽度或从预测中学习到的置信度分数。然而，这些方法本质上与训练领域绑定，限制了它们的泛化能力。我们提出了一种选择性预测框架，通过基于元学习从最近滞后中提取的结构特征来建模预测误差的经验百分位数（即尺度不变的统计量），从而解决了这一限制。通过将拒绝决策与预测本身解耦，并基于领域无关的特征，该框架能够实现异构时间序列间有效的放弃迁移。在领域内和迁移学习设置中的实验表明，拒绝预测为具有挑战性的样本可以在不同的覆盖率水平上持续提高预测准确性。

## Abstract
Deep learning methods have achieved state-of-the-art in time series forecasting, yet their accuracy varies considerably across samples, as some instances remain inherently difficult to predict. Reject option mechanisms, which allow models to abstain from high-risk predictions, are well established in classification and regression but underexplored in forecasting. Existing abstention strategies typically rely on proxies, such as the width of the prediction interval or learned confidence scores derived from forecasts. However, these approaches are inherently tied to the training domain, limiting their ability to generalize. We propose a selective forecasting framework that addresses this limitation by modeling the empirical percentile of forecasting errors, that is, a scale-invariant statistic, based on structural characteristics extracted from recent lags via metalearning. By decoupling the rejection decision from the forecast itself and grounding it in domain-agnostic features, the framework enables effective abstention transfer across heterogeneous time series. Experiments in both in-domain and transfer learning settings show that rejecting samples predicted as challenging consistently improves forecasting accuracy across coverage levels.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：深度学习方法在时间序列预测中性能出众，但准确性随样本剧烈波动，某些时段/实例本质难预测。现有拒绝选项（reject option）机制多用于分类和回归，在预测中研究不足；且现有方法常依赖预测区间宽度或置信度分数，这些代理与训练域强绑定，难以泛化到新领域或不同特征的时间序列。
- **整体意义**：本文提出**基于元学习的选择性预测框架**，通过建模预测误差的经验百分位数（尺度不变统计量），利用最近滞后窗口的结构特征（如趋势、季节性、复杂度）来预测每个预测原点的相对风险，从而在发出预测前决定是否弃权。框架解耦了拒绝决策与预测模型及数据尺度，使得弃权机制可以跨异构时间序列迁移，提升预测可靠性。

#### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将拒绝问题转化为**预测误差百分位数的回归问题**，输入为滞后窗口的元特征，输出为当前原点在所属序列内误差的排名（百分位）。由于百分位归一化且特征提取自局部结构而非预测输出，该映射可跨域泛化。
- **关键技术细节（两阶段流程）**：
  1. **性能估计（Performance Estimation）**：在源域上使用滚动原点评估（rolling origin evaluation），对每个预测原点，训练预测模型（如KAN或NHITS）到当前时间，计算后续h步的sMAPE误差 \(e_{i,t}\)。对每个序列，将所有误差转换为**经验百分位数** \(u_{i,t} = \frac{\#\{j: e_{i,j} < e_{i,t}\}}{|T_i|+1}\)，得到尺度不变的目标。
  2. **元模型训练（Meta-model Training）**：从每个滞后窗口 \(X_{i,t}\)（长度为p，p=2h）提取结构化特征（使用TSFEL库，包括统计、时域、谱域描述子）构成元特征向量 \(m_{i,t}\)。元模型（CatBoost回归器）学习 \(m_{i,t} \rightarrow u_{i,t}\) 的映射。
  3. **推理（Inference）**：对新滞后窗口提取元特征，元模型输出预测百分位 \(\hat{u}_{i,t}\)，若 \(\hat{u}_{i,t} \geq q\)（q为预定义阈值）则拒绝该预测，否则接受。支持零样本（源域直接应用）和域适应（用目标域少量标注数据微调元模型）两种模式。

#### 3. 实验设计：数据集、场景、对比方法
- **数据集**：使用M3、M1和Tourism三个公开时间序列集合，仅选取月度（M）和季度（Q）频率。
- **场景**：配对源/目标域：M3→M1（分M和Q）、M1→Tourism（M和Q）。每个场景包含：
  - 源域内评估（S）：用源域最后h个观测作为留出集。
  - 零样本迁移（Z）：源域训练的元模型直接应用于目标域留出集。
  - 域适应迁移（A）：用目标域前30%的窗口微调元模型后评估。
- **预测模型**：KAN（Kolmogorov-Arnold Networks）和NHITS（神经分层插值），均使用NeuralForecast实现并自动调参。
- **元模型**：CatBoost回归器，超参数通过30次随机搜索+分组交叉验证选择。
- **基线方法**：
  - PI-width：基于预测区间宽度（90%分位数，使用MAD归一化残差）
  - Residual-scale：最近原点sMAPE值的MAD
  - Residual-variance：基于点残差学生t区间宽度
  - Random：随机拒绝（下界）
  - Oracle：按真实错误拒绝（上界）
- **评估指标**：
  - 元级：Spearman秩相关性（ρ）、AUCO（风险-覆盖率曲线与Oracle间的面积）、ErrDrop（无拒绝错误与最严格拒绝时错误之比）
  - 基级：不同拒绝比例q（0.05,0.1,0.2,0.3,0.4）下的保留窗口sMAPE，及与Oracle的平均差距。

#### 4. 资源与算力
- **文中未明确说明**所用GPU型号、数量、训练时长等具体算力信息。仅提及：
  - 预测模型使用NeuralForecast进行自动超参数搜索（10次随机搜索）。
  - 元模型CatBoost的超参数通过30次随机搜索选择。
  - 未给出单次实验时长或总体计算资源。

#### 5. 实验数量与充分性
- **实验数量**：
  - 2个预测模型 × 4个数据集配对场景（M3-M→M1-M; M3-Q→M1-Q; M1-M→T-M; M1-Q→T-Q） = 8个主要配置。
  - 每个配置包含源域、零样本、域适应三种评估模式。
  - 元级评估给出ρ、AUCO、ErrDrop（共24组核心元级结果）。
  - 基级评估测试5个拒绝比例（q=0.05,0.1,0.2,0.3,0.4），并计算每个方法的平均差距，共8（配置）× 5（q） = 40组基级结果。
  - 额外进行系列级bootstrap显著性检验（确认元模型显著优于其他基线，仅一例p=0.191）。
- **充分性与公平性**：
  - 覆盖域内和迁移学习，同时考虑零样本和域适应，设计较为全面。
  - 对比了多种基线，包括基于不确定性（PI-width）、基于残差（Residual-scale/variance）、随机和Oracle。
  - 缺点：仅使用三个数据集（M3、M1、Tourism）且仅月度/季度频率，缺乏对更广泛数据（如日频、高频、长序列）的验证；基线主要基于最近残差，未与其他专门用于预测的拒绝方法（如能量模型、VAE-based novelty）对比。

#### 6. 论文的主要结论与发现
- **元模型能有效预测大误差**：在源域内Spearman秩相关ρ可达0.7-0.9，AUCO接近Oracle，ErrDrop表明可大幅降低保留预测错误。
- **零样本迁移性能下降但域适应可显著恢复**：零样本时ρ降至0.5-0.6，域适应后回升至0.71-0.82，AUCO和ErrDrop同步改善。
- **优于所有不确定性/残差基线**：在多数场景下，元模型在ρ、AUCO、基级sMAPE方面优于PI-width、Residual-scale、Residual-variance，且在系列级bootstrap测试中显著成立。
- **模型无关性**：在KAN和NHITS两种预测模型上表现一致。
- **选择性拒绝提升实际预测精度**：在基级评估中，随着拒绝比例增加，保留窗口的sMAPE持续下降，且元模型始终保持最低平均Oracle差距。

#### 7. 优点：方法或实验设计上的亮点
- **方法创新**：首次将元学习用于预测拒绝，采用**尺度不变目标（百分位）** 和**领域无关特征（结构特征）**，实现跨域迁移，与预测模型解耦，支持事前筛选。
- **实用性强**：可在发出预测前决定是否拒绝，无需等待预测输出；支持零样本和轻量域适应，适应实际部署。
- **实验设计严谨**：同时评估元级和基级性能，引入AUCO、ErrDrop等综合指标，进行bootstrap显著性检验，对比多种基线。
- **开源代码和数据**：提供GitHub仓库，可复现。

#### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制
- **数据域有限**：仅使用三个数据集（M3、M1、Tourism）且限于月度/季度频率，未测试更高频或低样本量场景。
- **基线选择局限**：对比方法限于基于最近残差的启发式（PI-width、Residual-scale/variance），未包括更先进的深度学习拒绝方法（如Time-Energy Model、VAE-based novelty rejection），可能低估了现有方法在域内的表现。
- **依赖元训练数据代表性**：若目标域序列长度短或特征分布严重偏移，零样本性能可能差；域适应需要部分标注数据。
- **无概率校准保证**：该方法输出相对风险排名，而非校准的预测置信区间，无法提供形式化的不确定性量化。
- **计算成本未讨论**：未给出元模型训练时间、推理开销，在超大规模序列集合上的可扩展性未知。

（完）
