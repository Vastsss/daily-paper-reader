---
title: "Speaking Numbers to LLMs: Multi-Wavelet Number Embeddings for Time Series Forecasting"
title_zh: 向LLM讲述数字：用于时间序列预测的多小波数字嵌入
authors: "Defu Cao, Zijie Lei, Muyan Weng, Jiao Sun, Yan Liu"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26487v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: LLM时间序列预测与数字嵌入
tldr: 大型语言模型的离散语言接口与连续数值不对齐，损害数值排序和预测可靠性。本文提出TempoWave，一种即插即用的多小波数字嵌入接口，将每个标量观测转换为基于多小波多尺度系数的数字级嵌入，直接覆盖标准token表示。在五个上下文丰富的时间序列预测基准上，TempoWave一致提升了LLM预测器的性能，达到新SOTA。该工作表明数值接口是关键瓶颈，多分辨率嵌入能更好结合LLM的上下文推理与精确数值预测。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26487v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1638, \"height\": 516, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26487v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 795, \"height\": 485, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26487v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 797, \"height\": 486, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26487v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 796, \"height\": 485, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26487v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1830, \"height\": 1100, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26487v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1821, \"height\": 369, \"label\": \"Table\"}]"
motivation: 解决LLM离散语言接口与连续数值的错位问题，提升时间序列预测中数值排序与可靠性。
method: TempoWave将标量观测转化为多小波多尺度数字嵌入，直接替换标准token表示，保持数值格式与数字身份。
result: 在五个时间序列基准上，TempoWave优于标准数值分词与替代嵌入接口，实现新SOTA。
conclusion: 多分辨率数字嵌入是LLM时序预测的关键优化方向，有效耦合上下文推理与数值精度。
---

## 摘要
大型语言模型（LLMs）因能整合异构文本信号，对上下文感知的时间序列预测具有吸引力，然而其离散的、面向语言的词元化与嵌入接口与连续数值不匹配，常常损害数值顺序与预测可靠性。我们提出TempoWave，一种即插即用的时间小波数字接口，将每个标量观测值映射为由多小波、多尺度系数构建的逐位嵌入。通过直接覆盖标准词元表示，TempoWave以Transformer兼容的形式无缝展现细粒度局部波动与宏观全局结构，确保在整个LLM流程中保持精确的数字格式化、独特的数字身份以及对常见归一化操作的鲁棒性。在五个上下文增强的预测基准上的实验表明，TempoWave相对于标准数字词元化与替代嵌入接口持续改进了基于LLM的预测器，达到了新的最先进水平。这些结果凸显了数字接口是一个关键瓶颈，并表明原则性的多分辨率嵌入能更好地将LLM的上下文推理与精确预测结合起来。我们的代码可在https://github.com/DC-research/TempoWAVE获取，模型可在https://huggingface.co/Melady/TempoWAVE访问。

## Abstract
Large language models (LLMs) are attractive for context-aware time series forecasting because they can integrate heterogeneous textual signals, yet their discrete, language-oriented tokenization and embedding interfaces are misaligned with continuous numerical values, often harming numerical ordering and forecasting reliability. We propose TempoWave, a plug-and-play temporal wavelet digit interface that maps each scalar observation into digit-wise embeddings constructed from multi-wavelet, multi-scale coefficients. By directly overriding standard token representations, TempoWave seamlessly exposes both fine-grained local fluctuations and macro global structures in a transformer-compatible form, ensuring that precise numerical formatting, distinct digit identity, and robustness to common normalization operations are maintained throughout the LLM pipeline. Experiments across five context-enriched forecasting benchmarks demonstrate that TempoWave consistently improves LLM-based forecasters over standard numeric tokenization and alternative embedding interfaces, achieving a new state-of-the-art. These results highlight the numeric interface as a key bottleneck and suggest that principled multi-resolution embeddings can better couple LLMs' contextual reasoning with precise forecasting. Our code is available at https://github.com/DC-research/TempoWAVE and our model can be accessed at https://huggingface.co/Melady/TempoWAVE.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）
- 大型语言模型（LLMs）因能整合文本上下文，在时间序列预测中具有潜力，但其离散的、面向语言的词元化与嵌入接口与连续的数值数据严重不匹配。
- 语言导向的词元化会破坏数值的序关系，例如将“2026”拆分为“20”和“26”，导致相近数值映射到不同词元序列，而远距离数值却可能共享子词元，引入虚假相似性。
- 这一“数值接口”瓶颈成为LLM用于时序预测的关键障碍，当前方法（如量化、符号化、外部工具）往往牺牲数值保真度或丢失多尺度结构。
- 论文旨在设计一种数值上可理解的嵌入，在保持LLM骨干不变的前提下，通过多分辨率分解保留数值关系与多尺度时序结构。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：提出**Multi-Wavelet Number Embedding (TempoWave)**，一种即插即用的数字级嵌入接口，将每个标量观测值映射为多小波、多尺度系数的稠密向量，直接替换LLM中对应数字token的嵌入。
- **关键技术细节**：
  1. **数字-词元映射**：将归一化后的数值转为固定精度数字串（如`V.FFFF`），每个数字作为独立token（`0`-`9`），文本/上下文token使用原始LLM嵌入，数字token路由至TempoWave模块。
  2. **多小波系数计算**：将每个数字归一化至`[0,1]`，表示为固定网格上的单位脉冲信号`fd`；选择多个小波基（如Haar、db4、Morlet、MexHat、sym4）及多个尺度，计算脉冲信号与小波的离散内积得到系数`Wψ,s(d)`。
  3. **嵌入构建**：将所有系数拼接为特征向量`ϕ(d)` ∈ ℝ^{kl}，通过固定映射（零填充或线性投影）对齐到LLM嵌入维度`D`，得到数字嵌入`E(d)`。由于只有10个数字，可预计算为小型嵌入表。
  4. **多尺度表示优势**：通过多小波、多尺度系数，每个嵌入编码了数字的局部细节与全局结构，对后续LayerNorm等归一化操作鲁棒，保持数字可区分性。
- **算法流程**：给定数值`x` → 提取数字序列 → 对每个数字计算系数 → 映射得嵌入 → 覆盖标准嵌入 → 输入LLM进行next-token预测（SFT训练）。

## 3. 实验设计
- **数据集/场景**：使用5个上下文丰富的预测基准：
  - **AUL**（澳大利亚用电，含新闻）、**BIT**（比特币，含新闻）
  - **MSPG**（墨尔本太阳能发电，含天气）、**PTF**（巴黎交通流量）、**LEU**（伦敦家庭用电）
  - 每个样本包含历史数值窗口及背景描述、天气、日期、新闻等文本上下文。
- **任务**：根据历史窗口+上下文预测未来k个值（生成式：输出固定精度数字串）。
- **基准方法**：
  - **时序专用模型**：DLinear、Autoformer、TimesNet、PatchTST、iTransformer、N-BEATS
  - **时序基础模型**：TimeLLM、Chronos、Moirai、ChatTime（上下文感知基线）
  - **数值嵌入接口**：FoNE（傅里叶数字嵌入）
- **公平性**：所有方法使用相同提示模板和上下文，仅数字接口不同；采用相同的前/后处理规则。
- **评估指标**：RMSE、MAE（反归一化后计算）。

## 4. 资源与算力
- 论文中**未明确说明**使用的GPU型号、数量、训练时长等算力细节。
- 仅提到代码开源（GitHub）和模型托管（Hugging Face），未提及实验硬件配置。

## 5. 实验数量与充分性
- **主实验**：5个数据集 × 2个指标（RMSE/MAE），对比10+基线，结果报告在表1。
- **消融实验**（表2）：将上下文递进移除（无上下文、无Catch22、无情境上下文、全上下文），评估各组件的贡献。
- **诊断分析**：通过预测token ID差分布（图2-4）比较TempoWave与FoNE、ChatTime的嵌入对齐质量。
- **充分性评估**：
  - 覆盖了新闻驱动型（AUL/BIT）和传感器型（MSPG/PTF/LEU）多种场景。
  - 基线选择全面，包含传统模型、基础模型以及替代数值接口。
  - 消融实验设计合理，验证了上下文互补性。
  - **不足**：仅使用Qwen2.5-1.5b作为LLM骨干，未在不同规模或架构（如LLaMA、GPT系列）上验证；未在纯数值（无上下文）设置下实验（论文附录提及为未来工作）；未报告统计显著性检验。

## 6. 主要结论与发现
- TempoWave在7/10个指标上达到新SOTA，平均MAE相对改进约7.0%。
- 在新闻驱动（AUL/BIT）上增益最大（MAE改善11.2%、6.3%），在传感器数据集上表现稳健。
- MAE改进比RMSE更一致，说明TempoWave减少典型误差，对极端值效果相对较弱。
- 对比FoNE（同类数值嵌入接口），TempoWave在BIT等非平稳场景下性能显著提升（RMSE从1.71降至0.80）。
- 上下文信息与TempoWave互补：全上下文配置最优，Catch22统计特征贡献最大。
- 嵌入对齐分析：TempoWave诱导接近高斯分布的token预测差，而FoNE分布更平坦，标准LLM出现异常尖峰（重复预测固定token）。

## 7. 优点
- **方法创新**：首次将多小波多尺度分解引入LLM数字嵌入，保留了数值的序关系与多分辨率结构。
- **实用性**：即插即用，不修改LLM骨干，只需在嵌入层替换数字token；仅有10个数字，计算开销极小。
- **鲁棒性**：理论分析表明对LayerNorm/RMSNorm等归一化操作不敏感，保持数字可区分性。
- **实验扎实**：涵盖多种场景、大量基线、消融与诊断分析，结果客观可信。
- **开源友好**：代码和模型均已公开，便于复现和推广。

## 8. 不足与局限
- **骨干模型单一**：仅测试了Qwen2.5-1.5b，未验证在更大LLM（如7B、13B）或其他架构上的泛化能力。
- **未覆盖纯数值场景**：实验仅关注上下文丰富场景，未评估非上下文（仅数值序列）预测，论文也将其列为未来工作。
- **可复现细节缺失**：未报告实验硬件、训练时长、超参数选择（如小波基个数、尺度集合、对齐映射方式），影响完全复现。
- **推广性限制**：五个数据集虽代表性较强，但均来自特定领域（能源、交通、金融），在医疗、气候等其他领域的效果有待验证。
- **极端值处理**：论文未专门分析对异常值或极值的鲁棒性（RMSE改进较小可能暗示对极端误差敏感）。
- **统计检验欠缺**：未提供置信区间或显著性检验，结论的统计可靠性有待加强。

（完）
