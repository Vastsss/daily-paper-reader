---
title: "APEX: A Network-Native Time-Series Foundation Model for Forecasting and Anomaly Detection for Wireless Edge Operations"
title_zh: APEX：面向无线边缘运维的预测与异常检测的网络原生时序基础模型
authors: "Swadhin Pradhan, Niloo Bahadori, Peiman Amini"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11553v1"
tags: ["query:ts-fm"]
score: 9.0
evidence: 时间序列基础模型
tldr: "无线网络遥测信号具有突发、零膨胀和跨层耦合特性，通用时间序列基础模型迁移效果差。为此提出APEX，一种网络原生的decoder-only transformer模型，在约4500个生产网络的100万AP时间序列上预训练。在DHCP退化基准任务上，APEX-Large相比最强基线MAE降低18%，F1达0.93；APEX-Edge可在边缘硬件上亚秒级推理。结果表明网络原生预训练为主动无线运维提供了实用基础。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11553v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1052, \"height\": 540, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11553v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 646, \"height\": 424, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11553v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 879, \"height\": 460, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11553v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 758, \"height\": 426, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11553v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 746, \"height\": 350, \"label\": \"Table\"}]"
motivation: 通用时间序列基础模型因忽视无线网络遥测特性（突发、零膨胀、跨层耦合）而迁移效果差。
method: 提出网络原生的decoder-only transformer APEX，在百万级AP多变量遥测数据上预训练，支持云端大模型和边缘小模型。
result: "在DHCP退化任务上，APEX-Large比最强基线MAE降低18%、F1=0.93；APEX-Edge实现边缘亚秒级隐私推理。"
conclusion: 网络原生预训练是主动无线运维的实用基础，兼顾性能与边缘部署。
---

## 摘要
通用时序基础模型难以有效迁移到无线网络遥测数据，因为这类信号具有突发性、零膨胀性，并且跨协议层耦合。我们提出了APEX，一个面向企业AP遥测预测的网络原生仅解码器Transformer，并以DHCP退化作为代表性网络任务对其进行评估。APEX在来自约4,500个生产无线网络的10通道多变量遥测数据（约10万条AP时间序列，每个AP34个指标）上进行了预训练，并以APEX-Large（269M，云端）和APEX-Edge（10.5M，边缘）两种形式提供。在192步（4天）的DHCP退化基准测试中，APEX-Large的MAE比最强的基础模型基线（Toto）降低了18%，比SARIMA降低了38%，异常检测F1=0.93；而APEX-Edge能够在AP级边缘硬件上实现亚秒级、隐私保护的推理。这些结果表明，网络原生预训练是主动无线运维的实用基础。

## Abstract
Generic time-series foundation models transfer poorly to wireless network telemetry whose signals are bursty, zero-inflated, and coupled across protocol layers. We present APEX, a network-native, decoder-only transformer for forecasting enterprise AP telemetry, and evaluate it on DHCP degradation as a representative network task. APEX is pre-trained on 10-channel multivariate telemetry from ~4,500 production wireless networks (~100K AP time series, 34 metrics per AP), and is available as APEX-Large (269M, cloud) and APEX-Edge (10.5M, edge). On a 192-step (4-day) DHCP degradation benchmark, APEX-Large reduces MAE by 18% over the strongest foundation-model baseline (Toto) and 38% over SARIMA, with anomaly-detection F1 = 0.93, while APEX-Edge enables sub-second, privacy-preserving inference on AP-class edge hardware. These results suggest network-native pre-training is a practical foundation for proactive wireless operations.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **问题**：通用时间序列基础模型（如TimesFM、Toto、Chronos）在无线网络遥测数据上迁移效果差，因为网络信号具有**突发性、零膨胀性、跨协议层耦合**等独特特性，与公开基准（金融、能源、天气）分布差异大。
- **背景**：企业无线接入点（AP）故障（如DHCP超时）常被用户感知后才被发现，但AP本身可收集丰富遥测数据。云端导出原始遥测存在带宽、隐私、延迟成本，希望直接在AP端实现主动预测和异常检测。
- **意义**：论证了**网络原生预训练**的实用性——通过专属数据集和协议感知的因果链输入，显著优于通用基础模型，并能在资源受限的AP硬件上部署。

### 论文提出的方法论

- **核心思想**：构建一个**网络原生的decoder-only patched transformer**，在大量真实生产网络遥测数据上预训练，学习协议层间的依赖关系。通过**多变量因果链输入**（10通道，包含DHCP因果链目标和外生拓扑/异常信号）捕获跨层信号，统一实现预测和异常检测。
- **技术细节**：
  - **架构**：decoder-only Transformer，输入时间序列经z-score归一化后划分为**非重叠补丁（patch长度P=16）**。补丁线性投影为d_model维，加上可学习位置编码，使用因果自注意力。前馈网络采用**SwiGLU**激活。
  - **两个变体**：APEX-Large（269M参数，16层，d_model=1024，16头）用于云端；APEX-Edge（10.5M参数，10层，d_model=256，4头）用于AP边缘。
  - **训练**：MSE损失，AdamW优化器（β1=0.9, β2=0.95，权重衰减0.05），余弦学习率带5% warmup，梯度裁剪1.0，混合精度（AMP），梯度检查点，4×A10G GPU分布式数据并行（DDP）。深度缩放初始化稳定训练。早停（验证损失patience=5）。
  - **不确定性量化**：推理时保持dropout，进行**MC-dropout**（50次随机前向）得到P5/P95分位数作为预测区间，无需额外模型。
  - **数据预处理**：两阶段聚合，每30分钟一个时间点，每个AP得到34个指标，从中选择10个关键通道（5个DHCP因果链目标+5个外生信号）。
- **算法流程**（文字描述）：
  1. **Phase1（离线，云端）**：从约4500个生产网络收集历史遥测→分层聚合/预处理→在34个指标（10通道多变量或单变量）上预训练APEX→导出APEX-Edge检查点（~40MB）。
  2. **Phase2（在线，边缘）**：AP本地收集实时遥测→应用相同预处理→运行APEX-Edge推理，产生预测和异常告警。仅传输紧凑告警（~KB/天），原始数据不离设备。

### 实验设计

- **数据集/场景**：来自**约4500个生产无线网络**的约**10万条AP时间序列**，每个AP有34个指标（22个DHCP协议、6个RF、2个接口错误、4个拓扑）。默认时间粒度30分钟（每天48个观测点）。训练/测试分割：每个AP最后**192个时间步（4天）**作为测试集，其余用于训练。
- **基准任务**：**DHCP退化**预测与异常检测（代表跨层故障场景）。预测指标为**CLIENT_DHCP_SUCCESS_RATE**的未来192步。
- **对比方法**：
  - **统计模型**：SARIMA、VAR
  - **通用基础模型**：TimesFM（200M）、Toto（151M）、Chronos-2（120M）——均保持零样本迁移，未在网络数据上微调。
  - **APEX变体**：APEX-Large (multi/1D)、APEX-Edge (multi/1D)
- **异常检测**：对比单变量（Z-Score、Isolation Forest、SARIMA CI）和多变量（VAR-Mahalanobis、SARIMAX CI、基础模型集合、APEX MC-dropout）方法。**共识伪标签**：时间步被≥3个独立方法标记则为异常。

### 资源与算力

- 文中明确提到：训练使用**4×A10G GPU**，采用混合精度、梯度检查点、分布式数据并行（DDP）。但**未给出具体训练时长**（如epoch数或总时间），也未说明预训练数据规模的具体时长（如多少时间步）。需指出这一信息缺失。

### 实验数量与充分性

- **实验数量**：
  - 预测性能对比（Table 2）：仅报告了**单一指标**（CLIENT_DHCP_SUCCESS_RATE）的MAE、RMSE、MAPE。未展示其他34个指标的结果。
  - 异常检测性能（Table 3）：报告了Precision、Recall、F1，对比9种方法。
  - 消融实验：设计了**2×2设计**（大小×模态），清晰分离了模型容量和跨通道结构的贡献。
  - 边缘部署实验（Table 4）：测量延迟和内存。
- **充分性评价**：
  - **优点**：消融设计合理，对比基线全面（统计、通用FM、网络原生），验证了网络原生预训练和因果链输入的必要性。
  - **不足**：
    1. **评估指标单一**：仅观测DHCP success rate，未扩展到其他协议（如RF、漫游）或指标。
    2. **异常检测标签为共识伪标签**，而非人工标注，可能引入偏差或遗漏真实异常模式。
    3. **零样本迁移对比不完全公平**：通用FM未在APEX训练数据上微调或领域内重训（但论文目的正是证明预训练数据差异），不过若允许微调，结果可能变化。
    4. **未与轻量级TSFM（如小版Chronos或蒸馏模型）对比**，边缘部署优势的边际不够清晰。

### 论文的主要结论与发现

1. **网络原生预训练大幅提升预测精度**：APEX-Large (multi) 在DHCP success rate预测上MAE为2.98，比最强通用FM Toto（3.64）降低**18%**，比SARIMA（4.82）降低**38%**。
2. **多变量因果链输入是关键**：APEX-Edge (multi) 以仅10.5M参数匹配Toto类精度（MAE 3.87 vs 3.64），而APEX-Edge (1D) 退化到SARIMA水平（4.78），证明跨通道结构在参数大幅减少时起决定性补偿作用。
3. **统一预测与异常检测可行**：MC-dropout预测区间实现异常检测F1=0.93，接近VAR-Mahalanobis（0.94），且捕捉非线性故障模式，省去独立检测流程。
4. **边缘部署实用**：APEX-Edge在ARM Cortex-A76（树莓派5）上单次推理202ms，50次MC-dropout 11.4s（可降至5次实现亚秒），峰值内存428MB，满足AP硬件资源约束，实现零云依赖、数据隐私、亚秒行动延迟。

### 优点

- **领域定制深入**：针对无线网络遥测特性设计输入通道（DHCP因果链），而非通用时间序列表示。
- **统一框架**：单检查点同时提供预测和异常检测，简化运维部署。
- **边缘-云端协同**：提供大模型和小模型，分别满足精度和效率需求，且小模型通过多变量输入弥补参数缩减损失。
- **实验设计清晰**：2×2消融有效分离容量和模态贡献；共识伪标签在缺少人工标注时提供实用基准。
- **实际硬件验证**：边缘延迟在树莓派上实测，接近真实AP SoC，可信度高。

### 不足与局限

- **评估范围窄**：仅对一个指标（DHCP success rate）做预测评估，未验证对RF、接口错误等指标的泛化性。异常检测任务也仅基于DHCP退化。
- **标签质量**：异常检测依赖共识伪标签（≥3方法投票），可能偏向被多数方法感知的异常，而遗漏罕见但真实的故障模式。
- **对比公平性**：通用FM为纯零样本，若允许在APEX数据上继续预训练或微调，性能差距可能缩小。
- **边缘硬件测试非真实AP**：使用树莓派5作为代理，虽声称与Qualcomm Wi-Fi 7 NPro平台的ARM核心类似，但未在实际AP上验证（如内存带宽、热约束等）。
- **未公开数据与代码**：论文依赖思科内部数据，无法复现实验；也未开源模型权重。
- **缺失训练时长与具体算力需求**：无法评估预训练的计算成本。
- **未与其他边缘TSFM方案对比**：如轻量级LSTM、TinyTimeMixer等，边缘部署的优势边界不清晰。

（完）
