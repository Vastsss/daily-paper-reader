---
title: Time-Series Foundation Model Embeddings for Remaining Useful Life Estimation
title_zh: 用于剩余使用寿命估计的时间序列基础模型嵌入
authors: "Amir El-Ghoussani, Michele De Vita, Ronald Naumann, Valiseios Belagiannis"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11990v1"
tags: ["query:ts-fm"]
score: 9.0
evidence: 利用冻结预训练的时间序列基础模型Chronos-2进行剩余寿命估计
tldr: 剩余寿命预测对工业预测性维护至关重要，但现有方法依赖大量特征工程或标注数据。本文提出一种轻量学习方法，利用冻结预训练时间序列基础模型Chronos-2提取特征，并训练小型回归网络进行剩余寿命预测。在两种工业设备的多变量传感器数据上，该方法优于循环、卷积、Transformer和梯度提升基线，且随着上下文窗口增长性能显著提升，展示了基础模型在工业场景下的数据高效替代方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11990v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 929, \"height\": 758, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11990v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 924, \"height\": 420, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11990v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 835, \"height\": 359, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11990v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 587, \"height\": 195, \"label\": \"Table\"}]"
motivation: 减少对特征工程和大规模标注数据的依赖，探索冻结的预训练时间序列基础模型在剩余寿命预测中的适用性。
method: 以冻结的Chronos-2作为骨干提取上下文窗口特征，训练轻量回归神经网络预测剩余寿命。
result: 在两种工业设备数据上，该方法一致优于循环、卷积、Transformer及梯度提升等基线方法。
conclusion: 时间序列基础模型特征为工业剩余寿命估计提供了一种实用、数据高效的替代方案，且长历史窗口能显著提升性能。
---

## 摘要
剩余使用寿命（RUL）预测对于工业预测性维护至关重要，然而许多基于学习的方法依赖于大量的特征工程或大规模标注数据集来训练特定任务的序列模型。在这项工作中，我们引入了一种轻量级学习方法，利用冻结的预训练时间序列基础模型（TSFM），并结合一个小型回归头，从多变量传感器流中进行RUL估计。具体而言，我们使用Chronos-2作为冻结的主干网络来提取上下文窗口特征，并训练一个轻量级回归神经网络进行RUL预测。在来自两种设备类型的真实工业传感器数据上的实验表明，在相同的预处理和评估协议下，Chronos-2特征始终优于递归、卷积、基于Transformer和梯度提升的基线方法。我们进一步分析了上下文长度的影响，发现随着历史数据增加，性能显著提升，这表明TSFM表示为工业环境中的RUL估计提供了一种实用且数据高效的替代方案。

## Abstract
Remaining Useful Life (RUL) prediction is essential for industrial predictive maintenance, yet many learning-based approaches rely on extensive feature engineering or large labeled datasets to train task-specific sequence models. In this work, we introduce a lightweight learning approach, in which we leverage a frozen pretrained time-series foundation model (TSFM) and combine it with a small regression head for RUL estimation from multivariate sensor streams. More specifically, we use Chronos-2 as a frozen backbone to extract context window features and train a lightweight regression neural network for RUL prediction. Experiments on real-world industrial sensor data from two device types show that Chronos-2 features consistently improve over recurrent, convolutional, Transformer-based, and gradient-boosting baselines under the same preprocessing and evaluation protocol. We further analyze the impact of context length and find that performance improves significantly with longer histories, indicating that TSFM representation offer a practical and data-efficient alternative for RUL estimation in industrial settings.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：工业预测性维护中的剩余使用寿命（Remaining Useful Life, RUL）估计对于减少停机时间和维修成本至关重要。现有基于学习的方法通常依赖于手工特征工程或大规模标注数据来训练任务专用的序列模型，这导致训练成本高、泛化能力弱。
- **整体含义**：作者提出利用预训练的时间序列基础模型（Time-Series Foundation Model, TSFM）作为通用特征提取器，避免从头训练复杂网络，从而实现轻量化、数据高效的RUL预测。

---

### 2. 论文提出的方法论

- **核心思想**：使用冻结的预训练TSFM（Chronos-2）作为骨干网络，从多变量传感器历史窗口中提取上下文嵌入，再通过一个轻量级回归头（小型MLP）预测RUL。
- **关键技术细节**：
  - **数据预处理**：对非均匀时间戳进行线性插值，得到固定时间步长；剔除过长间隔的样本；进行异常值裁剪（1%和99%分位数）和Z-score标准化（仅基于训练集）。
  - **标签构建**：根据维修日志计算到下一次故障的时间差（单位：天），并截断到1000天上限。
  - **模型架构**：采用冻结的Chronos-2（解码器仅Transformer）提取隐藏状态序列 \(H \in \mathbb{R}^{L \times h}\)，取对应时间步的嵌入；回归头为两层MLP（隐藏维度m，ReLU激活，Dropout），最后输出非负RUL预测值。
- **公式/算法流程**（文字说明）：
  1. 将多变量传感器序列 \(X_{t-L:t}\) 输入冻结的Chronos-2，获得嵌入 \(H\)。
  2. 取对应时刻t的嵌入 \(h_t\) 输入MLP回归头 \(g_\phi\)，得到预测 \(\hat{y}_t = g_\phi(h_t)\)。
  3. 仅训练回归头参数 \(\phi\)，骨干网络参数不变。

---

### 3. 实验设计

- **数据集**：工业传感器数据来自Nokia Solutions and Networks，包含两种不同设备（Device A和Device B）。Device A有297,345个标注样本，87个传感器通道；Device B有119,364个样本，51个通道。均以1小时为间隔重采样。
- **基准方法**：
  - 非序列方法：线性回归、随机森林、梯度提升（基于窗口统计特征）。
  - 序列方法：GRU、LSTM、时间卷积网络（TCN）、Transformer编码器。
- **评价指标**：平均绝对误差（MAE）和均方误差（MSE）。
- **数据划分**：按时间顺序85:15分割，避免时间泄露。

---

### 4. 资源与算力

- **训练时长**：约2小时（单次训练）。
- **GPU型号**：单张NVIDIA A6000。
- **模型参数量**：Device A约300K，Device B约250K（仅回归头参数）。
- **声明**：文中未明确说明批量大小和具体训练轮次数（最大50轮），但提供了优化器（Adam，学习率1e-3）和批量大小（64）。

---

### 5. 实验数量与充分性

- **实验数量**：包含三项主要实验：
  1. **5步窗口下的性能对比**（表1）：在两种设备上比较所有方法。
  2. **上下文长度影响**（图2）：变化窗口长度L（5~100步），观察MAE变化。
  3. **回归头架构消融**（表2）：比较线性头、2层MLP、4层MLP。
- **充分性与公平性**：
  - 对比方法涵盖非序列和序列模型，且所有方法使用相同的预处理、时间划分和评估协议，保证了公平性。
  - 消融实验验证了主干网络贡献，上下文长度实验系统分析了关键超参数。
  - 但仅使用两个工业设备数据集，缺乏公开数据集（如C-MAPSS）的对比，限制了泛化性验证。

---

### 6. 论文的主要结论与发现

- Chronos-2冻结特征在所有对比方法中取得最低MAE和MSE（例如Device A：MAE=44，优于TCN的88）。
- 增加上下文窗口长度显著提升性能：从L=5到L=80，MAE降低约2倍；超过80步后性能饱和。
- 即使使用线性头，Chronos-2嵌入也远超传统序列模型（MAE 60 vs. 88），表明深度主干是关键。
- 增加回归头容量（2层->4层）带来的额外提升有限，说明大部分映射已由预训练嵌入完成。

---

### 7. 优点

- **轻量高效**：仅需训练小型MLP（参数<500K），显著降低计算和标注成本。
- **数据高效**：利用冻结的通用TSFM，避免任务专用特征工程，在小样本场景（如Device B）仍有效。
- **系统性分析**：深入研究了上下文长度的影响，揭示了长历史对RUL预测的重要性。
- **公平实验**：统一预处理和时间划分，对比基线种类丰富。

---

### 8. 不足与局限

- **数据集限制**：仅使用两个私有工业设备数据集，未在公开基准（如NASA C-MAPSS）上验证，泛化性存疑。
- **多变量通道处理**：Chronos-2原为单变量时间序列设计，文中未讨论如何有效处理多传感器通道的交互（直接展平或独立嵌入）。
- **未评估分布偏移**：未测试传感器缺失、操作工况变化或低标注比例下的鲁棒性。
- **概率输出缺失**：文中提及未来工作可利用Chronos-2的概率特性提供不确定性估计，但当前版本仅输出点估计。
- **超参数敏感性**：仅探索了窗口长度，未系统分析其他超参数（如MLP宽度、dropout率）的影响。

（完）
