---
title: Time-Series Foundation Model Embeddings for Remaining Useful Life Estimation
title_zh: 用于剩余使用寿命估计的时间序列基础模型嵌入
authors: "Amir El-Ghoussani, Michele De Vita, Ronald Naumann, Valiseios Belagiannis"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11990v1"
tags: ["query:ts-fm"]
score: 9.0
evidence: 使用预训练时间序列基础模型Chronos-2的冻结嵌入进行剩余寿命预测
tldr: 针对工业设备剩余使用寿命预测中依赖大量特征工程和标签数据的问题，提出一种轻量学习方法：利用冻结的预训练时间序列基础模型Chronos-2提取多变量传感器数据上下文特征，结合小型回归神经网络实现端到端RUL估计。在两种设备真实数据上实验，该方法优于循环、卷积、Transformer和梯度提升基线，且随着历史上下文长度增加性能显著提升，证明了预训练基础模型在工业RUL预测中具有数据高效和实用性的优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11990v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 929, \"height\": 758}, {\"url\": \"assets/figures/arxiv/2606.11990v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 924, \"height\": 420}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11990v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 835, \"height\": 359}, {\"url\": \"assets/tables/arxiv/2606.11990v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 587, \"height\": 195}]"
motivation: 现有RUL预测方法依赖任务特定特征工程或大规模标签数据，需要更轻量高效的方案。
method: 将Chronos-2作为冻结骨干提取传感器序列特征，训练轻量回归网络预测RUL。
result: 在两种工业设备数据上，该方法一致优于多种基线，且长上下文带来更大性能提升。
conclusion: 预训练时间序列基础模型为工业RUL估计提供了一种数据高效的实用替代方案。
---

## 摘要
剩余使用寿命（RUL）预测对于工业预测性维护至关重要，然而许多基于学习的方法依赖于大量的特征工程或大规模标注数据集来训练特定任务的序列模型。在这项工作中，我们引入了一种轻量级学习方法，利用冻结的预训练时间序列基础模型（TSFM）并结合一个小型回归头，从多变量传感器流中进行RUL估计。具体来说，我们使用Chronos-2作为冻结主干来提取上下文窗口特征，并训练一个轻量级回归神经网络进行RUL预测。在来自两种设备类型的真实工业传感器数据上的实验表明，在相同的预处理和评估协议下，Chronos-2特征始终优于循环、卷积、基于Transformer和梯度提升的基线方法。我们进一步分析了上下文长度的影响，发现使用更长的历史数据可以显著提高性能，这表明TSFM表示为工业环境中的RUL估计提供了一种实用且数据高效的替代方案。

## Abstract
Remaining Useful Life (RUL) prediction is essential for industrial predictive maintenance, yet many learning-based approaches rely on extensive feature engineering or large labeled datasets to train task-specific sequence models. In this work, we introduce a lightweight learning approach, in which we leverage a frozen pretrained time-series foundation model (TSFM) and combine it with a small regression head for RUL estimation from multivariate sensor streams. More specifically, we use Chronos-2 as a frozen backbone to extract context window features and train a lightweight regression neural network for RUL prediction. Experiments on real-world industrial sensor data from two device types show that Chronos-2 features consistently improve over recurrent, convolutional, Transformer-based, and gradient-boosting baselines under the same preprocessing and evaluation protocol. We further analyze the impact of context length and find that performance improves significantly with longer histories, indicating that TSFM representation offer a practical and data-efficient alternative for RUL estimation in industrial settings.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：工业预测性维护中，剩余使用寿命（RUL）估计是关键任务，但传统数据驱动方法通常依赖大量手工特征工程或大规模标注数据来训练任务特定的序列模型（如LSTM、CNN、Transformer等），这导致训练资源消耗大、泛化能力有限。
- **动机**：时间序列基础模型（TSFM，如Chronos-2）在预测任务中表现出色，但很少被用于RUL估计。作者希望探索能否利用冻结的预训练TSFM作为特征提取器，通过简单的轻量级回归头来实现高效、数据友好的RUL预测，避免对大量标注数据和任务特定训练的依赖。

## 2. 方法论

- **核心思想**：将预训练的TSFM（Chronos-2）作为冻结的骨干网络，从多变量传感器历史窗口中提取上下文特征，然后训练一个轻量级MLP回归头进行RUL预测。
- **关键技术细节**：
  - **数据预处理**：对不规则采样的传感器数据线性插值到统一时间网格（步长Δt=1小时）；剔除长时间间隔缺失；全局异常值裁剪（1%和99%分位数）；每个通道归一化（零均值单位方差，仅用训练集统计量）。
  - **RUL标签构建**：根据维修日志中的故障时间戳，计算从当前时刻到下一个故障的时间差（以天为单位），并截断到最大1000天。
  - **模型架构**：
    - 冻结的Chronos-2模型：处理长度为L的上下文窗口 \(X_{t-L:t}\)，输出嵌入序列 \(H \in \mathbb{R}^{L \times h}\)（h为隐藏维度）。
    - 回归头：2层MLP（隐藏层宽度m，ReLU激活，Dropout率p），输入为上下文窗口的嵌入向量 \(h_{t-L:t}\)，输出标量RUL预测 \(\hat{y}_t\)，并施加ReLU保证非负。
  - **训练**：仅训练MLP头，使用MSE损失，Adam优化器，学习率1e-3，最大50 epoch，batch size 64。

## 3. 实验设计

- **数据集**：Nokia提供的工业传感器数据集，包含两种设备（Device A 和 Device B）。
  - Device A：约297,345个标注样本，87个传感器通道。
  - Device B：约119,364个标注样本，51个传感器通道。
  - 预处理后统一采样间隔1小时，默认上下文长度L=5。
- **基准方法**：
  - 非序列方法：线性回归、随机森林。
  - 序列方法：GRU、LSTM、梯度提升（基于窗口统计特征）、TCN（时序卷积网络）、Transformer编码器。
  - 所有基线使用相同预处理、相同时间序列分割（85:15训练/测试，避免数据泄漏）。
- **评估指标**：MAE、MSE。

## 4. 资源与算力

- **GPU**：单张NVIDIA A6000（未提及数量，但推测为1张）。
- **训练时间**：约2小时（50 epoch）。
- **模型参数量**：Device A约300K参数，Device B约250K参数（主要来自MLP头，Chronos-2冻结不更新）。

## 5. 实验数量与充分性

- **主要实验**：在两个数据集上对比8种基线（表I），以及在不同上下文长度下的性能对比（图2，L从5到100+），以及回归头消融实验（表II：线性头、2层MLP、4层MLP）。
- **充分性评价**：
  - 基线覆盖全面（含非序列、循环、卷积、Transformer、梯度提升），对比公平。
  - 上下文长度消融实验展示了方法的可扩展性。
  - 回归头消融验证了冻结骨干的有效性（线性头已优于TCN）。
  - **不足**：未与更多先进RUL方法（如基于物理的混合模型、扩散模型）对比；未进行跨域泛化测试；未讨论不确定性定量评估。

## 6. 主要结论与发现

- 冻结的Chronos-2特征在RUL估计上显著优于所有基线（MAE降低约2-4倍）。
- 上下文长度对性能影响显著：从L=5增加到L=80，MAE下降约50%，之后趋于饱和；而其他基线对上下文长度不敏感。
- 即使使用最简单的线性回归头，Chronos-2特征也优于训练好的TCN（MAE 60 vs 88），表明预训练表征质量高。
- 该方法数据高效（仅需训练小规模MLP头），适合工业场景。

## 7. 优点

- **轻量高效**：冻结大模型，仅训练小型MLP头，训练时间短（2小时），参数少（~300K）。
- **数据友好**：利用预训练知识，减少对大规模标注数据的需求。
- **泛化潜力**：在两个不同设备的数据集上一致优于多种基线，且能有效利用长历史上下文。
- **实验设计严谨**：时间序列分割避免数据泄漏，预处理公平，超参数在验证集调整。

## 8. 不足与局限

- **模型依赖**：仅测试了Chronos-2一个TSFM，未与其他TSFM（如TimesFM、PatchTST）对比。
- **场景有限**：只涉及两种工业设备（传感器数51/87），未验证在更广泛设备或公开基准（如CMAPSS）上的表现。
- **鲁棒性未评估**：未测试缺失传感器、操作条件变化、数据分布偏移等现实问题（论文仅作为未来工作提及）。
- **不确定性缺失**：当前为点估计，未提供预测置信区间，对维护决策支持不够。
- **非序列基线简单**：对比的线性回归/随机森林直接使用单点传感器值，未有更精细的手工特征工程。

（完）
