---
title: Causal Semantic Alignment for LLM-based Time Series Forecasting
title_zh: 基于因果关系语义对齐的LLM时间序列预测
authors: "Kexuan Zhang, Xiaobei Zou, Cesare Alippi, Gary G. Yen, Yang Tang"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08262v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 基于LLM的时间序列预测与因果对齐
tldr: LLM时序方法常忽略动态与不变成分的混杂，动态成分作为混淆因子导致虚假相关。本文提出CVAformer，先解耦变量为不变和动态成分，再用因果干预消除动态影响，并以非因果注意力捕捉变量交互。实验覆盖长短期、少样本和零样本预测，性能匹配或超越SOTA，部分数据集精度显著提升。该工作为LLM时序任务提供了新视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08262v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1495, \"height\": 398}, {\"url\": \"assets/figures/arxiv/2606.08262v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1411, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2606.08262v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1500, \"height\": 839}, {\"url\": \"assets/figures/arxiv/2606.08262v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 746, \"height\": 232}, {\"url\": \"assets/figures/arxiv/2606.08262v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1391, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2606.08262v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1310, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2606.08262v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1701, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2606.08262v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1507, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2606.08262v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 691, \"height\": 413}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08262v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1559, \"height\": 1025}, {\"url\": \"assets/tables/arxiv/2606.08262v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1354, \"height\": 508}, {\"url\": \"assets/tables/arxiv/2606.08262v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1554, \"height\": 641}, {\"url\": \"assets/tables/arxiv/2606.08262v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1455, \"height\": 640}]"
motivation: 现有LLM时序方法忽视动态与不变成分的混杂，动态成分作为混淆因子导致虚假相关。
method: 提出CVAformer，先解耦每个变量为不变和动态成分，再用因果干预消除动态混淆，并用非因果注意力实现变量级对齐。
result: 在长短期、少样本和零样本预测中匹配或超越最先进方法，部分数据集精度显著提升。
conclusion: 变量级对齐和动态解耦为LLM时序任务提供了新视角并验证其有效性。
---

## 摘要
近年来，大语言模型（LLMs）的进展通过使时间模式与预训练的词嵌入对齐，为时间序列预测开辟了新的可能性。然而，大多数基于LLM的方法忽视了时间序列的异质性，其中动态波动与不变语义相互纠缠。这种纠缠在对齐过程中引入了虚假相关性，因为动态成分通过同时影响不变成分和最终的对齐嵌入而充当混杂因素。为了解决这个问题，提出了一种变体级别对齐框架CVAformer。CVAformer在对齐前明确地将每个变体分解为不变成分和动态成分，并应用因果干预来减轻动态成分的混杂效应。为了更好地支持变体级别对齐，CVAformer用非因果注意力机制取代了LLM中的标准因果注意力机制，该机制在每个时间步捕获变体之间的相互作用。在长期、短期、少样本和零样本预测设置上的大量实验表明，CVAformer在大多数数据集上匹配或超过了最先进的性能，并且在某些情况下取得了明显更高的准确性。实验结果验证了CVAformer中变体级别对齐和动态解耦的有效性，为基于LLM的时间序列任务提供了新视角。

## Abstract
Recent advances in Large Language Models (LLMs) have opened new possibilities for time series forecasting by enabling alignment between temporal patterns and pretrained word embeddings. However, most LLM-based methods overlook the heterogeneous nature of time series, where dynamic fluctuations and invariant semantics are entangled. This entanglement introduces spurious correlations during the alignment, as dynamic components act as confounders by simultaneously influencing invariant components and the resulting aligned embeddings. To address this issue, a variable-level alignment framework CVAformer is proposed. CVAformer explicitly disentangles each variable into invariant and dynamic components just before alignment, and applies causal intervention to mitigate the confounding effect of the dynamics. To better support variable-level alignment, CVAformer replaces the standard causal attention in LLMs with a non-causal attention mechanism that captures interactions among variables at each time step. Extensive experiments across long-term, short-term, few-shot, and zero-shot forecasting settings indicate that CVAformer matches or exceeds state-of-the-art performance on most datasets, and in some cases achieves notably better accuracy. Experimental results validate the effectiveness of variable-level alignment and dynamic disentanglement in CVAformer, offering a new perspective for LLM-based time series tasks.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有基于大语言模型（LLM）的时间序列预测方法通常将时间序列整体映射到预训练的词嵌入空间，但忽视了时间序列的异质性——即每个变量同时包含稳定的**不变成分**（代表变量固有语义）和**动态成分**（代表外部波动）。这两者纠缠在一起，在跨模态对齐时，动态成分作为**混杂因子**同时影响不变成分和最终对齐嵌入，引入虚假相关性，导致语义锚点漂移和对齐不稳定。此外，现有方法多采用时间级对齐（按时间步或补丁对齐）或因果注意力，不适合变量间无序的交互建模。
- **整体意义**：提出了一种基于因果干预的变量级对齐框架 CVAformer，旨在消除动态成分的混杂效应，实现更稳定、可解释的跨模态对齐，从而提升 LLM 在多种时间序列预测场景下的泛化能力与准确性。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- **变量级对齐**：将每个变量（而非时间点或补丁）作为对齐单元，与预训练词嵌入的语义原型进行匹配，保留变量语义一致性。
- **因果解耦与后门调整**：将每个变量的时间嵌入分解为不变成分（E_invariant）和动态成分（E_dynamic），将动态成分视为混杂因子，通过后门调整公式 P(A|do(I)) = Σ_c P(A|I, C=c)P(C=c) 近似进行因果干预，消除动态对对齐的干扰。
- **非因果注意力**：在时序分支中改用非因果注意力（无自回归掩码），捕获变量间无序依赖关系，更贴合多变量交互本质。

### 关键技术细节
1. **输入嵌入与分解**：对多元时间序列 X 进行嵌入（含辅助先验如均值、标准差、趋势斜率），经 MHSA 得到 E_time；然后通过多个 CausalCov 块提取不变成分 E_invariant，再用 MLP 从残差中学习动态成分 E_dynamic。
2. **对比学习解耦损失**：采用 MoCo 风格的对比损失 L_invariant，促进不变成分对时间增强的稳定性，抑制与动态成分的纠缠。
3. **因果对齐模块**：
   - 计算时间协方差矩阵并汇总统计量，通过 CausalEncoder 生成紧凑的混杂因子摘要 E_causal。
   - 在交叉注意力中，查询 Q = (E_invariant + E_causal)W_q，键/值来自 PCA 降维后的词嵌入原型 E_word_hat。
   - 通过软门控 G 融合对齐嵌入 E_align 与动态嵌入 E_dynamic，得到最终文本分支表示 E_text。
4. **时序分支**：堆叠非因果注意力块（NCBlocks）处理变量间交互，保持置换等变性。
5. **优化目标**：总损失 L_total = L_sup + λ1 L_consistency + λ2 L_invariant，其中 L_sup 为预测损失，L_consistency 为文本分支与时间分支预测的一致性（L1 损失），L_invariant 为对比解耦损失。仅更新 LoRA 适配器、位置嵌入和归一化层，冻结 LLM 主干。

## 3. 实验设计：数据集、场景、基准、对比方法

- **数据集**：
  - 长期预测：Weather、Traffic、Electricity、ETTh1、ETTh2、ETTm1、ETTm2（共7个数据集，输入长度96，预测长度{96,192,336,720}）。
  - 短期预测：M4 数据集（年度、季度、月度、其他，共15个评估子项）。
  - 少样本预测：ETT 数据集，仅使用10%训练数据。
  - 零样本预测：跨域迁移（如 ETTh1→ETTm1, ETTh2→ETTm2 等4个迁移方向）。
- **基准方法**：
  - LLM 基方法：TimeLLM、GPT4TS、CALF。
  - Transformer 基：iTransformer、PatchTST、FEDformer、Crossformer。
  - MLP 基：DLinear。
  - CNN 基：TimesNet。
  - 短期额外：N-HiTS、N-BEATS。
- **评估指标**：MSE、MAE（长期）；SMAPE、MASE、OWA（短期）。
- **实验公平性**：所有方法遵循 TimesNet 协议，固定输入长度96；多次重复报告均值和标准差。

## 4. 资源与算力

- **文中明确说明**：所有实验在单个 NVIDIA A40 GPU 上运行，使用 PyTorch 实现。
- **未明确说明**：未提供总训练时长（仅给出 ETTh1 预测长度96下每 epoch 训练时间 13.74s，总 20 epochs 约 274.8s），未提及每个实验的重现次数细节（除部分指标给出标准差外），未提供完整训练的 GPU 小时数。

## 5. 实验数量与充分性

- **实验数量**：覆盖4大任务（长期、短期、少样本、零样本），共7个长期数据集×4种预测长度=28组；M4 短期4个子类别×3指标×2度量=24组；少样本4个数据集×4预测长度=16组；零样本4个迁移方向×4预测长度=16组；加上消融实验（3种变体+置换敏感性）、LLM 骨干泛化实验（GPT-2, BERT, DeepSeek-R1）和注意力可视化等，总共超过80组实验。
- **充分性与客观性**：
  - **充分**：涵盖了主要预测任务和稀缺数据场景，消融实验验证了核心模块（因果解耦、非因果注意力、对比损失）的贡献。
  - **公平**：采用统一基准协议，对比方法来自公开论文的实现，使用 TimesNet 基准代码。但零样本设置中仅使用 ETT 内部互相迁移，未扩展到其他域间（如天气→交通），外部泛化验证有限。
  - **风险**：长期预测 Traffic 数据集上 iTransformer 略优，作者承认该数据集周期性强，CVAformer 的因果建模优势不显著。少样本/零样本部分结果虽优，但未与更多 LLM 方法（如 Lag-Llama）对比。

## 6. 论文的主要结论与发现

1. **CVAformer 在长期预测中平均优于所有基线**：MSE 相对 PatchTST 降低 10.07%，相对 GPT4TS 降低 6.82%。
2. **短期预测中，在 11/15 个评估项上排名第一**，整体 OWA 比 PatchTST 低 2.8%。
3. **少样本/零样本场景下优势更明显**：少样本平均误差比 PatchTST 降低 13%；零样本比 GPT4TS 降低 11.2%。
4. **消融实验证实**：移除因果解耦组件导致 MSE 增加 3.2%（ETTh1），验证了后门调整的有效性；非因果注意力比因果注意力更适合变量级建模；对比损失促进解耦。
5. **非因果注意力可视化和 t-SNE 分析**表明，CVAformer 的注意力图更关注变量间交互，且时间嵌入与词嵌入分布更接近，对齐效果更好。
6. **模型兼容性**：CVAformer 可适配不同 LLM 骨干（GPT-2、BERT、DeepSeek-R1），保持竞争力。

## 7. 优点

- **方法创新**：
  - 首次将因果后门调整引入 LLM 时间序列对齐，系统解决了动态成分的混杂问题。
  - 变量级对齐替代时间级对齐，保留变量语义完整性，避免时间补丁带来的语义碎片化。
  - 非因果注意力设计自然契合变量间无序交互，提升建模灵活性和可解释性。
- **实验全面**：覆盖广泛预测任务和稀缺数据场景，消融实验严谨，对各个模块贡献有定量分析。
- **性能强**：在多数数据集上达到 SOTA，尤其少样本/零样本提升显著，表明模型泛化能力好。
- **实用性**：内存占用（709 MB）和训练时间（13.74 s/epoch）在 LLM 方法中具有竞争力，LoRA 微调高效。

## 8. 不足与局限

- **实验覆盖不全**：
  - 零样本仅限 ETT 内部迁移，未涉及跨域（如金融→天气）或大型真实世界场景，缺乏对更广泛领域泛化的验证。
  - 未与最新 LLM 时序方法（如 Lag-Llama, Chronos, MOIRAI）对比，可能延迟 SOTA 比较。
- **方法局限**：
  - 对比损失依赖数据增强，增强策略的选择可能影响解耦质量，文中未深入讨论。
  - 后门调整假设动态成分可观测且混杂结构已知，现实时间序列的因果图可能更复杂（如存在未观测混杂）。
  - 非因果注意力虽适合变量交互，但若变量间存在时间滞后依赖（如滞后因果结构），可能丢失顺序信息。
- **偏差风险**：在 Traffic 等强周期性数据集上优势不显著，说明方法对高度规则序列的适应性不如某些专用模型。
- **应用限制**：需要预训练 LLM 骨干，计算资源仍高于轻量模型（如 iTransformer 内存仅 42.64 MB），不适合资源受限的边端设备。

（完）
