---
title: "TimeRouter: Efficient and Adaptive Routing of Time-Series Foundation Models"
title_zh: TimeRouter：高效自适应的时间序列基础模型路由
authors: "Kanghui Ning, Yushan Jiang, Kashif Rasul, Anderson Schneider, Yuriy Nevmyvaka, Dongjin Song"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11625v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 时序基础模型的高效路由选择，使用轻量判别门控
tldr: 时间序列基础模型（TSFMs）存在异构归纳偏差，单一模型难以在所有场景占优，现有LLM控制器路由开销大。TimeRouter通过轻量级判别路由、选择性门控和集成回退实现高效专家选择，无需LLM调用。在GIFT-EVAL排行榜上达到LB MASE 0.6765，取得最优性能。消融实验揭示了池组成和选择性门控对路由设计的重要性，为未来基于基础模型池的智能时间序列系统提供了模块化轻量路由方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11625v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 722, \"height\": 1068}, {\"url\": \"assets/figures/arxiv/2606.11625v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 856, \"height\": 363}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11625v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 785, \"height\": 748}, {\"url\": \"assets/tables/arxiv/2606.11625v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 819, \"height\": 373}, {\"url\": \"assets/tables/arxiv/2606.11625v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 578, \"height\": 281}, {\"url\": \"assets/tables/arxiv/2606.11625v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 728, \"height\": 604}, {\"url\": \"assets/tables/arxiv/2606.11625v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1292, \"height\": 544}]"
motivation: 现有TSFMs路由依赖LLM控制器带来高推理开销，且缺乏对模型互补性的有效利用，亟需高效自适应路由方法。
method: TimeRouter结合学习型路由头、选择性门控和集成回退，通过轻量判别机制在预训练TSFM池中自适应选择专家。
result: 在GIFT-EVAL榜单上取得LB MASE 0.6765，超越现有方法，且无额外LLM推理成本。
conclusion: TimeRouter验证了轻量路由在基础模型池中的有效性，为构建模块化智能时间序列系统提供了实用框架。
---

## 摘要
时间序列基础模型（TSFMs）在新兴的智能时间序列系统中被越来越多地用作预测专家。然而，TSFMs表现出异质的归纳偏置，且没有单一模型能在所有预测场景中持续占据主导地位，这使得专家选择成为一个关键挑战。现有系统通常将此决策委托给基于LLM的控制器，导致大量推理开销。我们提出TimeRouter，一个高效的路由框架，通过轻量级判别路由、选择性门控和集成回退，利用预训练TSFM池中的经验互补性。具体而言，TimeRouter结合了学习路由头、选择性门和集成回退，能够在推理时无需调用LLM实现自适应专家选择。TimeRouter在GIFT-EVAL排行榜上达到了最先进性能，LB MASE为0.6765。除了基准性能外，我们的消融研究为TSFM路由设计提供了实证见解，强调了池组成和选择性门控的重要性。总之，这些结果使TimeRouter成为未来基于基础模型池的智能时间序列系统的模块化轻量级路由层。我们的代码可在 https://github.com/UConn-DSIS/TimeRouter 获取。

## Abstract
Time-series foundation models (TSFMs) are increasingly explored as predictive experts within emerging agentic time-series systems. However, TSFMs exhibit heterogeneous inductive biases, and no single model consistently dominates across forecasting regimes, making expert selection a critical challenge. Existing systems often delegate this decision to LLM-based controllers, incurring substantial inference overhead. We present TimeRouter, an efficient routing framework that leverages empirical complementarity across a pool of pretrained TSFMs through lightweight discriminative routing, selective gating, and ensemble fallback. Concretely, TimeRouter combines a learned routing head, a selective gate, and an ensemble fallback, enabling adaptive expert selection without invoking an LLM at inference time. TimeRouter achieves state-of-the-art performance on the GIFT-EVAL leaderboard, with an LB MASE of 0.6765. Beyond benchmark performance, our ablation studies provide empirical insights into TSFM routing design, highlighting the importance of pool composition and selective gating. Taken together, these results position TimeRouter as a modular and lightweight routing layer for future agentic time-series systems built upon foundation-model pools. Our code is available at https://github.com/UConn-DSIS/TimeRouter.

---

## 论文详细总结（自动生成）

### 论文详细总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **背景**：时间序列基础模型（TSFMs）快速发展，如 Chronos、TimesFM 等，但它们在架构和训练数据上存在异构归纳偏置，没有任何单一模型能对所有预测场景（频率、周期、域、噪声结构）都最优。因此，关键挑战不是寻找通用最优模型，而是为每个输入自适应地选择最合适的 TSFM，这自然形成了一个路由问题。
- **现有局限**：当前智能时间序列系统（如 TimeCopilot、TSOrchestra）通常依赖 LLM 控制器进行模型选择，推理开销巨大（数百毫秒级），不适合实时应用；而非 LLM 方法（如 Synapse、ZooCast）或需动态重权重全池或依赖嵌入匹配，缺乏轻量判别路由方案。
- **核心动机**：提出一种高效、无需 LLM 调用的自适应路由框架，充分利用池中 TSFM 的互补性，实现即插即用、低成本推理。

#### 2. 方法论：核心思想、关键技术细节
- **整体思想**：TimeRouter 将路由视为一个学习问题：训练一个轻量判别器（路由头）为每个输入打分，结合选择性门控（信任判别结果或退化为集成），实现自适应专家选择。
- **技术细节**：
  - **路由头**：使用一对多（OvA）分类器（默认 XGBoost），特征图 $\phi(x) \in \mathbb{R}^d$ 包含上下文统计量、每个 TSFM 的交叉验证 CV 分数、下采样后的各模型预测值（堆叠泛化风格）。每个 TSFM 对应一个二元分类器 $g_{\theta_k}$，预测该模型是否为当前输入的真实最优专家（oracle-best）。
  - **选择性门控**：推理时，将 K 个分类器得分 L1 归一化为 $p(x)$。计算决策空间中的**边际** $m(x)=p_{(1)}-p_{(2)}$ 和预测空间中的**多样性** $d(x)$（各模型预测值在时间步上的标准差均值）。若 $m(x)<\tau_m$ 或 $d(x)<\tau_d$，则退化为集成回退（Ensemble fallback）；否则选择得分最高专家。
  - **集成回退**：使用 CV-逆加权平均，权重 $w_k \propto 1 / (\text{CV score}_k + \epsilon)$，其中 CV score 为每个 TSFM 在单窗口上下文尾部的 MASE。
  - **训练**：所有 TSFM 固定（冻结），仅训练路由头。特征和 oracle 标签预先计算，训练对象是二元交叉熵。使用 5 种子平均得分，门控阈值通过 5 折 GroupKFold 网格搜索确定。
- **公式与算法流程**（文字说明）：
  1. 输入单变量上下文 x。
  2. 提取特征 $\phi(x)$（305维，4个TSFM池情况）。
  3. 每个 TSFM 的 OvA 分类器输出得分，L1 归一化得到 $p(x)$。
  4. 计算边际 $m(x)$ 和多样性 $d(x)$。
  5. 若 $m(x) < \tau_m$ 或 $d(x) < \tau_d$，则用 CV-逆加权集成输出最终预测；否则选择得分最高 TSFM 的预测。

#### 3. 实验设计
- **数据集与基准**：GIFT-EVAL 基准，包含 97 个任务（单变量时间序列预测），使用公开排行榜上的 LB MASE（几何平均 MASE 与季节朴素 MASE 的比值，越低越好）。
- **基础模型池**：四个冻结的 TSFM：Chronos-2、FlowState、PatchTST-FM、Sundial（均来自不同前沿研究，确保互补性）。
- **对比方法**：
  - 单 TSFM：Chronos-2、TimesFM-2.5、PatchTST-FM、TiRex、FlowState、Moirai2、Sundial、TTM-R2。
  - 智能系统：TSOrchestra（LLM-based）、MoiraiAgent（微调3B LLM）、Credence（排行榜条目）、ZooCast（嵌入选择）、Synapse（自适应仲裁）、TimeCopilot（LLM Agent）。
- **结果**：TimeRouter 取得 LB MASE = 0.6765，超越所有方法，包括最强单模型 Chronos-2（0.6978）和最强智能系统 TSOrchestra（0.6768），且不产生 LLM 推理开销。

#### 4. 资源与算力
- **训练**：约 110 秒（针对 4-FM 池 ~155K 训练行），使用 CPU 训练 5 个 XGBoost 二元分类器。
- **推理**：每序列 9.9 ms（CPU，XGBoost 3.1.3，Intel Xeon Gold 5318Y，16 线程），不依赖 GPU。
- **模型规模**：路由器大小仅 1.72 MB。
- **未提及具体 GPU 型号和数量**：因为路由器完全在 CPU 上运行，且 TSFM 推理时间未计入路由开销中（文内比较均排除 TSFM 正向传播时间）。TSFM 自身可能需 GPU，但论文未说明训练 TSFM 的资源。

#### 5. 实验数量与充分性
- **实验数量**：主要结果在 GIFT-EVAL 排行榜（97 任务）上给出。消融实验包括：
  - 选择性门控消融（门控 vs 无门控，按长短周期分层）。
  - 路由头消融（XGBoost、LightGBM、随机森林、MLP、逻辑回归）。
  - 池组成消融：所有 4-C3 子集和 6-C2 子集。
  - 效率对比（训练时间、推理延迟 vs 其他系统）。
- **充分性与公平性**：
  - 所有对比方法均引用官方数值（排行榜或原论文），TimeRouter 结果公开可复现（已开源）。
  - 消融实验设计系统，覆盖关键组件：门控、分类器家族、池多样性和规模。
  - 门控消融按任务周期（短/中/长）分层，揭示差异。
  - 池组成消融显示 Chronos-2 的重要性以及池大小对性能的贡献。
  - **不足**：仅在一个基准（GIFT-EVAL）上评测，缺乏跨域跨基准泛化性验证；只评测单变量；未考虑多种损失函数（MASE 为主）；未测试更大大小的池（如 K>4）。但作者在论文中讨论了未来扩展。

#### 6. 主要结论与发现
- TimeRouter 实现了 GIFT-EVAL 排行榜当前最优性能（LB MASE 0.6765），验证了**轻量判别路由**的有效性。
- **无需 LLM 即可达到或超越 LLM 驱动的智能系统**，且推理延迟低至 9.9 ms，大幅降低开销。
- 消融实验结论：
  - **门控**在长周期任务中增益显著（+90 bp），在短周期中略微倒退（-14 bp），说明多样性更适合不确定场景。
  - **路由头**：非线性模型（XGBoost 等）远优于线性逻辑回归，但各非线性模型间差异很小（±11 bp），说明性能取决于非线性特征交互建模。
  - **池组成**：Chronos-2 是关键锚点，池大小越大性能越好（K=4 > K=3 > K=2），每步约 40 bp 提升。

#### 7. 优点
- **高效**：训练仅110秒、推理毫秒级，不引入LLM开销，适合实时场景。
- **模块化**：路由层与基础模型解耦，可轻松添加/替换TSFM，无需重训原模型。
- **可解释性**：边际和多样性提供直观置信度信号，门控逻辑清晰。
- **开源复现**：代码已在 GitHub 公开，实验结果可验证。
- **综合分析**：不仅给出 SOTA，还通过详实消融提供了路由设计的经验指导（如池组合、门控触发机制）。

#### 8. 不足与局限
- **单变量限制**：当前只处理单变量时间序列，未扩展至多变量场景（但多变量可通过逐变量路由处理，论文未讨论）。
- **基准单一**：仅在 GIFT-EVAL 上评估，未在多个异构基准（如 Monash、LibCity）上验证泛化性。
- **池规模有限**：仅测试 4 个 TSFM，更大池（如 10+）的性能和计算开销尚未探讨。
- **特征工程依赖**：特征映射（305维）包含较多手工统计特征，可能引入设计偏差；不同数据集可能需要调整。
- **门控阈值选择**：通过 5 折 OOF 网格搜索确定，可能对训练分布敏感，实际部署中分布偏移可能降低性能。
- **未优化集成回退**：CV-逆加权简单有效，但可能存在更优组合策略（如动态加权），文中未深入对比。
- **对比方法可复现性**：部分基线（如 TSOrchestra）无公开代码，延迟估计依赖报告值，不够精确。

（完）
