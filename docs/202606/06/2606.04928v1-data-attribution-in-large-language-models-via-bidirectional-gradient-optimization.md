---
title: Data Attribution in Large Language Models via Bidirectional Gradient Optimization
title_zh: 基于双向梯度优化的大语言模型数据归因
authors: "Frédéric Berdoz, Luca A. Lanzendörfer, Kaan Bayraktar, Roger Wattenhofer"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04928v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 使用双向梯度优化的大语言模型数据归因方法
tldr: 大型语言模型(LLM)的广泛应用提出了数据溯源和问责问题。本文针对自回归LLM的训练数据归因(TDA)问题，提出通过双向梯度优化（梯度上升和下降）扰动生成样本来测量训练样本损失变化的方法。该方法支持任意粒度归因，在事实和风格归因上均优于基线，显著提升了模型可解释性，为负责任AI提供了重要工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04928v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04928v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 831, \"height\": 661, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04928v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1847, \"height\": 330, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04928v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04928v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1842, \"height\": 321, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04928v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1841, \"height\": 1154, \"label\": \"Table\"}]"
motivation: 理解哪些训练数据影响LLM输出是开放问题，现有归因方法在准确性和粒度上不足。
method: 对生成文本施加双向梯度优化（上升和下降）扰动模型，计算训练样本损失变化实现归因。
result: 在预训练模型上，该方法在影响指标上超越基线，支持事实与风格层面的细粒度归因。
conclusion: 该方法有效增强LLM可解释性，支撑模型治理与数据溯源需求。
---

## 摘要
大语言模型（LLMs）越来越多地部署在各种应用中，提出了关于治理、问责和数据来源的关键问题。理解哪些训练数据对模型输出影响最大仍然是一个基本未解决的问题。我们通过训练数据归因（TDA）来解决自回归LLMs的挑战，扩展了逆公式：如果模型在训练期间看到了生成的输出，训练数据将如何受到影响？我们的方法通过对生成文本样本进行双向梯度优化（梯度上升和下降）来扰动基础模型，并测量训练样本中损失的相应变化。我们的框架支持任意数据粒度的归因，能够实现事实和风格归因。我们在带有已知数据集的预训练模型上与基线方法进行评估，并显示在影响力指标上优于以前的工作，从而增强了模型的可解释性，这是负责任AI系统的关键要求。

## Abstract
Large Language Models (LLMs) are increasingly deployed across diverse applications, raising critical questions for governance, accountability, and data provenance. Understanding which training data most influenced a model's output remains a fundamental open problem. We address this challenge through training data attribution (TDA) for auto-regressive LLMs by expanding upon the inverse formulation: How would training data be affected if the model had seen the generated output during training? Our method perturbs the base model using bidirectional gradient optimization (gradient ascent and descent) on a generated text sample and measures the resulting change in loss across training samples. Our framework supports attribution at arbitrary data granularity, enabling both factual and stylistic attribution. We evaluate our method against baselines on pretrained models with known datasets, and show that it outperforms previous work on influence metrics, thereby enhancing model interpretability, an essential requirement for accountable AI systems.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）的输出受哪些训练数据影响？这属于训练数据归因（TDA）问题，是实现AI可解释性、问责制和数据溯源的关键。
- **研究动机**：现有TDA方法在自回归LLM上存在不足——要么依赖昂贵的Hessian矩阵逆运算（如影响函数），要么局限于事实追踪或单粒度归因。作者希望提出一种**简单、可扩展、支持任意粒度**的归因方法，适用于开放文本生成场景。
- **整体含义**：通过逆向视角——模拟模型“看到”生成输出后训练数据损失的变化——来估计影响力，并引入双向梯度优化（上升+下降）提升归因质量，从而增强模型透明度。

### 2. 论文提出的方法论

- **核心思想**：基于**镜像影响力假设**（Mirrored Influence Hypothesis），即训练样本对模型输出的影响，近似等于模型输出对训练样本的反向影响。作者通过**对生成文本同时进行梯度上升和梯度下降**，得到两个扰动模型，再计算每个训练样本在这两个模型下损失的绝对值变化（Bidirectional Influence Score, BIS），从而排序影响力。
- **关键技术细节**：
  - 使用**Fisher信息矩阵（FIM）的对角近似**正则化梯度更新，防止灾难性遗忘（类似Elastic Weight Consolidation, EWC）。
  - 更新公式：  
    \(\theta^{(m+1)}_{\pm} \leftarrow \theta^{(m)}_{\pm} \pm \alpha N \hat{F}_{\theta^*}^{-1} \nabla \ell(x, \theta)\)  
    其中\(\hat{F}_{\theta^*}\)为对角FIM近似，\(\alpha\)为学习率，\(N\)为训练样本数。
  - 归因分数：\(I(x_i; \hat{x}) = |\ell(x_i, \theta_{\hat{x}}^-) - \ell(x_i, \theta_{\hat{x}}^+)|\)，按绝对值降序排列得到最 influential 的训练样本。
- **算法流程**（文字说明）：
  1. 加载预训练模型\(\pi_{\theta^*}\)。
  2. 对给定生成文本\(\hat{x}\)，执行\(M\)步梯度上升得到\(\theta_{\hat{x}}^+\)，执行\(M\)步梯度下降得到\(\theta_{\hat{x}}^-\)。
  3. 遍历训练数据集\(D\)，计算每个样本\(x_i\)在\(\theta_{\hat{x}}^-\)和\(\theta_{\hat{x}}^+\)下的损失绝对值差。
  4. 按BIS排序，输出top-k训练样本。

### 3. 实验设计

- **数据集/场景**：
  - **事实归因**：Wikipedia摘要（WIT数据集，230M tokens），训练GPT-2风格模型，上下文窗口256，滑动步长128。
  - **风格归因**：Project Gutenberg文学文本（2M tokens），训练另一个GPT-2风格模型。
  - 生成方式：对实体或短句进行单次提示（如“Ancient Rome.”），使用Top-50采样、温度1、重复惩罚1.5（事实场景）或无惩罚（风格场景）。
- **基准（baseline）**：
  - **Random**（随机选取训练样本）
  - **BM25**（基于词频的检索方法）
  - **TRACK STAR**（Chang et al., 2025，基于随机投影梯度的可扩展影响函数）
  - **Gecko**（Lee et al., 2024，基于文本嵌入的检索方法）
- **评价指标**：
  - **Tail-Patch Absolute (TPA) score**：对top-k样本做一次梯度更新后，计算生成样本似然变化的绝对值比率。假设 influential 样本会导致更大变化。
  - **Retraining from scratch**：直接移除top-k样本后重新训练模型，比较测试样本loss的增加量（作为ground-truth验证）。

### 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量、训练时长**。仅提及“从头训练”GPT-2风格模型（较小规模，上下文256），以及归因步骤使用\(M=10\)次梯度更新、学习率\(\alpha=1\times10^{-4}\)。作者在局限性中指出DABGO需要两次完整遍历训练语料，计算成本高，不适合工业级LLM（如Llama 3.1），但未给出具体硬件配置。

### 5. 实验数量与充分性

- **实验数量**：
  - 两组主要实验：事实归因（25个查询）、风格归因（多个作者各选一段文本）。
  - 消融实验：对比仅用梯度下降、仅用梯度上升、双向联合（DABGO）的TPA分数（Table 2）。
  - 额外验证：在4个测试样本上做完整的从头重训练实验（移除top-20/50/100样本），对比loss变化（Figure 2）。
- **充分性与客观性**：
  - **充分**：涵盖事实和风格两种场景，对比了多种baseline，包含定量（TPA）和定性（示例表）分析，还做了重训练验证。
  - **公平**：所有方法使用相同查询和训练数据，随机基线作为噪声下界。但baseline中BM25在事实场景k=1时最优，作者解释为词汇重叠优势；风格场景下Gecko在低k表现好，DABGO在更高k下显著领先。结果整体客观。

### 6. 论文的主要结论与发现

- **主要结论**：DABGO在事实和风格归因中均**显著优于**现有baseline（BM25、TRACK STAR、Gecko及Random），尤其在更大k下表现突出。
- **发现**：
  - 双向梯度（上升+下降）优于单向（仅上升或下降），因为LLM输出与训练分布的关系不是单向的，上升和下降能互补捕获不同信号。
  - 风格归因中，影响力往往分散在多个样本中（而非单一“关键段落”），DABGO能有效识别这些分布性影响。
  - 定性示例显示，DABGO能定位到训练样本中与生成文本最相关的短语（如“centered on the city of Rome”）或风格特征（如第一人称战场叙事）。

### 7. 优点

- **方法创新**：将双向梯度优化引入归因，简单有效，无需存储所有训练样本梯度，只需两次后处理遍历。
- **支持细粒度**：可扩展至token级归因，提供更好的可解释性（通过高亮关键tokens）。
- **适应性强**：适用于开放文本生成，不限于事实追踪或单字预测。
- **验证严谨**：除代理指标（TPA）外，还通过完整重训练实验验证了归因的因果有效性，增强了可信度。

### 8. 不足与局限

- **计算成本**：需要两次完整遍历训练语料（前向+反向），对大规模LLM（如数百B tokens）仍不实用。
- **忽略样本交互**：仅考虑单个样本独立影响力，无法捕捉样本间的协同或对抗效应。
- **token级归因未验证因果性**：尽管给出了高亮示例，但未做因果检验，需未来工作确认。
- **实验规模有限**：仅使用小型GPT-2模型（自训练，约124M参数？未明确规模），未在更大模型（如Llama、GPT-3）上验证。
- **可能存在偏差**：Fisher对角近似和固定步长可能影响准确性；基线的选择不包含最新方法（如TRAK、DataInf等，尽管文中提及了这些方法但未作为对比）。
- **开放生成场景的评估挑战**：TPA指标虽有效，但并非完美代理，重训练验证仅覆盖4个查询，样本量小。

（完）
