---
title: Bandwidth-Aware and Cost-Efficient Pipeline Parallel Scheduling in Geo-Distributed LLM Training
title_zh: 地理分布式LLM训练中带宽感知且成本高效的流水线并行调度
authors: "Han Zhang, Jianchun Liu, Hongli Xu"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25375v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 地理分布式LLM训练调度
tldr: "跨区域大语言模型训练面临GPU稀缺、异构带宽和电价差异，现有调度器难以平衡延迟与成本，且多租户场景下存在头阻塞问题。本文提出BACE-Pipe框架，通过动态作业优先级机制、带宽感知路径查找和成本最小化分配策略，在满足通信约束的同时优先选择低价区域资源。实验结果表明，相比于先进基线，平均作业完成时间降低27.9%-64.7%，总电费降低12.6%-30.6%。该工作有效缓解了头阻塞，提升了资源利用率，实现了延迟与成本的双重优化。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25375v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 693, \"height\": 490, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25375v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 718, \"height\": 827, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25375v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 921, \"height\": 513, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25375v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 929, \"height\": 365, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25375v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 927, \"height\": 371, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25375v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 926, \"height\": 364, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25375v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 922, \"height\": 507, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25375v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 759, \"height\": 157, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25375v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 921, \"height\": 979, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25375v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 871, \"height\": 524, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25375v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1527, \"height\": 332, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25375v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1564, \"height\": 418, \"label\": \"Table\"}]"
motivation: 现有调度器在跨区域LLM训练中难以兼顾异构带宽和电价，导致高延迟或高成本，且多租户环境下头阻塞严重。
method: 提出BACE-Pipe，包含动态作业优先级、带宽感知路径查找和成本最小化GPU分配，综合优化执行顺序、通信可行性和电价。
result: "平均JCT降低27.9%-64.7%，总电费降低12.6%-30.6%，优于现有基线。"
conclusion: BACE-Pipe有效平衡延迟与成本，缓解头阻塞，适合异构跨区域LLM训练场景。
---

## 摘要
大型语言模型（LLM）的快速发展使得跨地域训练成为必要，因为单个云区域的GPU资源稀缺。在跨区域场景下，流水线并行（PP）通信效率高，但在异构区域间带宽和区域电价下调度PP仍然具有挑战性。现有调度器要么是延迟优先，导致高电力成本，要么是成本优先，依赖僵化的资源分配从而延长作业完成时间（JCT）。它们在多租户环境中优化执行顺序时也效果不佳，因为长时间运行且带宽密集的作业可能导致队首（HoL）阻塞并降低整体性能。为此，我们提出了BACE-Pipe，一种针对跨地理分布式集群的LLM训练的带宽感知且成本高效的流水线调度框架。BACE-Pipe首先引入一种动态作业优先级机制，通过联合考虑作业特征（如计算时间）和实时网络利用率来优化执行顺序。然后，它采用带宽感知的路径查找器来识别满足通信约束的可行跨区域流水线路径，从而防止通信阻塞流水线。在所有可行路径中，成本最小化分配器通过优先将资源分配给电价较低的区域来确定最优GPU放置策略。因此，BACE-Pipe减轻了HoL阻塞，提高了资源利用率，并同时减少了JCT和总电力成本。大量仿真表明，与最先进的基线相比，BACE-Pipe将平均JCT降低了27.9%-64.7%，总电力成本降低了12.6%-30.6%。

## Abstract
The rapid evolution of large language models (LLMs) has made geographically distributed training necessary due to GPU scarcity within a single cloud region. In such cross-region settings, Pipeline Parallelism (PP) is communication-efficient, yet scheduling PP remains challenging under heterogeneous inter-region bandwidth and regional electricity prices. Existing schedulers are either delay-first, incurring high electricity cost, or cost-first, relying on rigid resource allocation that prolongs Job Completion Time (JCT). They are also ineffective at optimizing execution order in multi-tenant environments, where long-running and bandwidth-intensive jobs can cause head-of-line (HoL) blocking and degrade overall performance. To this end, we propose BACE-Pipe, a bandwidth-aware and cost-efficient pipeline scheduling framework for LLM training across geo-distributed clusters. BACE-Pipe first introduces a dynamic job prioritization mechanism that optimizes execution order by jointly considering job characteristics (e.g., computation time) and real-time network utilization. It then employs a bandwidth-aware pathfinder to identify feasible cross-region pipeline paths that satisfy communication constraints, thereby preventing communication from stalling the pipeline. Among all feasible paths, a cost-minimizing allocator determines the optimal GPU placement strategy by preferentially assigning resources to regions with lower electricity prices. Consequently, BACE-Pipe mitigates HoL blocking, improves resource utilization, and simultaneously reduces both JCT and total electricity cost. Extensive simulations show that BACE-Pipe reduces average JCT by 27.9%--64.7% and total electricity cost by 12.6%--30.6% compared with state-of-the-art baselines.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在跨地理区域的大语言模型（LLM）训练场景中，单个云区域 GPU 资源稀缺，迫使训练任务必须跨区域调度。但现有调度器难以同时处理**异构区域间带宽**和**区域电价差异**这两大挑战：
  - **延迟优先调度器**（如 Crux、CASSINI）追求最短作业完成时间（JCT），但忽视电价差异，导致高昂电费。
  - **成本优先调度器**（如 TanGo）只关注降低成本，但资源分配僵化，延长 JCT；且两者在多租户环境下都未优化作业执行顺序，导致队首阻塞（HoL blocking）问题。
- **整体含义**：需要一种既能感知带宽瓶颈、又能利用电价差异、同时动态调整作业执行顺序的调度框架，以同时降低平均 JCT 和总电费。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 核心思想
提出 **BACE-Pipe**，一个控制面与数据面解耦的调度框架。控制面包括状态收集器、作业解析器和作业调度器，后者通过**三步决策**完成调度：
1. **动态作业优先级**：根据计算强度和带宽敏感度自适应排序。
2. **带宽感知路径查找（Pathfinder）**：为选定作业构造满足通信约束的跨区域流水线路径。
3. **成本最小化分配器（Cost-Min Allocator）**：在可行路径中优先将 GPU 分配到低电价区域。

### 2.2 关键技术细节

**① 动态作业优先级机制**
- 定义计算强度 \(I_j\) = 单 GPU 执行时间归一化值，带宽敏感度 \(D_j\) = 最小带宽需求归一化值。
- 引入动态权重 \(\alpha\) = 当前活跃作业占用总带宽 / 总带宽容量，反映网络拥塞程度。
- 优先级公式：\(\text{Priority}_j = (1-\alpha)(1-I_j) + \alpha(1-D_j)\)。当网络空闲（\(\alpha\) 小）时偏向短作业（低 \(I_j\)）；当网络拥塞（\(\alpha\) 大）时偏向带宽高效作业（低 \(D_j\)）。

**② 带宽感知路径查找器（Pathfinder）**
- 首先判断是否有单个区域 GPU 容量 ≥ 最优 GPU 数 \(K^*\)（由最小化每迭代时间求得），若有则选电价最低区域。
- 若无，则采用类似 Prim 算法的贪心扩展：从初始区域出发，每次加入与当前尾节点有最高带宽链路的区域，并检查瓶颈带宽是否满足通信–计算平衡条件（\(A/b_{\min} \le t_{\text{comp}}(g')\)）。直到累计 GPU 数达到 \(K^*\) 或无法扩展。对所有可能的起始区域枚举，选择 GPU 数最多（其次平均电价最低）的路径。

**③ 成本最小化分配器（Cost-Min Allocator）**
- 对给定路径和 GPU 总数，先保证每个区域至少分配 1 个 GPU 以保持流水线连通性，然后将剩余 GPU 按电价升序依次分配给最便宜的区域（不超过其容量）。

## 3. 实验设计

### 3.1 数据集与场景
- **模拟环境**：6 个全球区域（爱尔兰、美国纽约、德国法兰克福、日本东京、新加坡、澳大利亚悉尼），GPU 容量从 16 到 128 不等，电价取自真实国际商业电价，带宽基于 AWS EC2 G4 实例范围（25-100 Gbps）随机采样，区域间链路带宽取两端平均值。
- **工作负载**：8 个 LLM 训练作业，参数规模从 14B 到 101B（FLM-101B、Solar-Open-100B、Llama-3.1-70B、Falcon-40B、Qwen2.5-32B、Gemma-3-27B、Ministral-3-14B、Qwen2.5-14B）。
- **数据集分配**：每个作业随机分配 Alpaca-52k（~50MB）、WikiText-103（~0.5GB）或 OpenWebText（~38GB）之一，代表小、中、大三种数据规模。

### 3.2 对比基线（Benchmark）
1. **LCF**（成本优先，单区域最低电价）
2. **LDF**（延迟优先，单区域最大 GPU 容量）
3. **CR-LCF**（跨区域 LCF：按电价升序聚合 GPU）
4. **CR-LDF**（跨区域 LDF：从最大 GPU 区域贪心添加高带宽区域）

所有方法都限制每作业 GPU 数不超过最优数 \(K^*\)，确保公平性。

### 3.3 评估指标
- 平均 JCT（作业完成时间）
- 总电力成本

## 4. 资源与算力

- 论文中**未明确说明实际训练所用 GPU 型号与数量**，模拟中使用的是 Nvidia A6000 GPU，每个区域 GPU 容量为 {16, 32, 64, 128}，总集群规模未明确列出。
- 训练时长方面，文中仅给出单次迭代时间公式，未报告实际训练耗时（因为基于仿真）。
- 注意：这是调度算法仿真，并非真实训练实验。

## 5. 实验数量与充分性

- **主要对比实验**：1 组端到端性能比较（图 4），展示 BACE-Pipe 与 4 个基线的归一化平均 JCT 和总成本。
- **敏感性分析**：3 组实验
  - 带宽缩放（0.3×、0.9×、1.5×）（图 5）
  - GPU 容量缩放（0.5×、0.75×、1.25×）（图 6）
  - 工作负载强度（8、12、16、20、24 个作业）（图 7）
- **消融实验**：1 组（图 8），移除 Priority、Pathfinder、Cost-Min 三个组件后性能变化。
- **充分性判断**：
  - 实验覆盖了主要影响因素（带宽、GPU 容量、作业数量），验证了算法在不同场景下的鲁棒性。
  - 消融实验清楚展示了各组件贡献。
  - 对比基线全面，覆盖了工业标准和跨区域扩展方法。
  - **不足**：所有实验均为仿真，缺乏实际集群部署验证；未考虑动态到达作业的时间分布（如泊松到达）；消融实验只做了一种场景（默认配置），未在不同缩放因子下重复消融。

## 6. 论文的主要结论与发现

1. **BACE-Pipe 显著优于基线**：平均 JCT 降低 27.9%-64.7%，总电费降低 12.6%-30.6%。
2. **“跨区域悖论”**：简单增加 GPU 数（如 CR-LDF/CR-LCF）在高带宽环境下反而恶化性能（JCT 比单区域 LDF 还高 28.8%），因为队首阻塞导致大型作业长期占用资源。
3. **动态优先级是解决队首阻塞的关键**，尤其在带宽充裕或 GPU 稀缺时效果更明显。
4. **Pathfinder 是性能贡献最大的组件**（移除后 JCT 增加 52.5%，成本增加 20.5%）。
5. **Cost-Min Allocator 不仅降低成本，还通过将 GPU 集中在低价区域减少资源碎片，轻微改善 JCT**。

## 7. 优点

- **方法创新**：首次将动态优先级、带宽感知路径查找和成本最小化分配三者有机结合，同时优化 JCT 和成本。
- **理论严谨**：将调度问题形式化为多目标组合优化，并给出 NP-hard 证明，启发式设计有明确依据（SJF 思想、Prim 算法启发）。
- **实验设计全面**：敏感性分析覆盖带宽、GPU 容量、负载强度三个维度；消融实验量化各组件贡献。
- **结果解释深入**：详细分析了“跨区域悖论”的成因，并验证了动态优先级的有效性。
- **实际价值**：直接针对云计算中 GPU 稀缺与电价差异的真实痛点，可部署于现有云平台。

## 8. 不足与局限

1. **实验局限于仿真**：未在真实跨区域集群（如 AWS、GCP）上验证，实际网络延迟、链路波动、竞争行为等可能影响结果。
2. **作业到达模型简化**：假设所有作业同时等待调度，实际云环境中作业动态到达，论文未考虑在线调度（如事件驱动）。
3. **消融实验仅做一组默认配置**：未在不同带宽/GPU 缩放下重复消融，无法确认各组件贡献是否随环境变化。
4. **仅考虑流水线并行**：未与数据并行、模型并行等其他并行策略的联合调度进行比较，实际训练可能混合多种并行。
5. **假设每作业 GPU 数固定为 \(K^*\)**：未考虑作业自适应调整并行度（如弹性训练），限制了灵活性。
6. **电价模型简单**：仅使用固定电价，未考虑时间变化的动态电价（如现货市场）。
7. **网络模型理想化**：假设带宽已知且恒定，实际 WAN 带宽会随时间波动。

（完）
