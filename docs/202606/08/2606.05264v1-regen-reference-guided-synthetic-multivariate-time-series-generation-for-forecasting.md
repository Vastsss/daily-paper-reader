---
title: "REGEN: Reference-Guided Synthetic Multivariate Time Series Generation for Forecasting"
title_zh: REGEN：参考引导的多元时间序列生成用于预测
authors: "Moulik Gupta, Dhruv Kumar, Murari Mandal, Saurabh Deshpande"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.05264v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 参考引导的合成多变量时间序列生成方法用于预测
tldr: 多变量时间序列预测面临数据稀缺问题，现有生成方法要么产生领域无关样本，要么缺乏对周期、局部变异和跨变量依赖结构的显式控制。本文提出ReGeN，一种参考引导生成框架，将观测序列作为结构支架，分解为周期骨干、深度核高斯过程残差和结构因果模型中的跨变量依赖，通过可控温度采样扩展分布覆盖。实验表明，ReGeN生成数据可有效替代真实数据，在强周期领域甚至超越原始数据，基于ReGeN预训练的基础模型优于现有合成方法。该方法表明在低数据场景下，如何结构化利用参考数据与数据量同等重要。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05264v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1423, \"height\": 523, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05264v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1429, \"height\": 1000, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05264v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1336, \"height\": 1644, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05264v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1339, \"height\": 1965, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05264v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1245, \"height\": 1044, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05264v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1170, \"height\": 613, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05264v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 885, \"height\": 236, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05264v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 956, \"height\": 581, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05264v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1377, \"height\": 368, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05264v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1387, \"height\": 495, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05264v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1318, \"height\": 710, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05264v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1316, \"height\": 559, \"label\": \"Table\"}]"
motivation: 现实世界多变量时间序列样本稀少，现有生成方法无法有效利用参考数据中的周期、局部变异和跨变量结构，导致生成数据缺乏领域特定可控性。
method: ReGeN将参考序列分解为相位对齐的周期骨干、深度核高斯过程残差和带滞后跨变量依赖的结构因果模型，通过温度采样可控生成多样化样本。
result: ReGeN生成数据在替代真实数据时预测退化最小，在交通等强周期域优于真实数据，基于其预训练的模型超越其他合成方法。
conclusion: 低数据场景下，结构化利用参考数据的方式与数据量同等关键，ReGeN为可控合成提供了有效范式。
---

## 摘要
训练鲁棒的多元时间序列预测模型需要大量多样化的语料库，然而许多实际领域仅提供少量观测序列。现有生成方法无法解决这一不匹配问题：基于先验的方法（如CauKer、TimePFN）产生领域无关的样本，而数据驱动方法（如TimeGAN）将参考视为黑盒监督，放弃了对其周期结构、局部变异和跨变量动态的显式控制。我们提出ReGeN，一种参考引导的生成流水线，它将观测序列视为可控制合成的结构支架，而非需要模仿的样例。ReGeN将每个参考分解为三个可解释组件：捕捉主要领域形态的相位对齐周期骨架；通过深核高斯过程建模的每变量随机残差；以及通过具有拟合耦合系数的结构因果模型注入的滞后感知跨变量依赖关系。以可控温度对这些组件进行采样，扩大了分布覆盖范围，同时保留了基于领域的结构。我们证明，ReGeN生成的数据能够持续替代真实兄弟数据，且预测性能下降最小，并且在交通等强周期领域中，甚至可以超越真实源数据本身。我们进一步表明，在ReGeN语料库上预训练的基础模型优于在基于先验和数据驱动的合成替代品上预训练的模型。这表明在低数据场景下，参考数据的结构利用方式与数据量本身同样重要。

## Abstract
Training robust multivariate time series forecasting models requires large, diverse corpora, yet many real-world domains provide only a handful of observed sequences. Existing generators fail to resolve this mismatch: prior-based approaches (e.g., CauKer, TimePFN) produce domain-agnostic samples, while data-driven methods (e.g., TimeGAN) treat references as black-box supervision, forfeiting explicit control over periodic structure, local variability, and cross-variable dynamics.   We propose ReGeN, a reference-guided generative pipeline that treats observed sequences not as examples to imitate, but as structural scaffolds for controllable synthesis. ReGeN decomposes each reference into three interpretable components: a phase-aligned periodic backbone capturing dominant domain morphology; per-variable stochastic residuals modeled with a deep-kernel Gaussian process; and lag-aware cross-variable dependencies injected through a structural causal model with fitted coupling coefficients. Sampling these components at controllable temperature broadens distributional coverage while preserving domain-grounded structure.   We show that ReGeN-generated data consistently substitutes for real sibling data with minimal forecasting degradation, and in strongly periodic domains such as traffic, can outperform the real source itself. We further show that a foundation model pretrained on ReGeN corpora outperforms those pretrained on prior-based and data-driven synthetic alternatives. This suggests that in low-data regimes, how reference data is structurally exploited can matter as much as how much data is available.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **核心问题**：多变量时间序列预测模型在数据稀缺场景下性能严重受限。现实世界中的运营级多变量语料（如建筑能耗、交通网络、云基础设施）通常仅有几十到几百条观测序列，远不足以训练泛化能力强的预测模型。
- **现有方法的不足**：
  - **基于先验的生成器**（如 ForecastPFN、TimePFN、CauKer）使用领域无关的数学基元（正弦模板、GP 核库、随机 DAG）构造数据，不包含目标域的任何特定形态、不确定性纹理或跨变量耦合。
  - **数据驱动的生成器**（如 TimeGAN、C-RNN-GAN）将整个参考语料作为黑盒训练信号，需要大量真实序列才能稳定训练，且无法显式控制周期结构、局部变异和跨变量依赖。
- **关键洞察**：即使仅有少量真实序列，它们也编码了丰富的领域特定结构（如建筑需求周期、温度与冷却负荷的耦合）。这些结构不应被绕过，而应被主动利用。**如何结构化地利用参考数据，其重要性不亚于数据量本身**。

### 论文提出的方法论

#### 核心思想：参考引导的分解式生成
将每条真实观测序列分解为三个可解释组件，分别建模后以可控温度采样并组合，生成新的合成序列。所有组件均**直接拟合于真实数据**，而非来自通用先验或黑盒目标。

#### 关键技术细节

- **结构分解**：对每个变量 $c$ 的标准化序列 $y_{c,t}$，分解为周期模板 $\tau_{c,t}$ 和残差 $r_{c,t}$。
  - 相位对齐模板：按特征周期 $P$ 将观测对齐，相同相位点取平均得到 $\tau^{(s)}_c(p)$。
  - 残差：$r_{c,t} = y_{c,t} - \tau_c(t \mod P)$。
  - 通过方差解释率（VE）筛选高质量分解，保留 VE ≥ η 的样本用于模板库和残差建模。

- **周期骨架**：从模板库中选取模板 $\tau_{c,m}$，通过幅度因子 $a_c$ 缩放，生成合成周期骨架 $\hat\tau_{c,t}$。

- **残差动力学建模（深度核自回归）**：
  - 构建上下文窗口 $u_{c,t} = [\bar r_{c,t-W}, \dots, \bar r_{c,t-1}]$。
  - 通过 CNN+LSTM 编码器提取隐特征 $z_{c,t} = g_\theta(u_{c,t})$。
  - 定义高斯过程 $f_c \sim GP(0, k_c(z, z'))$，预测均值 $\mu_{c,t}$ 和方差 $\sigma_{c,t}^2$。
  - 采样残差 $\hat r_{c,t} = \mu_{c,t} + \lambda_c \sigma_{c,t} \epsilon_t$，其中 $\lambda_c$ 为温度控制多样化程度。

- **跨变量依赖（有向因果图）**：
  - 通过四种因果发现方法（PCMCI、DYNOTEARS、VARLiNGAM、随机森林 Granger 检验）的共识估计有向无环图（DAG）。
  - 为每个子变量 $c$ 定义父变量贡献 $g_{c,t} = \sum_{p \in Pa(c)} \sum_{\ell \in L_{p\to c}} w_{p,c,\ell} s_{p,t-\ell}$。
  - 最终子变量轨迹为**凸组合**：$\hat x_{c,t} = \alpha_c \hat x_{c,t}^{int} + (1-\alpha_c) h(g_{c,t})$，其中 $\alpha_c$ 采样自 [0.7, 0.9] 保留内生主导。

#### 算法流程（文字描述）

1. **输入**：真实多变量序列集合 $\mathcal{D} = \{X^{(s)}\}_{s=1}^S$。
2. **分解**：对每个变量进行相位对齐分解，计算 VE 并筛选高质量样本。
3. **模板库构建**：保留通过 VE 阈值的模板及其幅度统计。
4. **残差模型训练**：对筛选后的平均信号拟合深度核 GP 模型。
5. **因果图估计**：通过共识流程得到 DAG 及耦合系数。
6. **合成**：
   - 随机选择模板，采样幅度因子，生成周期骨架。
   - 自回归采样残差（带温度参数）。
   - 添加低幅漂移动态。
   - 根据 DAG 对子变量进行凸组合。
7. **输出**：合成多变量数据集，大小与真实语料匹配。

### 实验设计

- **数据集与场景**：12 个真实数据集，涵盖五个领域：
  - **能源**：BDG-2 Bear、Panther、Bull、Hog（建筑能耗，小时级）
  - **云基础设施**：Azure VM 2017、Borg 2011（5 分钟级）
  - **交通**：PEMS-04、PEMS-08（5 分钟级）
  - **气候**：Subseasonal、Subseasonal Precipitation（日级）
  - **住宅能源**：Residential PV Power、Residential Load Power（分钟级）
- **评估协议**：
  - **TRTR**（Train on Real, Test on Real）：真实→真实，作为黄金上界。
  - **TSTR**（Train on Synthetic, Test on Real）：合成→真实，衡量合成数据独立替代能力。
  - **TRSTR**（Train on Real + Synthetic, Test on Real）：数据增强效果。
- **对比方法**：
  - **TimeGAN**（Jinsung Yoon et al., 2019）：参考引导的对抗生成器。
  - **CauKer**（Xie et al., 2025）：基于先验的因果生成器，不依赖真实数据。
- **预测架构**（5种，涵盖不同归纳偏好）：
  - iTransformer（约1M参数，自注意力）
  - DLinear（4,673参数，显式季节-残差分解）
  - S-Mamba（约1.1M参数，状态空间模型）
  - Moirai-small（约13.8M参数，掩码编码器基础模型）
- **评估指标**：MSE、MAE、MASE、WQL。
- **实验设置**：统一输入上下文 96 步，预测 24 步；合成语料大小与真实语料匹配。

### 资源与算力

- **硬件**：NVIDIA A10G GPU（22.1 GB VRAM），AMD EPYC 7R32 CPU（4核8线程），32 GB 系统 RAM，242 GB SSD。
- **软件**：Ubuntu 24.04.4 LTS。
- **未明确说明**：具体训练时长、生成耗时未报告。

### 实验数量与充分性

- **实验组数**：超过 72 个 TSTR 迁移设置（12 数据集 × 2 方向 × 3 架构 = 72），加上 TRSTR 对比（12 数据集 × 3 架构 = 36），加上全语料基础模型对比（3 种生成方法 × 4 指标），加上消融实验（残差消融、SCM 混合消融），加上频谱分析、t-SNE 可视化、方差解释分析等。总计约 100+ 组实验。
- **充分性评价**：
  - 覆盖了多个领域、多种架构、多种协议，实验设计系统且全面。
  - 对比了现有主流合成方法（TimeGAN 和 CauKer），并保证了语料大小匹配。
  - 消融实验证实了各组件贡献，且 Spearman 相关分析提供了定量解释。
  - **可能存在的公平性问题**：TimeGAN 和 CauKer 的实验配置可能与最佳实践有差异，但文中未详细说明调参过程；Moirai-small 的预训练从零开始，超参数是否完全对齐未提及。

### 论文的主要结论与发现

1. **合成数据的替代能力**：在 67% 的 TSTR 设置中（48/72），使用 ReGeN 合成数据替代真实兄弟数据，MSE 变化在 ±3% 以内；在交通等强周期领域，合成数据甚至超过真实数据。
2. **数据增强收益**：将真实数据与合成数据联合训练（TRSTR），对 iTransformer 和 S-Mamba 有持续提升，对 DLinear 提升有限（因其自身已具备显式分解）。
3. **优于现有生成方法**：
   - 相比 TimeGAN：Moirai-small 预训练 MSE 降低 41.1%。
   - 相比 CauKer：MSE 降低 2.3%，证明了参考引导的附加价值。
4. **组件贡献**：残差建模和跨变量耦合均对下游性能有可测量的正向贡献（去掉残差平均 MSE 上升 11.0%，去掉 SCM 混合平均 MSE 上升 6.0%）。
5. **频谱与几何保真**：合成数据保留了真实数据的频谱主峰和低频谱形状；t-SNE 投影显示几何分布大致对齐。

### 优点

- **方法层面**：
  - 模块化、可解释：周期、残差、跨变量依赖三个组件可独立建模和调整。
  - 可控性强：通过温度 $\lambda$ 平衡多样性与保真度；通过凸系数 $\alpha$ 控制内生/外生比重。
  - 小样本友好：不需要大量数据训练生成器，仅需少量参考序列即可提取结构。
  - 与下游模型无关：三种不同归纳偏好的架构上均有效。
- **实验层面**：
  - 覆盖多个真实世界的低数据场景；评估协议全面（TRTR/TSTR/TRSTR）。
  - 消融实验深入，并引入统计相关分析（Spearman）支撑结论。
  - 提供了频谱和几何可视化，定性验证了生成质量。

### 不足与局限

- **SCM 耦合在低数据高维度场景下可能有害**：如在 Hog 数据集（24 条序列，15 个变量）中，共识图估计噪声较大，去掉 SCM 混合反而提升性能。需要更严格的阈值或自适应方法。
- **全语料预训练仅报告了 Moirai-small**：由于计算限制，未在更多基础模型（如 TimesNet、PatchTST）上验证，外推性有限。
- **消融实验仅使用 iTransformer**：对 DLinear 和 S-Mamba 的组件效应未知，可能存在架构依赖。
- **温度超参数 $\lambda$ 和漂移动态 $d_{c,t}$ 的调优不够系统**：文中设为固定值（附录未详细说明），可能不是最优配置。
- **计算成本未量化**：生成一条合成序列的耗时、Deep GP 训练时间、共识 DAG 计算时间均未报告，不利于实际部署评估。
- **未与最新的数据增强方法对比**（如 TSMix、Mixup 等）、未与最近的大规模预训练模型（如 Chronos、TimesFM）进行公平比较（仅与 CauKer 和 TimeGAN 对比，且 CauKer 本身不是为特定域设计的）。

（完）
