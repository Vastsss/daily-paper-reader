---
title: Partial Wavelet Canonical Coherence for Nonstationary Signals with High Dimensional Confounders
title_zh: 高维混杂因素下非平稳信号的偏小波典型相干
authors: "Haibo Wu, Marina I. Knight, Hernando Ombao"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24554v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 非平稳时间序列分析
tldr: 针对多变量非平稳时间序列易受高维混淆因素干扰的问题，提出部分小波典型相干性方法。首次在频域建立时间序列部分典型相关分析，通过局部小波谱矩阵实现尺度特异性时变估计，并融合主成分降维确保高维稳定调整。模拟证实可消除混淆导致的虚假关联并准确恢复直接关联。在ETF数据上揭示调整外部市场影响后仍存在显著时变与尺度依赖的直接典型关联。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24554v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1229, \"height\": 603, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24554v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1591, \"height\": 594, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24554v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 933, \"height\": 610, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24554v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1630, \"height\": 515, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24554v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1618, \"height\": 1105, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24554v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1583, \"height\": 547, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24554v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 934, \"height\": 660, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24554v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1601, \"height\": 548, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24554v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 910, \"height\": 447, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24554v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1559, \"height\": 816, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24554v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1561, \"height\": 506, \"label\": \"Table\"}]"
motivation: 现有典型相关分析无法处理非平稳性和高维混淆因素，亟需频域方法。
method: 基于多元局部平稳小波框架，利用局部小波谱矩阵估计，并引入主成分降维实现高维调整。
result: 模拟有效消除虚假关联并恢复真实关联；ETF数据揭示时变尺度依赖的直接关联。
conclusion: 提出首个时间序列部分典型相关分析的频域方法，可处理非平稳和高维混淆。
---

## 摘要
我们提出了偏小波典型相干方法，用于在调整可能的高维混杂因素后，测量两个多元非平稳时间序列之间的直接典型关联。据我们所知，这是首个建立时间序列偏典型相关分析的频域公式的方法。通过小波方法，所提出的方法能够提供尺度特异、时变关联度量，适应潜在的数据非平稳性。我们在多元局部平稳小波框架下公式化了目标量，通过局部小波谱矩阵开发了原则性估计，并引入了主成分降维以在高维情况下实现稳定调整。模拟结果表明，该方法能够去除由混杂因素引起的虚假边缘关联，并准确恢复直接关联，包括在高维设置中。对美国交易所交易基金的分析显示，在调整外部市场效应后，存在显著的时变和尺度依赖的直接典型关联。

## Abstract
We develop Partial Wavelet Canonical Coherence for measuring the direct canonical association between two multivariate nonstationary time series after adjustment for possibly high-dimensional confounders. To the best of our knowledge, this is the first method that establishes a frequency-domain formulation of the partial canonical correlation analysis for time series. Through a wavelet approach, the proposed method yields a scale-specific, time-varying measure of association capable to work with potential data nonstationarities. We formulate the target quantity under the multivariate locally stationary wavelet framework, develop principled estimation through local wavelet spectral matrices, and incorporate principal-component reduction for stable adjustment in high-dimensions. Simulations show that the method removes spurious marginal association induced by confounding and accurately recovers direct association, including in higher-dimensional settings. Analysis of U.S. exchange-traded funds reveals substantial time-varying and scale-dependent direct canonical association after adjustment for external market effects.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对这篇论文进行结构化、深入、客观的总结。

---

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在金融、神经科学等领域，常需分析两个**多变量非平稳时间序列**组（如两组ETF基金）之间的直接关联，但这种关联可能被**高维的、共享的外部因素**（如全球市场波动）所混淆。现有方法存在明显局限：
    - **典型相关分析（CCA）**：只能在时域处理，且无法识别依赖关系的频域成分。
    - **相干性分析（如傅里叶分析）**：通常是成对变量的分析，不适用于组对组分析，且假设信号是平稳的。
    - **现有的偏典型相关分析**：在时间序列领域，其定义和估计大多限于平稳假设的**时域**，缺乏能同时处理**非平稳性**、**频域特性**和**高维混淆因素**的统一框架。
- **整体含义**：该论文旨在填补这一方法论空白，提出一个全新的、能同时解决上述所有挑战的统计模型。这为理解非平稳多变量系统中直接的、动态的、尺度特定的组间依赖关系提供了强大工具。

### 2. 论文提出的方法论

- **核心思想**：将**经典偏典型相关分析（Partial CCA）** 的思想扩展到**小波域**，从而在**时频域**内去除混淆因素的影响，并自然地处理信号的非平稳性。
- **关键技术细节**：
    - **理论基础**：建立在**多元局部平稳小波（MvLSW）** 框架之上。该框架能够用时变的局部小波谱（LWS）矩阵来描述非平稳多变量时间序列的时频特性。
    - **部分化操作（Partialization）**：在给定尺度 `j` 和重标时间 `u` 上，定义**局部小波谱矩阵** `Sj(u)`。通过Schur补（Schur complement）形式，从 `{Xt}` 和 `{Yt}` 的LWS矩阵中移除由混淆过程 `{Zt}` 解释的线性部分：
        - 偏自谱： `S_{j;XX·Z}(u) = S_{j;XX}(u) - S_{j;XZ}(u) * S^{-1}_{j;ZZ}(u) * S_{j;ZX}(u)`
        - 交叉谱同理计算 `S_{j;YY·Z}(u)` 和 `S_{j;XY·Z}(u)`。
    - **定义部分小波典型相干（Partial WaveCanCoh）**：在去除 `{Zt}` 影响后的偏谱矩阵基础上，求解一个约束优化问题，找到能使两组残差信号相关性最大化的线性组合（典型向量 `a_j, b_j`）。这个最大相关值即为 `Partial WaveCanCoh`。
    - **等价特征值求解**：该优化问题等价于求矩阵 `N_{j;a|Z} = S^{-1}_{j;XX·Z} * S_{j;XY·Z} * S^{-1}_{j;YY·Z} * S_{j;YX·Z}` 的最大特征值。
    - **估计算法**：
        1.  **高维降维（可选）**：若混淆过程 `{Zt}` 维度过高，先用PCA提取其前 `r` 个主成分作为其低维近似 `{ft}`。
        2.  **融合与谱估计**：将 `Xt`、`Yt` 和 `Zt`（或其主成分 `bt`）拼接成联合过程 `Wt`，用标准的MvLSW谱估计方法（校正平滑周期图）估计其联合LWS矩阵 `Sj(u)`。
        3.  **偏谱计算**：根据前述Schur补公式计算所有需要的偏LWS矩阵。
        4.  **特征分解**：对 `N` 矩阵进行特征分解，最大特征值平方根即为 `Partial WaveCanCoh` 的估计值。
- **公式与算法流程**（文字说明）：
    1.  **输入**：三组时间序列 `X_t`, `Y_t`, `Z_t`。
    2.  **步骤**：
        - a) 对 `Z_t` 应用主成分分析（PCA）降维。
        - b) 构建联合序列 `W_t = (X_t, Y_t, Z_t_components)`。
        - c) 校正平滑周期图估计 `W_t` 的局部小波谱（LWS）矩阵。
        - d) 从LWS矩阵中提取出各个子块的谱矩阵。
        - e) 利用Schur补公式计算去除 `Z_t` 影响后的偏谱矩阵。
        - f) 用偏谱矩阵构建矩阵 `N`。
        - g) 求矩阵 `N` 的最大特征值，即为 `Partial WaveCanCoh` 估计值。

### 3. 实验设计

- **数据集/场景**：
    - **模拟数据**：基于MvLSW模型生成，包含三个组件 `{Xt}`, `{Yt}`, `{Zt}`。
        - **场景1（无直接关联）**：`{Xt}` 与 `{Yt}` 之间无直接依赖，但都受到 `{Zt}` 的影响，用于验证方法能否识别出虚假关联。
        - **场景2（有时变直接关联）**：`{Xt}` 与 `{Yt}` 之间存在时间变化的直接依赖，同时存在 `{Zt}` 混淆，用于验证恢复真实信号的能力。
        - **场景3（高维混淆）**：将混淆过程 `{Zt}` 的维度从 `5` 提升到 `10` 和 `20`，测试PCA降维策略的有效性。
    - **真实数据**：2022年3月至2026年4月的美国ETF**日收益率**数据。
        - `Xt` 组：核心市场ETF (SPY, QQQ, IWM, XLK)。
        - `Yt` 组：特定行业ETF (XLF, XLI, XLY, XLE)。
        - `Zt` 组：外部市场ETF（国际股票ETF），维度从5增加到10、20、30。
- **基准（Benchmark）**：未调整的原始**小波典型相干（WaveCanCoh）** 作为对比基准。
- **对比方法**：主要对比了本文提出的 `Partial WaveCanCoh` 与其基础版本 `WaveCanCoh`，以及从 `Zt` 维度变化来观察调整效果。在模拟部分，还生成了经验零分布作为阈值检验边界。

### 4. 资源与算力

- **未明确说明**。论文未提及所使用的GPU型号、数量或训练时长。考虑到该方法主要涉及统计计算（矩阵运算、特征分解），而非大规模深度学习模型训练，其算力需求很可能是在普通工作站或服务器CPU上即可完成。

### 5. 实验数量与充分性

- **实验数量**：
    - **模拟实验**：针对3种不同场景，每个场景下进行了**1000次**独立重复的蒙特卡洛模拟。
    - **真实数据实验**：考虑了4种不同的混淆维度（`K=5,10,20,30`），并在4个不同尺度（`j=1,2,3,4`）上展示了结果。
- **充分性与公平性**：
    - **充分**：实验设计较为全面。在模拟中，不仅验证了方法有效时（有直接关联）的参数恢复能力，还验证了方法无效时（无直接关联）是否能正确得出零结果（严谨地校准了第一类错误率）。同时也考察了高维混淆下的鲁棒性。
    - **客观**：对比基准 `WaveCanCoh` 是与之最直接相关的、未调整的版本，对比非常恰当。
    - **存在局限**：缺乏与其他同类偏相关方法（若存在）的直接比较。虽然文称“这是首个频域方法”，但理论上可以与某些时域偏CCA方法（如Dégerine, 1990）进行比较，以突出小波方法的优势。

### 6. 论文的主要结论与发现

- **方法有效**：提出的 `Partial WaveCanCoh` 能够**有效区分**出由混淆因素导致的虚假关联和真实的直接关联。在无直接关联时，其估计值接近零；在有直接关联时，能**准确恢复**出随时间的动态变化模式。
- **高维鲁棒性**：当混淆变量维度很高时，通过PCA降维后处理，算法依然能够稳定地恢复出真实的直接关联。
- **现实数据发现**：对美国ETF市场的分析表明，调整了外部国际市场影响后，核心市场ETF组与特定行业ETF组之间**仍然存在显著的直接关联**。这种关联是**随时间变化**且**依赖尺度（投资期限）** 的。例如，高频（短期）的关联变弱，而低频（长期）的关联可能变强。这证明了考虑时变和频率特异性的必要性。
- **校准良好**：在零假设下的模拟表明，方法的第一类错误率能被良好地控制在名义水平附近。

### 7. 优点

- **首创性**：首次在频域（小波域）中为时间序列建立了偏典型相关分析，解决了该领域的长期空白。
- **理论扎实**：基于MvLSW框架，使方法在数学上严谨，能够处理非平稳性，并提供了估计的一致性证明。
- **实用性强**：能够同时提供时域、频域（尺度）和变量层面的信息，非常适合于分析复杂的动态系统（如金融市场、脑网络）。
- **可扩展性**：提出了结合PCA降维的高维解决方案，使得方法可以应用于现代大数据背景下的高维混淆场景。
- **流程清晰**：给出了完整的、可直接实现的算法步骤（Algorithm 1）。

### 8. 不足与局限

- **线性假设**：方法基于**线性**偏相关（通过线性回归进行偏化）。对于变量间存在强非线性依赖关系的情况，此方法可能不适用或需要扩展。
- **实验覆盖**：模拟实验只设计了两种尺度的活动（`j=1,4`），且依赖结构较为简单。对于更复杂、多尺度同步活动的场景评估不足。
- **降维策略**：PCA降维策略假设混淆效应主要由低维公共因子捕获。如果高维混淆过程的信息分散在很多独立的分量上，PCA可能导致重要混淆信息的丢失。
- **假设检验**：论文提供了估计方法，但没有提供相应的假设检验框架（如置信区间构造、显著性检验的P值），影响了推断的严谨性。虽然在零假设下做了校准，但应用时需要用户自行构建比较基准（如通过置换检验）。
- **计算复杂度**：虽然未提及算力需求，但谱估计（涉及平滑和矩阵求逆）以及每个时间和尺度都进行的特征分解，对非常长的时间序列或高维数据可能计算成本较高。

（完）
