---
title: "SOHET: Sequence Of Heterogeneous Events Transformer with Self-Supervised Pre-Training"
title_zh: SOHET：基于自监督预训练的异构事件序列变换器
authors: "Kees Jan de Vries, Mustafa Radha, Mathijs de Jong"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21356v1"
tags: ["query:ts-fm"]
score: 9.0
evidence: 异构事件序列的自监督预训练
tldr: "异质事件序列预测在欺诈检测等应用中至关重要，但现有方法难以同时建模多种事件类型和时序依赖。本文提出SOHET，采用事件类型特定表格编码器与时间和类型嵌入的层级Transformer架构，支持因果或双向处理。针对因果场景设计三项自监督预训练目标。在Booking.com大规模欺诈检测任务上，SOHET优于FlexTPP等基线5.8%，预训练额外提升2.6%并加快收敛2.4%；在EBES基准上，双向SOHET在8个任务中6个达到或超过最佳。该方法为异质事件序列建模提供了高效架构与预训练范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21356v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1768, \"height\": 857, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21356v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 745, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21356v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 699, \"height\": 386, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21356v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1779, \"height\": 423, \"label\": \"Table\"}]"
motivation: 现有方法难以有效建模异质事件序列中多种事件类型及其复杂时序依赖，需设计统一架构并利用未标注数据提升性能。
method: 提出SOHET，结合事件类型特定表格编码器与时间/类型嵌入，通过因果或双向Transformer处理序列，并引入三个自监督预训练目标。
result: "在Booking.com欺诈检测任务上，SOHET优于FlexTPP等模型5.8%，预训练额外提升2.6%并加速收敛2.4%；在EBES基准上，6/8任务达到或超过最佳。"
conclusion: SOHET通过层级编码与自监督预训练有效提升异质事件序列预测性能，验证了架构与预训练策略的有效性。
---

## 摘要
许多机器学习应用依赖于异构事件流进行预测，无论是因果性地随着事件到达还是双向地在完整序列上进行。我们提出SOHET（异构事件序列变换器），这是一种层次化架构，结合了事件类型特定的表格编码器与时间和类型嵌入，通过因果或双向变换器处理。我们为因果设置引入了三个自监督预训练目标。在一个拥有17种事件类型的大型专有真实世界Booking.com欺诈检测任务中，SOHET比FlexTPP、NAPPT和CIPPT提升了5.8%。预训练带来了额外的2.6%增益和2.4%的更快速收敛。在EBES基准测试中，双向SOHET在8个任务中的6个上匹配或超过了已发布的最佳结果。

## Abstract
Many machine learning applications rely on heterogeneous event streams to make predictions, either causally as events arrive or bidirectionally over complete sequences. We propose SOHET (Sequence Of Heterogeneous Events Transformer), a hierarchical architecture combining event-type-specific tabular encoders with temporal and type embeddings, processed by a causal or bidirectional transformer. We introduce three self-supervised pre-training objectives for the causal setting. On a proprietary large-scale real-world Booking.com fraud detection task with 17 event types, SOHET outperforms FlexTPP, NAPPT, and CIPPT by 5.8%. Pre-training yields an additional 2.6% gain and 2.4% faster convergence. On the EBES benchmark, bidirectional SOHET matches or exceeds the published best on 6 out of 8 tasks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：许多机器学习应用（如欺诈检测、医疗预测、推荐系统）依赖于**异构事件流**——即序列中每个事件具有不同的类型和对应的属性（数值/类别特征）。现有方法主要分为两类：因果预测（随事件到达实时更新）和双向预测（基于完整序列分类）。然而，大多数预训练工作集中在双向或同构事件序列上，**因果异构场景相对未被充分探索**。
- **核心挑战**：如何同时建模事件的时间依赖、事件类型的多样性以及每种事件特有的结构化特征，并利用大规模无标注数据进行预训练以提升性能和收敛速度。
- **整体含义**：提出了一个统一的架构（SOHET），支持因果和双向两种模式，并通过三项自监督预训练目标有效利用未标记的异构事件数据，显著提升了下游任务的准确率和训练效率。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：层次化架构——先对每个事件进行类型特定的特征编码，再通过Transformer进行序列建模；设计因果预训练任务，利用下一事件的时间、类型和特征对比学习。
- **事件编码**：每个事件的编码 = 时间嵌入（基于相对时间差∆τ_r和累积时间差∆τ_c的逐段线性嵌入）+ 事件类型嵌入 + 特征编码（通过每个事件类型独立的FT-Transformer编码器，输出共享维度的[CLS]表示）。
- **序列建模**：因果任务使用**ModernBERT Decoder**（因果掩码），双向任务使用**ModernBERT Encoder**（全可见）。支持变长序列的位置感知拼接。
- **自监督预训练（仅因果模式）**：
  1. **下一事件类型预测**：分类头预测下一事件的类型（增加一个结束标记）。
  2. **下一时间差预测**：回归两个时间差（相对和累积）的对数变换值。
  3. **对比下一行嵌入学习**：受JEPA和CLIP启发，将当前事件表示通过投影头得到查询向量，与后续M个事件的特征编码计算余弦相似度，使用交叉熵损失迫使查询最接近真正下一事件的特征编码（M=10）。三者损失等权重相加。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - **Booking.com欺诈检测数据集**：专有大规模真实数据，包含17种事件类型（如属性上线、预订、客服交互、财务交易等），任务为在线欺诈风险预测（因果/每事件预测）。时间分割，80/20训练/验证。
  - **EBES基准**（8个同构事件序列数据集）：Taobao、BPI17、PhysioNet2012、Retail、Age、ArabicDigits、ElectricDevices、Pendulum。任务为序列分类（双向），指标为ROC-AUC或准确率。
- **对比方法**：
  - 欺诈检测：**FlexTPP**、**NAPPT**、**CIPPT**（均为因果异构事件模型），参数匹配隐藏维度320和Transformer深度8（CIPPT深度4）。
  - EBES：9个基线（CoLES、GRU、MLEM、Transformer、Mamba、ConvTran、mTAND、PrimeNet、MLP），结果取自Osin et al. (2025)。
- **实验设置**：欺诈检测重复5个种子；EBES每个数据集用Optuna调参（50 trials），最终结果5个种子平均值。预训练后微调也重复5个种子。

### 4. 资源与算力

- **文中未明确说明GPU型号、数量及训练时长**。仅提及超参数：隐藏维度320，batch size 128（欺诈检测）或64/128（EBES），学习率1e-4，训练至早停（patience 20或10 epoch）。模型参数量：SOHET约79M（因包含事件特定编码器），FlexTPP约11M，NAPPT约10M，CIPPT约5M。
- **结论**：论文未提供算力消耗数据，无法评估训练成本。

### 5. 实验数量与充分性

- **实验数量**：
  - 两组主要实验（欺诈检测+EBES基准），欺诈检测中对比了3个基线，并测试了预训练效果；EBES对比了9个基线。
  - 消融实验：**未进行**。作者在附录B.5中承认未做完整消融研究，仅讨论了可能解释性能差异的架构差异（如层次设计、时间表示、词表限制）。
  - 预训练实验：仅对SOHET和基线进行了预训练+微调，但观察到NAPPT和CIPPT预训练不稳定，未做深入调试。
- **充分性与公平性**：
  - 公平性较好：基线参数（隐藏维度、Transformer层数）尽量匹配，但SOHET参数量更大（因事件编码器），这可能带来优势。
  - 实验覆盖了两个主要场景（因果异构、双向同构），但在更多公开异构数据集上未验证。
  - 统计学可靠性：多次种子重复，报告均值±标准差。
  - **不足**：缺乏对每个设计组件（如特定编码器、时间嵌入方式、不同预训练目标）的消融，难以确定每个因素的贡献。

### 6. 论文的主要结论与发现

1. **SOHET在欺诈检测上显著优于现有因果异构模型**：监督学习下AP 0.452，比最强的NAPPT（0.427）高**5.8%**。
2. **自监督预训练带来额外提升**：预训练后AP提升至0.464（+2.6%），且收敛速度提升**2.4倍**。
3. **双向SOHET在EBES同构序列基准上竞争力强**：在8个任务中的6个达到或超过最佳，尤其在Pendulum上从0.777提升至0.838（+7.9%）。
4. **层次架构和新型预训练目标有效**：事件类型特定编码器处理异构数据更灵活，对比学习避免重构原始特征，更适合欺诈检测场景。

### 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - 统一了因果与双向处理，具有实用灵活性。
  - 事件类型独立的FT-Transformer编码器，能够处理每个类型特有的列数、语义和缺失模式。
  - 新颖的对比下一行嵌入学习，避免特征级重构，且与CLIP类似使用温度缩放和交叉熵，训练稳定。
  - 时间表示同时使用相对和累积时间差，通过逐段线性嵌入增加表达能力。
- **实验设计亮点**：
  - 在专有大规模真实数据上验证，结果有实际意义。
  - 多个种子重复，报告统计量。
  - 与强基线（最接近的NAPPT、FlexTPP）公平匹配深度和维度。

### 8. 不足与局限

- **缺乏消融实验**：无法分离各组件（事件编码器vs时间表示vs预训练目标）的贡献。
- **预训练稳定性问题**：NAPPT和CIPPT在预训练时出现数值不稳定、梯度发散，SOHET未受影响，但论文未深入分析原因或提供补救。
- **参数量不匹配**：SOHET参数量（79M）远大于基线（5-11M），性能优势可能部分来自容量增加，而非架构本身。
- **实验覆盖有限**：仅在欺诈检测一个因果异构任务上验证，未在公开异构数据集（如MIMIC-III、MBD）上测试；同构序列实验中PhysioNet2012和ArabicDigits表现未超越基线。
- **计算资源未报告**：可复现性和能耗评估困难。
- **应用限制**：事件类型独立编码导致模型参数随类型数线性增长，可能难以扩展到数百种类型；未研究参数共享策略。
- **模态扩展**：论文仅处理表格化特征，未来可结合文本、图像等多模态，但目前未涉及。

（完）
