---
title: Interpretable Uncertainty for Adaptive Retrieval and Reasoning in Question Answering
title_zh: 可解释的不确定性用于问答中的自适应检索与推理
authors: "Ritajit Dey, Iadh Ounis, Graham McDonald"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07380v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 专注于问答中的大型语言模型
tldr: 大语言模型在问答中易产生幻觉且缺乏透明度。现有检索增强生成依赖不透明策略或多步提示。本文提出基于模型内部表示的不确定性感知框架，通过单次前向传递估计知识不足与知识冲突信号，自适应触发检索或额外推理。该方法提供透明、高效的替代方案，支持可解释的用户工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07380v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 885, \"height\": 441, \"label\": \"Figure\"}]"
motivation: 解决LLM在问答中因知识不足或矛盾导致幻觉，且现有RAG策略不透明、效率低的问题。
method: 从LLM隐藏状态提取显式不确定性信号，区分知识不足与知识冲突，在单次前向传播中估计，引导自适应检索或推理。
result: 在QA任务上，自适应策略在保持准确性的同时提高透明度，优于固定RAG或多步提示方法。
conclusion: 提出的不确定性感知框架实现了高效、可解释的检索与推理决策，有助于构建可信赖的问答系统。
---

## 摘要
大型语言模型（LLM）在问答（QA）中表现出色，但仍然容易出现幻觉且透明度有限。检索增强生成（RAG）可以提高事实性，但关于何时以及如何从外部资源进行检索的决策通常基于不透明的策略或计算效率低下的多步提示过程。我们提出了一种基于从LLM内部表示中提取的显式信号的自适应QA不确定性感知框架。我们区分了知识不足与知识模糊或冲突，并通过单次前向传递从隐藏状态中高效地估计这些信号。这些信号指导系统行为：当知识不足时触发RAG，而当模糊或冲突较高时应用额外推理。通过将自适应决策建立在可分解且高效可估计的不确定性信号上，该方法为现有的检索和推理策略提供了一种透明且实用的替代方案，支持可解释的用户面向工具的设计。

## Abstract
Large language models (LLMs) achieve a strong performance in question answering (QA), but remain prone to hallucinations and suffer from limited transparency. Retrieval-augmented generation (RAG) can improve factuality, yet decisions about when and how to retrieve from external resources are typically based on opaque policies or computationally inefficient multi-step prompting procedures. We propose an uncertainty-aware framework for adaptive QA based on explicit signals derived from LLM internal representations. We distinguish between knowledge insufficiency and knowledge ambiguity or conflict, and efficiently estimate these from hidden states in a single forward pass. These signals guide system behaviour: RAG is triggered when knowledge is insufficient, while additional reasoning is applied when ambiguity or conflict is high. By grounding adaptive decisions in decomposed and efficiently estimable uncertainty signals, this approach provides a transparent and practical alternative to existing retrieval and reasoning strategies supporting the design of interpretable user-facing tools.

---

## 论文详细总结（自动生成）

# 论文中文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLM）在问答任务中虽表现强劲，但易产生幻觉、依赖不完整或过时的内部知识，且推理过程缺乏透明性。检索增强生成（RAG）能提升事实性，但现有自适应检索策略要么依赖隐式学习的黑箱决策策略（如Self-RAG、Adaptive-RAG），要么依赖多步提示（如CoT），前者不透明、无法区分不同不确定性来源，后者计算效率低。因此，需要一种既透明又高效、能显式建模不同类型不确定性的决策机制。
- **研究动机**：在问答中，不确定性可能源于两类不同问题：**知识不足**（模型训练数据中缺乏相关事实）和**知识模糊/冲突**（存在多个相互竞争的候选答案）。两者需要不同的应对策略：知识不足应触发检索以补充信息；知识模糊/冲突则应增加测试时计算（如CoT、自一致性）以从候选答案中正确选择。现有方法无法区分这两种情况，导致决策不当。
- **整体含义**：本文提出基于LLM内部表示（隐藏状态）估计这两种不确定性信号，并据此自适应决定是否检索或增加推理，从而实现透明、高效的问答系统，支持可解释的用户工具设计。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：从LLM的隐藏状态中，通过轻量级回归探针（regression probes）在单次前向传递中估计两个不确定性指标：
  1. **知识不足**：估计为事实在LLM预训练语料中的出现频率（越低表示知识越不足）。参考Kandpal等人（2023）的方法，使用回归探针从隐藏状态预测该计数。
  2. **知识模糊/冲突**：估计为预训练语料中不同版本事实出现次数的熵（越高表示冲突越大）。参考Tomov等人（2026）的方法，同样使用回归探针预测该熵值。
- **关键技术细节**：
  - 探针训练：基于LLM的隐藏状态（从某一层或若干层提取）作为输入，训练线性或浅层回归模型分别预测上述两个标量值。不依赖额外采样或多步提示。
  - 自适应决策机制：基于阈值判断：
    - 若知识不足超过阈值 → 触发RAG（检索外部证据）；
    - 若知识模糊/冲突超过阈值 → 触发额外推理（如CoT或自一致性）；
    - 若两者均低 → 直接回答。
  - 整个框架如图1所示：输入问题→LLM前向→提取隐藏状态→估计两个不确定性→决策→执行对应策略。

## 3. 实验设计：数据集、基准与对比方法

- **数据集**：使用NQ（Natural Questions）数据集进行事实型问答实验。
- **模型**：Llama-2-7b-chat。
- **对比方法**：
  - **LLM-only基线**：直接由LLM回答，不检索。
  - **Always-on RAG基线**：对所有问题都执行RAG。
  - **本文提出的自适应策略**：基于Semantic Entropy (SE)或Weighted Entropy Production Rate (WEPR)两种不确定性估计方法触发RAG。
- **评估指标**：准确性（准确率），使用McNemar检验评估统计显著性。
- **初步结果**：
  - 基于SE的自适应策略：比LLM-only提升+5.9%，比Always-on RAG提升+3.3%。
  - 基于WEPR的自适应策略：比LLM-only提升+4.7%，比Always-on RAG提升+2.1%。
  - 所有提升均具统计显著性（McNemar检验）。

## 4. 资源与算力

- **未明确说明**：论文中未提及使用的GPU型号、数量、训练时长等算力信息。仅提到使用Llama-2-7b-chat模型，但未提供硬件配置或训练探针的具体资源消耗。这是本文信息缺失之处。

## 5. 实验数量与充分性

- **实验数量**：仅报告了NQ数据集上的一组初步结果，使用了两种不同的不确定性估计方法（SE和WEPR），对比了三个基线（LLM-only、Always-on RAG、自适应策略）。未进行消融实验、未在不同模型或数据集上验证。
- **充分性评价**：实验非常初步，仅作为“promising”的验证。缺乏对多种模型（如更大模型、不同架构）、多个数据集（如TriviaQA、HotpotQA）、不同推理方法（CoT、自一致性）的比较。未验证知识模糊/冲突场景的单独效果（论文中提及仅使用不确定性信号触发RAG，未实际测试触发额外推理的情况）。因此实验覆盖不足，不够充分；但作为短篇论文（workshop论文）的初步结果，可接受。

## 6. 论文的主要结论与发现

- **核心发现**：基于不确定性信号的自适应检索策略，在事实型问答上优于“总是检索”或“从不检索”的基线，验证了不确定性驱动的自适应QA的可行性。
- **理论贡献**：提出应区分“知识不足”与“知识模糊/冲突”两种不确定性，并分别设计不同应对措施，使决策可解释。
- **效率主张**：通过单次前向传递估计不确定性，避免了多步采样或辅助模型，保持了高效性。
- **设计理念**：可解释性应作为设计原则（而非事后工具），使系统能够暴露其面临的知识问题类型并解释行为。

## 7. 优点：方法或实验设计上的亮点

- **方法创新点**：
  - 首次提出从LLM隐藏状态中分解估计两种不同来源的不确定性（不足 vs. 冲突）。
  - 回归探针方法轻量、高效（单次前向），适合多步或智能体场景。
  - 决策机制简单、透明：基于显式阈值的规则，易于理解和调试。
- **实验设计亮点**：
  - 使用了两种不同的不确定性估计方法（SE、WEPR）进行验证，增加结果稳健性。
  - 统计显著性检验（McNemar检验）确保差异可靠。
  - 明确区分了两种不确定性场景并通过具体例子说明（如“Forest Green Rovers” vs “Chelsea”主教练问题）。

## 8. 不足与局限

- **实验覆盖严重不足**：仅在一个数据集（NQ）上使用一个模型（Llama-2-7b-chat）进行实验，未测试其他规模模型、其他领域问答、多跳问答或长文本场景。
- **未验证完全框架**：论文提出的完整框架包括“知识冲突时触发额外推理”，但初步实验仅使用了不确定性信号触发RAG（即只验证了知识不足场景），未实际实现和评估知识冲突时的推理分支。
- **阈值选择未讨论**：如何确定阈值（知识不足、冲突的阈值）未说明，可能存在敏感性。
- **探针训练的依赖**：回归探针需要标注数据（事实出现次数、冲突熵），获取这些标注本身可能有成本，且探针泛化性未验证。
- **可解释性验证缺失**：声称提供透明性和可解释性，但未设计用户实验或量化可解释性指标。
- **算力资源不透明**：未报告训练探针所需算力，导致难以评估实际部署成本。

（完）
