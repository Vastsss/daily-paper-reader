---
title: "SSDA: Bridging Spectral and Structural Gaps via Dual Adaptation for Vision-Based Time Series Forecasting"
title_zh: SSDA：通过双重适配弥合频谱与结构差距，用于基于视觉的时间序列预测
authors: "Mingrui Zhang, Hanchen Yang, Wengen Li, Xudong Jiang, Yichao Zhang, Jihong Guan, Shuigeng Zhou"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.12550v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 用于时间序列预测的大型视觉模型
tldr: 本研究针对将时间序列转化为图像进行预测时存在的频谱和结构差异问题，提出了SSDA双分支适配网络。通过频谱幅值对齐器（SMA）使渲染图像的统计特性接近自然图像，并利用结构引导低秩适配（SG-LoRA）修复因1D转2D导致的逻辑断层。实验证明，SSDA在多个基准测试中表现优异，有效提升了预训练视觉模型在时间序列预测中的迁移效率和准确性。
selection_source: fresh_fetch
motivation: 揭示了时间序列渲染图像与自然图像在频谱分布上的差异，以及1D序列转2D网格造成的结构逻辑断层，限制了视觉模型的迁移潜力。
method: 提出SSDA框架，包含用于对齐频谱统计特性的频谱幅值对齐器（SMA）和用于注入时间位置信息的结构引导低秩适配（SG-LoRA）。
result: 在七个真实世界数据集的全量和少样本实验中，SSDA的预测性能均显著优于现有的基于大语言模型和视觉模型的强基准方法。
conclusion: 通过在频谱和结构层面进行双重适配，该研究成功弥合了视觉与时间序列数据的鸿沟，充分释放了预训练视觉模型的预测能力。
---

## 摘要
大视觉模型（LVMs）最近被证明是出人意料有效的时间序列预测器，只需将时间数据渲染为图像即可。然而，这一成功建立在一个很大程度上未经检验的前提之上：渲染的时间序列图像与自然图像足够接近，使得预训练模型中的知识能够有效迁移。我们认为仍然存在两个差距，即频谱差距和结构差距，这从根本上限制了 LVMs 在时间序列预测方面的潜力。在频谱方面，我们系统地揭示了渲染的时间序列图像表现出比 LVMs 预训练识别的自然图像明显更浅的功率谱。在结构方面，将一维时间序列重塑为二维网格会制造虚假的空域相邻关系，同时切断真实的时间连续性，从而误导预训练 LVMs 的空间归纳偏置。为了弥合这些差距，我们提出了 SSDA，这是一个双支路网络，通过频谱和结构适配来释放 LVMs 在时间序列预测中的全部潜力。在数据层面，频谱幅度对齐器（SMA）应用二维快速傅里叶变换（2D FFT），在保留相位的同时，选择性地增强幅度谱以趋向自然图像统计特性。在模型层面，结构引导低秩适配（SG-LoRA）将位置感知的时间编码注入到 patch 嵌入中，并通过低秩更新来适配注意力机制。这两个支路进一步自适应融合以产生最终预测。在七个真实世界基准测试上的广泛实验表明，SSDA 在全样本和少样本设置下均始终优于强大的基于 LVM 和 LLM 的基线模型。代码公开于 https://anonymous.4open.science/r/SSDA-8C5B。

## Abstract
Large vision models (LVMs) have recently proven to be surprisingly effective time series forecasters, simply by rendering temporal data as images. This success, how ever, rests on a largely unexamined premise: the rendered time series images are sufficiently close to natural images for knowledge in pre-trained models to transfer effectively. We argue that two gaps still remain, i.e., spectral and structural gaps, fundamentally limiting the potential of LVMs for time series forecasting. Spectrally, we systematically reveal that rendered time series images exhibit a markedly shallower power spectrum than the natural images LVMs are pre-trained to recognize. Structurally, reshaping 1D temporal sequences into 2D grids fabricates spurious spatial adjacencies while severing genuine temporal continuities, misleading the spatial inductive biases of pre-trained LVMs. To bridge these gaps, we propose SSDA, a dual-branch network that spectrally and structurally adapts to unlock the full potential of LVMs for time series forecasting. At the data level, a Spectral Magnitude Aligner (SMA) applies 2D FFT to selectively enhance the magnitude spectrum toward natural-image statistics while preserving phase. At the model level, a Structural-Guided Low-Rank Adaptation (SG-LoRA) injects position-aware temporal encodings into patch embeddings and adapts at tention via low-rank updates. The two branches are further adaptively fused to produce the final forecast. Extensive experiments on seven real-world benchmarks demonstrate that SSDA consistently outperforms strong LVM- and LLM-based baselines under both full-shot and few-shot settings. Code is publicly available at https://anonymous.4open.science/r/SSDA-8C5B.

---

## 论文详细总结（自动生成）

### 论文总结：SSDA——弥合频谱与结构差距的视觉时间序列预测适配框架

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了将**大型视觉模型（LVMs）**应用于**时间序列预测（TSF）**时存在的根本性挑战。虽然将一维时间序列渲染为二维图像并利用预训练视觉模型（如 MAE）已取得一定成效，但作者指出，渲染图与自然图像之间存在显著的“模态鸿沟”：
*   **频谱差距（Spectral Gap）：** 渲染的时间序列图像功率谱比自然图像更浅（衰减更慢），导致预训练知识迁移效率低下。
*   **结构差距（Structural Gap）：** 将 1D 序列折叠为 2D 网格会产生虚假的空域相邻关系（原本时间上很远的点在图像中相邻），并切断真实的时间连续性（行末与下行行首在空间上分离）。
**SSDA** 的提出旨在通过双重适配机制系统性地解决这两个问题。

#### 2. 方法论：核心思想与关键技术
SSDA 采用双分支网络结构，分别从数据层面和模型层面进行适配：
*   **频谱分支 - 频谱幅值对齐器 (SMA)：**
    *   **核心思想：** 在频域调整图像统计特性。
    *   **流程：** 对输入图像进行 2D 快速傅里叶变换（FFT），分解为幅值和相位。使用轻量级卷积模块选择性增强幅值谱，使其接近自然图像的幂律分布（$1/f^\alpha$），同时保留相位信息以维持时间结构。最后通过逆 FFT 还原图像并输入冻结的 LVM 编码器。
*   **结构分支 - 结构引导低秩适配 (SG-LoRA)：**
    *   **核心思想：** 恢复被 2D 变换破坏的时间逻辑。
    *   **关键技术：** 
        *   **时间接地适配器 (TGA)：** 将 2D Patch 映射回原始 1D 时间索引，注入正弦时间位置编码。
        *   **可学习融合门：** 动态平衡原始视觉特征与时间结构信息。
        *   **LoRA 适配：** 通过低秩矩阵更新注意力机制的投影权重，使模型在微调中重新学习时间连续性。
*   **自适应融合：** 使用可学习的标量权重 $\beta$ 自动平衡两个分支的输出，产生最终预测。

#### 3. 实验设计
*   **数据集：** 使用了 7 个真实世界基准数据集：ETT（ETTh1, ETTh2, ETTm1, ETTm2）、Weather、Traffic 和 Electricity。
*   **预测场景：** 涵盖全量样本（Full-shot）和少样本（Few-shot，仅 5% 或 10% 训练数据）预测。
*   **对比方法（11 个基准）：**
    *   **基于 LVM：** VisionTS, DMMV, Time-VLM。
    *   **基于 LLM：** Time-LLM, TimeCMA, GPT4TS。
    *   **经典模型：** PatchTST, TimesNet, FEDformer, Informer, DLinear。
*   **评价指标：** 均方误差（MSE）和平均绝对误差（MAE）。

#### 4. 资源与算力
*   **硬件：** 使用了工作站配备的 **8 台 NVIDIA GeForce RTX 4090 GPU** (每台 24GB 显存)。
*   **软件环境：** Python 3.10, PyTorch 2.2.2, CUDA 12.1。
*   **训练细节：** Batch size 为 32，训练 10 个 epoch，采用早停机制（patience=3）。

#### 5. 实验数量与充分性
*   **实验规模：** 论文进行了极其详尽的实验，包括 7 个数据集在 4 种预测长度（96, 192, 336, 720）下的全量对比，以及少样本场景下的鲁棒性测试。
*   **消融实验：** 针对 SMA、SG-LoRA、TGA 等核心组件进行了多组消融实验，验证了各部分的必要性。
*   **参数敏感性：** 分析了回顾窗口长度、周期性参数、隐藏层维度及残差权重 $\lambda$ 的影响。
*   **充分性评价：** 实验设计客观、公平，对比了当前最先进的视觉和语言预训练模型，样本覆盖面广，结论具有较强的统计说服力。

#### 6. 主要结论与发现
*   **性能领先：** SSDA 在 48 项全量实验指标中获得第一，显著优于 DMMV 等强基线。
*   **视觉优于语言：** 实验证实视觉预训练模型在时间序列上的迁移潜力大于语言模型，因为连续信号与图像的结构对齐更自然。
*   **鸿沟确实存在：** 频谱分析证实了时间序列图像与自然图像的统计差异，且 SMA 和 SG-LoRA 的引入确实能有效弥合这些差距。
*   **少样本优势：** 在数据稀缺时，SSDA 表现出极强的稳定性，证明了预训练视觉知识的有效利用。

#### 7. 优点
*   **理论深刻：** 首次系统性地量化并定义了视觉时间序列预测中的“频谱”和“结构”模态鸿沟。
*   **设计精巧：** SMA 在保留相位（时间逻辑）的同时调整幅值（统计特性），SG-LoRA 解决了 2D 渲染带来的逻辑断层。
*   **参数高效：** 相比于全量微调或持续预训练，SSDA 采用 LoRA 和轻量级适配器，计算开销相对可控。

#### 8. 不足与局限
*   **周期性依赖：** 图像渲染高度依赖于预设的“周期性（Periodicity）”参数，对于周期性不明显或多周期的复杂序列，渲染效果可能受限。
*   **计算开销：** 虽然使用了 LoRA，但将 1D 序列转换为 224x224 的高分辨率图像并经过大型 Transformer 编码器，其推理延迟和显存占用仍高于纯线性或轻量级 CNN 模型。
*   **应用限制：** 目前主要针对单变量或变量数较少的场景，在极高维度的多元时间序列上的扩展性有待进一步验证。

（完）
