---
title: "HEPA: A Self-Supervised Horizon-Conditioned Event Predictive Architecture for Time Series"
title_zh: HEPA：一种用于时间序列的自监督时界条件化事件预测架构
authors: "Jonas Petersen, Gian-Alessandro Lombardi, Riccardo Maggioni, Camilla Mazzoleni, Federico Martelli, Philipp Petersen"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11130v2"
tags: ["query:ts-fm"]
score: 9.0
evidence: 时间序列事件预测的自监督预训练
tldr: 针对多变量时间序列中关键事件预测面临的标注数据稀缺挑战，本文提出HEPA架构。该方法结合自监督联合嵌入预测架构（JEPA）与因果Transformer，通过预训练捕获可预测的动态特征，并利用视界条件预测器生成单调生存累积分布函数。在11个领域的14个基准测试中，HEPA在参数量和标注需求大幅减少的情况下，性能优于PatchTST和iTransformer等主流模型。
selection_source: fresh_fetch
motivation: 旨在解决多变量时间序列中关键事件稀少且标注成本高昂导致的预测难题。
method: 采用基于JEPA的因果Transformer预训练编码器来预测未来表征，并微调视界条件预测器以输出生存累积分布函数。
result: 在14个基准测试中的10个表现优于PatchTST、iTransformer等模型，且显著降低了可调参数量和对标注数据的依赖。
conclusion: HEPA证明了自监督表征学习在处理跨领域、少样本时间序列事件预测任务中的高效性和通用性。
---

## 摘要
多变量时间序列中的关键事件（从涡轮机故障到心律失常）需要准确的预测，但由于此类事件稀少且标注成本高昂，标注数据非常匮乏。我们提出了 HEPA（时界条件化事件预测架构），它基于两个核心原则。首先，通过联合嵌入预测架构（JEPA）预训练一个因果 Transformer 编码器：一个时界条件化预测器学习预测未来的表示而非未来的数值，从而迫使编码器仅从无标注数据中捕捉可预测的时间动态。其次，我们冻结编码器，仅针对目标事件微调预测器，从而在不同时界上生成单调的生存累积分布函数（CDF）。在所有基准测试中采用固定的架构和优化器超参数，HEPA 处理了水污染、网络攻击检测、波动率机制以及跨 11 个领域的另外 8 种事件类型。在 14 个基准测试中的至少 10 个上，HEPA 超过了包括 PatchTST、iTransformer、MAE 和 Chronos-2 在内的领先时间序列架构，且调优参数减少了一个数量级；在生命周期数据集上，所需的标注数据也减少了一个数量级。

## Abstract
Critical events in multivariate time series, from turbine failures to cardiac arrhythmias, demand accurate prediction, yet labeled data is scarce because such events are rare and costly to annotate. We introduce HEPA (Horizon-conditioned Event Predictive Architecture), built on two key principles. First, a causal Transformer encoder is pretrained via a Joint-Embedding Predictive Architecture (JEPA): a horizon-conditioned predictor learns to forecast future representations rather than future values, forcing the encoder to capture predictable temporal dynamics from unlabeled data alone. Second, we freeze the encoder and finetune only the predictor toward the target event, producing a monotonic survival cumulative distribution function (CDF) over horizons. With fixed architecture and optimiser hyperparameters across all benchmarks, HEPA handles water contamination, cyberattack detection, volatility regimes, and eight further event types across 11 domains, exceeding leading time-series architectures including PatchTST, iTransformer, MAE, and Chronos-2 on at least 10 of 14 benchmarks, with an order of magnitude fewer tuned parameters and, on lifecycle datasets, an order of magnitude less labeled data.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **HEPA**（Horizon-conditioned Event Predictive Architecture）的新型自监督学习架构，旨在解决多变量时间序列中关键事件预测面临的标注数据稀缺和任务碎片化问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究动机**：在工业（如涡轮机故障）、医疗（如心律失常）和航天等领域，关键事件虽然影响巨大，但在实际数据中极少发生，且标注成本极高。
*   **核心问题**：现有的方法通常分为“剩余寿命预测（RUL）”和“异常检测”两大阵营，缺乏统一的架构和评估标准。此外，传统的生成式预训练（预测未来数值）往往会捕捉过多的无用噪声，而非与事件相关的核心动态。
*   **整体含义**：HEPA 旨在通过自监督学习，在无标注数据中提取可预测的时间动态，并以极少的标注数据实现跨领域的精准事件预测。

### 2. 方法论：核心思想与技术细节
HEPA 的构建基于两个阶段：
*   **阶段一：自监督预训练（JEPA 路径）**
    *   **核心思想**：采用联合嵌入预测架构（JEPA）。不预测未来的具体数值，而是预测未来时段的“表征（Representation）”。
    *   **架构**：使用因果 Transformer 编码器将观测序列映射为嵌入向量 $h_t$。
    *   **预测器**：一个 MLP 预测器接收 $h_t$ 和目标时界（Horizon）$\Delta t$，输出对未来表征的预测 $\hat{h}$。
    *   **损失函数**：结合了 L1 预测损失和 **SIGReg**（正则化项），后者用于防止表征崩溃（Representation Collapse），确保嵌入空间呈各向同性的高斯分布。
*   **阶段二：预测器微调（Predictor Finetuning）**
    *   **核心思想**：冻结编码器，仅微调预测器和一个轻量级的事件头。
    *   **生存建模**：将事件预测参数化为离散时间生存累积分布函数（CDF）。通过这种设计，预测的事件概率随预测时界的增加而单调递增，符合物理逻辑。
    *   **理论支撑**：论文提出了一个信息论界限（Proposition 1），证明了预训练损失越低，编码器保留的下游事件相关信息就越多。

### 3. 实验设计
*   **数据集与场景**：涵盖 11 个领域的 14 个基准测试，包括 C-MAPSS（涡轮机失效）、SMAP（航天器异常）、MBA（心律失常）、GECCO（水污染）、BATADAL（网络攻击）等。
*   **评估指标**：提出统一的 **h-AUROC**（时界平均 AUROC），该指标对类别不平衡鲁棒，且无需设定阈值。
*   **对比方法**：
    *   **架构类**：PatchTST, iTransformer, MAE（掩码自编码器）。
    *   **基础模型类**：Chronos-2, MOMENT, TFM-2.5, Moirai。
    *   **领域特定类**：STAR (RUL 领域), Anomaly Transformer (异常检测领域)。

### 4. 资源与算力
*   **硬件**：实验在单张 **NVIDIA A10G GPU** (24GB) 上运行。
*   **效率**：HEPA 的预训练极其高效，每个数据集的预训练时间**不足 1 分钟**。
*   **全量实验耗时**：包含 14 个数据集、5 个随机种子的完整实验流程在 **2 小时内**即可完成。

### 5. 实验数量与充分性
*   **实验规模**：在 14 个基准数据集上进行了测试，每个实验运行 5 个随机种子（基础模型对比为 3 个种子），提供了均值和标准差。
*   **消融实验**：涵盖了微调模式（全量微调 vs. 预测器微调）、目标编码器更新规则（EMA vs. 联合训练）、预测器权重初始化等多个维度的消融研究。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了最先进的通用架构，还对比了参数量大得多的时间序列基础模型，并验证了在极低标注比例（如 2% 标签）下的性能表现。

### 6. 主要结论与发现
*   **性能领先**：在 14 个基准测试中的 10 个上，HEPA 优于 PatchTST、iTransformer 等主流模型。
*   **参数高效**：HEPA 仅需微调 19.8 万个参数，比端到端训练少一个数量级（约 11 倍）。
*   **标签高效**：在生命周期数据集（如 C-MAPSS）上，HEPA 仅用 2% 的标注数据即可保留 92% 的全量标签性能。
*   **理论验证**：实验证实了预训练损失与下游任务表现之间的强负相关性，验证了其理论推导。

### 7. 优点与亮点
*   **统一框架**：成功将 RUL 预测和异常检测统一在同一个概率表面 $p(t, \Delta t)$ 下。
*   **单调性保证**：通过生存分析建模，从数学架构上保证了预测概率随时间跨度增加的合理性。
*   **训练极快**：相比于需要大规模预训练的基础模型，HEPA 证明了“小而精”的针对性自监督预训练在特定领域的高效性。
*   **理论与实践结合**：不仅有出色的实验结果，还提供了严谨的信息论证明。

### 8. 不足与局限
*   **传感器局部化事件**：在某些事件仅由少数传感器通道决定的数据集（如 MBA 心律失常）上，HEPA 的表现略逊于 iTransformer，这可能是因为其通道融合（Channel-fusion）策略稀释了关键信号。
*   **校准风险**：由于使用了加权二元交叉熵损失，模型输出的概率尺度可能存在偏差（Miscalibration），在安全敏感应用中需要进行后验校准。
*   **预训练依赖**：性能高度依赖于未来动态的可预测性。如果事件发生前没有任何前兆信号（如突发性故障），该方法将失效。

（完）
