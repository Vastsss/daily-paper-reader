---
title: "Compact Latent Manifold Translation: A Parameter-Efficient Foundation Model for Cross-Modal and Cross-Frequency Physiological Signal Synthesis"
title_zh: 紧凑潜流形转换：一种用于跨模态与跨频率生理信号合成的参数高效基础模型
authors: "Bo Cui, Xiaowen Song, Yaowen Zhang, Shunzhe Zhang, B. J. F. van Beijnum, Monique Tabak, Ying Wang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13248v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 生理时间序列的高效参数基础模型
tldr: 针对生理信号（如ECG和PPG）在跨模态和跨频率合成中存在的模态纠缠及高计算成本问题，本文提出了一种轻量化基础模型CLMT（0.09B参数）。该模型通过分层残差向量量化（RVQ）将信号解耦为离散潜流形，并利用上下文提示翻译器实现跨模态映射。实验证明，CLMT在PPG转ECG及超分辨率任务上表现卓越，显著提升了临床诊断指标，为边缘设备部署的多模态医疗模型提供了新方案。
selection_source: fresh_fetch
motivation: 现有生理信号模型面临模态纠缠、高频生成能力不足以及计算开销过大导致难以在边缘设备部署的挑战。
method: 提出一种两阶段离散翻译范式，利用分层RVQ分词器将信号解耦为离散潜流形，并结合上下文提示翻译器进行序列映射。
result: 仅用0.09B参数即在PPG转ECG任务中将R峰检测F1分数从0.37提升至0.83，并在超分辨率任务中达到0.9956的相关性。
conclusion: 该研究通过学习生物信号的通用离散语言，实现了一种高效、高保真且适用于边缘部署的多模态医疗基础模型。
---

## 摘要
心电图（ECG）和光电容积脉搏波（PPG）等生理时间序列的分析，长期受到异构记录设备产生的模态和频率差异的阻碍。现有的基础模型通常依赖于连续潜空间，这往往会导致严重的模态纠缠，缺乏高保真跨频率生成能力，且计算成本高昂，阻碍了在边缘设备上的部署。在本文中，我们提出了紧凑潜流形转换（CLMT），这是一个参数量极低（0.09B）的统一框架，通过一种新颖的两阶段离散转换范式来弥合这些差距。首先，我们引入了一种利用分层残差向量量化（RVQ）的通用分词器，将异构信号解耦为孤立且结构良好的离散潜流形，从而有效防止模态间的干扰。其次，上下文提示潜转换器通过整合静态生理先验，在不同模态间映射这些离散标记，将复杂的信号合成重新定义为纯粹的潜序列转换任务。广泛的评估表明，我们的 0.09B 模型显著优于大规模基准模型。在跨模态 PPG 到 ECG 的合成中，它解决了时间相位漂移问题，并将临床 R 波峰检测的 F1 分数从 0.37（基准）大幅提升至 0.83。此外，在极端的跨频率超分辨率（25Hz 到 100Hz）任务中，它成功恢复了高频诊断特征点，实现了前所未有的 0.9956 皮尔逊相关系数。通过以极小的计算开销学习生物信号的通用离散语言，我们的方法为可边缘部署的多模态医学基础模型开辟了新路径。

## Abstract
The analysis of physiological time series, such as electrocardiograms (ECG) and photoplethysmograms (PPG), is persistently hindered by modality and frequency gaps stemming from heterogeneous recording devices. Existing foundation models typically rely on continuous latent spaces, which frequently suffer from severe modality entanglement, lack high-fidelity cross-frequency generative capacity, and impose high computational costs that prohibit edge-device deployment. In this paper, we propose Compact Latent Manifold Translation (CLMT), a highly parameter-efficient (0.09B) unified framework that bridges these gaps through a novel two-stage discrete translation paradigm. First, we introduce a Universal Tokenizer utilizing Hierarchical Residual Vector Quantization (RVQ) to decouple heterogeneous signals into isolated, well-structured discrete latent manifolds, effectively preventing inter-modality interference. Second, a Context-Prompted Latent Translator maps these discrete tokens across modalities by integrating static physiological priors, reframing complex signal synthesis as a pure latent sequence translation task. Extensive evaluations demonstrate that our 0.09B model significantly outperforms massive baselines. In cross-modal PPG-to-ECG synthesis, it resolves temporal phase drift and dramatically improves the clinical R-peak detection F1-score from 0.37 (baseline) to 0.83. Furthermore, in extreme cross-frequency super-resolution (25Hz to 100Hz), it successfully recovers high-frequency diagnostic landmarks, achieving an unprecedented Pearson correlation of 0.9956. By learning a universal discrete language for biological signals with a fraction of the computational footprint, our approach sets a new trajectory for edge-deployable, multi-modal medical foundation models.

---

## 论文详细总结（自动生成）

### 论文总结：紧凑潜流形转换（CLMT）基础模型

#### 1. 核心问题与整体含义
生理信号（如心电图 ECG 和脉搏波 PPG）的分析长期受限于**模态差异**（传感器物理原理不同）和**频率差异**（临床设备与可穿戴设备采样率不一）。现有基础模型存在三大痛点：
*   **模态纠缠**：连续潜空间难以区分不同信号的物理来源。
*   **高频缺失**：在跨频率合成（超分辨率）时，由于“回归均值”效应，无法恢复关键的临床诊断特征（如 R 峰）。
*   **计算开销大**：模型参数量巨大，难以在资源受限的边缘设备（如智能手表）上部署。

#### 2. 方法论
CLMT 提出了一种**两阶段离散潜流形翻译**范式：
*   **第一阶段：通用分词器（Universal Tokenizer）**
    *   采用**分层残差向量量化（Hierarchical RVQ）**。
    *   **核心思想**：将信号解耦。顶层量化器捕捉跨模态共享的宏观节律（如心率），底层量化器捕捉模态特有的微观形态（如 ECG 的 QRS 波群）。
    *   引入**形状感知重构损失**，确保离散字典能保留局部梯度和全局相位。
*   **第二阶段：下游任务适配器**
    *   **跨模态翻译（Stage 2A）**：利用轻量级 Transformer 将 PPG 潜序列映射为 ECG 潜序列，并引入静态生理先验（如年龄、性别）作为 Prompt。
    *   **超分辨率 MAE（Stage 2B）**：在离散潜空间内进行掩码自编码（MAE），实现从低频（25Hz）到高频（100Hz）的补全。
    *   **Snapping 效应**：推理时将连续预测“对齐”到冻结的离散 Codebook 上，强制输出符合生理先验的锐利波形，避免模糊。

#### 3. 实验设计
*   **数据集/场景**：涵盖了异构采样率和多模态生理信号。
*   **任务**：PPG 到 ECG 的跨模态合成、25Hz 到 100Hz 的 4 倍超分辨率。
*   **Benchmark 与对比方法**：
    *   **跨模态**：对比了端到端回归（E2E）、P2E 基准模型。
    *   **超分辨率**：对比了线性插值、三次插值、1D CNN。
    *   **基础模型对比**：与大规模模型 CSFM（1.17亿参数）进行潜空间拓扑对比。
*   **评价指标**：RMSE、MAE、皮尔逊相关系数（PCC）、R 峰检测 F1 分数、Fréchet 距离（FD）。

#### 4. 资源与算力
*   **硬件**：使用 NVIDIA RTX A6000 Pro GPU。
*   **训练细节**：采用 AdamW 优化器，初始学习率 $1 \times 10^{-3}$，Weight Decay $1 \times 10^{-4}$，Batch Size 为 64。
*   **时长**：文中未明确给出总训练小时数，但提到使用了早停机制（Patience=5）。

#### 5. 实验数量与充分性
*   **实验规模**：进行了跨模态翻译实验、超分辨率实验、消融实验（针对静态提示和两阶段架构）以及潜空间可视化分析（UMAP/t-SNE）。
*   **充分性**：实验设计较为全面。通过 UMAP 证明了模态隔离的有效性，通过 t-SNE 证明了心脏相位梯度的连续性。
*   **客观性**：对比了多个维度的指标（时域、频域、临床诊断指标），并设置了性能上限（Intra-modal autoencoding）作为参考，评估较为客观。

#### 6. 主要结论与发现
*   **参数高效性**：模型仅 0.09B（90M）参数，比同类基础模型（如 CSFM）减少了约 23% 的参数，且性能更优。
*   **临床价值**：在 PPG 转 ECG 任务中，将 R 峰检测的 F1 分数从 0.37 提升至 0.83，解决了相位漂移问题。
*   **超分辨率突破**：在 4 倍超分辨率任务中，PCC 达到 0.9956，成功恢复了 20Hz 以上的高频谐波。
*   **离散化的优势**：离散 Codebook 起到了“生理约束”的作用，有效克服了连续生成模型常见的过平滑问题。

#### 7. 优点
*   **架构创新**：分层 RVQ 巧妙地实现了生理信号的宏观/微观解耦。
*   **生成质量高**：通过“Snapping”机制生成的波形具有极高的形态保真度，满足临床诊断需求。
*   **部署友好**：极小的参数量使其具备在可穿戴设备上进行实时推理的潜力。

#### 8. 不足与局限
*   **分布外（OOD）风险**：对于训练集中未包含的罕见心律失常，离散量化可能会将其“强行归类”为正常模式，导致漏诊。
*   **先验依赖**：模型性能部分依赖于静态生理元数据，在缺乏患者信息的紧急情况下，精度可能下降。
*   **泛化验证**：虽然在 ECG/PPG 上表现优异，但尚未在脑电（EEG）或肌电（EMG）等更多生理模态上验证通用性。

（完）
