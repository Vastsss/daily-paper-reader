---
title: Efficient Prompt Learning for Traffic Forecasting
title_zh: 高效的交通预测提示学习
authors: "Qianru Zhang, Xinyi Gao, Alexander Zhou, Reynold Cheng, Siu-Ming Yiu, Hongzhi Yin"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08273v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 时空交通预测的提示学习
tldr: 针对时空图神经网络在交通预测中因分布偏移导致泛化能力差的问题，本文提出了一种轻量级且模型无关的提示微调框架 SimpleST。该方法通过在保持预训练模型参数不变的情况下引入提示机制，实现了对新分布的高效适配。在五个真实数据集上的实验证明，SimpleST 在预测精度和计算效率上均优于现有方法，显著提升了模型的泛化性能。
selection_source: fresh_fetch
motivation: 现有的时空图神经网络在面对交通动态引起的分布偏移时，往往表现出较低的泛化能力。
method: 提出 SimpleST 框架，通过轻量级的提示微调机制，在不改变预训练模型参数的前提下实现跨分布适配。
result: 在五个真实城市时空数据集上的实验表明，该方法在预测准确性和计算效率方面均具有显著优势。
conclusion: SimpleST 为提升预训练时空模型在分布外场景下的泛化与适配能力提供了一种高效且通用的解决方案。
---

## 摘要
准确的交通预测对于优化交通系统、增强资源配置以及提升整体城市管理至关重要。时空图神经网络（GNNs）已取得最先进的性能，并被广泛应用于各种时空预测场景。然而，这些预测方法通常表现出较低的泛化能力，难以应对由时空动态引起的分布偏移。为了应对这一挑战，我们提出了一种通过高效提示来增强时空图神经网络泛化与自适应能力的方法。具体而言，我们为时空图神经网络引入了一个轻量级且模型无关的提示微调框架，命名为 SimpleST。它有助于在保持模型参数固定的情况下，使预训练的时空图神经网络适应新的分布。这种提示机制降低了自适应的开销和复杂性，实现了对预训练模型的高效利用，以进行分布外泛化。在五个真实世界的城市时空数据集上进行的广泛实验证明了我们的方法在预测准确性和计算效率方面的优越性。

## Abstract
Accurate traffic prediction is essential for optimizing transportation systems, enhancing resource allocation, and improving overall urban administration. Spatio-temporal graph neural networks (GNNs) have achieved state-of-the-art performance and have been widely used in various spatio-temporal prediction scenarios. However, these prediction methods often exhibit low generalization ability, struggling with distribution shifts caused by spatio-temporal dynamics. To address this challenge, we propose an approach to enhance the generalization and adaptation of spatio-temporal GNNs through efficient prompting. Specifically, we introduce a lightweight and model-agnostic prompt tuning framework for spatio-temporal GNNs, named SimpleST. It facilitates adapting pre-trained spatio-temporal GNNs to novel distributions while keeping the model parameters fixed. This prompt mechanism reduces the overhead and complexity of adaptation, enabling efficient utilization of pre-trained models for out-of-distribution generalization. Extensive experiments conducted on five real-world urban spatio-temporal datasets demonstrate the superiority of our approach in terms of prediction accuracy and computational efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient Prompt Learning for Traffic Forecasting》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：时空图神经网络（ST-GNNs）在交通预测中表现优异，但面临两大挑战：
    1.  **时空分布偏移**：由于城市动态变化（如人口迁移、突发事件），测试数据分布往往偏离训练数据，导致模型泛化能力差。
    2.  **计算开销巨大**：为了适应新分布，传统方法通常需要全参数微调，这在资源受限或对延迟敏感的实时场景中不可行。
*   **核心目标**：提出一种轻量级、模型无关的提示学习（Prompt Learning）框架，在不改变预训练模型参数的前提下，通过极少量的参数更新实现对新分布的高效适配。

### 2. 方法论：SimpleST 框架
*   **核心思想**：将提示网络设计为一个“数据编辑器（Data Editor）”。它接收偏移后的输入数据，通过学习残差变换，将其映射回预训练模型能够理解的特征空间。
*   **关键技术细节**：
    *   **冻结骨干网络**：预训练好的 ST-GNN（如 MTGNN, AGCRN 等）参数完全固定。
    *   **时间感知提示网络（Temporal Prompt Network）**：由 2 层时间卷积网络（TCN）和 2 层多层感知机（MLP）组成。TCN 用于捕捉局部时间动态和偏移模式，MLP 进行非线性投影。
    *   **残差连接（Skip Connection）**：最终输出为 `原始输入 + 提示网络输出`。这种设计确保了如果分布偏移很小，提示网络只需输出接近零的微调值，保证了训练的稳定性。
    *   **理论保障**：论文通过 Lipschitz 连续性证明了该组合模型的稳定性，并推导了其在领域偏移下的泛化误差上界。

### 3. 实验设计
*   **数据集**：使用了 5 个真实世界的加州高速公路交通数据集：PeMSD03, PeMSD04, PeMSD07, PeMSD08 和大规模数据集 PeMS-Bay。
*   **骨干模型（Backbones）**：ASTGNN, AGCRN, MTGNN, STG-NCDE。
*   **对比方法（Baselines）**：
    *   **基础模型**：直接使用预训练模型。
    *   **分布外（OOD）泛化方法**：CauSTG, IRM。
    *   **微调方法**：全参数微调（Finetune）。
*   **评估指标**：MAE（平均绝对误差）、RMSE（均方根误差）、MAPE（平均绝对百分比误差）。

### 4. 资源与算力
*   **硬件配置**：实验在一台配备 **8 张 NVIDIA H100 80GB GPU** 的服务器上运行，CPU 为 48 核 Intel Xeon Max 9468。
*   **训练效率**：
    *   SimpleST 仅更新不到 **2%** 的模型参数。
    *   适配速度比全参数微调快 **2 倍至 46 倍**。
    *   显存占用显著降低（例如在某些数据集上 GPU 成本降低了约 40%-80%）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **总体性能对比**：在 5 个数据集上结合 4 种不同架构的骨干模型进行了全面测试。
    *   **消融实验**：验证了 TCN、MLP 和残差连接各自的贡献。
    *   **效率研究**：对比了不同数据量（1天、1周、2周）下的训练耗时。
    *   **扩展性实验**：验证了模型在处理大规模图结构时的计算复杂度。
    *   **鲁棒性验证**：包括跨城市（加州到东京）迁移实验和基于 SUMO 的动态交通仿真验证。
*   **评价**：实验设计非常充分且客观。通过跨架构、跨城市、跨仿真环境的多维度对比，有力证明了该方法的通用性和高效性。

### 6. 主要结论与发现
*   **精度提升**：SimpleST 在所有数据集上均优于全参数微调和 OOD 基准方法，平均 MAE 降低了 **3%–6%**。
*   **效率飞跃**：在保持甚至提升精度的同时，适配时间大幅缩短，证明了提示学习在时空数据处理中的巨大潜力。
*   **分布适配**：案例研究显示，SimpleST 能有效修正测试集中出现的异常波动（如早晚高峰偏移），而未适配的模型在这些点位误差极大。

### 7. 优点与亮点
*   **模型无关性**：作为一个“插件”，它可以适配任何现有的 ST-GNN 架构，具有极强的通用性。
*   **参数极其精简**：仅需更新极少量参数即可达到甚至超过全量微调的效果，非常适合边缘计算设备。
*   **理论与实践结合**：不仅有详尽的实验，还提供了关于收敛性和稳定性的数学证明。
*   **现实意义**：针对交通预测中常见的“概念漂移”问题给出了极具工程参考价值的解决方案。

### 8. 不足与局限
*   **空间提示缺失**：目前主要侧重于时间维度的提示（TCN），虽然 GNN 骨干处理了空间，但如果空间拓扑发生剧烈变化（如道路封锁），仅靠时间提示可能不足。
*   **依赖预训练质量**：该方法的效果高度依赖于预训练骨干模型捕获基础时空特征的能力。
*   **超参数敏感性**：虽然论文做了超参数研究，但在不同城市规模下，TCN 的核大小和隐藏层维度可能仍需手动调优。

（完）
