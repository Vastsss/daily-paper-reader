---
title: "NoRIN: Backbone-Adaptive Reversible Normalization for Time-Series Forecasting"
title_zh: NoRIN：面向时间序列预测的骨干自适应可逆归一化
authors: "Shun Zhang, Yuyang Xiao"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10823v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 用于时间序列预测的非线性可逆归一化
tldr: 针对时间序列预测中现有可逆归一化方法（如RevIN）仅限于线性变换、无法处理重尾和偏态分布的问题，本文提出了NoRIN。NoRIN采用非线性的Johnson $S_U$变换，通过引入形状参数来调整分布。为解决梯度下降导致的参数退化问题，研究者将形状参数的选择与模型训练解耦，利用分位数拟合和贝叶斯优化进行优化。实验证明，不同模型架构对归一化参数有特定偏好，NoRIN显著提升了多种主流模型的预测性能。
selection_source: fresh_fetch
motivation: 现有的可逆归一化方法多为线性变换，难以处理时间序列中普遍存在的非正态分布特征（如重尾和偏态）。
method: 提出基于Johnson $S_U$变换的非线性归一化方法NoRIN，并采用解耦的形状参数优化策略（分位数拟合+贝叶斯优化）以避免梯度退化。
result: 在多个数据集和模型架构上的实验表明，NoRIN找到的非线性参数显著优于线性基准，且最优参数具有明显的模型依赖性。
conclusion: 非线性归一化能有效提升时间序列预测效果，且针对不同骨干网络进行自适应的形状参数调整是至关重要的。
---

## 摘要
可逆实例归一化 (RevIN) 及其后续方法 (Dish-TS, SAN, FAN) 已成为时间序列预测的事实标准插件，然而它们应用于每个数据点的映射是严格仿射的 ($x \mapsto ax+b$)，因此无法重塑底层分布——重尾特征依然显著，偏度也未得到修正。我们提出了 NoRIN，这是一种基于 arcsinh 形式 Johnson $S_U$ 变换的非线性可逆归一化，具有两个控制尾部特征和偏度的形状参数 $(\delta,\varepsilon)$；RevIN 使用的线性 $Z$-score 仅在 $\delta\to \infty$ 的极限下恢复。通过梯度下降将 $(\delta,\varepsilon)$ 与骨干网络联合训练，会在几个 epoch 内可靠地将它们推向该线性极限——我们将这一现象称为退化问题 (degeneration problem)：预测损失在局部对形状不敏感，且高容量骨干网络会补偿其输入的任何单调重参数化。NoRIN 通过将形状选择与梯度训练解耦来规避退化：$(\delta,\varepsilon)$ 通过闭合形式的 Slifker-Shapiro 分位数拟合进行初始化，并根据验证集目标通过贝叶斯优化进行精细化，而内部训练循环与标准的 RevIN 风格训练相同。在 6 个代表性骨干网络 × 5 个真实世界数据集 × 3 个预测步长（共 90 种配置）的实验中，解耦形状优化恢复出的 $(\delta^\star,\varepsilon^\star)$ 系统性地远离线性极限，且其取值随骨干网络的不同而变化。这从经验上支持了核心论点：不同的骨干网络确实需要不同的归一化参数才能达到其最佳性能。

## Abstract
Reversible instance normalization (RevIN) and its successors (Dish-TS, SAN, FAN) have become the de facto plug-in for time-series forecasting, yet the map they apply to each data point is strictly affine, $x \mapsto ax+b$, so they cannot reshape the underlying distribution -- heavy tails remain heavy and skewness remains uncorrected. We propose NoRIN, a non-linear reversible normalization based on the arcsinh-form Johnson $S_U$ transform with two shape parameters $(δ,\varepsilon)$ that control tailedness and skewness; the linear $Z$-score used by RevIN is recovered only in the limit $δ\to \infty$. Training $(δ,\varepsilon)$ jointly with the backbone via gradient descent reliably pushes them toward this linear limit within a few epochs -- a phenomenon we name the degeneration problem: the forecasting loss is locally indifferent to shape, and the high-capacity backbone compensates for any monotone reparameterization of its input. NoRIN escapes the degeneration by decoupling shape selection from gradient training: $(δ,\varepsilon)$ are initialized by a closed-form Slifker-Shapiro quantile fit and refined by Bayesian optimization on the validation objective, while the inner training loop is identical to standard RevIN-style training. Across six representative backbones x five real-world datasets x three prediction horizons (90 configurations), decoupled shape optimization recovers $(δ^\star,\varepsilon^\star)$ that sit systematically far from the linear limit, with values that vary in a backbone-dependent way. This empirically supports the central thesis: different backbones genuinely require different normalization parameters to reach their best performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《NoRIN: Backbone-Adaptive Reversible Normalization for Time-Series Forecasting》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：在深度时间序列预测中，可逆实例归一化（如 RevIN）已成为处理非平稳性的标准配置。然而，现有的 RevIN 及其变体（如 Dish-TS, SAN, FAN）本质上都是**线性仿射变换**（$x \mapsto ax + b$）。
*   **核心矛盾**：线性变换只能调整均值和方差，无法改变分布的“形状”。现实世界的数据常具有**重尾（Heavy-tailed）**和**偏态（Skewness）**特征，线性方法无法修正这些高阶统计特性，限制了模型的泛化能力。
*   **科学挑战**：直接将非线性变换引入端到端训练会导致“**退化问题**”（Degeneration Problem），即梯度下降会将非线性参数推向线性极限，使非线性设计失效。

### 2. 方法论
*   **核心思想**：引入基于 **Johnson $S_U$ 变换**的非线性可逆归一化，并通过**解耦优化策略**解决训练退化问题。
*   **技术细节**：
    *   **Johnson $S_U$ 变换**：利用 $\text{arcsinh}$ 函数实现非线性映射。公式为 $Z = \varepsilon + \delta \cdot \text{arcsinh}(\frac{X - \lambda}{\xi})$，其中 $\lambda$ 和 $\xi$ 是位置和尺度参数（采用鲁棒的 Median 和 MAD），而 $\delta$ 和 $\varepsilon$ 是控制尾部厚度和偏度的形状参数。
    *   **退化问题诊断**：研究发现，由于高容量骨干网络可以补偿输入的单调重参数化，且损失函数对形状参数的局部梯度不敏感，联合训练会导致 $\delta \to \infty$（退化回线性 $Z$-score）。
    *   **解耦形状优化（Decoupled Shape Optimization）**：
        1.  **初始化**：使用闭合形式的 Slifker–Shapiro 分位数拟合法快速估计初始形状参数。
        2.  **外部搜索**：将形状参数视为超参数，利用贝叶斯优化（基于 Optuna 的 TPE 或 GP 采样器）在验证集上寻找最优配置。
        3.  **内部训练**：在固定的形状参数下，按常规方式训练骨干网络权重。

### 3. 实验设计
*   **数据集**：涵盖 5 个真实世界基准数据集：Exchange, ETTh1, ETTh2, ETTm1, ETTm2。
*   **骨干网络（Backbones）**：选取了 6 种代表性模型：Informer, PatchTST, iTransformer, DLinear, TimesNet, FEDformer。
*   **预测场景**：设置了 3 种预测步长（Horizon）：96, 336, 720。
*   **对比基准（Benchmarks）**：
    *   无归一化（None）
    *   线性归一化：RevIN, SAN, Dish-TS, DeStat。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号（如 A100 或 V100）及具体的训练总时长。
*   **实验规模推算**：文中提到共进行了 **90 种配置**（6 骨干 × 5 数据集 × 3 步长），每种配置包含 60 次贝叶斯优化试验，且最终结果基于 3 个随机种子取平均，总计进行了 **1,620 次完整训练运行**。这表明该研究具有相当大的计算工作量。

### 5. 实验数量与充分性
*   **实验充分性**：实验设计非常全面，覆盖了从传统的线性模型（DLinear）到最新的 Transformer 变体（PatchTST, iTransformer）。
*   **客观性与公平性**：
    *   采用了 **Paired Wilcoxon 符号秩检验**，在 $p < 10^{-14}$ 的水平上证明了 NoRIN 的显著性。
    *   进行了详细的**消融实验**（验证 $\delta$ 和 $\varepsilon$ 的必要性）和**灵敏度分析**（验证超参数鲁棒性、种子方差及 HPO 的可重复性）。
    *   通过 121 点网格搜索展示了损失平面的平滑性，证明了最优解的稳定性。

### 6. 主要结论与发现
*   **性能领先**：NoRIN 在几乎所有配置下均显著优于 RevIN 等线性基准。
*   **骨干网络依赖性**：不同的模型对分布形状有显著不同的偏好。例如，基于注意力的 Informer 偏好较大的 $\delta$（较弱的压缩），而 PatchTST 和 DLinear 则偏好极小的 $\delta$（强力的重尾压缩）。
*   **突破线性天花板**：实验证明，最优形状参数系统性地远离线性极限，证明了非线性重塑分布对提升预测精度的必要性。

### 7. 优点
*   **理论深刻**：首次识别并命名了非线性归一化中的“退化问题”，解释了为何此类显而易见的方法此前难以成功。
*   **即插即用**：NoRIN 作为一个模块，可以无缝集成到任何现有的预测骨干网络中，无需修改模型内部结构。
*   **鲁棒性强**：通过引入中位数（Median）和绝对离差（MAD）代替均值和方差，增强了对异常值的抵抗力。

### 8. 不足与局限
*   **计算开销**：虽然解耦优化解决了训练问题，但引入了额外的外部超参数搜索环节（如 60 次 BO 试验），增加了离线训练的总耗时。
*   **搜索空间限制**：部分实验观察到参数触及搜索边界（如 $\varepsilon = \pm 1$），暗示在某些极端数据集上可能需要更宽的搜索范围。
*   **覆盖范围**：实验主要集中在长程预测基准上，尚未在极度非平稳或事件驱动的稀疏序列（如金融高频数据、交通突发事件）上进行验证。

（完）
