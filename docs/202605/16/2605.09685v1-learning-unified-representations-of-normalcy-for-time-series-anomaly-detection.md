---
title: Learning Unified Representations of Normalcy for Time Series Anomaly Detection
title_zh: 学习用于时间序列异常检测的统一正常性表示
authors: "Prithul Sarker, Sushmita Sarker, Nicholas G. Murray, Alireza Tavakkoli"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09685v1"
tags: ["query:ts-fm"]
score: 9.0
evidence: 使用生成模型进行多变量时间序列异常检测
tldr: 针对多变量时间序列异常检测中难以学习鲁棒正常数据分布的问题，本文提出了一种统一的无监督异常检测框架（U2AD）。该方法利用基于分数的生成模型，结合时间依赖分数网络和统一训练目标，同时捕捉局部和全局时间上下文，并通过常微分方程求解器进行确定性重构。实验证明，该方法在检测精度和预警及时性上均显著优于现有最先进方法。
selection_source: fresh_fetch
motivation: 现有的无监督方法难以在缺乏先验知识的情况下，学习到能与异常模式明确区分的鲁棒正常数据分布。
method: 提出U2AD框架，采用基于分数的生成模型和时间依赖分数网络，结合局部与全局上下文来刻画正常数据的流形。
result: 实验表明U2AD在检测准确率上超越了现有SOTA方法，并能显著提前识别出异常的发生。
conclusion: 通过统一的生成式建模和确定性重构，该研究为多变量时间序列异常检测提供了一种高效且精准的解决方案。
---

## 摘要
无监督异常检测的核心挑战是在没有其特征先验知识的情况下识别异常模式。虽然现有方法已经解决了该问题的某些方面，但它们通常难以学习到一种能够与异常模式区分开的鲁棒正常数据分布表示。在本文中，我们提出了一种名为统一无监督异常检测（$\text{U}^2\text{AD}$）的新型框架，全面解决了多变量时间序列中的异常检测问题。我们的方法利用基于分数的生成建模来学习正常样本的底层数据分布。我们引入了一种新型的时间依赖分数网络和统一的训练目标，在考虑局部和全局时间上下文的同时，共同描绘出正常数据的流形。随后，通过使用常微分方程求解器的确定性采样过程进行重构。我们广泛的实验评估表明，$\text{U}^2\text{AD}$ 不仅在检测准确性上优于当前最先进的方法，而且能在异常发生的显著早期阶段识别出它们。

## Abstract
The core challenge in unsupervised anomaly detection is identifying abnormal patterns without prior knowledge of their characteristics. While existing methods have addressed aspects of this problem, they often struggle to learn a robust representation of the normal data distribution that is distinct from anomalous patterns. In this paper, we present a novel framework, Unified Unsupervised Anomaly Detection ($\text{U}^2\text{AD}$), that comprehensively addresses anomaly detection in multivariate time series. Our approach learns the underlying data distribution of normal samples by utilizing score-based generative modeling. We introduce a novel time-dependent score network and a unified training objective that together delineate the manifold of normal data while considering both local and global temporal contexts. Reconstruction is then performed via a deterministic sampling process using an ordinary differential equation solver. Our extensive experimental evaluations demonstrate that $\text{U}^2\text{AD}$ not only outperforms current state-of-the-art methods in detection accuracy but also identifies anomalies at significantly earlier stages of their occurrence.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **$\text{U}^2\text{AD}$（Unified Unsupervised Anomaly Detection）** 的新型多变量时间序列异常检测框架。以下是对该论文的结构化深入分析：

### 1. 核心问题与整体含义
*   **研究动机**：无监督异常检测的核心在于构建精确的“正常性（Normalcy）”模型。现有方法通常依赖单一原则（如重构误差、密度估计或边界划分），难以在包含未标记异常的训练数据中学习到鲁棒的正常分布表示。
*   **核心挑战**：如何有效地将正常数据的复杂高维结构与微妙的异常偏差区分开，并解决现有生成模型（如GAN）训练不稳定或模式坍塌的问题。
*   **整体含义**：论文提出通过**基于分数的生成模型（Score-based Generative Modeling）**来统一密度、边界、重构和上下文学习，从而构建一个更全面的正常性表征。

### 2. 方法论
*   **核心思想**：利用分数匹配（Score Matching）学习正常数据对数概率密度的梯度场（分数函数）。在推理阶段，通过确定性常微分方程（ODE）将输入数据向学习到的“正常流形”进行重构，异常点因偏离该流形而产生更高的重构误差和分数空间距离。
*   **关键技术细节**：
    *   **数据扰动（前向过程）**：使用方差保持（VP）随机微分方程（SDE）向原始数据添加连续的高斯噪声。
    *   **双路径分数网络（Dual-Pathway Score Network）**：
        *   **全局路径**：利用多头自注意力机制捕捉长距离时间相关性。
        *   **局部路径**：利用基于余弦相似度的机制提取细粒度的局部形状模式。
    *   **统一训练目标（$L_{total}$）**：结合了四种互补的损失函数：
        1.  **去噪分数匹配（DSM）**：学习数据分布的梯度场。
        2.  **体积最小化（VM）**：借鉴单类分类思想，确保正常数据在分数空间中紧凑分布。
        3.  **上下文信息增益（Γ）**：通过对称KL散度衡量局部与全局特征的一致性。
        4.  **重构损失（Rec）**：确保确定性采样能精确还原正常部分。
    *   **确定性重构（逆向过程）**：使用概率流ODE求解器（如RK45）消除随机性，实现稳定的样本生成。
    *   **异常评分机制**：综合重构误差、分数空间距离以及上下文一致性权重。

### 3. 实验设计
*   **数据集**：使用了四个主流的多变量时间序列基准数据集：**MSL**（火星科学实验室）、**SMAP**（土壤水分主动被动卫星）、**SWaT**（安全水处理系统）和**PSM**（汇总服务器指标）。
*   **对比方法（Baselines）**：共对比了12种最先进的方法，涵盖：
    *   重构类：InterFusion, OmniAnomaly, BeatGAN。
    *   边界类：DeepSVDD。
    *   密度类：DAGMM。
    *   传统类：Deep Isolation Forest (DIF)。
    *   注意力类：AnomalyTrans, TranAD, DCdetector, SARAD。
    *   扩散/生成类：IMdiffusion, TimesNet。
*   **评估指标**：除了传统的F1-score（点调整后）、AUC-ROC、AUC-PR，还引入了更稳健的**VUS（表面下体积）**指标，以及衡量预警及时性的**ADD（平均检测延迟）**和新提出的**NRD（归一化响应延迟）**。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA A30 (24GB)** GPU 上运行。
*   **软件框架**：基于 PyTorch 实现。
*   **训练细节**：Batch size 为 256，使用 Adam 优化器，初始学习率为 $10^{-4}$，采用了早停机制和指数学习率调度。ODE 求解器使用了 `scipy.integrate.solve_ivp` 中的 RK45 方法。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在4个数据集上进行了完整的性能对比。
    *   进行了包含15种不同损失函数组合的**消融实验**，验证了四个训练目标的必要性。
    *   进行了**参数敏感性分析**（如窗口大小 $N$、网络层数 $K$、损失权重 $\lambda_3$）。
    *   测试了在**极少量训练数据（20%）**下的鲁棒性。
*   **充分性评价**：实验设计非常充分且客观。通过引入 VUS 和 NRD 指标，避开了传统点调整 F1-score 可能存在的性能高估问题。每组实验运行多次并取平均值，保证了结果的统计可靠性。

### 6. 主要结论与发现
*   **性能领先**：$\text{U}^2\text{AD}$ 在所有数据集上的平均 F1-score 达到 96.56%，显著优于之前的 SOTA 模型（如 AnomalyTrans 的 94.61%）。
*   **极速预警**：该模型在异常发生的早期阶段（平均仅占异常总时长的 **38.52%** 时）就能识别出异常，远快于 TimesNet 等模型。
*   **鲁棒性强**：即使在训练数据受限（仅20%）的情况下，其表现依然稳定，证明了分数匹配在学习底层分布方面的强大能力。

### 7. 优点与亮点
*   **多维统一**：首次将分数匹配（生成式）、体积最小化（边界式）和上下文学习（结构式）统一在一个框架内。
*   **早期检测能力**：通过 NRD 指标量化并证明了其在工业预警中极具价值的快速响应能力。
*   **确定性重构**：利用 ODE 求解器替代随机扩散采样，解决了生成模型在异常检测中因随机性导致的误报问题。

### 8. 不足与局限
*   **计算开销**：虽然使用了确定性 ODE 求解器，但相比于简单的自编码器，其推理过程（数值积分）可能涉及多次网络前向计算，对实时性要求极高的场景可能存在延迟挑战。
*   **超参数依赖**：模型包含多个损失项权重（$\lambda_1, \lambda_2, \lambda_3$）和 SDE 参数（$\beta_{min}, \beta_{max}$），在不同特性的数据集上可能需要精细调优。
*   **应用限制**：论文主要在标准基准数据集上验证，对于极度非平稳或具有强烈概念漂移（Concept Drift）的实时流数据，其表现尚待进一步验证。

（完）
