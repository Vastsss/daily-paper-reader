---
title: "NPMixer: Hierarchical Neighboring Patch Mixing for Time Series Forecasting"
title_zh: NPMixer：用于时间序列预测的分层相邻补丁混合
authors: "Jung Min Choi, Vijaya Krishna Yalavarthi, Lars Schmidt-Thieme"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07476v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 多变量时间序列预测架构
tldr: NPMixer 是一种用于多变量时间序列预测的分层架构，旨在解决局部动态和全局依赖建模的难题。该模型引入可学习平稳小波变换（LSWT）自适应分解趋势与细节，并利用邻域混合块（Neighboring Mixer Block）通过分层 MLP 处理非重叠 Patch，从而捕捉多尺度时间模式。实验证明其在多个基准数据集上显著优于现有 SOTA 模型。
selection_source: fresh_fetch
motivation: 旨在解决多变量时间序列预测中局部时间动态复杂以及跨变量全局依赖难以捕捉的问题。
method: 采用可学习小波变换进行信号分解，并结合分层 MLP 结构在 Patch 级别提取多尺度时间特征。
result: 在 7 个基准数据集的 28 种实验设置中，NPMixer 在 20 种情况下取得了最优的 MSE 性能。
conclusion: NPMixer 通过自适应分解和分层 Patch 混合机制，为复杂时间序列预测提供了一种高效且性能卓越的解决方案。
---

## 摘要
由于局部时间动态的复杂性以及多个变量之间的全局依赖关系，多元时间序列预测仍然是一项挑战。在本文中，我们提出了 NPMixer，这是一种分层架构，其特点是具有可学习的平稳小波变换（Learnable Stationary Wavelet Transform），能够以数据依赖的方式自适应地学习滤波器系数，从而将信号分解为趋势和细节分量。我们的框架引入了一个相邻混合器块（Neighboring Mixer Block），通过在一系列非重叠补丁（patches）上运行的分层 MLP 层来捕捉局部时间动态。具体而言，该混合器块利用 MLP 学习这些补丁内部和之间的时序模式，从而扩大感受野以捕捉多尺度依赖关系。通道混合编码器（Channel-Mixing Encoder）被应用于高频分量，以学习通道间的相关性，同时保持底层全局趋势的稳定性。在七个基准数据集上的广泛实验表明，NPMixer 一贯优于现有最先进的模型，在 28 个评估的 MSE 实验设置中有 20 个（71.4%）取得了更好的性能。

## Abstract
Multivariate time series forecasting remains a challenge due to the complexity of local temporal dynamics and global dependencies across multiple variables.   In this paper, we propose \textbf{N}eighboring \textbf{P}atching \textbf{Mixer} (\textbf{NPMixer}), a hierarchical architecture featuring a Learnable Stationary Wavelet Transform that adaptively learns filter coefficients to decompose signals into trend and detail components in a data-dependent manner.   Our framework introduces a Neighboring Mixer Block that captures local temporal dynamics through a series of hierarchical MLP layers operating on non-overlapping patches.   Specifically, the mixer block utilizes MLPs to learn temporal patterns within and across these patches, expanding the receptive field to capture multi-scale dependencies.   A Channel-Mixing Encoder is applied to high-frequency components to learn channel correlations while preserving the stability of the underlying global trend.   Extensive experiments on seven benchmark datasets demonstrate that NPMixer consistently outperforms state-of-the-art models, achieving better performance in 20 out of 28 ($71.4\%$) evaluated experimental setups for MSE.

---

## 论文详细总结（自动生成）

这是一份关于论文《NPMixer: Hierarchical Neighboring Patch Mixing for Time Series Forecasting》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
多变量时间序列（MTS）预测的难点在于如何同时捕捉复杂的**局部时间动态**和变量间的**全局依赖关系**。
*   **研究动机**：现有的 Patch（补丁）技术（如 PatchTST）多采用重叠滑动窗口，这虽然能捕捉局部语义，但带来了巨大的计算开销和信息冗余。
*   **核心问题**：如何在降低计算冗余的同时，通过一种高效的分层机制，自适应地从不同频率尺度提取时间序列的趋势与细节特征。

### 2. 核心方法论
NPMixer 提出了一种简单而有效的架构，包含三个核心组件：
*   **可学习平稳小波变换 (LSWT)**：
    *   不同于固定基函数的传统小波，LSWT 将滤波器系数参数化为可学习张量，使信号分解（趋势分量 $A$ 和细节分量 $D$）能根据数据特征自适应优化。
    *   采用平稳小波变换（SWT）省略了下采样步骤，保持了平移不变性和原始序列长度。
*   **通道混合编码器 (Channel-Mixing Encoder)**：
    *   仅应用于高频细节分量。借鉴 iTransformer 的思想，将每个变量的整个序列视为一个 Token，利用自注意力机制建模变量间的相关性。
*   **相邻混合器块 (Neighboring Mixer Block)**：
    *   **非重叠补丁**：将序列划分为不重叠的 Patch，降低冗余。
    *   **分层配对策略**：采用“分组-混合-溶解”（Group-Mix-Dissolve）策略。在每一层，相邻的 Patch 或块被配对并通过 MLP 处理，感受野随层数呈指数级扩大（$S_k = 2^{k-1}P$）。
    *   **非对称更新**：设计了具有方向偏置的残差连接，初始块专注于未来导向信号，后续块则优先考虑历史连续性。

### 3. 实验设计
*   **数据集**：使用了 7 个标准基准数据集：ETTh1, ETTh2, ETTm1, ETTm2, Weather, Electricity, Traffic。
*   **预测场景**：长程预测，预测步长 $H \in \{96, 192, 336, 720\}$。
*   **对比方法 (Baselines)**：包括 SimpleTM, TimeXer, CrossGNN, iTransformer, PatchTST, TimeMixer 等 6 种最新的 SOTA 模型。
*   **评估指标**：均方误差 (MSE) 和平均绝对误差 (MAE)。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **优化工具**：提到了使用 **Optuna** 框架进行自动超参数搜索，并使用 TPE 采样器确保实验的可重复性。
*   **效率分析**：在附录中对比了推理速度，指出 NPMixer 虽然参数量比 SimpleTM 多，但由于非重叠 Patch 和分层 MLP 结构更利于 GPU 并行化，其训练速度比 SimpleTM 快约 2.7 倍。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集、4 个预测长度下进行了测试，共计 28 组核心实验设置。
*   **消融实验**：针对 LSWT、可学习性、相邻混合器、通道混合编码器分别做了移除测试，验证了各组件的必要性。
*   **回看窗口分析**：测试了不同输入长度 $L \in \{96, 192, 336, 512\}$ 对性能的影响。
*   **充分性评价**：实验设计较为全面，涵盖了不同领域的数据和多种消融场景，每组实验均取 3 次随机种子的平均值，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：NPMixer 在 20/28 (71.4%) 的 MSE 测试中取得了最优性能，尤其在 ETT 系列数据集上提升显著。
*   **分层机制有效性**：消融实验证明，递归的 Patch 聚合比直接处理所有 Patch 效果更好，能更系统地捕捉局部到全局的依赖。
*   **频率域优势**：在频率域进行多尺度学习对于捕捉精确的时间动态至关重要。
*   **长窗口潜力**：NPMixer 在处理长回看窗口（如 $L=512$）时表现出更强的鲁棒性，能有效利用更丰富的历史信息。

### 7. 优点与亮点
*   **高效性**：通过非重叠 Patch 减少了计算开销，且分层 MLP 结构比 Transformer 更易于并行加速。
*   **自适应分解**：可学习的小波变换允许模型根据任务目标（最小化预测误差）动态调整频谱分析基准。
*   **结构创新**：引入了非对称更新策略，符合时间序列预测的因果逻辑。

### 8. 不足与局限
*   **长度限制**：分层聚合依赖于指数级块大小（$2^{k-1}$），这对输入长度有一定约束，不满足时需要补零（Zero-padding），可能引入边缘分布偏移。
*   **静态滤波器**：虽然小波滤波器在训练时可学习，但在推理阶段是静态的，可能难以应对非平稳数据的概念漂移。
*   **超参敏感**：模型性能对初始 Patch 大小 $P$ 较为敏感，若 $P$ 与数据的潜在周期不匹配，可能影响局部语义提取。

（完）
