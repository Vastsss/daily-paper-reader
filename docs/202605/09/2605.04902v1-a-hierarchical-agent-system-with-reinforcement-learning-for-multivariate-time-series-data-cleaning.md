---
title: A Hierarchical Agent System with Reinforcement Learning for Multivariate Time Series Data Cleaning
title_zh: 一种用于多变量时间序列数据清洗的强化学习层次化智能体系统
authors: "Yuhan Shi, Yuanyuan Yao, Lu Chen, Mourad Khayati, Tianyi Li"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04902v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 用于多变量时间序列数据清洗的强化学习
tldr: 针对多变量时间序列中多种质量问题共存且缺乏真值参考的挑战，本文提出了一种基于强化学习的分层智能体系统。该系统将清洗过程建模为联合优化问题，通过高层智能体确定处理顺序，底层智能体选择最优清洗模型，并结合双阶段奖励机制在无真值情况下优化清洗路径。实验证明，该方法在提升数据质量和下游任务表现上显著优于现有技术。
selection_source: fresh_fetch
motivation: 现有的多变量时间序列清洗方法通常只能处理单一质量问题，且高度依赖难以获取的真值数据或领域规则。
method: 提出一种分层强化学习架构，通过高层智能体优化处理顺序，底层智能体选择清洗模型，并利用双阶段奖励机制引导优化。
result: "实验结果显示，该系统在数据清洗质量上提升了高达96%，在下游任务性能上提升了27%。"
conclusion: 该分层智能体系统为复杂多变量时间序列的自动化清洗提供了一种无需真值参考的高效解决方案。
---

## 摘要
多变量时间序列（MTS）经常受到并发质量问题的影响，例如缺失值、离群值和违反约束，这些问题严重损害了下游分析。现有的清洗方法仅能修复有限的一组此类问题，因此不适用于多种质量问题同时出现的场景。此外，这些方法通常依赖于地面真值（ground truth）数据或特定领域的规则，而这两者在实际应用中往往难以获取。在本文中，我们介绍了 \sys，这是一个旨在清洗 MTS 中多种数据质量问题的强化学习智能体系统。我们将清洗过程建模为一个联合优化问题，同时处理质量问题的处理顺序和清洗模型的选择，从而在庞大的候选清洗流水线空间中进行高效搜索。我们的框架依赖于层次化智能体架构，其中高层智能体决定处理数据质量问题的顺序，而底层智能体则为每个问题识别最合适的清洗方法。为了引导智能体找到最优清洗流水线，我们提出了一种双阶段奖励机制，将上游（清洗）和下游性能耦合，从而在不依赖地面真值的情况下实现有效优化。实验结果表明，\sys 一贯优于现有方法，在数据清洗质量上提升高达 96%，在下游性能上提升高达 27%。

## Abstract
Multivariate time series (MTS) are frequently affected by co-occurring quality issues, such as missing values, outliers, and constraint violations, which significantly undermine downstream analytics. Existing cleaning approaches fix only a limited set of such issues, making them ill-suited for scenarios where multiple quality problems arise simultaneously. Furthermore, these methods commonly depend on the availability of ground truth data or domain-specific rules, both of which are rarely accessible in real-world applications.   In this paper, we introduce \sys, an agent system with reinforcement learning designed to clean multiple data quality issues in MTS. We cast the cleaning process as a joint optimization problem that simultaneously handles quality issue order and cleaning model selection, allowing efficient navigation of the large space of possible cleaning pipelines. Our framework relies on a hierarchical agent architecture, where a high-level agent determines the order in which data quality issues should be processed, while a low-level agent identifies the most suitable cleaning method for each issue. To guide the agent toward an optimal cleaning pipeline, we propose a dual-stage reward mechanism that couples upstream (cleaning) and downstream performance, enabling effective optimization without relying on ground truth. Our experimental results show that \sys consistently outperforms existing methods, achieving up to 96\% improvement in data cleaning quality and 27\% improvement in downstream performance.

---

## 论文详细总结（自动生成）

以下是对论文《AegisTS: A Hierarchical Agent System with Reinforcement Learning for Multivariate Time Series Data Cleaning》的深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：多变量时间序列（MTS）在物联网和传感器网络中广泛存在，但常伴随缺失值、离群值、重复值和违反约束等多种质量问题。
*   **核心挑战**：
    1.  **并发性与顺序性**：现有工具通常只针对单一问题，而多种错误共存时，清洗顺序（如先补缺失值还是先去离群值）会显著影响最终效果。
    2.  **缺乏真值（Ground Truth）**：实际场景中很难获得干净的参考数据来评估清洗效果。
    3.  **搜索空间巨大**：清洗算子选择与执行顺序的组合呈指数级增长，难以通过暴力搜索找到最优流水线。
*   **研究目的**：开发一个自动化的、无需真值参考的智能系统，能够针对特定数据集生成最优的清洗流水线。

### 2. 方法论
AegisTS 将 MTS 清洗建模为一个**层次化强化学习（HRL）**任务：
*   **层次化智能体架构**：
    *   **高层智能体（High-Level Agent）**：负责战略决策，决定当前应处理哪种类型的质量问题（缺失、离群、约束违反或终止）。
    *   **底层智能体（Low-Level Agent）**：负责战术执行，在确定的问题类型下，从算子库中选择最合适的具体清洗算法。
*   **关键技术细节**：
    *   **数据质量检测器**：包含约束挖掘器（利用 MAD 统计量和多项式建模自动从脏数据中提取时序和变量间约束）和异常检测模型选择器（FMMS）。
    *   **双阶段奖励机制**：
        1.  **密集奖励（Dense Reward）**：由轻量级代理模型（如 DLinear）提供，结合数据平滑度、修改距离和局部质量提升，给予每步操作即时反馈。
        2.  **稀疏奖励（Sparse Reward）**：在流水线完成后，由复杂的下游目标模型（如 LSTM、InceptionTime）提供最终性能增益反馈。
    *   **搜索剪枝**：引入依赖感知剪枝（如发现空值时强制优先填充），将搜索复杂度从指数级降低到线性。

### 3. 实验设计
*   **数据集**：使用了 4 个真实世界数据集，涵盖电力（ETTh1）、重型设备监控（IDF_OilTemp）、手语识别（Libras）和手写识别（Handwriting）。
*   **下游任务**：预测（Forecasting）、分类（Classification）、聚类（Clustering）。
*   **对比基准（Baselines）**：
    *   **EDITOR**：最新的多分辨率 MTS 清洗方法。
    *   **Clean4TSDB**：基于约束挖掘和错误画像的集成系统。
    *   **DiffPrep**：最先进的可微分表格数据预处理流水线生成方法（经适配用于时序）。
    *   **Sampling**：随机采样算子并排列顺序的暴力策略。
*   **评估指标**：上游指标（F1、NMSE、RRA）和下游任务指标（$\Delta$Perf）。

### 4. 资源与算力
*   **硬件环境**：Linux 工作站，配置 Intel Core i9-10900K CPU，128 GB RAM。
*   **GPU 使用**：单块 **NVIDIA RTX 3090 (24 GB VRAM)**。
*   **训练时长**：文中提供了不同数据集的运行时间（见表 5），例如在 ETTh1 上训练加推理总耗时约 2016 秒，在更复杂的 Handwriting 数据集上约 21674 秒（约 6 小时）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集、3 种任务类型上进行了全面测试。
*   **消融实验**：对比了“单层智能体”、“无下游任务引导”、“无质量指标引导”等变体，验证了层次化架构和双阶段奖励的必要性。
*   **泛化性测试**：进行了**跨数据集迁移实验**（如在 ETTh1 上训练的智能体直接用于清洗 IDF_OilTemp），证明了系统学习到的是通用的清洗策略而非简单的过拟合。
*   **参数敏感性**：对奖励函数中的多个超参数（$\lambda, \mu$）进行了详细的敏感性分析。
*   **评价**：实验设计严谨，对比方法具有代表性，覆盖了从数据质量到下游应用的全维度，客观性较强。

### 6. 主要结论与发现
*   **性能领先**：AegisTS 在数据清洗质量（NMSE）上比次优方法提升高达 96%，下游任务性能提升高达 27%。
*   **顺序的重要性**：实验证实了清洗顺序对结果有显著影响，AegisTS 能够自动发现如“去重 -> 填充 -> 修复离群 -> 修复约束”的最优路径。
*   **无需真值可行性**：通过结合轻量级代理模型和内在约束挖掘，系统可以在完全没有干净数据参考的情况下实现高效优化。
*   **效率优势**：层次化架构比单层强化学习智能体快 1.9 到 2.8 倍。

### 7. 优点
*   **首创性**：这是首个针对 MTS 清洗的层次化强化学习智能体系统，解决了多类型错误并发处理的难题。
*   **端到端优化**：将数据清洗与下游任务性能直接挂钩，确保清洗后的数据真正“有用”。
*   **鲁棒的约束挖掘**：能够在脏数据中提取可靠的物理和统计约束，降低了对领域专家知识的依赖。
*   **可扩展性**：算子库是可扩展的，可以轻松集成新的清洗算法。

### 8. 不足与局限
*   **训练开销**：虽然推理很快，但初始训练（强化学习的探索过程）仍需数小时，对于实时性要求极高的流式清洗场景可能存在延迟。
*   **算子依赖**：系统的上限取决于底层算子库的质量，如果库中缺乏针对特定领域错误的算子，系统无法凭空修复。
*   **代理模型偏差**：双阶段奖励依赖于轻量级代理模型，如果代理模型与最终复杂模型的性能趋势不一致，可能会引导智能体走向次优解。

（完）
