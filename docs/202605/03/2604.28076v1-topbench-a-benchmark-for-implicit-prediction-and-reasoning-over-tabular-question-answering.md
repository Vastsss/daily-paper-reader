---
title: "TopBench: A Benchmark for Implicit Prediction and Reasoning over Tabular Question Answering"
title_zh: TopBench：表格问答中隐式预测与推理的基准测试
authors: "An-Yang Ji, Jun-Peng Jiang, De-Chuan Zhan, Han-Jia Ye"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.28076v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: LLM对历史模式的预测推理
tldr: 针对现有表格问答主要局限于信息提取而忽视隐式预测的问题，本文提出了TopBench基准测试。该基准包含779个样本，涵盖单点预测、决策制定、因果效应分析和复杂过滤四类子任务，旨在评估大模型在识别潜在意图及进行可靠预测推理方面的能力。研究发现，当前模型在识别预测意图上存在困难，且提升预测精度需更复杂的建模与推理能力。
selection_source: fresh_fetch
motivation: 现实世界中的表格查询往往具有隐式预测性，需要从历史模式中推断未观察到的答案，而现有基准主要关注简单的信息检索。
method: 提出了TopBench基准，包含779个样本和四种预测子任务，并对多种大模型在文本和智能体工作流下进行了评估。
result: 实验表明当前模型常将预测任务误认为简单的查找任务，且意图识别的准确性是实现预测行为的前提。
conclusion: 提升表格预测任务的性能需要模型具备更强的意图消歧能力以及更高级的建模与推理技术。
---

## 摘要
大语言模型（LLMs）推动了表格问答（Table Question Answering）的发展，其中大多数查询可以通过提取信息或简单聚合来回答。然而，现实世界中一类常见的查询具有隐式预测性，需要从历史模式中推断未观测到的答案，而不仅仅是简单的检索。这些查询带来了两个挑战：识别潜在意图以及在海量表格上进行可靠的预测性推理。为了评估 LLMs 在此类带有隐式预测任务的表格问答中的表现，我们推出了 TopBench，这是一个包含 779 个样本的基准测试，涵盖了从单点预测到决策制定、干预效果分析（treatment effect analysis）以及复杂过滤的四个子任务，要求模型生成的输出涵盖推理文本和结构化表格。我们在基于文本和基于代理（agentic）的工作流下评估了多种模型。实验表明，当前模型通常在意图识别方面面临困难，往往默认仅进行查找。深入分析发现，准确的意图消歧是引导这些预测行为的前提。此外，提升预测精度的上限需要集成更复杂的建模或推理能力。

## Abstract
Large Language Models (LLMs) have advanced Table Question Answering, where most queries can be answered by extracting information or simple aggregation. However, a common class of real-world queries is implicitly predictive, requiring the inference of unobserved answers from historical patterns rather than mere retrieval. These queries introduce two challenges: recognizing latent intent and reliable predictive reasoning over massive tables. To assess LLMs in such Tabular questiOn answering with implicit Prediction tasks, we introduce TopBench, a benchmark consisting of 779 samples across four sub-tasks, ranging from single-point prediction to decision making, treatment effect analysis, and complex filtering, requiring models to generate outputs spanning reasoning text and structured tables. We evaluate diverse models under both text-based and agentic workflows. Experiments reveal that current models often struggle with intent recognition, defaulting to just lookups. Deeper analysis identifies that accurate intent disambiguation serves as the prerequisite for leading these predictive behaviors. Furthermore, elevating the upper bound of prediction precision requires the integration of more sophisticated modeling or reasoning capabilities.

---

## 论文详细总结（自动生成）

这是一份关于论文《TopBench: A Benchmark for Implicit Prediction and Reasoning over Tabular Question Answering》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
传统的表格问答（TableQA）主要关注**显式事实的检索**（如“某人的保费是多少？”）或**简单聚合**（如“平均保费是多少？”）。然而，现实场景中大量的查询具有**隐式预测性**（如“根据我的个人资料，我预期的保费是多少？”）。
这类任务要求模型不仅要理解表格结构，还要：
*   **识别潜在意图**：将非结构化的自然语言描述抽象为表格特征集。
*   **执行预测推理**：基于历史数据模式推断未观测到的结果。
目前缺乏专门评估 LLM 在表格数据上进行隐式预测能力的基准测试，这正是本文的研究动机。

### 2. 论文提出的方法论
论文提出了 **TopBench** 基准，其核心思想是将隐式预测 TableQA 形式化为一个两阶段推理问题：意图抽象（提取特征 $x'$）和预测推理（学习映射 $f: x \to y$ 并估计 $y'$）。

**核心技术细节：**
*   **任务分类体系**：
    1.  **单点预测**：估计特定配置下的未知数值。
    2.  **决策制定**：比较多个配置的预测结果以选择最优方案。
    3.  **干预效果分析**：评估变量变化（$\Delta x$）如何影响结果（$\Delta y$）。
    4.  **排名与过滤**：在大规模候选名单中筛选符合预测标准的子集。
*   **评估流水线**：
    *   **自然语言推理评估**：使用 LLM-as-a-Judge 提取预测值和区间，并进行逻辑评分。
    *   **幻觉校验机制**：通过文本标准化、模糊匹配和自然语言推理（NLI）确保提取的数值严格源自模型输出。
    *   **回归准确率公式**：$Acc_{reg} = (\lambda_1 \cdot S_{point} + \lambda_2 \cdot S_{iou}) \times P_{width}$，综合考虑了点估计精度、区间覆盖率及宽度惩罚。

### 3. 实验设计
*   **数据集/场景**：包含 779 个高质量样本，源自 35 个真实的 Kaggle 表格，涵盖医疗、金融和日常咨询领域。表格规模跨度大（从 <1k 行到 >6M 行）。
*   **对比方法**：评估了 9 种代表性模型，分为三类：
    *   **通用 LLM**：GPT-5.2, Claude Sonnet 4.5, Gemini 3 Flash, DeepSeek-V3.2-Instruct, Qwen3-Instruct。
    *   **推理增强模型**：DeepSeek-V3.2-Thinking, Qwen3-Thinking。
    *   **表格专家模型**：TableLLM (8B/13B)。
*   **推理范式**：对比了“基于文本的推理”（直接输入序列化表格）和“智能体工作流”（通过 ReAct 循环执行 Python 代码）。

### 4. 资源与算力
论文**未明确说明**具体的训练算力（如 GPU 型号、数量或训练时长）。这主要是因为该研究侧重于对现有预训练模型进行 Benchmark 评估，而非从头训练新模型。实验中的智能体工作流是在 Docker 沙盒环境中运行 Python 代码。

### 5. 实验数量与充分性
*   **实验规模**：针对 9 种模型在 4 大类任务上进行了全面测试，并区分了文本模式和 Agent 模式。
*   **消融实验**：
    *   **语义信息消融**：测试提供目标列、任务类型和特征描述对性能的影响。
    *   **预测建模能力消融**：对比了模型自发建模与使用专业集成学习器（如 XGBoost, TabPFN）的差距。
*   **客观性与公平性**：采用了混合验证流程（LLM 审计 + 人工复核），并针对长文本截断策略进行了对比实验，确保了评估的严谨性。

### 6. 论文的主要结论与发现
*   **性能瓶颈**：当前 LLM 在隐式预测任务上表现脆弱，大多数得分低于 0.60，远逊于事实检索能力。
*   **意图识别障碍**：模型常将预测任务误判为检索任务。例如，Qwen3 在 Agent 模式下常尝试查找不存在的行而非训练预测模型。
*   **Agent 模式的双刃剑**：代码执行能显著提升复杂算术任务（如干预效果分析）的准确性，但若意图识别错误，性能反而会下降。
*   **推理模型的局限**：推理增强模型（如 Qwen3-Thinking）在处理长表格输入时容易陷入循环逻辑，导致性能不稳定。
*   **专家模型的缺失**：现有的表格专家模型因过度拟合显式提取任务，在处理隐式预测意图时几乎完全失效。

### 7. 优点：方法或实验设计亮点
*   **填补空白**：首次正式定义并量化了表格问答中的“隐式预测”任务，贴近工业界真实需求。
*   **高质量数据合成**：采用逻辑驱动采样（如硬负采样）和双视角提示（用户 vs 数据持有者），避免了机械化的合成文本。
*   **严谨的评估体系**：引入了幻觉校验流水线，解决了 LLM-as-a-Judge 可能出现的提取错误问题，并设计了科学的回归评分指标。

### 8. 不足与局限
*   **检索简化**：实验假设查询与相关的历史表格已经配对好，但在开放场景中，如何从海量数据库中检索出正确的历史表格仍是挑战。
*   **模型偏差风险**：评估高度依赖 DeepSeek 或 GPT 作为裁判模型，可能存在潜在的偏见。
*   **预测上限受限**：即使意图识别正确，LLM 生成的简单线性回归代码往往难以处理复杂的非线性数据关系，预测精度仍有提升空间。

（完）
