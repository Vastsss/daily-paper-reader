---
title: "LLMs Reading the Rhythms of Daily Life: Aligned Understanding for Behavior Prediction and Generation"
title_zh: LLMs解读日常生活节奏：用于行为预测与生成的对齐理解
authors: "Fanjin Meng, Jingtao Ding, Nian Li, Yizhou Sun, Yong Li"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23578v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 大语言模型用于行为预测和复杂序列建模
tldr: 本研究针对人类日常行为建模中的长尾行为、可解释性及多任务统一等挑战，提出了行为理解对齐（BUA）框架。该框架通过三阶段课程学习，将预训练行为模型的序列嵌入作为对齐锚点，使大语言模型（LLM）能够理解并生成复杂的行为序列。实验证明，BUA在行为预测和生成任务上均显著优于现有方法，为智能助手和推荐系统提供了更具解释性和灵活性的建模方案。
selection_source: fresh_fetch
motivation: 传统行为建模方法在处理长尾分布和可解释性方面存在不足，且LLM与行为数据之间存在显著的模态差异。
method: 提出BUA框架，利用三阶段课程学习和序列嵌入对齐技术，将LLM引入行为建模并支持多轮对话下的预测与生成。
result: 在两个真实世界数据集上的实验表明，BUA在行为预测和生成任务中均显著超越了现有的深度学习和行为预训练模型。
conclusion: BUA框架成功弥合了LLM与行为数据间的模态鸿沟，为复杂人类行为的理解、预测和生成提供了一种高效且灵活的新范式。
---

## 摘要
人类的日常行为表现为受意图、偏好和上下文影响的复杂序列。有效地对这些行为进行建模对于个人助手和推荐引擎等智能系统至关重要。尽管深度学习和行为预训练的最新进展提高了行为预测能力，但关键挑战仍然存在——特别是在处理长尾行为、增强可解释性以及在统一框架内支持多任务方面。大语言模型（LLMs）由于其丰富的语义、强大的可解释性和生成能力，提供了一个充满前景的方向。然而，行为数据与自然语言之间的结构和模态差异限制了 LLMs 的直接应用。为了弥补这一差距，我们提出了行为理解对齐（Behavior Understanding Alignment, BUA），这是一个通过结构化课程学习过程将 LLMs 集成到人类行为建模中的新颖框架。BUA 采用来自预训练行为模型的序列嵌入作为对齐锚点，并通过三阶段课程引导 LLM，同时多轮对话设置引入了预测和生成能力。在两个真实世界数据集上的实验表明，BUA 在这两项任务中都显著优于现有方法，突显了其在将 LLMs 应用于复杂人类行为建模方面的有效性和灵活性。

## Abstract
Human daily behavior unfolds as complex sequences shaped by intentions, preferences, and context. Effectively modeling these behaviors is crucial for intelligent systems such as personal assistants and recommendation engines. While recent advances in deep learning and behavior pre-training have improved behavior prediction, key challenges remain--particularly in handling long-tail behaviors, enhancing interpretability, and supporting multiple tasks within a unified framework. Large language models (LLMs) offer a promising direction due to their semantic richness, strong interpretability, and generative capabilities. However, the structural and modal differences between behavioral data and natural language limit the direct applicability of LLMs.   To address this gap, we propose Behavior Understanding Alignment (BUA), a novel framework that integrates LLMs into human behavior modeling through a structured curriculum learning process. BUA employs sequence embeddings from pretrained behavior models as alignment anchors and guides the LLM through a three-stage curriculum, while a multi-round dialogue setting introduces prediction and generation capabilities. Experiments on two real-world datasets demonstrate that BUA significantly outperforms existing methods in both tasks, highlighting its effectiveness and flexibility in applying LLMs to complex human behavior modeling.

---

## 论文详细总结（自动生成）

这是一份关于论文《LLMs Reading the Rhythms of Daily Life: Aligned Understanding for Behavior Prediction and Generation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
人类日常行为建模（如预测下一动作或生成行为序列）对智能助手和推荐系统至关重要。尽管深度学习和行为预训练模型（如 Life2Vec）取得了进展，但仍面临三大挑战：
*   **长尾行为处理难**：稀疏或新兴行为难以被传统模型捕捉。
*   **缺乏可解释性**：传统模型多为“黑盒”，无法提供人类可理解的推理过程。
*   **任务孤立**：现有模型通常只能执行单一的预测或生成任务，缺乏统一框架。
**研究动机**：利用大语言模型（LLM）丰富的语义知识和生成能力来解决上述问题。然而，行为数据（时间、地点、动作 ID）与自然语言之间存在巨大的**模态差异**，LLM 无法直接理解原始行为序列。

### 2. 方法论：核心思想与关键技术
论文提出了 **BUA（Behavior Understanding Alignment，行为理解对齐）** 框架，核心思想是通过结构化的课程学习，将行为序列特征对齐到 LLM 的语义空间。

*   **序列嵌入对齐**：使用预训练的行为编码器（如 BehaveGPT）提取行为序列的隐藏状态，通过一个轻量级的两层 MLP（多层感知机）将其投影为 LLM 可识别的“行为锚点”嵌入。
*   **三阶段课程学习（Curriculum Learning）**：
    1.  **阶段1：序列级特征提取**。训练模型重建历史序列、总结当前场景并推理未来场景，建立基础的跨模态感知。
    2.  **阶段2：用户级特征理解**。识别关键行为、发现行为模式并总结高层用户特征（如“该用户喜欢在通勤时看新闻”）。
    3.  **阶段3：自我反思优化**。引导模型识别并纠正自身生成的低质量用户画像，提升理解的深度和准确性。
*   **多轮对话与损失平衡**：
    *   采用多轮对话形式，将“理解-预测-生成”串联成推理链。
    *   **损失平衡策略**：针对预测任务输出短（几个 token）、理解任务输出长（上百 token）导致的训练不均衡问题，引入了与答案长度成反比的权重因子，确保模型不会忽略短输出的预测任务。

### 3. 实验设计
*   **数据集**：
    *   **Behavior Dataset**：来自手机日志，包含 2.4 万用户、37 种行为类型、5000 万条事件。
    *   **Tencent Dataset**：来自社交网络和轨迹数据，包含 2000 用户、14 种行为意图。
*   **Benchmark 与对比方法**：
    *   **传统/预训练模型**：SASRec, BehaveGPT。
    *   **LLM 增强/对齐模型**：TallRec, A-LLMRec, CoLLM, LLaRa, PITuning, AlphaFuse。
    *   **生成模型**：SAND, D2A。
    *   **通用模型**：DeepSeek-V3。
*   **评估指标**：预测任务使用加权精准率/召回率及长尾准确率；生成任务使用 BLEU、TVD（总变差距离）和 JSD（JS 散度）。

### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA A100 40G GPU**。
*   **底座模型**：选用 **Qwen2.5-7B**。
*   **训练细节**：采用 LoRA 微调，Batch Size 为 96，训练 3 个 Epoch。
*   **时长**：实验通常在 **8 小时内**完成。

### 5. 实验数量与充分性
论文进行了多维度的充分实验：
*   **主实验**：在两个数据集上验证了预测和生成任务，BUA 均达到 SOTA。
*   **消融实验**：分别验证了三个课程阶段、损失平衡策略以及序列嵌入的必要性。
*   **跨模型增强**：证明 BUA 提取的用户特征可以显著提升其他模型（如 TallRec）的性能。
*   **人类评估**：通过 120 组样本的盲测，证明 BUA 生成的行为解释在合理性和可解释性上接近人类水平。
*   **错误分析与鲁棒性**：分析了不同子任务对最终预测误差的影响，并更换了底座模型和编码器进行验证。实验设计客观、对比公平。

### 6. 主要结论与发现
*   **对齐是关键**：通过序列嵌入而非简单的 ID 对齐，LLM 能更深层地理解行为逻辑。
*   **长尾优势显著**：BUA 在长尾行为预测上的提升（约 22.9%-37.4%）远超高频行为，证明了语义知识对稀疏数据的补偿作用。
*   **理解促进预测**：先进行行为模式理解再进行预测的多轮对话模式，比直接预测效果更好。
*   **可解释性飞跃**：模型能够将碎片化的行为 ID 转化为高层的认知特征总结。

### 7. 优点
*   **系统性的课程设计**：从简单序列重建到复杂自我反思，符合认知规律。
*   **统一框架**：成功将行为理解、预测和生成整合在一个对话模型中。
*   **损失平衡策略**：有效解决了多任务学习中因输出长度差异导致的训练偏置问题。
*   **可解释性强**：提供了定性和定量的证据，证明模型确实“读懂”了生活节奏。

### 8. 不足与局限
*   **依赖预训练编码器**：BUA 的性能受限于前端行为编码器（如 BehaveGPT）的质量。
*   **收敛不一致**：预测任务和生成任务的收敛速度存在差异，联合优化时可能难以同时达到最优。
*   **推理效率**：作为基于 LLM 的方法，其推理速度（约 40 分钟处理 2 万样本）虽与同类 LLM 方法持平，但远慢于传统轻量级模型。
*   **跨域挑战**：虽然在不同国家数据集上做了测试，但在极低资源或完全未见过的行为域下的泛化能力仍有待加强。

（完）
