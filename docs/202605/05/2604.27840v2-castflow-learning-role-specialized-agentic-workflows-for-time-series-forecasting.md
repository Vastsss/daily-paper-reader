---
title: "CastFlow: Learning Role-Specialized Agentic Workflows for Time Series Forecasting"
title_zh: CastFlow：用于时间序列预测的学习型角色专门化智能体工作流
authors: "Bokai Pan, Mingyue Cheng, Zhiding Liu, Shuo Yu, Xiaoyu Tao, Yuchong Wu, Qi Liu, Defu Lian, Enhong Chen"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27840v2"
tags: ["query:llm-ts"]
score: 10.0
evidence: 基于大语言模型的时间序列预测框架
tldr: 针对现有LLM时间序列预测方法存在的静态生成、模式提取受限等问题，本文提出CastFlow动态智能体预测框架。该框架通过规划、行动、预测和反思的智能体工作流，结合记忆模块和多视图工具包，实现了多轮上下文获取和迭代预测优化。CastFlow采用角色分工设计，结合通用推理与领域专用数值预测，并通过SFT和RLVR两阶段训练优化，在多个数据集上取得了领先的预测性能。
selection_source: fresh_fetch
motivation: 现有的基于LLM的时间序列预测方法多采用静态单次生成模式，难以充分提取时间模式和利用上下文信息。
method: 提出一种包含规划、行动、预测和反思的智能体工作流，并结合角色分工设计及SFT与RLVR两阶段训练。
result: 在多个不同领域的数据集上进行的广泛实验证明，CastFlow的预测效果优于现有的强基准模型。
conclusion: CastFlow通过引入动态智能体工作流和角色分工机制，为实现更具适应性和准确性的时间序列预测提供了新路径。
---

## 摘要
最近，大语言模型（LLMs）在时间序列预测中展现出巨大的潜力。然而，大多数现有的基于 LLM 的预测方法仍遵循静态生成范式，即通过单次传递直接将历史观测值映射到未来值。在这种范式下，预测受到有限的时间模式提取、单轮上下文特征获取、单次预测生成以及缺乏集成预测支持的限制。为了解决这些局限性，本研究提出了 CastFlow，这是一个动态智能体预测框架，支持多视图时间模式提取、多轮上下文特征获取、迭代预测优化以及基于集成预测的预测。首先，CastFlow 将预测过程组织为规划、行动、预测和反思，建立了一个智能体工作流。其次，该工作流由一个检索先前经验的记忆模块和一个构建诊断证据并提供可靠集成预测基线的多视图工具包支持。第三，CastFlow 采用了角色专门化设计，将通用推理与专门的数值预测相结合。在这种设计下，冻结的 LLM 保留通用推理能力，而经过微调的领域特定 LLM 则基于集成预测基线（而非从零开始）执行证据引导的数值预测。为了优化微调后的领域特定 LLM，我们进一步开发了一种面向工作流的两阶段训练方法，结合了监督微调（SFT）和带有可验证奖励的强化学习（RLVR）。为了评估 CastFlow 的有效性，我们在多种数据集上进行了广泛的实验，结果表明它在与强基线的对比中取得了优越的整体结果。我们希望这项工作能为实现更具自适应性和准确性的时间序列预测迈出一步。

## Abstract
Recently, large language models (LLMs) have shown great promise in time series forecasting. However, most existing LLM-based forecasting methods still follow a static generative paradigm that directly maps historical observations to future values in a single pass. Under this paradigm, forecasting is constrained by limited temporal pattern extraction, single-round acquisition of contextual features, one-shot forecast generation, and lack of support from ensemble forecasts. To address these limitations, in this work, we propose CastFlow, a dynamic agentic forecasting framework that enables multi-view temporal pattern extraction, multi-round contextual features acquisition, iterative forecast refinement, and forecasting with ensemble forecasts. First, CastFlow organizes the forecasting process into planning, action, forecasting, and reflection, establishing an agentic workflow. Second, this workflow is supported by a memory module that retrieves prior experience and a multi-view toolkit that constructs diagnostic evidence and provides a reliable ensemble forecast baseline. Third, CastFlow adopts a role-specialized design that combines general-purpose reasoning with specialized numerical forecasting. Under this design, a frozen LLM preserves general-purpose reasoning, while a fine-tuned domain-specific LLM performs evidence-guided numerical forecasting based on the ensemble forecast baseline, rather than from scratch. To optimize a fine-tuned domain-specific LLM, we further develop a two-stage workflow-oriented training that combines supervised fine-tuning (SFT) and reinforcement learning with verifiable rewards (RLVR). To evaluate the effectiveness of CastFlow, we conduct extensive experiments on diverse datasets and show that it achieves superior overall results against strong baselines. We hope that this work can serve as a step toward more adaptive and accurate time series forecasting.

---

## 论文详细总结（自动生成）

这是一份关于论文《CastFlow: Learning Role-Specialized Agentic Workflows for Time Series Forecasting》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的基于大语言模型（LLM）的时间序列预测方法大多遵循**静态生成范式**，即通过单次前向传递直接将历史数据映射为预测值。这种方式存在四大局限：
*   **模式提取受限**：难以捕捉复杂的时间动态。
*   **上下文获取单一**：缺乏多轮交互来深入理解数据背景。
*   **单次生成风险**：数值预测缺乏迭代修正机制。
*   **缺乏集成支持**：难以有效利用现有成熟预测模型的先验知识。

论文提出了 **CastFlow**，旨在将预测任务从“单次生成”转变为“**基于证据引导的决策过程**”，通过构建一个动态的智能体工作流来提升预测的准确性与自适应能力。

### 2. 方法论：核心思想与关键技术
CastFlow 的核心思想是**角色专门化（Role-Specialization）**与**动态工作流**的结合：

*   **智能体工作流**：包含规划（Planning）、行动（Action）、预测（Forecasting）和反思（Reflection）四个阶段。
*   **角色分工设计**：
    *   **通用推理层（冻结）**：使用强大的冻结 LLM（如 Grok 4）负责规划工具调用和逻辑反思。
    *   **数值预测引擎（微调）**：使用较小的领域特定 LLM（如 Qwen3-4B）负责根据收集的证据对基线进行数值修正。
*   **多视图工具包（Multi-view Toolkit）**：
    *   **基础锚定器**：通过检索历史相似模式，构建集成预测基线（Ensemble Baseline）。
    *   **统计/频谱剖析器、动态监测器、残差诊断器**：提供趋势、周期、突变及模型偏差等诊断证据。
*   **记忆模块**：存储并检索成功的推理轨迹和策略，为规划提供先验经验。
*   **两阶段训练流程**：
    1.  **SFT（监督微调）**：对齐推理信号与数值预测，建立基础协议遵循能力。
    2.  **RLVR（可验证奖励的强化学习）**：采用 **GRPO（群体相对策略优化）** 算法，以预测精度（MSE/MAE）作为可验证奖励，优化模型的数值修正能力。

### 3. 实验设计
*   **数据集**：涵盖 10 个真实世界数据集，涉及电力市场（BE, DE, FR, NP, PJM）、电网（ETTh, ETTm）、可再生能源（WP, SP）和水文（MOPEX）。
*   **Benchmark（基准）**：对比了 21 种方法，分为五类：
    *   统计模型（ARIMA, Prophet）。
    *   机器学习（XGBoost, LightGBM）。
    *   深度学习（PatchTST, iTransformer, Autoformer 等）。
    *   时间序列大模型（Chronos, TimesFM, Sundial）。
    *   基于 LLM/智能体的方法（Time-LLM, Time-R1, AlphaCast 等）。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA A800 GPU**。
*   **模型配置**：推理智能体使用 Grok 4（API/冻结），本地预测模型使用 Qwen3-4B。
*   **训练时长**：SFT 阶段进行 1 个 epoch，RLVR 阶段进行 3 个 epoch。文中未明确给出具体的总训练小时数，但强调了 GRPO 算法相比 PPO 具有更低的内存开销。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的对比实验（10 个数据集 × 21 个基线）、组件消融实验（验证记忆、工具、反思的必要性）、工具包分类消融、训练策略对比（SFT vs RLVR）、模型缩放实验（1.7B 到 8B）以及记忆检索敏感性分析。
*   **充分性与公平性**：实验设置了统一的 7:1:2 数据划分，严格遵守时间顺序，且在评估时将所有预测值反向转换回原始量级进行对比，确保了实验的客观性与公平性。

### 6. 主要结论与发现
*   **性能领先**：CastFlow 在绝大多数数据集（尤其是长期预测）上显著优于所有基线模型。
*   **角色分工的必要性**：实验证明，直接让通用 LLM 做数值预测效果较差，而“通用推理引导 + 专用数值修正”的协作模式效果最优。
*   **强化学习的作用**：RLVR 阶段通过 GRPO 显著提升了模型在连续数值空间中的修正精度，超越了单纯的 SFT 模仿学习。
*   **基线的重要性**：基础锚定器提供的集成基线是预测成功的关键，智能体在此基础上进行“残差修正”比从零生成更稳定。

### 7. 优点与亮点
*   **范式创新**：将预测任务建模为动态的、可解释的智能体决策流，而非黑盒映射。
*   **训练高效**：引入 GRPO 算法，在不需要价值函数网络的情况下实现了高效的强化学习优化。
*   **鲁棒性强**：通过记忆模块和反思机制，有效减少了 LLM 常见的格式错误和数值幻觉。
*   **工具集成**：多视图工具包的设计非常全面，涵盖了统计、频谱和动力学等多个维度。

### 8. 不足与局限
*   **推理延迟**：由于涉及多轮智能体交互、工具调用和反思循环，其推理速度显著慢于传统的单次推理模型（如 DLinear 或 PatchTST）。
*   **依赖性**：系统性能高度依赖于底层冻结推理模型（如 Grok 4）的规划能力。
*   **特定场景波动**：在某些极高频波动且外部变量关联弱的数据集（如 PJM）上，提升幅度相对有限。
*   **应用限制**：对于需要毫秒级响应的实时预测场景，该框架的计算开销可能过大。

（完）
