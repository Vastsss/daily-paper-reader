---
title: "Why Model Selection Fails in Time Series Forecasting: An Empirical Study of Instability Across Data Regimes"
title_zh: 为什么模型选择在时间序列预测中会失效：关于跨数据机制不稳定性的实证研究
authors: "Tahir Cetin Akinci, Alfredo A. Martinez-Morales"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01608v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 时间序列预测中模型选择不稳定性实证研究
tldr: 本研究探讨了时间序列预测中基于规则的模型选择失效的原因。通过引入包含趋势、季节性、噪声和时间依赖性的描述符框架，研究分析了数据特征与模型性能的关系。实验表明，基于规则的选择机制在多领域数据集上表现不佳，模型排名在不同场景下具有高度不稳定性。研究揭示了预测性能对数据特征和预测界限的高度敏感性，强调了模型选择的上下文依赖性，并指出需要更具适应性的数据驱动策略。
selection_source: fresh_fetch
motivation: 旨在探究为何简单的基于规则的数据特征描述符无法可靠地指导时间序列预测模型的选择。
method: 提出一种基于描述符的框架，通过测量趋势、季节性、噪声等属性来表征时间序列，并构建规则映射模型。
result: 实验发现基于规则的选择准确率极低，且模型性能在不同数据特征和预测界限下表现出显著的排名不稳定性。
conclusion: 静态描述符方法无法可靠预测预测性能，模型选择具有强烈的上下文依赖性，需转向更灵活的数据驱动策略。
---

## 摘要
时间序列预测模型在具有不同统计和结构属性的数据集上往往表现出不一致的性能。尽管有多种预测技术可供选择，但目前尚不清楚是否可以通过简单的数据特征来可靠地指导模型选择。本文通过分析数据机制描述符与模型性能之间的关系，探讨了基于规则的模型选择在时间序列预测中失效的原因。本文引入了一个基于描述符的框架，利用可测量的属性（包括趋势强度、季节性、噪声水平和时间依赖性）来表征时间序列。基于这些描述符，制定了一种基于规则的选择机制，将数据机制映射到候选预测模型。该方法在跨越不同领域和预测时界的多个真实世界数据集上进行了评估。结果表明，基于规则的模型选择准确率较低，仅在极少数情况下能正确识别模型。在推荐模型与经验最优模型之间观察到了显著差异，特别是在噪声较大和混合机制的情况下。进一步分析表明，模型性能对数据集特征和预测时界都高度敏感，导致在不同场景下排名存在严重的不稳定性。这些发现解释了为什么简单的启发式规则无法泛化，并证明了无法使用静态的、基于描述符的方法来可靠地预测预测性能。本研究提供了实证证据，表明时间序列预测中的模型选择本质上依赖于上下文，并强调了对更具自适应性的数据驱动策略的需求。

## Abstract
Time series forecasting models often exhibit inconsistent performance across datasets with varying statistical and structural properties. Despite the wide range of available forecasting techniques, it remains unclear whether model selection can be reliably guided by simple data characteristics. This paper investigates why rule-based model selection fails in time series forecasting by analyzing the relationship between data-regime descriptors and model performance. A descriptor-based framework is introduced to characterize time series using measurable properties, including trend strength, seasonality, noise level, and temporal dependence. Based on these descriptors, a rule-based selection mechanism is formulated to map data regimes to candidate forecasting models. The approach is evaluated on multiple real-world datasets across different domains and forecasting horizons. The results show that rule-based model selection achieves low accuracy, with correct model identification occurring in only a small fraction of cases. Significant discrepancies are observed between recommended and empirically optimal models, particularly in noisy and mixed regimes. Further analysis reveals that model performance is highly sensitive to both dataset characteristics and forecasting horizon, resulting in substantial ranking instability across scenarios. These findings explain why simple heuristic rules fail to generalize and demonstrate that forecasting performance cannot be reliably predicted using static, descriptor-based approaches. This study provides empirical evidence that model selection in time series forecasting is inherently context-dependent and highlights the need for more adaptive, data-driven strategies.

---

## 论文详细总结（自动生成）

以下是对论文《Why Model Selection Fails in Time Series Forecasting: An Empirical Study of Instability Across Data Regimes》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管时间序列预测技术（从统计模型到深度学习）取得了巨大进步，但模型性能对数据特征高度敏感。从业者通常依赖“试错法”或简单的启发式规则（如：有季节性用ETS，噪声大用随机森林）来选择模型。
*   **核心问题**：论文探讨了为什么这些基于规则的静态模型选择方法在实际应用中经常失效，并试图通过实证研究揭示数据特征（Data Regimes）与模型表现之间复杂且不稳定的关系。

### 2. 方法论
论文提出了一个基于描述符（Descriptor-based）的分析框架：
*   **数据机制表征**：将时间序列 $x(t)$ 映射为一个多维描述符向量 $r$，包含六个关键统计属性：
    1.  **趋势强度 ($r_1$)**：线性拟合斜率与信号变异性的比值。
    2.  **季节性 ($r_2$)**：通过频谱功率浓度衡量周期性。
    3.  **噪声水平 ($r_3$)**：残差方差占总方差的比例。
    4.  **非平稳性 ($r_4$)**：基于ADF检验的p值。
    5.  **间歇性 ($r_5$)**：衡量数据的稀疏程度。
    6.  **结构性断点 ($r_6$)**：单位时间内变点（Change points）的数量。
*   **模型兼容性函数**：定义评分函数 $\phi_k(r)$，通过线性加权或相似度度量来评估候选模型 $M_k$ 与数据特征 $r$ 的匹配度。
*   **规则映射算法**：构建了一个确定性的决策流程（如：低噪声且高自相关则选ARIMA），作为评估基准。
*   **可解释性分析**：引入局部灵敏度度量，通过计算评分函数对描述符的梯度来量化各特征对模型选择的影响。

### 3. 实验设计
*   **数据集**：涵盖四个领域的真实世界数据：
    *   **Energy** (M4竞赛数据)
    *   **Tourism** (旅游需求数据)
    *   **Electricity** (UCI电力消耗数据)
    *   **PGCB** (孟加拉国国家电网负荷数据)
*   **对比方法（候选模型）**：
    *   **统计模型**：ARIMA, ETS, Naive, SeasonalNaive。
    *   **机器学习**：Random Forest (RF), XGBoost。
    *   **深度学习**：LSTM, GRU。
*   **Benchmark 策略**：
    *   **Best Single Model**：固定使用单一模型。
    *   **Grid Search**：理想状态下的详尽超参数优化。
    *   **AutoML**：使用 AutoGluon v1.0 自动建模框架。
*   **评估指标**：RMSE 和 MAE，采用 3 折滚动验证（Rolling Validation）。

### 4. 资源与算力
*   **说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。考虑到涉及 LSTM/GRU 及 AutoGluon，通常需要具备 CUDA 支持的 GPU 环境，但文中重点在于实证分析而非计算效率优化。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个大型数据集上进行了测试，涵盖了不同的预测时界（$H=1$ 和 $H=24$）。
*   **充分性与客观性**：
    *   实验设计较为客观，采用了统一的预处理和交叉验证协议。
    *   通过 Friedman 检验进行了统计显著性分析。
    *   **局限性**：虽然数据集涵盖了不同领域，但数据集的总数（4个）相对较少，可能不足以完全代表所有可能的时间序列机制。

### 6. 主要结论与发现
*   **选择准确率极低**：基于规则的选择机制整体准确率仅为 **12.5%**，在噪声较大或混合机制下几乎完全失效。
*   **排名不稳定性**：模型表现对数据集特征和预测时界（Horizon）高度敏感。在短时界表现优异的模型，在长时界可能表现极差，导致模型排名剧烈波动。
*   **统计显著性缺失**：Friedman 检验显示（p=0.3476），不同模型间的性能差异在统计上并不显著，说明没有单一模型能统治所有场景。
*   **描述符局限性**：低维的静态统计描述符不足以捕捉决定模型性能的高阶复杂交互作用。

### 7. 优点
*   **系统性框架**：提供了一套标准化的时间序列特征提取和模型匹配分析流程。
*   **揭示了“失效”本质**：不同于以往追求更高精度的论文，本文深入探讨了模型选择“为什么不行”，为预测工作流的自动化提供了警示。
*   **可解释性**：通过灵敏度分析试图打开模型选择的“黑盒”。

### 8. 不足与局限
*   **数据集覆盖面**：仅使用了 4 个数据集，对于证明“通用性失效”来说样本量略显单薄。
*   **规则过于简化**：实验中使用的启发式规则（Algorithm 1）较为基础，可能未代表最先进的元学习（Meta-learning）选择策略。
*   **实时性挑战**：在流数据环境下，实时提取描述符（如频谱分析和变点检测）的计算开销可能成为瓶颈。
*   **缺乏替代方案的深度验证**：论文明确了静态规则的失败，但对于其提出的“更具适应性的数据驱动策略”未给出具体的、高性能的替代实现。

（完）
