---
title: Real-Time Neural Distributed Energy Resources Dispatch with Feasibility Guarantees
title_zh: 具有可行性保证的实时神经分布式能源调度
authors: "Jie Zhu, Yinliang Xu, Hongbin Sun"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00317v1"
tags: ["query:llm"]
score: 9.0
evidence: 具有可行性保证的神经分布式能源调度
tldr: 针对可再生能源高频实时调度的需求，本文提出一种无需外部求解器的神经调度框架。该框架通过凸包络定理推导DistFlow模型的凸内部近似，并结合基于鲁棒优化的仿射策略与二分投影方案，在保证非凸潮流约束可行性的同时，实现了毫秒级的调度速度和接近最优的性能，解决了神经网络在电力调度中难以严格满足约束的难题。
selection_source: fresh_fetch
motivation: 传统的神经网络调度方法在处理非凸潮流约束时，难以在不依赖外部求解器的情况下保证解的严格可行性。
method: 利用凸包络定理构建DistFlow模型的凸内部近似，并结合鲁棒优化仿射策略与二分投影机制实现快速的可行性恢复。
result: 实验结果显示，该方法能在10的负3次方秒量级内恢复解的可行性，同时保持了接近最优的调度效果。
conclusion: 该框架为分布式能源的实时、安全调度提供了一种兼具计算效率与理论可行性保障的神经求解方案。
---

## 摘要
可再生能源渗透率的不断提高使得高频实时调度变得必不可少。虽然基于神经网络的代理模型能够实现高效计算的调度，但在不使用外部求解器的情况下严格执行非凸潮流约束仍然是一个根本性的挑战。为了弥补这一差距，本文提出了一种具有严格可行性保证的无求解器神经调度框架。首先通过凸包络定理推导了 DistFlow 模型的凸内部近似。基于该近似，构建了一种基于鲁棒优化的仿射策略，以产生理论上经过认证的内点映射规则，随后将其嵌入到基于二分的投影方案中，从而在无需任何外部求解器的情况下，高效地恢复不可行神经网络输出的可行性。实验结果表明，所提方法在保持近乎最优性能的同时，能在 $10^{-3}$ 秒量级内恢复可行性。

## Abstract
The growing penetration of renewable energy necessitates high-frequency real-time scheduling. While neural network-based surrogates enable computationally efficient scheduling, strictly enforcing nonconvex power flow constraints without external solvers remains a fundamental challenge. To bridge this gap, this letter proposes a solver-free neural dispatch framework with rigorous feasibility guarantees. A convex inner approximation of the DistFlow model is first derived via the convex envelope theorem. Building upon this approximation, a robust optimization-based affine policy is formulated to yield a theoretically certified interior-point mapping rule, which is then embedded within a bisection-based projection scheme to efficiently recover feasibility for infeasible NN outputs without any external solver. Experimental results demonstrate that the proposed method restores feasibility on the order of $10^{-3}$ s while maintaining near-optimal performance.

---

## 论文详细总结（自动生成）

### 论文结构化总结：具有可行性保证的实时神经分布式能源调度

#### 1. 核心问题与研究动机
*   **研究背景**：随着可再生能源渗透率提高，电网需要高频（秒级）实时调度以应对功率波动。
*   **核心问题**：传统的神经网络（NN）调度虽然计算速度快，但难以严格满足非凸的潮流约束（Power Flow Constraints）。
*   **现有方案局限**：
    *   **罚函数法**：无法保证 100% 可行性。
    *   **外部求解器投影**：虽然能保证可行性，但依赖 IPOPT 等求解器，计算耗时长，违背了 NN 实时调度的初衷。
    *   **同胚映射/内点投影**：现有方法在处理包含“孔洞”的非凸可行域时存在局限，且缺乏可解释的内点生成机制。
*   **研究目标**：开发一种**无需外部求解器**、具有**严格理论保证**且能处理**非凸约束**的神经调度框架。

#### 2. 核心方法论
该论文提出了一种基于内点投影的无求解器神经调度框架，核心步骤如下：
*   **凸内部近似（Convex Inner Approximation）**：利用凸包络定理（Convex Envelope Theorem）对非凸的 DistFlow 模型进行松弛和线性化，推导出潮流方程的线性内近似边界。
*   **鲁棒仿射内点规则（Affine IP Rule）**：
    *   为了在不调用求解器的情况下实时获得可行域内的“内点”，论文设计了一个关于输入负荷/光伏出力的仿射函数 $f_{IP}(x) = W_{IP}x + w_{IP}$。
    *   通过离线求解一个**鲁棒优化问题**，确定参数 $W_{IP}$ 和 $w_{IP}$，确保对于任何可能的输入波动，该规则生成的点都严格落在非凸可行域的内部。
*   **二分投影方案（Bisection-based Projection）**：
    *   在线运行时，若 NN 输出的点不满足约束，则在 NN 输出点与仿射内点之间连线。
    *   利用二分法在该线段上快速搜索最接近 NN 输出的可行点。
*   **神经训练策略**：采用结合了物理约束惩罚项的监督学习，并利用隐函数定理（Implicit Function Theorem）进行灵敏度分析，以优化网络参数。

#### 3. 实验设计
*   **测试场景**：
    *   **IEEE 33 节点系统**（含 7 个光伏单元）。
    *   **129 节点系统**（含 28 个光伏单元）。
*   **对比方法（Benchmark）**：
    *   **V-NN**：普通监督学习神经网络。
    *   **P-NN**：带罚函数的神经网络。
    *   **O-NN**：使用外部求解器（IPOPT）进行正交投影的方法。
    *   **B-NN**：本文提出的基于内点投影的方法。
*   **评估指标**：最优性差距（Optimal Gap）、可行率（Feasibility Rate）、推理时间、投影耗时。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 GPU/CPU 类型）及训练时长。
*   **实现细节**：提到使用了全连接神经网络（两个隐藏层），超参数经过仔细调优，并提供了 GitHub 源代码链接。

#### 5. 实验数量与充分性
*   **实验规模**：针对每个系统生成了 7,000 个样本，按 5:1:1 比例划分为训练、验证和测试集。
*   **充分性评价**：
    *   实验涵盖了中小型（33节点）和较大规模（129节点）的配电网，具有一定的代表性。
    *   对比了从基础 NN 到最先进的求解器投影法，实验维度完整。
    *   **客观性**：通过 1,000 个测试样本的箱线图展示了投影时间的分布，证明了所提方法在效率上的稳定性。

#### 6. 主要结论与发现
*   **100% 可行性**：所提 B-NN 方法在所有测试样本中均实现了 100% 的可行性，彻底解决了 V-NN 和 P-NN 无法保证约束的问题。
*   **极高的计算效率**：投影过程在 **$10^{-3}$ 秒（毫秒级）**量级完成。相比 O-NN（求解器投影），速度提升了约 **28 倍**。
*   **近优性能**：在保证可行性的前提下，最优性损失极小（在 33 节点系统中仅比 O-NN 多出约 0.059% 的误差）。

#### 7. 优点与亮点
*   **理论严谨**：通过鲁棒优化和凸包络定理，为内点的存在性和可行性提供了严格的理论证明。
*   **无求解器依赖**：在线运行阶段完全脱离外部优化软件，极大地提升了部署的便捷性和实时性。
*   **稳定性强**：相比基于求解器的方法，其计算时间方差极小，更适合对时间确定性要求高的工业场景。

#### 8. 不足与局限
*   **拓扑限制**：该方法基于 DistFlow 模型，目前主要适用于**径向（Radial）配电网**，对于复杂的环网或输电网（AC-OPF）的适用性有待进一步验证。
*   **最优性权衡**：虽然误差很小，但由于投影是向内点方向收缩，其结果在理论上略逊于正交投影（O-NN）。
*   **离线计算负担**：虽然在线速度快，但离线阶段需要求解复杂的鲁棒优化问题来确定仿射规则参数，若电网拓扑频繁变动，重新计算的成本较高。

（完）
