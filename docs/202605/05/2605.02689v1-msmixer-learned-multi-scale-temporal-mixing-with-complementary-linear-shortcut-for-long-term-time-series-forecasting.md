---
title: "MSMixer: Learned Multi-Scale Temporal Mixing with Complementary Linear Shortcut for Long-Term Time Series Forecasting"
title_zh: MSMixer：用于长期时间序列预测的带有互补线性捷径的可学习多尺度时间混合模型
authors: Ahmed Cherif
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02689v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 用于长期时间序列预测的多尺度MLP架构
tldr: 针对长期时间序列预测中单一分辨率难以捕捉多尺度模式的问题，本文提出MSMixer。这是一种轻量级多尺度MLP架构，通过三个并行分支处理不同下采样率的时间特征，并结合可学习的门控机制和DLinear残差连接。在保持低参数量和线性复杂度的同时，MSMixer在多个基准数据集上显著优于现有轻量级模型，并能与Transformer类模型媲美。
selection_source: fresh_fetch
motivation: 现有的轻量级MLP模型通常在单一时间分辨率下运行，难以同时捕捉快速波动、中程周期和长期宏观趋势。
method: 提出一种包含三个并行尺度分支、可学习Softmax门控权重以及DLinear互补快捷连接的通道独立型多尺度MLP架构。
result: 在ETT基准测试中，MSMixer以仅112K的参数量实现了最低的平均MSE（0.357），优于DLinear和NLinear，且在多项配置中超越了Transformer模型。
conclusion: MSMixer证明了通过多尺度特征融合与线性快捷连接的结合，轻量级模型可以在极低计算开销下实现卓越的长期时间序列预测性能。
---

## 摘要
长期时间序列预测要求模型能够从固定的回顾窗口中同时捕捉快速振荡、中程周期性和缓慢演变的宏观趋势。现有的轻量级基于 MLP 的模型通常在单一时间分辨率上运行，限制了它们显式建模多尺度模式的能力。我们提出了 MSMixer，这是一种通道独立的多尺度 MLP 架构，通过三项互补创新解决了这一局限性：(i) 在下采样因子为 {1x, 4x, 16x} 处设置三个带有独立 MLP 块的并行尺度分支；(ii) 一个可学习的 softmax 门控，用于动态加权分支输出；(iii) 一个提供全窗口趋势和季节性上下文的 DLinear 互补捷径。在预测长度 H=96 时，MSMixer 仅包含 112K 个参数，且运行复杂度为 O(T)。在四个 ETT 基准测试集上，采用标准的时间顺序划分和三个随机种子进行评估，MSMixer 在轻量级模型中实现了最低的平均均方误差（MSE, 0.357），优于 DLinear（0.386, -7.4%）和 NLinear（0.365, -2.1%），在 16 种配置中胜出 12 种。与文献中的五个基于 Transformer 的基准模型相比，MSMixer 在 16 种配置中的 9 种里取得了最佳或次佳的 MSE，同时参数量比 PatchTST 少 5 倍。消融实验和敏感性分析证实了多尺度分支和 DLinear 捷径的互补贡献。

## Abstract
Long-term time series forecasting requires models that simultaneously capture rapid oscillations, medium-range periodicities, and slowly evolving macro-trends from a fixed look-back window. Existing lightweight MLP-based models typically operate on a single temporal resolution, limiting their ability to explicitly model patterns at multiple scales. We propose MSMixer, a channel-independent multi-scale MLP architecture that addresses this limitation through three complementary innovations: (i) three parallel scale branches at down-sample factors {1x, 4x, 16x} with independent MLP blocks, (ii) a learnable softmax gate that dynamically weighs branch outputs, and (iii) a DLinear complementary shortcut that provides full-window trend and seasonality context. MSMixer contains only 112K parameters at H=96 and runs at O(T) complexity. Evaluated on four ETT benchmarks with standard chronological splits and three random seeds, MSMixer achieves the lowest average MSE (0.357) among lightweight models, outperforming DLinear (0.386, -7.4%) and NLinear (0.365, -2.1%), winning 12 of 16 configurations. Against five Transformer-based baselines from the literature, MSMixer achieves best or second-best MSE in 9 of 16 configurations while using 5x fewer parameters than PatchTST. Ablation and sensitivity analyses confirm the complementary contributions of the multi-scale branches and the DLinear shortcut.

---

## 论文详细总结（自动生成）

以下是对论文《MSMixer: Learned Multi-Scale Temporal Mixing with Complementary Linear Shortcut for Long-Term Time Series Forecasting》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
长期时间序列预测（LTSF）的核心挑战在于如何从固定的回顾窗口中同时捕捉**多尺度模式**，包括快速的随机波动、中程的周期性以及缓慢演变的宏观趋势。
*   **现有局限：** 现有的轻量级模型（如 DLinear, NLinear）虽然计算效率高，但通常在单一时间分辨率下运行，难以显式建模多尺度特征。
*   **研究动机：** 提出一种既能保持 MLP 架构的线性复杂度和低参数量，又能通过多分辨率分支有效融合不同尺度信息的模型。

### 2. 论文提出的方法论：MSMixer
MSMixer 采用通道独立（Channel-Independent）的架构，核心由三个创新组件构成：
*   **并行多尺度分支（Multi-Scale Branches）：**
    *   模型设置了三个并行分支，分别对应不同的下采样因子：1x（原始分辨率）、4x 和 16x。
    *   每个分支使用独立的 MLP 块处理对应尺度的特征，旨在分别捕捉高频、中频和低频模式。
*   **可学习的门控机制（Learnable Softmax Gate）：**
    *   为了动态调整不同尺度对预测结果的贡献，模型引入了一个基于 Softmax 的可学习权重参数。
    *   该门控机制根据数据特征自动学习如何加权融合三个分支的输出。
*   **DLinear 互补捷径（Complementary Shortcut）：**
    *   在多尺度分支之外，并联了一个 DLinear 模块作为残差连接。
    *   该捷径通过趋势-季节性分解（Decomposition），为模型提供全窗口的宏观背景信息，弥补了下采样可能带来的局部信息损失。

### 3. 实验设计
*   **数据集：** 采用了电力变压器温度（ETT）系列的四个基准数据集：ETTh1, ETTh2, ETTm1, ETTm2。
*   **实验场景：** 长期预测任务，回顾窗口 $L=96$，预测长度 $H \in \{96, 192, 336, 720\}$。
*   **Benchmark 与对比方法：**
    *   **轻量级模型：** DLinear, NLinear。
    *   **Transformer 类模型：** PatchTST, iTransformer, Crossformer, FEDformer, Autoformer。
*   **评估指标：** 均方误差（MSE）和平均绝对误差（MAE），采用标准的时间顺序划分和三个随机种子取平均值。

### 4. 资源与算力
*   **参数量：** 在 $H=96$ 时，MSMixer 仅有约 **112K** 个参数，比 PatchTST 少约 5 倍。
*   **复杂度：** 运行复杂度为 $O(T)$（线性复杂度）。
*   **算力说明：** 论文摘要和正文摘要部分**未明确说明**具体的 GPU 型号、数量或具体的训练时长。但从其轻量级 MLP 的性质可以推断，其训练和推理开销远低于 Transformer 类模型。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 4 个数据集，每个数据集 4 种预测长度，共 16 种配置。
*   **充分性：** 
    *   使用了 3 个随机种子以确保结果的统计稳定性。
    *   进行了**消融实验**，验证了多尺度分支和 DLinear 捷径各自的贡献。
    *   进行了**敏感性分析**，探讨了不同下采样因子对性能的影响。
*   **客观性：** 对比了当前最先进的 Transformer 模型和经典的轻量级模型，实验设计符合 LTSF 领域的标准规范。

### 6. 主要结论与发现
*   **性能领先：** 在轻量级模型中，MSMixer 取得了最低的平均 MSE（0.357），在 16 种配置中胜出 12 种，显著优于 DLinear（提升 7.4%）和 NLinear（提升 2.1%）。
*   **媲美 Transformer：** 尽管参数量极小，MSMixer 在 9/16 的配置中达到了与 PatchTST 等重型模型相当甚至更好的表现。
*   **多尺度的重要性：** 实验证明，动态融合多个时间分辨率的特征比单一分辨率预测更稳健。

### 7. 优点
*   **高效性：** 极低的参数量和线性计算复杂度，适合资源受限的部署场景。
*   **架构简洁：** 结合了 MLP 的简单性和多尺度分析的有效性，易于实现。
*   **动态适应：** 可学习的门控机制允许模型根据不同序列的特性自动调整关注的尺度。

### 8. 不足与局限
*   **数据集覆盖面：** 论文主要集中在 ETT 系列数据集上，虽然这是标准做法，但缺乏在 Weather、Traffic、Electricity 等更多样化数据集上的广泛验证。
*   **下采样策略：** 采用固定的下采样因子（1, 4, 16），对于某些具有特殊周期（如 7 天、24 小时）的时间序列，可能需要更精细或自适应的采样频率。
*   **长期依赖：** 尽管在 $H=720$ 表现良好，但作为基于 MLP 的模型，在处理极长距离的非线性依赖时，理论上可能仍逊色于具有全局注意力机制的 Transformer。

（完）
