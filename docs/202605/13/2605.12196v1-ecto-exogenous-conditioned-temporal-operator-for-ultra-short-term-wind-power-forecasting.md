---
title: "ECTO: Exogenous-Conditioned Temporal Operator for Ultra-Short-Term Wind Power Forecasting"
title_zh: ECTO：用于超短期风电预测的外生条件时间算子
authors: "Cao Yuan, Junjun Wang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12196v1"
tags: ["query:llm-wd"]
score: 10.0
evidence: 利用时间算子进行超短期风电功率预测
tldr: 针对超短期风电预测中气象外生变量利用不足的问题，本文提出ECTO框架。该框架包含物理引导的变量选择（PGVS）和外生条件状态细化（ECRR）两个核心模块，通过分层稀疏选择和专家混合机制，实现了对气象变量的动态建模与预测校准。实验证明，ECTO在多个风电场数据集上均取得领先性能，显著提升了预测精度和可解释性。
selection_source: fresh_fetch
motivation: 现有的深度学习预测方法未能充分利用气象外生变量的物理结构，且难以适应不同站点和预测时界的动态变化。
method: 提出ECTO框架，利用PGVS模块进行物理感知的稀疏变量选择，并通过ECRR模块利用专家混合机制进行增益偏差校准。
result: "在三个不同气候和容量的风电场实验中，ECTO的均方误差（MSE）最低，相比最强基准提升了2.2%至8.6%。"
conclusion: ECTO通过结合物理先验与动态校准，有效提升了超短期风电预测的准确性，并展现出良好的物理可解释性。
---

## 摘要
准确的超短期风电预测对于电网调度和备用管理至关重要，但由于风力发电的非平稳性和条件依赖特性，这仍然具有挑战性。气象外生变量蕴含着丰富的预测信息，但最具信息量的变量组合会随站点、运行工况和预测时界的变化而改变。现有的深度学习方法要么通过统一混合或软门控将外生输入视为通用的辅助通道，要么依赖于 PCA 等固定的预处理步骤，未能充分利用气象变量的物理结构。我们提出了 ECTO（Exogenous-Conditioned Temporal Operator，外生条件时间算子），这是一个将外生变量建模分解为两个互补模块的统一框架。物理基础变量选择（PGVS）利用领域感知的物理先验和 sparsemax 激活函数，对外生变量进行分层且具有组感知能力的稀疏选择，从而生成紧凑且具有条件自适应性的外生上下文。外生条件状态细化（ECRR）通过混合专家（MoE）范式，将预测路由至学习到的状态专家，由其进行增益-偏差校准和特定时界的修正。在涵盖不同气候、容量（66–200 MW）和外生维度（11–13 个变量）的三个风电场上的实验表明，ECTO 在所有站点均实现了最低的均方误差（MSE），相较于最强基线模型的提升幅度在 2.2% 至 5.2% 之间，在较长预测时界（H=32）下提升幅度扩大至 8.6%。消融实验证实了各外生相关组件的积极贡献（PGVS 提升 1.84%，ECRR 提升 2.86%），可解释性分析则揭示了 PGVS 学习到了具有物理意义且特定于站点的变量选择模式，而 ECRR 则收敛于跨站点一致且区分明确的校准策略。

## Abstract
Accurate ultra-short-term wind power forecasting is critical for grid dispatch and reserve management, yet remains challenging due to the non-stationary, condition-dependent nature of wind generation. Meteorological exogenous variables carry substantial predictive information, but the most informative variable combination varies across sites, operating conditions, and prediction horizons. Existing deep learning approaches either treat exogenous inputs as generic auxiliary channels through uniform mixing or soft gating, or rely on fixed preprocessing steps such as PCA, without exploiting the physical structure of meteorological variables. We propose ECTO (Exogenous-Conditioned Temporal Operator), a unified framework that decomposes exogenous variable modeling into two complementary modules. Physically-Grounded Variable Selection (PGVS) performs hierarchical, group-aware sparse selection over exogenous variables using a domain-informed physical prior and sparsemax activations, producing a compact, condition-adaptive exogenous context. Exogenous-Conditioned Regime Refinement (ECRR) routes the forecast through learned regime experts that apply gain--bias calibration and horizon-specific corrections via a mixture-of-experts paradigm. Experiments on three wind farms spanning different climates, capacities (66--200 MW), and exogenous dimensions (11--13 variables) demonstrate that ECTO achieves the lowest MSE across all sites, with relative improvements over the strongest baseline ranging from 2.2% to 5.2%, widening to 8.6% at the longer prediction horizon ($H=32$). Ablation analysis confirms that each exogenous-related component contributes positively (PGVS +1.84%, ECRR +2.86%), and interpretability analysis reveals that PGVS learns physically meaningful, site-specific variable selection patterns, while ECRR converges to well-separated calibration strategies consistent across sites.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ECTO**（Exogenous-Conditioned Temporal Operator）的新型深度学习框架，专门用于解决超短期风电功率预测中外生变量（如气象数据）建模不足的问题。

### 1. 核心问题与研究动机
*   **核心问题**：风电具有极强的非平稳性和条件依赖性。虽然气象变量（风速、温度、压力等）对预测至关重要，但不同变量在不同站点、不同工况（如大风 vs. 微风）和不同预测时界下的贡献度是动态变化的。
*   **研究动机**：现有的深度学习模型通常将外生变量简单地与目标序列拼接或通过 PCA 降维，忽略了气象变量之间的物理结构（如风速与空气密度的耦合），且缺乏根据当前环境状态动态调整预测结果的机制。

### 2. 方法论
ECTO 框架将外生变量建模分解为“选择”和“校准”两个核心阶段：
*   **物理引导的变量选择 (PGVS)**：
    *   **物理分组**：基于领域知识将变量分为“风速组”和“大气组”。
    *   **分层稀疏选择**：首先对物理组进行评分，再对组内变量评分。使用 **Sparsemax** 激活函数实现“硬选择”（产生精确的零权重），从而剔除无关噪声。
    *   **特征融合**：通过交叉注意力机制将筛选后的外生上下文注入到由时间分词器（Temporal Tokenizer）提取的目标功率特征中。
*   **外生条件状态细化 (ECRR)**：
    *   **专家混合机制 (MoE)**：利用外生上下文作为路由信号，将预测引导至不同的“状态专家”。
    *   **增益-偏差校准**：每个专家学习特定的缩放（Gain）和偏移（Bias）参数，对基础预测进行非线性修正。
    *   **时界特定修正**：针对预测步长的增加（如第1步 vs. 第16步）引入专门的校准嵌入，以应对随时间累积的预测不确定性。

### 3. 实验设计
*   **数据集**：使用了三个具有代表性的风电场数据：
    *   **WF1 & WF4**：位于中国东部，容量分别为 99MW 和 66MW，具有不同的零功率比例。
    *   **Xinjiang**：位于中国西北干旱区，容量 200MW，具有剧烈的昼夜温差和多层风速观测。
*   **Benchmark 与对比方法**：
    *   对比了 9 种主流模型：包括 Transformer 变体（Informer, PatchTST, iTransformer, TimeXer）、线性模型（DLinear, XLinear, CrossLinear）以及多尺度模型（TimesNet, TimeMixer）。
*   **评估指标**：MSE（均方误差）、MAE（平均绝对误差）、NSE（纳什效率系数）。

### 4. 资源与算力
*   **硬件**：论文提到实验在“单张 NVIDIA GPU”上运行，但未明确说明具体型号（如 A100 或 RTX 3090）。
*   **软件与训练**：基于 PyTorch 实现，使用 Adam 优化器和 OneCycleLR 调度器，最大训练轮数为 50 Epoch，设置了早停机制（Patience=10）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个数据集上进行了主实验，涵盖了 H=16（4小时）和 H=32（8小时）两种预测时界。
*   **消融实验**：非常详尽，分别验证了外生变量的有无、物理分组的作用、PGVS 模块各组件以及 ECRR 模块各组件的贡献。
*   **误差分解**：按功率等级、爬坡强度和预测步长对误差进行了细分分析。
*   **客观性**：所有实验均重复运行 3 次并报告了标准差，确保了结果的统计显著性和公平性。

### 6. 主要结论与发现
*   **性能领先**：ECTO 在所有站点均取得最低 MSE，相比最强基线（XLinear）提升了 2.2%~5.2%，在长时界（H=32）下优势扩大至 8.6%。
*   **物理一致性**：PGVS 成功识别出风速是主导因素，但在中等功率区间，温度和压力（影响空气密度）的权重显著上升，符合物理规律。
*   **校准策略**：ECRR 自动演化出两种清晰的校准策略（下调预测值和上调偏移量），且这种模式在不同站点间具有高度一致性。

### 7. 优点
*   **结构化建模**：不同于黑盒模型，ECTO 引入物理先验（分组与分层选择），增强了模型的可解释性。
*   **动态适应性**：通过 MoE 路由，模型能根据当前天气状态（如是否发生爬坡事件）动态调整校准逻辑。
*   **稀疏性**：Sparsemax 的应用有效降低了高维气象数据带来的噪声干扰。

### 8. 不足与局限
*   **物理分组依赖**：目前的物理分组仍依赖人工先验知识，若应用于缺乏物理背景的通用时间序列，可能需要额外的自动分组机制。
*   **专家数量固定**：MoE 的专家数量（K=4）是预设的超参数，缺乏根据数据复杂度自动调整专家规模的灵活性。
*   **空间建模缺失**：该研究侧重于单站点的外生变量建模，未考虑风电场之间的空间相关性或地形影响。

（完）
