---
title: Latent Chain-of-Thought Improves Structured-Data Transformers
title_zh: 隐式思维链提升结构化数据 Transformer
authors: "Carson Dudley, Samet Oymak"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11262v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 针对时间序列和表格数据的潜思维链
tldr: 本研究受大语言模型思维链（CoT）成功的启发，探索了在时间序列和表格等结构化数据中应用潜在思维链的效果。作者提出一种循环机制，通过将隐藏状态压缩为反馈令牌并重新输入模型，实现多轮潜在计算以增强模型表达能力。在36个数据集上的实验表明，该方法显著优于同等深度的基准模型及循环Transformer，证明了思维链是扩展结构化数据推理计算量的有效手段。
selection_source: fresh_fetch
motivation: 旨在验证思维链和增加推理时计算量是否能像在自然语言处理中一样，提升结构化数据模型的推理能力。
method: 提出一种将查询位置隐藏状态压缩为反馈令牌并循环输入模型的潜在思维链方案。
result: "在9个时间序列和27个表格数据集上，该方法分别带来了10.99%和5.31%的平均性能增益。"
conclusion: 潜在思维链是提升结构化数据Transformer性能的一种有效且可扩展的推理计算方式。
---

## 摘要
思维链以及更广泛的测试时计算已知能够增强语言模型的表达能力，并引领了推理领域的重大创新。受此启发，本文探讨了隐式思维链以及深度和循环对时间序列和表格数据的影响。我们提出了一种循环方案，其中结构化数据 Transformer 在初始前向传递后，将其查询位置的隐藏状态压缩为反馈标记，并将其附加到输入中再次处理，从而在预测前实现多轮隐式计算。我们将 CoT 模型与同等深度的无 CoT 基准模型、在有效深度上与 CoT 模型匹配的更深基准模型，以及具有权重共享循环但没有额外思维链标记的循环 Transformer 进行了对比。在时间序列预测和表格预测的 36 个数据集上，隐式思维链在 8/9 个时间序列数据集（平均提升 10.99%）和 22/27 个表格数据集（平均提升 5.31%）上优于基准模型。在这两种设置下，CoT 模型平均表现最佳。这些结果表明，思维链是扩展结构化数据测试时计算的一个有效维度。

## Abstract
Chain-of-thought and more broadly test-time compute are known to augment the expressive capabilities of language models and have led to major innovations in reasoning. Motivated by this success, this paper explores latent chain-of-thought as well as the impact of depth and looping for time-series and tabular data. We propose a recurrent scheme in which a structured-data transformer, after an initial forward pass, compresses its query-position hidden states into feedback tokens that are appended to the input and processed again, allowing multiple rounds of latent computation before prediction. We compare CoT models against a same-depth no-CoT baseline, a deeper baseline matched to the CoT model in effective depth, and a looped transformer with weight-tied recurrence but no additional chain-of-thought tokens. Across 36 datasets in time-series forecasting and tabular prediction, latent chain-of-thought improves over the baseline on 8/9 time-series datasets (+10.99\% average gain) and 22/27 tabular datasets (+5.31\% average gain). Across both settings, the CoT models perform the best on average. These results demonstrate that chain-of-thought is a useful axis for scaling test-time compute for structured data.

---

## 论文详细总结（自动生成）

这是一份关于论文《Latent Chain-of-Thought Improves Structured-Data Transformers》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题是：**大语言模型（LLM）中通过“思维链（CoT）”增加测试时计算量（Test-time Compute）的成功经验，能否迁移到处理结构化数据（如时间序列和表格数据）的 Transformer 模型中？**

研究背景在于，虽然 OpenAI o1 等模型证明了增加推理步骤能显著提升逻辑推理能力，但结构化数据模型通常仍采用单次前向传递。作者试图探索一种“隐式思维链（Latent CoT）”机制，让模型在做出最终预测前进行多轮内部“思考”，从而提升其在复杂预测任务中的表现。

### 2. 核心方法论
作者提出了一种基于**循环反馈（Recurrent Feedback）**的隐式思维链方案：
*   **核心思想**：不要求模型输出自然语言，而是将模型在查询位置（Query positions）的隐藏状态（Hidden States）视为“潜在想法”，经过压缩后重新作为输入反馈给模型。
*   **算法流程**：
    1.  **初始传递**：输入原始数据，Transformer 生成初始隐藏状态。
    2.  **状态压缩**：提取预测位置的隐藏状态，通过一个小型多层感知机（MLP）将其压缩为“反馈标记（Feedback Tokens）”。
    3.  **序列拼接**：将这些反馈标记附加到原始输入序列的末尾。
    4.  **循环计算**：将更新后的序列再次输入同一个 Transformer（权重共享）。
    5.  **最终预测**：经过 $R$ 轮循环后，由预测头从最后的隐藏状态中解码出结果。
*   **关键细节**：该方法在表格任务中应用于 TabICL 的上下文学习阶段，在时间序列任务中应用于 Patch-based Transformer 的预测阶段。

### 3. 实验设计
*   **数据集**：
    *   **时间序列**：LTSF-9 基准测试中的 9 个标准长程预测数据集（如 ETT, ECL, Traffic 等）。
    *   **表格数据**：OpenML 数据库中的 27 个子集（涵盖二分类、多分类和回归任务）。
*   **对比方法（Baselines）**：
    1.  **同深度基准（Baseline）**：$L$ 层 Transformer，单次前向传递。
    2.  **更深基准（Deeper）**：$2L$ 层 Transformer，匹配 CoT 模型的总计算量，但参数量翻倍且无循环。
    3.  **循环基准（Looped）**：权重共享的循环 Transformer，但**不添加**额外的反馈标记（仅重复处理原始序列）。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到使用 AdamW 优化器，学习率为 $3 \times 10^{-4}$，Batch Size 为 128，最大训练 100 个 Epoch 并使用早停（Early Stopping）机制。

### 5. 实验数量与充分性
*   **实验规模**：共在 36 个数据集上进行了测试，涵盖了不同的任务类型（分类、回归、预测）。
*   **消融与深度分析**：
    *   测试了不同的循环深度 $R \in \{0, 1, 2, 4, 8\}$。
    *   对比了训练深度与测试深度不一致的情况（泛化性测试）。
    *   对比了四种不同的循环基准配置（Universal Transformer 风格 vs. 单块循环）。
*   **客观性**：所有模型均从零开始训练（From scratch），以排除预训练规模带来的干扰，确保了对比的公平性。

### 6 主要结论与发现
*   **性能提升**：隐式 CoT 在 8/9 的时间序列数据集上取得领先（平均提升 **10.99%**），在 22/27 的表格数据集上胜出（平均提升 **5.31%**）。
*   **优于更深模型**：CoT 模型在平均排名上优于参数量更多的“更深基准”，说明增益并非单纯来自计算量，而是来自循环机制。
*   **反馈标记的必要性**：CoT 模型优于“循环基准（Looped）”，证明了将中间计算结果作为显式标记（Tokens）存入序列的重要性——这为模型提供了存储中间推理结果的“草稿纸”。
*   **外推能力**：在 $R=4$ 训练的模型在 $R=8$ 推理时仍能保持甚至提升性能，显示了循环更新的泛化能力。

### 7. 优点
*   **创新性**：首次将隐式思维链的概念系统性地引入到非语言的结构化数据 Transformer 中。
*   **参数高效**：通过权重共享（Weight-tying），在不增加参数量的前提下，通过增加推理时间显著提升了模型能力。
*   **实验严谨**：通过设置“更深基准”和“无标记循环基准”，清晰地隔离了计算量、参数量和 CoT 机制各自的贡献。

### 8. 不足与局限
*   **规模限制**：实验主要在中小规模数据集上从零训练，尚未在大规模预训练基础模型（Foundation Models）上验证其有效性。
*   **推理延迟**：增加循环次数会线性增加推理延迟，这在实时性要求高的场景下可能是个障碍。
*   **缺乏可解释性**：目前尚不清楚反馈标记（Feedback Tokens）具体编码了什么信息（是残差、不确定性还是特征细化），缺乏深层的机械解释。
*   **固定步数**：循环深度 $R$ 是预设的，而非根据输入难度动态调整（Adaptive Pondering）。

（完）
