---
title: Efficient and Adaptive Human Activity Recognition via LLM Backbones
title_zh: 基于大语言模型骨干网络的高效自适应人体活动识别
authors: "Aleksandr Bredikhin, Philippe Lalanda, German Vega"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12019v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: LLM作为传感器时间序列的通用时间主干
tldr: 本研究针对人体活动识别（HAR）中模型训练成本高、数据需求大及跨域适应性差的问题，提出了一种将预训练大语言模型（LLM）作为通用时间序列骨干网络的新范式。通过引入结构化卷积投影将多维传感器信号映射至LLM潜空间，并结合LoRA参数高效微调技术，在保持骨干网络冻结的同时实现了高效适配。实验证明，该方法在低数据和少样本场景下具有极高的收敛速度、数据效率和跨数据集迁移能力，为复用基础模型解决传感器感知任务开辟了新路径。
selection_source: fresh_fetch
motivation: 传统HAR模型依赖从头训练的任务特定架构，导致计算成本高、数据需求大且在领域偏移下鲁棒性不足。
method: 利用冻结的预训练LLM作为时间序列骨干，通过卷积投影层弥合模态差异，并采用LoRA进行参数高效的适配。
result: 该方法在多个基准数据集上实现了快速收敛和强数据效率，尤其在少样本和跨数据集迁移任务中表现优异。
conclusion: 研究证明了LLM可以作为HAR系统的高效、节俭且可扩展的基础，展示了跨领域复用基础模型的巨大潜力。
---

## 摘要
人体活动识别（HAR）是普适计算系统中的核心任务，模型必须在严格的计算限制下运行，同时对异构且不断演变的部署条件保持鲁棒性。近期基于 Transformer 架构的进展显著提升了识别性能，但通常依赖于从头开始训练的任务特定模型，导致高昂的训练成本、庞大的数据需求以及对领域偏移的有限适应性。在本文中，我们提出了一种范式转变，即重用预训练大语言模型（LLMs）作为基于传感器的 HAR 的通用时间骨干网络，而不是设计领域特定的 Transformer。为了弥合惯性时间序列与语言模型之间的模态鸿沟，我们引入了一种结构化卷积投影，将多元加速度计和陀螺仪信号映射到 LLM 的潜空间中。预训练骨干网络保持冻结，并使用参数高效的低秩自适应（LoRA）进行微调，从而大幅减少了可训练参数的数量和整体训练成本。通过在标准 HAR 基准数据集上的广泛实验，我们证明该方法能够实现快速收敛、强大的数据效率以及鲁棒的跨数据集迁移，特别是在低数据和少样本场景下。同时，我们的结果强调了卷积前端与 LLM 的互补作用，其中局部不变性在信号层面处理，而长程时间依赖性则由预训练骨干网络捕获。总之，这项工作证明了 LLM 可以作为自适应 HAR 系统的一种实用、节俭且可扩展的基础，为在原始语言领域之外重用基础模型开辟了新方向。

## Abstract
Human Activity Recognition (HAR) is a core task in pervasive computing systems, where models must operate under strict computational constraints while remaining robust to heterogeneous and evolving deployment conditions. Recent advances based on Transformer architectures have significantly improved recognition performance, but typically rely on task-specific models trained from scratch, resulting in high training cost, large data requirements, and limited adaptability to domain shifts. In this paper, we propose a paradigm shift that reuses large pretrained language models (LLMs) as generic temporal backbones for sensor-based HAR, instead of designing domain-specific Transformers. To bridge the modality gap between inertial time series and language models, we introduce a structured convolutional projection that maps multivariate accelerometer and gyroscope signals into the latent space of the LLM. The pretrained backbone is kept frozen and adapted using parameter-efficient Low-Rank Adaptation (LoRA), drastically reducing the number of trainable parameters and the overall training cost. Through extensive experiments on standard HAR benchmarks, we show that this approach enables rapid convergence, strong data efficiency, and robust cross-dataset transfer, particularly in low-data and few-shot settings. At the same time, our results highlight the complementary roles of convolutional frontends and LLMs, where local invariances are handled at the signal level while long-range temporal dependencies are captured by the pretrained backbone. Overall, this work demonstrates that LLMs can serve as a practical, frugal, and scalable foundation for adaptive HAR systems, opening new directions for reusing foundation models beyond their original language domain.

---

## 论文详细总结（自动生成）

### 基于大语言模型骨干网络的高效自适应人体活动识别（HARLLM）总结

#### 1. 核心问题与研究动机
人体活动识别（HAR）是普适计算的核心，但当前主流的深度学习模型（尤其是 Transformer 架构）通常需要从零开始针对特定任务进行训练。这导致了以下问题：
*   **训练成本高**：需要大量的标注数据和计算资源。
*   **适应性差**：模型难以应对设备异构性、传感器位置变化及领域偏移（Domain Shift）。
*   **资源受限**：在物联网和可穿戴设备场景下，模型需满足严格的功耗和计算限制。
**研究动机**：探索是否可以重用预训练的大语言模型（LLM）作为通用的时间序列骨干网络，利用其强大的序列建模能力来处理传感器数据，从而实现高效、节俭且自适应的 HAR 系统。

#### 2. 核心方法论
论文提出了 **HARLLM** 框架，其核心思想是将 LLM 视为通用的时间推理器，通过模态对齐机制将传感器信号映射至 LLM 的潜空间。

*   **核心架构**：
    1.  **输入表示**：将三轴加速度计和陀螺仪信号（6通道）切分为固定长度的时间窗，并进一步细分为非重叠的“时间帧”（Temporal Frames）。
    2.  **卷积投影模块（Convolutional Projection Module）**：
        *   **模态特定编码**：独立处理加速度和陀螺仪信号，提取低级运动特征。
        *   **特征融合与多分支处理**：通过 1D 卷积栈捕捉不同抽象层级的局部动态。
        *   **线性投影**：将提取的特征映射到 LLM 的嵌入维度（如 GPT-2 的 768 维）。
    3.  **冻结骨干与 LoRA 适配**：
        *   使用预训练的 LLM（如 GPT-2 Small/Medium, Qwen-1.8B）作为骨干。
        *   **骨干网络完全冻结**，仅在自适应层（Query, Key, Value 投影）中插入 **LoRA（低秩自适应）** 模块。
        *   可训练参数量极低（以 GPT-2 Small 为例，仅占总参数量的不到 1%）。
    4.  **时间池化与分类**：对 LLM 输出的上下文嵌入进行均值池化，通过轻量级分类头输出活动类别。

#### 3. 实验设计
*   **数据集**：
    *   **HHAR**：包含异构设备（手机/手表）和多用户，用于评估数据效率和迁移能力。
    *   **UCI HAR**：受控环境下的标准数据集，用于评估绝对性能。
    *   **RealWorld**：传感器位置不固定，用于评估在极端现实环境下的鲁棒性。
*   **对比方法（Benchmarks）**：
    *   经典模型：CNN, DeepConvLSTM。
    *   Transformer 类：常规 Transformer, **HART**（专门针对 HAR 设计的 SOTA 模型）。
*   **实验场景**：全监督训练、低数据量训练（1% - 80% 数据）、留一数据集（LODO）跨域迁移实验。

#### 4. 资源与算力
*   **硬件环境**：实验在 **Google Colab 免费版**上完成，仅使用 **单块商用 GPU**。
*   **训练时长**：端到端训练时间始终控制在 **1 小时以内**。
*   **参数规模**：以 GPT-2 Small 为骨干时，可训练参数仅约 120 万个。
这证明了该方法对算力需求极低，适合学术原型开发和工业快速评估。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个主流基准数据集，进行了多组消融实验（不同数据比例、不同 LLM 骨干）。
*   **充分性**：通过 LODO 实验验证了跨数据集的泛化能力，通过 1% 极低数据量实验验证了少样本学习能力。
*   **客观性**：对比了当前 HAR 领域的 SOTA 模型（如 HART），实验设计遵循了标准的 HAR 评估协议，结果具有说服力。

#### 6. 主要结论与发现
*   **LLM 的跨界能力**：预训练 LLM 学习到的序列建模能力可以有效迁移至惯性传感器数据，充当高级时间推理器。
*   **互补作用**：卷积前端负责处理局部不变性和信号级特征，而 LLM 负责捕捉长程依赖和活动转换。
*   **高效性**：HARLLM 在 HHAR 数据集上达到了 SOTA 性能（F1-score 98.43%），且在仅有 10% 标注数据时仍能保持极高准确率。
*   **迁移优势**：在未见过的目标数据集上，即使不进行微调，预训练模型也展现了较强的零样本/少样本迁移潜力。

#### 7. 优点与亮点
*   **范式创新**：打破了“从头训练任务特定 Transformer”的传统，验证了 LLM 作为 HAR 基础模型的可行性。
*   **参数高效**：利用 LoRA 技术，在极小的计算开销下实现了高性能。
*   **数据节约**：在标注数据稀缺的现实场景中表现优异，具有极强的实用价值。

#### 8. 不足与局限
*   **局部不变性挑战**：在 RealWorld 数据集上，其表现略逊于 HART。这说明 LLM 无法自动习得传感器位置/方向的不变性，仍需依赖更强大的卷积前端设计。
*   **模态局限**：目前仅验证了加速度计和陀螺仪，对于更复杂的生理信号或其他多模态数据的泛化性尚待研究。
*   **依赖前端**：模型的上限在很大程度上受限于卷积投影模块的设计，若前端无法有效对齐模态，LLM 的性能将受限。

（完）
