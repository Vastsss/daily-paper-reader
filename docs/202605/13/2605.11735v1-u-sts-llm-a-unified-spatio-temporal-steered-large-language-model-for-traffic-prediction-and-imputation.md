---
title: U-STS-LLM A Unified Spatio-Temporal Steered Large Language Model for Traffic Prediction and Imputation
title_zh: U-STS-LLM：一种用于流量预测和插补的统一时空引导大语言模型
authors: "Yichen Zhang, Jun Li"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11735v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 适配LLM用于时空交通预测与插值
tldr: 本研究针对蜂窝网络流量分析中的预测与补全任务，提出了统一框架U-STS-LLM。该模型通过创新的动态时空注意力偏差生成器，将功能图与节点状态结合，显式引导大语言模型捕获时空结构。结合LoRA微调与门控自适应融合机制，U-STS-LLM在保持参数效率的同时实现了多任务联合学习。实验证明，该模型在长周期预测和高缺失率补全任务中均刷新了SOTA记录，为基础模型应用于非语言结构化领域提供了新范式。
selection_source: fresh_fetch
motivation: 旨在解决传统时空图神经网络泛化能力有限以及大语言模型在处理时空数据时缺乏结构化引导和收敛不稳定的问题。
method: 提出一种由动态时空注意力偏差生成器引导的统一LLM框架，利用LoRA微调和门控自适应融合机制实现高效的时空特征建模。
result: 在真实蜂窝网络数据集的实验中，U-STS-LLM在长周期预测和高缺失率数据补全任务上均取得了当前最优的性能表现。
conclusion: 该研究成功验证了通过引入显式时空引导，大语言模型可以高效且稳定地胜任复杂的非语言结构化数据分析任务。
---

## 摘要
现代蜂窝网络的高效运行取决于对时空流量数据的准确分析。掌握这些模式对于核心网络功能至关重要，主要包括预测未来负载以预防拥塞，以及插补因传感器故障或传输错误导致的缺失值以确保数据连续性。尽管预测和插补之间存在紧密联系，但它们在历史上一直作为独立的分支领域发展。主流范式时空图神经网络（STGNNs）虽然有效，但通常具有专用性、计算密集且泛化能力有限。与此同时，适配大型预训练语言模型（LLMs）为序列建模提供了一种强大的替代方案，但现有方法提供的结构引导较弱，导致收敛不稳定且过于关注预测任务。为了弥补这些差距，我们提出了 U-STS-LLM，这是一个基于时空引导 LLM 构建的统一框架。我们的核心创新是一个动态时空注意力偏差生成器，它将持久的功能图与瞬态节点状态相结合，以显式引导 LLM 的注意力。结合通过低秩自适应（LoRA）微调的部分冻结主干网络和门控自适应融合机制，该模型实现了稳定且参数高效的适配。在统一的多任务目标下训练，U-STS-LLM 学习到了整体的数据表示。在真实蜂窝数据集上的广泛实验表明，U-STS-LLM 在长周期预测和高缺失率插补方面均达到了新的最先进性能，同时保持了卓越的训练效率和稳定性，为在结构化、非语言领域利用基础模型提供了新的蓝图。

## Abstract
The efficient operation of modern cellular networks hinges on the accurate analysis of spatio-temporal traffic data. Mastering these patterns is essential for core network functions, chiefly forecasting future load to pre-empt congestion and imputing missing values caused by sensor failures or transmission errors to ensure data continuity. While deeply connected, forecasting and imputation have historically evolved as separate sub-fields. The dominant paradigm, Spatio-Temporal Graph Neural Networks (STGNNs), while effective, are often specialized, computationally intensive, and exhibit limited generalization. Concurrently, adapting large pre-trained language models (LLMs) offers a powerful alternative for sequence modeling, yet existing approaches provide weak structural guidance, leading to unstable convergence and a narrow focus on forecasting. To bridge these gaps, we propose U-STS-LLM, a unified framework built on a spatio-temporally steered LLM. Our core innovation is a Dynamic Spatio-Temporal Attention Bias Generator that synthesizes a persistent functional graph with transient nodal states to explicitly steer the LLM's attention. Coupled with a partially frozen backbone tuned via Low-Rank Adaptation (LoRA) and a Gated Adaptive Fusion mechanism, the model achieves stable, parameter-efficient adaptation. Trained under a unified multi-task objective, U-STS-LLM learns a holistic data representation. Extensive experiments on real-world cellular datasets demonstrate that U-STS-LLM establishes new state-of-the-art performance in both long-horizon forecasting and high-missing-rate imputation, while maintaining remarkable training efficiency and stability, offering a novel blueprint for harnessing foundation models in structured, non-linguistic domains.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《U-STS-LLM: A Unified Spatio-Temporal Steered Large Language Model for Traffic Prediction and Imputation》** 的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在 5G 及未来网络中，蜂窝流量数据具有高度的非平稳性、突发性和复杂的时空依赖性。目前该领域面临三大挑战：
*   **任务割裂**：流量预测（预测未来）和数据插补（补全缺失值）在历史上被视为独立任务，缺乏统一的建模框架。
*   **传统模型局限**：时空图神经网络（STGNNs）虽然有效，但通常针对特定数据集设计，计算开销大且泛化能力差。
*   **LLM 适配不足**：现有基于大语言模型（LLM）的方法在处理非语言的时空数据时，缺乏显式的结构引导，导致收敛不稳定，且大多仅关注预测任务，忽略了插补。

**研究动机**：利用 LLM 强大的序列建模和泛化能力，通过引入显式的时空引导机制，构建一个能够同时胜任预测与插补的统一框架。

### 2. 方法论：核心思想与关键技术
U-STS-LLM 的核心思想是**“时空引导（Steering）”**，即不改变 LLM 的核心架构，而是通过外部生成的偏差（Bias）来引导其注意力机制。

*   **任务自适应调节（Task-Adaptive Conditioning）**：引入动态输入缩放机制，根据输入样本的统计特性预测缩放因子，增强模型对不同数据量级的鲁棒性。
*   **时空特征嵌入与融合**：
    *   **感知路径**：通过 2D 卷积和分组 1D 卷积提取全局时空上下文。
    *   **引导路径**：利用 GRU（预测任务）或双向自回归 GRU（ARGRU，插补任务）生成初步的、节点级的稳定估计信号。
*   **动态时空注意力偏差生成器（核心创新）**：
    *   利用历史流量特征的相似度构建静态功能图。
    *   结合当前输入的瞬态节点状态，生成动态注意力偏差矩阵 $\tilde{B}$。
    *   将 $\tilde{B}$ 注入 LLM 的注意力计算中：$Attention(Q, K, V) = Softmax(\frac{QK^T}{\sqrt{d}} + \tilde{B})V$。
*   **部分冻结主干与 LoRA 微调**：冻结 GPT-2 的底层和前馈网络（FFN），仅对顶层注意力模块进行 LoRA（低秩自适应）微调，实现参数高效适配。
*   **门控自适应融合**：将 LLM 的深度精炼输出与初步引导信号进行加权融合，稳定训练过程。

### 3. 实验设计
*   **数据集**：使用意大利电信大数据挑战赛的真实数据集，包括 **Milan（米兰）** 和 **Trento（特伦托）**。
*   **场景**：
    *   **长周期预测**：基于过去 48 小时数据预测未来 24 小时。
    *   **高缺失率插补**：处理 70% - 80% 的随机数据缺失。
*   **Benchmark 与对比方法**：
    *   基础模型：LSTM, ConvLSTM。
    *   时间序列模型：DLinear, TimesNet。
    *   时空模型：STGCN。
    *   LLM 适配模型：GATGPT, GPT4ST, GCNGPT, ST-LLM, ST-LLM+。
    *   专用插补模型：BRITS, SAITS。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX A6000 GPU**。
*   **软件**：Python 3.8, PyTorch 2.4.1, CUDA 11.8。
*   **训练细节**：采用了 AdamW 优化器、线性预热（Warm-up）和余弦退火学习率策略，并引入了早停机制。文中强调了该模型在保持高性能的同时，具有较低的推理延迟和极少的训练参数量。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种不同的电信业务类型（SMS-IN/OUT, CALL-IN/OUT, Internet），每种业务均进行了预测和插补测试。
*   **消融实验**：针对时空嵌入（STE）、引导信号（GS）和图嵌入（GE）进行了三组消融实验，验证了各模块的必要性。
*   **泛化性测试**：进行了极具挑战性的 **Zero-shot（零样本）跨城市迁移实验**（在米兰训练，直接在特伦托测试）。
*   **充分性评价**：实验设计非常充分，不仅对比了 MAE 和 RMSE 等精度指标，还通过 UMAP 可视化分析了模型内部的特征表示，并对比了参数量与推理延迟，体现了客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：在预测任务中，MAE 和 RMSE 平均提升约 **23.8%**；在插补任务中，平均提升约 **12%**。
*   **零样本能力惊人**：在跨城市迁移实验中，U-STS-LLM 的表现远超所有基准模型，提升幅度达到 **35% - 40%**，证明其捕获的是通用的时空规律而非局部记忆。
*   **多任务协同**：统一训练目标使得模型能够学习到更稳健的数据流形表示，预测和插补任务互为促进。
*   **效率优势**：通过 LoRA 和部分冻结策略，模型在极少可训练参数下实现了 SOTA 性能，且推理速度快。

### 7. 优点
*   **架构优雅**：通过“注意力偏差”而非“重构架构”来引入时空先验，完美保留了 LLM 的预训练知识。
*   **泛化性极强**：Zero-shot 实验结果证明了该模型作为“时空基础模型”的潜力。
*   **实用性高**：统一了预测与插补，降低了网络运维中部署多个专用模型的成本。
*   **动态性**：动态偏差生成器能捕捉随时间变化的功能性节点关系，优于静态物理图。

### 8. 不足与局限
*   **空间预处理依赖**：实验中使用了 K-means 聚类将 10,000 个节点缩减为 200 个，虽然降低了计算压力，但可能损失了极细粒度的空间特征。
*   **LLM 选型单一**：主要基于 GPT-2 进行实验，尚未探讨在更大规模（如 Llama-3）或不同架构（如 Mamba）下的表现。
*   **应用领域限制**：目前仅在电信流量数据上验证，在交通流、气象预测等其他时空领域的普适性仍需进一步验证。

（完）
