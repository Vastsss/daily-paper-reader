---
title: "Pretraining Strategies and Scaling for ECG Foundation Models: A Systematic Study"
title_zh: 心电图（ECG）基础模型的预训练策略与扩展：一项系统性研究
authors: "M A Al-Masud, Nils Strodthoff"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12241v1"
tags: ["query:ts-fm"]
score: 10.0
evidence: 心电图生理时间序列基础模型预训练策略的系统研究
tldr: 本研究系统评估了心电图（ECG）基础模型的预训练策略与规模效应。通过对比五种自监督学习目标及高达1100万条公开样本的预训练过程，研究发现对比预测编码（CPC）在多样化临床任务中表现最稳健。此外，实验证明结构化状态空间模型（SSM）凭借其强大的归纳偏置，在性能上显著优于Transformer和CNN，为生理信号领域的基础模型开发提供了重要指导。
selection_source: fresh_fetch
motivation: 针对心电图基础模型，目前尚缺乏对其预训练方法、数据规模效应及模型架构优劣的系统性对比研究。
method: 在1100万条公开ECG样本上，对比了五种自监督学习目标以及CNN、Transformer和结构化状态空间模型（SSM）的性能。
result: 对比预测编码（CPC）表现最优，且SSM架构在ECG表征学习中展现出比Transformer和CNN更显著的优越性。
conclusion: 预训练策略的选择和模型架构的归纳偏置是构建高效ECG基础模型的关键，而非仅仅依赖于增加预训练数据规模。
---

## 摘要
专门的基础模型正开始在各个医学子领域出现，但预训练方法论以及随预训练数据集规模变化的参数扩展（parametric scaling）很少得到系统且同类对比的评估。本研究聚焦于心电图（ECG）数据的基础模型，ECG 是全球范围内捕获最广泛的生理时间序列之一。我们对预训练方法论进行了全面评估，涵盖了 ECG 基础模型的五种不同的对比式和非对比式自监督学习目标，并研究了它们在预训练数据集规模达到 1100 万个输入样本（全部来自公开来源）时的扩展行为。预训练策略对下游性能具有显著且一致的影响，其中对比预测编码（Contrastive Predictive Coding，略领先于 JEPA）在各种临床任务中产生了最具迁移性的表征。对于大多数目标函数，扩展预训练数据在达到 1100 万个样本时仍能持续产生显著的改进。我们还比较了所有预训练方法论下的模型架构，并发现有证据表明，与 Transformer 和 CNN 模型相比，结构化状态空间模型（Structured State Space Models）具有明显的优越性。我们假设，结构化状态空间模型强大的归纳偏置（inductive biases），而非仅仅是预训练规模，是有效 ECG 表征学习的主要驱动力，这对该领域以及潜在的其他生理信号领域未来的基础模型开发具有重要意义。

## Abstract
Specialized foundation models are beginning to emerge in various medical subdomains, but pretraining methodologies and parametric scaling with the size of the pretraining dataset are rarely assessed systematically and in a like-for-like manner. This work focuses on foundation models for electrocardiography (ECG) data, one of the most widely captured physiological time series world-wide. We present a comprehensive assessment of pretraining methodologies, covering five different contrastive and non-contrastive self-supervised learning objectives for ECG foundation models, and investigate their scaling behavior with pretraining dataset sizes up to 11M input samples, exclusively from publicly available sources. Pretraining strategy has a meaningful and consistent impact on downstream performance, with contrastive predictive coding (slightly ahead of JEPA) yielding the most transferable representations across diverse clinical tasks. Scaling pretraining data continues to yield meaningful improvements up to 11M samples for most objectives. We also compare model architectures across all pretraining methodologies and find evidence for a clear superiority of structured state space models compared to transformers and CNN models. We hypothesize that the strong inductive biases of structured state space models, rather than pretraining scale alone, are the primary driver of effective ECG representation learning, with important implications for future foundation model development in this and potentially other physiological signal domains.

---

## 论文详细总结（自动生成）

这篇论文对心电图（ECG）基础模型的预训练策略、模型架构及数据规模效应进行了系统性的研究。以下是详细的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在生理信号（如ECG）领域，虽然基础模型（Foundation Models, FMs）层出不穷，但由于各模型在预训练目标、架构和数据规模上存在巨大差异，很难确定究竟是哪个因素驱动了性能提升。
*   **研究动机**：
    *   缺乏对不同自监督学习（SSL）目标的“同类对比”（Like-for-like）评估。
    *   不清楚预训练数据规模（Scaling）在ECG领域是否存在类似自然语言处理（NLP）的幂律定律。
    *   需要验证哪种模型架构（CNN、Transformer、SSM）最适合处理长序列生理信号。

### 2. 方法论与核心思想
*   **核心思想**：在统一的实验框架下，固定编码器架构和训练配置，仅改变预训练目标或数据规模，从而实现公平对比。
*   **关键技术细节**：
    *   **五种预训练目标**：
        1.  **data2vec**：预测EMA（指数移动平均）教师模型的上下文表征。
        2.  **DinoSR**：结合在线聚类与掩码预测。
        3.  **JEPA**：在嵌入空间预测掩码区域的潜在表征。
        4.  **CPC (Contrastive Predictive Coding)**：通过对比学习预测未来的潜在状态。
        5.  **HuBERT++**：改进版HuBERT，使用Sinkhorn-Knopp算法进行软聚类分配，防止表征坍缩。
    *   **模型架构**：对比了 **S4 (结构化状态空间模型)**、**Transformer (带RoPE位置编码)** 和 **Net1D (CNN)**。
    *   **输入处理**：标准12导联ECG，采样率240Hz，输入长度2.5秒（600个时间步）。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用了HEEDB、HEEDB-Emory和CODE-15%三个公开数据集，总计约 **1100万条** 样本。
    *   **下游任务**：涵盖26个临床任务，包括成人/儿童ECG解释、心脏结构预测、临床预后预测、患者特征识别等。
*   **基准（Benchmark）**：
    *   **外部模型**：ECGFounder (CNN基座) 和 ECG-JEPA (Transformer基座)。
    *   **监督学习**：从头训练的S4模型作为强基准。
*   **评估模式**：全参数微调（Finetuning）、冻结特征提取（Frozen）和线性探测（Linear）。

### 4. 资源与算力
*   **硬件**：
    *   **预训练**：单张 **NVIDIA RTX PRO 6000 Blackwell Server Edition** GPU。
    *   **下游评估**：**NVIDIA L40** GPU。
*   **训练时长**：
    *   在1100万条数据上训练10个Epoch，不同目标的耗时在26.3小时（HuBERT++）到45.1小时（CPC）之间。
    *   小规模（10.6万条）预训练约需30-40分钟。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。研究涵盖了5种SSL目标、3种架构、5个数据规模梯度（1.8万到1100万）、26个下游任务，并进行了大量的消融实验（如学习率、模型维度、层数、输入长度等）。
*   **公平性**：研究采用了严格的“同类对比”，确保了除变量外的所有超参数一致。通过中心化内核对齐（CKA）分析了层间表征相似性，增加了实验的客观深度。

### 6. 主要结论与发现
*   **架构优越性**：**结构化状态空间模型（SSM/S4）显著优于 Transformer 和 CNN**。SSM展现出更强的归纳偏置，能更好地捕捉ECG的长时间序列特征。
*   **策略影响**：**CPC（对比预测编码）** 产生的表征最具迁移性，在多样化临床任务中表现最稳健，JEPA紧随其后。
*   **规模效应**：预训练数据规模在达到1100万样本时仍能带来性能提升，且**预训练验证损失与下游任务误差之间存在显著的正相关性**。
*   **微调建议**：在目标数据集上进行**领域自适应持续预训练（Domain-adaptive pretraining）**比单纯增加预测头复杂度更有效。

### 7. 优点（亮点）
*   **最大规模公开研究**：这是目前针对ECG基础模型最全面的预训练研究，且全部基于公开数据，具有极高的可复现性。
*   **架构洞察**：明确了SSM在生理信号处理中的潜力，挑战了Transformer在所有领域都最优的固有印象。
*   **系统性**：不仅关注准确率，还分析了标签效率、计算效率（FLOPs/推理延迟）和表征相似性。

### 8. 不足与局限
*   **监督信号单一**：研究仅关注纯自监督学习，未引入文本（如诊断报告）或其他模态的弱监督信号。
*   **超参数敏感性**：虽然做了消融，但某些模型（如data2vec）可能在特定超参数下表现更好，目前的结论可能受限于搜索空间。
*   **表征分析深度**：对学习到的生理特征缺乏更深层的生物医学解释，主要依赖于线性探测等统计指标。

（完）
