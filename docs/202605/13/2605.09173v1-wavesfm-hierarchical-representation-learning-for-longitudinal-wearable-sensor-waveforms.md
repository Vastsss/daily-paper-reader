---
title: "WavesFM: Hierarchical Representation Learning for Longitudinal Wearable Sensor Waveforms"
title_zh: WavesFM：针对纵向可穿戴传感器波形的分层表示学习
authors: "Peng Cao, Zhijian Yang, Tennison Liu, Jonathan Wang, Jiang Wu, Magdalena Proszewska, Arvind Pillai, Mingwu Gao, Amir Farjadian, Lawrence Cai, Emily Blanchard, Daniel McDuff, Pramod Rudrapatna, Matthew Thompson, Anupam Pathak, Mark Malhotra, Shwetak Patel, Dina Katabi, Paolo Di Achille, Ming-Zher Poh"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.09173v1"
tags: ["query:ts-fm"]
score: 9.0
evidence: 用于纵向可穿戴传感器波形的基础模型
tldr: 本研究针对可穿戴传感器生理信号（如PPG和加速度）的长序列、高频率及标注稀缺等挑战，提出了WavesFM基础模型。该模型采用两阶段自监督学习框架：首先通过片段级编码器提取局部波形特征，再利用时间编码器建模跨多日的长程动态变化。WavesFM在数百万小时的大规模数据集上进行预训练，在涵盖人口统计、生活方式及健康状况等58项任务中表现卓越，有效弥补了局部形态与长期行为模式建模之间的鸿沟。
selection_source: fresh_fetch
motivation: 现有的自监督学习方法难以同时兼顾高分辨率原始波形的局部细节与长达数周的生理信号宏观动态变化。
method: 提出一种分层自监督学习框架，通过片段级编码器提取局部嵌入，并结合时间编码器捕捉多日跨度的昼夜节律与长期生理演变。
result: 在超过680万小时的大规模数据集上预训练后，该模型在涉及健康、生活方式等58项下游预测任务中均取得了领先性能。
conclusion: WavesFM证明了分层表示学习在处理大规模、长序列生理信号中的有效性，为可穿戴设备健康监测提供了强大的基础模型。
---

## 摘要
可穿戴传感器能够在自由生活条件下持续获取高分辨率生理波形，例如光电容积脉搏波和加速度计信号。然而，由于采样频率高、多模态依赖性以及极长的序列长度（例如数周的记录），加之地面真值标签的稀缺，从这些信号中推断健康相关的表型面临着重大挑战。为了应对这些挑战，现有的自监督学习（SSL）方法通常遵循两种范式：（1）从短波形片段中学习丰富的形态表示，但通过简单的聚合忽略了纵向动态；或者（2）从跨越较长时间跨度的粗粒度手工特征（如心率、步数）中建模行为模式，但放弃了原始波形中细微且具有预测性的特征。为了弥补这一差距，我们提出了 WavesFM，这是一种利用两阶段自监督学习框架处理纵向生理数据的基础模型。具体而言，我们将学习问题分解为两个阶段：首先，预训练一个片段级编码器以从短波形中提取局部嵌入；随后，训练一个时间编码器来建模这些嵌入在多日跨度内的序列。这种分层方法克服了高分辨率、长序列数据的计算复杂性，使整体模型能够同时捕捉局部信号语义以及主导生理动态的复杂昼夜和日间变化。WavesFM 在第一阶段使用了超过 680 万小时（N=32.4 万人）的记录进行预训练，在第二阶段使用了 530 万小时（N=1 万人）的记录进行预训练，在涵盖人口统计学、生活方式、健康状况和药物治疗的 58 个不同任务中展现了卓越的性能。

## Abstract
Wearable sensors enable the continuous acquisition of high-resolution physiological waveforms, such as photoplethysmography and accelerometry, under free-living conditions. However, inferring health-related phenotypes from these signals presents significant challenges due to high sampling frequencies, multimodal dependencies, and extreme sequence lengths (e.g., weeks of recordings), compounded by a scarcity of ground-truth labels. To address these challenges, existing self-supervised learning (SSL) methodologies typically follow two paradigms: (1) learning rich morphological representations from short waveform segments while collapsing longitudinal dynamics through simple aggregation, or (2) modeling behavioral patterns from coarse, hand-crafted features (e.g. heart rate, step counts) spanning longer horizons but foregoing subtle, predictive signatures in raw waveforms. To bridge this gap, we propose WavesFM, a foundation model utilizing a two-stage SSL framework for longitudinal physiological data. Specifically, we decompose the learning problem into two stages: first, a segment-level encoder is pretrained to extract local embeddings from short waveforms; subsequently, a temporal encoder is trained to model the sequence of these embeddings across a multi-day horizon. This hierarchical approach overcomes the computational complexity of high-resolution, long-sequence data, allowing the overall model to capture both local signal semantics and the complex circadian and inter-day variations governing physiological dynamics. Pretrained on over 6.8M hours (N=324k individuals) of recordings for the first stage and 5.3M hours (N=10k) for the second stage, WavesFM demonstrates superior performance across 58 diverse tasks spanning demographics, lifestyle, health conditions, and medications.

---

## 论文详细总结（自动生成）

以下是对论文《WavesFM: Hierarchical Representation Learning for Longitudinal Wearable Sensor Waveforms》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从可穿戴设备（如智能手表）产生的**高频率、多模态、超长纵向生理信号**（如持续数周的 PPG 和加速度计数据）中，在**标注稀缺**的情况下，有效地提取能够反映健康表型的特征。
*   **研究动机**：
    *   **分辨率权衡**：现有方法要么只关注短时间内的波形形态（形态中心），忽略了长期的动态变化；要么只关注粗粒度的统计指标（行为中心，如每分钟步数），丢失了原始波形中的细微生理特征。
    *   **计算挑战**：一周 100Hz 的传感器数据包含约 $10^8$ 个数据点，直接进行端到端处理在计算上是不可行的。
    *   **标签不对称**：海量的无标签生理流数据与极度稀缺的临床诊断标签之间存在巨大鸿沟。

### 2. 方法论：分层自监督学习框架
WavesFM 采用两阶段自监督学习（SSL）框架，将问题分解为局部形态学习和全局动态建模：
*   **第一阶段：片段级表示学习（Stage I）**
    *   **核心思想**：学习 15 秒短片段内的局部形态特征。
    *   **技术细节**：使用 EfficientNet-B7 作为编码器，输入 PPG 和加速度计（ACC）波形。
    *   **目标函数**：采用**受试者对比学习（Subject-Contrastive Learning）**。将同一受试者的不同时间段信号视为正样本对，不同受试者的信号视为负样本对，强制模型提取受试者特有的稳定生理特征。
*   **第二阶段：时间动态学习（Stage II）**
    *   **核心思想**：在第一阶段生成的嵌入空间中，建模跨越一周的长程生理节律。
    *   **关键技术**：
        *   **时间分箱**：将 15 秒的嵌入向量聚合为 5 分钟的 bin，形成一周 2016 个步长的序列。
        *   **分解位置编码**：引入“星期几”和“一天中的时间”两个可学习的查找表，显式注入昼夜节律和周节律先验。
        *   **掩码嵌入预测（MEP）**：采用多尺度稀疏掩码策略，随机遮盖部分时间点的嵌入。
        *   **双分支解码器**：包含一个**局部解码器**（通过交叉注意力重建目标）和一个**全局解码器**（通过均值池化瓶颈重建目标），旨在防止模型通过简单的线性插值“走捷径”，迫使编码器压缩长程健康动态。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：第一阶段使用 680 万小时（32.4 万人）数据；第二阶段使用 530 万小时（1 万人，6.1 万个完整周记录）数据。
    *   **下游评估**：使用 Fitbit 高血压研究的独立数据集，包含 11,736 名参与者，平均每人有 11.7 周的数据。
*   **任务设置**：共 58 项下游分类任务，涵盖人口统计学（年龄、性别、肥胖）、生活方式（吸烟、饮酒、饮食）、健康状况（房颤、糖尿病、抑郁等）和药物治疗（ACE 抑制剂、β 受体阻滞剂等）。
*   **对比方法（Baselines）**：
    *   **形态中心模型**：仅对第一阶段嵌入进行均值池化。
    *   **行为中心模型**：使用手工提取的生理特征（如心率、HRV、运动量）代替第一阶段嵌入，再进行第二阶段训练。
    *   **公共基础模型**：PaPaGei、Pulse-PPG、AnyPPG。
    *   **监督学习基准**：LSTM、交叉注意力机制等。

### 4. 资源与算力
*   **第一阶段**：在相当于 NVIDIA DGX-2 的集群（16 张 V100 GPU）上训练，耗时约 **96 小时**。
*   **第二阶段**：在单张 NVIDIA V100 GPU 上训练，耗时约 **24 小时**。
*   **推理/评估**：下游任务采用线性探测（Linear Probing），在冻结的嵌入上训练逻辑回归。

### 5. 实验数量与充分性
*   **实验规模**：论文在 58 个不同的临床和行为任务上进行了验证，实验规模巨大。
*   **消融实验**：针对双分支解码器、掩码比例、位置编码方式、周级嵌入策略等进行了详细的消融研究。
*   **鲁棒性测试**：专门设计了针对“数据缺失”的实验（如仅白天、仅夜间、仅周末数据），模拟真实可穿戴设备佩戴不规律的场景。
*   **客观性**：采用了受试者级别的划分（70/15/15）以防止数据泄露，并使用自助法（Bootstrap）计算 95% 置信区间，实验设计严谨、公平。

### 6. 主要结论与发现
*   **性能领先**：WavesFM 在 58 项任务中的平均排名为 1.44，显著优于所有基线模型。
*   **协同效应**：证明了同时建模微观形态和宏观节律的必要性。例如，在预测肥胖（形态相关）和酒精摄入（节律相关）方面，WavesFM 均表现出显著优势。
*   **节律捕捉**：可视化分析显示，第二阶段编码器成功捕捉到了完整的 24 小时昼夜生理节律，而仅靠形态特征只能区分睡眠和清醒。
*   **标签效率**：在仅有 10% 标注数据的情况下，WavesFM 的线性探测性能优于从头训练的监督学习模型。

### 7. 优点与亮点
*   **分层架构设计**：巧妙地解决了高频信号与长程建模之间的计算矛盾。
*   **双分支解码器**：通过结构化约束解决了自监督学习中常见的“捷径学习”问题。
*   **现实鲁棒性**：对传感器数据常见的缺失问题具有极强的容忍度。
*   **大规模验证**：使用了数百万小时的真实世界数据，任务覆盖面极广，具有很强的临床参考价值。

### 8. 不足与局限
*   **硬件依赖性**：模型假设 PPG 和 ACC 模态同时存在，且针对特定采样率设计，对异构硬件的泛化能力尚待验证。
*   **可解释性**：作为深度学习基础模型，其生成的稠密嵌入缺乏直接的临床解释性，难以说明具体是哪些信号特征驱动了特定疾病的预测。
*   **数据闭源**：由于涉及敏感健康数据，预训练和评估数据集均未公开，限制了社区的完全复现。
*   **急性事件检测**：目前模型侧重于稳定的慢性健康表型，对于急性事件（如心脏骤停）的瞬时检测能力未做评估。

（完）
