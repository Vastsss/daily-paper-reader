---
title: "SplitFT: An Adaptive Federated Split Learning System For LLMs Fine-Tuning"
title_zh: SplitFT：一种用于大语言模型微调的自适应联邦拆分学习系统
authors: "Yimeng Shan, Zhaorui Zhang, Sheng Di, Yu Liu, Xiaoyi Lu, Benben Liu"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26388v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 大语言模型微调的联邦拆分学习
tldr: 针对大语言模型微调中客户端资源受限和通信开销大的挑战，本文提出 SplitFT 系统。该系统采用自适应联邦拆分学习框架，允许客户端根据算力和性能动态调整拆分层，并通过降低拆分层的 LoRA 秩来显著减少通信量。此外，研究还引入了基于长度的 Dirichlet 数据划分方法以应对数据异构性。实验表明，SplitFT 在微调效率和模型性能上均优于现有先进方法。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在联邦拆分学习微调过程中，因设备异构导致的拆分层难以适配及通信开销过大的核心痛点。
method: 开发了 SplitFT 系统，通过自适应拆分层选择、拆分层 LoRA 秩缩减以及基于长度的数据划分策略来优化训练过程。
result: 在多个流行基准测试中，SplitFT 在微调时间效率和模型预测准确性方面均超越了当前最先进的技术方案。
conclusion: SplitFT 证明了通过自适应架构和通信优化，可以有效提升异构客户端环境下大语言模型联邦微调的可行性与效率。
---

## 摘要
联邦拆分学习已被证明是解决经典联邦学习中客户端计算资源限制的有效方法，同时能保证跨数据所有者的分布式模型训练中的数据隐私。然而，当这种训练策略应用于大语言模型（LLMs）微调时，面临着一些严峻挑战。这些挑战包括在不同客户端之间自适应地设置切分层，以解决显著影响系统性能的数据和设备异构性问题。此外，在微调过程中有效减少通信开销也是另一个挑战。目前尚无工作尝试解决这些挑战。为了填补这一空白，我们提出了 SplitFT，一种用于大语言模型微调的自适应联邦拆分学习系统。SplitFT 允许不同客户端根据其计算资源和训练模型性能设置不同的切分层。SplitFT 还提出降低切分层中的 LoRA 秩，以减少通信开销。除了为我们提出的联邦拆分学习系统模拟现实应用中的异构数据外，我们还提出了一种基于长度的狄利克雷方法，将训练数据划分到不同的客户端。大量的实验结果表明，在各种流行基准测试中，我们提出的方法在微调时间效率和模型性能方面均优于现有最先进的方法。

## Abstract
Federated Split Learning has been identified as an efficient approach to address the computational resource constraints of clients in classical federated learning, while guaranteeing data privacy for distributed model training across data owners. However, it faces some critical challenges when such a training strategy meets large language models (LLMs) for fine-tuning. Such challenges include setting the cutlayer adaptively across different clients to address the data and device heterogeneity issues, which affect the system performance significantly. In addition, efficiently reducing the communication overhead during the fine-tuning procedure is also another challenge. No work tries to address these challenges.   To bridge this gap, we propose SplitTF, an adaptive federated split learning system for LLMs fine-tuning. SplitFT enables different clients to set different cut layers according to their computation resources and trained model performance. SplitFT also proposes to reduce the LoRA rank in cutlayer to reduce the communication overhead. In addition to simulating the heterogeneous data in real-world applications for our proposed split federated learning system, we propose a length-based Dirichlet approach to divide the training data into different clients. Extensive experimental results show that our proposed approach outperforms the state-of-the-art approach for fine-tuning time efficiency and model performance based on various popular benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《SplitFT: An Adaptive Federated Split Learning System For LLMs Fine-Tuning》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：在大语言模型（LLMs）微调场景下，传统的联邦学习（FL）要求客户端具备极高的计算能力以承载完整模型，而联邦拆分学习（FSL）虽然缓解了这一压力，但在面对**设备异构性**（不同客户端算力不同）、**通信开销巨大**（拆分层传输激活值和梯度）以及**数据异构性**（非独立同分布数据）时仍存在显著瓶颈。
*   **研究背景**：随着 LLM 规模扩大，边缘设备难以独立完成微调。现有的拆分学习方案通常采用固定拆分层，无法适应动态变化的客户端资源，且未针对 LLM 微调特有的通信需求进行优化。

### 2. 方法论
SplitFT 系统提出了三个核心创新点：
*   **自适应拆分层选择（Adaptive Cut-layer Selection）**：允许不同客户端根据自身的实时计算资源（如显存、算力）和训练过程中的模型性能表现，动态调整模型拆分的位置。这确保了算力强的设备承担更多计算，算力弱的设备减少负担。
*   **拆分层 LoRA 秩缩减（LoRA Rank Reduction in Cut-layer）**：针对拆分学习中频繁传输激活值和梯度的通信瓶颈，SplitFT 在拆分层引入 LoRA 结构，并专门降低该层的 LoRA 秩（Rank）。通过减少低秩矩阵的维度，显著压缩了需要在客户端与服务器之间传输的数据量。
*   **基于长度的狄利克雷数据划分（Length-based Dirichlet Partitioning）**：为了模拟现实中高度异构的数据分布，提出了一种基于文本长度的划分方法。该方法利用狄利克雷分布将不同长度特征的数据分配给不同客户端，以测试系统在极端数据异构下的鲁棒性。

### 3. 实验设计
*   **数据集与场景**：论文在多个流行基准测试（Benchmarks）上进行了验证（摘要中未列出具体名称，通常包含 GLUE、SQuAD 或类似 LLM 评估集）。
*   **对比方法（Baselines）**：对比了当前最先进的（State-of-the-art）联邦学习和拆分学习微调方案。
*   **评估指标**：主要关注**微调时间效率**（训练速度、通信耗时）和**模型预测准确性**（性能表现）。

### 4. 资源与算力
*   **算力说明**：根据提供的摘要和元数据，文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）、GPU 数量及具体的训练总时长。通常此类研究会在实验设置章节详细列出，但当前提取文本中未包含这些细节。

### 5. 实验数量与充分性
*   **实验规模**：论文声称进行了“大量的实验结果”（Extensive experimental results），涵盖了多种基准测试。
*   **充分性评价**：
    *   **多维度验证**：实验不仅关注了模型精度，还重点考察了时间效率，这对于分布式系统研究至关重要。
    *   **消融/对比**：通过与 SOTA 方法对比，证明了自适应策略和通信优化策略的有效性。
    *   **异构性模拟**：引入基于长度的数据划分，增强了实验在模拟现实复杂环境下的说服力。

### 6. 主要结论与发现
*   **效率提升**：SplitFT 显著降低了微调过程中的通信开销，提高了整体训练效率。
*   **性能优越**：在处理异构设备和异构数据时，SplitFT 的模型预测准确性优于现有的固定拆分方案。
*   **自适应价值**：动态调整拆分层能更好地平衡系统负载，避免了“木桶效应”（即弱设备拖慢整体进度）。

### 7. 优点
*   **灵活性高**：打破了传统拆分学习中所有客户端必须在同一层拆分的限制。
*   **通信优化精准**：巧妙地结合了 LoRA 技术与拆分学习，通过降低特定层的秩来解决最核心的通信痛点。
*   **现实模拟度高**：提出的基于长度的数据划分方法比传统的随机划分更贴近 LLM 应用的真实场景。

### 8. 不足与局限
*   **同步开销**：虽然减少了单次传输量，但自适应调整拆分层可能会增加服务器端的协调复杂度和同步等待时间。
*   **隐私风险**：虽然拆分学习保护了原始数据，但拆分层的激活值传输仍可能存在潜在的梯度泄露风险（文中未详细讨论防御机制）。
*   **硬件细节缺失**：摘要部分未提供具体的硬件资源消耗数据，难以直观评估其在极低功耗设备上的可行性。

（完）
