---
title: "PAMod: Modeling Cyclical Shifts via Phase-Amplitude Modulation for Non-stationary Time Series Forecasting"
title_zh: PAMod：通过相位-振幅调制为非平稳时间序列预测建模周期性偏移
authors: "Yingbo Zhou, Yutong Ye, Shuhao Li, Rui Qian, Qiang Huang, Lemao Liu, Li Sun, Dejing Dou"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00466v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 非平稳时间序列预测
tldr: 针对非平稳时间序列预测中分布随时间周期性偏移的挑战，本文提出PAMod框架。该方法在归一化特征空间中引入相位-振幅调制机制，通过相位调制捕捉均值偏移，振幅调制适应方差变化。PAMod在数学上等价于动态反归一化，作为一种轻量级且即插即用的技术，在12个基准数据集上显著提升了预测精度并达到SOTA水平。
selection_source: fresh_fetch
motivation: 传统归一化方法假设历史与未来分布一致，无法有效处理现实应用中随周期性位置（如季节、节假日）变化的非平稳分布偏移。
method: 在归一化特征空间中利用周期性嵌入进行相位和振幅调制，分别对表示进行平移和缩放以模拟均值和方差的动态变化。
result: 实验证明PAMod在12个真实世界基准上均取得最优性能，且相比现有方法更节省计算资源，并能显著增强其他预测模型的表现。
conclusion: PAMod通过将分布自适应与表示学习优雅地统一，为非平稳时间序列预测提供了一种高效、通用的周期性偏移建模方案。
---

## 摘要
现实世界的时间序列预测面临着非平稳统计特性的根本挑战，包括均值和方差随时间的偏移。虽然可逆实例归一化（RevIN）通过对输入进行平稳化和对输出进行反归一化展现了潜力，但它依赖于历史分布与未来分布保持一致的强假设。我们观察到，在许多实际应用中，分布偏移遵循与周期性位置相关的循环模式（例如，季节性和节假日波动）。为此，我们提出了 PAMod，这是一个轻量级且强大的框架，通过在归一化特征空间中进行相位-振幅调制来建模周期性分布偏移。PAMod 学习周期性嵌入来调制表示：相位调制捕捉均值偏移，而振幅调制适应方差变化。至关重要的是，我们在数学上证明了在归一化空间中进行调制等同于应用动态反归一化，从而实现了分布自适应与表示学习的优雅统一。在十二个真实世界基准数据集上的广泛实验表明，PAMod 以更少的计算资源实现了最先进的性能。此外，我们的调制机制作为一种新颖的即插即用技术，可以通过简单的集成来改进现有的时间序列预测方法。

## Abstract
Real-world time series forecasting faces the fundamental challenge of non-stationary statistical properties, including shifts in mean and variance over time. While reversible instance normalization (RevIN) has shown promise by stationarizing inputs and denormalizing outputs, it relies on the strong assumption that historical and future distributions remain identical. We observe that in many practical applications, distribution shifts follow cyclical patterns that correlate with periodic positions (e.g., seasonal and holiday volatility). To this end, we propose PAMod, a lightweight yet powerful framework that models cyclical distribution shifts via Phase-Amplitude Modulation in the normalized feature space. PAMod learns periodic embeddings to modulate representations: phase modulation captures mean shifts, while amplitude modulation adapts to variance changes. Crucially, we prove mathematically that modulating in normalized space is equivalent to applying dynamic denormalization, offering an elegant unification of distribution adaptation and representation learning. Extensive experiments on twelve real-world benchmarks demonstrate that PAMod achieves state-of-the-art performance with fewer computational resources. Furthermore, our modulation mechanism, as a novel plug-and-play technique, can improve existing time-series forecasting methods with simple integration.

---

## 论文详细总结（自动生成）

以下是对论文《PAMod: Modeling Cyclical Shifts via Phase-Amplitude Modulation for Non-stationary Time Series Forecasting》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现实世界的时间序列具有**非平稳性**（均值和方差随时间变化），导致模型难以泛化。
*   **研究背景**：现有的归一化方法（如 RevIN）假设历史和未来的分布是一致的，这在存在周期性波动（如季节、节假日）的场景下并不成立。
*   **动机**：作者观察到分布偏移往往遵循与周期性位置相关的**结构化循环模式**。因此，与其盲目消除非平稳性，不如显式地建模这种可预测的周期性分布偏移。

### 2. 方法论：相位-振幅调制 (PAMod)
*   **核心思想**：借鉴通信理论中的相位-振幅调制，在归一化特征空间中引入可学习的周期性嵌入，动态调整表示的均值和方差。
*   **关键技术细节**：
    *   **可学习周期嵌入**：根据预定义的周期长度 $L$（如 24 小时或 168 小时），通过查找表（Lookup Table）获取当前时间点在周期内的嵌入向量。
    *   **相位调制 (Phase Modulation)**：通过**加法**变换捕捉系统性的均值偏移（Mean Shifts）。
    *   **振幅调制 (Amplitude Modulation)**：通过**乘法**缩放适应方差的变化（Variance Changes）。
    *   **数学等价性**：论文在理论上证明了在归一化空间中进行这种调制，等价于应用一种**动态反归一化**，从而将分布自适应与表示学习统一。
*   **流程**：输入序列 $\rightarrow$ 实例归一化 $\rightarrow$ 周期嵌入引导的相位-振幅调制 $\rightarrow$ MLP 预测头 $\rightarrow$ 反归一化输出。

### 3. 实验设计
*   **数据集**：涵盖了 12 个真实世界基准数据集，包括 ETT (m1, m2, h1, h2)、Electricity (ECL)、Traffic、Solar-Energy、Weather 以及 4 个 PEMS 交通数据集。
*   **Benchmark 设置**：预测长度 $H \in \{96, 192, 336, 720\}$（PEMS 为 $\{12, 24, 48, 96\}$），回顾窗口固定为 96。
*   **对比方法**：对比了 9 种最新的 SOTA 模型，包括 TQNet (2025)、TimeEmb (2025)、FilterTS (2025)、iTransformer (2024)、PatchTST (2023) 等。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 4090 GPU (24GB)**。
*   **训练细节**：使用 Adam 优化器，训练 30 个 epoch，采用早停机制（patience=5）。
*   **效率表现**：PAMod 极其轻量，参数量比基于 Transformer 的方法少 **5-10 倍**。在 Traffic 数据集（862 个变量）上，其内存占用和训练速度均显著优于 iTransformer 和 PatchTST。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在 12 个数据集、多个预测步长下进行了详尽对比。
    *   **消融实验**：验证了相位/振幅组件、不同损失函数、周期长度 $\tau$ 的影响。
    *   **兼容性实验**：将 PAMod 作为插件集成到 DLinear、PatchTST、iTransformer 等模型中，均观察到性能提升。
    *   **可视化分析**：通过 t-SNE 展现了学习到的嵌入权重如何解耦均值和方差。
*   **评价**：实验设计非常充分且客观。作者不仅关注精度（MSE/MAE），还深入探讨了模型在不同回顾窗口下的敏感性以及计算效率，证明了方法的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：PAMod 在绝大多数基准测试中达到了新的 SOTA，MSE 降低了 4%–16%。
*   **即插即用**：作为一种通用的增强模块，PAMod 能显著提升现有主流预测模型的性能，且不增加过多计算负担。
*   **周期性重要性**：实验证明，当调制参数 $\tau$ 与数据集真实的物理周期（如 24 小时）对齐时，效果最佳。

### 7. 优点与亮点
*   **理论优雅**：将通信领域的调制理论引入时间序列归一化，并给出了严谨的数学证明。
*   **极高效率**：在保持高精度的同时，极大地降低了参数量和计算开销，适合大规模多变量场景。
*   **解耦建模**：成功将非平稳性分解为可解释的均值偏移（相位）和方差波动（振幅）。

### 8. 不足与局限
*   **固定周期假设**：模型依赖于预定义的周期长度 $L$。对于周期不明显、存在多个重叠周期或周期随时间漂移的序列，效果可能会受限。
*   **通道共享周期**：目前设计中所有通道共享相同的周期长度，但在某些复杂系统中，不同变量可能具有不同的周期特性。
*   **对周期错位的敏感性**：如果周期长度设置错误，模型捕捉结构化偏移的能力会下降。

（完）
