---
title: Deployment-Efficient Short-Term Load Forecasting in AI Data Centers via Sequence-to-Point Knowledge Distillation
title_zh: 通过序列到点知识蒸馏实现 AI 数据中心的高效部署短期负荷预测
authors: "Lei Wang, Jiahao Chen, Fanping Sui, Ying Zhang, Di Shi"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00681v1"
tags: ["query:llm"]
score: 9.0
evidence: AI数据中心短期负荷预测与电网协调
tldr: 针对AI数据中心GPU节点级负载波动剧烈且难以预测的问题，本文提出一种高效部署的知识蒸馏框架。该框架利用高容量序列教师模型捕捉非平稳负载动态，并通过序列到点的蒸馏策略，将时间知识迁移至轻量化学生模型。实验表明，该方法在提升预测精度的同时，将模型参数和内存占用降低了10倍以上，有效解决了高精度预测与低延迟部署之间的权衡难题。
selection_source: fresh_fetch
motivation: AI数据中心负载具有高度突发性和非平稳性，现有高容量模型难以满足大规模部署的内存和延迟要求。
method: 提出一种序列到点的知识蒸馏框架，通过对齐预测行为和时间池化表示，将复杂教师模型的时序知识迁移至轻量化学生模型。
result: 在MIT Supercloud数据集上，该学生模型在提升预测精度的同时，将模型参数和内存占用降低了10倍以上。
conclusion: 该研究通过知识蒸馏实现了高精度与轻量化的平衡，为AI数据中心的实时电力管理和电网协调提供了高效的预测方案。
---

## 摘要
准确预测 AI 数据中心突发且非平稳的电力需求变得日益重要，因为 GPU 节点级别由工作负载驱动的突发变化会影响实时运行效率、功率管理以及电网与数据中心的协调。然而，高容量预测模型由于内存和延迟要求通常难以大规模部署，而轻量级预测器可能无法捕捉短时域的时间动态。为了解决这种准确性与部署之间的权衡问题，本文提出了一种用于 AI 数据中心短期负荷预测的高效部署知识蒸馏框架。该框架首先训练一个用于多步负荷轨迹预测的高容量序列教师模型，其中利用残差学习来提高非平稳运行条件下的鲁棒性。随后，利用紧凑的神经网络架构开发了一个轻量级点对点学生模型，用于低延迟滚动推理。为了将时间知识从教师模型转移到学生模型，引入了一种序列到点蒸馏策略，通过对齐近期预测行为和时间池化表示来实现。对 MIT Supercloud 数据集的案例研究表明，所提出的学生模型在提高预测准确性的同时，比近期的深度学习基准模型在参数内存和模型大小方面的部署占用减少了 10 倍以上。

## Abstract
Accurately forecasting the bursty and non-stationary power demand of AI data centers has become increasingly important, as abrupt workload-driven variations at the GPU-node level can affect real-time operational efficiency, power management, and grid-data center coordination. However, high-capacity forecasting models are often difficult to deploy at scale because of their memory and latency requirements, while lightweight predictors may fail to capture short-horizon temporal dynamics. To address this accuracy-deployment tradeoff, this paper proposes a deployment-efficient knowledge distillation framework for short-term load forecasting in AI data centers. The proposed framework first trains a high-capacity sequence teacher model for multi-step load trajectory prediction, where residual learning is used to improve robustness under non-stationary operating conditions. A lightweight point-wise student model is then developed for low-latency rolling inference using a compact neural network architecture. To transfer temporal knowledge from the teacher to the student, a sequence-to-point distillation strategy is introduced by aligning near-term predictive behavior and temporally pooled representations. Case studies on the MIT Supercloud dataset demonstrate that the proposed student model improves forecasting accuracy over recent deep learning baselines while reducing the deployment footprint by over 10x in parameter memory and model size.

---

## 论文详细总结（自动生成）

### 论文总结：通过序列到点知识蒸馏实现 AI 数据中心的高效部署短期负荷预测

#### 1. 核心问题与整体含义
随着 AI 训练任务的激增，AI 数据中心的电力需求呈现出极强的**非平稳性**和**突发性**（如 GPU 节点级别的作业切换、硬件功率管理导致的负载骤增骤降）。
*   **研究动机**：准确的短期负荷预测对于实时作业调度和电网协调至关重要。
*   **核心挑战**：高容量模型（如 Transformer）虽然精度高，但内存和延迟开销大，难以在成千上万的 GPU 节点上大规模部署；而轻量级模型（如简单 MLP）往往难以捕捉复杂的时间动态。
*   **整体含义**：本文旨在通过“知识蒸馏”技术，将复杂模型的时序建模能力转移到极简模型中，解决高精度预测与低开销部署之间的矛盾。

#### 2. 方法论
论文提出了一种**序列到点（Sequence-to-Point）知识蒸馏框架**，核心步骤如下：
*   **残差学习框架**：不直接预测绝对负荷，而是预测未来负荷相对于当前观测值的增量（Residual），以提高对非平稳数据的鲁棒性。
*   **教师模型（Teacher）**：采用基于 Transformer Encoder 的高容量序列模型，输入历史序列（$L=360$），通过多头自注意力和注意力池化（Attention Pooling）捕捉长程依赖，输出多步预测轨迹。
*   **学生模型（Student）**：采用极简的两层多层感知器（MLP），仅接收当前时刻的点输入（Point-wise），旨在实现极低延迟的滚动推理。
*   **蒸馏策略**：
    *   **Logit 蒸馏**：将教师模型的多步预测轨迹投影为单步“软标签”，引导学生模型对齐教师的短期预测行为。
    *   **特征蒸馏**：将教师模型通过注意力池化提取的“时间上下文向量”投影到学生模型的特征空间，使仅接收点输入的学生模型也能获得序列层面的时间感知能力。

#### 3. 实验设计
*   **数据集**：使用 **MIT Supercloud 数据集**，包含高性能计算（HPC）系统中 GPU 加速节点的真实遥测数据（GPU/内存利用率、温度、作业统计等），分辨率为 1 分钟。
*   **Benchmark（基准方法）**：对比了近期文献中常用的深度学习模型，包括 **LSTM** 和 **GRU**。
*   **对比维度**：
    *   **预测精度**：平均绝对误差（MAE）、均方根误差（RMSE）。
    *   **部署效率**：参数量（Params）、FP32 内存占用、磁盘模型大小、CPU 推理延迟（均值及 P95 分位数）。

#### 4. 资源与算力
*   **硬件环境**：实验在配备 Intel Xeon E5-2680 v4 CPU (2.4 GHz) 和 **NVIDIA GeForce RTX 3080 Ti GPU** 的工作站上完成。
*   **训练细节**：使用 AdamW 优化器，初始学习率为 $1 \times 10^{-4}$。
*   **算力说明**：论文未明确给出总训练时长，但从模型规模（教师 69K 参数，学生 6.1K 参数）来看，该方案的训练成本较低，重点在于推理端的轻量化。

#### 5. 实验数量与充分性
*   **实验组设置**：对比了教师模型、学生模型以及两种主流 RNN 基准模型，并进行了**消融实验**（验证有无蒸馏、不同蒸馏组件对性能的影响）。
*   **充分性评价**：实验涵盖了从精度到硬件部署性能的全方位评估。消融实验清晰地展示了 Logit 蒸馏和特征蒸馏对提升轻量模型能力的贡献。实验设计较为客观，采用了时间序列标准的训练/验证/测试集划分（70%/15%/15%）。

#### 6. 主要结论与发现
*   **精度提升**：所提出的学生模型在 MAE 上比 LSTM 降低了 **36.5%**，比 GRU 降低了 **39.2%**，表现接近高容量的教师模型。
*   **部署效率飞跃**：相比于 LSTM，学生模型的参数量减少了 **13.4 倍**，磁盘占用从 520KB 降至 27.74KB，CPU 推理延迟从 4.8ms 缩短至 **0.6ms**。
*   **蒸馏价值**：单纯依靠地面真值（Ground-truth）监督训练的轻量模型效果较差，而通过教师模型的行为对齐和特征迁移，学生模型能有效继承序列建模能力。

#### 7. 优点
*   **部署友好性**：针对 TinyML 和边缘推理场景设计，极大地降低了 AI 数据中心节点级预测的计算负担。
*   **架构巧妙**：通过“序列到点”的蒸馏逻辑，打破了“高性能必须依赖长序列输入”的常规思维。
*   **鲁棒性强**：结合残差学习处理 AI 负载的剧烈波动，具有很强的工程实用价值。

#### 8. 不足与局限
*   **数据集单一**：仅在 MIT Supercloud 一个数据集上进行了验证，对于不同架构（如不同互联拓扑或专用 AI 芯片）的数据中心泛化性有待进一步观察。
*   **超参数依赖**：特征蒸馏权重 $\lambda$ 等超参数需要通过网格搜索确定，在不同场景下可能需要重新调优。
*   **预测时域**：研究集中在极短期（15 分钟内）预测，对于更长周期的电力规划支持有限。

（完）
