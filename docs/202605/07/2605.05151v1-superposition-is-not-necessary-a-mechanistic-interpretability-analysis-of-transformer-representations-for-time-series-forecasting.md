---
title: "Superposition Is Not Necessary: A Mechanistic Interpretability Analysis of Transformer Representations for Time Series Forecasting"
title_zh: 叠加并非必要：针对时间序列预测中 Transformer 表示的机械可解释性分析
authors: Alper Yıldırım
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05151v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 时间序列预测中Transformer表示的可解释性分析
tldr: 本研究利用机械可解释性工具稀疏自编码器（SAEs）分析了 PatchTST 模型在时间序列预测中的内部表示。研究发现，单层窄维度 Transformer 即可匹配深层模型的性能，且其 FFN 层表示并不依赖强叠加机制。实验显示，即使扩大 SAE 字典维度，表示仍保持稀疏且对因果干预不敏感。这一结果表明标准预测基准可能不需要复杂的组合表示，从而为简单线性模型的持续竞争力提供了机械论层面的解释。
selection_source: fresh_fetch
motivation: 旨在通过机械可解释性分析，探究 Transformer 在时间序列预测中的表示机制，并解释简单线性模型为何在该领域依然表现强劲。
method: 采用稀疏自编码器（SAEs）探测 PatchTST 模型 FFN 层的中间激活值，并结合不同字典规模和因果干预实验进行验证。
result: 实验发现扩大 SAE 字典对性能影响微乎其微，且大部分过完备字典特征保持失活，对主导特征的干预也仅产生极小的预测扰动。
conclusion: 叠加机制在标准时间序列预测基准中并非必要，这暗示此类任务对复杂组合表示的需求较低，解释了线性模型的有效性。
---

## 摘要
Transformer 架构已被广泛应用于时间序列预测，然而，使其在自然语言处理（NLP）中表现强大的表示机制是否在时间序列数据上真正发挥作用，仍有待探索。简单线性模型（如 DLinear）持续的竞争力引发了不断的争论，但目前尚未对这一现象提供机械性的解释。我们通过应用稀疏自编码器（SAEs）——一种机械可解释性工具——来探测 PatchTST 的内部表示，从而填补这一空白。我们首先确定，在常用基准测试中，单层、窄维度的 Transformer 与更深层配置的预测性能相当。随后，我们在 GELU 激活后的中间前馈网络（FFN）激活值上训练 SAEs，字典大小范围从原始维度的 0.5 倍到 4.0 倍。扩展字典带来的下游性能变化微乎其微（平均 0.214%），且过完备字典的大部分保持非激活状态。对主导潜在特征进行有针对性的因果干预仅产生极小的预测扰动。在所有评估设置中，我们没有观察到实证证据表明所分析的 FFN 表示依赖于强叠加。相反，这些表示保持稀疏，在激进的字典扩展下保持稳定，并且对潜在干预很大程度上不敏感。这些结果表明，在标准预测基准上获得竞争性性能并不需要叠加，这暗示这些基准可能不需要驱动 Transformer 在语言建模中取得成功的丰富组合表示，并有助于解释简单线性模型持续保持竞争力的原因。

## Abstract
Transformer architectures have been widely adopted for time series forecasting, yet whether the representational mechanisms that make them powerful in NLP actually engage on time series data remains unexplored. The persistent competitiveness of simple linear models such as DLinear has fueled ongoing debate, but no mechanistic explanation for this phenomenon has been offered. We address this gap by applying sparse autoencoders (SAEs), a tool from mechanistic interpretability, to probe the internal representations of PatchTST. We first establish that a single-layer, narrow-dimensional transformer matches the forecasting performance of deeper configurations across commonly used benchmarks. We then train SAEs on the post-GELU intermediate FFN activations with dictionary sizes ranging from 0.5x to 4.0x the native dimensionality. Expanding the dictionary yields negligible downstream performance change (average 0.214%), with large portions of overcomplete dictionaries remaining inactive. Targeted causal interventions on dominant latent features produce minimal forecast perturbation. Across all evaluated settings, we observe no empirical evidence that the analyzed FFN representations rely on strong superposition. Instead, the representations remain sparse, stable under aggressive dictionary expansion, and largely insensitive to latent interventions. These results demonstrate that superposition is not necessary for competitive performance on standard forecasting benchmarks, suggesting they may not demand the rich compositional representations that drive transformer success in language modeling, and helping explain the persistent competitiveness of simple linear models

---

## 论文详细总结（自动生成）

这篇论文通过机械可解释性（Mechanistic Interpretability）的视角，深入探讨了 Transformer 在时间序列预测任务中的内部表示机制，旨在解释为什么简单的线性模型在这一领域依然具有极强的竞争力。

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管 Transformer 在 NLP 领域取得了巨大成功，但在时间序列预测中，简单的线性模型（如 DLinear）往往能达到甚至超越复杂 Transformer 的性能。
*   **核心问题**：Transformer 在 NLP 中强大的核心机制——**叠加（Superposition）**（即在有限维度中压缩和组合大量特征的能力）是否在时间序列任务中同样存在？
*   **整体含义**：论文试图通过机械可解释性工具（稀疏自编码器 SAEs）来探测 Transformer 的内部工作原理，为“线性模型为何如此有效”提供理论和实验层面的解释。

### 2. 方法论：核心思想与关键技术
*   **基础架构**：选用 **PatchTST** 作为分析对象，并进行了现代化改进（使用 RoPE 旋转位置编码和 RMSNorm）。
*   **核心思想**：使用**稀疏自编码器（SAEs）**来分解 Transformer 内部前馈网络（FFN）的激活值，观察其特征表示的稀疏性和组合方式。
*   **关键技术细节**：
    *   **探测位置**：在 FFN 层的 GELU 激活函数之后提取激活向量。
    *   **SAE 训练**：训练一个包含编码器和解码器的线性网络，通过 $L_1$ 正则化强制实现潜在特征的稀疏性。
    *   **字典扩展**：设置不同规模的 SAE 字典（原始维度的 0.5x、1.0x、4.0x），观察增加“特征槽位”是否能捕捉到更多有意义的特征。
    *   **因果干预**：在推理阶段人为放大 SAE 识别出的主导特征，观察其对最终预测结果的影响。

### 3. 实验设计
*   **数据集**：使用了 8 个标准长时预测基准数据集：Weather、Electricity、Traffic、Exchange 以及 ETT 系列（h1, h2, m1, m2）。
*   **Benchmark**：
    *   对比了单层、窄维度的 PatchTST 与原始多层 PatchTST 的性能。
    *   对比了不同 SAE 字典规模下的重构保真度与下游预测误差。
*   **对比方法**：主要针对 PatchTST 内部表示进行深度剖析，并与 DLinear 等线性模型的表现逻辑进行关联分析。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：提到使用了 `bfloat16` 混合精度训练，SAE 训练在 10^6 个激活向量上进行，每个配置运行多达 50 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8 个数据集、4 个预测长度（96, 192, 336, 720），共计 32 组基础评估。
*   **消融与校准**：进行了 256 次 SAE 训练运行以进行稀疏性惩罚（$\lambda$）的参数扫掠，并进行了 FFN 层的零消融（Zero Ablation）实验。
*   **充分性评价**：实验设计非常严谨且充分。通过字典缩放、死神经元分析和因果干预三个维度交叉验证，确保了结论的客观性。

### 6. 主要结论与发现
*   **单层即足够**：单层、窄维度的 Transformer 就能匹配深层模型的性能，说明这些任务的内在维度极低。
*   **叠加并非必要**：扩大 SAE 字典规模对下游预测性能的提升微乎其微（平均仅 0.214%），且过完备字典中存在大量“死潜在特征”。
*   **因果敏感度低**：对内部特征的剧烈干预（放大 5 倍）仅导致极小的预测偏移（约 0.031 MAE），说明模型并未依赖复杂的稀疏特征组合。
*   **结论总结**：标准时间序列基准测试不需要 Transformer 擅长的复杂特征组合能力，这从机械论角度解释了为什么线性模型能保持竞争力。

### 7. 优点
*   **视角独特**：首次将 SAE 机械可解释性工具应用于时间序列 Transformer，跳出了单纯刷榜的范式。
*   **论证严密**：不仅观察现象，还通过因果干预验证特征的功能性，避免了“虚假解释”。
*   **解释力强**：为 DLinear 争论提供了直观的内部表示证据，具有很高的学术价值。

### 8. 不足与局限
*   **探测范围有限**：仅分析了 FFN 层，未深入探讨注意力机制（Attention Heads）和残差流（Residual Stream）中的表示。
*   **基准偏差风险**：研究结论主要基于现有的标准基准数据集。作者也承认，在更复杂的领域（如医疗 EEG 或金融高频数据）中，叠加机制可能仍然存在并发挥作用。
*   **模型单一**：主要针对 PatchTST 进行了深入分析，虽然其具有代表性，但结论在其他变体（如 iTransformer）上的普适性仍需进一步验证。

（完）
