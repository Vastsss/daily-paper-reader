---
title: "ITS-Mina: A Harris Hawks Optimization-Based All-MLP Framework with Iterative Refinement and External Attention for Multivariate Time Series Forecasting"
title_zh: ITS-Mina：一种基于哈里斯鹰优化、具有迭代细化和外部注意力的全 MLP 框架，用于多变量时间序列预测
authors: "Pourya Zamanvaziri, Amirhossein Sadr, Aida Pakniyat, Dara Rahmati"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27981v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 多元时间序列预测框架
tldr: 针对多元时间序列预测，本文提出ITS-Mina框架。该框架采用全MLP架构，通过迭代细化机制增强时间表示，引入外部注意力模块捕捉全局依赖，并利用哈里斯鹰优化算法自动调整Dropout率。实验证明，该模型在六个基准数据集上达到了SOTA或极具竞争力的性能，且计算效率优于Transformer模型。
selection_source: fresh_fetch
motivation: 旨在解决Transformer模型计算成本高的问题，利用更高效的全MLP架构实现高性能的多元时间序列预测。
method: 结合了共享参数的残差混合器迭代细化机制、基于可学习记忆单元的外部注意力模块以及哈里斯鹰优化算法。
result: 在六个主流基准数据集上的实验结果显示，ITS-Mina在多个预测周期内均优于或等同于11种主流基线模型。
conclusion: ITS-Mina证明了通过迭代细化和自适应正则化，全MLP架构能在保持低计算复杂度的同时实现卓越的预测精度。
---

## 摘要
多变量时间序列预测在金融分析、能源管理和交通规划等众多实际应用中发挥着关键作用。虽然基于 Transformer 的架构在该任务中广受欢迎，但最近的研究表明，更简单的基于 MLP 的模型能够以显著降低的计算成本实现具有竞争力甚至更优越的性能。在本文中，我们提出了 ITS-Mina，这是一种用于多变量时间序列预测的新型全 MLP 框架，它集成了三项关键创新：(1) 一种迭代细化机制，通过重复应用共享参数的残差混合器堆栈来逐步增强时间表示，在不增加不同参数数量的情况下有效加深了模型的计算能力；(2) 一个外部注意力模块，用可学习的记忆单元取代传统的自注意力，以线性计算复杂度捕捉跨样本的全局依赖关系；(3) 一种用于自动调整 Dropout 率的哈里斯鹰优化（HHO）算法，实现了针对每个数据集量身定制的自适应正则化。在六个广泛使用的基准数据集上进行的广泛实验表明，与 11 个基准模型相比，ITS-Mina 在多个预测周期内均实现了最先进或极具竞争力的性能。

## Abstract
Multivariate time series forecasting plays a pivotal role in numerous real-world applications, including financial analysis, energy management, and traffic planning. While Transformer-based architectures have gained popularity for this task, recent studies reveal that simpler MLP-based models can achieve competitive or superior performance with significantly reduced computational cost. In this paper, we propose ITS-Mina, a novel all-MLP framework for multivariate time series forecasting that integrates three key innovations: (1) an iterative refinement mechanism that progressively enhances temporal representations by repeatedly applying a shared-parameter residual mixer stack, effectively deepening the model's computational capacity without multiplying the number of distinct parameters; (2) an external attention module that replaces traditional self-attention with learnable memory units, capturing cross-sample global dependencies at linear computational complexity; and (3) a Harris Hawks Optimization (HHO) algorithm for automatic dropout rate tuning, enabling adaptive regularization tailored to each dataset. Extensive experiments on six widely-used benchmark datasets demonstrate that ITS-Mina achieves state-of-the-art or highly competitive performance compared to eleven baseline models across multiple forecasting horizons.

---

## 论文详细总结（自动生成）

这是一份关于论文《ITS-Mina: A Harris Hawks Optimization-Based All-MLP Framework with Iterative Refinement and External Attention for Multivariate Time Series Forecasting》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：多变量时间序列预测（MTSF）在金融、能源和交通等领域至关重要。虽然 Transformer 架构因捕捉长程依赖的能力而流行，但其二次方计算复杂度较高。近期研究发现，简单的 MLP 模型在性能上可媲美甚至超越复杂的 Transformer。
*   **核心问题**：现有的 MLP 模型存在三个局限：
    1.  **缺乏迭代能力**：通常采用固定深度的单次前向传播，难以处理复杂模式。
    2.  **缺乏全局上下文**：仅依赖局部特征交互，无法捕捉跨样本的全局依赖。
    3.  **超参数调节困难**：Dropout 等关键正则化参数通常依赖耗时的手动网格搜索。
*   **整体含义**：本文提出 ITS-Mina 框架，旨在通过全 MLP 架构实现高效、精准且具有自适应正则化能力的预测。

### 2. 方法论：核心思想与关键技术
ITS-Mina 框架由四个阶段组成，核心思想是结合参数共享的深度计算与启发式优化：
*   **迭代细化机制（Iterative Refinement）**：引入共享参数的残差混合器堆栈（Mixer Stack）。通过将同一组 MLP 块（包含时间混合和特征混合）循环应用 $N$ 次，在不增加参数量的前提下加深了计算深度，实现了对时间表示的逐步增强。
*   **外部注意力模块（External Attention）**：用两个可学习的记忆矩阵（单元）取代传统的自注意力。该设计将计算复杂度从 $O(L^2)$ 降低到线性复杂度 $O(L)$，同时通过共享记忆单元捕捉整个数据集层面的全局相关性，起到隐式正则化的作用。
*   **哈里斯鹰优化（HHO）自动调优**：将 Dropout 率的确定视为连续空间优化问题。利用 HHO 算法（模拟鹰群捕食行为的元启发式算法）在训练过程中自动搜索最优 Dropout 率，以实现针对不同数据集的自适应正则化。
*   **算法流程**：输入数据经实例归一化后，进入 $N$ 轮迭代细化，随后通过外部注意力注入全局背景，最后经线性投影输出预测值并进行逆归一化。

### 3. 实验设计
*   **数据集**：使用了 6 个主流基准数据集：Traffic（交通）、Electricity（电力）以及 ETT 系列（ETTh1, ETTh2, ETTm1, ETTm2）。
*   **预测周期（Horizons）**：涵盖 96、192、336、720 四个时间步长。
*   **对比方法（Baselines）**：共对比了 11 种模型，包括：
    *   **Transformer 类**：Informer, Autoformer, FEDformer, Crossformer, iTransformer。
    *   **MLP 类**：DLinear, CI-TSMixer, PatchMixer。
    *   **其他**：PatchTST（基于 Patch 的 Transformer）, TimesNet, MICN。

### 4. 资源与算力
*   **硬件设备**：实验在 **NVIDIA RTX 3090 GPU** 上进行。
*   **训练细节**：使用 Adam 优化器，最大训练轮数为 100 Epochs，设有早停机制（Patience=5）。
*   **超参数搜索**：结构参数（如层数、隐藏维度、学习率等）通过 Optuna 框架优化，Dropout 率由 HHO 算法独立优化。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个数据集、4 个预测周期上进行了完整测试，共计 24 组核心实验。
*   **充分性评价**：实验设计较为充分。不仅对比了最新的 SOTA 模型（如 iTransformer, PatchTST），还涵盖了 MSE 和 MAE 两种评价指标。
*   **客观性**：作者采用了标准的数据集划分方式，并引用了基线模型论文中的原始结果或使用官方代码进行复现，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：ITS-Mina 在所有 24 组实验设置中均取得了 **MSE 指标的第一名**；在 MAE 指标上，有 21 组排名第一，其余 3 组排名第二。
*   **高维数据优势**：在 Traffic（862 特征）和 Electricity（321 特征）等高维数据集上，ITS-Mina 的领先优势尤为明显。
*   **效率与深度的平衡**：通过权重共享的迭代机制，模型在保持轻量级参数规模的同时，获得了深层网络的表达能力。

### 7. 优点与亮点
*   **参数高效性**：通过权重绑定（Weight Tying）实现迭代细化，极大地节省了显存和参数存储空间。
*   **线性复杂度**：外部注意力机制解决了长序列预测中的计算瓶颈。
*   **自动化正则化**：引入 HHO 算法优化 Dropout，减少了人工调参的随机性和工作量，增强了模型的泛化能力。

### 8. 不足与局限
*   **计算开销**：虽然参数量少，但迭代细化机制（$N$ 次循环）和 HHO 优化过程（需要多次评估适应度）会增加训练阶段的时间成本。
*   **任务覆盖面**：论文目前仅聚焦于多变量预测任务，尚未在异常检测、分类或填补等其他时间序列任务上验证其通用性。
*   **自适应停止**：目前的迭代次数 $N$ 是预设的固定值，若能实现根据输入复杂度动态调整迭代次数（Adaptive Halting），模型将更具灵活性。

（完）
