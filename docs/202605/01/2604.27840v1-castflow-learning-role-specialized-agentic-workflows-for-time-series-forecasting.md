---
title: "CastFlow: Learning Role-Specialized Agentic Workflows for Time Series Forecasting"
title_zh: CastFlow：学习用于时间序列预测的角色专业化智能体工作流
authors: "Bokai Pan, Mingyue Cheng, Zhiding Liu, Shuo Yu, Xiaoyu Tao, Yuchong Wu, Qi Liu, Defu Lian, Enhong Chen"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27840v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 基于大语言模型的时序智能体预测框架
tldr: 针对现有LLM时间序列预测方法多为静态生成、缺乏多轮推理和集成支持的问题，本文提出CastFlow动态智能体框架。该框架将预测过程组织为规划、行动、预测和反思的工作流，并采用角色专业化设计：由冻结的LLM负责通用推理，微调的领域LLM负责基于证据的数值预测。通过结合SFT和带可验证奖励的强化学习（RLVR）进行两阶段训练，CastFlow在多个数据集上实现了超越基准模型的预测性能。
selection_source: fresh_fetch
motivation: 现有的LLM预测方法受限于单次生成的静态范式，难以充分提取时间模式并利用多轮上下文特征。
method: 提出一种包含规划、行动、预测和反思的智能体工作流，并结合角色专业化设计与两阶段强化学习训练优化模型。
result: 在多样化的数据集上进行的广泛实验证明，CastFlow在整体预测效果上优于现有的强基准模型。
conclusion: 该工作通过引入动态智能体工作流和角色分工，为构建更具适应性和准确性的时间序列预测系统提供了新方向。
---

## 摘要
最近，大语言模型（LLMs）在时间序列预测中展现出巨大潜力。然而，大多数现有的基于 LLM 的预测方法仍遵循静态生成范式，即在单次传递中直接将历史观测值映射到未来值。在这种范式下，预测受到有限的时间模式提取、单轮上下文特征获取、单次预测生成以及缺乏集成预测支持的限制。为了解决这些局限性，在这项工作中，我们提出了 CastFlow，这是一个动态智能体预测框架，支持多视图时间模式提取、多轮上下文特征获取、迭代预测细化以及基于集成预测的预测。首先，CastFlow 将预测过程组织为规划、行动、预测和反思，建立了一个智能体工作流。其次，该工作流由一个检索先前经验的记忆模块和一个构建诊断证据并提供可靠集成预测基准的多视图工具包支持。第三，CastFlow 采用了角色专业化设计，将通用推理与专业数值预测相结合。在这种设计下，冻结的 LLM 保留通用推理能力，而经过微调的领域特定 LLM 则基于集成预测基准（而非从零开始）执行证据引导的数值预测。为了优化微调后的领域特定 LLM，我们进一步开发了一种面向工作流的两阶段训练方法，结合了监督微调（SFT）和带可验证奖励的强化学习（RLVR）。为了评估 CastFlow 的有效性，我们在不同数据集上进行了广泛实验，结果表明它在与强基准模型的对比中取得了优异的综合结果。我们希望这项工作能为实现更具自适应性和准确性的时间序列预测迈出一步。

## Abstract
Recently, large language models (LLMs) have shown great promise in time series forecasting. However, most existing LLM-based forecasting methods still follow a static generative paradigm that directly maps historical observations to future values in a single pass. Under this paradigm, forecasting is constrained by limited temporal pattern extraction, single-round acquisition of contextual features, one-shot forecast generation, and lack of support from ensemble forecasts. To address these limitations, in this work, we propose CastFlow, a dynamic agentic forecasting framework that enables multi-view temporal pattern extraction, multi-round contextual features acquisition, iterative forecast refinement, and forecasting with ensemble forecasts. First, CastFlow organizes the forecasting process into planning, action, forecasting, and reflection, establishing an agentic workflow. Second, this workflow is supported by a memory module that retrieves prior experience and a multi-view toolkit that constructs diagnostic evidence and provides a reliable ensemble forecast baseline. Third, CastFlow adopts a role-specialized design that combines general-purpose reasoning with specialized numerical forecasting. Under this design, a frozen LLM preserves general-purpose reasoning, while a fine-tuned domain-specific LLM performs evidence-guided numerical forecasting based on the ensemble forecast baseline, rather than from scratch. To optimize a fine-tuned domain-specific LLM, we further develop a two-stage workflow-oriented training that combines supervised fine-tuning (SFT) and reinforcement learning with verifiable rewards (RLVR). To evaluate the effectiveness of CastFlow, we conduct extensive experiments on diverse datasets and show that it achieves superior overall results against strong baselines. We hope that this work can serve as a step toward more adaptive and accurate time series forecasting.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CastFlow** 的新型时间序列预测框架，旨在通过动态智能体工作流（Agentic Workflow）解决大语言模型（LLM）在数值预测中的局限性。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：现有的基于 LLM 的时序预测方法大多遵循“静态生成范式”，即单次将历史数据映射到预测值。这种方式存在四个缺陷：时间模式提取有限、上下文特征获取单一、缺乏迭代优化机制、难以兼顾通用推理能力与高精度数值预测。
*   **研究动机**：作者认为预测不应是简单的函数映射，而应是一个动态的决策过程。此外，LLM 在处理文本推理和精确数值回归时存在目标冲突（即“推理-精度困境”），需要通过角色分工来化解。

### 2. 方法论：核心思想与技术细节
CastFlow 将预测任务重新定义为一个由**规划（Planning）、行动（Action）、预测（Forecasting）和反思（Reflection）**组成的结构化工作流。
*   **角色专业化设计（Role-Specialized Design）**：
    *   **通用推理层（冻结）**：使用强大的冻结 LLM（如 Grok 4）负责规划工具调用和反思逻辑验证。
    *   **数值预测引擎（微调）**：使用较小的 LLM（如 Qwen3-4B）作为专门的预测器，负责根据工具提供的证据进行数值修正。
*   **关键组件**：
    *   **多视图工具包（Multi-view Toolkit）**：包含 11 个工具，分为基础锚定器（提供集成预测基准）、统计与频谱分析器、动力学监测器和残差诊断器。
    *   **策略记忆模块（Memory Module）**：存储并检索成功的规划轨迹和工具使用模式，为当前任务提供先验经验。
*   **两阶段工作流训练**：
    *   **第一阶段：监督微调（SFT）**：让模型学习基本的协议遵循和推理链条。
    *   **第二阶段：带可验证奖励的强化学习（RLVR）**：采用 **GRPO（群组相对策略优化）** 算法。奖励函数结合了绝对误差（MSE）和相对于集成基准的改进量（相对增益），迫使模型利用证据进行精准修正。

### 3. 实验设计
*   **数据集**：涵盖 10 个真实世界数据集，包括电力市场（BE, DE, NP, FR, PJM）、电网（ETTh, ETTm）、可再生能源（WP, SP）和水文流速（MOPEX）。
*   **基准模型（Benchmark）**：对比了 21 种方法，分为五类：
    1.  统计模型（Prophet, ARIMA）。
    2.  机器学习（XGBoost, LightGBM）。
    3.  深度学习（PatchTST, iTransformer, ConvTimeNet 等）。
    4.  时序基础模型（Chronos, TimesFM, Sundial）。
    5.  基于 LLM/智能体的方法（Time-LLM, Time-R1, AlphaCast 等）。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **2 张 NVIDIA A800 GPU**。
*   **训练细节**：
    *   SFT 阶段：在跨域联合训练上运行 1 个 epoch。
    *   RLVR 阶段：使用 GRPO 算法，运行 3 个 epoch。
    *   推理模型：Grok 4 作为冻结骨干，Qwen3-4B 作为可训练本地模型。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的对比实验（10 个数据集 × 21 个基准模型），并提供了 MSE 和 MAE 两种指标。
*   **消融实验**：非常详尽，包括组件消融（无工具、无记忆、无反思）、工具类别消融、训练策略消融（SFT vs RLVR）、骨干模型规模分析（1.7B 到 8B）以及记忆检索敏感性分析。
*   **客观性**：实验设置了严格的 7:1:2 数据划分，并确保所有基准模型在相同的回看窗口和预测长度下运行，评估过程较为公平。

### 6. 主要结论与发现
*   **性能领先**：CastFlow 在绝大多数长短期预测任务中均优于现有最强基准（如 Chronos 和 iTransformer）。
*   **角色分工的必要性**：实验证明，单纯依靠大型 LLM 直接生成数值效果较差，而“集成基准 + 智能体修正”的协作模式能显著提升精度。
*   **强化学习的作用**：RLVR 阶段通过可验证的数值奖励，成功引导模型从简单的“模仿行为”转向“优化精度”，有效解决了 LLM 的数值幻觉问题。

### 7. 优点与亮点
*   **范式创新**：将时序预测从“序列到序列”的映射转变为“基于证据的决策过程”。
*   **可验证奖励**：利用时序预测天然的数值反馈作为 RL 的奖励，避免了传统 RL 中奖励函数难以定义的难题。
*   **鲁棒性**：通过集成预测基准（Foundational Anchorer）为智能体提供了“保底”性能，使其不至于从零开始盲目预测。

### 8. 不足与局限
*   **推理开销**：由于涉及多轮对话、工具调用和反思重试，其推理延迟和计算成本显著高于传统的深度学习模型（如 DLinear）。
*   **复杂性**：系统架构复杂，涉及多个模型协作和复杂的工具链，部署和维护难度较大。
*   **数据依赖**：虽然具有跨域泛化能力，但对于极高频或极度非平稳的数据，工具包生成的诊断信号可能存在噪声，干扰智能体判断。

（完）
