---
title: "PAMNet: Cycle-aware Phase-Amplitude Modulation Network for Multivariate Time Series Forecasting"
title_zh: PAMNet：用于多元时间序列预测的周期感知相位-振幅调制网络
authors: "Yingbo Zhou, Yutong Ye, Zhiwei Ling, Shuhao Li, Rui Qian, Jian Xiong, Li Sun, Dejing Dou"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.02938v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 利用周期感知调制的多元时间序列预测
tldr: PAMNet 针对多变量时间序列预测中周期性模式建模不充分的问题，提出了一种周期感知的相位-振幅调制网络。该方法通过双分支调制器显式解耦相位和振幅分量，利用循环嵌入捕捉相位偏移，通过强度建模调整振幅变化，并采用轻量化融合机制实现高效预测，在多个数据集上达到了领先性能。
selection_source: fresh_fetch
motivation: 现有方法在建模周期性时要么计算开销过大，要么忽略了相位与振幅之间的内在耦合关系。
method: 提出 PAMNet，通过双分支调制器分别学习相位位置和振幅强度，并利用轻量级元素级融合显式建模两者的交互。
result: 在 12 个真实世界数据集上的广泛实验表明，该方法通过相位-振幅解耦机制实现了最先进的预测性能。
conclusion: PAMNet 为时间序列预测中的周期性建模提供了一个高效且有效的新视角，证明了显式解耦相位与振幅的优越性。
---

## 摘要
可靠的周期性模式是准确进行多元时间序列预测的基础。然而，现有方法要么通过具有高计算开销的复杂模型架构（如 Transformer）隐式提取周期性，要么在显式建模周期性组件时忽略了内在的相位-振幅耦合。为了解决这些问题，我们提出了一种新型的周期感知相位-振幅调制网络（PAMNet），该网络将周期性模式显式分解为互补的相位和振幅组件。其核心创新在于双分支调制器，该调制器具有用于相位定位和振幅调制的专用可学习嵌入。相位分支采用循环嵌入来捕捉与相位相关的均值偏移，而振幅分支则对强度变化进行建模，以适应方差的变化。具有逐元素融合功能的轻量级调制器高效地结合了这些组件，从而在无需复杂注意力机制的情况下实现对其相互作用的显式建模。在 12 个真实世界数据集上的广泛实验表明，我们的方法通过其新颖的相位-振幅解耦机制实现了最先进的性能，为时间序列预测中的周期性建模提供了新的视角。

## Abstract
Reliable periodic patterns serve as a fundamental basis for accurate multivariate time series forecasting. However, existing methods either implicitly extract periodicity through complex model architectures (e.g., Transformers) with high computational overhead or overlook the intrinsic phase-amplitude coupling when modeling periodic components explicitly. To address these issues, we propose a novel Cycle-aware Phase-Amplitude Modulation Network (PAMNet) that explicitly decomposes periodic patterns into complementary phase and amplitude components. The core innovation lies in its dual-branch modulator, featuring dedicated learnable embeddings for phase positioning and amplitude modulation. The phase branch employs cyclical embeddings to capture phase-dependent mean shifts, while the amplitude branch models intensity variations to adapt to changes in variance. A lightweight modulator with element-wise fusion efficiently combines these components, enabling explicit modeling of their interactions without complex attention mechanisms. Extensive experiments on twelve real-world datasets demonstrate that our method achieves state-of-the-art performance through its novel phase-amplitude decoupling mechanism, offering a new perspective for cyclical modeling in time series forecasting.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **PAMNet** 的新型多元时间序列预测框架，旨在通过显式解耦周期性模式中的“相位”和“振幅”来提升预测精度。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在多元时间序列（MTS）中，周期性模式往往具有**非平稳性**，即周期的相位（事件发生的时间点）和振幅（事件的强度）会随时间独立且耦合地变化。
*   **研究动机**：
    *   **隐式建模缺陷**：如 Transformer 等模型通过注意力机制隐式捕捉周期性，计算开销大且缺乏可解释性。
    *   **显式建模缺陷**：现有的显式分解方法（如季节性-趋势分解）通常将周期性视为整体，忽略了相位和振幅之间的复杂耦合关系。
*   **整体含义**：论文提出通过信号处理中的“调制”思想，将周期性信号拆解为相位分支和振幅分支，从而更精准地捕捉现实世界数据中的周期性波动。

### 2. 方法论：核心思想与技术细节
PAMNet 将预测任务建模为**自适应信号调制过程**，核心组件包括：
*   **输入编码**：采用倒置嵌入（Inverted Embedding），将时间点视为变量特征，将变量视为独立 Token。
*   **周期索引（Cyclical Index）**：根据时间戳计算当前处于周期内的位置 $t$（如一天中的第几小时）。
*   **双载波嵌入（Dual Carrier Embeddings）**：
    *   **相位载波 $E_P(t)$**：可学习的嵌入向量，用于捕捉与相位相关的均值偏移（Mean Shifts）。
    *   **振幅载波 $E_A(t)$**：用于建模强度变化，以适应方差（Variance）的波动。
*   **并行调制器（Parallel Modulator）**：
    *   借鉴幅度调制（AM）原理，使用**阿达马乘积（Hadamard Product）**将输入特征与相位/振幅载波结合。
    *   公式简化表达：$M(E_X, S) = W_3(\text{Dropout}(\text{SiLU}(W_1 E_X) \odot W_2 S))$。
*   **输出融合**：将相位调制和振幅调制的特征相加融合，通过轻量级 MLP 生成最终预测。

### 3. 实验设计
*   **数据集**：涵盖 12 个真实世界基准数据集，包括 ETT 系列（4个）、电力（ECL）、交通（Traffic）、天气（Weather）、太阳能（Solar）以及 PEMS 系列（4个）。
*   **预测场景**：长程预测（Horizon: 96, 192, 336, 720）和短程预测（PEMS 系列）。
*   **对比方法（Benchmark）**：对比了 9 种最先进（SOTA）的模型，包括：
    *   **周期显式模型**：TQNet, CycleNet, TimeMixer。
    *   **深度架构模型**：iTransformer, PatchTST, TimesNet。
    *   **线性/频率模型**：DLinear, FilterTS, Amplifier。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 4090 (24GB)** GPU。
*   **软件/优化器**：PyTorch 实现，使用 Adam 优化器。
*   **训练细节**：训练 30 个 epoch，采用早停机制（patience=5）。
*   **损失函数**：采用混合 MAE 损失（结合了时域和频域的损失），以增强训练稳定性。

### 5. 实验数量与充分性
*   **实验规模**：在 12 个数据集上进行了全方位的性能对比，并在 20/24 个评估指标上取得了第一。
*   **消融实验**：非常充分。验证了激活函数选择、损失函数影响、相位/振幅组件的必要性，以及将可学习嵌入替换为固定正弦函数的性能下降。
*   **鲁棒性测试**：
    *   **输入长度变化**：测试了从 48 到 720 的回看窗口。
    *   **缺失值处理**：模拟了 10% 和 30% 的数据丢失场景。
    *   **外生变量**：进行了多变量到单变量的预测实验。
*   **客观性**：使用了 5 个随机种子计算标准差（Error Bars），结果显示波动极小，证明了实验的客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：PAMNet 在绝大多数数据集上超越了现有 SOTA 模型，尤其在具有强周期性的电力和交通数据上表现突出。
*   **解耦有效性**：实验证明，显式解耦相位和振幅比将其视为单一组件建模更有效。
*   **高效性**：相比 Transformer 类模型，PAMNet 具有更低的内存占用和更快的训练速度（在 ETTm1 上比 TQNet 更快、更省显存）。
*   **可解释性**：通过 t-SNE 可视化证明，模型确实学习到了具有区分性的周期相位状态和通道特异性的振幅强度。

### 7. 优点（亮点）
*   **物理启发**：将信号处理中的相位-振幅调制理论引入深度学习，具有较强的理论支撑。
*   **轻量化**：避免了复杂的注意力机制，利用元素级乘法实现高效调制，计算复杂度低。
*   **抗噪性强**：在数据缺失 30% 的情况下，性能下降远小于其他模型，显示出极强的鲁棒性。

### 8. 不足与局限
*   **周期长度依赖**：模型需要预先定义周期长度 $c$（如 24 或 168）。虽然实验显示其对 $c$ 的敏感度低于同类模型，但在周期完全未知或动态变化的场景下仍受限。
*   **单尺度限制**：目前主要针对单一主导周期进行建模，对于存在多个嵌套周期（如同时存在日周期和周周期）的复杂交互建模尚不显式。
*   **应用限制**：对于完全无周期性（纯随机游走）的数据，该方法的优势可能无法体现。

（完）
