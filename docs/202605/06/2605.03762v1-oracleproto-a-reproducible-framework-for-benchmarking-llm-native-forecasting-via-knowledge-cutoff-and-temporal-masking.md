---
title: "OracleProto: A Reproducible Framework for Benchmarking LLM Native Forecasting via Knowledge Cutoff and Temporal Masking"
title_zh: OracleProto：通过知识截止与时间掩码评估大语言模型原生预测能力的可复现框架
authors: "Yiding Ma, Chengyun Ruan, Kaibo Huang, Zhongliang Yang, Linna Zhou"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03762v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 评估LLM原生预测能力与情境判断
tldr: OracleProto是一个用于评估大语言模型（LLM）原生预测能力的基准测试框架。针对现有实时基准易失效和回顾性基准存在数据泄露的问题，该框架通过知识截止日期对齐、工具级时间掩码和内容级泄露检测，将已发生的事件重构为受控的预测样本。实验证明其能有效区分模型预测质量并显著降低泄露率，为LLM预测能力的公平比较和下游训练提供了可审计、可复用的数据接口。
selection_source: fresh_fetch
motivation: 现有的LLM预测评估面临实时基准不可复用和回顾性基准因预训练数据泄露而失真的双重挑战。
method: 提出OracleProto框架，结合模型知识截止日期对齐、时间掩码、泄露检测及分层评分，重构受控的预测样本。
result: "在六种主流LLM上的实验显示，该框架将残留泄露率降至1%水平，比仅使用工具过滤降低了一个数量级。"
conclusion: OracleProto将LLM预测评估转化为可审计、可复用的数据集级能力，为模型公平对比和下游微调提供了可靠信号。
---

## 摘要
大语言模型正从静态文本生成器转向现实世界的决策支持系统，其中预测是一项综合能力，连接了信息收集、证据整合、情境判断和面向行动的决策。这种能力在金融、政策、工业和科学研究中有着广泛需求，但其评估仍然困难：实时基准测试在答案出现前评估预测，是衡量预测能力最纯净的方式，但一旦事件解决就会失效；回顾性基准测试具有可复现性，但无法可靠地将真正的预测与模型在预训练期间可能已经学到的事实区分开来。提示模型“假装不知道”无法替代真实的知识边界。我们提出了 OracleProto，一个用于评估大语言模型原生预测能力的可复现框架。OracleProto 通过结合模型截止日期对齐的样本准入、工具级时间掩码、内容级泄漏检测、离散答案归一化和分层评分，将已解决的事件重构为具有时间限制的预测样本。在基于 FutureX-Past 衍生的数据集上，通过对六种当代大语言模型进行实例化，OracleProto 在受控的信息边界下区分了预测质量、采样稳定性和成本效率，同时将残留泄漏降低至 1% 水平，比仅使用工具级时间过滤低了一个数量级。OracleProto 将大语言模型预测从一次性评估转变为可审计、可重用且可训练的数据集级能力，为公平的跨模型比较提供了统一接口，并为下游的有监督微调（SFT）和强化学习（RL）提供了受控的信号源。代码和数据可在 https://github.com/MaYiding/OracleProto 和 https://huggingface.co/datasets/MaYiding/OracleProto 获取。

## Abstract
Large language models are moving from static text generators toward real-world decision-support systems, where forecasting is a composite capability that links information gathering, evidence integration, situational judgment, and action-oriented decision making. This capability is in broad demand across finance, policy, industry, and scientific research, yet its evaluation remains difficult: live benchmarks evaluate forecasts before answers exist, making them the cleanest way to measure forecasting ability, but they expire once events resolve; retrospective benchmarks are reproducible, but they cannot reliably distinguish genuine forecasting from facts a model may have already learned during pretraining. Prompting models to "pretend not to know" cannot replace a genuine knowledge boundary. We propose OracleProto, a reproducible framework for evaluating LLM native forecasting capability. OracleProto reconstructs resolved events into time-bounded forecasting samples by combining model-cutoff-aligned sample admission, tool-level temporal masking, content-level leakage detection, discrete answer normalization, and hierarchical scoring. Instantiated on a FutureX-Past-derived dataset with six contemporary LLMs, OracleProto distinguishes forecasting quality, sampling stability, and cost efficiency under controlled information boundaries, while reducing residual leakage to the $1\%$ level, an order of magnitude below tool-only temporal filtering. OracleProto turns LLM forecasting from one-off evaluation into an auditable, reusable, and trainable dataset-level capability, providing a unified interface for fair cross-model comparison and a controlled signal source for downstream SFT and RL. Code and data are available at https://github.com/MaYiding/OracleProto and https://huggingface.co/datasets/MaYiding/OracleProto.

---

## 论文详细总结（自动生成）

### OracleProto 论文深度结构化总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
预测能力（Forecasting）是衡量大语言模型（LLM）是否具备可用“世界模型”及不确定性推理能力的关键指标。然而，评估 LLM 的预测能力面临**“预测悖论”**：
*   **实时基准（Live Benchmarks）：** 针对尚未发生的事件，虽能杜绝数据泄露，但一旦事件解决，题目便会失效，且难以复现和审计。
*   **回顾性基准（Retrospective Benchmarks）：** 针对已发生事件，虽可复现，但模型可能在预训练阶段已见过答案，导致评估结果变为“事实检索”而非“原生预测”。

**研究动机：** 论文旨在建立一个既能利用已解决的历史事件进行复现，又能通过严格的时间边界控制（知识截止日期与时间掩码）来消除数据泄露的评估框架，将预测评估从“一次性竞赛”转变为“可审计、可累积的数据集级能力”。

---

#### 2. 论文提出的方法论
核心思想是**OracleProto 框架**，它通过重构已解决事件的信息边界，使其对特定模型重新变为“未发生”的预测任务。

**关键技术细节：**
*   **样本准入控制（Knowledge Cutoff Alignment）：** 仅当模型的知识截止日期（$\kappa_M$）早于预测截止日期（$\chi_i$）时，该样本才被允许进入评估，防止参数化知识泄露。
*   **工具级时间掩码（Temporal Masking）：** 在 ReAct 循环中，强制搜索工具仅检索发布日期早于 $\chi_i$ 的信息。
*   **内容级泄露检测（Content-level Leakage Detection）：** 引入辅助检测器 $H_{aux}$（独立 LLM），对搜索返回的摘要和网页内容进行二次审计，剔除包含未来信息的片段（如“事后总结”或“周年回顾”）。
*   **离散答案归一化：** 将自然语言输出映射到有限的标签集（如 A, B, C），通过确定性解析器（Parser）消除格式干扰。
*   **分层评分体系：** 从有效性（Validity）、单项准确率（Item）、问题稳定性（Question）到模型综合表现（Model）进行多级评估。

---

#### 3. 实验设计
*   **数据集：** 基于 **FutureX-Past** 衍生，筛选了 80 个离散选择题（涵盖 Yes/No、二选一、多选题），事件发生时间在 2026 年 3 月至 4 月之间。
*   **对比模型：** 选取了 6 种当代主流 LLM：DeepSeek-V3.2-Exp、GLM 5、Qwen3.5-Flash、MiniMax M2.5、Kimi K2.5、Doubao Seed 2.0 Lite。
*   **评估指标：** 综合准确率（Composite Accuracy）、pass@1、Cohen’s $\kappa$（一致性）、FSS（技能得分）、成本效率（单位正确预测成本）以及残留泄露率。

---

#### 4. 资源与算力
*   **算力说明：** 论文未提及具体的 GPU 训练硬件（如 A100/H100 数量），因为该研究主要侧重于**推理评估框架**而非模型训练。
*   **成本记录：** 详细记录了通过 OpenRouter 平台调用 API 的费用。例如，Qwen3.5-Flash 的总成本仅为 0.45 USD，而 Kimi K2.5 约为 6.79 USD。

---

#### 5. 实验数量与充分性
*   **实验规模：** 每个问题进行 $n=3$ 次独立采样，总计约 1440 次完整的推理循环（80 题 × 6 模型 × 3 次）。
*   **泄露审计：** 针对 270 个随机抽取的搜索结果进行了人工与机器的双重交叉审计，以验证泄露控制的有效性。
*   **充分性评价：** 实验覆盖了不同厂商、不同规模的模型，并引入了统计学指标（如 Wilson 置信区间、Fleiss’ $\kappa$）来保证结果的客观性。虽然 80 个问题的样本量不算巨大，但作为框架验证实验，其深度和多维度分析（成本、稳定性、泄露率）是充分且公平的。

---

#### 6. 论文的主要结论与发现
*   **性能表现：** DeepSeek-V3.2-Exp 和 GLM 5 在综合准确率上领先（约 60%），但 Qwen3.5-Flash 凭借极高的性价比（成本仅为前者的 1/16）共同定义了成本-质量帕累托前沿。
*   **泄露控制：** OracleProto 将残留泄露率降低至 **1.1%** 左右，比单纯依赖工具层过滤降低了一个数量级，证明了内容级审计的必要性。
*   **稳定性差异：** Qwen 系列在多次试验间表现出最高的一致性（Fleiss’ $\kappa$ 最高），而 Kimi 的预测方差较大。
*   **多选挑战：** 所有模型在多项选择题（MC-Multi）上的表现普遍较差，得分显著低于单选题。

---

#### 7. 优点（亮点）
*   **可复现性：** 解决了预测评估“过期即作废”的问题，使历史数据能转化为持续增长的训练资产。
*   **严谨的边界控制：** 结合了参数截止、工具过滤和语义审计，是目前对 LLM 预测评估中数据泄露控制最严密的框架之一。
*   **数据飞轮效应：** 提出过期基准不再是垃圾，而是可以根据模型截止日期重新激活的“零污染”训练样本。
*   **工程化完备：** 提供了完整的代码、解析器和审计协议，具有很高的实用价值。

---

#### 8. 不足与局限
*   **样本规模限制：** 初始实验仅使用了 80 个问题，对于全面评估模型在复杂垂直领域（如金融细分）的预测能力可能略显单薄。
*   **依赖披露信息：** 框架高度依赖厂商披露的知识截止日期（Knowledge Cutoff），若厂商虚报或截止日期模糊，评估效力会受损。
*   **元数据风险：** 搜索工具的时间过滤依赖网页的 `published_date` 元数据，若网页元数据错误且内容中未提及时间，仍存在微小泄露风险。
*   **应用范围：** 目前主要针对离散选择题，对于开放式预测（生成式描述）的评估尚未完全覆盖。

（完）
