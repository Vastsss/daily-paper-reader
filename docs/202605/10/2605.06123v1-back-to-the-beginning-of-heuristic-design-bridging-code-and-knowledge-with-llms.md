---
title: "Back to the Beginning of Heuristic Design: Bridging Code and Knowledge with LLMs"
title_zh: 回到启发式设计的起点：利用大语言模型桥接代码与知识
authors: "Nguyen Viet Tuan Kiet, Bui Dinh Pham, Dao Van Tung, Tran Cong Dao, Huynh Thi Thanh Binh"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06123v1"
tags: ["query:llm"]
score: 8.0
evidence: 大语言模型用于组合优化的自动启发式设计
tldr: 本研究针对组合优化中的自动启发式设计（AHD），指出当前“自下而上”的代码中心化搜索模式存在局限。作者提出了一种“自上而下”的新范式，将知识视为核心搜索对象，而代码仅作为知识的实例化工具。通过统计学习视角下的失真-压缩权衡进行形式化，并在多种框架中实现。实验证明，这种知识优先的搜索显著提升了效率、迁移性和泛化能力，为构建可解释且可复用的启发式设计提供了新路径。
selection_source: fresh_fetch
motivation: 传统的自动启发式设计依赖于从底层代码实现推导高层原则，这种“自下而上”的模式限制了知识的显式表达与跨任务复用。
method: 提出以知识为核心的“自上而下”搜索范式，利用大模型迭代演化可解释的假设，并将代码视为验证这些知识的手段。
result: 该方法在组合优化等任务中表现优异，不仅提高了发现效率，还在跨问题迁移和泛化性能上超越了纯代码驱动的方法。
conclusion: 自动启发式设计的未来在于构建和演化能够超越单一搜索过程、具有长期价值的可解释性知识体系。
---

## 摘要
大语言模型（LLMs）近期推动了组合优化（CO）中自动启发式设计（AHD）的发展，在此过程中，候选启发式算法被迭代地提出、评估和改进。大多数现有方法在可执行程序空间内进行搜索，并从执行反馈中提取见解以指导后续迭代。由于这一过程是从底层实现转向高层原理，我们将其称为“自下而上”的范式。我们认为这种观点是不完整的，并引入了一种互补的“自上而下”视角：知识成为主要的搜索对象，而代码仅用于实例化和测试知识，从而使所学内容显性化，并可在不同问题和搜索轨迹之间复用。我们通过统计学习的视角将这一转变形式化，揭示了失真-压缩（distortion-compression）权衡，并将其在基于种群和基于树的 AHD 框架中进行了实例化。在组合优化及其他任务中，“知识优先”的搜索提高了发现效率、迁移能力和泛化能力，通常优于以代码为中心的流水线，而结合这两种策略则能获得进一步的提升。我们的结果表明，AHD 的进展取决于迭代地构建和演化可解释的假设，这些假设在单一搜索轨迹之外仍具有保留价值。

## Abstract
Large language models (LLMs) have recently advanced automatic heuristic design (AHD) for combinatorial optimization (CO), where candidate heuristics are iteratively proposed, evaluated, and refined. Most existing approaches search over executable programs and distill insights from execution feedback to guide later iterations. Because this process moves from low-level implementations to high-level principles, we refer to it as a bottom-up paradigm. We argue that this view is incomplete and introduce a complementary top-down perspective: knowledge becomes the primary search object and code merely instantiates and tests it, making what is learned explicit and reusable across problems and trajectories. We formalize this shift through a statistical-learning view that exposes a distortion--compression trade-off, and instantiate it in both population-based and tree-based AHD frameworks. Across CO and tasks beyond it, knowledge-first search improves discovery efficiency, transfer, and generalization, often outperforming code-centric pipelines, while combining both strategies yields further gains. Our results suggest that progress in AHD depends on iteratively constructing and evolving interpretable hypotheses that retain value beyond a single search trajectory.

---

## 论文详细总结（自动生成）

这是一份关于论文《Back to the Beginning of Heuristic Design: Bridging Code and Knowledge with LLMs》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：当前的自动启发式设计（AHD）主要采用“自下而上”（Bottom-Up）的模式，即 LLM 生成代码，通过执行反馈提取见解。作者认为这种模式是不完整的，因为知识（Knowledge）在其中仅作为代码生成的副产品。
*   **核心问题**：如何将“知识”提升为搜索的第一优先级？论文探讨了如果将知识作为主要搜索对象，而将代码仅视为知识的实例化（Instantiation），是否能提高搜索效率、迁移能力和泛化性。
*   **背景**：AHD 旨在自动化设计组合优化（CO）问题的启发式规则，目前已有的框架（如 FunSearch, ReEvo）在代码空间搜索上取得了进展，但在跨任务复用和高层原则演化方面仍有欠缺。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：对比并提出了**“自上而下”（Top-Down, TD）**的范式。
    *   **自下而上 (BU)**：路径为 `代码 θ → 评估 L → 知识 K`。知识是对已探索代码的后验总结。
    *   **自上而下 (TD)**：路径为 `知识 K → 代码 θ → 评估 L`。首先在抽象的知识空间搜索原则，再将其转化为可执行代码进行验证。
*   **关键技术细节**：
    *   **统计学习视角**：引入了**失真-压缩（Distortion-Compression）权衡**。TD 搜索在更紧凑的知识空间进行，降低了自适应泛化成本（压缩收益），但可能因抽象过粗而丢失底层实现细节（失真成本）。
    *   **贝叶斯解释**：将反射过程建模为近似后验更新，其中经验反馈用于修正对潜在设计原则的信念。
    *   **框架实例化**：
        *   **TD-Population**：在种群演化中，交叉和变异操作直接作用于“知识单元”而非代码行。
        *   **TD-Tree**：在蒙特卡洛树搜索（MCTS）中，节点存储知识状态，扩展过程生成新的设计原则。
        *   **Dual 模式**：并行维护知识种群和代码种群，通过“蒸馏”（代码→知识）和“落地”（知识→代码）进行双向互动。

### 3. 实验设计
*   **数据集与场景**：
    *   **经典组合优化 (CO)**：TSP（旅行商问题）、CVRP（车辆路径问题）、JSSP（作业车间调度）、OP（定向赛问题）、QAP（二次分配问题）。
    *   **序列组合优化 (SCO)**：会议演讲调度、在线广告排序等。
    *   **科学发现任务**：符号回归（SR）、蛋白质工程（PE）。
*   **Benchmark 与对比方法**：
    *   对比了主流 AHD 框架的 BU 和 TD 版本，包括 **ReEvo**、**MCTS-AHD**、**FunSearch**。
    *   基准算法包括传统的 ACO（蚁群算法）、GLS（引导局部搜索）以及 NCO（神经组合优化）模型（如 POMO, DIFUSCO）。
*   **特殊设置**：
    *   **跨域迁移**：测试从 TSP 学习到的知识是否能直接指导 VRP 变体的搜索。
    *   **稀疏评估**：在只有 50% 的候选代码被实际执行的情况下，观察搜索性能。

### 4. 资源与算力
*   **模型使用**：主要使用了 **GPT-4o-mini**，部分实验对比了 **GPT-5-nano**（注：文中出现的型号，可能为特定版本或未来预设）。
*   **算力需求**：论文明确指出该方法**不需要高昂的算力**。
    *   **硬件**：中端 CPU 即可，不依赖 GPU（除非本地运行 LLM）。
    *   **时长**：单次 ReEvo 运行约 15-20 分钟，MCTS-AHD 约 40 分钟。
    *   **成本**：使用 GPT-4o-mini 的单次实验 API 成本约为 0.10 美元。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了超过 15 个问题领域和 7 种算法骨干。
*   **充分性**：
    *   所有实验均运行 **5 次独立重复**并取平均值，提供了标准差。
    *   进行了详尽的消融实验（BU vs TD vs Dual）。
    *   包含了敏感性分析（不同 LLM 后端）和失败案例分析。
*   **公平性**：在对比 BU 和 TD 时，严格控制了 **LLM 调用次数**和**代码评估预算**完全一致，确保性能提升仅源于搜索范式的改变。

### 6. 主要结论与发现
*   **效率提升**：在相同预算下，TD 范式在大多数 CO 任务中比 BU 范式能找到更优的启发式算法。
*   **迁移性强**：知识（原则）比代码更容易跨问题迁移。在跨域实验中，TD 迁移的效果显著优于直接迁移代码。
*   **稀疏评估优势**：在减少 50% 评估次数的情况下，TD 依然能保持高性能，因为它能利用未评估代码的“结构信息”来优化知识。
*   **Dual 模式最优**：结合了 BU 的底层反馈和 TD 的高层引导的“双重演化”通常能取得最佳排名。

### 7. 优点与亮点
*   **理论深度**：不仅是工程实现，还通过信息论和贝叶斯框架为“知识优先”搜索提供了理论支撑。
*   **可解释性**：TD 模式生成的中间产物是自然语言描述的设计原则，使得自动化设计过程不再是黑盒。
*   **通用性**：实验不仅限于组合优化，还扩展到了符号回归和蛋白质工程，证明了范式的普适性。

### 8. 不足与局限
*   **抽象保真度风险**：如果 LLM 提取的知识过于笼统（如“优先选近的”），可能会丢失对特定问题至关重要的底层实现细节（失真）。
*   **对 LLM 先验的依赖**：TD 搜索高度依赖 LLM 对该领域的初始理解。如果 LLM 对某个冷门领域完全没有先验知识，生成的“原则”可能具有误导性。
*   **评估成本**：虽然提出了稀疏评估，但在极其复杂的仿真环境下，评估依然是 AHD 的主要瓶颈。

（完）
