---
title: On Adaptivity in Zeroth-Order Optimization
title_zh: 论零阶优化中的自适应性
authors: "Hassan Dbouk, Nidham Gazagnadou, Matthias Reisser, Christos Louizos"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03869v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 大语言模型的内存受限微调
tldr: 本研究探讨了大语言模型微调中零阶（ZO）优化算法的自适应性。作者发现，在高维空间下，ZO-Adam等自适应方法相比于调优后的ZO-SGD并无收敛优势，且内存开销巨大。基于ZO梯度缺乏坐标异质性的发现，作者提出了MEAZO算法，通过仅维护单个标量实现全局步长自适应。实验证明，MEAZO在保持ZO-SGD低内存占用的同时，达到了ZO-Adam的性能，并显著提升了对步长选择的鲁棒性。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型微调中，传统自适应零阶优化算法因内存开销过大且在某些高维场景下收敛优势不明显的问题。
method: 提出MEAZO优化器，通过仅跟踪一个全局标量来实现步长自适应，从而在降低内存消耗的同时保持优化效率。
result: 在多个LLM家族和任务上的实验显示，MEAZO以ZO-SGD的内存成本实现了与ZO-Adam相当的性能，并表现出更强的步长鲁棒性。
conclusion: 高维零阶优化中逐坐标的自适应机制并非必要，采用全局标量自适应的MEAZO是内存受限场景下更优的微调方案。
---

## 摘要
我们研究了自适应零阶（ZO）优化在大语言模型（LLM）内存受限微调中的有效性。与之前的观点相反，我们证明了诸如 ZO-Adam 之类的自适应零阶方法相比于经过良好调优的 ZO-SGD 并没有收敛优势，同时还会产生显著的内存开销。我们的分析表明，在高维空间中，零阶梯度缺乏逐坐标的异质性，这使得自适应机制在内存使用上效率低下。利用这一发现，我们提出了 MEAZO，这是一种内存高效的自适应零阶优化器，它仅跟踪单个标量来进行全局步长自适应。我们在标准假设下为该方法提供了理论收敛保证。在多个 LLM 家族和任务上的实验表明，MEAZO 在保持 ZO-SGD 内存占用的同时，达到了与 ZO-Adam 相当的性能。在合成二次问题和 LLM 微调上的额外实验进一步证明了 MEAZO 对步长选择具有更强的鲁棒性，特别是在分组或块结构优化设置中。

## Abstract
We investigate the effectiveness of adaptive zeroth-order (ZO) optimization for memory-constrained fine-tuning of large language models (LLMs). Contrary to prior claims, we show that adaptive ZO methods such as ZO-Adam offer no convergence advantage over well-tuned ZO-SGD, while incurring significant memory overhead. Our analysis reveals that in high dimensions, ZO gradients lack coordinate-wise heterogeneity, rendering adaptive mechanisms memory inefficient. Leveraging this insight, we propose MEAZO, a memory-efficient adaptive ZO optimizer that tracks only a single scalar for global step size adaptation. We support our method with theoretical convergence guarantees under standard assumptions. Experiments across multiple LLM families and tasks demonstrate that MEAZO matches ZO-Adam's performance with the memory footprint of ZO-SGD. Additional experiments on synthetic quadratic problems and LLM fine-tuning further demonstrate MEAZO's enhanced robustness to step size choices, particularly in grouped or block-structured optimization settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《On Adaptivity in Zeroth-Order Optimization》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在大语言模型（LLM）微调中，零阶（ZO）优化因仅需前向传播、极度节省内存而备受关注。近期研究试图将一阶优化中的自适应机制（如 Adam 的逐坐标缩放）引入 ZO 领域（如 ZO-Adam）。
*   **核心问题**：作者质疑在**高维空间**下，ZO-Adam 这种逐坐标（Coordinate-wise）的自适应是否真的有效。
*   **整体含义**：论文通过理论和实验证明，在高维 LLM 微调中，ZO 梯度由于随机扰动的各向同性，缺乏坐标间的异质性。因此，传统的逐坐标自适应不仅浪费内存，且收敛优势微乎其微。

### 2. 方法论：MEAZO 优化器
*   **核心思想**：既然逐坐标统计量在高维 ZO 中趋于一致，不如将其简化为**全局标量自适应**。
*   **关键技术细节**：
    *   **MEAZO (Memory-Efficient Adaptive ZO)**：不为每个参数维护一阶/二阶矩，而是仅跟踪一个全局标量 $v_t$，它是投影梯度平方的指数移动平均（EMA）。
    *   **更新公式**：参数更新步长由该全局标量进行归一化：$x_{t+1} = x_t - \frac{\eta}{\sqrt{\hat{v}_t} + \zeta} \hat{g}_t$，其中 $\hat{g}_t$ 是 ZO 梯度估计。
    *   **分组 MEAZO (Grouped MEAZO)**：为了兼顾灵活性，可以为模型的每个层或块（Block）维护一个独立的标量，实现块级别的自适应，而内存开销依然远小于逐参数统计。
*   **理论保证**：作者在标准非凸假设下证明了 MEAZO 的收敛性，并推导出了其收敛速率。

### 3. 实验设计
*   **数据集/场景**：
    *   **自然语言处理**：XSum（摘要）、SST-2（情感分析）、SQuAD（问答）。
    *   **计算机视觉**：Oxford-Pets（图像分类）。
    *   **合成实验**：二次型优化问题（用于验证坐标异质性）。
*   **模型（Benchmark）**：Llama-3.2 (1B), Qwen-3 (0.6B, 8B), Ministral (14B), OPT (1.3B), ViT-B/16。
*   **对比方法**：ZO-SGD (MeZO), ZO-Adam, R-AdaZO, FZOO。
*   **微调技术**：LoRA (Rank 16) 和 Prompt Tuning (20 tokens)。

### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA A100 或 H100 (80GB)** GPU。
*   **训练配置**：通常训练 10,000 步，Batch Size 为 8。
*   **精度**：大部分实验在 FP32 下进行，部分（如 Ministral 14B）在 BF16 下进行。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 0.6B 到 14B 不同规模的多个模型家族，跨越 NLP 和 CV 领域。
*   **充分性**：
    *   每组实验均运行了 **3 个随机种子**并取平均值。
    *   进行了极其详尽的**步长（Learning Rate）搜索**（分为粗筛和精筛两个阶段），确保对比的公平性。
    *   包含了消融实验（如扰动分布的影响、$\beta_1$ 动量的必要性）。
*   **客观性**：作者诚实地指出，在经过完美调优后，ZO-SGD 往往能达到与自适应方法相当的性能，这增强了结论的可信度。

### 6. 主要结论与发现
*   **自适应的本质作用**：在 ZO 优化中，自适应机制的主要贡献不是提高最终精度，而是**增强对初始学习率选择的鲁棒性**。
*   **坐标异质性缺失**：在高维空间，ZO-Adam 的二阶矩向量各坐标值几乎相等，证明了逐坐标自适应的冗余性。
*   **MEAZO 的优越性**：MEAZO 以 ZO-SGD 的内存成本（几乎零额外开销），实现了与 ZO-Adam 相当的性能和鲁棒性。
*   **分组优势**：在块结构优化中，MEAZO 能自动识别不同层对扰动的敏感度（前层通常更敏感），从而自动调整各层的有效步长。

### 7. 优点
*   **内存效率极高**：解决了 ZO-Adam 在微调超大模型时内存翻倍的问题。
*   **理论与实践结合**：不仅有严谨的收敛性证明，还通过合成实验直观展示了高维 ZO 梯度的同质化现象。
*   **实用性强**：MEAZO 易于实现，且对超参数（学习率）不敏感，非常适合资源受限的边缘设备微调。

### 8. 不足与局限
*   **收敛速度限制**：尽管比 ZO-SGD 更稳健，但 ZO 方法整体收敛速度仍远慢于一阶（FO）方法（通常需要更多步数）。
*   **实验范围**：虽然测试了 14B 模型，但尚未在千亿级（100B+）模型上验证其极端高维下的表现。
*   **任务类型**：主要集中在判别式和简单的生成任务，对于极其复杂的长文本推理任务验证较少。

（完）
