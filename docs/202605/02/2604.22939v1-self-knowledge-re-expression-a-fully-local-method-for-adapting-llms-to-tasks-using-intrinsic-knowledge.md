---
title: "Self Knowledge Re-expression: A Fully Local Method for Adapting LLMs to Tasks Using Intrinsic Knowledge"
title_zh: 自我知识重表达：一种利用内在知识使大语言模型适应任务的全本地方法
authors: "Mengyu Wang, Xiaoying Zhi, Zhiyi Li, Robin Schmucker, Shay B. Cohen, Tiejun Ma, Fran Silavong"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22939v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 利用内在知识使大语言模型适应专门任务
tldr: 本研究针对大语言模型在非生成式任务中受限于“逐词预测”机制而无法充分发挥内在知识的问题，提出了“自我知识重表达”（SKR）方法。SKR是一种完全本地化、无需人工标注或模型蒸馏的任务无关适配方法，通过将LLM的输出从通用文本生成转化为高效的任务特定表达，显著提升了模型在信息检索、目标检测和异常检测等任务中的性能，在多个基准测试中表现优异。
selection_source: fresh_fetch
motivation: 大语言模型传统的逐词预测范式在处理非生成式专业任务时存在知识表达瓶颈，限制了其内在知识的有效利用。
method: 提出一种名为SKR的完全本地化方法，仅使用无标注数据将LLM的输出从通用生成转变为任务特定的高效表达。
result: "实验显示该方法使信息检索召回率提升超40%，目标检测延迟降低76%，并在MMDocRAG数据集上超越领先模型12.6%。"
conclusion: SKR证明了通过优化LLM的知识表达机制而非重新获取知识，可以在无监督条件下显著提升其在特定任务中的适配能力。
---

## 摘要
虽然下一标记预测（NTP）范式使大语言模型（LLMs）能够表达其内在知识，但其序列特性限制了在专门的非生成任务上的性能。我们将这一性能瓶颈归因于大语言模型的知识表达机制，而非知识获取方面的缺陷。为了解决这一问题，我们提出了自我知识重表达（SKR），这是一种新颖的、与任务无关的适配方法。SKR 将大语言模型的输出从通用的标记生成转变为高效的、特定于任务的表达。SKR 是一种完全本地的方法，仅使用未标注的数据，既不需要人工监督，也不需要模型蒸馏。在大型金融文档数据集上的实验表明了显著的改进：信息检索任务的 Recall@1 提升超过 40%，目标检测延迟降低超过 76%，异常检测 AUPRC 提升超过 33%。我们在 MMDocRAG 数据集上的结果比领先的检索模型至少高出 12.6%。

## Abstract
While the next-token prediction (NTP) paradigm enables large language models (LLMs) to express their intrinsic knowledge, its sequential nature constrains performance on specialized, non-generative tasks. We attribute this performance bottleneck to the LLMs' knowledge expression mechanism, rather than to deficiencies in knowledge acquisition. To address this, we propose Self-Knowledge Re-expression (SKR), a novel, task-agnostic adaptation method. SKR transforms the LLM's output from generic token generation to highly efficient, task-specific expression. SKR is a fully local method that uses only unannotated data, requiring neither human supervision nor model distillation. Experiments on a large financial document dataset demonstrate substantial improvements: over 40% in Recall@1 for information retrieval tasks, over 76% reduction in object detection latency, and over 33% increase in anomaly detection AUPRC. Our results on the MMDocRAG dataset surpass those of leading retrieval models by at least 12.6%.

---

## 论文详细总结（自动生成）

这是一份关于论文《Self Knowledge Re-expression: A Fully Local Method for Adapting LLMs to Tasks Using Intrinsic Knowledge》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：大语言模型（LLM）通常被视为庞大的知识库，其性能提升往往依赖于扩大模型规模或增加预训练数据（知识获取）。然而，作者发现 LLM 在非生成式任务（如信息检索、目标检测、数值回归）中表现不佳，其根本原因并非缺乏知识，而是受限于**下一标记预测（Next-Token Prediction, NTP）**这一单一的表达机制。
*   **核心问题**：NTP 范式在处理高维向量（检索）、连续数值（检测坐标）或精确概率分布（分类）时存在严重的“表达瓶颈”。论文旨在探索如何不依赖外部监督，仅利用模型已有的内在知识，通过改变输出表达方式来释放模型潜力。

### 2. 方法论：自我知识重表达（SKR）
SKR 是一种全本地、任务无关的适配方法，核心思想是将知识表达从通用的 $E_{ntp}$ 转换为任务特定的 $E_T$。其流程分为两个关键步骤：
*   **步骤 1：自我标注（Self-Annotation）**
    *   利用 LLM 原有的生成能力（NTP），在无标注的原始数据 $D_x$ 上生成任务相关的文本。例如，在检索任务中让模型根据图像生成问题；在检测任务中通过“模糊定位”或多轮对话生成坐标字符串。
    *   通过轻量级后处理（如规则解析或 OCR 辅助）将这些文本转换为任务所需的格式（如数值坐标、二进制标签），形成伪标签 $\hat{D}_y$。
*   **步骤 2：知识重表达（Knowledge Re-expression）**
    *   **架构调整**：移除原有的 NTP 输出层，针对特定任务添加新的输出头（如用于检索的线性投影层、用于检测的回归头、用于异常检测的分类头）。
    *   **微调过程**：使用 LoRA（低秩适配）技术，在 $(D_x, \hat{D}_y)$ 上最小化任务特定的损失函数（如对比损失、均方误差 MSE 或交叉熵 CE）。
    *   **公式化表达**：$M_{E_{ntp}} \xrightarrow{SKR} M_{E_T}$。此过程仅调整模型如何“说出”知识，而不引入外部新知识。

### 3. 实验设计
*   **任务场景**：
    1.  **信息检索 (TIR)**：包括文本-文本和文本-图像检索。
    2.  **目标检测 (TOD)**：定位文档中的图表、表格等。
    3.  **异常检测 (TAD)**：判断图像与查询是否匹配（类别极度不平衡场景）。
*   **数据集**：
    *   **工业级金融数据集**：包含 10 万份报告，用于验证实际应用效果。
    *   **开源基准**：MMDocRAG（多模态检索）、DocVQA-2020、SciMMIR（科学文献检索）、CUB 200 2011（鸟类检测与分类）。
*   **对比方法（Benchmark）**：
    *   **Base 模型**：未经适配的原始 LLM/MLLM。
    *   **E5-V**：一种无需训练的表示提取方法。
    *   **SFT（监督微调）**：基于 NTP 范式的传统微调。
    *   **工业 SOTA**：OpenAI (ada-002), Nomic, ColPali, ColQwen 等专门训练的检索模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到使用了 LoRA 适配器，可训练参数量不到总参数量的 **0.5%**。训练参数包括：Batch Size 为 4，学习率为 $1 \times 10^{-4}$，不同任务训练 5 到 50 个 Epoch 不等。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 种纯文本 LLM（Mistral, Phi-3, Qwen2, Llama-3.1）和 3 种多模态模型（LLaVA-1.6, Phi-3-VL, Qwen2-VL）。
    *   在 5 个不同的数据集上进行了验证。
    *   **消融实验**：对比了“自我标注”与“外部强模型（GPT-4o）标注”的效果，以及“任务特定头”与“NTP 头部”的性能差异。
    *   **模型缩放分析**：使用了 Gemma-3 家族（4B, 12B, 27B）分析模型规模对 SKR 效果的影响。
*   **充分性评价**：实验设计较为全面，通过 CKA（中心内核对齐）分析深入探讨了表示层的偏移，客观地证明了性能提升源于表达机制的改变。

### 6. 主要结论与发现
*   **性能飞跃**：在检索任务中，SKR 适配后的模型在 Recall@1 上提升超过 40%，甚至在 MMDocRAG 上超越了专门微调的工业模型 12.6%。
*   **效率提升**：在目标检测任务中，通过将多轮对话压缩为单次回归输出，推理延迟降低了 **76%** 以上。
*   **知识验证**：消融实验显示，对于已有相关知识的模型（如 Qwen2-VL），自我标注的效果甚至优于 GPT-4o 的标注，证明了 LLM 内部潜藏着未被充分利用的“沉默知识”。
*   **表达瓶颈证实**：CKA 分析显示，SKR 主要改变了模型的深层表示，使其更符合任务目标，证实了 NTP 确实是限制非生成任务性能的枷锁。

### 7. 优点
*   **全本地与隐私性**：无需调用外部 API 或人工标注，适合金融、医疗等对数据隐私敏感的行业。
*   **低成本适配**：仅需极少量（约 1000 条）无标注数据即可完成高质量适配。
*   **解耦设计**：首次形式化地将“知识表达”与“参数知识”解耦，为 LLM 适配提供了新的范式。
*   **通用性强**：方法不依赖特定架构，在纯文本和多模态模型上均表现稳健。

### 8. 不足与局限
*   **知识依赖性**：SKR 无法凭空创造知识。如果模型本身在预训练阶段未接触过相关领域（知识缺失），SKR 的效果将受限。
*   **标注策略的复杂性**：对于不具备原生定位能力的模型（如 LLaVA），需要设计复杂的“模糊定位”策略来获取初始伪标签，这增加了实施门槛。
*   **私有数据集偏差**：核心实验部分使用了私有金融数据集，外部研究者难以完全复现其在特定工业场景下的数值。
*   **后处理依赖**：部分任务（如 TOD）仍需 OCR 等外部工具辅助进行自我标注的精细化，并非完全意义上的“端到端”自我进化。

（完）
