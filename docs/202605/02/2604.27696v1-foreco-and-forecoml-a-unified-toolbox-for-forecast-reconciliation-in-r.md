---
title: "FoReco and FoRecoML: A Unified Toolbox for Forecast Reconciliation in R"
title_zh: FoReco 和 FoRecoML：R 语言中预测协调的统一工具箱
authors: "Daniele Girolimetto, Jeroen Rombouts, Ines Wilms, Yangzhuoran Fin Yang"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27696v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 多变量时间序列的预测协调
tldr: 针对分层和分组时间序列预测中的一致性与准确性问题，本文推出了 FoReco 和 FoRecoML 两个 R 语言工具包。该工具箱填补了现有软件在同时处理横截面、时间和跨时间预测协调方面的空白。FoReco 专注于经典及基于回归的线性方法，而 FoRecoML 则引入了基于机器学习的非线性协调技术。该工具既为初学者提供了便捷的默认配置，也为专家提供了高度灵活的自定义功能，是预测协调领域的综合性解决方案。
selection_source: fresh_fetch
motivation: 现有的预测协调软件缺乏一个能够同时涵盖横截面、时间和跨时间维度的统一框架。
method: 研发了 FoReco 和 FoRecoML 两个 R 包，分别采用线性回归和机器学习方法来实现多维度的预测协调。
result: 成功构建了一个功能全面的工具箱，支持多种预测协调框架，并兼顾了易用性与灵活性。
conclusion: 该工具箱为处理线性约束时间序列提供了强大的技术支持，是提升预测一致性的重要研究与实践工具。
---

## 摘要
预测协调已成为提高具有线性约束的多变量时间序列（如层级和分组序列）预测准确性和一致性的关键。然而，目前仍缺乏能够同时涵盖截面、时间以及跨时间协调的综合性软件。R 语言包 FoReco 和 FoRecoML 通过提供一个全面且统一的框架填补了这一空白。这两个包分别针对截面、时间及跨时间框架，实现了经典的以及基于回归的线性协调方法，以及基于机器学习的非线性方法。这些包旨在兼顾易用性和灵活性，提供了合理的默认选项，使新用户能够以极小的努力应用协调方法，同时仍允许专家用户通过自定义设置完全控制并探索前沿扩展。凭借这种双重关注，FoReco 和 FoRecoML 成为从事预测协调工作的从业者和研究人员的通用工具。

## Abstract
Forecast reconciliation has become key to improving the accuracy and coherence of forecasts for linearly constrained multiple time series, such as hierarchical and grouped series. Yet, comprehensive software that jointly covers cross-sectional, temporal, and cross-temporal reconciliation has so far been lacking. The R packages FoReco and FoRecoML address this gap by offering a comprehensive and unified framework. The packages respectively implement classical and regression-based linear reconciliation approaches, and non-linear approaches based on machine learning for cross-sectional, temporal and cross-temporal frameworks. Designed for accessibility and flexibility, these packages provide sensible default options that allow new users to apply reconciliation methods with minimal effort, while still giving expert users full control to explore state-of-the-art extensions through customized settings. With this dual focus, FoReco and FoRecoML are versatile tools for practitioners and researchers working on forecast reconciliation.

---

## 论文详细总结（自动生成）

以下是对论文《FoReco and FoRecoML: A Unified Toolbox for Forecast Reconciliation in R》的结构化总结：

### 1. 论文的核心问题与整体含义
该论文介绍并演示了两个 R 语言工具包：**FoReco** 和 **FoRecoML**。其核心研究动机在于解决多变量时间序列（如具有地理层级或时间聚合约束的序列）在独立预测后出现的“不一致性”问题（即底层预测之和不等于高层预测）。
*   **背景**：预测协调（Forecast Reconciliation）是确保层级或分组时间序列在逻辑上保持一致并提高预测准确性的关键步骤。
*   **核心问题**：现有的 R 或 Python 工具包功能分散，通常仅支持单一维度（如仅横截面或仅时间）的协调，且缺乏统一的接口来同时处理线性回归方法和非线性机器学习方法。

### 2. 论文提出的方法论
论文提出了一个统一的框架，涵盖了三种主要的协调维度：**横截面（Cross-sectional）**、**时间（Temporal）**和**跨时间（Cross-temporal）**。
*   **核心思想**：采用“原始预测输入，协调预测输出”的后处理逻辑。
*   **FoReco（线性方法）**：
    *   **经典法**：包括自上而下（Top-down）、自下而上（Bottom-up）和中间向两端（Middle-out）。
    *   **基于回归的方法**：利用最小迹（Min Trace）框架，通过投影矩阵将原始预测映射到一致的空间。支持 OLS、WLS（加权最小二乘）和 GLS（广义最小二乘，含收缩估计）。
    *   **层级条件一致性（LCC）**：允许用户设定某些层级的预测为“不可变”（Immutable）。
*   **FoRecoML（非线性方法）**：
    *   **核心逻辑**：利用机器学习模型（如随机森林、XGBoost、LightGBM）学习底层序列与所有层级原始预测之间的复杂非线性关系。
    *   **流程**：在验证集上训练模型预测底层序列，随后通过自下而上（Bottom-up）方法确保全局一致性。

### 3. 实验设计
论文通过两个真实场景验证了工具包的功能：
*   **场景 A（横截面）**：使用**澳大利亚旅游需求数据集**（525 条序列）。
    *   **Benchmark**：使用 ETS 模型生成的原始预测。
    *   **对比方法**：BU、TD、LCC、OLS、Shrinkage（收缩估计）。
*   **场景 B（时间与跨时间）**：使用**意大利电力负荷数据**（7 个区域及总计）。
    *   **Benchmark**：季节性 ARIMA 模型。
    *   **对比方法**：OLS、Shrinkage、WLSV（序列方差缩放）以及基于 XGBoost 的机器学习协调方法。
*   **概率预测**：演示了基于高斯分布和基于样本（Bootstrap）的概率协调。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）。
*   **计算特性**：文中指出 `FoReco` 采用线性代数运算，计算极其轻量且快速；而 `FoRecoML` 由于涉及机器学习模型的训练和超参数调优（通过 `mlr3` 框架），其计算开销显著高于线性方法，但未给出具体的训练时长。

### 5. 实验数量与充分性
*   **实验规模**：论文分别针对横截面、时间和跨时间三种框架进行了详细的代码演示和结果对比。
*   **充分性**：作为一篇介绍软件工具包的论文，其实验设计侧重于展示功能的**全面性**（如非负约束、不可变约束、概率预测等）而非单纯的性能竞赛。实验涵盖了从点预测到分布预测的多种变体，对比了多种协方差估计方法，逻辑严密且客观。

### 6. 论文的主要结论与发现
*   **统一性**：FoReco 系列包是首个在 R 语言中提供跨维度（横截面+时间+跨时间）统一接口的工具。
*   **性能提升**：实验表明，跨时间协调（Cross-temporal）通常比单一维度的协调能获得更准确的结果。
*   **灵活性**：机器学习方法（FoRecoML）能够捕捉基准预测中的非线性偏差，在某些复杂场景下优于传统的线性投影方法。

### 7. 优点
*   **设计哲学统一**：函数命名规范（如 `cs*`、`te*`、`ct*` 分别对应不同框架），极大降低了用户的学习成本。
*   **功能高度集成**：支持非负约束、不可变预测（Immutable forecasts）、多种时间聚合方案（求和、平均、首末值）。
*   **扩展性强**：`FoRecoML` 深度集成 `mlr3` 框架，支持几乎所有主流的机器学习算法和自动调参。

### 8. 不足与局限
*   **贝叶斯支持缺失**：该工具包不包含贝叶斯预测协调功能（作者建议用户使用 `bayesRecon` 等专门包）。
*   **ML 复杂性**：机器学习协调方法依赖于高质量的验证集（Validation set）来训练模型，如果数据量较小，可能会出现过拟合。
*   **计算成本**：对于超大规模层级系统，`FoRecoML` 的训练时间可能成为瓶颈。

（完）
