---
title: "Large Spectrum Models (LSMs): Decoder-Only Transformer-Powered Spectrum Activity Forecasting via Tokenized RF Data"
title_zh: 大规模频谱模型 (LSMs)：基于分词化射频数据并由仅解码器 Transformer 驱动的频谱活动预测
authors: "Mohammad Mosiur Lunar, Mehmet C. Vuran"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10825v1"
tags: ["query:ts-fm"]
score: 9.0
evidence: 用于预测的基础大频谱模型
tldr: 针对动态频谱接入中的频谱稀缺问题，本文提出了大频谱模型（LSMs）。通过创新的RF分词器将原始IQ测量值转化为包含频率、时间等信息的Token序列，并利用Gemma、LLaMA、Mistral等五种主流Decoder-only架构在22TB的海量频谱数据上进行训练。实验表明，LSMs在频谱预测任务中表现优异且具备良好的泛化能力，为大规模射频频谱预测提供了新范式。
selection_source: fresh_fetch
motivation: 随着连接设备激增，动态频谱接入需要更精准的短期频谱预测来解决频谱资源稀缺问题。
method: 提出一种新型RF分词器将原始IQ数据转化为Token，并采用多种主流大语言模型架构进行训练。
result: 最佳模型LSM-Mistral实现了3.25 dB的均方根误差，且在不同地点的微调实验中展现出强大的泛化性能。
conclusion: 实验证明Decoder-only Transformer架构能够有效应用于大规模射频频谱预测任务。
---

## 摘要
动态频谱接入 (DSA) 已成为下一代无线系统的关键支柱，旨在解决由于联网设备快速增长而导致的频谱稀缺问题。准确的短期频谱预测对 DSA 至关重要，而数据驱动的方法已被证明最为有效。大语言模型 (LLM) 架构的最新进展和广泛采用为频谱预测提供了新的机遇。本文提出了基础的大规模频谱模型 (LSMs)。文中引入了一种新型射频分词器 (RF tokenizer)，通过将每个功率谱密度值映射到固定词表，并结合增益、频率、FFT 仓 (bin) 和时间戳信息的嵌入，将原始 IQ 测量数据转换为标记 (token) 序列。在这些分词化的频谱数据上训练了五种成熟的开源 LLM 架构（Gemma-2B、GPT-2、LLaMA-7B、Mistral-7B 和 Phi-1），用于执行频谱预测任务，从而产生 LSMs。为了利用 LSMs 的缩放增益 (scaling gains)，采用了一个全自动室外无线测试平台，在 33 个 sub-GHz 频段收集了超过 22 TB 的原始频谱数据，总计产生 84 亿个标记。在所有 33 个频段中，表现最好的模型 (LSM-Mistral) 实现了 3.25 dB 的均方根误差 (RMSE)，且 97% 的预测平均绝对误差 (MAE) 低于 5 dB。通过在不同地点收集的数据上对模型进行微调，展示了 LSMs 的泛化能力，其 RMSE 保持在 3.7 dB 以下。这些结果表明，广泛使用的仅解码器 Transformer 架构可以作为大规模射频频谱预测的有效预测模型。

## Abstract
Dynamic spectrum access (DSA) has become a key pillar of next-generation wireless systems to address the spectrum scarcity due to the rapid growth of connected devices. Accurate short-term spectrum forecasting is critical for DSA, where data-driven approaches have proven most effective. Recent advances in and widespread adoption of large language model (LLM) architectures present new opportunities for spectrum prediction. In this paper, foundational large spectrum models (LSMs) are presented. A novel RF tokenizer is introduced to convert raw IQ measurements into token sequences by mapping each power-spectral density value to a fixed vocabulary along with embedding gain, frequency, FFT bin, and timestamp information. Five established open-source LLM architectures (Gemma-2B, GPT-2, LLaMA-7B, Mistral-7B, and Phi-1) are trained on this tokenized spectrum data for the task of spectrum forecasting, yielding LSMs. To leverage the scaling gains of LSMs, a fully automated outdoor wireless testbed is employed to collect over 22 TB of raw spectrum data across 33 sub-GHz frequency bands, yielding 8.4B tokens in total. Across all 33 bands, the best model (LSM-Mistral) achieves a root-mean-square error of 3.25 dB and 97% of predictions have a mean absolute error below 5 dB. Generalization of LSMs is illustrated by fine-tuning the models on data collected in different locations, where RMSE is maintained below 3.7 dB. These results demonstrate that widespread decoder-only transformer architectures can serve as effective predictive models for large-scale RF spectrum forecasting.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为**大频谱模型（Large Spectrum Models, LSMs）**的新型架构，旨在利用大语言模型（LLM）的序列建模能力来解决无线通信中的频谱预测问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
随着物联网（IoT）设备的激增，无线频谱资源日益稀缺。**动态频谱接入（DSA）**是提高频谱利用率的关键，而其核心挑战在于如何准确预测短期的频谱占用情况。
*   **研究动机：** 传统的统计模型或简单的深度学习模型（如LSTM）在处理高度动态、长程相关的射频（RF）数据时表现有限。
*   **核心思想：** 借鉴大语言模型的成功经验，将射频频谱数据“文本化”（Tokenization），并利用仅解码器（Decoder-only）的Transformer架构来捕捉频谱活动的复杂时空模式。

### 2. 核心方法论
论文提出了一个完整的从原始信号到预测结果的框架：
*   **数据预处理：** 将原始IQ数据通过短时傅里叶变换（STFT）转换为功率谱密度（PSD）图，并采用最大池化（Max-pooling）和修整平均法（Trimmed Mean）进行降维，以保留关键活动并去除离群值。
*   **RF分词器（RF Tokenizer）：** 
    *   将-118dBm到-18dBm的PSD值映射为101个离散Token。
    *   将元数据（增益、中心频率、频率仓索引、时间戳等）也转换为Token。
    *   引入特殊起始/结束标签（Token 102-127）来结构化元数据块。
*   **模型架构：** 采用了五种主流架构的缩减版：**Gemma、GPT-2、LLaMA、Mistral、Phi-1**。所有模型均保持了原始架构的注意力机制（如MQA、GQA、MHA）和激活函数。
*   **混合损失函数：** 针对高色散（HD）频段数据，设计了交叉熵（CE）与均方根（RMS）的混合损失函数，以增强模型对剧烈波动数据的学习能力。

### 3. 实验设计
*   **数据集：** 
    *   **BIG-RED数据集：** 在内布拉斯加州林肯市通过城市级测试平台收集，涵盖33个sub-GHz频段，原始数据量达**22 TB**，转化为**84亿个Token**。
    *   **辅助站点数据：** 用于验证泛化能力的异地小规模数据集（2.46亿Token）。
    *   **仿真数据：** 使用MATLAB 5G工具箱生成的不同信噪比（SINR）下的下行链路信号。
*   **基准模型（Benchmarks）：** 
    *   不同层数和隐藏层大小的堆叠式**LSTM**。
    *   专门的时间序列Transformer模型 **Time-GPT**。

### 4. 资源与算力
论文明确记录了实验的算力消耗：
*   **硬件：** 单张 **NVIDIA H100 GPU (80GB)**。
*   **训练时长：** 
    *   LSM-Gemma: 76.66 小时
    *   LSM-GPT: 82.16 小时
    *   LSM-LLaMA: 74.93 小时
    *   LSM-Mistral: 67.04 小时
    *   LSM-Phi: 90.49 小时
*   **总计：** 约 400 GPU 小时。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了33个不同的频段，包括授权频段和非授权频段。
*   **充分性：** 实验设计非常充分。不仅对比了五种不同的Transformer变体，还进行了跨地理位置的微调实验（Generalization）以及在不同干扰水平下的鲁棒性测试。
*   **公平性：** 通过控制模型参数规模（170M至400M之间）和统一的训练策略，确保了不同架构之间对比的客观性。

### 6. 主要结论与发现
*   **性能优异：** 表现最好的 **LSM-Mistral** 实现了 **3.25 dB** 的平均均方根误差（RMSE），远优于LSTM基准。
*   **架构差异：** 采用分组查询注意力（GQA）和多查询注意力（MQA）的架构（如LLaMA和Mistral）在处理动态频谱数据时比传统多头注意力（MHA）更具鲁棒性。
*   **泛化能力：** 预训练的LSM在仅使用不到3%的数据进行微调后，即可在全新地理位置实现极高的预测精度。
*   **Token化的有效性：** 证明了将连续的射频数值离散化为Token是可行的，且能有效利用LLM的推理能力。

### 7. 优点与亮点
*   **首创性：** 这是首个系统性地将通用LLM架构应用于大规模真实世界射频频谱预测的研究。
*   **数据贡献：** 贡献了22TB的真实世界频谱数据集，并开源了分词器和预训练模型，具有极高的学术价值。
*   **端到端框架：** 提出了一套完整的RF数据Token化方案，解决了射频数据与Transformer架构不兼容的问题。

### 8. 不足与局限
*   **计算开销：** 尽管预测精度高，但LSM的训练和推理成本远高于传统的LSTM，在嵌入式或实时性要求极高的射频设备上部署可能存在挑战。
*   **频段覆盖：** 目前主要集中在 sub-GHz 频段，尚未验证在毫米波（mmWave）或太赫兹等更高频率、更宽带宽场景下的表现。
*   **降维损失：** 为了适应计算资源，对原始数据进行了大幅度的下采样（从20M采样点降至256个点），可能会丢失某些极短促的突发信号特征。

（完）
