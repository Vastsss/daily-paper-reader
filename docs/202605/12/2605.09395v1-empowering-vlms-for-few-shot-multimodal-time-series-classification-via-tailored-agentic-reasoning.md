---
title: Empowering VLMs for Few-Shot Multimodal Time Series Classification via Tailored Agentic Reasoning
title_zh: 通过定制化智能体推理赋能视觉语言模型进行少样本多模态时间序列分类
authors: "Lin Li, Jiawei Huang, Qihao Quan, Dan Li, Boxin Li, Xiao Zhang, Erli Meng, Wenjie Feng, Jian Lou, See-Kiong Ng"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09395v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 用于多模态时间序列分类的视觉语言模型智能体推理
tldr: MarsTSC是首个利用视觉语言模型（VLM）智能体推理进行少样本多模态时间序列分类的框架。它通过生成器、反射器和修改器三个角色的协作，构建并动态优化自进化知识库。该框架还引入了测试时更新策略以应对少样本偏差和分布偏移，在12个基准测试中表现优异，不仅显著提升了分类精度，还提供了基于特征证据的可解释推理依据。
selection_source: fresh_fetch
motivation: 针对少样本多模态时间序列分类中特征提取难和缺乏可解释性的问题，探索如何利用VLM的推理能力提升分类性能。
method: 提出MarsTSC框架，通过生成器推理、反射器诊断错误及修改器更新知识库的协同机制，并结合测试时更新策略动态优化上下文。
result: 在12个主流基准测试和6种VLM骨干网络上的实验表明，MarsTSC在少样本条件下显著优于传统模型和现有的时间序列大模型。
conclusion: 该研究证明了定制化的智能体推理和自进化知识库能有效增强VLM处理复杂时间序列任务的能力，并提供人类可读的决策依据。
---

## 摘要
在本文中，我们提出了首个用于少样本多模态时间序列分类（TSC）的视觉语言模型（VLM）智能体推理框架（MarsTSC），该框架引入了一个自进化的知识库作为动态上下文，并通过反思性智能体推理进行迭代优化。该框架包含三个协作角色：i) 生成器（Generator）通过推理进行可靠的分类；ii) 反思器（Reflector）诊断推理错误的根本原因，针对生成器忽略的时间特征产生具有辨别力的见解；iii) 修改器（Modifier）对知识库应用经过验证的更新，以防止上下文崩溃。我们进一步引入了一种测试时更新策略，以实现谨慎且持续的知识库优化，从而缓解少样本偏差和分布偏移。在 12 个主流时间序列基准测试上的广泛实验表明，MarsTSC 在 6 个 VLM 骨干网络上均实现了显著且一致的性能提升，在少样本条件下优于传统的和基于基础模型的时间序列基准方法，同时生成可解释的原理，将每个分类决策建立在人类可读的特征证据之上。

## Abstract
In this paper, we propose the first VL$\underline{\textbf{M}}$ $\underline{\textbf{a}}$gentic $\underline{\textbf{r}}$easoning framework for few-$\underline{\textbf{s}}$hot multimodal $\underline{\textbf{T}}$ime $\underline{\textbf{S}}$eries $\underline{\textbf{C}}$lassification ($\textbf{MarsTSC}$), which introduces a self-evolving knowledge bank as a dynamic context iteratively refined via reflective agentic reasoning. The framework comprises three collaborative roles: i) Generator conducts reliable classification via reasoning; ii) Reflector diagnoses the root causes of reasoning errors to yield discriminative insights targeting the temporal features overlooked by Generator; iii) Modifier applies verified updates to the knowledge bank to prevent context collapse. We further introduce a test-time update strategy to enable cautious, continuous knowledge bank refinement to mitigate few-shot bias and distribution shift. Extensive experiments across 12 mainstream time series benchmarks demonstrate that $\textbf{MarsTSC}$ delivers substantial and consistent performance gains across 6 VLM backbones, outperforming both classical and foundation model-based time series baselines under few-shot conditions, while producing interpretable rationales that ground each classification decision in human-readable feature evidence.

---

## 论文详细总结（自动生成）

以下是对论文《Empowering VLMs for Few-Shot Multimodal Time Series Classification via Tailored Agentic Reasoning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：时间序列（TS）数据包含数值、文本和图像等多种互补模态。虽然视觉语言模型（VLM）在处理多模态数据上展现出潜力，但在**少样本（Few-Shot）**场景下，现有方法存在两大局限：
    1.  **上下文静态性**：依赖固定的提示词或统计摘要，无法随样本学习而进化。
    2.  **缺乏定制化推理**：缺乏针对时间序列特征（如趋势、周期、异常点）的深度推理机制，难以从极少量标签中榨取判别性知识。
*   **核心问题**：如何引导 VLM 的推理行为，为少样本多模态时间序列分类构建一个恰当且动态进化的上下文？

### 2. 方法论：MarsTSC 框架
MarsTSC 提出了一个基于智能体推理的框架，核心思想是通过**自进化知识库（Knowledge Bank）**来存储和更新任务相关的判别性特征。
*   **核心组件：结构化知识库**：
    *   包含类内特征、类间差异、易错点、区分策略及领域知识。
    *   采用**原子化设计**（Bullet points），支持精确的增删改操作，防止上下文崩溃。
*   **关键技术：三大协作角色**：
    1.  **生成器 (Generator)**：结合知识库和多模态输入（数值统计+折线图），生成分类决策及推理轨迹（CoT）。
    2.  **反射器 (Reflector)**：诊断错误原因。通过对比查询样本与正确/错误类别的典型样本，提取被忽略的微观特征。
    3.  **修改器 (Modifier)**：根据反射器的见解，对知识库执行 `ADD`（增加）、`MODIFY`（修改）、`DELETE`（删除）操作。
*   **测试时更新 (Test-Time Update, TTU)**：
    *   **双阶段生成**：第一遍推理作为“可疑参考”，第二遍进行自我复核以减少幻觉。
    *   **延迟更新机制**：将知识分为原型、参考和候选三级，仅当候选知识在测试流中多次验证后才提升等级，确保知识库不被噪声污染。

### 3. 实验设计
*   **数据集**：涵盖 UCR/UEA 存档中的 **12 个主流基准数据集**，涉及医疗监控（ECG200）、工业识别、环境传感等 7 个领域。
*   **实验场景**：主要聚焦于 **3-shot** 设置（每类仅 3 个训练样本）。
*   **对比方法 (Benchmarks)**：
    *   **传统机器学习**：DrCIF, HIVE-COTE 2.0。
    *   **深度学习/时间序列模型**：TimesNet, Autoformer, PatchTST。
    *   **大模型基础方法**：GPT4TS, MOMENT（时间序列基础模型）。
*   **VLM 骨干网络**：测试了 Gemini-3.1-pro（主模型）、GPT-5、Qwen-3.5、Kimi-K2.5 等 6 种模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：由于该框架主要依赖于调用大模型的 API（如 Gemini, GPT-5）进行推理和文本处理，其核心开销在于 API 的 Token 消耗和推理延迟，而非传统的模型参数梯度训练。

### 5. 实验数量与充分性
*   **实验规模**：在 12 个数据集上进行了详尽测试，并针对 6 种不同的 VLM 后端验证了迁移性。
*   **消融实验**：涵盖了测试时更新、双阶段生成、训练期反思等关键模块的有效性验证。
*   **充分性评价**：实验设计非常充分。作者不仅对比了少样本场景，还展示了 MarsTSC（3-shot）在某些数据集上能抗衡甚至超越传统模型在**全样本（Full-shot）**下的表现。此外，还通过随机种子测试验证了样本选择的鲁棒性。

### 6. 主要结论与发现
*   **性能卓越**：MarsTSC 在少样本分类精度上显著优于所有基线方法，平均准确率提升明显。
*   **推理价值**：直接使用 VLM 进行推理效果有限，但通过“智能体反思+动态知识库”引导后，性能大幅跃升。
*   **自进化能力**：知识库的 Token 数量在训练初期增长后趋于稳定，证明了原子化更新机制能有效筛选高质量知识，避免冗余。
*   **可解释性**：框架能生成人类可读的推理依据，将决策过程锚定在具体的波形特征或统计指标上。

### 7. 优点（亮点）
*   **首创性**：首次将 VLM 智能体推理引入多模态时间序列分类领域。
*   **知识管理精细**：通过原子化 Bullet points 和“增删改”动作，解决了长上下文学习中的信息丢失和知识污染问题。
*   **测试时自适应**：提出的延迟更新策略（TTU）为处理时间序列的分布偏移（Distribution Shift）提供了新思路。
*   **多模态融合**：巧妙结合了数值统计的精确性和图像表示的直观性。

### 8. 不足与局限
*   **推理成本**：由于涉及多次迭代反思和双阶段生成，API 调用成本和推理延迟可能较高，难以满足超高频实时分类需求。
*   **依赖底座能力**：实验显示底座 VLM 的逻辑推理能力直接影响最终效果，在较弱的模型上性能增益会打折扣。
*   **图像模态单一**：目前主要使用折线图（Line Plot），对于极长序列或高维数据的可视化表达可能存在信息损失。

（完）
