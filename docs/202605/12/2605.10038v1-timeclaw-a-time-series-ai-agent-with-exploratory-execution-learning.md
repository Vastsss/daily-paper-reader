---
title: "TimeClaw: A Time-Series AI Agent with Exploratory Execution Learning"
title_zh: TimeClaw：一种具有探索性执行学习的时间序列 AI 智能体
authors: "Hangchen Liu, Dongyuan Li, Renhe Jiang, Jiewen Deng, Weiwei Ye, Yoshihide Sekimoto"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10038v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 基于LLM和基础模型的时间序列AI智能体
tldr: TimeClaw是一个针对时间序列分析的AI智能体框架，旨在解决现有LLM智能体在处理金融、天气等任务时仅关注单次执行而忽视探索性学习的问题。它通过“探索-比较-蒸馏-注入”四阶段循环，将探索性执行转化为可重用的分层经验。该方法在保持基础模型冻结的同时，利用指标监督和工具丢弃策略提升了推理质量，在17项任务中表现优于基准模型。
selection_source: fresh_fetch
motivation: 现有的时间序列AI智能体缺乏从探索性执行中学习的能力，且容易陷入工具先验坍缩，导致无法在多种有效路径中找到最优解。
method: 提出了TimeClaw框架，通过探索、比较、蒸馏和注入四个阶段，将执行经验转化为分层知识，并结合任务感知工具丢弃策略优化推理。
result: 在涵盖金融和天气预测等17个任务的评估中，TimeClaw相比基准模型取得了显著且一致的性能提升。
conclusion: 研究表明，科学分析系统的瓶颈不仅在于执行能力，更在于如何有效地比较、蒸馏和重用探索性经验。
---

## 摘要
时间序列分析是金融和天气等领域预测、监测和决策的基础，在这些领域中，解决任务通常需要数值准确性和上下文推理。最近的进展已从专门的神经预测器转向基于大语言模型（LLM）和基础模型的方法，这些模型能够对时间序列输入进行推理并使用外部工具。然而，大多数此类系统仍然是以执行为中心的：它们专注于解决当前实例，但很少从探索性执行中学习。这在可验证的数值设置中尤其受限，因为在这种设置下，多个候选执行和工具使用程序可能对任务都是有效的，但在定量质量上却存在显著差异，且早期的成功可能会触发工具先验崩溃（tool-prior collapse），从而抑制进一步的探索。为了解决这一局限性，我们提出了 TimeClaw，这是一个探索性执行学习框架，它通过“探索、比较、蒸馏和重新注入”四个阶段的循环，将探索性执行转化为可重用的分层蒸馏经验。TimeClaw 结合了指标监督的探索性执行学习、任务感知的工具丢弃（tool dropout）以及用于推理时重新注入的分层蒸馏经验，同时保持基础模型冻结并避免在线测试时自适应。在与 MTBench 对齐的评估中，涵盖了金融和天气预测及推理任务的 17 个任务，TimeClaw 相比基准模型取得了持续的提升。这些结果表明，对于科学系统而言，瓶颈不仅在于执行时的能力，还在于如何对探索性经验进行比较、蒸馏和重用。

## Abstract
Time series analysis underpins forecasting, monitoring, and decision making in domains such as finance and weather, where solving a task often requires both numerical accuracy and contextual reasoning. Recent progress has moved from specialized neural predictors to approaches built on LLMs and foundation models that can reason over time series inputs and use external tools. However, most such systems remain execution-centric: they focus on solving the current instance but learn little from exploratory execution. This is especially limiting in verifiable numeric settings, where multiple candidate executions and tool-use procedures may all be task-valid yet differ sharply in quantitative quality, and where early success can trigger tool-prior collapse that suppresses further exploration. To address this limitation, we present TimeClaw, an exploratory execution learning framework that turns exploratory execution into reusable hierarchical distilled experience through a four-stage loop: Explore, Compare, Distill, and Reinject. TimeClaw combines metric-supervised exploratory execution learning, task-aware tool dropout, and hierarchical distilled experience for inference-time reinjection, while keeping the base model frozen and avoiding online test-time adaptation. In an MTBench-aligned evaluation with 17 tasks that span finance and weather prediction and reasoning tasks, TimeClaw delivers consistent gains over the baselines. These results suggest that, for scientific systems, the bottleneck is not only execution-time capability, but how exploratory experience is compared, distilled, and reused.

---

## 论文详细总结（自动生成）

以下是对论文《TimeClaw: A Time-Series AI Agent with Exploratory Execution Learning》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在金融预测、天气分析等科学领域，时间序列任务不仅要求数值准确，还需要复杂的上下文推理。现有的基于大语言模型（LLM）的智能体大多是“执行中心化”的，即专注于解决当前任务，而无法从探索过程中积累可重用的经验。
*   **核心挑战**：
    1.  **仅完成监督不足**：在数值任务中，多个执行路径可能都“完成”了任务，但数值精度（如MAE/RMSE）差异巨大。仅凭“是否完成”作为反馈信号无法区分优劣。
    2.  **工具先验坍缩（Tool-prior Collapse）**：智能体在探索早期容易过度依赖少数几个成功过的工具，导致探索范围萎缩，无法发现更优的工具组合。
*   **核心目标**：开发一个能够通过探索性执行进行学习，并将经验转化为外部知识库以辅助未来推理的智能体框架。

### 2. 方法论
TimeClaw 提出了一个 **“探索-比较-蒸馏-注入”（Explore-Compare-Distill-Reinject）** 的四阶段循环：
*   **指标监督的探索性学习**：在探索阶段，智能体生成多个候选执行路径（Branches），利用真实标签（Ground Truth）和数值指标（如MAE）对不同路径进行定量评价，识别出“最优执行策略”。
*   **任务感知工具丢弃（Task-aware Tool Dropout）**：为了缓解先验坍缩，系统根据工具的历史使用频率动态计算保留概率。频繁使用的工具会被暂时屏蔽，强制智能体尝试“冷门”工具，从而维持执行路径的多样性。
*   **分层蒸馏经验（Hierarchical Distilled Experience）**：将探索得到的经验存储在模型权重之外，分为四个层次：
    *   **Notes**：原始的单次执行记录。
    *   **Memory**：结构化的可重用规则（9元组形式，包含适用条件、推荐/避开的工具等）。
    *   **Tools**：特定工具的使用建议。
    *   **Skills**：针对特定任务类型的标准作业程序（SOP）。
*   **推理时重新注入**：在正式推理时，基础模型保持冻结，系统根据当前任务检索相关的蒸馏经验并注入 Prompt 中，引导智能体做出更优决策。

### 3. 实验设计
*   **数据集/场景**：涵盖金融（股票预测、MACD预测、趋势分类、新闻相关性推理）和天气（气温预报、极值预测、趋势推理、MCQA问答）两大领域。
*   **Benchmark**：使用了与 **MTBench** 对齐的评估套件，包含 17 个任务，共计 30 个评估指标。
*   **对比方法**：
    *   **闭源/通用模型**：GPT-4o, Gemini, Claude, DeepSeek, 以及当时最强的基准 GPT-5.4。
    *   **开源模型**：Qwen-3, Llama-3 系列及其变体。

### 4. 资源与算力
*   **硬件配置**：实验使用了 **2 张 NVIDIA RTX 3090 GPU**。
*   **训练/探索时长**：在当前设置下，完成一个完整的探索运行大约需要 **1 天**。
*   **算力开销说明**：主要的计算成本和 Token 消耗集中在探索阶段，因为需要为每个样本生成并评估多个候选路径。

### 5. 实验数量与充分性
*   **实验规模**：在 17 个任务上进行了广泛测试，报告了 30 组指标数据。
*   **消融实验**：设计了专门的消融组，对比了“无经验注入”和“无记忆层”的效果，验证了分层经验和工具丢弃策略的独立贡献。
*   **充分性评价**：实验设计较为客观，通过在独立的探索语料库上学习并在未见过的测试集上评估，排除了数据泄露风险。对比基准涵盖了主流的最强模型，结果具有说服力。

### 6. 主要结论与发现
*   **性能领先**：TimeClaw 在 30 个评估指标中的 **27 个** 上达到了最优性能，尤其在数值预测任务（如天气预报 MSE）中提升显著。
*   **学习价值**：证明了对于科学智能体，瓶颈不仅在于执行能力，更在于如何比较和重用探索经验。
*   **策略优化**：通过学习，智能体能够从简单的“数值映射”进化为“多步推理策略”（例如：先检测异常值，再结合新闻情绪进行偏差修正）。

### 7. 优点
*   **非侵入式学习**：无需微调模型参数，通过外部知识库实现能力的持续进化，适合闭源模型。
*   **定量反馈机制**：将数值指标引入智能体学习循环，解决了科学领域中“对错”之外的“优劣”评价问题。
*   **抗坍缩设计**：创新的工具丢弃机制有效解决了强化学习中常见的探索与利用（Exploration vs. Exploitation）失衡问题。

### 8. 不足与局限
*   **成本问题**：探索阶段的 Token 消耗和 API 调用成本较高，大规模推广需考虑经济性。
*   **实时性限制**：目前采用的是离线探索模式，尚未实现真正的在线实时学习（Online Learning）。
*   **领域依赖**：虽然在金融和天气领域表现优异，但在缺乏明确数值评价指标的开放域任务中，该框架的有效性尚未得到验证。
*   **工具库依赖**：智能体的表现高度受限于预设工具库的丰富程度和基础模型的指令遵循能力。

（完）
