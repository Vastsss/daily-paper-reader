---
title: "Event Fields: Learning Latent Event Structure for Waveform Foundation Models"
title_zh: 事件场：学习波形基础模型的潜在事件结构
authors: "Li Na, Yuanyun Zhang, Shi Li"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08685v1"
tags: ["query:ts-fm"]
score: 9.0
evidence: 生理时间序列的波形基础模型
tldr: 该研究提出了一种名为 Event Fields 的新型波形基础模型，将生理时间序列视为潜在事件过程而非传统的序列令牌。通过自监督学习框架，模型在随机分割和时频投影间保持一致性，捕捉临床相关的事件结构。该方法在心律失常分类、血液动力学预测等任务中表现优异，显著提升了模型的鲁棒性和标注效率，为医疗基础模型提供了新的归纳偏置。
selection_source: fresh_fetch
motivation: 传统的基于序列或补丁的表示方法难以捕捉生理信号中具有临床意义的、跨越时间的潜在事件结构。
method: 引入一种自监督学习框架，结合分割感知编码器和潜在交互算子，通过对齐不同分割和时频投影下的表示来学习事件级组织。
result: 在心律失常分类、血液动力学预测和波形检索等多个生理基准测试中，该方法在性能、鲁棒性和标签效率上均优于强序列基准模型。
conclusion: 从以信号为中心转向以事件为中心的表示，为生理动力学建模提供了更合适的归纳偏置，是扩展医疗基础模型的一条有效路径。
---

## 摘要
我们提出了一类新型波形基础模型，通过将生理时间序列建模为潜在事件过程的实现，背离了传统的基于序列的表示方法。我们的方法并非将信号视为局部 token 或 patch 的集合，而是假设具有临床意义的结构源自时间上延伸且相互作用的事件，这些事件的边界和动力学并非直接可观测。为了捕捉这种结构，我们引入了一个自监督学习框架，该框架强制要求同一波形的随机分割和时频投影之间保持一致性，从而鼓励生成对信号级扰动具有不变性、同时保留事件级组织的表示。所得模型结合了分割感知编码器和潜在交互算子，后者用于捕捉推断事件之间的依赖关系，并通过共享事件表示对齐模态，从而自然地扩展到多模态设置。在包括心律失常分类、血流动力学预测和波形检索在内的一系列生理基准测试中，与强大的基于序列的基线模型相比，所提方法提高了性能、鲁棒性和标签效率。这些结果表明，从以信号为中心转向以事件为中心的表示，为建模生理动力学提供了更合适的归纳偏置，并为医疗保健领域基础模型的扩展提供了一条互补路径。

## Abstract
We propose a new class of waveform foundation models that departs from conventional sequence based representations by modeling physiological time series as realizations of latent event processes. Rather than treating signals as collections of local tokens or patches, our approach assumes that clinically meaningful structure arises from temporally extended, interacting events whose boundaries and dynamics are not directly observed. To capture this structure, we introduce a self supervised learning framework that enforces consistency across stochastic segmentations and time frequency projections of the same waveform, encouraging representations that are invariant to signal level perturbations while preserving event level organization. The resulting model combines a segmentation aware encoder with a latent interaction operator that captures dependencies among inferred events, and naturally extends to multimodal settings by aligning modalities through shared event representations. Across a range of physiological benchmarks, including arrhythmia classification, hemodynamic prediction, and waveform retrieval, the proposed method improves performance, robustness, and label efficiency relative to strong sequence based baselines. These results suggest that shifting from signal centric to event centric representations provides a more appropriate inductive bias for modeling physiological dynamics and offers a complementary path to scaling foundation models in healthcare.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Event Fields** 的新型波形基础模型，旨在改变生理信号（如心电图 ECG、脉搏波 PPG 等）的建模范式。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的生理波形基础模型大多沿用自然语言处理（NLP）或计算机视觉（CV）的思路，将信号视为局部令牌（tokens）或补丁（patches）的序列。
*   **研究动机**：作者认为这种“以信号为中心”的方法与临床推理不符。临床医生通常通过更高层次的抽象（如心跳、周期、爆发、转换）来解释波形，这些事件在时间上是延伸的、相互作用的，且边界往往不可直接观测。
*   **整体含义**：论文提出将生理信号建模为“潜在事件场”的实现，通过捕捉事件间的结构和动力学，为医疗基础模型提供更符合生理特性的归纳偏置。

### 2. 方法论：核心思想与关键技术
*   **潜在事件场模型**：将波形 $x(t)$ 视为多个潜在事件的连续叠加。每个事件由潜在状态、起始时间、持续时间和振幅参数组成。
*   **随机事件分割（Stochastic Segmentations）**：由于事件边界不可见，模型通过随机采样多种可能的分割方案来处理边界模糊性，强制模型在不同分割下保持表示的一致性。
*   **时频投影（Time-Frequency Projections）**：通过随机的子带滤波、相位扰动、时间翘曲等操作生成波形的多样化视图，迫使模型学习对信号级噪声不敏感但对事件结构敏感的特征。
*   **架构设计**：
    *   **分层编码器**：包含局部特征提取、分割感知聚合（将局部特征池化为事件嵌入）。
    *   **潜在交互算子（Latent Interaction Operator）**：利用核化算子（类似于注意力机制但带有时间距离偏置）建模事件间的周期性耦合和长程依赖。
*   **自监督学习目标**：
    *   **一致性损失（Lcons）**：使用 InfoNCE 损失对齐同一波形在不同分割和投影下的全局表示。
    *   **分割正则化（Lseg）**：通过稀疏性、平滑性和跨视图一致性约束，引导模型预测有意义的事件边界。

### 3. 实验设计
*   **数据集**：整合了来自临床和可穿戴设备的 ECG、PPG 和 EMG 大规模无标注数据集进行预训练。
*   **评估任务（Benchmark）**：
    1.  **心律失常分类**（ECG）。
    2.  **血流动力学状态预测**（多模态 ECG-PPG）。
    3.  **波形检索**（寻找生理相似信号）。
*   **对比方法**：掩码重构模型（类似 MAE）、对比序列编码器（类似 SimCLR）、时频混合模型。

### 4. 资源与算力
*   **算力说明**：论文提到使用了 AdamW 优化器，批大小（Batch Size）为 512。
*   **缺失信息**：文中**未明确说明**具体的 GPU 型号（如 A100/H100）、GPU 数量以及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主任务性能对比（表1）。
    *   针对不同标注比例（1%、5%、10%）的标签效率实验（表2）。
    *   针对噪声、时间翘曲、频率掩码的鲁棒性测试（表3）。
    *   消融实验（表4），分别验证了分割一致性、时频投影和交互算子的作用。
*   **充分性评价**：实验设计较为全面，涵盖了分类、检索、多模态及鲁棒性等多个维度，并使用了 5 个随机种子取平均值，结果具有统计学意义。

### 6. 主要结论与发现
*   **性能领先**：Event Fields 模型在所有基准任务上均优于传统的基于序列的模型，尤其在多模态对齐和检索任务中提升显著。
*   **标签效率高**：在仅有 1% 标注数据的情况下，该模型表现出极强的泛化能力，远超基线模型。
*   **鲁棒性强**：由于预训练中引入了时频投影，模型对信号失真和传感器伪影具有更强的抵抗力。
*   **可解释性**：定性分析显示，模型自动学习到的事件边界与临床上的 QRS 波群等生理特征高度吻合。

### 7. 优点
*   **范式创新**：从“信号片段”转向“潜在事件”，更贴合生理信号的本质生成过程。
*   **多模态对齐**：通过共享潜在事件场自然地处理不同采样率和同步问题的多模态数据。
*   **结构化偏置**：引入的交互算子能显式建模生理周期性，增强了长程依赖的捕捉能力。

### 8. 不足与局限
*   **可解释性局限**：虽然边界预测有意义，但潜在事件的语义（即每个事件具体代表什么生理过程）仍缺乏直接的数学可解释性或硬性约束。
*   **计算开销**：由于需要为每个样本生成多个随机视图和分割，预训练阶段的计算成本高于简单的掩码重构方法。
*   **超参数敏感性**：随机分割的长度分布和投影算子的选择可能对不同类型的生理信号（如脑电 EEG vs 肌电 EMG）有不同的影响，需进一步调优。

（完）
