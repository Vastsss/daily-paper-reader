---
title: "LeapTS: Rethinking Time Series Forecasting as Adaptive Multi-Horizon Scheduling"
title_zh: LeapTS：将时间序列预测重新构思为自适应多时域调度
authors: "Sheng Pan, Ming Jin, Bo Du, Shirui Pan"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10292v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 将时间序列预测视为自适应多步调度
tldr: 本研究提出LeapTS框架，将时间序列预测重新定义为自适应多步调度过程。针对传统模型固定映射导致的预测不灵活问题，LeapTS结合分层控制器动态选择预测尺度，并利用神经常微分方程驱动连续状态演化。实验表明，该方法在提升预测精度的同时，显著提高了推理速度，并能通过追踪调度轨迹自主捕捉非平稳动态。
selection_source: fresh_fetch
motivation: 传统预测模型通常采用固定的历史到目标的映射方式，限制了模型在预测过程中适应演化上下文的能力并导致时间解耦。
method: 引入分层控制器动态选择预测尺度与步长，并利用神经常微分方程驱动连续时间状态演化以耦合不规则动态。
result: "LeapTS在多个数据集上将预测性能提升了至少7.4%，同时推理速度比主流Transformer模型快2.6至5.3倍。"
conclusion: 将预测任务重构为动态调度过程能有效捕捉非平稳动态，在提升预测精度的同时大幅优化了推理效率。
---

## 摘要
时间序列预测是许多现实应用中的重要工具，支持资源优化和决策制定等任务。尽管架构上取得了显著进展，但大多数现代模型仍将预测任务视为从历史到目标时域的固定映射。这导致了未来时间点之间的时间解耦，并限制了模型在预测过程中适应不断演变的上下文的能力。在这项工作中，我们提出了 LeapTS，这是一个将时间序列预测重新表述为预测时域上的动态调度过程的新型框架。具体而言，LeapTS 使用以下方式将预测过程组织为多级决策：(1) 分层控制器，用于在每一步动态选择最佳预测尺度和推进长度；(2) 由神经受控微分方程（Neural Controlled Differential Equations）驱动的连续时间状态演化。在此过程中，受控更新机制显式地将不规则的时间动力学与离散调度反馈耦合在一起。在真实世界和合成数据集上的广泛评估表明，LeapTS 将整体预测性能提高了至少 7.4%，同时与代表性的基于 Transformer 的模型相比，实现了 2.6 倍至 5.3 倍的推理加速。此外，通过显式追踪调度轨迹，我们揭示了模型如何自主调整其预测行为以捕捉非平稳动力学。

## Abstract
Time series forecasting serves as an essential tool for many real-world applications, supporting tasks such as resource optimization and decision-making. Despite significant architectural advancements, most modern models still treat forecasting task as a fixed mapping from history to target horizons. This induces temporal decoupling across future time points and limits the model's ability to adapt to the evolving context as forecasting progresses. In this work, we present LeapTS, a novel framework that reformulates time series forecasting as a dynamic scheduling process over the prediction horizon. Specifically, LeapTS organizes the forecasting process into multi-level decisions using: (1) the hierarchical controller to dynamically select the optimal prediction scale and advancement length at each step, and (2) continuous-time state evolution driven by neural controlled differential equations. Within this process, the controlled update mechanism explicitly couples the irregular temporal dynamics with discrete scheduling feedback. Extensive evaluations on both real-world and synthetic datasets demonstrate that LeapTS improves overall forecasting performance by at least 7.4% while achieving a 2.6$\times$ to 5.3$\times$ inference speedup over representative Transformer-based models. Furthermore, by explicitly tracing the scheduling trajectories, we reveal how the model autonomously adapts its forecasting behavior to capture non-stationary dynamics.

---

## 论文详细总结（自动生成）

这是一份关于论文《LeapTS: Rethinking Time Series Forecasting as Adaptive Multi-Horizon Scheduling》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：时间序列预测在资源优化和决策中至关重要。目前主流深度学习模型主要遵循两种范式：**递归式（Recursive）**（逐步预测，易产生误差累积且计算开销大）和**直接式（Direct）**（一次性映射全时域，忽略了未来时间点间的动态依赖和上下文演化）。
*   **核心问题**：现有方法缺乏“上下文适应性”，无法根据预测过程中不断变化的动态特征（如平稳期与波动期）灵活调整预测策略。
*   **论文目标**：提出 **LeapTS** 框架，将预测任务重构为一个**自适应的多时域调度过程**，使模型能像人类调度员一样，根据当前状态动态决定“看多远”和“怎么看”。

### 2. 方法论
LeapTS 结合了全局粗略预测与局部精细调度分支，核心组件包括：
*   **分层控制器 (H-Controller)**：
    *   **高层调度**：利用 Gumbel-Softmax 机制在“短、中、长”三个时间尺度中做出离散选择。
    *   **底层调度**：在选定的尺度范围内，回归预测具体的推进步长（Advancement Length）。
*   **状态演化与更新机制**：
    *   **步内摘要更新 (Intra-Step)**：将当前步生成的预测片段压缩为反馈向量，指导下一步决策。
    *   **步间连续演化 (Inter-Step)**：采用**神经受控微分方程 (NCDE)** 驱动隐藏状态演化。NCDE 能够处理调度过程中产生的不规则时间增量，显式耦合时间动力学与调度反馈。
*   **可微分调度**：通过引入“软掩码（Soft Mask）”技术，解决了离散步长选择在反向传播中的不可微问题，实现了端到端训练。

### 3. 实验设计
*   **数据集**：
    *   **真实世界数据**：13个多变量数据集（包括 ETT 系列、Electricity、Weather、Traffic、Exchange、SP500、ILI、COVID、Wiki）。
    *   **单变量数据**：M4 竞赛数据集（涵盖年、季、月、周、日、小时频率）。
    *   **合成数据**：设计了三种场景（高维异构、因果驱动、隐变量驱动）以测试模型在极端非平稳环境下的鲁棒性。
*   **Benchmark 与对比方法**：
    *   对比了包括 **iTransformer, PatchTST, TimeMixer, TimesNet, DLinear, TSMixer, Amplifier, AMD** 等在内的多种最先进（SOTA）模型。

### 4. 资源与算力
*   **硬件环境**：使用了多块 **NVIDIA A100 80GB GPU**。
*   **训练细节**：所有实验均重复 5 次以获取平均性能和置信区间。
*   **效率表现**：论文明确指出，LeapTS 在推理速度上比代表性的 Transformer 模型快 **2.6倍至5.3倍**，且内存占用更低。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的对比实验，涵盖了长短期预测、单/多变量预测。
*   **消融实验**：针对调度分支、分层结构、CDE 单元等核心组件进行了详尽的消融测试。
*   **分析深度**：提供了调度轨迹的可视化、波动率与调度行为的相关性分析、以及预测误差上界的数学证明。
*   **评价**：实验设计非常充分，通过合成数据验证了模型在处理“黑盒”动力学系统时的优越性，结果具有高度的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：LeapTS 在真实数据集上的 MAE 平均提升了 **7.4%**，在 M4 竞赛指标（OWA）上取得第一。
*   **自适应调度行为**：分析发现模型展现出“波动感知”能力——在低波动区域倾向于选择长步长以提高效率，在高波动或转折点区域自动切换为短步长以捕捉细节。
*   **效率优势**：通过跳跃式的调度而非逐点预测，大幅提升了长时域预测的推理效率。

### 7. 优点
*   **范式创新**：打破了传统的固定映射思维，引入了动态调度的视角，为非平稳序列预测提供了新路径。
*   **可解释性**：调度的轨迹（尺度选择和步长）是显式可追踪的，有助于理解模型的决策逻辑。
*   **数学支撑**：利用 NCDE 处理不规则时间序列，理论基础扎实，并给出了误差界限证明。

### 8. 不足与局限
*   **泛化性挑战**：目前模型主要在特定数据集上从头训练，虽然在合成数据上表现良好，但其作为“通用大模型”在零样本（Zero-shot）场景下的表现仍需进一步探索。
*   **实现复杂度**：相比于简单的线性模型（如 DLinear），LeapTS 的分层控制器和 NCDE 增加了算法实现的复杂度和超参数调优的难度。

（完）
