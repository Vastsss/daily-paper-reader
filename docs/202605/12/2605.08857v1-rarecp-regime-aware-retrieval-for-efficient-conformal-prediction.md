---
title: "RareCP: Regime-Aware Retrieval for Efficient Conformal Prediction"
title_zh: RareCP：面向高效符合预测的状态感知检索
authors: "Manuel Heurich, Maximilian Granz, Tim Landgraf"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08857v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 时间序列预测的不确定性量化
tldr: 针对时间序列预测中存在的非平稳性、漂移和异构误差行为，本文提出RareCP。该方法通过混合余弦注意力专家捕捉不同误差状态，并利用超网络适应时间漂移。通过检索最相关的校准样本并构建加权共形分位数，RareCP在保持覆盖率的同时显著提高了预测区间的效率，在GIFT-Eval基准测试中表现优于现有基准。
selection_source: fresh_fetch
motivation: 传统共形预测方法难以处理时间序列中的非平稳性，特别是平滑漂移和共存的多种误差状态。
method: 提出一种状态感知检索方法，结合混合专家模型捕捉误差状态，并利用超网络动态调整核参数以追踪时间漂移。
result: 在GIFT-Eval基准上，RareCP在维持经验覆盖率的前提下，比现有共形基准和基础模型的不确定性估计具有更高的区间效率。
conclusion: 实验证明，状态特定专家、漂移自适应核、稀疏检索和教师锚定机制共同提升了时间序列不确定性量化的性能。
---

## 摘要
时间序列预测不确定性量化的最新进展表明，符合预测（conformal prediction）可以提供可靠的预测区间，但标准的符合预测方法在时间依赖性、漂移和异构误差行为下往往效率低下。现有方法通常要么随时间更新覆盖不足率，要么学习无约束的校准权重，而没有明确区分非平稳性的两个核心来源：平滑漂移的误差分布和共存的不同误差状态。我们提出了 RareCP，一种用于自适应符合时间序列预测的状态感知检索方法。RareCP 通过余弦注意力专家混合学习局部校准表示，每个专家捕捉不同的误差状态，同时一个紧凑的超网络调整核参数以追踪时间漂移。给定一个新的预测上下文，RareCP 检索前 k 个最相关的校准样本，分配相似度权重，并对其有符号残差形成加权符合分位数，从而产生非对称预测区间。自适应核使用平滑区间评分目标进行训练，并在参数空间锚定到一个轻量级教师核，以保持稳定的局部表示。在 GIFT-Eval 基准测试中，RareCP 在保持经验覆盖率的同时，比近期的符合预测基准和基础模型的不确定性估计提高了区间效率。消融实验证实，特定状态专家、漂移自适应核、稀疏检索和教师锚定均对最终性能有所贡献。

## Abstract
Recent advances in uncertainty quantification for time series forecasting show that conformal prediction can provide reliable prediction intervals, yet standard conformal methods are often inefficient under temporal dependence, drift, and heterogeneous error behavior. Existing methods typically either update miscoverage rates over time or learn unconstrained calibration weights, without explicitly separating two central sources of nonstationarity: smoothly drifting error distributions and co-existing distinct error regimes. We introduce RareCP, a regime-aware retrieval method for adaptive conformal time series prediction. RareCP learns local calibration representations through a mixture of cosine-attention experts that each capture distinct error regimes, while a compact hypernetwork adapts the kernel parameters to track temporal drift. Given a new forecasting context, RareCP retrieves the top-k most relevant calibration examples, assigns similarity weights, and forms a weighted conformal quantile over their signed residuals, yielding asymmetric prediction intervals. The adaptive kernel is trained using a smooth interval score objective, with a parameter-space anchor to a lightweight teacher kernel to preserve stable local representations. On the GIFT-Eval benchmark, RareCP improves interval efficiency over recent conformal baselines and foundation model uncertainty estimates while maintaining empirical coverage. Ablations confirm that regime-specific experts, drift-adaptive kernels, sparse retrieval, and teacher anchoring each contribute to the final performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《RareCP: Regime-Aware Retrieval for Efficient Conformal Prediction》的结构化深度总结：

### 1. 核心问题与整体含义
该论文针对**时间序列预测中的不确定性量化（UQ）**问题展开研究。传统的符合预测（Conformal Prediction, CP）方法虽然能提供分布无关的覆盖率保证，但在处理时间序列时面临两大挑战：
*   **非平稳性（Non-stationarity）：** 时间序列违反了 CP 核心的“可交换性”假设，存在平滑的时间漂移（Drift）。
*   **异构误差状态（Heterogeneous Error Regimes）：** 预测误差往往不是均匀分布的，而是存在多个共存的、截然不同的误差状态（如平稳期与波动期）。
现有方法通常只关注其中之一，导致生成的预测区间（Prediction Intervals）要么过宽（效率低），要么无法及时适应状态切换。

### 2. 方法论
论文提出了 **RareCP**，一种状态感知的检索式自适应符合预测框架。其核心思想是：**通过检索过去相似状态下的残差来构建当前的预测区间。**

*   **状态感知检索（Regime-Aware Retrieval）：** 采用专家混合模型（MoE）架构。多个低容量的“余弦注意力专家”分别捕捉不同的残差状态。
*   **超网络自适应（Hypernetwork Adaptation）：** 使用一个紧凑的超网络，根据当前的预测上下文和数据集特征，动态生成检索映射函数的参数。这使得模型能够追踪平滑的时间漂移。
*   **稀疏 Top-k 机制：** 给定新样本，RareCP 从校准集中检索前 $k$ 个最相关的残差样本，并分配相似度权重。
*   **局部有符号残差校准：** 不同于传统的绝对残差，RareCP 使用有符号残差来构建局部累积分布函数（CDF），从而生成能够适应偏斜分布的**非对称预测区间**。
*   **训练机制：** 使用平滑的 Winkler 评分（Interval Score）作为损失函数进行端到端训练，并引入“教师锚定（Teacher Anchoring）”机制，将学生超网络生成的参数约束在预训练的轻量级教师模型附近，以保证表示的稳定性。

### 3. 实验设计
*   **数据集：** 使用了大规模基准测试 **GIFT-Eval**，包含两个子集：
    *   **Bench10：** 10 个核心数据集（涵盖能源、交通、气候、金融等）。
    *   **Bench22：** 完整的 22 个数据集，用于测试模型的泛化与迁移能力。
*   **预测后端（Backbones）：** 为了证明方法的通用性，RareCP 搭配了四种不同的模型：TiRex、TimesFM-2.5、TabPFN-TS（均为基础模型）以及传统的 ARIMA。
*   **对比方法：**
    *   **CP 基准：** Uniform SplitCP、ACI、DtACI、NexCP、HopCPT、KOWCPI、ResCP。
    *   **原生 UQ：** 直接对比基础模型（如 Chronos, Moirai, TimesFM 等）自带的不确定性估计头。
*   **评估指标：** 归一化 Winkler 评分（主指标，衡量区间效率）、区间宽度、经验覆盖率（Coverage）。

### 4. 资源与算力
*   **硬件环境：** 实验在一台配备单块 **NVIDIA A6000 GPU (48GB VRAM)** 和 64 核 CPU（256GB RAM）的服务器上完成。
*   **训练时长：** 论文提供了详细的时间统计。在 Bench10 上，RareCP 的单次训练（100 epoch）大约需要 **175.68 秒**，推理（预测）阶段每个数据集约需 **2.73 秒**。相比之下，它比简单的 ACI 慢，但比 ResCP 等复杂方法更高效。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 22 个数据集上进行了测试，并针对 Bench10 进行了扩展实验（将测试序列增加到 10 万步及全量数据），验证了长序列下的稳定性。
*   **消融实验：** 做了非常详尽的分解实验，验证了余弦注意力（+13.9% 提升）、Top-k 检索（+4.6%）、超网络（+1.7%）、MoE 专家混合（+2.1%）等每个组件的贡献。
*   **客观性：** 采用了 10 个随机种子取平均值，并为所有对比方法提供了相同的超参数搜索（HPO）预算（100 次 Optuna 试验），确保了对比的公平性。

### 6. 主要结论
*   **效率显著提升：** RareCP 在所有后端模型上均取得了最低的 Winkler 评分，意味着在保持相同覆盖率的前提下，它能提供更窄、更精准的区间。
*   **超越原生 UQ：** RareCP 作为一个后处理插件，其表现甚至优于许多大型基础模型（如 TimesFM）自带的不确定性估计。
*   **后端无关性：** 即使是为不具备概率输出的 ARIMA 模型加上 RareCP，其 UQ 质量也能超过部分专门的深度学习模型。
*   **状态切换能力：** 可视化结果证实，RareCP 能在波动剧烈时迅速放宽区间，在平稳期迅速收窄。

### 7. 优点（亮点）
*   **解耦设计：** 明确区分了“平滑漂移”和“离散状态切换”，并分别用超网络和 MoE 应对，设计精巧。
*   **非对称性：** 通过有符号残差处理，解决了时间序列中常见的正负误差不对称问题。
*   **理论支撑：** 提供了关于 Top-Kn 检索一致性的理论分析，证明了该方法在渐进意义下的有效性。

### 8. 不足与局限
*   **训练成本：** 相比于无需训练的 ACI 或简单的加权 CP，RareCP 需要离线训练过程，且超网络的引入增加了参数量（约 1.5M-5.5M）。
*   **极端异构性挑战：** 在 Bench22 这种极度异构的数据集上，虽然效率依然领先，但经验覆盖率有时会略低于目标值（如 0.72 vs 0.80），说明在处理突发性极端漂移时仍有改进空间。
*   **数据依赖：** 检索机制的效果高度依赖于校准集（Calibration Set）的质量和代表性。

（完）
