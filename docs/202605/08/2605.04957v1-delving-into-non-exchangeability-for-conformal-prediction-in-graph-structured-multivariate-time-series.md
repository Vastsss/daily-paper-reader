---
title: Delving into Non-Exchangeability for Conformal Prediction in Graph-Structured Multivariate Time Series
title_zh: 深入探讨图结构多元时间序列中符合预测的非交换性
authors: "Ruichao Guo, Xingyao Han, Luo Wenshui, Zhe Liu, Chen Gong, Hesheng Wang"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04957v1"
tags: ["query:llm-ts"]
score: 8.0
evidence: 多变量时间序列的点预测
tldr: 针对图结构多元时间序列预测中的不确定性量化问题，本文探讨了节点间耦合导致的非交换性挑战。提出“谱图条件交换性（SGCE）”概念，利用图小波变换将数据分解为代表全局趋势的低频和近似交换的高频成分。基于此开发的 SCALE 方法通过自适应门控机制在谱域进行符合预测，在保证理论覆盖率的同时显著提升了预测区间的效率。
selection_source: fresh_fetch
motivation: 传统的符合预测依赖交换性假设，而图结构时间序列中节点间的强耦合违反了这一前提，导致直接应用 CP 方法不可靠。
method: 提出 SGCE 理论并开发 SCALE 方法，利用图小波分解高低频成分，并在低频嵌入的指导下对高频残差进行自适应符合预测。
result: 在真实交通数据集上的实验证明，SCALE 不仅实现了有效的覆盖率保证，且在覆盖率与区间效率的权衡上显著优于现有最先进方法。
conclusion: 本研究通过谱域分解和条件交换性有效解决了图数据中的非交换性问题，为复杂时空数据的不确定性量化提供了创新的解决方案。
---

## 摘要
图结构多元时间序列的点预测是一个基础问题，但此类预测的严谨不确定性量化仍未得到充分探索。符合预测（CP）在交换性假设下提供具有可靠覆盖率保证的不确定性估计，该假设要求联合数据分布在置换下保持不变。然而，在图结构时间序列中，固有的跨节点耦合可能会违反交换性条件，使得直接应用 CP 变得不可靠。受谱图理论启发，这种耦合存在于全局趋势中，并可以通过低频分量来表征，而高频分量则几乎是可交换的。因此，我们提出了一个名为谱图条件交换性（SGCE）的新概念，它将可交换的高频分量以低频分量为条件，以保留全局趋势并在谱域中实现有效的 CP。基于 SGCE，我们进一步提出了通过小波变换实现的谱符合预测（SCALE）。SCALE 使用图小波分解低频/高频分量，并通过对低频嵌入进行自适应门控，使高频残差符合化。在真实交通数据集上的实验结果表明，SCALE 不仅实现了有效的覆盖率，而且在覆盖率与效率的权衡上始终优于最先进的 CP 方法。

## Abstract
Point forecasting for graph-structured multivariate time series is a fundamental problem, but rigorous uncertainty quantification for such predictions is still underexplored. Conformal prediction (CP) offers uncertainty estimation with a solid coverage guarantee under the exchangeability assumption, which requires the joint data distribution to be unchanged under permutation. However, in graph-structured time series, inherent cross-node coupling can violate the exchangeability condition, making direct application of CP unreliable. Inspired by the spectral graph theory, such coupling resides in global trends and can be characterized by the low-frequency components, while high-frequency components are nearly exchangeable. Therefore, we propose a novel concept named Spectral Graph Conditional Exchangeability (SGCE), which conditions exchangeable high-frequency components on low-frequency ones to preserve global trends and enable effective CP in the spectral domain. Based on SGCE, we further propose Spectral Conformal prediction via wAveLEt transform (SCALE). SCALE uses graph wavelets to decompose low/high-frequency components and conformalizes high-frequency residuals via adaptive gating over a low-frequency embedding. Experimental results on real-world traffic datasets show that SCALE not only achieves valid coverage but also consistently improves the coverage-efficiency trade-off over the state-of-the-art CP methods.

---

## 论文详细总结（自动生成）

以下是对论文《Delving into Non-Exchangeability for Conformal Prediction in Graph-Structured Multivariate Time Series》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在具有复杂空间耦合和时间依赖的图结构多元时间序列（MTS）中，实现严谨且高效的不确定性量化（UQ）。
*   **研究动机**：
    *   **点预测的局限性**：现有的时空图神经网络（STGNNs）虽能提供准确的点预测，但在高风险场景下缺乏可靠的置信区间。
    *   **交换性假设失效**：符合预测（CP）通常依赖“交换性”假设，即数据分布在置换下保持不变。然而，图结构数据中节点间的强耦合（跨节点交互）严重破坏了这一假设，导致传统 CP 方法生成的预测区间不可靠或过于保守。
    *   **谱域洞察**：作者发现，导致非交换性的主要因素是全局趋势（体现为图谱中的低频成分），而高频成分（局部变化）在给定低频条件时更接近交换性。

### 2. 论文提出的方法论
*   **核心思想**：提出**谱图条件交换性（SGCE）**概念。即：高频残差分量在以低频趋势分量为条件时，满足近似交换性。基于此，开发了 **SCALE** 框架。
*   **关键技术细节**：
    1.  **图小波变换（SGWT）**：利用谱图理论将预测残差分解为低频成分（$\hat{L}_t$，捕捉全局趋势）和多尺度高频成分（$\hat{H}_t$，捕捉局部波动）。
    2.  **双通道处理架构**：
        *   **低频通道**：使用可学习的编码器（如 MLP + 节点/时间嵌入）提取时空趋势的条件嵌入 $C_t$。
        *   **高频通道**：使用无参数的统计提取器计算节点内的标准差（STD）和均方根（RMS），避免引入额外的空间耦合。
    3.  **自适应门控融合**：通过低频嵌入生成的门控机制动态调节高频特征，预测条件分位数。
    4.  **损失函数**：采用 Pinball Loss（分位数回归损失）进行端到端训练，以估计特定失盖率 $\alpha$ 下的残差区间。

### 3. 实验设计
*   **数据集**：四个真实世界交通流量基准数据集：**METR-LA**、**PEMS04**、**PEMS07** 和 **PEMS08**。
*   **Benchmark（对比方法）**：
    *   **基础 CP**：SCP（标准拆分符合预测）。
    *   **时序 CP**：SeqCP、NexCP、EnbPI（针对时间序列改进的方法）。
    *   **图/相关性 CP**：**CoREL**（目前最先进的关联符合预测方法）、HopCPT、ConForME。
*   **评估指标**：经验覆盖率（Coverage）、预测区间宽度（PI-Width，越窄越高效）、Winkler 分数（综合衡量覆盖率与宽度）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 PyTorch、PyTorch Lightning 和 PyTorch Geometric 等框架。实验设置中提到 Batch Size 为 32 或 128，训练轮数为 30 到 200 轮不等。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个数据集上针对 3 个不同的失盖率（$\alpha=0.05, 0.1, 0.2$）进行了全面测试。
    *   **消融实验**：验证了“无低频条件（w/o LF）”和“无图小波分解（w/o SGWT）”的情况。
    *   **鲁棒性测试**：更换了不同的点预测骨干模型（Transformer, GWNet, DCRNN），证明方法具有模型无关性。
    *   **参数敏感性**：分析了小波尺度 $S$ 对结果的影响。
*   **充分性评价**：实验设计非常充分且客观。通过多随机种子运行（5次）并报告均值与标准差，确保了结果的公平性。

### 6. 论文的主要结论与发现
*   **有效性**：SCALE 在所有数据集上均能保持接近名义水平的覆盖率，证明了 SGCE 理论的正确性。
*   **高效性**：相比于最先进的 CoREL，SCALE 生成的预测区间更窄（例如在 METR-LA 上宽度减少约 14.4%），显著提升了不确定性估计的效率。
*   **谱域优势**：实验证实，在谱域进行高低频解耦比直接在时域建模空间相关性更能有效恢复交换性。

### 7. 优点（亮点）
*   **理论创新**：首次将谱图理论与符合预测的交换性问题结合，提出了 SGCE 这一新颖的理论框架。
*   **即插即用**：作为一种后处理（Post-hoc）方法，SCALE 可以适配任何现有的 STGNN 预测模型。
*   **效率提升**：通过自适应门控机制，在保证覆盖率的前提下显著压缩了预测区间的宽度，具有极高的实用价值。

### 8. 不足与局限
*   **图结构依赖**：方法的性能高度依赖于预定义的图邻接矩阵。如果图结构存在噪声或错误，谱分解的效果可能会受到影响。
*   **计算复杂度**：虽然使用了轻量级模块，但图小波变换（涉及特征值分解或多项式近似）在大规模图上的计算开销仍需关注。
*   **超参数选择**：小波基函数的选择和截止频率（Cutoff）的确定需要一定的先验知识或额外的自动选择算法（如附录所述）。

（完）
