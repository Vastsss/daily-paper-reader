---
title: "ORPilot: A Production-Oriented Agentic LLM-for-OR Tool for Optimization Modeling"
title_zh: ORPilot：一种面向生产的、用于优化建模的智能体化运筹学大语言模型工具
authors: Guangrui Xie
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02728v1"
tags: ["query:llm"]
score: 8.0
evidence: 用于优化建模和决策支持的智能体 LLM
tldr: ORPilot是一个面向生产环境的开源智能体系统，旨在将复杂的业务问题转化为可求解的优化模型。针对实际场景中描述模糊、数据原始及求解器兼容性等挑战，它引入了对话访谈、独立数据采集、参数计算及求解器无关的中间表示（IR）等核心组件。该系统不仅支持多种主流求解器，还具备自我修复能力，在IndustryOR等基准测试中表现优异，是首个针对生产级业务问题的运筹优化工具。
selection_source: fresh_fetch
motivation: 现有的学术型LLM-for-OR工具通常假设问题描述清晰且数据已格式化，难以应对现实生产中模糊的需求和大规模原始数据。
method: 提出了包含对话访谈、数据采集、参数计算及求解器无关中间表示（IR）的智能体架构，并结合求解器回溯实现自我修复。
result: 在IndustryOR基准测试中准确率超越了现有最先进工具，并在NL4OPT和NLP4LP等学术基准上保持了相当的性能。
conclusion: ORPilot填补了学术研究与生产实践之间的空白，为自动化构建生产级运筹优化模型提供了高效且通用的解决方案。
---

## 摘要
本文介绍了 ORPilot，这是一个开源的智能体 AI 系统，旨在将现实世界的业务问题转化为可供求解器使用的优化模型。与假设具有清晰问题规范和预格式化内联数据的学术型运筹学大语言模型（LLM-for-OR）工具不同，ORPilot 专为生产环境设计，能够处理模糊的描述、大规模原始运营数据以及跨求解器后端的移植需求。该系统引入了四个创新组件：(1) 对话式访谈智能体，用于引导出完整的问题规范；(2) 数据收集智能体，独立于提示词检索数据；(3) 参数计算智能体，用于连接原始表格数据与模型就绪参数；(4) 求解器无关的中间表示（IR），用于在无需调用大语言模型的情况下，确定性地重新编译为 Gurobi、CPLEX、PuLP、Pyomo 或 OR-Tools 求解器代码。此外，自修复重试循环利用求解器的回溯信息进行针对性修复。ORPilot 代表了针对生产级业务问题而非教科书式运筹学（OR）案例的首次尝试。对现实问题的评估展示了令人鼓舞的结果。在针对传统学术基准测试（IndustryOR、NL4OPT 和 NLP4LP）的测试中，ORPilot 在 IndustryOR 基准上的准确率超过了现有最先进工具，并在 NL4OPT 和 NLP4LP 上表现相当。

## Abstract
This paper presents ORPilot, an open-source agentic AI system that translates real-world business problems into solver-ready optimization models. Unlike academic LLM-for-OR tools that assume clean problem specifications with preformatted inline data, ORPilot is designed for production conditions: ambiguous descriptions, large-scale raw operational data, and the need for portability across solver backends. The system introduces four novel components: (1) a conversational interview agent to elicit complete problem specifications, (2) a data collection agent that retrieves data independently of prompts, (3) a parameter computation agent to bridge raw tabular data and model-ready parameters, and (4) a solver-agnostic Intermediate Representation (IR) for deterministic, zero-LLM-call recompilation to Gurobi, CPLEX, PuLP, Pyomo, or OR-Tools solvers. Additionally, self-correcting retry loops utilize solver tracebacks for targeted repairs. ORPilot represents the first attempt to target production-level business problems rather than textbook operations research (OR) cases. Evaluation on real-world problems demonstrates promising results. When tested against traditional academic benchmarks: IndustryOR, NL4OPT and NLP4LP, ORPilot outperformed state-of-the-art tools in accuracy on the IndustryOR benchmark and delivered comparable performance on NL4OPT and NLP4LP.

---

## 论文详细总结（自动生成）

### ORPilot：面向生产的运筹优化建模智能体工具论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
运筹学（OR）在物流、供应链和金融等领域具有极高的应用价值，但从自然语言业务需求到数学优化模型的转化存在极高门槛。现有的 LLM-for-OR 工具（如 OptiMUS, OR-LLM）大多处于学术研究阶段，存在以下局限：
*   **假设过于理想化**：假设问题描述完整且无歧义。
*   **数据处理能力弱**：通常要求数据直接嵌入 Prompt 中，无法处理大规模原始运营数据（如数万行的 CSV 文件）。
*   **缺乏可移植性**：生成的代码通常绑定特定求解器，难以在不同后端（如 Gurobi, CPLEX）间切换。
*   **缺乏复用性**：每次修改数据或更换求解器往往需要重新调用昂贵的 LLM。

**ORPilot** 的动机是填补学术工具与生产环境之间的鸿沟，设计一个能够处理模糊描述、大规模原始数据并具备确定性重编译能力的生产级智能体系统。

#### 2. 论文提出的方法论
ORPilot 基于 **LangGraph** 状态机构建，核心思想是将建模过程拆解为多个专门的智能体节点，通过结构化的中间表示（IR）解耦逻辑与实现。

*   **核心组件与流程**：
    1.  **访谈智能体 (Interview Agent)**：通过多轮对话引导用户补充缺失信息（如目标函数、约束条件、索引维度），直到形成完整规范。
    2.  **数据采集智能体 (Data Collection Agent)**：根据访谈结果定义数据 Schema，指导用户准备 CSV 文件，并验证数据的完整性。
    3.  **参数计算智能体 (Parameter Computation Agent)**：这是本文的一大创新。它编写并执行 Python 脚本，从原始数据中提取派生参数（如计算距离矩阵、聚合需求、计算 Big-M 值），确保后续建模步骤获得的是“模型就绪”的数据。
    4.  **代码生成与自修复循环 (Code Generation & Retry Loop)**：生成特定求解器的代码。若执行报错或结果不可行，系统会将 Traceback 反馈给 LLM 进行自动修复。
    5.  **中间表示 (IR)**：定义了一种求解器无关的 JSON Schema。它捕获了集合、参数、变量、目标和约束的逻辑结构。
    6.  **IR 编译器 (Deterministic IR Compiler)**：一个确定性的程序，无需 LLM 即可将 IR 编译为 Gurobi、CPLEX、PuLP、Pyomo 或 OR-Tools 代码。

#### 3. 实验设计
论文采用了双重评估策略：
*   **现实世界大规模场景**：
    *   **多周期多产品供应链优化 (LP)**：涉及 50 个生产点、50 个配送中心、500 个客户、500 种产品、12 个周期，变量数近 1000 万。
    *   **带二元决策的供应链优化 (MIP)**：引入设施开启/关闭决策。
    *   **大规模货运分配问题 (IP)**：涉及 3200 个货件和 400 个承运商。
*   **学术基准测试 (Benchmarks)**：
    *   **IndustryOR**：最具工业代表性的基准。
    *   **NL4OPT**：教科书式的线性规划问题。
    *   **NLP4LP**：包含结构化 JSON 数据的复杂 LP 问题。
*   **对比方法**：与 LLMOPT、ORLM、OptiMUS-0.3 等当前最先进（SOTA）工具进行对比。

#### 4. 资源与算力
*   **模型使用**：实验主要使用了 GPT-4o、Claude 3.5 Sonnet（文中称为 4.6，可能指代未来版本或笔误）、DeepSeek-Chat 和 DeepSeek-Reasoner。
*   **算力说明**：论文**未明确说明**具体的 GPU 训练算力，因为 ORPilot 主要是基于 API 的推理系统，而非从头训练模型。代码执行在安全的沙箱环境中完成。

#### 5. 实验数量与充分性
*   **实验规模**：论文详细展示了 3 组大规模工业级案例的全过程（包括对话记录、生成的代码和 IR），并在 3 个学术数据集上进行了定量分析。
*   **充分性评价**：实验设计较为充分。它不仅证明了系统处理千万级变量问题的能力，还通过学术基准测试证明了其在通用任务上的竞争力。对比实验涵盖了 2024-2025 年间的主流工具，具有较强的客观性和公平性。

#### 6. 主要结论与发现
*   **工业场景优势**：在 IndustryOR 基准上，ORPilot 配合 Claude-Sonnet 达到了 75% 的准确率，远超 OptiMUS-0.3 (37%)。
*   **IR 的价值**：通过 IR 编译器，系统实现了“零 LLM 调用”的后端切换和 What-if 分析，显著降低了生产环境下的 API 成本。
*   **参数计算的重要性**：证明了在原始数据和数学模型之间建立“参数计算层”是处理现实问题的关键。
*   **模型性能**：DeepSeek-Reasoner 在逻辑推理上表现优异，但延迟较高；Claude-Sonnet 在速度与准确率平衡上表现最佳。

#### 7. 优点（亮点）
*   **生产导向**：首个明确针对模糊需求和大规模原始 CSV 数据设计的工具。
*   **解耦设计**：引入 IR 使得模型逻辑与求解器语法分离，增强了系统的鲁棒性和可移植性。
*   **全流程自动化**：从最初的“聊天”到最终的“业务报告”，覆盖了 OR 咨询的完整生命周期。
*   **自修复能力**：利用求解器反馈闭环修复代码错误，提高了复杂问题的成功率。

#### 8. 不足与局限
*   **语义验证缺失**：目前的重试循环仅在代码报错或不可行时触发。如果模型逻辑错误（如漏掉约束）但依然能算出“最优解”，系统无法自动识别。
*   **不可行性分析不足**：当问题本身由于数据原因不可行时，系统可能会陷入无效的重试循环，而非直接告知用户矛盾点。
*   **依赖前沿模型**：系统性能高度依赖于底层 LLM 的推理能力，在较弱的模型上表现可能大幅下降。
*   **数据隐私**：虽然数据不直接进入 Prompt，但 Schema 和计算逻辑仍需通过 API，对极度敏感的行业可能存在合规风险。

（完）
