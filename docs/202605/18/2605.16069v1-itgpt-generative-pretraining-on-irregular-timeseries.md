---
title: "ITGPT: Generative Pretraining on Irregular Timeseries"
title_zh: ITGPT：不规则时间序列上的生成式预训练
authors: "Antoine Honoré, Ming Xiao"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.16069v1"
tags: ["query:llm-ts"]
score: 10.0
evidence: 使用基于Transformer的LLM框架对不规则时间序列进行生成式预训练
tldr: ITGPT是一个针对不规则采样多模态时间序列设计的生成式预训练模型。针对医疗和预测性维护中数据不规则、缺失及标注成本高的问题，该模型结合了自监督学习（SSL）和类GPT的生成式目标，无需重采样或插值即可处理复杂数据。实验证明其在TIHM和CompX数据集上达到SOTA，且在少样本场景下表现优异，为非结构化时间序列分析提供了新方案。
selection_source: fresh_fetch
motivation: 传统模型难以高效处理不规则采样、缺失值及多模态数据，且在标注数据稀缺时难以利用大规模无标注信息。
method: 提出一种基于注意力机制的ITGPT架构，通过自监督学习和类GPT的生成式预训练目标，直接处理原始不规则时间序列。
result: 在医疗和预测性维护任务中，ITGPT无需重采样或数据插补即可达到SOTA性能，并在少样本情况下显著优于纯监督学习。
conclusion: ITGPT证明了生成式预训练在处理大规模非结构化时间序列数据中的有效性，是实现高效实际推理任务的重要进展。
---

## 摘要
时间序列回归模型通常难以利用大规模的有标签多模态数据，特别是在数据采样不规则或包含缺失值的情况下。这在医疗保健和预测性维护等领域非常常见，在这些领域中，数据是从不可靠的来源收集的，且标注需要专家知识或昂贵的设备。基于 Transformer 的大语言模型已证明通过自监督学习 (SSL) 和生成式预训练 (GPT) 框架在文本等结构化数据上是有效的。然而，此类模型缺乏高效处理不规则采样的多模态时间序列数据的灵活性。在本文中，我们介绍了 ITGPT，这是一种基于注意力的架构，旨在通过允许使用 SSL 损失和类 GPT 目标进行训练，来处理多模态、不规则采样的时间序列。我们在 TIHM 数据集的医疗保健任务和 CompX 数据集的预测性维护任务上评估了其性能。我们的结果表明，ITGPT 在不需要重采样、特征融合或显式数据插补的情况下，实现了最先进的性能。此外，当标签稀缺时，ITGPT 通过 SSL 和 GPT 训练有效地利用了无标签数据，优于纯监督方法。这代表了在实际推理任务中高效使用大规模非结构化时间序列数据集迈出的重要一步。

## Abstract
Timeseries regression models often struggle to leverage large volumes of labeled multimodal data, particularly when the data are irregularly sampled or contain missing values. This is common in domains like healthcare and predictive maintenance, where data are collected from unreliable sources, and labeling requires expert knowledge or costly equipments. Transformer-based large language models have proven effective on structured data such as text through self-supervised learning (SSL) and generative pretraining (GPT) frameworks. However, such models lack the flexibility to efficiently process irregularly sampled multimodal timeseries data. In this paper, we introduce ITGPT, an attention-based architecture designed for handling multimodal, irregularly sampled timeseries by allowing training with both SSL losses and GPT-like objectives. We evaluate its performance on a healthcare task with the TIHM dataset, and a predictive maintenance task with the CompX dataset. Our results demonstrate that ITGPT achieves state-of-the-art performance without requiring resampling, feature fusion or explicit data imputation. Furthermore, when labels are scarce, ITGPT effectively leverages unlabeled data through SSL and GPT training, outperforming the purely supervised approach. This represents an important step towards efficiently using large and unstructured timeseries datasets for practical inference tasks.

---

## 论文详细总结（自动生成）

### ITGPT：不规则时间序列上的生成式预训练论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何在不进行重采样、数据插补或特征融合的情况下，高效处理**采样频率不一致、存在缺失值且标注成本高昂**的多模态不规则时间序列数据。
*   **研究动机**：在医疗保健（如痴呆症监测）和工业维护（如卡车故障预测）领域，传感器数据通常是异步且不规则的。虽然 Transformer 和 GPT 框架在结构化文本数据上取得了巨大成功，但现有的时间序列模型在处理这种高度非结构化的多模态数据时缺乏灵活性，且难以利用大规模无标注数据进行预训练。

#### 2. 方法论
ITGPT 扩展了 ITNet 架构，通过编码器-解码器结构实现了类 GPT 的生成式预训练：
*   **核心思想**：引入“锚点（Anchor）”时间线的概念。编码器将不同频率的多模态输入同步到一个预定义的锚点时间线上；解码器则从锚点表示中重建原始各模态的数据。
*   **关键技术细节**：
    *   **因果交叉注意力（Causal Cross-Attention）**：确保模型在预测 $t$ 时刻时仅使用 $t$ 之前的历史信息。
    *   **位置编码（PE）**：使用正弦/余弦函数将标量时间戳投影到高维空间，捕捉时间间隔信息。
    *   **架构堆叠**：通过残差连接将多对编码器-解码器链式连接，形成深层模型。
    *   **训练目标**：支持**自监督学习（SSL）**（预测下一时刻的输入值，使用 MSE 损失）和**监督学习**（分类任务，使用交叉熵 CE 损失）。

#### 3. 实验设计
*   **数据集**：
    1.  **TIHM 数据集**：远程医疗监测数据，包含 56 名痴呆症患者的家庭活动（红外、门磁）和生理指标（体温、血压等），属于极稀疏的多模态数据。
    2.  **SCANIA CompX 数据集**：真实的重型卡车预测性维护数据集，包含 28,596 辆车的 15 种模态（直方图变量、数值计数器、车辆规格等），采样极不规则。
*   **对比方法与 Benchmark**：
    *   **纯监督学习（CE）**：仅使用有标签数据训练。
    *   **SSL + CE**：同时计算输入数据的预测损失和标签分类损失。
    *   **GPT → CE**：先进行生成式预训练，再在下游任务上微调。
    *   **基准线**：与 TIHM 原始论文中的线性插值+线性模型进行对比。

#### 4. 资源与算力
*   **算力资源**：论文提到计算得到了瑞典国家超级计算机中心（NSC）提供的 **Berzelius 资源**支持（该资源通常配备 NVIDIA A100/H100 GPU 集群）。
*   **具体参数**：使用了 ADAM 优化器，学习率为 $5 \times 10^{-4}$，Batch Size 为 64，训练轮数为 20 Epochs。
*   **明确说明**：文中**未明确列出**具体的 GPU 型号数量、单次实验的精确耗时或总算力消耗。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 5 折交叉验证以确保结果的统计显著性。
    *   **消融实验**：详细测试了模型深度（1 到 7 层）、Dropout 比例（0% 到 30%）、混合层类型（线性、1层 MLP、2层 MLP）。
    *   **少样本实验**：针对 CompX 数据集，测试了从 0.1% 到 1% 不同比例标签下的性能表现。
*   **充分性评价**：实验设计较为充分，涵盖了超参数敏感性分析和不同训练策略的对比，客观地展示了模型在极少标签情况下的优势。

#### 6. 主要结论与发现
*   **SOTA 性能**：ITGPT 在无需任何数据预处理（如插值）的情况下，在两个数据集上均达到了最先进的性能。
*   **预训练优势**：在标签稀缺（如仅 0.1% 标签）时，GPT 预训练和 SSL 模式显著优于纯监督学习。
*   **深度与正则化**：增加模型深度可以提升性能，但必须配合适当的 Dropout（如 10%-20%）以防止过拟合。
*   **混合层影响**：基于 MLP 的模态混合层优于简单的线性混合层。

#### 7. 优点
*   **端到端处理**：直接处理原始不规则时间序列，避免了重采样带来的信息损失或人工插补引入的偏差。
*   **灵活性**：架构支持多模态输入，且能通过生成式任务利用海量无标签数据。
*   **鲁棒性**：在医疗和工业两个截然不同的领域均验证了有效性。

#### 8. 不足与局限
*   **计算复杂度**：注意力机制的计算开销随序列长度增加，论文虽提到了未来优化方向（如稀疏注意力），但当前版本在大规模实时推理上可能存在压力。
*   **解释性**：作为深度学习模型，其内部决策过程（如各模态对最终预测的贡献度）尚不够直观。
*   **类别不平衡处理**：在 CompX 数据集中，对于某些模糊类别（如 Class 6）的处理仍依赖于手动剔除，缺乏更自动化的噪声标签处理机制。

（完）
