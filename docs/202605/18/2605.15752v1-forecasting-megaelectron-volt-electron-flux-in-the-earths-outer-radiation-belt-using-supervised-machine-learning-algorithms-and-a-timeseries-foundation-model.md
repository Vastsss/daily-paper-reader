---
title: "Forecasting megaelectron-volt electron flux in the Earth's outer radiation belt using supervised machine learning algorithms and a timeseries foundation model"
title_zh: 使用监督机器学习算法和时间序列基础模型预测地球外辐射带中的兆电子伏特电子通量
authors: "Rungployphan Kieokaew, Ryad Guezzi, François Ginisty, Hadrien Mariaccia"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15752v1"
tags: ["query:ts-fm"]
score: 9.0
evidence: 使用时序基础模型进行预测
tldr: 本研究旨在提高地球外辐射带1-MeV高能电子通量的预测精度，以降低卫星运行风险。研究利用2013-2023年的多源空间天气数据，训练了包括CNN、LSTM、Transformer在内的多种机器学习模型，并创新性地引入了时间序列基础模型TimesFM。通过将TimesFM与动态协变量回归结合（TimesFM+Cov），在6小时预报时效下实现了平均0.9的R2评分，显著优于传统模型，展示了预训练大模型在空间天气领域的应用潜力。
selection_source: fresh_fetch
motivation: 准确预测对卫星具有潜在威胁的地球外辐射带高能电子通量，是保障航天器安全运行的关键。
method: 开发了一套机器学习流水线，对比了传统监督学习算法与预训练时间序列基础模型TimesFM及其结合脊回归的混合应用效果。
result: "混合模型TimesFM+Cov在L-shell 2.8至6.0范围内表现最佳，平均R2达到0.9，相比次优模型在不同区域提升了12%至48%。"
conclusion: 研究证明了预训练基础模型在空间天气预报中的有效性，为利用先进AI技术提升辐射带环境监测提供了新范式。
---

## 摘要
准确预测地球外辐射带中的兆电子伏特（MeV）电子对于降低风险和航天器运行至关重要，因为这些电子可能对卫星构成重大威胁。我们开发了一个基于机器学习的流水线，用于预测 1-MeV 电子通量的变化，首先关注 6 小时的预测时效。利用 2013-2023 年间 POES NOAA-15 测量的沉降电子、GOES 测量的近 1-MeV 电子通量、L1 点附近的太阳风测量值以及地磁活动指数作为输入，我们训练了包括线性回归、一维卷积神经网络、长短期记忆网络（LSTM）和 Transformer-Encoder 在内的算法，以 0.1 的分辨率预测 McIlwain L-壳层在 2.8 到 6.0 之间的 1-MeV 电子通量。特别地，我们利用时间序列基础模型 TimesFM 进行了 (1) 零样本预测和 (2) 一种混合应用，即将过去动态协变量的岭回归与 TimesFM 对残差的推理相结合。使用 2024 年 1 月至 6 月的数据作为样本外测试，我们发现名为 TimesFM+Cov 的 TimesFM 混合应用取得了最佳结果，在各 L-壳层上的平均 R2 达到 0.9，而所有其他模型的平均 R2 均低于 0.78。TimesFM+Cov 的 R2 在 L-壳层 2.8 到 4.7 之间保持在 0.9 以上，在 L=6.0 时降至 0.77，与我们的次优模型相比，在最低 L-壳层提高了 12%，在最高 L-壳层提高了 48%。我们的工作为如何将预训练基础模型应用于空间天气预报提供了一个新的视角。

## Abstract
Accurate forecasting of megaelectron-volt (MeV) electrons in the outer Earth's radiation belt, which can pose significant risks to satellites, is essential for risk mitigation and spacecraft operations. We develop a machine-learning-based pipeline for forecasting 1-MeV electron flux variations, focusing first on a 6-hour forecast horizon. Using precipitating electrons measured by POES NOAA-15, near 1-MeV electron flux measured by GOES, solar wind measurements near L1, and geomagnetic activity indices as inputs in 2013-2023, we train algorithms including linear regression, 1-D convolutional and long short-term memory neural networks, and Transformer-Encoder to forecast 1-MeV electron flux in McIlwain's L-shells between 2.8 and 6.0 with 0.1 bin resolution. Particularly, we exploit the timeseries foundation model TimesFM for (1) a zero-shot prediction and (2) a hybrid application involving the ridge regression on the past dynamic covariates combined with the TimesFM inference on the residuals. Using data from January-June 2024 as an out-of-sample test, we find that the hybrid application of TimesFM, named TimesFM+Cov, yields the best results with an average R2 of 0.9 across L-shells, compared to an average R2 under 0.78 for all other models. The R2 of TimesFM+Cov remains above 0.9 for L-shells between 2.8 and 4.7 and drops to 0.77 at L=6.0, indicating improvements of 12% at the lowest L-shell and 48% at the highest L-shell compared to our second-best models. Our work offers an alternative perspective on how a pretrained foundation model could be adapted for space weather forecasting.

---

## 论文详细总结（自动生成）

这篇论文探讨了利用先进的机器学习技术，特别是时间序列基础模型（TimesFM），来预测地球外辐射带中具有破坏性的高能电子通量。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：地球外辐射带中的兆电子伏特（MeV）高能电子（又称“杀手电子”）能穿透卫星屏蔽层，引起深层介质放电，导致卫星异常或失效。
*   **核心问题**：现有的物理模型或传统统计模型在预报精度、实时性以及长期验证方面存在局限。随着专用科学卫星（如范艾伦探测器）的退役，如何利用长期运行的业务卫星（如POES、GOES）数据构建一个高精度、可连续验证的预报流水线成为迫切需求。
*   **研究意义**：通过引入预训练的时间序列基础模型，探索AI大模型在空间天气预报这一特定物理领域的迁移能力和性能上限。

### 2. 方法论
*   **核心思想**：对比研究了传统监督学习算法与Google开发的预训练时间序列基础模型 **TimesFM** 的表现，并提出了一种结合物理协变量的混合预测架构。
*   **关键技术细节**：
    *   **传统模型**：实现了线性回归（LR）、一维卷积神经网络（Conv1D）、长短期记忆网络（LSTM）和Transformer编码器（TransEncode）。
    *   **TimesFM Zero-shot**：直接利用预训练模型对目标电子通量进行单变量自回归预测。
    *   **TimesFM+Cov（混合模型）**：这是本文的核心创新点。
        1.  **脊回归（Ridge Regression）**：首先利用过去一段时间的动态协变量（太阳风参数、地磁指数等）对目标值进行线性拟合。
        2.  **残差预测**：计算观测值与回归预测值之间的残差，然后利用TimesFM对该残差进行非线性推理预测。
        3.  **结果合成**：最终预报值为回归预测值与TimesFM残差预测值的总和。
*   **输入特征**：包括POES卫星的沉降电子通量、GOES卫星的近1-MeV电子通量、L1点的太阳风速度/密度/动压、行星际磁场Bz分量，以及Dst和Kp地磁指数。

### 3. 实验设计
*   **数据集**：
    *   **训练/验证集**：2013年至2023年的多源空间天气数据（涵盖一个完整的太阳活动周期）。
    *   **测试集**：2024年1月至6月的样本外数据，特别包含了2024年5月发生的极端地磁暴事件。
*   **实验场景**：针对McIlwain L-shell（L壳层）2.8至6.0范围，以0.1为分辨率构建了33个独立预测模型。
*   **Benchmark（基准）**：
    *   **持久性基准（Persistence Baseline）**：假设未来状态与当前状态一致。
    *   **对比方法**：上述提到的LR、Conv1D、LSTM、TransEncode以及TimesFM的零样本版本。

### 4. 资源与算力
*   **算力设施**：论文提到使用了法国国家信息与自动化研究所（INRIA）巴黎中心的 **CLEPS 基础设施** 进行计算。
*   **软件框架**：使用了 SunPy（空间物理处理）、PyTorch 2.3.1（深度学习训练）以及 Google 的 TimesFM 2.0 权重。
*   **明确说明**：文中**未明确列出**具体的 GPU 型号、数量或具体的训练总时长，但指出 TimesFM+Cov 模式相比传统模型在数据需求上更少（仅需6个月数据进行回归微调，而非11年）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对33个L壳层分别建模。
    *   测试了 6h、12h、24h、48h、72h 等多个预报时效。
    *   对比了 6h、12h、24h 三种不同的数据时间分辨率。
*   **充分性评价**：实验设计较为充分且客观。研究采用了“走步验证”（Walk-forward Validation）法，有效处理了时序数据的非平稳性；测试集包含了极端风暴事件，验证了模型在极端情况下的鲁棒性。

### 6. 主要结论与发现
*   **TimesFM+Cov 表现卓越**：在6小时预报时效下，平均 $R^2$ 达到 **0.9**，显著优于所有传统模型（平均 $R^2 < 0.78$）。
*   **区域性能提升**：在内侧 L-shell（2.8）提升了 12%，而在最具挑战性的外侧 L-shell（6.0）提升幅度高达 **48%**。
*   **零样本局限性**：TimesFM 的纯零样本预测效果甚至不如持久性基准，证明了在物理领域结合动态协变量（Covariates）的必要性。
*   **预报时效影响**：随着预报时效延长（如超过24小时），所有模型性能均下降，但 TimesFM+Cov 在 24h 分辨率下仍具有一定的参考价值。

### 7. 优点
*   **范式创新**：首次将预训练的时间序列基础模型引入辐射带电子预报，证明了大模型在空间物理领域的潜力。
*   **高效微调**：展示了混合模型在少量数据（6个月）下即可通过残差修正超越长期训练（11年）的传统模型。
*   **物理结合**：通过脊回归处理线性物理关联，TimesFM处理非线性残差，这种“线性+非线性”的解耦设计非常符合物理系统的特性。

### 8. 不足与局限
*   **预测延迟**：在磁暴发生初期，模型预测的通量上升往往比实际观测滞后（约数小时至一天），这对于极早期预警是不利的。
*   **峰值低估**：在极端事件中，模型倾向于低估电子通量的最高峰值（约 10-20% 的对数误差）。
*   **长时预报瓶颈**：对于 3 天以上的长时预报，模型表现衰减剧烈，尚未达到完美的业务化水平。
*   **数据依赖**：模型高度依赖 OMNI 数据的完整性，而实时业务中获取高质量、无缺失的太阳风数据仍具挑战。

（完）
