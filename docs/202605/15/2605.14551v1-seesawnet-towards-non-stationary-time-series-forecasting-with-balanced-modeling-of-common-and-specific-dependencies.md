---
title: "SeesawNet: Towards Non-stationary Time Series Forecasting with Balanced Modeling of Common and Specific Dependencies"
title_zh: SeesawNet：通过平衡建模通用与特定依赖关系实现非平稳时间序列预测
authors: "Hao Li, Lu Zhang, Liu Chong, Yankai Chen, Pengyang Wang, Yingjie Zhou"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14551v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 非平稳时间序列预测架构
tldr: 针对非平稳多元时间序列预测中实例归一化（IN）容易过度平滑实例特定结构信息的问题，本文提出SeesawNet。该模型通过自适应平稳-非平稳注意力机制（ASNA），在时间和通道维度上动态平衡通用依赖与实例特定依赖的建模。SeesawNet能够根据实例的非平稳特性自适应融合归一化序列和原始序列的信息，在多个真实数据集上达到了SOTA性能。
selection_source: fresh_fetch
motivation: 现有的实例归一化方法在减少分布偏移的同时，往往会抹除对建模异构性至关重要的实例特定结构信息。
method: 提出SeesawNet架构，核心是ASNA机制，它能同时从归一化和原始序列中提取依赖，并根据非平稳程度进行自适应融合。
result: 在多个真实世界的基准测试中，SeesawNet的一致性表现优于现有的最先进方法。
conclusion: 通过动态平衡通用与特定依赖的建模，SeesawNet有效解决了非平稳时间序列预测中的分布偏移与信息丢失之间的矛盾。
---

## 摘要
实例归一化（IN）被广泛应用于非平稳多变量时间序列预测中，以减少分布偏移并突出样本间的通用模式。然而，IN 可能会过度平滑实例特定的结构信息，而这些信息对于建模时间与跨通道的异质性至关重要。尽管先前的方法进一步抑制了分布差异或尝试恢复时间特定的依赖关系，但它们往往忽略了一个核心矛盾：如何根据每个实例的非平稳结构自适应地建模通用和实例特定的依赖关系。为了解决这一困境，我们提出了 SeesawNet，这是一种在时间与通道维度上动态平衡通用和实例特定依赖建模的统一架构。其核心是自适应平稳-非平稳注意力（ASNA），它从归一化序列中捕获通用依赖，从原始序列中捕获特定依赖，并根据实例级的非平稳性对它们进行自适应融合。基于 ASNA，SeesawNet 交替进行专门的时间和通道关系建模，以共同捕获远程和跨变量依赖。在多个真实世界基准测试上的广泛实验表明，SeesawNet 始终优于现有最先进的方法。

## Abstract
Instance normalization (IN) is widely used in non-stationary multivariate time series forecasting to reduce distribution shifts and highlight common patterns across samples. However, IN can over-smooth instance-specific structural information that is essential for modeling temporal and cross-channel heterogeneity. While prior methods further suppress distribution discrepancies or attempt to recover temporal specific dependencies, they often ignore a central tension: how to adaptively model common and instance-specific dependency based on each instance's non-stationary structures. To address this dilemma, we propose SeesawNet, a unified architecture that dynamically balances common and instance-specific dependency modeling in both temporal and channel dimensions. At its core is Adaptive Stationary-Nonstationary Attention (ASNA), which captures common dependencies from normalized sequences and specific dependencies from raw sequences, and adaptively fuses them according to instance-level non-stationarity. Built upon ASNA, SeesawNet alternates dedicated temporal and channel relationship modeling to jointly capture long-range and cross-variable dependencies. Extensive experiments on multiple real-world benchmarks demonstrate that SeesawNet consistently outperforms state-of-the-art methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SeesawNet** 的新型深度学习架构，旨在解决非平稳多变量时间序列预测中的核心矛盾。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在非平稳时间序列预测中，**实例归一化（Instance Normalization, IN）** 虽然能有效缓解分布偏移（Distribution Shift）并提取样本间的通用模式（Common Patterns），但会产生“过度平滑”效应，抹除对预测至关重要的**实例特定结构信息**（如特定趋势、局部波动及复杂的跨通道异质性）。
*   **研究动机**：现有的方法要么侧重于进一步消除非平稳性，要么尝试在归一化后恢复时间依赖，但缺乏一种能够根据每个实例的非平稳程度，在**时间**和**通道**两个维度上动态平衡“通用依赖”与“特定依赖”的统一机制。

### 2. 方法论
SeesawNet 的核心思想是通过双路径处理和自适应融合机制来维持平稳性与结构完整性之间的平衡。
*   **核心组件：自适应平稳-非平稳注意力（ASNA）**：
    *   **双分支注意力**：平稳分支（Stationary Attention）从归一化序列中提取跨样本的可迁移通用模式；非平稳分支（Non-stationary Attention）从原始序列中提取实例特定的结构线索。
    *   **自适应门控机制**：利用 Sigmoid 门控函数根据实例特征动态生成权重，决定两个分支信息的融合比例。
*   **整体架构流程**：
    1.  **双路径输入**：同时输入归一化序列（$X_{sta}$）和原始序列（$X_{non}$）。
    2.  **分块与嵌入（Patching & Embedding）**：将序列切分为 Patch 并映射到高维空间。
    3.  **时间补丁依赖学习层**：应用 ASNA 建模每个通道内部的时间补丁间依赖。
    4.  **通道关系学习层**：在时间下采样后，应用 ASNA 建模不同变量（通道）之间的相互关系。
    5.  **预测与反归一化**：通过线性层输出预测值，并利用输入实例的统计量进行反归一化还原量纲。

### 3. 实验设计
*   **数据集**：涵盖 9 个真实世界基准数据集，包括 ETT (ETTh1/2, ETTm1/2)、Exchange Rate、Weather、ILI、Solar-energy 和 ECL。
*   **对比方法（Baselines）**：
    *   **归一化改进类**：DDN, SAN。
    *   **归一化后依赖恢复类**：U-Mixer, NS-Transformer, TimeBridge。
    *   **强力预测模型**：PDF, iTransformer, PatchTST。
*   **评估指标**：均方误差（MSE）和平均绝对误差（MAE）。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA RTX 4090 GPU (24GB)**。
*   **软件实现**：基于 PyTorch，使用 Adam 优化器。
*   **效率表现**：文中给出了 GFLOPs 和推理延迟数据（如在 ETTm2 上为 0.16 GFLOPs，延迟 1.50ms），表明该模型在引入双分支后仍保持了与主流模型相当的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：在 9 个数据集、4 种预测长度（共 72 种设置）下进行了详尽测试。
*   **消融实验**：针对 ASNA 的平稳/非平稳分支、门控机制、时间/通道建模顺序以及损失函数（Fredf vs MSE）进行了多组对比。
*   **泛化性测试**：将 ASNA 模块插入到 PatchTST 和 iTransformer 中，验证了其作为通用插件的有效性。
*   **客观性**：实验遵循了学术界通用的数据集划分和超参数搜索标准，并进行了多次运行取平均值，结果具有较高的可信度和公平性。

### 6. 主要结论与发现
*   **性能领先**：SeesawNet 在 72 项设置中的 51 项达到了最优（SOTA），相比强力基准模型（如 iTransformer, PatchTST）平均降低了约 7.2%~10.2% 的误差。
*   **平衡的重要性**：消融实验证明，单纯依赖归一化或原始数据均不如自适应融合效果好。
*   **ASNA 的通用性**：ASNA 能够显著提升现有 Transformer 类模型的预测精度，证明了其处理非平稳性的优越性。
*   **可视化验证**：注意力热图显示，模型能根据样本偏离分布中心的程度，自动调整对“通用模式”和“特定结构”的关注权重。

### 7. 优点
*   **架构优雅**：通过 ASNA 模块统一了时间和通道维度的非平稳性处理，逻辑自洽。
*   **自适应性强**：门控机制允许模型针对不同非平稳特性的样本“因材施教”，而非采用统一的归一化策略。
*   **即插即用**：ASNA 展现了良好的模块化特性，易于集成到其他 Transformer 架构中。

### 8. 不足与局限
*   **计算开销增加**：虽然文中强调效率可控，但双分支注意力在理论上比单分支增加了约一倍的注意力计算量和参数量。
*   **超参数敏感性**：尽管有门控机制，但 Patch 大小、下采样比例等超参数对不同数据集的敏感性可能仍需精细调优。
*   **极端非平稳性挑战**：对于分布发生剧烈突变（Concept Drift）的极端场景，仅靠实例内的统计量和原始序列融合是否足够仍有待进一步探讨。

（完）
