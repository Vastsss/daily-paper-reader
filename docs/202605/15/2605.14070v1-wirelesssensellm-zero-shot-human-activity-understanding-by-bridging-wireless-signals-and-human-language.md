---
title: "WirelessSenseLLM: Zero-Shot Human Activity Understanding by Bridging Wireless Signals and Human Language"
title_zh: WirelessSenseLLM：通过桥接无线信号与人类语言实现零样本人体活动理解
authors: "Mahmuda Keya, Sneh Pillai, Jiawei Yuan, Kai Zeng, Long Jiao"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.14070v1"
tags: ["query:llm-ts"]
score: 9.0
evidence: 利用LLM桥接时序无线信号与人类语言
tldr: WirelessSenseLLM是一个利用大语言模型（LLM）实现零样本人体动作理解的框架。它通过CSI-to-Language适配器和跨模态投影机制，将未分割的Wi-Fi信道状态信息（CSI）映射到语言语义空间。该方法解决了CSI与语言间的模态失配及动作重叠问题，实现了对连续、重叠动作的细粒度自然语言描述和推理，显著提升了无线感知的泛化能力和可解释性。
selection_source: fresh_fetch
motivation: 现有的无线感知系统过度依赖精确的信号分割和预定义标签，难以在零样本场景下处理未分割的连续动作。
method: 提出一种语言驱动框架，通过CSI-到-语言适配器和跨模态投影，将CSI特征映射至与语言对齐的语义空间。
result: "在零样本动作理解中达到92%的准确率，并在事实性、推理质量及多人物理动作解释方面均优于现有方法。"
conclusion: WirelessSenseLLM证明了结合大语言模型可以实现鲁棒且具有可解释性的零样本无线人体动作理解。
---

## 摘要
人们对使无线感知系统能够从非分割的无线信号中解释人体运动的兴趣日益浓厚；然而，现有的基于信道状态信息（CSI）的应用严重依赖于准确的信号分割和预定义的动作标签，限制了它们在零样本场景中的适用性。我们提出了 WirelessSenseLLM，这是一个语言驱动的框架，利用大语言模型（LLMs）实现从非分割的 Wi-Fi CSI 中进行零样本人体运动理解。为了弥合时间序列 CSI 与离散语言表示之间的模态差距，我们引入了一个 CSI-to-Language 适配器和一种跨模态投影机制，将 CSI 特征映射到与语言对齐的语义空间中。这种设计能够生成关于连续和重叠人体运动的细粒度自然语言描述，在无需分割训练数据的情况下支持下游推理。我们解决了两个核心技术挑战：CSI 特征与语言嵌入之间的模态不匹配，以及非分割 CSI 流中的重叠动作。大量实验证明了其在零样本动作理解（92% 的准确率和 91% 的 F1 分数）、基于语言的推理质量（事实性提升 30%，推理能力提升 15%）以及多人物运动解释（比先前方法平均提升 12.33%）方面的强劲性能。这些结果突显了 WirelessSenseLLM 在从 CSI 信号中进行鲁棒且可解释的人体运动理解方面的有效性。

## Abstract
There is growing interest in enabling wireless sensing systems to interpret human motion from unsegmented wireless signals; however, existing CSI-based applications rely heavily on accurate signal segmentation and predefined action labels, limiting their applicability in zero-shot scenarios. We present WirelessSenseLLM, a language-driven framework that leverages large language models (LLMs) to enable zero-shot human motion understanding from unsegmented Wi-Fi Channel State Information (CSI). To bridge the modality gap between time-series CSI and discrete language representations, we introduce a CSI-to-Language Adapter and a cross-modal projection mechanism that maps CSI features into a language-aligned semantic space. This design enables the generation of fine-grained natural language descriptions of sequential and overlapping human motions, supporting downstream reasoning without segmented training data. We address two core technical challenges: modality mismatch between CSI features and language embeddings, and overlapping actions in unsegmented CSI streams. Extensive experiments demonstrate strong performance in zero-shot action understanding (92% accuracy and 91% F1-score), language-based reasoning quality (30% factual and 15% reasoning improvements), and multi-person motion explanation with an average 12.33% improvement over prior methods. These results highlight WirelessSenseLLM's effectiveness for robust and interpretable human motion understanding from CSI signals.

---

## 论文详细总结（自动生成）

### 论文总结：WirelessSenseLLM - 通过桥接无线信号与人类语言实现零样本人体活动理解

#### 1. 核心问题与整体含义（研究动机和背景）
传统的基于 Wi-Fi 信道状态信息（CSI）的人体感知系统存在两个主要瓶颈：
*   **依赖精确分割与标签**：现有方法通常需要将连续信号精确分割为单个动作，并依赖预定义的类别标签（如“走路”、“坐下”），这在处理现实中连续、重叠且未标记的动作流时表现较差。
*   **缺乏零样本泛化能力**：传统模型难以识别训练集中未出现过的新动作。

**WirelessSenseLLM** 的研究动机在于利用大语言模型（LLM）强大的语义推理和跨域知识，将连续的 CSI 信号直接转化为自然语言描述。其核心目标是实现**无需信号分割**、**无需预定义标签**的零样本人体运动理解。

#### 2. 方法论
该框架通过两个阶段的训练流程，弥合了时序 CSI 信号与离散文本标记之间的模态鸿沟：

*   **核心架构**：包含 WiFi 编码器（提取时序特征）、CSI-to-Language 适配器（语义对齐）、跨模态投影层（映射至 LLM 空间）以及微调后的 LLM（Vicuna-7B v1.5）。
*   **关键技术细节**：
    *   **第一阶段：语义预对齐**。使用对比学习（Contrastive Learning）训练适配器。将 CSI 特征与对应的文本嵌入（Text Embedding）在共享语义空间中进行对齐，同时引入同步视频特征作为辅助监督。
    *   **第二阶段：跨模态投影与 LLM 微调**。将对齐后的 CSI 特征通过投影层转化为 LLM 可理解的 Token。使用 **LoRA（低秩自适应）** 技术对 LLM 进行参数高效微调，使其能够根据 CSI 输入生成细粒度的动作描述。
    *   **推理机制**：引入了**思维链（CoT）**和**上下文学习（ICL）**，引导 LLM 进行逻辑推理（例如：从“手臂举起”和“身体下蹲”推理出“正在做波比跳”）。

#### 3. 实验设计
*   **数据集**：
    *   **自建数据集**：基于 Person-in-WiFi 3D 采集的 CSI 数据，利用 LLaVA 和 GPT-4 生成初始标注，并经过人工审核，构建了包含 9.7 万条“无线-文本”对的数据集。
    *   **辅助数据**：引入了 MoVid 等视频-文本数据集（约 2.5 万对）进行语义增强。
*   **场景**：涵盖单人及多人（2-4 人）场景，涉及连续、重叠的复合动作。
*   **Benchmark 与对比方法**：
    *   **传统基准**：SVM、Vision CNN、Vision RNN（在分割后的数据上运行）。
    *   **多模态基准**：Video-LLaVA（作为跨模态推理能力的对比）。
*   **评价指标**：准确率、F1-score，以及自然语言生成指标（ROUGE-1/L, BLEU, METEOR, BERTScore），并引入 GPT-4o 作为客观评分员。

#### 4. 资源与算力
*   **模型规模**：使用了 Vicuna-7B v1.5 作为基础语言模型。
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）及具体的训练时长。但考虑到 7B 参数模型的 LoRA 微调和近 10 万条数据的处理，通常需要消费级或企业级 GPU（如 A100 或 RTX 4090）支持。

#### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了零样本动作识别、单人动作描述生成、多人（最多 4 人）复杂场景推理。
*   **充分性评价**：
    *   **较为充分**：通过消融实验验证了 ICL 和 CoT 对性能的提升；对比了传统机器学习与先进的多模态 LLM。
    *   **客观性**：使用了自动化指标与“LLM-as-a-judge”相结合的方法，减少了单一指标的偏差。
    *   **局限性**：实验环境相对固定，缺乏在不同物理环境（如不同房间布局）下的鲁棒性交叉验证。

#### 6. 主要结论与发现
*   **零样本能力强**：在完全未见的动作上，结合 CoT 推理的准确率达到了 **92%**，远超传统监督学习方法。
*   **语义推理优势**：相比于视频模型，WirelessSenseLLM 在事实准确性（提升 30%）和时序流描述（提升 15%）上表现更优，证明了 CSI 信号在捕捉动态运动方面的独特价值。
*   **多人场景挑战**：随着人数增加（从 2 人到 4 人），性能出现明显下降，但仍优于现有的基准模型。

#### 7. 优点
*   **摆脱分割依赖**：实现了对未分割连续信号的直接处理，更符合真实应用场景。
*   **高可解释性**：输出不再是冰冷的类别标签，而是详细的自然语言，能够解释“为什么”判定为某种动作。
*   **跨模态对齐创新**：提出的 CSI-to-Language 适配器有效地解决了无线信号与文本之间的模态失配问题。

#### 8. 不足与局限
*   **环境敏感性**：CSI 信号易受环境布局影响，论文未深入探讨模型在异构环境下的迁移能力。
*   **空间推理限制**：在精细的身体部位识别和复杂的空间关系描述上，性能仍有提升空间（受限于 CSI 信号的空间分辨率）。
*   **多人干扰**：在 4 人及以上的高密度场景下，信号叠加严重，导致推理准确度大幅衰减。
*   **实时性未知**：由于依赖大型语言模型，其在边缘设备上的实时推理延迟尚未得到验证。

（完）
